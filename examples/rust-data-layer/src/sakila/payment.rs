// Code generated by tpl from sakila.payment. DO NOT EDIT.

//! The `sakila.payment` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `sakila.payment`.
#[derive(Debug, Clone)]
pub struct Payment {
    /// `payment_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub payment_id: u16,
    /// `customer_id`, declared `smallint(5) unsigned` NOT NULL.
    pub customer_id: u16,
    /// `staff_id`, declared `tinyint(3) unsigned` NOT NULL.
    pub staff_id: u8,
    /// `rental_id`, declared `int(11)` NULL.
    pub rental_id: Option<i32>,
    /// `amount`, declared `decimal(5,2)` NOT NULL.
    pub amount: super::Decimal,
    /// `payment_date`, declared `datetime` NOT NULL.
    pub payment_date: sqlx::types::chrono::NaiveDateTime,
    /// `last_update`, declared `timestamp` NULL.
    pub last_update: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
}

impl Payment {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> PaymentKey {
        PaymentKey {
            payment_id: self.payment_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Payment {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            payment_id: row.try_get("payment_id")?,
            customer_id: row.try_get("customer_id")?,
            staff_id: row.try_get("staff_id")?,
            rental_id: row.try_get("rental_id")?,
            amount: row.try_get("amount")?,
            payment_date: row.try_get("payment_date")?,
            last_update: row.try_get("last_update")?,
        })
    }
}

/// The primary key of `sakila.payment`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct PaymentKey {
    /// `payment_id`, declared `smallint(5) unsigned`.
    pub payment_id: u16,
}

/// One column of `sakila.payment`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: PaymentColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum PaymentColumn {
    /// `payment_id`, declared `smallint(5) unsigned`.
    PaymentId,
    /// `customer_id`, declared `smallint(5) unsigned`.
    CustomerId,
    /// `staff_id`, declared `tinyint(3) unsigned`.
    StaffId,
    /// `rental_id`, declared `int(11)`.
    RentalId,
    /// `amount`, declared `decimal(5,2)`.
    Amount,
    /// `payment_date`, declared `datetime`.
    PaymentDate,
    /// `last_update`, declared `timestamp`.
    LastUpdate,
}

impl PaymentColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::PaymentId => "`payment_id`",
            Self::CustomerId => "`customer_id`",
            Self::StaffId => "`staff_id`",
            Self::RentalId => "`rental_id`",
            Self::Amount => "`amount`",
            Self::PaymentDate => "`payment_date`",
            Self::LastUpdate => "`last_update`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::PaymentId => "payment_id",
            Self::CustomerId => "customer_id",
            Self::StaffId => "staff_id",
            Self::RentalId => "rental_id",
            Self::Amount => "amount",
            Self::PaymentDate => "payment_date",
            Self::LastUpdate => "last_update",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::PaymentId,
            Self::CustomerId,
            Self::StaffId,
            Self::RentalId,
            Self::Amount,
            Self::PaymentDate,
            Self::LastUpdate,
        ]
    }
}

impl std::fmt::Display for PaymentColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for PaymentColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "payment_id" => Ok(Self::PaymentId),
            "customer_id" => Ok(Self::CustomerId),
            "staff_id" => Ok(Self::StaffId),
            "rental_id" => Ok(Self::RentalId),
            "amount" => Ok(Self::Amount),
            "payment_date" => Ok(Self::PaymentDate),
            "last_update" => Ok(Self::LastUpdate),
            _ => Err(super::Error::UnknownColumn {
                table: "sakila.payment",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `sakila.payment`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct PaymentOrder {
    /// The column to order by.
    pub column: PaymentColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `sakila.payment`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct PaymentFilter {
    /// Where set, requires `payment_id` to equal it.
    pub payment_id: Option<u16>,
    /// Where set, requires `payment_id` to be at least it.
    pub payment_id_from: Option<u16>,
    /// Where set, requires `payment_id` to be at most it.
    pub payment_id_to: Option<u16>,
    /// Where set, requires `customer_id` to equal it.
    pub customer_id: Option<u16>,
    /// Where set, requires `customer_id` to be at least it.
    pub customer_id_from: Option<u16>,
    /// Where set, requires `customer_id` to be at most it.
    pub customer_id_to: Option<u16>,
    /// Where set, requires `staff_id` to equal it.
    pub staff_id: Option<u8>,
    /// Where set, requires `staff_id` to be at least it.
    pub staff_id_from: Option<u8>,
    /// Where set, requires `staff_id` to be at most it.
    pub staff_id_to: Option<u8>,
    /// Where set, requires `rental_id` to equal it.
    pub rental_id: Option<i32>,
    /// Where set, requires `rental_id` to be at least it.
    pub rental_id_from: Option<i32>,
    /// Where set, requires `rental_id` to be at most it.
    pub rental_id_to: Option<i32>,
    /// Where set, requires `rental_id` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub rental_id_is_null: Option<bool>,
    /// Where set, requires `amount` to equal it.
    pub amount: Option<super::Decimal>,
    /// Where set, requires `amount` to be at least it.
    pub amount_from: Option<super::Decimal>,
    /// Where set, requires `amount` to be at most it.
    pub amount_to: Option<super::Decimal>,
    /// Where set, requires `payment_date` to equal it.
    pub payment_date: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `payment_date` to be at least it.
    pub payment_date_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `payment_date` to be at most it.
    pub payment_date_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `last_update` to equal it.
    pub last_update: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `last_update` to be at least it.
    pub last_update_from: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `last_update` to be at most it.
    pub last_update_to: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `last_update` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub last_update_is_null: Option<bool>,
}

impl PaymentFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.payment_id.is_none()
            && self.payment_id_from.is_none()
            && self.payment_id_to.is_none()
            && self.customer_id.is_none()
            && self.customer_id_from.is_none()
            && self.customer_id_to.is_none()
            && self.staff_id.is_none()
            && self.staff_id_from.is_none()
            && self.staff_id_to.is_none()
            && self.rental_id.is_none()
            && self.rental_id_from.is_none()
            && self.rental_id_to.is_none()
            && self.rental_id_is_null.is_none()
            && self.amount.is_none()
            && self.amount_from.is_none()
            && self.amount_to.is_none()
            && self.payment_date.is_none()
            && self.payment_date_from.is_none()
            && self.payment_date_to.is_none()
            && self.last_update.is_none()
            && self.last_update_from.is_none()
            && self.last_update_to.is_none()
            && self.last_update_is_null.is_none()
    }
}
