// Code generated by tpl from sakila.staff. DO NOT EDIT.

//! The `sakila.staff` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `sakila.staff`.
#[derive(Debug, Clone)]
pub struct Staff {
    /// `staff_id`, declared `tinyint(3) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub staff_id: u8,
    /// `first_name`, declared `varchar(45)` NOT NULL.
    pub first_name: String,
    /// `last_name`, declared `varchar(45)` NOT NULL.
    pub last_name: String,
    /// `address_id`, declared `smallint(5) unsigned` NOT NULL.
    pub address_id: u16,
    /// `picture`, declared `blob` NULL.
    pub picture: Option<Vec<u8>>,
    /// `email`, declared `varchar(50)` NULL.
    pub email: Option<String>,
    /// `store_id`, declared `tinyint(3) unsigned` NOT NULL.
    pub store_id: u8,
    /// `active`, declared `tinyint(1)` NOT NULL.
    pub active: i8,
    /// `username`, declared `varchar(16)` NOT NULL.
    pub username: String,
    /// `password`, declared `varchar(40)` NULL.
    pub password: Option<String>,
    /// `last_update`, declared `timestamp` NOT NULL.
    pub last_update: sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>,
}

impl Staff {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> StaffKey {
        StaffKey {
            staff_id: self.staff_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Staff {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            staff_id: row.try_get("staff_id")?,
            first_name: row.try_get("first_name")?,
            last_name: row.try_get("last_name")?,
            address_id: row.try_get("address_id")?,
            picture: row.try_get("picture")?,
            email: row.try_get("email")?,
            store_id: row.try_get("store_id")?,
            active: row.try_get("active")?,
            username: row.try_get("username")?,
            password: row.try_get("password")?,
            last_update: row.try_get("last_update")?,
        })
    }
}

/// The primary key of `sakila.staff`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct StaffKey {
    /// `staff_id`, declared `tinyint(3) unsigned`.
    pub staff_id: u8,
}

/// One column of `sakila.staff`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: StaffColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum StaffColumn {
    /// `staff_id`, declared `tinyint(3) unsigned`.
    StaffId,
    /// `first_name`, declared `varchar(45)`.
    FirstName,
    /// `last_name`, declared `varchar(45)`.
    LastName,
    /// `address_id`, declared `smallint(5) unsigned`.
    AddressId,
    /// `picture`, declared `blob`.
    Picture,
    /// `email`, declared `varchar(50)`.
    Email,
    /// `store_id`, declared `tinyint(3) unsigned`.
    StoreId,
    /// `active`, declared `tinyint(1)`.
    Active,
    /// `username`, declared `varchar(16)`.
    Username,
    /// `password`, declared `varchar(40)`.
    Password,
    /// `last_update`, declared `timestamp`.
    LastUpdate,
}

impl StaffColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::StaffId => "`staff_id`",
            Self::FirstName => "`first_name`",
            Self::LastName => "`last_name`",
            Self::AddressId => "`address_id`",
            Self::Picture => "`picture`",
            Self::Email => "`email`",
            Self::StoreId => "`store_id`",
            Self::Active => "`active`",
            Self::Username => "`username`",
            Self::Password => "`password`",
            Self::LastUpdate => "`last_update`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::StaffId => "staff_id",
            Self::FirstName => "first_name",
            Self::LastName => "last_name",
            Self::AddressId => "address_id",
            Self::Picture => "picture",
            Self::Email => "email",
            Self::StoreId => "store_id",
            Self::Active => "active",
            Self::Username => "username",
            Self::Password => "password",
            Self::LastUpdate => "last_update",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::StaffId,
            Self::FirstName,
            Self::LastName,
            Self::AddressId,
            Self::Picture,
            Self::Email,
            Self::StoreId,
            Self::Active,
            Self::Username,
            Self::Password,
            Self::LastUpdate,
        ]
    }
}

impl std::fmt::Display for StaffColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for StaffColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "staff_id" => Ok(Self::StaffId),
            "first_name" => Ok(Self::FirstName),
            "last_name" => Ok(Self::LastName),
            "address_id" => Ok(Self::AddressId),
            "picture" => Ok(Self::Picture),
            "email" => Ok(Self::Email),
            "store_id" => Ok(Self::StoreId),
            "active" => Ok(Self::Active),
            "username" => Ok(Self::Username),
            "password" => Ok(Self::Password),
            "last_update" => Ok(Self::LastUpdate),
            _ => Err(super::Error::UnknownColumn {
                table: "sakila.staff",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `sakila.staff`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct StaffOrder {
    /// The column to order by.
    pub column: StaffColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `sakila.staff`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct StaffFilter {
    /// Where set, requires `staff_id` to equal it.
    pub staff_id: Option<u8>,
    /// Where set, requires `staff_id` to be at least it.
    pub staff_id_from: Option<u8>,
    /// Where set, requires `staff_id` to be at most it.
    pub staff_id_to: Option<u8>,
    /// Where set, requires `first_name` to equal it.
    pub first_name: Option<String>,
    /// Where set, requires `first_name` to match it, as `LIKE` matches.
    pub first_name_like: Option<String>,
    /// Where set, requires `last_name` to equal it.
    pub last_name: Option<String>,
    /// Where set, requires `last_name` to match it, as `LIKE` matches.
    pub last_name_like: Option<String>,
    /// Where set, requires `address_id` to equal it.
    pub address_id: Option<u16>,
    /// Where set, requires `address_id` to be at least it.
    pub address_id_from: Option<u16>,
    /// Where set, requires `address_id` to be at most it.
    pub address_id_to: Option<u16>,
    /// Where set, requires `picture` to equal it.
    pub picture: Option<Vec<u8>>,
    /// Where set, requires `picture` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub picture_is_null: Option<bool>,
    /// Where set, requires `email` to equal it.
    pub email: Option<String>,
    /// Where set, requires `email` to match it, as `LIKE` matches.
    pub email_like: Option<String>,
    /// Where set, requires `email` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub email_is_null: Option<bool>,
    /// Where set, requires `store_id` to equal it.
    pub store_id: Option<u8>,
    /// Where set, requires `store_id` to be at least it.
    pub store_id_from: Option<u8>,
    /// Where set, requires `store_id` to be at most it.
    pub store_id_to: Option<u8>,
    /// Where set, requires `active` to equal it.
    pub active: Option<i8>,
    /// Where set, requires `active` to be at least it.
    pub active_from: Option<i8>,
    /// Where set, requires `active` to be at most it.
    pub active_to: Option<i8>,
    /// Where set, requires `username` to equal it.
    pub username: Option<String>,
    /// Where set, requires `username` to match it, as `LIKE` matches.
    pub username_like: Option<String>,
    /// Where set, requires `password` to equal it.
    pub password: Option<String>,
    /// Where set, requires `password` to match it, as `LIKE` matches.
    pub password_like: Option<String>,
    /// Where set, requires `password` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub password_is_null: Option<bool>,
    /// Where set, requires `last_update` to equal it.
    pub last_update: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `last_update` to be at least it.
    pub last_update_from: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `last_update` to be at most it.
    pub last_update_to: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
}

impl StaffFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.staff_id.is_none()
            && self.staff_id_from.is_none()
            && self.staff_id_to.is_none()
            && self.first_name.is_none()
            && self.first_name_like.is_none()
            && self.last_name.is_none()
            && self.last_name_like.is_none()
            && self.address_id.is_none()
            && self.address_id_from.is_none()
            && self.address_id_to.is_none()
            && self.picture.is_none()
            && self.picture_is_null.is_none()
            && self.email.is_none()
            && self.email_like.is_none()
            && self.email_is_null.is_none()
            && self.store_id.is_none()
            && self.store_id_from.is_none()
            && self.store_id_to.is_none()
            && self.active.is_none()
            && self.active_from.is_none()
            && self.active_to.is_none()
            && self.username.is_none()
            && self.username_like.is_none()
            && self.password.is_none()
            && self.password_like.is_none()
            && self.password_is_null.is_none()
            && self.last_update.is_none()
            && self.last_update_from.is_none()
            && self.last_update_to.is_none()
    }
}
