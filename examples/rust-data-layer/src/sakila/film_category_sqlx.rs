// Code generated by tpl from sakila.film_category. DO NOT EDIT.

//! The `sqlx` implementation of the `sakila.film_category` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::film_category::{FilmCategory, FilmCategoryFilter, FilmCategoryKey, FilmCategoryOrder};
use super::film_category_repository::FilmCategoryRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "sakila.film_category";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `film_id`, `category_id`, `last_update` FROM `film_category`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `film_id`, `category_id`, `last_update` FROM `film_category` WHERE `film_id` = ? AND `category_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `film_category` (`film_id`, `category_id`, `last_update`) VALUES (?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `film_category` SET `last_update` = ? WHERE `film_id` = ? AND `category_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `film_category` WHERE `film_id` = ? AND `category_id` = ?";

/// The `sqlx` data layer of `sakila.film_category`.
#[derive(Debug, Clone)]
pub struct FilmCategorySqlx {
    pool: sqlx::MySqlPool,
}

impl FilmCategorySqlx {
    /// Builds the data layer of `sakila.film_category` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl FilmCategoryRepository for FilmCategorySqlx {
    async fn create(&self, row: &mut FilmCategory) -> Result<(), Error> {
        sqlx::query(INSERT)
            .bind(row.film_id)
            .bind(row.category_id)
            .bind(row.last_update)
            .execute(&self.pool)
            .await?;
        Ok(())
    }

    async fn read(&self, key: &FilmCategoryKey) -> Result<FilmCategory, Error> {
        sqlx::query_as::<_, FilmCategory>(READ)
            .bind(key.film_id)
            .bind(key.category_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &FilmCategory) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(row.last_update)
            .bind(row.film_id)
            .bind(row.category_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &FilmCategoryKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.film_id)
            .bind(key.category_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: FilmCategoryFilter,
        order: &[FilmCategoryOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<FilmCategory>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.film_id {
                conditions
                    .push("`film_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.film_id_from {
                conditions
                    .push("`film_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.film_id_to {
                conditions
                    .push("`film_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.category_id {
                conditions
                    .push("`category_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.category_id_from {
                conditions
                    .push("`category_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.category_id_to {
                conditions
                    .push("`category_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update {
                conditions
                    .push("`last_update` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update_from {
                conditions
                    .push("`last_update` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update_to {
                conditions
                    .push("`last_update` <= ")
                    .push_bind_unseparated(value);
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<FilmCategory>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
