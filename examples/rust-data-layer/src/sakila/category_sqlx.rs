// Code generated by tpl from sakila.category. DO NOT EDIT.

//! The `sqlx` implementation of the `sakila.category` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::category::{Category, CategoryFilter, CategoryKey, CategoryOrder};
use super::category_repository::CategoryRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "sakila.category";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `category_id`, `name`, `last_update` FROM `category`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `category_id`, `name`, `last_update` FROM `category` WHERE `category_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `category` (`name`, `last_update`) VALUES (?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `category` SET `name` = ?, `last_update` = ? WHERE `category_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `category` WHERE `category_id` = ?";

/// The `sqlx` data layer of `sakila.category`.
#[derive(Debug, Clone)]
pub struct CategorySqlx {
    pool: sqlx::MySqlPool,
}

impl CategorySqlx {
    /// Builds the data layer of `sakila.category` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl CategoryRepository for CategorySqlx {
    async fn create(&self, row: &mut Category) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(&row.name)
            .bind(row.last_update)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.category_id =
            u8::try_from(assigned).map_err(|source| Error::AssignedKey {
                table: TABLE,
                column: "category_id",
                source,
            })?;
        Ok(())
    }

    async fn read(&self, key: &CategoryKey) -> Result<Category, Error> {
        sqlx::query_as::<_, Category>(READ)
            .bind(key.category_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &Category) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.name)
            .bind(row.last_update)
            .bind(row.category_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &CategoryKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.category_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: CategoryFilter,
        order: &[CategoryOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<Category>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.category_id {
                conditions
                    .push("`category_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.category_id_from {
                conditions
                    .push("`category_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.category_id_to {
                conditions
                    .push("`category_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.name {
                conditions
                    .push("`name` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.name_like {
                conditions
                    .push("`name` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update {
                conditions
                    .push("`last_update` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update_from {
                conditions
                    .push("`last_update` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update_to {
                conditions
                    .push("`last_update` <= ")
                    .push_bind_unseparated(value);
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<Category>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
