// Code generated by tpl from sakila.city. DO NOT EDIT.

//! The `sqlx` implementation of the `sakila.city` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::city::{City, CityFilter, CityKey, CityOrder};
use super::city_repository::CityRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "sakila.city";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `city_id`, `city`, `country_id`, `last_update` FROM `city`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `city_id`, `city`, `country_id`, `last_update` FROM `city` WHERE `city_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `city` (`city`, `country_id`, `last_update`) VALUES (?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `city` SET `city` = ?, `country_id` = ?, `last_update` = ? WHERE `city_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `city` WHERE `city_id` = ?";

/// The `sqlx` data layer of `sakila.city`.
#[derive(Debug, Clone)]
pub struct CitySqlx {
    pool: sqlx::MySqlPool,
}

impl CitySqlx {
    /// Builds the data layer of `sakila.city` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl CityRepository for CitySqlx {
    async fn create(&self, row: &mut City) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(&row.city)
            .bind(row.country_id)
            .bind(row.last_update)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.city_id =
            u16::try_from(assigned).map_err(|source| Error::AssignedKey {
                table: TABLE,
                column: "city_id",
                source,
            })?;
        Ok(())
    }

    async fn read(&self, key: &CityKey) -> Result<City, Error> {
        sqlx::query_as::<_, City>(READ)
            .bind(key.city_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &City) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.city)
            .bind(row.country_id)
            .bind(row.last_update)
            .bind(row.city_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &CityKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.city_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: CityFilter,
        order: &[CityOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<City>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.city_id {
                conditions
                    .push("`city_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.city_id_from {
                conditions
                    .push("`city_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.city_id_to {
                conditions
                    .push("`city_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.city {
                conditions
                    .push("`city` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.city_like {
                conditions
                    .push("`city` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.country_id {
                conditions
                    .push("`country_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.country_id_from {
                conditions
                    .push("`country_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.country_id_to {
                conditions
                    .push("`country_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update {
                conditions
                    .push("`last_update` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update_from {
                conditions
                    .push("`last_update` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update_to {
                conditions
                    .push("`last_update` <= ")
                    .push_bind_unseparated(value);
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<City>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
