// Code generated by tpl from sakila.staff. DO NOT EDIT.

//! The `sqlx` implementation of the `sakila.staff` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::staff::{Staff, StaffFilter, StaffKey, StaffOrder};
use super::staff_repository::StaffRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "sakila.staff";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `staff_id`, `first_name`, `last_name`, `address_id`, `picture`, `email`, `store_id`, `active`, `username`, `password`, `last_update` FROM `staff`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `staff_id`, `first_name`, `last_name`, `address_id`, `picture`, `email`, `store_id`, `active`, `username`, `password`, `last_update` FROM `staff` WHERE `staff_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `staff` (`first_name`, `last_name`, `address_id`, `picture`, `email`, `store_id`, `active`, `username`, `password`, `last_update`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `staff` SET `first_name` = ?, `last_name` = ?, `address_id` = ?, `picture` = ?, `email` = ?, `store_id` = ?, `active` = ?, `username` = ?, `password` = ?, `last_update` = ? WHERE `staff_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `staff` WHERE `staff_id` = ?";

/// The `sqlx` data layer of `sakila.staff`.
#[derive(Debug, Clone)]
pub struct StaffSqlx {
    pool: sqlx::MySqlPool,
}

impl StaffSqlx {
    /// Builds the data layer of `sakila.staff` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl StaffRepository for StaffSqlx {
    async fn create(&self, row: &mut Staff) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(&row.first_name)
            .bind(&row.last_name)
            .bind(row.address_id)
            .bind(&row.picture)
            .bind(&row.email)
            .bind(row.store_id)
            .bind(row.active)
            .bind(&row.username)
            .bind(&row.password)
            .bind(row.last_update)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.staff_id =
            u8::try_from(assigned).map_err(|source| Error::AssignedKey {
                table: TABLE,
                column: "staff_id",
                source,
            })?;
        Ok(())
    }

    async fn read(&self, key: &StaffKey) -> Result<Staff, Error> {
        sqlx::query_as::<_, Staff>(READ)
            .bind(key.staff_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &Staff) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.first_name)
            .bind(&row.last_name)
            .bind(row.address_id)
            .bind(&row.picture)
            .bind(&row.email)
            .bind(row.store_id)
            .bind(row.active)
            .bind(&row.username)
            .bind(&row.password)
            .bind(row.last_update)
            .bind(row.staff_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &StaffKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.staff_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: StaffFilter,
        order: &[StaffOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<Staff>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.staff_id {
                conditions
                    .push("`staff_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.staff_id_from {
                conditions
                    .push("`staff_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.staff_id_to {
                conditions
                    .push("`staff_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.first_name {
                conditions
                    .push("`first_name` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.first_name_like {
                conditions
                    .push("`first_name` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_name {
                conditions
                    .push("`last_name` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_name_like {
                conditions
                    .push("`last_name` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.address_id {
                conditions
                    .push("`address_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.address_id_from {
                conditions
                    .push("`address_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.address_id_to {
                conditions
                    .push("`address_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.picture {
                conditions
                    .push("`picture` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.picture_is_null {
                conditions.push(if is_null {
                    "`picture` IS NULL"
                } else {
                    "`picture` IS NOT NULL"
                });
            }
            if let Some(value) = filter.email {
                conditions
                    .push("`email` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.email_like {
                conditions
                    .push("`email` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.email_is_null {
                conditions.push(if is_null {
                    "`email` IS NULL"
                } else {
                    "`email` IS NOT NULL"
                });
            }
            if let Some(value) = filter.store_id {
                conditions
                    .push("`store_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.store_id_from {
                conditions
                    .push("`store_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.store_id_to {
                conditions
                    .push("`store_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.active {
                conditions
                    .push("`active` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.active_from {
                conditions
                    .push("`active` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.active_to {
                conditions
                    .push("`active` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.username {
                conditions
                    .push("`username` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.username_like {
                conditions
                    .push("`username` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.password {
                conditions
                    .push("`password` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.password_like {
                conditions
                    .push("`password` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.password_is_null {
                conditions.push(if is_null {
                    "`password` IS NULL"
                } else {
                    "`password` IS NOT NULL"
                });
            }
            if let Some(value) = filter.last_update {
                conditions
                    .push("`last_update` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update_from {
                conditions
                    .push("`last_update` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update_to {
                conditions
                    .push("`last_update` <= ")
                    .push_bind_unseparated(value);
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<Staff>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
