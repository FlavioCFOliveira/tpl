// Code generated by tpl from sakila.store. DO NOT EDIT.

//! The `sakila.store` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `sakila.store`.
#[derive(Debug, Clone)]
pub struct Store {
    /// `store_id`, declared `tinyint(3) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub store_id: u8,
    /// `manager_staff_id`, declared `tinyint(3) unsigned` NOT NULL.
    ///
    /// It is part of a unique index.
    pub manager_staff_id: u8,
    /// `address_id`, declared `smallint(5) unsigned` NOT NULL.
    pub address_id: u16,
    /// `last_update`, declared `timestamp` NOT NULL.
    pub last_update: sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>,
}

impl Store {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> StoreKey {
        StoreKey {
            store_id: self.store_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Store {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            store_id: row.try_get("store_id")?,
            manager_staff_id: row.try_get("manager_staff_id")?,
            address_id: row.try_get("address_id")?,
            last_update: row.try_get("last_update")?,
        })
    }
}

/// The primary key of `sakila.store`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct StoreKey {
    /// `store_id`, declared `tinyint(3) unsigned`.
    pub store_id: u8,
}

/// One column of `sakila.store`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: StoreColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum StoreColumn {
    /// `store_id`, declared `tinyint(3) unsigned`.
    StoreId,
    /// `manager_staff_id`, declared `tinyint(3) unsigned`.
    ManagerStaffId,
    /// `address_id`, declared `smallint(5) unsigned`.
    AddressId,
    /// `last_update`, declared `timestamp`.
    LastUpdate,
}

impl StoreColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::StoreId => "`store_id`",
            Self::ManagerStaffId => "`manager_staff_id`",
            Self::AddressId => "`address_id`",
            Self::LastUpdate => "`last_update`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::StoreId => "store_id",
            Self::ManagerStaffId => "manager_staff_id",
            Self::AddressId => "address_id",
            Self::LastUpdate => "last_update",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::StoreId,
            Self::ManagerStaffId,
            Self::AddressId,
            Self::LastUpdate,
        ]
    }
}

impl std::fmt::Display for StoreColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for StoreColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "store_id" => Ok(Self::StoreId),
            "manager_staff_id" => Ok(Self::ManagerStaffId),
            "address_id" => Ok(Self::AddressId),
            "last_update" => Ok(Self::LastUpdate),
            _ => Err(super::Error::UnknownColumn {
                table: "sakila.store",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `sakila.store`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct StoreOrder {
    /// The column to order by.
    pub column: StoreColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `sakila.store`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct StoreFilter {
    /// Where set, requires `store_id` to equal it.
    pub store_id: Option<u8>,
    /// Where set, requires `store_id` to be at least it.
    pub store_id_from: Option<u8>,
    /// Where set, requires `store_id` to be at most it.
    pub store_id_to: Option<u8>,
    /// Where set, requires `manager_staff_id` to equal it.
    pub manager_staff_id: Option<u8>,
    /// Where set, requires `manager_staff_id` to be at least it.
    pub manager_staff_id_from: Option<u8>,
    /// Where set, requires `manager_staff_id` to be at most it.
    pub manager_staff_id_to: Option<u8>,
    /// Where set, requires `address_id` to equal it.
    pub address_id: Option<u16>,
    /// Where set, requires `address_id` to be at least it.
    pub address_id_from: Option<u16>,
    /// Where set, requires `address_id` to be at most it.
    pub address_id_to: Option<u16>,
    /// Where set, requires `last_update` to equal it.
    pub last_update: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `last_update` to be at least it.
    pub last_update_from: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `last_update` to be at most it.
    pub last_update_to: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
}

impl StoreFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.store_id.is_none()
            && self.store_id_from.is_none()
            && self.store_id_to.is_none()
            && self.manager_staff_id.is_none()
            && self.manager_staff_id_from.is_none()
            && self.manager_staff_id_to.is_none()
            && self.address_id.is_none()
            && self.address_id_from.is_none()
            && self.address_id_to.is_none()
            && self.last_update.is_none()
            && self.last_update_from.is_none()
            && self.last_update_to.is_none()
    }
}
