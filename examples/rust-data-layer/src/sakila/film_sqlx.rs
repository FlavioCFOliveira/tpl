// Code generated by tpl from sakila.film. DO NOT EDIT.

//! The `sqlx` implementation of the `sakila.film` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::film::{Film, FilmFilter, FilmKey, FilmOrder};
use super::film_repository::FilmRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "sakila.film";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `film_id`, `title`, `description`, `release_year`, `language_id`, `original_language_id`, `rental_duration`, `rental_rate`, `length`, `replacement_cost`, `rating`, `special_features`, `last_update` FROM `film`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `film_id`, `title`, `description`, `release_year`, `language_id`, `original_language_id`, `rental_duration`, `rental_rate`, `length`, `replacement_cost`, `rating`, `special_features`, `last_update` FROM `film` WHERE `film_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `film` (`title`, `description`, `release_year`, `language_id`, `original_language_id`, `rental_duration`, `rental_rate`, `length`, `replacement_cost`, `rating`, `special_features`, `last_update`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `film` SET `title` = ?, `description` = ?, `release_year` = ?, `language_id` = ?, `original_language_id` = ?, `rental_duration` = ?, `rental_rate` = ?, `length` = ?, `replacement_cost` = ?, `rating` = ?, `special_features` = ?, `last_update` = ? WHERE `film_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `film` WHERE `film_id` = ?";

/// The `sqlx` data layer of `sakila.film`.
#[derive(Debug, Clone)]
pub struct FilmSqlx {
    pool: sqlx::MySqlPool,
}

impl FilmSqlx {
    /// Builds the data layer of `sakila.film` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl FilmRepository for FilmSqlx {
    async fn create(&self, row: &mut Film) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(&row.title)
            .bind(&row.description)
            .bind(row.release_year)
            .bind(row.language_id)
            .bind(row.original_language_id)
            .bind(row.rental_duration)
            .bind(&row.rental_rate)
            .bind(row.length)
            .bind(&row.replacement_cost)
            .bind(&row.rating)
            .bind(&row.special_features)
            .bind(row.last_update)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.film_id =
            u16::try_from(assigned).map_err(|source| Error::AssignedKey {
                table: TABLE,
                column: "film_id",
                source,
            })?;
        Ok(())
    }

    async fn read(&self, key: &FilmKey) -> Result<Film, Error> {
        sqlx::query_as::<_, Film>(READ)
            .bind(key.film_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &Film) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.title)
            .bind(&row.description)
            .bind(row.release_year)
            .bind(row.language_id)
            .bind(row.original_language_id)
            .bind(row.rental_duration)
            .bind(&row.rental_rate)
            .bind(row.length)
            .bind(&row.replacement_cost)
            .bind(&row.rating)
            .bind(&row.special_features)
            .bind(row.last_update)
            .bind(row.film_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &FilmKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.film_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: FilmFilter,
        order: &[FilmOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<Film>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.film_id {
                conditions
                    .push("`film_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.film_id_from {
                conditions
                    .push("`film_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.film_id_to {
                conditions
                    .push("`film_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.title {
                conditions
                    .push("`title` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.title_like {
                conditions
                    .push("`title` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.description {
                conditions
                    .push("`description` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.description_like {
                conditions
                    .push("`description` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.description_is_null {
                conditions.push(if is_null {
                    "`description` IS NULL"
                } else {
                    "`description` IS NOT NULL"
                });
            }
            if let Some(value) = filter.release_year {
                conditions
                    .push("`release_year` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.release_year_from {
                conditions
                    .push("`release_year` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.release_year_to {
                conditions
                    .push("`release_year` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.release_year_is_null {
                conditions.push(if is_null {
                    "`release_year` IS NULL"
                } else {
                    "`release_year` IS NOT NULL"
                });
            }
            if let Some(value) = filter.language_id {
                conditions
                    .push("`language_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.language_id_from {
                conditions
                    .push("`language_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.language_id_to {
                conditions
                    .push("`language_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.original_language_id {
                conditions
                    .push("`original_language_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.original_language_id_from {
                conditions
                    .push("`original_language_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.original_language_id_to {
                conditions
                    .push("`original_language_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.original_language_id_is_null {
                conditions.push(if is_null {
                    "`original_language_id` IS NULL"
                } else {
                    "`original_language_id` IS NOT NULL"
                });
            }
            if let Some(value) = filter.rental_duration {
                conditions
                    .push("`rental_duration` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.rental_duration_from {
                conditions
                    .push("`rental_duration` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.rental_duration_to {
                conditions
                    .push("`rental_duration` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.rental_rate {
                conditions
                    .push("`rental_rate` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.rental_rate_from {
                conditions
                    .push("`rental_rate` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.rental_rate_to {
                conditions
                    .push("`rental_rate` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.length {
                conditions
                    .push("`length` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.length_from {
                conditions
                    .push("`length` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.length_to {
                conditions
                    .push("`length` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.length_is_null {
                conditions.push(if is_null {
                    "`length` IS NULL"
                } else {
                    "`length` IS NOT NULL"
                });
            }
            if let Some(value) = filter.replacement_cost {
                conditions
                    .push("`replacement_cost` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.replacement_cost_from {
                conditions
                    .push("`replacement_cost` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.replacement_cost_to {
                conditions
                    .push("`replacement_cost` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.rating {
                conditions
                    .push("`rating` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.rating_like {
                conditions
                    .push("`rating` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.rating_is_null {
                conditions.push(if is_null {
                    "`rating` IS NULL"
                } else {
                    "`rating` IS NOT NULL"
                });
            }
            if let Some(value) = filter.special_features {
                conditions
                    .push("`special_features` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.special_features_like {
                conditions
                    .push("`special_features` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.special_features_is_null {
                conditions.push(if is_null {
                    "`special_features` IS NULL"
                } else {
                    "`special_features` IS NOT NULL"
                });
            }
            if let Some(value) = filter.last_update {
                conditions
                    .push("`last_update` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update_from {
                conditions
                    .push("`last_update` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update_to {
                conditions
                    .push("`last_update` <= ")
                    .push_bind_unseparated(value);
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<Film>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
