// Code generated by tpl from sakila.address. DO NOT EDIT.

//! The `sakila.address` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `sakila.address`.
#[derive(Debug, Clone)]
pub struct Address {
    /// `address_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub address_id: u16,
    /// `address`, declared `varchar(50)` NOT NULL.
    pub address: String,
    /// `address2`, declared `varchar(50)` NULL.
    pub address2: Option<String>,
    /// `district`, declared `varchar(20)` NOT NULL.
    pub district: String,
    /// `city_id`, declared `smallint(5) unsigned` NOT NULL.
    pub city_id: u16,
    /// `postal_code`, declared `varchar(10)` NULL.
    pub postal_code: Option<String>,
    /// `phone`, declared `varchar(20)` NOT NULL.
    pub phone: String,
    /// `last_update`, declared `timestamp` NOT NULL.
    pub last_update: sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>,
}

impl Address {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> AddressKey {
        AddressKey {
            address_id: self.address_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Address {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            address_id: row.try_get("address_id")?,
            address: row.try_get("address")?,
            address2: row.try_get("address2")?,
            district: row.try_get("district")?,
            city_id: row.try_get("city_id")?,
            postal_code: row.try_get("postal_code")?,
            phone: row.try_get("phone")?,
            last_update: row.try_get("last_update")?,
        })
    }
}

/// The primary key of `sakila.address`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct AddressKey {
    /// `address_id`, declared `smallint(5) unsigned`.
    pub address_id: u16,
}

/// One column of `sakila.address`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: AddressColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum AddressColumn {
    /// `address_id`, declared `smallint(5) unsigned`.
    AddressId,
    /// `address`, declared `varchar(50)`.
    Address,
    /// `address2`, declared `varchar(50)`.
    Address2,
    /// `district`, declared `varchar(20)`.
    District,
    /// `city_id`, declared `smallint(5) unsigned`.
    CityId,
    /// `postal_code`, declared `varchar(10)`.
    PostalCode,
    /// `phone`, declared `varchar(20)`.
    Phone,
    /// `last_update`, declared `timestamp`.
    LastUpdate,
}

impl AddressColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::AddressId => "`address_id`",
            Self::Address => "`address`",
            Self::Address2 => "`address2`",
            Self::District => "`district`",
            Self::CityId => "`city_id`",
            Self::PostalCode => "`postal_code`",
            Self::Phone => "`phone`",
            Self::LastUpdate => "`last_update`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::AddressId => "address_id",
            Self::Address => "address",
            Self::Address2 => "address2",
            Self::District => "district",
            Self::CityId => "city_id",
            Self::PostalCode => "postal_code",
            Self::Phone => "phone",
            Self::LastUpdate => "last_update",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::AddressId,
            Self::Address,
            Self::Address2,
            Self::District,
            Self::CityId,
            Self::PostalCode,
            Self::Phone,
            Self::LastUpdate,
        ]
    }
}

impl std::fmt::Display for AddressColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for AddressColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "address_id" => Ok(Self::AddressId),
            "address" => Ok(Self::Address),
            "address2" => Ok(Self::Address2),
            "district" => Ok(Self::District),
            "city_id" => Ok(Self::CityId),
            "postal_code" => Ok(Self::PostalCode),
            "phone" => Ok(Self::Phone),
            "last_update" => Ok(Self::LastUpdate),
            _ => Err(super::Error::UnknownColumn {
                table: "sakila.address",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `sakila.address`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct AddressOrder {
    /// The column to order by.
    pub column: AddressColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `sakila.address`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct AddressFilter {
    /// Where set, requires `address_id` to equal it.
    pub address_id: Option<u16>,
    /// Where set, requires `address_id` to be at least it.
    pub address_id_from: Option<u16>,
    /// Where set, requires `address_id` to be at most it.
    pub address_id_to: Option<u16>,
    /// Where set, requires `address` to equal it.
    pub address: Option<String>,
    /// Where set, requires `address` to match it, as `LIKE` matches.
    pub address_like: Option<String>,
    /// Where set, requires `address2` to equal it.
    pub address2: Option<String>,
    /// Where set, requires `address2` to match it, as `LIKE` matches.
    pub address2_like: Option<String>,
    /// Where set, requires `address2` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub address2_is_null: Option<bool>,
    /// Where set, requires `district` to equal it.
    pub district: Option<String>,
    /// Where set, requires `district` to match it, as `LIKE` matches.
    pub district_like: Option<String>,
    /// Where set, requires `city_id` to equal it.
    pub city_id: Option<u16>,
    /// Where set, requires `city_id` to be at least it.
    pub city_id_from: Option<u16>,
    /// Where set, requires `city_id` to be at most it.
    pub city_id_to: Option<u16>,
    /// Where set, requires `postal_code` to equal it.
    pub postal_code: Option<String>,
    /// Where set, requires `postal_code` to match it, as `LIKE` matches.
    pub postal_code_like: Option<String>,
    /// Where set, requires `postal_code` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub postal_code_is_null: Option<bool>,
    /// Where set, requires `phone` to equal it.
    pub phone: Option<String>,
    /// Where set, requires `phone` to match it, as `LIKE` matches.
    pub phone_like: Option<String>,
    /// Where set, requires `last_update` to equal it.
    pub last_update: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `last_update` to be at least it.
    pub last_update_from: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `last_update` to be at most it.
    pub last_update_to: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
}

impl AddressFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.address_id.is_none()
            && self.address_id_from.is_none()
            && self.address_id_to.is_none()
            && self.address.is_none()
            && self.address_like.is_none()
            && self.address2.is_none()
            && self.address2_like.is_none()
            && self.address2_is_null.is_none()
            && self.district.is_none()
            && self.district_like.is_none()
            && self.city_id.is_none()
            && self.city_id_from.is_none()
            && self.city_id_to.is_none()
            && self.postal_code.is_none()
            && self.postal_code_like.is_none()
            && self.postal_code_is_null.is_none()
            && self.phone.is_none()
            && self.phone_like.is_none()
            && self.last_update.is_none()
            && self.last_update_from.is_none()
            && self.last_update_to.is_none()
    }
}
