// Code generated by tpl from sakila.payment. DO NOT EDIT.

//! The `sqlx` implementation of the `sakila.payment` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::payment::{Payment, PaymentFilter, PaymentKey, PaymentOrder};
use super::payment_repository::PaymentRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "sakila.payment";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `payment_id`, `customer_id`, `staff_id`, `rental_id`, `amount`, `payment_date`, `last_update` FROM `payment`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `payment_id`, `customer_id`, `staff_id`, `rental_id`, `amount`, `payment_date`, `last_update` FROM `payment` WHERE `payment_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `payment` (`customer_id`, `staff_id`, `rental_id`, `amount`, `payment_date`, `last_update`) VALUES (?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `payment` SET `customer_id` = ?, `staff_id` = ?, `rental_id` = ?, `amount` = ?, `payment_date` = ?, `last_update` = ? WHERE `payment_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `payment` WHERE `payment_id` = ?";

/// The `sqlx` data layer of `sakila.payment`.
#[derive(Debug, Clone)]
pub struct PaymentSqlx {
    pool: sqlx::MySqlPool,
}

impl PaymentSqlx {
    /// Builds the data layer of `sakila.payment` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl PaymentRepository for PaymentSqlx {
    async fn create(&self, row: &mut Payment) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(row.customer_id)
            .bind(row.staff_id)
            .bind(row.rental_id)
            .bind(&row.amount)
            .bind(row.payment_date)
            .bind(row.last_update)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.payment_id =
            u16::try_from(assigned).map_err(|source| Error::AssignedKey {
                table: TABLE,
                column: "payment_id",
                source,
            })?;
        Ok(())
    }

    async fn read(&self, key: &PaymentKey) -> Result<Payment, Error> {
        sqlx::query_as::<_, Payment>(READ)
            .bind(key.payment_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &Payment) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(row.customer_id)
            .bind(row.staff_id)
            .bind(row.rental_id)
            .bind(&row.amount)
            .bind(row.payment_date)
            .bind(row.last_update)
            .bind(row.payment_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &PaymentKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.payment_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: PaymentFilter,
        order: &[PaymentOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<Payment>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.payment_id {
                conditions
                    .push("`payment_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.payment_id_from {
                conditions
                    .push("`payment_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.payment_id_to {
                conditions
                    .push("`payment_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.customer_id {
                conditions
                    .push("`customer_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.customer_id_from {
                conditions
                    .push("`customer_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.customer_id_to {
                conditions
                    .push("`customer_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.staff_id {
                conditions
                    .push("`staff_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.staff_id_from {
                conditions
                    .push("`staff_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.staff_id_to {
                conditions
                    .push("`staff_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.rental_id {
                conditions
                    .push("`rental_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.rental_id_from {
                conditions
                    .push("`rental_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.rental_id_to {
                conditions
                    .push("`rental_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.rental_id_is_null {
                conditions.push(if is_null {
                    "`rental_id` IS NULL"
                } else {
                    "`rental_id` IS NOT NULL"
                });
            }
            if let Some(value) = filter.amount {
                conditions
                    .push("`amount` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.amount_from {
                conditions
                    .push("`amount` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.amount_to {
                conditions
                    .push("`amount` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.payment_date {
                conditions
                    .push("`payment_date` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.payment_date_from {
                conditions
                    .push("`payment_date` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.payment_date_to {
                conditions
                    .push("`payment_date` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update {
                conditions
                    .push("`last_update` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update_from {
                conditions
                    .push("`last_update` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.last_update_to {
                conditions
                    .push("`last_update` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.last_update_is_null {
                conditions.push(if is_null {
                    "`last_update` IS NULL"
                } else {
                    "`last_update` IS NOT NULL"
                });
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<Payment>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
