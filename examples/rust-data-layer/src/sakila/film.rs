// Code generated by tpl from sakila.film. DO NOT EDIT.

//! The `sakila.film` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `sakila.film`.
#[derive(Debug, Clone)]
pub struct Film {
    /// `film_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub film_id: u16,
    /// `title`, declared `varchar(128)` NOT NULL.
    pub title: String,
    /// `description`, declared `text` NULL.
    pub description: Option<String>,
    /// `release_year`, declared `year(4)` NULL.
    pub release_year: Option<u16>,
    /// `language_id`, declared `tinyint(3) unsigned` NOT NULL.
    pub language_id: u8,
    /// `original_language_id`, declared `tinyint(3) unsigned` NULL.
    pub original_language_id: Option<u8>,
    /// `rental_duration`, declared `tinyint(3) unsigned` NOT NULL.
    pub rental_duration: u8,
    /// `rental_rate`, declared `decimal(4,2)` NOT NULL.
    pub rental_rate: super::Decimal,
    /// `length`, declared `smallint(5) unsigned` NULL.
    pub length: Option<u16>,
    /// `replacement_cost`, declared `decimal(5,2)` NOT NULL.
    pub replacement_cost: super::Decimal,
    /// `rating`, declared `enum('G','PG','PG-13','R','NC-17')` NULL.
    pub rating: Option<String>,
    /// `special_features`, declared `set('Trailers','Commentaries','Deleted Scenes','Behind the Scenes')` NULL.
    pub special_features: Option<String>,
    /// `last_update`, declared `timestamp` NOT NULL.
    pub last_update: sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>,
}

impl Film {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> FilmKey {
        FilmKey {
            film_id: self.film_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Film {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            film_id: row.try_get("film_id")?,
            title: row.try_get("title")?,
            description: row.try_get("description")?,
            release_year: row.try_get("release_year")?,
            language_id: row.try_get("language_id")?,
            original_language_id: row.try_get("original_language_id")?,
            rental_duration: row.try_get("rental_duration")?,
            rental_rate: row.try_get("rental_rate")?,
            length: row.try_get("length")?,
            replacement_cost: row.try_get("replacement_cost")?,
            rating: row.try_get("rating")?,
            special_features: row.try_get("special_features")?,
            last_update: row.try_get("last_update")?,
        })
    }
}

/// The primary key of `sakila.film`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct FilmKey {
    /// `film_id`, declared `smallint(5) unsigned`.
    pub film_id: u16,
}

/// One column of `sakila.film`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: FilmColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum FilmColumn {
    /// `film_id`, declared `smallint(5) unsigned`.
    FilmId,
    /// `title`, declared `varchar(128)`.
    Title,
    /// `description`, declared `text`.
    Description,
    /// `release_year`, declared `year(4)`.
    ReleaseYear,
    /// `language_id`, declared `tinyint(3) unsigned`.
    LanguageId,
    /// `original_language_id`, declared `tinyint(3) unsigned`.
    OriginalLanguageId,
    /// `rental_duration`, declared `tinyint(3) unsigned`.
    RentalDuration,
    /// `rental_rate`, declared `decimal(4,2)`.
    RentalRate,
    /// `length`, declared `smallint(5) unsigned`.
    Length,
    /// `replacement_cost`, declared `decimal(5,2)`.
    ReplacementCost,
    /// `rating`, declared `enum('G','PG','PG-13','R','NC-17')`.
    Rating,
    /// `special_features`, declared `set('Trailers','Commentaries','Deleted Scenes','Behind the Scenes')`.
    SpecialFeatures,
    /// `last_update`, declared `timestamp`.
    LastUpdate,
}

impl FilmColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::FilmId => "`film_id`",
            Self::Title => "`title`",
            Self::Description => "`description`",
            Self::ReleaseYear => "`release_year`",
            Self::LanguageId => "`language_id`",
            Self::OriginalLanguageId => "`original_language_id`",
            Self::RentalDuration => "`rental_duration`",
            Self::RentalRate => "`rental_rate`",
            Self::Length => "`length`",
            Self::ReplacementCost => "`replacement_cost`",
            Self::Rating => "`rating`",
            Self::SpecialFeatures => "`special_features`",
            Self::LastUpdate => "`last_update`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::FilmId => "film_id",
            Self::Title => "title",
            Self::Description => "description",
            Self::ReleaseYear => "release_year",
            Self::LanguageId => "language_id",
            Self::OriginalLanguageId => "original_language_id",
            Self::RentalDuration => "rental_duration",
            Self::RentalRate => "rental_rate",
            Self::Length => "length",
            Self::ReplacementCost => "replacement_cost",
            Self::Rating => "rating",
            Self::SpecialFeatures => "special_features",
            Self::LastUpdate => "last_update",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::FilmId,
            Self::Title,
            Self::Description,
            Self::ReleaseYear,
            Self::LanguageId,
            Self::OriginalLanguageId,
            Self::RentalDuration,
            Self::RentalRate,
            Self::Length,
            Self::ReplacementCost,
            Self::Rating,
            Self::SpecialFeatures,
            Self::LastUpdate,
        ]
    }
}

impl std::fmt::Display for FilmColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for FilmColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "film_id" => Ok(Self::FilmId),
            "title" => Ok(Self::Title),
            "description" => Ok(Self::Description),
            "release_year" => Ok(Self::ReleaseYear),
            "language_id" => Ok(Self::LanguageId),
            "original_language_id" => Ok(Self::OriginalLanguageId),
            "rental_duration" => Ok(Self::RentalDuration),
            "rental_rate" => Ok(Self::RentalRate),
            "length" => Ok(Self::Length),
            "replacement_cost" => Ok(Self::ReplacementCost),
            "rating" => Ok(Self::Rating),
            "special_features" => Ok(Self::SpecialFeatures),
            "last_update" => Ok(Self::LastUpdate),
            _ => Err(super::Error::UnknownColumn {
                table: "sakila.film",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `sakila.film`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct FilmOrder {
    /// The column to order by.
    pub column: FilmColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `sakila.film`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct FilmFilter {
    /// Where set, requires `film_id` to equal it.
    pub film_id: Option<u16>,
    /// Where set, requires `film_id` to be at least it.
    pub film_id_from: Option<u16>,
    /// Where set, requires `film_id` to be at most it.
    pub film_id_to: Option<u16>,
    /// Where set, requires `title` to equal it.
    pub title: Option<String>,
    /// Where set, requires `title` to match it, as `LIKE` matches.
    pub title_like: Option<String>,
    /// Where set, requires `description` to equal it.
    pub description: Option<String>,
    /// Where set, requires `description` to match it, as `LIKE` matches.
    pub description_like: Option<String>,
    /// Where set, requires `description` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub description_is_null: Option<bool>,
    /// Where set, requires `release_year` to equal it.
    pub release_year: Option<u16>,
    /// Where set, requires `release_year` to be at least it.
    pub release_year_from: Option<u16>,
    /// Where set, requires `release_year` to be at most it.
    pub release_year_to: Option<u16>,
    /// Where set, requires `release_year` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub release_year_is_null: Option<bool>,
    /// Where set, requires `language_id` to equal it.
    pub language_id: Option<u8>,
    /// Where set, requires `language_id` to be at least it.
    pub language_id_from: Option<u8>,
    /// Where set, requires `language_id` to be at most it.
    pub language_id_to: Option<u8>,
    /// Where set, requires `original_language_id` to equal it.
    pub original_language_id: Option<u8>,
    /// Where set, requires `original_language_id` to be at least it.
    pub original_language_id_from: Option<u8>,
    /// Where set, requires `original_language_id` to be at most it.
    pub original_language_id_to: Option<u8>,
    /// Where set, requires `original_language_id` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub original_language_id_is_null: Option<bool>,
    /// Where set, requires `rental_duration` to equal it.
    pub rental_duration: Option<u8>,
    /// Where set, requires `rental_duration` to be at least it.
    pub rental_duration_from: Option<u8>,
    /// Where set, requires `rental_duration` to be at most it.
    pub rental_duration_to: Option<u8>,
    /// Where set, requires `rental_rate` to equal it.
    pub rental_rate: Option<super::Decimal>,
    /// Where set, requires `rental_rate` to be at least it.
    pub rental_rate_from: Option<super::Decimal>,
    /// Where set, requires `rental_rate` to be at most it.
    pub rental_rate_to: Option<super::Decimal>,
    /// Where set, requires `length` to equal it.
    pub length: Option<u16>,
    /// Where set, requires `length` to be at least it.
    pub length_from: Option<u16>,
    /// Where set, requires `length` to be at most it.
    pub length_to: Option<u16>,
    /// Where set, requires `length` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub length_is_null: Option<bool>,
    /// Where set, requires `replacement_cost` to equal it.
    pub replacement_cost: Option<super::Decimal>,
    /// Where set, requires `replacement_cost` to be at least it.
    pub replacement_cost_from: Option<super::Decimal>,
    /// Where set, requires `replacement_cost` to be at most it.
    pub replacement_cost_to: Option<super::Decimal>,
    /// Where set, requires `rating` to equal it.
    pub rating: Option<String>,
    /// Where set, requires `rating` to match it, as `LIKE` matches.
    pub rating_like: Option<String>,
    /// Where set, requires `rating` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub rating_is_null: Option<bool>,
    /// Where set, requires `special_features` to equal it.
    pub special_features: Option<String>,
    /// Where set, requires `special_features` to match it, as `LIKE` matches.
    pub special_features_like: Option<String>,
    /// Where set, requires `special_features` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub special_features_is_null: Option<bool>,
    /// Where set, requires `last_update` to equal it.
    pub last_update: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `last_update` to be at least it.
    pub last_update_from: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `last_update` to be at most it.
    pub last_update_to: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
}

impl FilmFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.film_id.is_none()
            && self.film_id_from.is_none()
            && self.film_id_to.is_none()
            && self.title.is_none()
            && self.title_like.is_none()
            && self.description.is_none()
            && self.description_like.is_none()
            && self.description_is_null.is_none()
            && self.release_year.is_none()
            && self.release_year_from.is_none()
            && self.release_year_to.is_none()
            && self.release_year_is_null.is_none()
            && self.language_id.is_none()
            && self.language_id_from.is_none()
            && self.language_id_to.is_none()
            && self.original_language_id.is_none()
            && self.original_language_id_from.is_none()
            && self.original_language_id_to.is_none()
            && self.original_language_id_is_null.is_none()
            && self.rental_duration.is_none()
            && self.rental_duration_from.is_none()
            && self.rental_duration_to.is_none()
            && self.rental_rate.is_none()
            && self.rental_rate_from.is_none()
            && self.rental_rate_to.is_none()
            && self.length.is_none()
            && self.length_from.is_none()
            && self.length_to.is_none()
            && self.length_is_null.is_none()
            && self.replacement_cost.is_none()
            && self.replacement_cost_from.is_none()
            && self.replacement_cost_to.is_none()
            && self.rating.is_none()
            && self.rating_like.is_none()
            && self.rating_is_null.is_none()
            && self.special_features.is_none()
            && self.special_features_like.is_none()
            && self.special_features_is_null.is_none()
            && self.last_update.is_none()
            && self.last_update_from.is_none()
            && self.last_update_to.is_none()
    }
}
