// Code generated by tpl from freight.customer. DO NOT EDIT.

//! The `freight.customer` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.customer`.
///
/// Clientes expedidores e destinatários, com limite de crédito e preferências do portal
#[derive(Debug, Clone)]
pub struct Customer {
    /// `customer_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub customer_id: u32,
    /// `legal_name`, declared `varchar(160)` NOT NULL.
    ///
    /// Registered company name as it appears on the bill of lading
    pub legal_name: String,
    /// `trading_name`, declared `varchar(160)` NULL.
    pub trading_name: Option<String>,
    /// `vat_number`, declared `varchar(20)` NOT NULL.
    ///
    /// It is part of a unique index.
    pub vat_number: String,
    /// `eori_number`, declared `varchar(17)` NULL.
    ///
    /// Economic Operators Registration and Identification number
    pub eori_number: Option<String>,
    /// `country_code`, declared `char(2)` NOT NULL.
    ///
    /// It is part of a unique index.
    pub country_code: String,
    /// `legacy_account_code`, declared `varchar(24)` NULL INVISIBLE.
    ///
    /// INVISIBLE: account code from the 1998 AS/400 ledger, kept for reconciliation only
    pub legacy_account_code: Option<String>,
    /// `portal_uuid`, declared `uuid` NOT NULL.
    ///
    /// It is part of a unique index.
    ///
    /// Opaque identifier exposed to the customer portal
    pub portal_uuid: String,
    /// `last_login_ipv4`, declared `inet4` NULL.
    pub last_login_ipv4: Option<std::net::Ipv4Addr>,
    /// `last_login_ipv6`, declared `inet6` NULL.
    pub last_login_ipv6: Option<std::net::Ipv6Addr>,
    /// `credit_limit_eur`, declared `decimal(14,2)` NOT NULL.
    pub credit_limit_eur: super::Decimal,
    /// `outstanding_eur`, declared `decimal(14,2)` NOT NULL.
    pub outstanding_eur: super::Decimal,
    /// `credit_headroom_eur`, declared `decimal(14,2)` NULL STORED GENERATED AS (`credit_limit_eur` - `outstanding_eur`).
    ///
    /// Stored generated column: what the sales desk is allowed to book today
    pub credit_headroom_eur: Option<super::Decimal>,
    /// `risk_score`, declared `decimal(5,2)` NULL.
    ///
    /// Internal credit risk, 0.00 best to 100.00 worst
    pub risk_score: Option<super::Decimal>,
    /// `payment_terms_days`, declared `smallint(5) unsigned` NOT NULL.
    pub payment_terms_days: u16,
    /// `preferences`, declared `longtext` NULL.
    ///
    /// Portal preferences; MariaDB implements JSON as LONGTEXT plus a json_valid CHECK
    pub preferences: Option<String>,
    /// `notification_flags`, declared `bit(8)` NOT NULL.
    ///
    /// Bit 0 e-mail, bit 1 SMS, bit 2 EDI, bit 3 portal
    pub notification_flags: u64,
    /// `is_active`, declared `tinyint(1)` NOT NULL.
    pub is_active: i8,
    /// `onboarded_on`, declared `date` NOT NULL.
    pub onboarded_on: sqlx::types::chrono::NaiveDate,
    /// `review_due_on`, declared `date` NOT NULL.
    ///
    /// Expression default: annual KYC review
    pub review_due_on: sqlx::types::chrono::NaiveDate,
    /// `contact_note`, declared `varchar(255)` NULL.
    ///
    /// Column charset differs from the table default: utf8mb3 from the legacy CRM import
    pub contact_note: Option<String>,
    /// `created_at`, declared `datetime(6)` NOT NULL.
    pub created_at: sqlx::types::chrono::NaiveDateTime,
    /// `updated_at`, declared `timestamp` NOT NULL.
    pub updated_at: sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>,
}

impl Customer {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> CustomerKey {
        CustomerKey {
            customer_id: self.customer_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Customer {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            customer_id: row.try_get("customer_id")?,
            legal_name: row.try_get("legal_name")?,
            trading_name: row.try_get("trading_name")?,
            vat_number: row.try_get("vat_number")?,
            eori_number: row.try_get("eori_number")?,
            country_code: row.try_get("country_code")?,
            legacy_account_code: row.try_get("legacy_account_code")?,
            portal_uuid: row.try_get("portal_uuid")?,
            last_login_ipv4: row.try_get("last_login_ipv4")?,
            last_login_ipv6: row.try_get("last_login_ipv6")?,
            credit_limit_eur: row.try_get("credit_limit_eur")?,
            outstanding_eur: row.try_get("outstanding_eur")?,
            credit_headroom_eur: row.try_get("credit_headroom_eur")?,
            risk_score: row.try_get("risk_score")?,
            payment_terms_days: row.try_get("payment_terms_days")?,
            preferences: row.try_get("preferences")?,
            notification_flags: row.try_get("notification_flags")?,
            is_active: row.try_get("is_active")?,
            onboarded_on: row.try_get("onboarded_on")?,
            review_due_on: row.try_get("review_due_on")?,
            contact_note: row.try_get("contact_note")?,
            created_at: row.try_get("created_at")?,
            updated_at: row.try_get("updated_at")?,
        })
    }
}

/// The primary key of `freight.customer`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct CustomerKey {
    /// `customer_id`, declared `int(10) unsigned`.
    pub customer_id: u32,
}

/// One column of `freight.customer`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: CustomerColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum CustomerColumn {
    /// `customer_id`, declared `int(10) unsigned`.
    CustomerId,
    /// `legal_name`, declared `varchar(160)`.
    LegalName,
    /// `trading_name`, declared `varchar(160)`.
    TradingName,
    /// `vat_number`, declared `varchar(20)`.
    VatNumber,
    /// `eori_number`, declared `varchar(17)`.
    EoriNumber,
    /// `country_code`, declared `char(2)`.
    CountryCode,
    /// `legacy_account_code`, declared `varchar(24)`.
    LegacyAccountCode,
    /// `portal_uuid`, declared `uuid`.
    PortalUuid,
    /// `last_login_ipv4`, declared `inet4`.
    LastLoginIpv4,
    /// `last_login_ipv6`, declared `inet6`.
    LastLoginIpv6,
    /// `credit_limit_eur`, declared `decimal(14,2)`.
    CreditLimitEur,
    /// `outstanding_eur`, declared `decimal(14,2)`.
    OutstandingEur,
    /// `credit_headroom_eur`, declared `decimal(14,2)`.
    CreditHeadroomEur,
    /// `risk_score`, declared `decimal(5,2)`.
    RiskScore,
    /// `payment_terms_days`, declared `smallint(5) unsigned`.
    PaymentTermsDays,
    /// `preferences`, declared `longtext`.
    Preferences,
    /// `notification_flags`, declared `bit(8)`.
    NotificationFlags,
    /// `is_active`, declared `tinyint(1)`.
    IsActive,
    /// `onboarded_on`, declared `date`.
    OnboardedOn,
    /// `review_due_on`, declared `date`.
    ReviewDueOn,
    /// `contact_note`, declared `varchar(255)`.
    ContactNote,
    /// `created_at`, declared `datetime(6)`.
    CreatedAt,
    /// `updated_at`, declared `timestamp`.
    UpdatedAt,
}

impl CustomerColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::CustomerId => "`customer_id`",
            Self::LegalName => "`legal_name`",
            Self::TradingName => "`trading_name`",
            Self::VatNumber => "`vat_number`",
            Self::EoriNumber => "`eori_number`",
            Self::CountryCode => "`country_code`",
            Self::LegacyAccountCode => "`legacy_account_code`",
            Self::PortalUuid => "`portal_uuid`",
            Self::LastLoginIpv4 => "`last_login_ipv4`",
            Self::LastLoginIpv6 => "`last_login_ipv6`",
            Self::CreditLimitEur => "`credit_limit_eur`",
            Self::OutstandingEur => "`outstanding_eur`",
            Self::CreditHeadroomEur => "`credit_headroom_eur`",
            Self::RiskScore => "`risk_score`",
            Self::PaymentTermsDays => "`payment_terms_days`",
            Self::Preferences => "`preferences`",
            Self::NotificationFlags => "`notification_flags`",
            Self::IsActive => "`is_active`",
            Self::OnboardedOn => "`onboarded_on`",
            Self::ReviewDueOn => "`review_due_on`",
            Self::ContactNote => "`contact_note`",
            Self::CreatedAt => "`created_at`",
            Self::UpdatedAt => "`updated_at`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::CustomerId => "customer_id",
            Self::LegalName => "legal_name",
            Self::TradingName => "trading_name",
            Self::VatNumber => "vat_number",
            Self::EoriNumber => "eori_number",
            Self::CountryCode => "country_code",
            Self::LegacyAccountCode => "legacy_account_code",
            Self::PortalUuid => "portal_uuid",
            Self::LastLoginIpv4 => "last_login_ipv4",
            Self::LastLoginIpv6 => "last_login_ipv6",
            Self::CreditLimitEur => "credit_limit_eur",
            Self::OutstandingEur => "outstanding_eur",
            Self::CreditHeadroomEur => "credit_headroom_eur",
            Self::RiskScore => "risk_score",
            Self::PaymentTermsDays => "payment_terms_days",
            Self::Preferences => "preferences",
            Self::NotificationFlags => "notification_flags",
            Self::IsActive => "is_active",
            Self::OnboardedOn => "onboarded_on",
            Self::ReviewDueOn => "review_due_on",
            Self::ContactNote => "contact_note",
            Self::CreatedAt => "created_at",
            Self::UpdatedAt => "updated_at",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::CustomerId,
            Self::LegalName,
            Self::TradingName,
            Self::VatNumber,
            Self::EoriNumber,
            Self::CountryCode,
            Self::LegacyAccountCode,
            Self::PortalUuid,
            Self::LastLoginIpv4,
            Self::LastLoginIpv6,
            Self::CreditLimitEur,
            Self::OutstandingEur,
            Self::CreditHeadroomEur,
            Self::RiskScore,
            Self::PaymentTermsDays,
            Self::Preferences,
            Self::NotificationFlags,
            Self::IsActive,
            Self::OnboardedOn,
            Self::ReviewDueOn,
            Self::ContactNote,
            Self::CreatedAt,
            Self::UpdatedAt,
        ]
    }
}

impl std::fmt::Display for CustomerColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for CustomerColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "customer_id" => Ok(Self::CustomerId),
            "legal_name" => Ok(Self::LegalName),
            "trading_name" => Ok(Self::TradingName),
            "vat_number" => Ok(Self::VatNumber),
            "eori_number" => Ok(Self::EoriNumber),
            "country_code" => Ok(Self::CountryCode),
            "legacy_account_code" => Ok(Self::LegacyAccountCode),
            "portal_uuid" => Ok(Self::PortalUuid),
            "last_login_ipv4" => Ok(Self::LastLoginIpv4),
            "last_login_ipv6" => Ok(Self::LastLoginIpv6),
            "credit_limit_eur" => Ok(Self::CreditLimitEur),
            "outstanding_eur" => Ok(Self::OutstandingEur),
            "credit_headroom_eur" => Ok(Self::CreditHeadroomEur),
            "risk_score" => Ok(Self::RiskScore),
            "payment_terms_days" => Ok(Self::PaymentTermsDays),
            "preferences" => Ok(Self::Preferences),
            "notification_flags" => Ok(Self::NotificationFlags),
            "is_active" => Ok(Self::IsActive),
            "onboarded_on" => Ok(Self::OnboardedOn),
            "review_due_on" => Ok(Self::ReviewDueOn),
            "contact_note" => Ok(Self::ContactNote),
            "created_at" => Ok(Self::CreatedAt),
            "updated_at" => Ok(Self::UpdatedAt),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.customer",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.customer`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct CustomerOrder {
    /// The column to order by.
    pub column: CustomerColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.customer`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct CustomerFilter {
    /// Where set, requires `customer_id` to equal it.
    pub customer_id: Option<u32>,
    /// Where set, requires `customer_id` to be at least it.
    pub customer_id_from: Option<u32>,
    /// Where set, requires `customer_id` to be at most it.
    pub customer_id_to: Option<u32>,
    /// Where set, requires `legal_name` to equal it.
    pub legal_name: Option<String>,
    /// Where set, requires `legal_name` to match it, as `LIKE` matches.
    pub legal_name_like: Option<String>,
    /// Where set, requires `trading_name` to equal it.
    pub trading_name: Option<String>,
    /// Where set, requires `trading_name` to match it, as `LIKE` matches.
    pub trading_name_like: Option<String>,
    /// Where set, requires `trading_name` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub trading_name_is_null: Option<bool>,
    /// Where set, requires `vat_number` to equal it.
    pub vat_number: Option<String>,
    /// Where set, requires `vat_number` to match it, as `LIKE` matches.
    pub vat_number_like: Option<String>,
    /// Where set, requires `eori_number` to equal it.
    pub eori_number: Option<String>,
    /// Where set, requires `eori_number` to match it, as `LIKE` matches.
    pub eori_number_like: Option<String>,
    /// Where set, requires `eori_number` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub eori_number_is_null: Option<bool>,
    /// Where set, requires `country_code` to equal it.
    pub country_code: Option<String>,
    /// Where set, requires `country_code` to match it, as `LIKE` matches.
    pub country_code_like: Option<String>,
    /// Where set, requires `legacy_account_code` to equal it.
    pub legacy_account_code: Option<String>,
    /// Where set, requires `legacy_account_code` to match it, as `LIKE` matches.
    pub legacy_account_code_like: Option<String>,
    /// Where set, requires `legacy_account_code` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub legacy_account_code_is_null: Option<bool>,
    /// Where set, requires `portal_uuid` to equal it.
    pub portal_uuid: Option<String>,
    /// Where set, requires `last_login_ipv4` to equal it.
    pub last_login_ipv4: Option<std::net::Ipv4Addr>,
    /// Where set, requires `last_login_ipv4` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub last_login_ipv4_is_null: Option<bool>,
    /// Where set, requires `last_login_ipv6` to equal it.
    pub last_login_ipv6: Option<std::net::Ipv6Addr>,
    /// Where set, requires `last_login_ipv6` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub last_login_ipv6_is_null: Option<bool>,
    /// Where set, requires `credit_limit_eur` to equal it.
    pub credit_limit_eur: Option<super::Decimal>,
    /// Where set, requires `credit_limit_eur` to be at least it.
    pub credit_limit_eur_from: Option<super::Decimal>,
    /// Where set, requires `credit_limit_eur` to be at most it.
    pub credit_limit_eur_to: Option<super::Decimal>,
    /// Where set, requires `outstanding_eur` to equal it.
    pub outstanding_eur: Option<super::Decimal>,
    /// Where set, requires `outstanding_eur` to be at least it.
    pub outstanding_eur_from: Option<super::Decimal>,
    /// Where set, requires `outstanding_eur` to be at most it.
    pub outstanding_eur_to: Option<super::Decimal>,
    /// Where set, requires `credit_headroom_eur` to equal it.
    pub credit_headroom_eur: Option<super::Decimal>,
    /// Where set, requires `credit_headroom_eur` to be at least it.
    pub credit_headroom_eur_from: Option<super::Decimal>,
    /// Where set, requires `credit_headroom_eur` to be at most it.
    pub credit_headroom_eur_to: Option<super::Decimal>,
    /// Where set, requires `credit_headroom_eur` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub credit_headroom_eur_is_null: Option<bool>,
    /// Where set, requires `risk_score` to equal it.
    pub risk_score: Option<super::Decimal>,
    /// Where set, requires `risk_score` to be at least it.
    pub risk_score_from: Option<super::Decimal>,
    /// Where set, requires `risk_score` to be at most it.
    pub risk_score_to: Option<super::Decimal>,
    /// Where set, requires `risk_score` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub risk_score_is_null: Option<bool>,
    /// Where set, requires `payment_terms_days` to equal it.
    pub payment_terms_days: Option<u16>,
    /// Where set, requires `payment_terms_days` to be at least it.
    pub payment_terms_days_from: Option<u16>,
    /// Where set, requires `payment_terms_days` to be at most it.
    pub payment_terms_days_to: Option<u16>,
    /// Where set, requires `preferences` to equal it.
    pub preferences: Option<String>,
    /// Where set, requires `preferences` to match it, as `LIKE` matches.
    pub preferences_like: Option<String>,
    /// Where set, requires `preferences` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub preferences_is_null: Option<bool>,
    /// Where set, requires `notification_flags` to equal it.
    pub notification_flags: Option<u64>,
    /// Where set, requires `notification_flags` to be at least it.
    pub notification_flags_from: Option<u64>,
    /// Where set, requires `notification_flags` to be at most it.
    pub notification_flags_to: Option<u64>,
    /// Where set, requires `is_active` to equal it.
    pub is_active: Option<i8>,
    /// Where set, requires `is_active` to be at least it.
    pub is_active_from: Option<i8>,
    /// Where set, requires `is_active` to be at most it.
    pub is_active_to: Option<i8>,
    /// Where set, requires `onboarded_on` to equal it.
    pub onboarded_on: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `onboarded_on` to be at least it.
    pub onboarded_on_from: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `onboarded_on` to be at most it.
    pub onboarded_on_to: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `review_due_on` to equal it.
    pub review_due_on: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `review_due_on` to be at least it.
    pub review_due_on_from: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `review_due_on` to be at most it.
    pub review_due_on_to: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `contact_note` to equal it.
    pub contact_note: Option<String>,
    /// Where set, requires `contact_note` to match it, as `LIKE` matches.
    pub contact_note_like: Option<String>,
    /// Where set, requires `contact_note` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub contact_note_is_null: Option<bool>,
    /// Where set, requires `created_at` to equal it.
    pub created_at: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `created_at` to be at least it.
    pub created_at_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `created_at` to be at most it.
    pub created_at_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `updated_at` to equal it.
    pub updated_at: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `updated_at` to be at least it.
    pub updated_at_from: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `updated_at` to be at most it.
    pub updated_at_to: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
}

impl CustomerFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.customer_id.is_none()
            && self.customer_id_from.is_none()
            && self.customer_id_to.is_none()
            && self.legal_name.is_none()
            && self.legal_name_like.is_none()
            && self.trading_name.is_none()
            && self.trading_name_like.is_none()
            && self.trading_name_is_null.is_none()
            && self.vat_number.is_none()
            && self.vat_number_like.is_none()
            && self.eori_number.is_none()
            && self.eori_number_like.is_none()
            && self.eori_number_is_null.is_none()
            && self.country_code.is_none()
            && self.country_code_like.is_none()
            && self.legacy_account_code.is_none()
            && self.legacy_account_code_like.is_none()
            && self.legacy_account_code_is_null.is_none()
            && self.portal_uuid.is_none()
            && self.last_login_ipv4.is_none()
            && self.last_login_ipv4_is_null.is_none()
            && self.last_login_ipv6.is_none()
            && self.last_login_ipv6_is_null.is_none()
            && self.credit_limit_eur.is_none()
            && self.credit_limit_eur_from.is_none()
            && self.credit_limit_eur_to.is_none()
            && self.outstanding_eur.is_none()
            && self.outstanding_eur_from.is_none()
            && self.outstanding_eur_to.is_none()
            && self.credit_headroom_eur.is_none()
            && self.credit_headroom_eur_from.is_none()
            && self.credit_headroom_eur_to.is_none()
            && self.credit_headroom_eur_is_null.is_none()
            && self.risk_score.is_none()
            && self.risk_score_from.is_none()
            && self.risk_score_to.is_none()
            && self.risk_score_is_null.is_none()
            && self.payment_terms_days.is_none()
            && self.payment_terms_days_from.is_none()
            && self.payment_terms_days_to.is_none()
            && self.preferences.is_none()
            && self.preferences_like.is_none()
            && self.preferences_is_null.is_none()
            && self.notification_flags.is_none()
            && self.notification_flags_from.is_none()
            && self.notification_flags_to.is_none()
            && self.is_active.is_none()
            && self.is_active_from.is_none()
            && self.is_active_to.is_none()
            && self.onboarded_on.is_none()
            && self.onboarded_on_from.is_none()
            && self.onboarded_on_to.is_none()
            && self.review_due_on.is_none()
            && self.review_due_on_from.is_none()
            && self.review_due_on_to.is_none()
            && self.contact_note.is_none()
            && self.contact_note_like.is_none()
            && self.contact_note_is_null.is_none()
            && self.created_at.is_none()
            && self.created_at_from.is_none()
            && self.created_at_to.is_none()
            && self.updated_at.is_none()
            && self.updated_at_from.is_none()
            && self.updated_at_to.is_none()
    }
}
