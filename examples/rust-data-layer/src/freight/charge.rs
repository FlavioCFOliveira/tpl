// Code generated by tpl from freight.charge. DO NOT EDIT.

//! The `freight.charge` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.charge`.
///
/// Encargos aplicados a cada expedição
#[derive(Debug, Clone)]
pub struct Charge {
    /// `charge_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub charge_id: u64,
    /// `consignment_id`, declared `bigint(20) unsigned` NOT NULL.
    pub consignment_id: u64,
    /// `charge_code`, declared `varchar(8)` NOT NULL.
    pub charge_code: String,
    /// `quantity`, declared `decimal(10,3)` NOT NULL.
    pub quantity: super::Decimal,
    /// `unit_amount`, declared `decimal(12,4)` NOT NULL.
    pub unit_amount: super::Decimal,
    /// `currency`, declared `char(3)` NOT NULL.
    pub currency: String,
    /// `fx_rate_to_eur`, declared `decimal(12,6)` NOT NULL.
    pub fx_rate_to_eur: super::Decimal,
    /// `net_amount`, declared `decimal(14,2)` NULL STORED GENERATED AS (round(`quantity` * `unit_amount`,2)).
    ///
    /// Stored generated column: the line total before tax
    pub net_amount: Option<super::Decimal>,
    /// `payer`, declared `enum('Shipper','Consignee','Third party')` NOT NULL.
    pub payer: String,
    /// `invoiced_on`, declared `date` NULL.
    pub invoiced_on: Option<sqlx::types::chrono::NaiveDate>,
    /// `invoice_number`, declared `varchar(20)` NULL.
    pub invoice_number: Option<String>,
}

impl Charge {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> ChargeKey {
        ChargeKey {
            charge_id: self.charge_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Charge {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            charge_id: row.try_get("charge_id")?,
            consignment_id: row.try_get("consignment_id")?,
            charge_code: row.try_get("charge_code")?,
            quantity: row.try_get("quantity")?,
            unit_amount: row.try_get("unit_amount")?,
            currency: row.try_get("currency")?,
            fx_rate_to_eur: row.try_get("fx_rate_to_eur")?,
            net_amount: row.try_get("net_amount")?,
            payer: row.try_get("payer")?,
            invoiced_on: row.try_get("invoiced_on")?,
            invoice_number: row.try_get("invoice_number")?,
        })
    }
}

/// The primary key of `freight.charge`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct ChargeKey {
    /// `charge_id`, declared `bigint(20) unsigned`.
    pub charge_id: u64,
}

/// One column of `freight.charge`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: ChargeColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum ChargeColumn {
    /// `charge_id`, declared `bigint(20) unsigned`.
    ChargeId,
    /// `consignment_id`, declared `bigint(20) unsigned`.
    ConsignmentId,
    /// `charge_code`, declared `varchar(8)`.
    ChargeCode,
    /// `quantity`, declared `decimal(10,3)`.
    Quantity,
    /// `unit_amount`, declared `decimal(12,4)`.
    UnitAmount,
    /// `currency`, declared `char(3)`.
    Currency,
    /// `fx_rate_to_eur`, declared `decimal(12,6)`.
    FxRateToEur,
    /// `net_amount`, declared `decimal(14,2)`.
    NetAmount,
    /// `payer`, declared `enum('Shipper','Consignee','Third party')`.
    Payer,
    /// `invoiced_on`, declared `date`.
    InvoicedOn,
    /// `invoice_number`, declared `varchar(20)`.
    InvoiceNumber,
}

impl ChargeColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::ChargeId => "`charge_id`",
            Self::ConsignmentId => "`consignment_id`",
            Self::ChargeCode => "`charge_code`",
            Self::Quantity => "`quantity`",
            Self::UnitAmount => "`unit_amount`",
            Self::Currency => "`currency`",
            Self::FxRateToEur => "`fx_rate_to_eur`",
            Self::NetAmount => "`net_amount`",
            Self::Payer => "`payer`",
            Self::InvoicedOn => "`invoiced_on`",
            Self::InvoiceNumber => "`invoice_number`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::ChargeId => "charge_id",
            Self::ConsignmentId => "consignment_id",
            Self::ChargeCode => "charge_code",
            Self::Quantity => "quantity",
            Self::UnitAmount => "unit_amount",
            Self::Currency => "currency",
            Self::FxRateToEur => "fx_rate_to_eur",
            Self::NetAmount => "net_amount",
            Self::Payer => "payer",
            Self::InvoicedOn => "invoiced_on",
            Self::InvoiceNumber => "invoice_number",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::ChargeId,
            Self::ConsignmentId,
            Self::ChargeCode,
            Self::Quantity,
            Self::UnitAmount,
            Self::Currency,
            Self::FxRateToEur,
            Self::NetAmount,
            Self::Payer,
            Self::InvoicedOn,
            Self::InvoiceNumber,
        ]
    }
}

impl std::fmt::Display for ChargeColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for ChargeColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "charge_id" => Ok(Self::ChargeId),
            "consignment_id" => Ok(Self::ConsignmentId),
            "charge_code" => Ok(Self::ChargeCode),
            "quantity" => Ok(Self::Quantity),
            "unit_amount" => Ok(Self::UnitAmount),
            "currency" => Ok(Self::Currency),
            "fx_rate_to_eur" => Ok(Self::FxRateToEur),
            "net_amount" => Ok(Self::NetAmount),
            "payer" => Ok(Self::Payer),
            "invoiced_on" => Ok(Self::InvoicedOn),
            "invoice_number" => Ok(Self::InvoiceNumber),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.charge",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.charge`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct ChargeOrder {
    /// The column to order by.
    pub column: ChargeColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.charge`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct ChargeFilter {
    /// Where set, requires `charge_id` to equal it.
    pub charge_id: Option<u64>,
    /// Where set, requires `charge_id` to be at least it.
    pub charge_id_from: Option<u64>,
    /// Where set, requires `charge_id` to be at most it.
    pub charge_id_to: Option<u64>,
    /// Where set, requires `consignment_id` to equal it.
    pub consignment_id: Option<u64>,
    /// Where set, requires `consignment_id` to be at least it.
    pub consignment_id_from: Option<u64>,
    /// Where set, requires `consignment_id` to be at most it.
    pub consignment_id_to: Option<u64>,
    /// Where set, requires `charge_code` to equal it.
    pub charge_code: Option<String>,
    /// Where set, requires `charge_code` to match it, as `LIKE` matches.
    pub charge_code_like: Option<String>,
    /// Where set, requires `quantity` to equal it.
    pub quantity: Option<super::Decimal>,
    /// Where set, requires `quantity` to be at least it.
    pub quantity_from: Option<super::Decimal>,
    /// Where set, requires `quantity` to be at most it.
    pub quantity_to: Option<super::Decimal>,
    /// Where set, requires `unit_amount` to equal it.
    pub unit_amount: Option<super::Decimal>,
    /// Where set, requires `unit_amount` to be at least it.
    pub unit_amount_from: Option<super::Decimal>,
    /// Where set, requires `unit_amount` to be at most it.
    pub unit_amount_to: Option<super::Decimal>,
    /// Where set, requires `currency` to equal it.
    pub currency: Option<String>,
    /// Where set, requires `currency` to match it, as `LIKE` matches.
    pub currency_like: Option<String>,
    /// Where set, requires `fx_rate_to_eur` to equal it.
    pub fx_rate_to_eur: Option<super::Decimal>,
    /// Where set, requires `fx_rate_to_eur` to be at least it.
    pub fx_rate_to_eur_from: Option<super::Decimal>,
    /// Where set, requires `fx_rate_to_eur` to be at most it.
    pub fx_rate_to_eur_to: Option<super::Decimal>,
    /// Where set, requires `net_amount` to equal it.
    pub net_amount: Option<super::Decimal>,
    /// Where set, requires `net_amount` to be at least it.
    pub net_amount_from: Option<super::Decimal>,
    /// Where set, requires `net_amount` to be at most it.
    pub net_amount_to: Option<super::Decimal>,
    /// Where set, requires `net_amount` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub net_amount_is_null: Option<bool>,
    /// Where set, requires `payer` to equal it.
    pub payer: Option<String>,
    /// Where set, requires `payer` to match it, as `LIKE` matches.
    pub payer_like: Option<String>,
    /// Where set, requires `invoiced_on` to equal it.
    pub invoiced_on: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `invoiced_on` to be at least it.
    pub invoiced_on_from: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `invoiced_on` to be at most it.
    pub invoiced_on_to: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `invoiced_on` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub invoiced_on_is_null: Option<bool>,
    /// Where set, requires `invoice_number` to equal it.
    pub invoice_number: Option<String>,
    /// Where set, requires `invoice_number` to match it, as `LIKE` matches.
    pub invoice_number_like: Option<String>,
    /// Where set, requires `invoice_number` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub invoice_number_is_null: Option<bool>,
}

impl ChargeFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.charge_id.is_none()
            && self.charge_id_from.is_none()
            && self.charge_id_to.is_none()
            && self.consignment_id.is_none()
            && self.consignment_id_from.is_none()
            && self.consignment_id_to.is_none()
            && self.charge_code.is_none()
            && self.charge_code_like.is_none()
            && self.quantity.is_none()
            && self.quantity_from.is_none()
            && self.quantity_to.is_none()
            && self.unit_amount.is_none()
            && self.unit_amount_from.is_none()
            && self.unit_amount_to.is_none()
            && self.currency.is_none()
            && self.currency_like.is_none()
            && self.fx_rate_to_eur.is_none()
            && self.fx_rate_to_eur_from.is_none()
            && self.fx_rate_to_eur_to.is_none()
            && self.net_amount.is_none()
            && self.net_amount_from.is_none()
            && self.net_amount_to.is_none()
            && self.net_amount_is_null.is_none()
            && self.payer.is_none()
            && self.payer_like.is_none()
            && self.invoiced_on.is_none()
            && self.invoiced_on_from.is_none()
            && self.invoiced_on_to.is_none()
            && self.invoiced_on_is_null.is_none()
            && self.invoice_number.is_none()
            && self.invoice_number_like.is_none()
            && self.invoice_number_is_null.is_none()
    }
}
