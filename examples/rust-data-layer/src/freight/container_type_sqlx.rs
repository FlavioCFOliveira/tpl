// Code generated by tpl from freight.container_type. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.container_type` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::container_type::{ContainerType, ContainerTypeFilter, ContainerTypeKey, ContainerTypeOrder};
use super::container_type_repository::ContainerTypeRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.container_type";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `iso_code`, `description`, `length_ft`, `height_ft_in`, `tare_weight_kg`, `max_payload_kg`, `internal_volume_cbm`, `is_reefer`, `is_tank` FROM `container_type`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `iso_code`, `description`, `length_ft`, `height_ft_in`, `tare_weight_kg`, `max_payload_kg`, `internal_volume_cbm`, `is_reefer`, `is_tank` FROM `container_type` WHERE `iso_code` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `container_type` (`iso_code`, `description`, `length_ft`, `height_ft_in`, `tare_weight_kg`, `max_payload_kg`, `internal_volume_cbm`, `is_reefer`, `is_tank`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `container_type` SET `description` = ?, `length_ft` = ?, `height_ft_in` = ?, `tare_weight_kg` = ?, `max_payload_kg` = ?, `internal_volume_cbm` = ?, `is_reefer` = ?, `is_tank` = ? WHERE `iso_code` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `container_type` WHERE `iso_code` = ?";

/// The `sqlx` data layer of `freight.container_type`.
#[derive(Debug, Clone)]
pub struct ContainerTypeSqlx {
    pool: sqlx::MySqlPool,
}

impl ContainerTypeSqlx {
    /// Builds the data layer of `freight.container_type` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl ContainerTypeRepository for ContainerTypeSqlx {
    async fn create(&self, row: &mut ContainerType) -> Result<(), Error> {
        sqlx::query(INSERT)
            .bind(&row.iso_code)
            .bind(&row.description)
            .bind(row.length_ft)
            .bind(&row.height_ft_in)
            .bind(&row.tare_weight_kg)
            .bind(&row.max_payload_kg)
            .bind(&row.internal_volume_cbm)
            .bind(row.is_reefer)
            .bind(row.is_tank)
            .execute(&self.pool)
            .await?;
        Ok(())
    }

    async fn read(&self, key: &ContainerTypeKey) -> Result<ContainerType, Error> {
        sqlx::query_as::<_, ContainerType>(READ)
            .bind(&key.iso_code)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &ContainerType) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.description)
            .bind(row.length_ft)
            .bind(&row.height_ft_in)
            .bind(&row.tare_weight_kg)
            .bind(&row.max_payload_kg)
            .bind(&row.internal_volume_cbm)
            .bind(row.is_reefer)
            .bind(row.is_tank)
            .bind(&row.iso_code)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &ContainerTypeKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(&key.iso_code)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: ContainerTypeFilter,
        order: &[ContainerTypeOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<ContainerType>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.iso_code {
                conditions
                    .push("`iso_code` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.iso_code_like {
                conditions
                    .push("`iso_code` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.description {
                conditions
                    .push("`description` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.description_like {
                conditions
                    .push("`description` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.length_ft {
                conditions
                    .push("`length_ft` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.length_ft_from {
                conditions
                    .push("`length_ft` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.length_ft_to {
                conditions
                    .push("`length_ft` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.height_ft_in {
                conditions
                    .push("`height_ft_in` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.height_ft_in_like {
                conditions
                    .push("`height_ft_in` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.tare_weight_kg {
                conditions
                    .push("`tare_weight_kg` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.tare_weight_kg_from {
                conditions
                    .push("`tare_weight_kg` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.tare_weight_kg_to {
                conditions
                    .push("`tare_weight_kg` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.max_payload_kg {
                conditions
                    .push("`max_payload_kg` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.max_payload_kg_from {
                conditions
                    .push("`max_payload_kg` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.max_payload_kg_to {
                conditions
                    .push("`max_payload_kg` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.internal_volume_cbm {
                conditions
                    .push("`internal_volume_cbm` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.internal_volume_cbm_from {
                conditions
                    .push("`internal_volume_cbm` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.internal_volume_cbm_to {
                conditions
                    .push("`internal_volume_cbm` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.is_reefer {
                conditions
                    .push("`is_reefer` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.is_reefer_from {
                conditions
                    .push("`is_reefer` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.is_reefer_to {
                conditions
                    .push("`is_reefer` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.is_tank {
                conditions
                    .push("`is_tank` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.is_tank_from {
                conditions
                    .push("`is_tank` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.is_tank_to {
                conditions
                    .push("`is_tank` <= ")
                    .push_bind_unseparated(value);
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<ContainerType>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
