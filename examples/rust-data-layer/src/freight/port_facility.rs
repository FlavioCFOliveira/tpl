// Code generated by tpl from freight.port_facility. DO NOT EDIT.

//! The `freight.port_facility` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.port_facility`.
///
/// Terminais e postos de acostagem, com a respectiva geometria — 港湾施設
#[derive(Debug, Clone)]
pub struct PortFacility {
    /// `facility_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub facility_id: u32,
    /// `port_id`, declared `smallint(5) unsigned` NOT NULL.
    ///
    /// It is part of a unique index.
    pub port_id: u16,
    /// `designation`, declared `varchar(80)` NOT NULL.
    ///
    /// It is part of a unique index.
    ///
    /// Terminal or berth designation, e.g. "Alcântara Norte"
    pub designation: String,
    /// `facility_kind`, declared `enum('Container terminal','Ro-Ro ramp','Bulk quay','Liquid bulk jetty','Reefer terminal','Lay-by berth')` NOT NULL.
    pub facility_kind: String,
    /// `quay_length_m`, declared `decimal(7,2)` NOT NULL.
    ///
    /// Continuous quay length in metres
    pub quay_length_m: super::Decimal,
    /// `crane_count`, declared `tinyint(3) unsigned` NOT NULL.
    ///
    /// Ship-to-shore gantry cranes
    pub crane_count: u8,
    /// `gate_opens_at`, declared `time` NOT NULL.
    ///
    /// Landside gate opening, local time
    pub gate_opens_at: sqlx::mysql::types::MySqlTime,
    /// `gate_closes_at`, declared `time` NOT NULL.
    ///
    /// Landside gate closing, local time
    pub gate_closes_at: sqlx::mysql::types::MySqlTime,
    /// `berth_outline`, declared `polygon` NOT NULL.
    ///
    /// Berth footprint as surveyed
    pub berth_outline: super::Wkb,
    /// `approach_channel`, declared `linestring` NULL.
    ///
    /// Centreline of the dredged approach
    pub approach_channel: Option<super::Wkb>,
    /// `anchorage_points`, declared `multipoint` NULL.
    ///
    /// Designated waiting anchorages
    pub anchorage_points: Option<super::Wkb>,
    /// `fairway_network`, declared `multilinestring` NULL.
    ///
    /// Buoyed fairways serving the facility
    pub fairway_network: Option<super::Wkb>,
    /// `restricted_areas`, declared `multipolygon` NULL.
    ///
    /// Areas closed to commercial traffic
    pub restricted_areas: Option<super::Wkb>,
    /// `reference_marker`, declared `point` NULL.
    ///
    /// Single published reference position
    pub reference_marker: Option<super::Wkb>,
    /// `survey_footprint`, declared `geometry` NULL.
    ///
    /// Latest survey outline, type not fixed in advance
    pub survey_footprint: Option<super::Wkb>,
    /// `notices_to_mariners`, declared `geometrycollection` NULL.
    ///
    /// Mixed geometry accompanying current notices
    pub notices_to_mariners: Option<super::Wkb>,
}

impl PortFacility {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> PortFacilityKey {
        PortFacilityKey {
            facility_id: self.facility_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for PortFacility {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            facility_id: row.try_get("facility_id")?,
            port_id: row.try_get("port_id")?,
            designation: row.try_get("designation")?,
            facility_kind: row.try_get("facility_kind")?,
            quay_length_m: row.try_get("quay_length_m")?,
            crane_count: row.try_get("crane_count")?,
            gate_opens_at: row.try_get("gate_opens_at")?,
            gate_closes_at: row.try_get("gate_closes_at")?,
            berth_outline: row.try_get("berth_outline")?,
            approach_channel: row.try_get("approach_channel")?,
            anchorage_points: row.try_get("anchorage_points")?,
            fairway_network: row.try_get("fairway_network")?,
            restricted_areas: row.try_get("restricted_areas")?,
            reference_marker: row.try_get("reference_marker")?,
            survey_footprint: row.try_get("survey_footprint")?,
            notices_to_mariners: row.try_get("notices_to_mariners")?,
        })
    }
}

/// The primary key of `freight.port_facility`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct PortFacilityKey {
    /// `facility_id`, declared `int(10) unsigned`.
    pub facility_id: u32,
}

/// One column of `freight.port_facility`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: PortFacilityColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum PortFacilityColumn {
    /// `facility_id`, declared `int(10) unsigned`.
    FacilityId,
    /// `port_id`, declared `smallint(5) unsigned`.
    PortId,
    /// `designation`, declared `varchar(80)`.
    Designation,
    /// `facility_kind`, declared `enum('Container terminal','Ro-Ro ramp','Bulk quay','Liquid bulk jetty','Reefer terminal','Lay-by berth')`.
    FacilityKind,
    /// `quay_length_m`, declared `decimal(7,2)`.
    QuayLengthM,
    /// `crane_count`, declared `tinyint(3) unsigned`.
    CraneCount,
    /// `gate_opens_at`, declared `time`.
    GateOpensAt,
    /// `gate_closes_at`, declared `time`.
    GateClosesAt,
    /// `berth_outline`, declared `polygon`.
    BerthOutline,
    /// `approach_channel`, declared `linestring`.
    ApproachChannel,
    /// `anchorage_points`, declared `multipoint`.
    AnchoragePoints,
    /// `fairway_network`, declared `multilinestring`.
    FairwayNetwork,
    /// `restricted_areas`, declared `multipolygon`.
    RestrictedAreas,
    /// `reference_marker`, declared `point`.
    ReferenceMarker,
    /// `survey_footprint`, declared `geometry`.
    SurveyFootprint,
    /// `notices_to_mariners`, declared `geometrycollection`.
    NoticesToMariners,
}

impl PortFacilityColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::FacilityId => "`facility_id`",
            Self::PortId => "`port_id`",
            Self::Designation => "`designation`",
            Self::FacilityKind => "`facility_kind`",
            Self::QuayLengthM => "`quay_length_m`",
            Self::CraneCount => "`crane_count`",
            Self::GateOpensAt => "`gate_opens_at`",
            Self::GateClosesAt => "`gate_closes_at`",
            Self::BerthOutline => "`berth_outline`",
            Self::ApproachChannel => "`approach_channel`",
            Self::AnchoragePoints => "`anchorage_points`",
            Self::FairwayNetwork => "`fairway_network`",
            Self::RestrictedAreas => "`restricted_areas`",
            Self::ReferenceMarker => "`reference_marker`",
            Self::SurveyFootprint => "`survey_footprint`",
            Self::NoticesToMariners => "`notices_to_mariners`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::FacilityId => "facility_id",
            Self::PortId => "port_id",
            Self::Designation => "designation",
            Self::FacilityKind => "facility_kind",
            Self::QuayLengthM => "quay_length_m",
            Self::CraneCount => "crane_count",
            Self::GateOpensAt => "gate_opens_at",
            Self::GateClosesAt => "gate_closes_at",
            Self::BerthOutline => "berth_outline",
            Self::ApproachChannel => "approach_channel",
            Self::AnchoragePoints => "anchorage_points",
            Self::FairwayNetwork => "fairway_network",
            Self::RestrictedAreas => "restricted_areas",
            Self::ReferenceMarker => "reference_marker",
            Self::SurveyFootprint => "survey_footprint",
            Self::NoticesToMariners => "notices_to_mariners",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::FacilityId,
            Self::PortId,
            Self::Designation,
            Self::FacilityKind,
            Self::QuayLengthM,
            Self::CraneCount,
            Self::GateOpensAt,
            Self::GateClosesAt,
            Self::BerthOutline,
            Self::ApproachChannel,
            Self::AnchoragePoints,
            Self::FairwayNetwork,
            Self::RestrictedAreas,
            Self::ReferenceMarker,
            Self::SurveyFootprint,
            Self::NoticesToMariners,
        ]
    }
}

impl std::fmt::Display for PortFacilityColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for PortFacilityColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "facility_id" => Ok(Self::FacilityId),
            "port_id" => Ok(Self::PortId),
            "designation" => Ok(Self::Designation),
            "facility_kind" => Ok(Self::FacilityKind),
            "quay_length_m" => Ok(Self::QuayLengthM),
            "crane_count" => Ok(Self::CraneCount),
            "gate_opens_at" => Ok(Self::GateOpensAt),
            "gate_closes_at" => Ok(Self::GateClosesAt),
            "berth_outline" => Ok(Self::BerthOutline),
            "approach_channel" => Ok(Self::ApproachChannel),
            "anchorage_points" => Ok(Self::AnchoragePoints),
            "fairway_network" => Ok(Self::FairwayNetwork),
            "restricted_areas" => Ok(Self::RestrictedAreas),
            "reference_marker" => Ok(Self::ReferenceMarker),
            "survey_footprint" => Ok(Self::SurveyFootprint),
            "notices_to_mariners" => Ok(Self::NoticesToMariners),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.port_facility",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.port_facility`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct PortFacilityOrder {
    /// The column to order by.
    pub column: PortFacilityColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.port_facility`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct PortFacilityFilter {
    /// Where set, requires `facility_id` to equal it.
    pub facility_id: Option<u32>,
    /// Where set, requires `facility_id` to be at least it.
    pub facility_id_from: Option<u32>,
    /// Where set, requires `facility_id` to be at most it.
    pub facility_id_to: Option<u32>,
    /// Where set, requires `port_id` to equal it.
    pub port_id: Option<u16>,
    /// Where set, requires `port_id` to be at least it.
    pub port_id_from: Option<u16>,
    /// Where set, requires `port_id` to be at most it.
    pub port_id_to: Option<u16>,
    /// Where set, requires `designation` to equal it.
    pub designation: Option<String>,
    /// Where set, requires `designation` to match it, as `LIKE` matches.
    pub designation_like: Option<String>,
    /// Where set, requires `facility_kind` to equal it.
    pub facility_kind: Option<String>,
    /// Where set, requires `facility_kind` to match it, as `LIKE` matches.
    pub facility_kind_like: Option<String>,
    /// Where set, requires `quay_length_m` to equal it.
    pub quay_length_m: Option<super::Decimal>,
    /// Where set, requires `quay_length_m` to be at least it.
    pub quay_length_m_from: Option<super::Decimal>,
    /// Where set, requires `quay_length_m` to be at most it.
    pub quay_length_m_to: Option<super::Decimal>,
    /// Where set, requires `crane_count` to equal it.
    pub crane_count: Option<u8>,
    /// Where set, requires `crane_count` to be at least it.
    pub crane_count_from: Option<u8>,
    /// Where set, requires `crane_count` to be at most it.
    pub crane_count_to: Option<u8>,
    /// Where set, requires `gate_opens_at` to equal it.
    pub gate_opens_at: Option<sqlx::mysql::types::MySqlTime>,
    /// Where set, requires `gate_opens_at` to be at least it.
    pub gate_opens_at_from: Option<sqlx::mysql::types::MySqlTime>,
    /// Where set, requires `gate_opens_at` to be at most it.
    pub gate_opens_at_to: Option<sqlx::mysql::types::MySqlTime>,
    /// Where set, requires `gate_closes_at` to equal it.
    pub gate_closes_at: Option<sqlx::mysql::types::MySqlTime>,
    /// Where set, requires `gate_closes_at` to be at least it.
    pub gate_closes_at_from: Option<sqlx::mysql::types::MySqlTime>,
    /// Where set, requires `gate_closes_at` to be at most it.
    pub gate_closes_at_to: Option<sqlx::mysql::types::MySqlTime>,
    /// Where set, requires `berth_outline` to equal it.
    pub berth_outline: Option<super::Wkb>,
    /// Where set, requires `approach_channel` to equal it.
    pub approach_channel: Option<super::Wkb>,
    /// Where set, requires `approach_channel` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub approach_channel_is_null: Option<bool>,
    /// Where set, requires `anchorage_points` to equal it.
    pub anchorage_points: Option<super::Wkb>,
    /// Where set, requires `anchorage_points` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub anchorage_points_is_null: Option<bool>,
    /// Where set, requires `fairway_network` to equal it.
    pub fairway_network: Option<super::Wkb>,
    /// Where set, requires `fairway_network` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub fairway_network_is_null: Option<bool>,
    /// Where set, requires `restricted_areas` to equal it.
    pub restricted_areas: Option<super::Wkb>,
    /// Where set, requires `restricted_areas` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub restricted_areas_is_null: Option<bool>,
    /// Where set, requires `reference_marker` to equal it.
    pub reference_marker: Option<super::Wkb>,
    /// Where set, requires `reference_marker` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub reference_marker_is_null: Option<bool>,
    /// Where set, requires `survey_footprint` to equal it.
    pub survey_footprint: Option<super::Wkb>,
    /// Where set, requires `survey_footprint` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub survey_footprint_is_null: Option<bool>,
    /// Where set, requires `notices_to_mariners` to equal it.
    pub notices_to_mariners: Option<super::Wkb>,
    /// Where set, requires `notices_to_mariners` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub notices_to_mariners_is_null: Option<bool>,
}

impl PortFacilityFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.facility_id.is_none()
            && self.facility_id_from.is_none()
            && self.facility_id_to.is_none()
            && self.port_id.is_none()
            && self.port_id_from.is_none()
            && self.port_id_to.is_none()
            && self.designation.is_none()
            && self.designation_like.is_none()
            && self.facility_kind.is_none()
            && self.facility_kind_like.is_none()
            && self.quay_length_m.is_none()
            && self.quay_length_m_from.is_none()
            && self.quay_length_m_to.is_none()
            && self.crane_count.is_none()
            && self.crane_count_from.is_none()
            && self.crane_count_to.is_none()
            && self.gate_opens_at.is_none()
            && self.gate_opens_at_from.is_none()
            && self.gate_opens_at_to.is_none()
            && self.gate_closes_at.is_none()
            && self.gate_closes_at_from.is_none()
            && self.gate_closes_at_to.is_none()
            && self.berth_outline.is_none()
            && self.approach_channel.is_none()
            && self.approach_channel_is_null.is_none()
            && self.anchorage_points.is_none()
            && self.anchorage_points_is_null.is_none()
            && self.fairway_network.is_none()
            && self.fairway_network_is_null.is_none()
            && self.restricted_areas.is_none()
            && self.restricted_areas_is_null.is_none()
            && self.reference_marker.is_none()
            && self.reference_marker_is_null.is_none()
            && self.survey_footprint.is_none()
            && self.survey_footprint_is_null.is_none()
            && self.notices_to_mariners.is_none()
            && self.notices_to_mariners_is_null.is_none()
    }
}
