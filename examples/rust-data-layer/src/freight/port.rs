// Code generated by tpl from freight.port. DO NOT EDIT.

//! The `freight.port` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.port`.
///
/// Portos marítimos, respectivos códigos UN/LOCODE e características de acostagem — ver anexo técnico do manual de operações
#[derive(Debug, Clone)]
pub struct Port {
    /// `port_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    ///
    /// Surrogate key; UN/LOCODE is the natural key
    pub port_id: u16,
    /// `un_locode`, declared `char(5)` NOT NULL.
    ///
    /// It is part of a unique index.
    ///
    /// UN/LOCODE, e.g. PTLIS — two-letter country plus three-letter location
    pub un_locode: String,
    /// `name`, declared `varchar(120)` NOT NULL.
    ///
    /// Port name as published by the port authority
    pub name: String,
    /// `country_code`, declared `char(2)` NOT NULL.
    ///
    /// ISO 3166-1 alpha-2; ASCII by definition, so stored as ASCII
    pub country_code: String,
    /// `local_name`, declared `varchar(120)` NULL.
    ///
    /// Name in the local language — binary collation, since Ålesund and Alesund are different ports
    pub local_name: Option<String>,
    /// `timezone_name`, declared `varchar(64)` NOT NULL.
    ///
    /// IANA time zone identifier
    pub timezone_name: String,
    /// `utc_offset_minutes`, declared `smallint(6)` NOT NULL.
    ///
    /// Standard-time offset in minutes; negative west of Greenwich
    pub utc_offset_minutes: i16,
    /// `berth_count`, declared `tinyint(3) unsigned` NOT NULL.
    ///
    /// Number of commercial berths
    pub berth_count: u8,
    /// `max_draught_m`, declared `decimal(5,2)` NOT NULL.
    ///
    /// Maximum permitted draught at the deepest berth, in metres
    pub max_draught_m: super::Decimal,
    /// `tidal_range_m`, declared `float` NULL.
    ///
    /// Mean spring tidal range, in metres
    pub tidal_range_m: Option<f32>,
    /// `annual_throughput_teu`, declared `int(10) unsigned` NULL.
    ///
    /// Container throughput for the last reported calendar year
    pub annual_throughput_teu: Option<u32>,
    /// `customs_round_clock`, declared `bit(1)` NOT NULL.
    ///
    /// Customs desk staffed 24/7
    pub customs_round_clock: u64,
    /// `harbour_dues_currency`, declared `char(3)` NOT NULL.
    ///
    /// ISO 4217 code in which harbour dues are invoiced
    pub harbour_dues_currency: String,
    /// `approach_notes`, declared `text` NULL.
    ///
    /// Free text from the pilot book
    pub approach_notes: Option<String>,
}

impl Port {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> PortKey {
        PortKey {
            port_id: self.port_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Port {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            port_id: row.try_get("port_id")?,
            un_locode: row.try_get("un_locode")?,
            name: row.try_get("name")?,
            country_code: row.try_get("country_code")?,
            local_name: row.try_get("local_name")?,
            timezone_name: row.try_get("timezone_name")?,
            utc_offset_minutes: row.try_get("utc_offset_minutes")?,
            berth_count: row.try_get("berth_count")?,
            max_draught_m: row.try_get("max_draught_m")?,
            tidal_range_m: row.try_get("tidal_range_m")?,
            annual_throughput_teu: row.try_get("annual_throughput_teu")?,
            customs_round_clock: row.try_get("customs_round_clock")?,
            harbour_dues_currency: row.try_get("harbour_dues_currency")?,
            approach_notes: row.try_get("approach_notes")?,
        })
    }
}

/// The primary key of `freight.port`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct PortKey {
    /// `port_id`, declared `smallint(5) unsigned`.
    pub port_id: u16,
}

/// One column of `freight.port`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: PortColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum PortColumn {
    /// `port_id`, declared `smallint(5) unsigned`.
    PortId,
    /// `un_locode`, declared `char(5)`.
    UnLocode,
    /// `name`, declared `varchar(120)`.
    Name,
    /// `country_code`, declared `char(2)`.
    CountryCode,
    /// `local_name`, declared `varchar(120)`.
    LocalName,
    /// `timezone_name`, declared `varchar(64)`.
    TimezoneName,
    /// `utc_offset_minutes`, declared `smallint(6)`.
    UtcOffsetMinutes,
    /// `berth_count`, declared `tinyint(3) unsigned`.
    BerthCount,
    /// `max_draught_m`, declared `decimal(5,2)`.
    MaxDraughtM,
    /// `tidal_range_m`, declared `float`.
    TidalRangeM,
    /// `annual_throughput_teu`, declared `int(10) unsigned`.
    AnnualThroughputTeu,
    /// `customs_round_clock`, declared `bit(1)`.
    CustomsRoundClock,
    /// `harbour_dues_currency`, declared `char(3)`.
    HarbourDuesCurrency,
    /// `approach_notes`, declared `text`.
    ApproachNotes,
}

impl PortColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::PortId => "`port_id`",
            Self::UnLocode => "`un_locode`",
            Self::Name => "`name`",
            Self::CountryCode => "`country_code`",
            Self::LocalName => "`local_name`",
            Self::TimezoneName => "`timezone_name`",
            Self::UtcOffsetMinutes => "`utc_offset_minutes`",
            Self::BerthCount => "`berth_count`",
            Self::MaxDraughtM => "`max_draught_m`",
            Self::TidalRangeM => "`tidal_range_m`",
            Self::AnnualThroughputTeu => "`annual_throughput_teu`",
            Self::CustomsRoundClock => "`customs_round_clock`",
            Self::HarbourDuesCurrency => "`harbour_dues_currency`",
            Self::ApproachNotes => "`approach_notes`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::PortId => "port_id",
            Self::UnLocode => "un_locode",
            Self::Name => "name",
            Self::CountryCode => "country_code",
            Self::LocalName => "local_name",
            Self::TimezoneName => "timezone_name",
            Self::UtcOffsetMinutes => "utc_offset_minutes",
            Self::BerthCount => "berth_count",
            Self::MaxDraughtM => "max_draught_m",
            Self::TidalRangeM => "tidal_range_m",
            Self::AnnualThroughputTeu => "annual_throughput_teu",
            Self::CustomsRoundClock => "customs_round_clock",
            Self::HarbourDuesCurrency => "harbour_dues_currency",
            Self::ApproachNotes => "approach_notes",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::PortId,
            Self::UnLocode,
            Self::Name,
            Self::CountryCode,
            Self::LocalName,
            Self::TimezoneName,
            Self::UtcOffsetMinutes,
            Self::BerthCount,
            Self::MaxDraughtM,
            Self::TidalRangeM,
            Self::AnnualThroughputTeu,
            Self::CustomsRoundClock,
            Self::HarbourDuesCurrency,
            Self::ApproachNotes,
        ]
    }
}

impl std::fmt::Display for PortColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for PortColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "port_id" => Ok(Self::PortId),
            "un_locode" => Ok(Self::UnLocode),
            "name" => Ok(Self::Name),
            "country_code" => Ok(Self::CountryCode),
            "local_name" => Ok(Self::LocalName),
            "timezone_name" => Ok(Self::TimezoneName),
            "utc_offset_minutes" => Ok(Self::UtcOffsetMinutes),
            "berth_count" => Ok(Self::BerthCount),
            "max_draught_m" => Ok(Self::MaxDraughtM),
            "tidal_range_m" => Ok(Self::TidalRangeM),
            "annual_throughput_teu" => Ok(Self::AnnualThroughputTeu),
            "customs_round_clock" => Ok(Self::CustomsRoundClock),
            "harbour_dues_currency" => Ok(Self::HarbourDuesCurrency),
            "approach_notes" => Ok(Self::ApproachNotes),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.port",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.port`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct PortOrder {
    /// The column to order by.
    pub column: PortColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.port`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct PortFilter {
    /// Where set, requires `port_id` to equal it.
    pub port_id: Option<u16>,
    /// Where set, requires `port_id` to be at least it.
    pub port_id_from: Option<u16>,
    /// Where set, requires `port_id` to be at most it.
    pub port_id_to: Option<u16>,
    /// Where set, requires `un_locode` to equal it.
    pub un_locode: Option<String>,
    /// Where set, requires `un_locode` to match it, as `LIKE` matches.
    pub un_locode_like: Option<String>,
    /// Where set, requires `name` to equal it.
    pub name: Option<String>,
    /// Where set, requires `name` to match it, as `LIKE` matches.
    pub name_like: Option<String>,
    /// Where set, requires `country_code` to equal it.
    pub country_code: Option<String>,
    /// Where set, requires `country_code` to match it, as `LIKE` matches.
    pub country_code_like: Option<String>,
    /// Where set, requires `local_name` to equal it.
    pub local_name: Option<String>,
    /// Where set, requires `local_name` to match it, as `LIKE` matches.
    pub local_name_like: Option<String>,
    /// Where set, requires `local_name` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub local_name_is_null: Option<bool>,
    /// Where set, requires `timezone_name` to equal it.
    pub timezone_name: Option<String>,
    /// Where set, requires `timezone_name` to match it, as `LIKE` matches.
    pub timezone_name_like: Option<String>,
    /// Where set, requires `utc_offset_minutes` to equal it.
    pub utc_offset_minutes: Option<i16>,
    /// Where set, requires `utc_offset_minutes` to be at least it.
    pub utc_offset_minutes_from: Option<i16>,
    /// Where set, requires `utc_offset_minutes` to be at most it.
    pub utc_offset_minutes_to: Option<i16>,
    /// Where set, requires `berth_count` to equal it.
    pub berth_count: Option<u8>,
    /// Where set, requires `berth_count` to be at least it.
    pub berth_count_from: Option<u8>,
    /// Where set, requires `berth_count` to be at most it.
    pub berth_count_to: Option<u8>,
    /// Where set, requires `max_draught_m` to equal it.
    pub max_draught_m: Option<super::Decimal>,
    /// Where set, requires `max_draught_m` to be at least it.
    pub max_draught_m_from: Option<super::Decimal>,
    /// Where set, requires `max_draught_m` to be at most it.
    pub max_draught_m_to: Option<super::Decimal>,
    /// Where set, requires `tidal_range_m` to equal it.
    pub tidal_range_m: Option<f32>,
    /// Where set, requires `tidal_range_m` to be at least it.
    pub tidal_range_m_from: Option<f32>,
    /// Where set, requires `tidal_range_m` to be at most it.
    pub tidal_range_m_to: Option<f32>,
    /// Where set, requires `tidal_range_m` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub tidal_range_m_is_null: Option<bool>,
    /// Where set, requires `annual_throughput_teu` to equal it.
    pub annual_throughput_teu: Option<u32>,
    /// Where set, requires `annual_throughput_teu` to be at least it.
    pub annual_throughput_teu_from: Option<u32>,
    /// Where set, requires `annual_throughput_teu` to be at most it.
    pub annual_throughput_teu_to: Option<u32>,
    /// Where set, requires `annual_throughput_teu` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub annual_throughput_teu_is_null: Option<bool>,
    /// Where set, requires `customs_round_clock` to equal it.
    pub customs_round_clock: Option<u64>,
    /// Where set, requires `customs_round_clock` to be at least it.
    pub customs_round_clock_from: Option<u64>,
    /// Where set, requires `customs_round_clock` to be at most it.
    pub customs_round_clock_to: Option<u64>,
    /// Where set, requires `harbour_dues_currency` to equal it.
    pub harbour_dues_currency: Option<String>,
    /// Where set, requires `harbour_dues_currency` to match it, as `LIKE` matches.
    pub harbour_dues_currency_like: Option<String>,
    /// Where set, requires `approach_notes` to equal it.
    pub approach_notes: Option<String>,
    /// Where set, requires `approach_notes` to match it, as `LIKE` matches.
    pub approach_notes_like: Option<String>,
    /// Where set, requires `approach_notes` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub approach_notes_is_null: Option<bool>,
}

impl PortFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.port_id.is_none()
            && self.port_id_from.is_none()
            && self.port_id_to.is_none()
            && self.un_locode.is_none()
            && self.un_locode_like.is_none()
            && self.name.is_none()
            && self.name_like.is_none()
            && self.country_code.is_none()
            && self.country_code_like.is_none()
            && self.local_name.is_none()
            && self.local_name_like.is_none()
            && self.local_name_is_null.is_none()
            && self.timezone_name.is_none()
            && self.timezone_name_like.is_none()
            && self.utc_offset_minutes.is_none()
            && self.utc_offset_minutes_from.is_none()
            && self.utc_offset_minutes_to.is_none()
            && self.berth_count.is_none()
            && self.berth_count_from.is_none()
            && self.berth_count_to.is_none()
            && self.max_draught_m.is_none()
            && self.max_draught_m_from.is_none()
            && self.max_draught_m_to.is_none()
            && self.tidal_range_m.is_none()
            && self.tidal_range_m_from.is_none()
            && self.tidal_range_m_to.is_none()
            && self.tidal_range_m_is_null.is_none()
            && self.annual_throughput_teu.is_none()
            && self.annual_throughput_teu_from.is_none()
            && self.annual_throughput_teu_to.is_none()
            && self.annual_throughput_teu_is_null.is_none()
            && self.customs_round_clock.is_none()
            && self.customs_round_clock_from.is_none()
            && self.customs_round_clock_to.is_none()
            && self.harbour_dues_currency.is_none()
            && self.harbour_dues_currency_like.is_none()
            && self.approach_notes.is_none()
            && self.approach_notes_like.is_none()
            && self.approach_notes_is_null.is_none()
    }
}
