// Code generated by tpl from freight.audit_event. DO NOT EDIT.

//! The `freight.audit_event` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.audit_event`.
///
/// Trilho de auditoria escrito pelos gatilhos; sobrevive à remoção das linhas que regista
#[derive(Debug, Clone)]
pub struct AuditEvent {
    /// `audit_event_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub audit_event_id: u64,
    /// `entity_name`, declared `varchar(64)` NOT NULL.
    pub entity_name: String,
    /// `entity_key`, declared `varchar(64)` NOT NULL.
    pub entity_key: String,
    /// `action`, declared `enum('INSERT','UPDATE','DELETE')` NOT NULL.
    pub action: String,
    /// `trigger_timing`, declared `enum('BEFORE','AFTER')` NOT NULL.
    pub trigger_timing: String,
    /// `changed_column`, declared `varchar(64)` NULL.
    pub changed_column: Option<String>,
    /// `old_value`, declared `varchar(255)` NULL.
    pub old_value: Option<String>,
    /// `new_value`, declared `varchar(255)` NULL.
    pub new_value: Option<String>,
    /// `actor`, declared `varchar(96)` NOT NULL.
    pub actor: String,
    /// `client_ipv4`, declared `inet4` NULL.
    pub client_ipv4: Option<std::net::Ipv4Addr>,
    /// `client_ipv6`, declared `inet6` NULL.
    pub client_ipv6: Option<std::net::Ipv6Addr>,
    /// `correlation_uuid`, declared `uuid` NULL.
    pub correlation_uuid: Option<String>,
    /// `observed_at`, declared `datetime(6)` NOT NULL.
    pub observed_at: sqlx::types::chrono::NaiveDateTime,
    /// `detail`, declared `text` NULL.
    pub detail: Option<String>,
}

impl AuditEvent {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> AuditEventKey {
        AuditEventKey {
            audit_event_id: self.audit_event_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for AuditEvent {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            audit_event_id: row.try_get("audit_event_id")?,
            entity_name: row.try_get("entity_name")?,
            entity_key: row.try_get("entity_key")?,
            action: row.try_get("action")?,
            trigger_timing: row.try_get("trigger_timing")?,
            changed_column: row.try_get("changed_column")?,
            old_value: row.try_get("old_value")?,
            new_value: row.try_get("new_value")?,
            actor: row.try_get("actor")?,
            client_ipv4: row.try_get("client_ipv4")?,
            client_ipv6: row.try_get("client_ipv6")?,
            correlation_uuid: row.try_get("correlation_uuid")?,
            observed_at: row.try_get("observed_at")?,
            detail: row.try_get("detail")?,
        })
    }
}

/// The primary key of `freight.audit_event`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct AuditEventKey {
    /// `audit_event_id`, declared `bigint(20) unsigned`.
    pub audit_event_id: u64,
}

/// One column of `freight.audit_event`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: AuditEventColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum AuditEventColumn {
    /// `audit_event_id`, declared `bigint(20) unsigned`.
    AuditEventId,
    /// `entity_name`, declared `varchar(64)`.
    EntityName,
    /// `entity_key`, declared `varchar(64)`.
    EntityKey,
    /// `action`, declared `enum('INSERT','UPDATE','DELETE')`.
    Action,
    /// `trigger_timing`, declared `enum('BEFORE','AFTER')`.
    TriggerTiming,
    /// `changed_column`, declared `varchar(64)`.
    ChangedColumn,
    /// `old_value`, declared `varchar(255)`.
    OldValue,
    /// `new_value`, declared `varchar(255)`.
    NewValue,
    /// `actor`, declared `varchar(96)`.
    Actor,
    /// `client_ipv4`, declared `inet4`.
    ClientIpv4,
    /// `client_ipv6`, declared `inet6`.
    ClientIpv6,
    /// `correlation_uuid`, declared `uuid`.
    CorrelationUuid,
    /// `observed_at`, declared `datetime(6)`.
    ObservedAt,
    /// `detail`, declared `text`.
    Detail,
}

impl AuditEventColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::AuditEventId => "`audit_event_id`",
            Self::EntityName => "`entity_name`",
            Self::EntityKey => "`entity_key`",
            Self::Action => "`action`",
            Self::TriggerTiming => "`trigger_timing`",
            Self::ChangedColumn => "`changed_column`",
            Self::OldValue => "`old_value`",
            Self::NewValue => "`new_value`",
            Self::Actor => "`actor`",
            Self::ClientIpv4 => "`client_ipv4`",
            Self::ClientIpv6 => "`client_ipv6`",
            Self::CorrelationUuid => "`correlation_uuid`",
            Self::ObservedAt => "`observed_at`",
            Self::Detail => "`detail`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::AuditEventId => "audit_event_id",
            Self::EntityName => "entity_name",
            Self::EntityKey => "entity_key",
            Self::Action => "action",
            Self::TriggerTiming => "trigger_timing",
            Self::ChangedColumn => "changed_column",
            Self::OldValue => "old_value",
            Self::NewValue => "new_value",
            Self::Actor => "actor",
            Self::ClientIpv4 => "client_ipv4",
            Self::ClientIpv6 => "client_ipv6",
            Self::CorrelationUuid => "correlation_uuid",
            Self::ObservedAt => "observed_at",
            Self::Detail => "detail",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::AuditEventId,
            Self::EntityName,
            Self::EntityKey,
            Self::Action,
            Self::TriggerTiming,
            Self::ChangedColumn,
            Self::OldValue,
            Self::NewValue,
            Self::Actor,
            Self::ClientIpv4,
            Self::ClientIpv6,
            Self::CorrelationUuid,
            Self::ObservedAt,
            Self::Detail,
        ]
    }
}

impl std::fmt::Display for AuditEventColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for AuditEventColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "audit_event_id" => Ok(Self::AuditEventId),
            "entity_name" => Ok(Self::EntityName),
            "entity_key" => Ok(Self::EntityKey),
            "action" => Ok(Self::Action),
            "trigger_timing" => Ok(Self::TriggerTiming),
            "changed_column" => Ok(Self::ChangedColumn),
            "old_value" => Ok(Self::OldValue),
            "new_value" => Ok(Self::NewValue),
            "actor" => Ok(Self::Actor),
            "client_ipv4" => Ok(Self::ClientIpv4),
            "client_ipv6" => Ok(Self::ClientIpv6),
            "correlation_uuid" => Ok(Self::CorrelationUuid),
            "observed_at" => Ok(Self::ObservedAt),
            "detail" => Ok(Self::Detail),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.audit_event",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.audit_event`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct AuditEventOrder {
    /// The column to order by.
    pub column: AuditEventColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.audit_event`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct AuditEventFilter {
    /// Where set, requires `audit_event_id` to equal it.
    pub audit_event_id: Option<u64>,
    /// Where set, requires `audit_event_id` to be at least it.
    pub audit_event_id_from: Option<u64>,
    /// Where set, requires `audit_event_id` to be at most it.
    pub audit_event_id_to: Option<u64>,
    /// Where set, requires `entity_name` to equal it.
    pub entity_name: Option<String>,
    /// Where set, requires `entity_name` to match it, as `LIKE` matches.
    pub entity_name_like: Option<String>,
    /// Where set, requires `entity_key` to equal it.
    pub entity_key: Option<String>,
    /// Where set, requires `entity_key` to match it, as `LIKE` matches.
    pub entity_key_like: Option<String>,
    /// Where set, requires `action` to equal it.
    pub action: Option<String>,
    /// Where set, requires `action` to match it, as `LIKE` matches.
    pub action_like: Option<String>,
    /// Where set, requires `trigger_timing` to equal it.
    pub trigger_timing: Option<String>,
    /// Where set, requires `trigger_timing` to match it, as `LIKE` matches.
    pub trigger_timing_like: Option<String>,
    /// Where set, requires `changed_column` to equal it.
    pub changed_column: Option<String>,
    /// Where set, requires `changed_column` to match it, as `LIKE` matches.
    pub changed_column_like: Option<String>,
    /// Where set, requires `changed_column` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub changed_column_is_null: Option<bool>,
    /// Where set, requires `old_value` to equal it.
    pub old_value: Option<String>,
    /// Where set, requires `old_value` to match it, as `LIKE` matches.
    pub old_value_like: Option<String>,
    /// Where set, requires `old_value` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub old_value_is_null: Option<bool>,
    /// Where set, requires `new_value` to equal it.
    pub new_value: Option<String>,
    /// Where set, requires `new_value` to match it, as `LIKE` matches.
    pub new_value_like: Option<String>,
    /// Where set, requires `new_value` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub new_value_is_null: Option<bool>,
    /// Where set, requires `actor` to equal it.
    pub actor: Option<String>,
    /// Where set, requires `actor` to match it, as `LIKE` matches.
    pub actor_like: Option<String>,
    /// Where set, requires `client_ipv4` to equal it.
    pub client_ipv4: Option<std::net::Ipv4Addr>,
    /// Where set, requires `client_ipv4` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub client_ipv4_is_null: Option<bool>,
    /// Where set, requires `client_ipv6` to equal it.
    pub client_ipv6: Option<std::net::Ipv6Addr>,
    /// Where set, requires `client_ipv6` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub client_ipv6_is_null: Option<bool>,
    /// Where set, requires `correlation_uuid` to equal it.
    pub correlation_uuid: Option<String>,
    /// Where set, requires `correlation_uuid` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub correlation_uuid_is_null: Option<bool>,
    /// Where set, requires `observed_at` to equal it.
    pub observed_at: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `observed_at` to be at least it.
    pub observed_at_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `observed_at` to be at most it.
    pub observed_at_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `detail` to equal it.
    pub detail: Option<String>,
    /// Where set, requires `detail` to match it, as `LIKE` matches.
    pub detail_like: Option<String>,
    /// Where set, requires `detail` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub detail_is_null: Option<bool>,
}

impl AuditEventFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.audit_event_id.is_none()
            && self.audit_event_id_from.is_none()
            && self.audit_event_id_to.is_none()
            && self.entity_name.is_none()
            && self.entity_name_like.is_none()
            && self.entity_key.is_none()
            && self.entity_key_like.is_none()
            && self.action.is_none()
            && self.action_like.is_none()
            && self.trigger_timing.is_none()
            && self.trigger_timing_like.is_none()
            && self.changed_column.is_none()
            && self.changed_column_like.is_none()
            && self.changed_column_is_null.is_none()
            && self.old_value.is_none()
            && self.old_value_like.is_none()
            && self.old_value_is_null.is_none()
            && self.new_value.is_none()
            && self.new_value_like.is_none()
            && self.new_value_is_null.is_none()
            && self.actor.is_none()
            && self.actor_like.is_none()
            && self.client_ipv4.is_none()
            && self.client_ipv4_is_null.is_none()
            && self.client_ipv6.is_none()
            && self.client_ipv6_is_null.is_none()
            && self.correlation_uuid.is_none()
            && self.correlation_uuid_is_null.is_none()
            && self.observed_at.is_none()
            && self.observed_at_from.is_none()
            && self.observed_at_to.is_none()
            && self.detail.is_none()
            && self.detail_like.is_none()
            && self.detail_is_null.is_none()
    }
}
