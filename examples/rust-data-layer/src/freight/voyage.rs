// Code generated by tpl from freight.voyage. DO NOT EDIT.

//! The `freight.voyage` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.voyage`.
///
/// Viagens programadas por navio e número de viagem
#[derive(Debug, Clone)]
pub struct Voyage {
    /// `vessel_imo`, declared `char(7)` NOT NULL.
    ///
    /// It is part of the primary key.
    pub vessel_imo: String,
    /// `voyage_number`, declared `varchar(12)` NOT NULL.
    ///
    /// It is part of the primary key.
    ///
    /// Carrier voyage number, e.g. 2026-014W; W and E denote direction
    pub voyage_number: String,
    /// `service_code`, declared `char(4)` NOT NULL.
    ///
    /// Liner service the voyage is operated under
    pub service_code: String,
    /// `etd`, declared `datetime` NOT NULL.
    ///
    /// Estimated time of departure from the first load port
    pub etd: sqlx::types::chrono::NaiveDateTime,
    /// `eta`, declared `datetime` NOT NULL.
    ///
    /// Estimated time of arrival at the last discharge port
    pub eta: sqlx::types::chrono::NaiveDateTime,
    /// `atd`, declared `datetime` NULL.
    ///
    /// Actual time of departure; NULL until the vessel sails
    pub atd: Option<sqlx::types::chrono::NaiveDateTime>,
    /// `status`, declared `enum('Planned','Announced','Sailing','Completed','Cancelled')` NOT NULL.
    pub status: String,
    /// `bunker_price_usd_t`, declared `decimal(9,2)` NULL.
    ///
    /// VLSFO price used for the bunker adjustment factor
    pub bunker_price_usd_t: Option<super::Decimal>,
    /// `slot_allocation_teu`, declared `mediumint(8) unsigned` NOT NULL.
    pub slot_allocation_teu: u32,
    /// `utilisation_pct`, declared `decimal(5,2)` NULL.
    pub utilisation_pct: Option<super::Decimal>,
}

impl Voyage {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> VoyageKey {
        VoyageKey {
            vessel_imo: self.vessel_imo.clone(),
            voyage_number: self.voyage_number.clone(),
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Voyage {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            vessel_imo: row.try_get("vessel_imo")?,
            voyage_number: row.try_get("voyage_number")?,
            service_code: row.try_get("service_code")?,
            etd: row.try_get("etd")?,
            eta: row.try_get("eta")?,
            atd: row.try_get("atd")?,
            status: row.try_get("status")?,
            bunker_price_usd_t: row.try_get("bunker_price_usd_t")?,
            slot_allocation_teu: row.try_get("slot_allocation_teu")?,
            utilisation_pct: row.try_get("utilisation_pct")?,
        })
    }
}

/// The primary key of `freight.voyage`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct VoyageKey {
    /// `vessel_imo`, declared `char(7)`.
    pub vessel_imo: String,
    /// `voyage_number`, declared `varchar(12)`.
    pub voyage_number: String,
}

/// One column of `freight.voyage`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: VoyageColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum VoyageColumn {
    /// `vessel_imo`, declared `char(7)`.
    VesselImo,
    /// `voyage_number`, declared `varchar(12)`.
    VoyageNumber,
    /// `service_code`, declared `char(4)`.
    ServiceCode,
    /// `etd`, declared `datetime`.
    Etd,
    /// `eta`, declared `datetime`.
    Eta,
    /// `atd`, declared `datetime`.
    Atd,
    /// `status`, declared `enum('Planned','Announced','Sailing','Completed','Cancelled')`.
    Status,
    /// `bunker_price_usd_t`, declared `decimal(9,2)`.
    BunkerPriceUsdT,
    /// `slot_allocation_teu`, declared `mediumint(8) unsigned`.
    SlotAllocationTeu,
    /// `utilisation_pct`, declared `decimal(5,2)`.
    UtilisationPct,
}

impl VoyageColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::VesselImo => "`vessel_imo`",
            Self::VoyageNumber => "`voyage_number`",
            Self::ServiceCode => "`service_code`",
            Self::Etd => "`etd`",
            Self::Eta => "`eta`",
            Self::Atd => "`atd`",
            Self::Status => "`status`",
            Self::BunkerPriceUsdT => "`bunker_price_usd_t`",
            Self::SlotAllocationTeu => "`slot_allocation_teu`",
            Self::UtilisationPct => "`utilisation_pct`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::VesselImo => "vessel_imo",
            Self::VoyageNumber => "voyage_number",
            Self::ServiceCode => "service_code",
            Self::Etd => "etd",
            Self::Eta => "eta",
            Self::Atd => "atd",
            Self::Status => "status",
            Self::BunkerPriceUsdT => "bunker_price_usd_t",
            Self::SlotAllocationTeu => "slot_allocation_teu",
            Self::UtilisationPct => "utilisation_pct",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::VesselImo,
            Self::VoyageNumber,
            Self::ServiceCode,
            Self::Etd,
            Self::Eta,
            Self::Atd,
            Self::Status,
            Self::BunkerPriceUsdT,
            Self::SlotAllocationTeu,
            Self::UtilisationPct,
        ]
    }
}

impl std::fmt::Display for VoyageColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for VoyageColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "vessel_imo" => Ok(Self::VesselImo),
            "voyage_number" => Ok(Self::VoyageNumber),
            "service_code" => Ok(Self::ServiceCode),
            "etd" => Ok(Self::Etd),
            "eta" => Ok(Self::Eta),
            "atd" => Ok(Self::Atd),
            "status" => Ok(Self::Status),
            "bunker_price_usd_t" => Ok(Self::BunkerPriceUsdT),
            "slot_allocation_teu" => Ok(Self::SlotAllocationTeu),
            "utilisation_pct" => Ok(Self::UtilisationPct),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.voyage",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.voyage`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct VoyageOrder {
    /// The column to order by.
    pub column: VoyageColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.voyage`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct VoyageFilter {
    /// Where set, requires `vessel_imo` to equal it.
    pub vessel_imo: Option<String>,
    /// Where set, requires `vessel_imo` to match it, as `LIKE` matches.
    pub vessel_imo_like: Option<String>,
    /// Where set, requires `voyage_number` to equal it.
    pub voyage_number: Option<String>,
    /// Where set, requires `voyage_number` to match it, as `LIKE` matches.
    pub voyage_number_like: Option<String>,
    /// Where set, requires `service_code` to equal it.
    pub service_code: Option<String>,
    /// Where set, requires `service_code` to match it, as `LIKE` matches.
    pub service_code_like: Option<String>,
    /// Where set, requires `etd` to equal it.
    pub etd: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `etd` to be at least it.
    pub etd_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `etd` to be at most it.
    pub etd_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `eta` to equal it.
    pub eta: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `eta` to be at least it.
    pub eta_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `eta` to be at most it.
    pub eta_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `atd` to equal it.
    pub atd: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `atd` to be at least it.
    pub atd_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `atd` to be at most it.
    pub atd_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `atd` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub atd_is_null: Option<bool>,
    /// Where set, requires `status` to equal it.
    pub status: Option<String>,
    /// Where set, requires `status` to match it, as `LIKE` matches.
    pub status_like: Option<String>,
    /// Where set, requires `bunker_price_usd_t` to equal it.
    pub bunker_price_usd_t: Option<super::Decimal>,
    /// Where set, requires `bunker_price_usd_t` to be at least it.
    pub bunker_price_usd_t_from: Option<super::Decimal>,
    /// Where set, requires `bunker_price_usd_t` to be at most it.
    pub bunker_price_usd_t_to: Option<super::Decimal>,
    /// Where set, requires `bunker_price_usd_t` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub bunker_price_usd_t_is_null: Option<bool>,
    /// Where set, requires `slot_allocation_teu` to equal it.
    pub slot_allocation_teu: Option<u32>,
    /// Where set, requires `slot_allocation_teu` to be at least it.
    pub slot_allocation_teu_from: Option<u32>,
    /// Where set, requires `slot_allocation_teu` to be at most it.
    pub slot_allocation_teu_to: Option<u32>,
    /// Where set, requires `utilisation_pct` to equal it.
    pub utilisation_pct: Option<super::Decimal>,
    /// Where set, requires `utilisation_pct` to be at least it.
    pub utilisation_pct_from: Option<super::Decimal>,
    /// Where set, requires `utilisation_pct` to be at most it.
    pub utilisation_pct_to: Option<super::Decimal>,
    /// Where set, requires `utilisation_pct` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub utilisation_pct_is_null: Option<bool>,
}

impl VoyageFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.vessel_imo.is_none()
            && self.vessel_imo_like.is_none()
            && self.voyage_number.is_none()
            && self.voyage_number_like.is_none()
            && self.service_code.is_none()
            && self.service_code_like.is_none()
            && self.etd.is_none()
            && self.etd_from.is_none()
            && self.etd_to.is_none()
            && self.eta.is_none()
            && self.eta_from.is_none()
            && self.eta_to.is_none()
            && self.atd.is_none()
            && self.atd_from.is_none()
            && self.atd_to.is_none()
            && self.atd_is_null.is_none()
            && self.status.is_none()
            && self.status_like.is_none()
            && self.bunker_price_usd_t.is_none()
            && self.bunker_price_usd_t_from.is_none()
            && self.bunker_price_usd_t_to.is_none()
            && self.bunker_price_usd_t_is_null.is_none()
            && self.slot_allocation_teu.is_none()
            && self.slot_allocation_teu_from.is_none()
            && self.slot_allocation_teu_to.is_none()
            && self.utilisation_pct.is_none()
            && self.utilisation_pct_from.is_none()
            && self.utilisation_pct_to.is_none()
            && self.utilisation_pct_is_null.is_none()
    }
}
