// Code generated by tpl from freight.document. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.document` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::document::{Document, DocumentFilter, DocumentKey, DocumentOrder};
use super::document_repository::DocumentRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.document";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `document_id`, `declaration_id`, `document_kind`, `file_name`, `media_type`, `byte_size`, `content_sha256`, `detached_signature`, `signature_thumbnail`, `page_preview`, `scanned_original`, `archive_bundle`, `uploaded_by`, `uploaded_at`, `retention_until` FROM `document`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `document_id`, `declaration_id`, `document_kind`, `file_name`, `media_type`, `byte_size`, `content_sha256`, `detached_signature`, `signature_thumbnail`, `page_preview`, `scanned_original`, `archive_bundle`, `uploaded_by`, `uploaded_at`, `retention_until` FROM `document` WHERE `document_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `document` (`declaration_id`, `document_kind`, `file_name`, `media_type`, `byte_size`, `content_sha256`, `detached_signature`, `signature_thumbnail`, `page_preview`, `scanned_original`, `archive_bundle`, `uploaded_by`, `uploaded_at`, `retention_until`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `document` SET `declaration_id` = ?, `document_kind` = ?, `file_name` = ?, `media_type` = ?, `byte_size` = ?, `content_sha256` = ?, `detached_signature` = ?, `signature_thumbnail` = ?, `page_preview` = ?, `scanned_original` = ?, `archive_bundle` = ?, `uploaded_by` = ?, `uploaded_at` = ?, `retention_until` = ? WHERE `document_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `document` WHERE `document_id` = ?";

/// The `sqlx` data layer of `freight.document`.
#[derive(Debug, Clone)]
pub struct DocumentSqlx {
    pool: sqlx::MySqlPool,
}

impl DocumentSqlx {
    /// Builds the data layer of `freight.document` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl DocumentRepository for DocumentSqlx {
    async fn create(&self, row: &mut Document) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(row.declaration_id)
            .bind(&row.document_kind)
            .bind(&row.file_name)
            .bind(&row.media_type)
            .bind(row.byte_size)
            .bind(&row.content_sha256)
            .bind(&row.detached_signature)
            .bind(&row.signature_thumbnail)
            .bind(&row.page_preview)
            .bind(&row.scanned_original)
            .bind(&row.archive_bundle)
            .bind(&row.uploaded_by)
            .bind(row.uploaded_at)
            .bind(row.retention_until)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.document_id = assigned;
        Ok(())
    }

    async fn read(&self, key: &DocumentKey) -> Result<Document, Error> {
        sqlx::query_as::<_, Document>(READ)
            .bind(key.document_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &Document) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(row.declaration_id)
            .bind(&row.document_kind)
            .bind(&row.file_name)
            .bind(&row.media_type)
            .bind(row.byte_size)
            .bind(&row.content_sha256)
            .bind(&row.detached_signature)
            .bind(&row.signature_thumbnail)
            .bind(&row.page_preview)
            .bind(&row.scanned_original)
            .bind(&row.archive_bundle)
            .bind(&row.uploaded_by)
            .bind(row.uploaded_at)
            .bind(row.retention_until)
            .bind(row.document_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &DocumentKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.document_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: DocumentFilter,
        order: &[DocumentOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<Document>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.document_id {
                conditions
                    .push("`document_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.document_id_from {
                conditions
                    .push("`document_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.document_id_to {
                conditions
                    .push("`document_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.declaration_id {
                conditions
                    .push("`declaration_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.declaration_id_from {
                conditions
                    .push("`declaration_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.declaration_id_to {
                conditions
                    .push("`declaration_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.declaration_id_is_null {
                conditions.push(if is_null {
                    "`declaration_id` IS NULL"
                } else {
                    "`declaration_id` IS NOT NULL"
                });
            }
            if let Some(value) = filter.document_kind {
                conditions
                    .push("`document_kind` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.document_kind_like {
                conditions
                    .push("`document_kind` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.file_name {
                conditions
                    .push("`file_name` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.file_name_like {
                conditions
                    .push("`file_name` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.media_type {
                conditions
                    .push("`media_type` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.media_type_like {
                conditions
                    .push("`media_type` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.byte_size {
                conditions
                    .push("`byte_size` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.byte_size_from {
                conditions
                    .push("`byte_size` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.byte_size_to {
                conditions
                    .push("`byte_size` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.content_sha256 {
                conditions
                    .push("`content_sha256` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.detached_signature {
                conditions
                    .push("`detached_signature` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.detached_signature_is_null {
                conditions.push(if is_null {
                    "`detached_signature` IS NULL"
                } else {
                    "`detached_signature` IS NOT NULL"
                });
            }
            if let Some(value) = filter.signature_thumbnail {
                conditions
                    .push("`signature_thumbnail` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.signature_thumbnail_is_null {
                conditions.push(if is_null {
                    "`signature_thumbnail` IS NULL"
                } else {
                    "`signature_thumbnail` IS NOT NULL"
                });
            }
            if let Some(value) = filter.page_preview {
                conditions
                    .push("`page_preview` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.page_preview_is_null {
                conditions.push(if is_null {
                    "`page_preview` IS NULL"
                } else {
                    "`page_preview` IS NOT NULL"
                });
            }
            if let Some(value) = filter.scanned_original {
                conditions
                    .push("`scanned_original` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.scanned_original_is_null {
                conditions.push(if is_null {
                    "`scanned_original` IS NULL"
                } else {
                    "`scanned_original` IS NOT NULL"
                });
            }
            if let Some(value) = filter.archive_bundle {
                conditions
                    .push("`archive_bundle` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.archive_bundle_is_null {
                conditions.push(if is_null {
                    "`archive_bundle` IS NULL"
                } else {
                    "`archive_bundle` IS NOT NULL"
                });
            }
            if let Some(value) = filter.uploaded_by {
                conditions
                    .push("`uploaded_by` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.uploaded_by_like {
                conditions
                    .push("`uploaded_by` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.uploaded_at {
                conditions
                    .push("`uploaded_at` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.uploaded_at_from {
                conditions
                    .push("`uploaded_at` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.uploaded_at_to {
                conditions
                    .push("`uploaded_at` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.retention_until {
                conditions
                    .push("`retention_until` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.retention_until_from {
                conditions
                    .push("`retention_until` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.retention_until_to {
                conditions
                    .push("`retention_until` <= ")
                    .push_bind_unseparated(value);
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<Document>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
