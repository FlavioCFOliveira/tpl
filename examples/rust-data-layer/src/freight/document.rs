// Code generated by tpl from freight.document. DO NOT EDIT.

//! The `freight.document` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.document`.
///
/// Documentos digitalizados e assinados que acompanham cada expedição
#[derive(Debug, Clone)]
pub struct Document {
    /// `document_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub document_id: u64,
    /// `declaration_id`, declared `bigint(20) unsigned` NULL.
    ///
    /// NULL for a document filed against the consignment rather than a declaration
    pub declaration_id: Option<u64>,
    /// `document_kind`, declared `enum('Bill of lading','Commercial invoice','Packing list','Certificate of origin','Phytosanitary certificate','Dangerous goods declaration','Delivery order')` NOT NULL.
    pub document_kind: String,
    /// `file_name`, declared `varchar(255)` NOT NULL.
    pub file_name: String,
    /// `media_type`, declared `varchar(120)` NOT NULL.
    pub media_type: String,
    /// `byte_size`, declared `int(10) unsigned` NOT NULL.
    pub byte_size: u32,
    /// `content_sha256`, declared `binary(32)` NOT NULL.
    ///
    /// It is part of a unique index.
    ///
    /// Fixed-width binary: SHA-256 of the stored bytes
    pub content_sha256: Vec<u8>,
    /// `detached_signature`, declared `varbinary(512)` NULL.
    ///
    /// Variable-width binary: CMS detached signature
    pub detached_signature: Option<Vec<u8>>,
    /// `signature_thumbnail`, declared `tinyblob` NULL.
    ///
    /// Rendered signature strip, always small
    pub signature_thumbnail: Option<Vec<u8>>,
    /// `page_preview`, declared `blob` NULL.
    ///
    /// First-page raster preview
    pub page_preview: Option<Vec<u8>>,
    /// `scanned_original`, declared `mediumblob` NULL.
    ///
    /// Full colour scan of the paper original
    pub scanned_original: Option<Vec<u8>>,
    /// `archive_bundle`, declared `longblob` NULL.
    ///
    /// Signed archival package retained for the statutory period
    pub archive_bundle: Option<Vec<u8>>,
    /// `uploaded_by`, declared `varchar(80)` NOT NULL.
    pub uploaded_by: String,
    /// `uploaded_at`, declared `datetime(6)` NOT NULL.
    pub uploaded_at: sqlx::types::chrono::NaiveDateTime,
    /// `retention_until`, declared `date` NOT NULL.
    ///
    /// Expression default: ten-year statutory retention
    pub retention_until: sqlx::types::chrono::NaiveDate,
}

impl Document {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> DocumentKey {
        DocumentKey {
            document_id: self.document_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Document {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            document_id: row.try_get("document_id")?,
            declaration_id: row.try_get("declaration_id")?,
            document_kind: row.try_get("document_kind")?,
            file_name: row.try_get("file_name")?,
            media_type: row.try_get("media_type")?,
            byte_size: row.try_get("byte_size")?,
            content_sha256: row.try_get("content_sha256")?,
            detached_signature: row.try_get("detached_signature")?,
            signature_thumbnail: row.try_get("signature_thumbnail")?,
            page_preview: row.try_get("page_preview")?,
            scanned_original: row.try_get("scanned_original")?,
            archive_bundle: row.try_get("archive_bundle")?,
            uploaded_by: row.try_get("uploaded_by")?,
            uploaded_at: row.try_get("uploaded_at")?,
            retention_until: row.try_get("retention_until")?,
        })
    }
}

/// The primary key of `freight.document`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct DocumentKey {
    /// `document_id`, declared `bigint(20) unsigned`.
    pub document_id: u64,
}

/// One column of `freight.document`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: DocumentColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum DocumentColumn {
    /// `document_id`, declared `bigint(20) unsigned`.
    DocumentId,
    /// `declaration_id`, declared `bigint(20) unsigned`.
    DeclarationId,
    /// `document_kind`, declared `enum('Bill of lading','Commercial invoice','Packing list','Certificate of origin','Phytosanitary certificate','Dangerous goods declaration','Delivery order')`.
    DocumentKind,
    /// `file_name`, declared `varchar(255)`.
    FileName,
    /// `media_type`, declared `varchar(120)`.
    MediaType,
    /// `byte_size`, declared `int(10) unsigned`.
    ByteSize,
    /// `content_sha256`, declared `binary(32)`.
    ContentSha256,
    /// `detached_signature`, declared `varbinary(512)`.
    DetachedSignature,
    /// `signature_thumbnail`, declared `tinyblob`.
    SignatureThumbnail,
    /// `page_preview`, declared `blob`.
    PagePreview,
    /// `scanned_original`, declared `mediumblob`.
    ScannedOriginal,
    /// `archive_bundle`, declared `longblob`.
    ArchiveBundle,
    /// `uploaded_by`, declared `varchar(80)`.
    UploadedBy,
    /// `uploaded_at`, declared `datetime(6)`.
    UploadedAt,
    /// `retention_until`, declared `date`.
    RetentionUntil,
}

impl DocumentColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::DocumentId => "`document_id`",
            Self::DeclarationId => "`declaration_id`",
            Self::DocumentKind => "`document_kind`",
            Self::FileName => "`file_name`",
            Self::MediaType => "`media_type`",
            Self::ByteSize => "`byte_size`",
            Self::ContentSha256 => "`content_sha256`",
            Self::DetachedSignature => "`detached_signature`",
            Self::SignatureThumbnail => "`signature_thumbnail`",
            Self::PagePreview => "`page_preview`",
            Self::ScannedOriginal => "`scanned_original`",
            Self::ArchiveBundle => "`archive_bundle`",
            Self::UploadedBy => "`uploaded_by`",
            Self::UploadedAt => "`uploaded_at`",
            Self::RetentionUntil => "`retention_until`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::DocumentId => "document_id",
            Self::DeclarationId => "declaration_id",
            Self::DocumentKind => "document_kind",
            Self::FileName => "file_name",
            Self::MediaType => "media_type",
            Self::ByteSize => "byte_size",
            Self::ContentSha256 => "content_sha256",
            Self::DetachedSignature => "detached_signature",
            Self::SignatureThumbnail => "signature_thumbnail",
            Self::PagePreview => "page_preview",
            Self::ScannedOriginal => "scanned_original",
            Self::ArchiveBundle => "archive_bundle",
            Self::UploadedBy => "uploaded_by",
            Self::UploadedAt => "uploaded_at",
            Self::RetentionUntil => "retention_until",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::DocumentId,
            Self::DeclarationId,
            Self::DocumentKind,
            Self::FileName,
            Self::MediaType,
            Self::ByteSize,
            Self::ContentSha256,
            Self::DetachedSignature,
            Self::SignatureThumbnail,
            Self::PagePreview,
            Self::ScannedOriginal,
            Self::ArchiveBundle,
            Self::UploadedBy,
            Self::UploadedAt,
            Self::RetentionUntil,
        ]
    }
}

impl std::fmt::Display for DocumentColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for DocumentColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "document_id" => Ok(Self::DocumentId),
            "declaration_id" => Ok(Self::DeclarationId),
            "document_kind" => Ok(Self::DocumentKind),
            "file_name" => Ok(Self::FileName),
            "media_type" => Ok(Self::MediaType),
            "byte_size" => Ok(Self::ByteSize),
            "content_sha256" => Ok(Self::ContentSha256),
            "detached_signature" => Ok(Self::DetachedSignature),
            "signature_thumbnail" => Ok(Self::SignatureThumbnail),
            "page_preview" => Ok(Self::PagePreview),
            "scanned_original" => Ok(Self::ScannedOriginal),
            "archive_bundle" => Ok(Self::ArchiveBundle),
            "uploaded_by" => Ok(Self::UploadedBy),
            "uploaded_at" => Ok(Self::UploadedAt),
            "retention_until" => Ok(Self::RetentionUntil),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.document",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.document`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct DocumentOrder {
    /// The column to order by.
    pub column: DocumentColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.document`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct DocumentFilter {
    /// Where set, requires `document_id` to equal it.
    pub document_id: Option<u64>,
    /// Where set, requires `document_id` to be at least it.
    pub document_id_from: Option<u64>,
    /// Where set, requires `document_id` to be at most it.
    pub document_id_to: Option<u64>,
    /// Where set, requires `declaration_id` to equal it.
    pub declaration_id: Option<u64>,
    /// Where set, requires `declaration_id` to be at least it.
    pub declaration_id_from: Option<u64>,
    /// Where set, requires `declaration_id` to be at most it.
    pub declaration_id_to: Option<u64>,
    /// Where set, requires `declaration_id` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub declaration_id_is_null: Option<bool>,
    /// Where set, requires `document_kind` to equal it.
    pub document_kind: Option<String>,
    /// Where set, requires `document_kind` to match it, as `LIKE` matches.
    pub document_kind_like: Option<String>,
    /// Where set, requires `file_name` to equal it.
    pub file_name: Option<String>,
    /// Where set, requires `file_name` to match it, as `LIKE` matches.
    pub file_name_like: Option<String>,
    /// Where set, requires `media_type` to equal it.
    pub media_type: Option<String>,
    /// Where set, requires `media_type` to match it, as `LIKE` matches.
    pub media_type_like: Option<String>,
    /// Where set, requires `byte_size` to equal it.
    pub byte_size: Option<u32>,
    /// Where set, requires `byte_size` to be at least it.
    pub byte_size_from: Option<u32>,
    /// Where set, requires `byte_size` to be at most it.
    pub byte_size_to: Option<u32>,
    /// Where set, requires `content_sha256` to equal it.
    pub content_sha256: Option<Vec<u8>>,
    /// Where set, requires `detached_signature` to equal it.
    pub detached_signature: Option<Vec<u8>>,
    /// Where set, requires `detached_signature` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub detached_signature_is_null: Option<bool>,
    /// Where set, requires `signature_thumbnail` to equal it.
    pub signature_thumbnail: Option<Vec<u8>>,
    /// Where set, requires `signature_thumbnail` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub signature_thumbnail_is_null: Option<bool>,
    /// Where set, requires `page_preview` to equal it.
    pub page_preview: Option<Vec<u8>>,
    /// Where set, requires `page_preview` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub page_preview_is_null: Option<bool>,
    /// Where set, requires `scanned_original` to equal it.
    pub scanned_original: Option<Vec<u8>>,
    /// Where set, requires `scanned_original` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub scanned_original_is_null: Option<bool>,
    /// Where set, requires `archive_bundle` to equal it.
    pub archive_bundle: Option<Vec<u8>>,
    /// Where set, requires `archive_bundle` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub archive_bundle_is_null: Option<bool>,
    /// Where set, requires `uploaded_by` to equal it.
    pub uploaded_by: Option<String>,
    /// Where set, requires `uploaded_by` to match it, as `LIKE` matches.
    pub uploaded_by_like: Option<String>,
    /// Where set, requires `uploaded_at` to equal it.
    pub uploaded_at: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `uploaded_at` to be at least it.
    pub uploaded_at_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `uploaded_at` to be at most it.
    pub uploaded_at_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `retention_until` to equal it.
    pub retention_until: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `retention_until` to be at least it.
    pub retention_until_from: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `retention_until` to be at most it.
    pub retention_until_to: Option<sqlx::types::chrono::NaiveDate>,
}

impl DocumentFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.document_id.is_none()
            && self.document_id_from.is_none()
            && self.document_id_to.is_none()
            && self.declaration_id.is_none()
            && self.declaration_id_from.is_none()
            && self.declaration_id_to.is_none()
            && self.declaration_id_is_null.is_none()
            && self.document_kind.is_none()
            && self.document_kind_like.is_none()
            && self.file_name.is_none()
            && self.file_name_like.is_none()
            && self.media_type.is_none()
            && self.media_type_like.is_none()
            && self.byte_size.is_none()
            && self.byte_size_from.is_none()
            && self.byte_size_to.is_none()
            && self.content_sha256.is_none()
            && self.detached_signature.is_none()
            && self.detached_signature_is_null.is_none()
            && self.signature_thumbnail.is_none()
            && self.signature_thumbnail_is_null.is_none()
            && self.page_preview.is_none()
            && self.page_preview_is_null.is_none()
            && self.scanned_original.is_none()
            && self.scanned_original_is_null.is_none()
            && self.archive_bundle.is_none()
            && self.archive_bundle_is_null.is_none()
            && self.uploaded_by.is_none()
            && self.uploaded_by_like.is_none()
            && self.uploaded_at.is_none()
            && self.uploaded_at_from.is_none()
            && self.uploaded_at_to.is_none()
            && self.retention_until.is_none()
            && self.retention_until_from.is_none()
            && self.retention_until_to.is_none()
    }
}
