// Code generated by tpl from freight.consignment. DO NOT EDIT.

//! The `freight.consignment` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.consignment`.
///
/// Expedições reservadas, do rascunho à entrega
#[derive(Debug, Clone)]
pub struct Consignment {
    /// `consignment_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub consignment_id: u64,
    /// `booking_reference`, declared `varchar(16)` NOT NULL.
    ///
    /// It is part of a unique index.
    ///
    /// Drawn from booking_reference_seq, prefixed BKG-
    pub booking_reference: String,
    /// `bill_of_lading_no`, declared `varchar(20)` NULL.
    ///
    /// It is part of a unique index.
    ///
    /// Issued only once the cargo is on board
    pub bill_of_lading_no: Option<String>,
    /// `shipper_id`, declared `int(10) unsigned` NOT NULL.
    pub shipper_id: u32,
    /// `consignee_id`, declared `int(10) unsigned` NULL.
    ///
    /// NULL for a "to order" bill of lading, which names no consignee at issue
    pub consignee_id: Option<u32>,
    /// `vessel_imo`, declared `char(7)` NULL.
    pub vessel_imo: Option<String>,
    /// `voyage_number`, declared `varchar(12)` NULL.
    pub voyage_number: Option<String>,
    /// `origin_locode`, declared `char(5)` NOT NULL.
    pub origin_locode: String,
    /// `destination_locode`, declared `char(5)` NOT NULL.
    pub destination_locode: String,
    /// `invoice_currency`, declared `char(3)` NOT NULL.
    ///
    /// Literal default
    pub invoice_currency: String,
    /// `payment_due_on`, declared `date` NOT NULL.
    ///
    /// Expression default: standard net-30 terms
    pub payment_due_on: sqlx::types::chrono::NaiveDate,
    /// `hs_code_override`, declared `varchar(12)` NULL.
    ///
    /// Explicit DEFAULT NULL
    pub hs_code_override: Option<String>,
    /// `shipper_reference`, declared `varchar(64)` NOT NULL.
    ///
    /// No DEFAULT clause at all, and NOT NULL: every booking arrives with the customer reference
    pub shipper_reference: String,
    /// `broker_reference`, declared `varchar(64)` NULL.
    ///
    /// No DEFAULT clause and nullable, to contrast with the explicit DEFAULT NULL above
    pub broker_reference: Option<String>,
    /// `incoterm`, declared `enum('EXW','FCA','FAS','FOB','CFR','CIF','CPT','CIP','DAP','DPU','DDP')` NOT NULL.
    pub incoterm: String,
    /// `service_scope`, declared `enum('Port to port','Door to door','Door to port','Port to door')` NOT NULL.
    pub service_scope: String,
    /// `handling_flags`, declared `set('Fragile','Stackable','Temperature controlled','Lloyd''s Register survey required','Out of gauge','Direct delivery')` NOT NULL.
    ///
    /// Note the apostrophe in the fourth member; a SET member may not contain a comma
    pub handling_flags: String,
    /// `status`, declared `enum('Draft','Booked','Loaded','In transit','Discharged','Delivered','Cancelled')` NOT NULL.
    pub status: String,
    /// `declared_value`, declared `decimal(14,2)` NOT NULL.
    pub declared_value: super::Decimal,
    /// `freight_amount`, declared `decimal(12,2)` NOT NULL.
    pub freight_amount: super::Decimal,
    /// `insurance_rate`, declared `decimal(6,4)` NULL.
    ///
    /// Rate applied to declared_value, e.g. 0.0035
    pub insurance_rate: Option<super::Decimal>,
    /// `insured_amount`, declared `decimal(14,2)` NULL VIRTUAL GENERATED AS (round(`declared_value` * coalesce(`insurance_rate`,0),2)).
    ///
    /// Virtual generated column: derived cover, never stored
    pub insured_amount: Option<super::Decimal>,
    /// `gross_weight_kg`, declared `decimal(12,3)` NOT NULL.
    ///
    /// Sum of the cargo items, kilograms
    pub gross_weight_kg: super::Decimal,
    /// `volume_cbm`, declared `decimal(10,3)` NULL.
    ///
    /// Cubic metres
    pub volume_cbm: Option<super::Decimal>,
    /// `notification_flags`, declared `bit(8)` NOT NULL.
    pub notification_flags: u64,
    /// `is_hazardous`, declared `tinyint(1)` NOT NULL.
    pub is_hazardous: i8,
    /// `marks_and_numbers`, declared `tinytext` NULL.
    ///
    /// Shipping marks stencilled on the packages
    pub marks_and_numbers: Option<String>,
    /// `goods_description`, declared `text` NOT NULL.
    ///
    /// Description of goods as it will print on the bill of lading
    pub goods_description: String,
    /// `packing_list`, declared `mediumtext` NULL.
    pub packing_list: Option<String>,
    /// `terms_and_conditions`, declared `longtext` NULL.
    ///
    /// Carrier terms in force at the moment of booking
    pub terms_and_conditions: Option<String>,
    /// `booked_at`, declared `datetime(6)` NOT NULL.
    pub booked_at: sqlx::types::chrono::NaiveDateTime,
    /// `updated_at`, declared `timestamp(3)` NOT NULL.
    pub updated_at: sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>,
}

impl Consignment {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> ConsignmentKey {
        ConsignmentKey {
            consignment_id: self.consignment_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Consignment {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            consignment_id: row.try_get("consignment_id")?,
            booking_reference: row.try_get("booking_reference")?,
            bill_of_lading_no: row.try_get("bill_of_lading_no")?,
            shipper_id: row.try_get("shipper_id")?,
            consignee_id: row.try_get("consignee_id")?,
            vessel_imo: row.try_get("vessel_imo")?,
            voyage_number: row.try_get("voyage_number")?,
            origin_locode: row.try_get("origin_locode")?,
            destination_locode: row.try_get("destination_locode")?,
            invoice_currency: row.try_get("invoice_currency")?,
            payment_due_on: row.try_get("payment_due_on")?,
            hs_code_override: row.try_get("hs_code_override")?,
            shipper_reference: row.try_get("shipper_reference")?,
            broker_reference: row.try_get("broker_reference")?,
            incoterm: row.try_get("incoterm")?,
            service_scope: row.try_get("service_scope")?,
            handling_flags: row.try_get("handling_flags")?,
            status: row.try_get("status")?,
            declared_value: row.try_get("declared_value")?,
            freight_amount: row.try_get("freight_amount")?,
            insurance_rate: row.try_get("insurance_rate")?,
            insured_amount: row.try_get("insured_amount")?,
            gross_weight_kg: row.try_get("gross_weight_kg")?,
            volume_cbm: row.try_get("volume_cbm")?,
            notification_flags: row.try_get("notification_flags")?,
            is_hazardous: row.try_get("is_hazardous")?,
            marks_and_numbers: row.try_get("marks_and_numbers")?,
            goods_description: row.try_get("goods_description")?,
            packing_list: row.try_get("packing_list")?,
            terms_and_conditions: row.try_get("terms_and_conditions")?,
            booked_at: row.try_get("booked_at")?,
            updated_at: row.try_get("updated_at")?,
        })
    }
}

/// The primary key of `freight.consignment`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct ConsignmentKey {
    /// `consignment_id`, declared `bigint(20) unsigned`.
    pub consignment_id: u64,
}

/// One column of `freight.consignment`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: ConsignmentColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum ConsignmentColumn {
    /// `consignment_id`, declared `bigint(20) unsigned`.
    ConsignmentId,
    /// `booking_reference`, declared `varchar(16)`.
    BookingReference,
    /// `bill_of_lading_no`, declared `varchar(20)`.
    BillOfLadingNo,
    /// `shipper_id`, declared `int(10) unsigned`.
    ShipperId,
    /// `consignee_id`, declared `int(10) unsigned`.
    ConsigneeId,
    /// `vessel_imo`, declared `char(7)`.
    VesselImo,
    /// `voyage_number`, declared `varchar(12)`.
    VoyageNumber,
    /// `origin_locode`, declared `char(5)`.
    OriginLocode,
    /// `destination_locode`, declared `char(5)`.
    DestinationLocode,
    /// `invoice_currency`, declared `char(3)`.
    InvoiceCurrency,
    /// `payment_due_on`, declared `date`.
    PaymentDueOn,
    /// `hs_code_override`, declared `varchar(12)`.
    HsCodeOverride,
    /// `shipper_reference`, declared `varchar(64)`.
    ShipperReference,
    /// `broker_reference`, declared `varchar(64)`.
    BrokerReference,
    /// `incoterm`, declared `enum('EXW','FCA','FAS','FOB','CFR','CIF','CPT','CIP','DAP','DPU','DDP')`.
    Incoterm,
    /// `service_scope`, declared `enum('Port to port','Door to door','Door to port','Port to door')`.
    ServiceScope,
    /// `handling_flags`, declared `set('Fragile','Stackable','Temperature controlled','Lloyd''s Register survey required','Out of gauge','Direct delivery')`.
    HandlingFlags,
    /// `status`, declared `enum('Draft','Booked','Loaded','In transit','Discharged','Delivered','Cancelled')`.
    Status,
    /// `declared_value`, declared `decimal(14,2)`.
    DeclaredValue,
    /// `freight_amount`, declared `decimal(12,2)`.
    FreightAmount,
    /// `insurance_rate`, declared `decimal(6,4)`.
    InsuranceRate,
    /// `insured_amount`, declared `decimal(14,2)`.
    InsuredAmount,
    /// `gross_weight_kg`, declared `decimal(12,3)`.
    GrossWeightKg,
    /// `volume_cbm`, declared `decimal(10,3)`.
    VolumeCbm,
    /// `notification_flags`, declared `bit(8)`.
    NotificationFlags,
    /// `is_hazardous`, declared `tinyint(1)`.
    IsHazardous,
    /// `marks_and_numbers`, declared `tinytext`.
    MarksAndNumbers,
    /// `goods_description`, declared `text`.
    GoodsDescription,
    /// `packing_list`, declared `mediumtext`.
    PackingList,
    /// `terms_and_conditions`, declared `longtext`.
    TermsAndConditions,
    /// `booked_at`, declared `datetime(6)`.
    BookedAt,
    /// `updated_at`, declared `timestamp(3)`.
    UpdatedAt,
}

impl ConsignmentColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::ConsignmentId => "`consignment_id`",
            Self::BookingReference => "`booking_reference`",
            Self::BillOfLadingNo => "`bill_of_lading_no`",
            Self::ShipperId => "`shipper_id`",
            Self::ConsigneeId => "`consignee_id`",
            Self::VesselImo => "`vessel_imo`",
            Self::VoyageNumber => "`voyage_number`",
            Self::OriginLocode => "`origin_locode`",
            Self::DestinationLocode => "`destination_locode`",
            Self::InvoiceCurrency => "`invoice_currency`",
            Self::PaymentDueOn => "`payment_due_on`",
            Self::HsCodeOverride => "`hs_code_override`",
            Self::ShipperReference => "`shipper_reference`",
            Self::BrokerReference => "`broker_reference`",
            Self::Incoterm => "`incoterm`",
            Self::ServiceScope => "`service_scope`",
            Self::HandlingFlags => "`handling_flags`",
            Self::Status => "`status`",
            Self::DeclaredValue => "`declared_value`",
            Self::FreightAmount => "`freight_amount`",
            Self::InsuranceRate => "`insurance_rate`",
            Self::InsuredAmount => "`insured_amount`",
            Self::GrossWeightKg => "`gross_weight_kg`",
            Self::VolumeCbm => "`volume_cbm`",
            Self::NotificationFlags => "`notification_flags`",
            Self::IsHazardous => "`is_hazardous`",
            Self::MarksAndNumbers => "`marks_and_numbers`",
            Self::GoodsDescription => "`goods_description`",
            Self::PackingList => "`packing_list`",
            Self::TermsAndConditions => "`terms_and_conditions`",
            Self::BookedAt => "`booked_at`",
            Self::UpdatedAt => "`updated_at`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::ConsignmentId => "consignment_id",
            Self::BookingReference => "booking_reference",
            Self::BillOfLadingNo => "bill_of_lading_no",
            Self::ShipperId => "shipper_id",
            Self::ConsigneeId => "consignee_id",
            Self::VesselImo => "vessel_imo",
            Self::VoyageNumber => "voyage_number",
            Self::OriginLocode => "origin_locode",
            Self::DestinationLocode => "destination_locode",
            Self::InvoiceCurrency => "invoice_currency",
            Self::PaymentDueOn => "payment_due_on",
            Self::HsCodeOverride => "hs_code_override",
            Self::ShipperReference => "shipper_reference",
            Self::BrokerReference => "broker_reference",
            Self::Incoterm => "incoterm",
            Self::ServiceScope => "service_scope",
            Self::HandlingFlags => "handling_flags",
            Self::Status => "status",
            Self::DeclaredValue => "declared_value",
            Self::FreightAmount => "freight_amount",
            Self::InsuranceRate => "insurance_rate",
            Self::InsuredAmount => "insured_amount",
            Self::GrossWeightKg => "gross_weight_kg",
            Self::VolumeCbm => "volume_cbm",
            Self::NotificationFlags => "notification_flags",
            Self::IsHazardous => "is_hazardous",
            Self::MarksAndNumbers => "marks_and_numbers",
            Self::GoodsDescription => "goods_description",
            Self::PackingList => "packing_list",
            Self::TermsAndConditions => "terms_and_conditions",
            Self::BookedAt => "booked_at",
            Self::UpdatedAt => "updated_at",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::ConsignmentId,
            Self::BookingReference,
            Self::BillOfLadingNo,
            Self::ShipperId,
            Self::ConsigneeId,
            Self::VesselImo,
            Self::VoyageNumber,
            Self::OriginLocode,
            Self::DestinationLocode,
            Self::InvoiceCurrency,
            Self::PaymentDueOn,
            Self::HsCodeOverride,
            Self::ShipperReference,
            Self::BrokerReference,
            Self::Incoterm,
            Self::ServiceScope,
            Self::HandlingFlags,
            Self::Status,
            Self::DeclaredValue,
            Self::FreightAmount,
            Self::InsuranceRate,
            Self::InsuredAmount,
            Self::GrossWeightKg,
            Self::VolumeCbm,
            Self::NotificationFlags,
            Self::IsHazardous,
            Self::MarksAndNumbers,
            Self::GoodsDescription,
            Self::PackingList,
            Self::TermsAndConditions,
            Self::BookedAt,
            Self::UpdatedAt,
        ]
    }
}

impl std::fmt::Display for ConsignmentColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for ConsignmentColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "consignment_id" => Ok(Self::ConsignmentId),
            "booking_reference" => Ok(Self::BookingReference),
            "bill_of_lading_no" => Ok(Self::BillOfLadingNo),
            "shipper_id" => Ok(Self::ShipperId),
            "consignee_id" => Ok(Self::ConsigneeId),
            "vessel_imo" => Ok(Self::VesselImo),
            "voyage_number" => Ok(Self::VoyageNumber),
            "origin_locode" => Ok(Self::OriginLocode),
            "destination_locode" => Ok(Self::DestinationLocode),
            "invoice_currency" => Ok(Self::InvoiceCurrency),
            "payment_due_on" => Ok(Self::PaymentDueOn),
            "hs_code_override" => Ok(Self::HsCodeOverride),
            "shipper_reference" => Ok(Self::ShipperReference),
            "broker_reference" => Ok(Self::BrokerReference),
            "incoterm" => Ok(Self::Incoterm),
            "service_scope" => Ok(Self::ServiceScope),
            "handling_flags" => Ok(Self::HandlingFlags),
            "status" => Ok(Self::Status),
            "declared_value" => Ok(Self::DeclaredValue),
            "freight_amount" => Ok(Self::FreightAmount),
            "insurance_rate" => Ok(Self::InsuranceRate),
            "insured_amount" => Ok(Self::InsuredAmount),
            "gross_weight_kg" => Ok(Self::GrossWeightKg),
            "volume_cbm" => Ok(Self::VolumeCbm),
            "notification_flags" => Ok(Self::NotificationFlags),
            "is_hazardous" => Ok(Self::IsHazardous),
            "marks_and_numbers" => Ok(Self::MarksAndNumbers),
            "goods_description" => Ok(Self::GoodsDescription),
            "packing_list" => Ok(Self::PackingList),
            "terms_and_conditions" => Ok(Self::TermsAndConditions),
            "booked_at" => Ok(Self::BookedAt),
            "updated_at" => Ok(Self::UpdatedAt),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.consignment",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.consignment`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct ConsignmentOrder {
    /// The column to order by.
    pub column: ConsignmentColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.consignment`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct ConsignmentFilter {
    /// Where set, requires `consignment_id` to equal it.
    pub consignment_id: Option<u64>,
    /// Where set, requires `consignment_id` to be at least it.
    pub consignment_id_from: Option<u64>,
    /// Where set, requires `consignment_id` to be at most it.
    pub consignment_id_to: Option<u64>,
    /// Where set, requires `booking_reference` to equal it.
    pub booking_reference: Option<String>,
    /// Where set, requires `booking_reference` to match it, as `LIKE` matches.
    pub booking_reference_like: Option<String>,
    /// Where set, requires `bill_of_lading_no` to equal it.
    pub bill_of_lading_no: Option<String>,
    /// Where set, requires `bill_of_lading_no` to match it, as `LIKE` matches.
    pub bill_of_lading_no_like: Option<String>,
    /// Where set, requires `bill_of_lading_no` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub bill_of_lading_no_is_null: Option<bool>,
    /// Where set, requires `shipper_id` to equal it.
    pub shipper_id: Option<u32>,
    /// Where set, requires `shipper_id` to be at least it.
    pub shipper_id_from: Option<u32>,
    /// Where set, requires `shipper_id` to be at most it.
    pub shipper_id_to: Option<u32>,
    /// Where set, requires `consignee_id` to equal it.
    pub consignee_id: Option<u32>,
    /// Where set, requires `consignee_id` to be at least it.
    pub consignee_id_from: Option<u32>,
    /// Where set, requires `consignee_id` to be at most it.
    pub consignee_id_to: Option<u32>,
    /// Where set, requires `consignee_id` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub consignee_id_is_null: Option<bool>,
    /// Where set, requires `vessel_imo` to equal it.
    pub vessel_imo: Option<String>,
    /// Where set, requires `vessel_imo` to match it, as `LIKE` matches.
    pub vessel_imo_like: Option<String>,
    /// Where set, requires `vessel_imo` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub vessel_imo_is_null: Option<bool>,
    /// Where set, requires `voyage_number` to equal it.
    pub voyage_number: Option<String>,
    /// Where set, requires `voyage_number` to match it, as `LIKE` matches.
    pub voyage_number_like: Option<String>,
    /// Where set, requires `voyage_number` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub voyage_number_is_null: Option<bool>,
    /// Where set, requires `origin_locode` to equal it.
    pub origin_locode: Option<String>,
    /// Where set, requires `origin_locode` to match it, as `LIKE` matches.
    pub origin_locode_like: Option<String>,
    /// Where set, requires `destination_locode` to equal it.
    pub destination_locode: Option<String>,
    /// Where set, requires `destination_locode` to match it, as `LIKE` matches.
    pub destination_locode_like: Option<String>,
    /// Where set, requires `invoice_currency` to equal it.
    pub invoice_currency: Option<String>,
    /// Where set, requires `invoice_currency` to match it, as `LIKE` matches.
    pub invoice_currency_like: Option<String>,
    /// Where set, requires `payment_due_on` to equal it.
    pub payment_due_on: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `payment_due_on` to be at least it.
    pub payment_due_on_from: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `payment_due_on` to be at most it.
    pub payment_due_on_to: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `hs_code_override` to equal it.
    pub hs_code_override: Option<String>,
    /// Where set, requires `hs_code_override` to match it, as `LIKE` matches.
    pub hs_code_override_like: Option<String>,
    /// Where set, requires `hs_code_override` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub hs_code_override_is_null: Option<bool>,
    /// Where set, requires `shipper_reference` to equal it.
    pub shipper_reference: Option<String>,
    /// Where set, requires `shipper_reference` to match it, as `LIKE` matches.
    pub shipper_reference_like: Option<String>,
    /// Where set, requires `broker_reference` to equal it.
    pub broker_reference: Option<String>,
    /// Where set, requires `broker_reference` to match it, as `LIKE` matches.
    pub broker_reference_like: Option<String>,
    /// Where set, requires `broker_reference` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub broker_reference_is_null: Option<bool>,
    /// Where set, requires `incoterm` to equal it.
    pub incoterm: Option<String>,
    /// Where set, requires `incoterm` to match it, as `LIKE` matches.
    pub incoterm_like: Option<String>,
    /// Where set, requires `service_scope` to equal it.
    pub service_scope: Option<String>,
    /// Where set, requires `service_scope` to match it, as `LIKE` matches.
    pub service_scope_like: Option<String>,
    /// Where set, requires `handling_flags` to equal it.
    pub handling_flags: Option<String>,
    /// Where set, requires `handling_flags` to match it, as `LIKE` matches.
    pub handling_flags_like: Option<String>,
    /// Where set, requires `status` to equal it.
    pub status: Option<String>,
    /// Where set, requires `status` to match it, as `LIKE` matches.
    pub status_like: Option<String>,
    /// Where set, requires `declared_value` to equal it.
    pub declared_value: Option<super::Decimal>,
    /// Where set, requires `declared_value` to be at least it.
    pub declared_value_from: Option<super::Decimal>,
    /// Where set, requires `declared_value` to be at most it.
    pub declared_value_to: Option<super::Decimal>,
    /// Where set, requires `freight_amount` to equal it.
    pub freight_amount: Option<super::Decimal>,
    /// Where set, requires `freight_amount` to be at least it.
    pub freight_amount_from: Option<super::Decimal>,
    /// Where set, requires `freight_amount` to be at most it.
    pub freight_amount_to: Option<super::Decimal>,
    /// Where set, requires `insurance_rate` to equal it.
    pub insurance_rate: Option<super::Decimal>,
    /// Where set, requires `insurance_rate` to be at least it.
    pub insurance_rate_from: Option<super::Decimal>,
    /// Where set, requires `insurance_rate` to be at most it.
    pub insurance_rate_to: Option<super::Decimal>,
    /// Where set, requires `insurance_rate` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub insurance_rate_is_null: Option<bool>,
    /// Where set, requires `insured_amount` to equal it.
    pub insured_amount: Option<super::Decimal>,
    /// Where set, requires `insured_amount` to be at least it.
    pub insured_amount_from: Option<super::Decimal>,
    /// Where set, requires `insured_amount` to be at most it.
    pub insured_amount_to: Option<super::Decimal>,
    /// Where set, requires `insured_amount` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub insured_amount_is_null: Option<bool>,
    /// Where set, requires `gross_weight_kg` to equal it.
    pub gross_weight_kg: Option<super::Decimal>,
    /// Where set, requires `gross_weight_kg` to be at least it.
    pub gross_weight_kg_from: Option<super::Decimal>,
    /// Where set, requires `gross_weight_kg` to be at most it.
    pub gross_weight_kg_to: Option<super::Decimal>,
    /// Where set, requires `volume_cbm` to equal it.
    pub volume_cbm: Option<super::Decimal>,
    /// Where set, requires `volume_cbm` to be at least it.
    pub volume_cbm_from: Option<super::Decimal>,
    /// Where set, requires `volume_cbm` to be at most it.
    pub volume_cbm_to: Option<super::Decimal>,
    /// Where set, requires `volume_cbm` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub volume_cbm_is_null: Option<bool>,
    /// Where set, requires `notification_flags` to equal it.
    pub notification_flags: Option<u64>,
    /// Where set, requires `notification_flags` to be at least it.
    pub notification_flags_from: Option<u64>,
    /// Where set, requires `notification_flags` to be at most it.
    pub notification_flags_to: Option<u64>,
    /// Where set, requires `is_hazardous` to equal it.
    pub is_hazardous: Option<i8>,
    /// Where set, requires `is_hazardous` to be at least it.
    pub is_hazardous_from: Option<i8>,
    /// Where set, requires `is_hazardous` to be at most it.
    pub is_hazardous_to: Option<i8>,
    /// Where set, requires `marks_and_numbers` to equal it.
    pub marks_and_numbers: Option<String>,
    /// Where set, requires `marks_and_numbers` to match it, as `LIKE` matches.
    pub marks_and_numbers_like: Option<String>,
    /// Where set, requires `marks_and_numbers` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub marks_and_numbers_is_null: Option<bool>,
    /// Where set, requires `goods_description` to equal it.
    pub goods_description: Option<String>,
    /// Where set, requires `goods_description` to match it, as `LIKE` matches.
    pub goods_description_like: Option<String>,
    /// Where set, requires `packing_list` to equal it.
    pub packing_list: Option<String>,
    /// Where set, requires `packing_list` to match it, as `LIKE` matches.
    pub packing_list_like: Option<String>,
    /// Where set, requires `packing_list` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub packing_list_is_null: Option<bool>,
    /// Where set, requires `terms_and_conditions` to equal it.
    pub terms_and_conditions: Option<String>,
    /// Where set, requires `terms_and_conditions` to match it, as `LIKE` matches.
    pub terms_and_conditions_like: Option<String>,
    /// Where set, requires `terms_and_conditions` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub terms_and_conditions_is_null: Option<bool>,
    /// Where set, requires `booked_at` to equal it.
    pub booked_at: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `booked_at` to be at least it.
    pub booked_at_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `booked_at` to be at most it.
    pub booked_at_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `updated_at` to equal it.
    pub updated_at: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `updated_at` to be at least it.
    pub updated_at_from: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
    /// Where set, requires `updated_at` to be at most it.
    pub updated_at_to: Option<sqlx::types::chrono::DateTime<sqlx::types::chrono::Utc>>,
}

impl ConsignmentFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.consignment_id.is_none()
            && self.consignment_id_from.is_none()
            && self.consignment_id_to.is_none()
            && self.booking_reference.is_none()
            && self.booking_reference_like.is_none()
            && self.bill_of_lading_no.is_none()
            && self.bill_of_lading_no_like.is_none()
            && self.bill_of_lading_no_is_null.is_none()
            && self.shipper_id.is_none()
            && self.shipper_id_from.is_none()
            && self.shipper_id_to.is_none()
            && self.consignee_id.is_none()
            && self.consignee_id_from.is_none()
            && self.consignee_id_to.is_none()
            && self.consignee_id_is_null.is_none()
            && self.vessel_imo.is_none()
            && self.vessel_imo_like.is_none()
            && self.vessel_imo_is_null.is_none()
            && self.voyage_number.is_none()
            && self.voyage_number_like.is_none()
            && self.voyage_number_is_null.is_none()
            && self.origin_locode.is_none()
            && self.origin_locode_like.is_none()
            && self.destination_locode.is_none()
            && self.destination_locode_like.is_none()
            && self.invoice_currency.is_none()
            && self.invoice_currency_like.is_none()
            && self.payment_due_on.is_none()
            && self.payment_due_on_from.is_none()
            && self.payment_due_on_to.is_none()
            && self.hs_code_override.is_none()
            && self.hs_code_override_like.is_none()
            && self.hs_code_override_is_null.is_none()
            && self.shipper_reference.is_none()
            && self.shipper_reference_like.is_none()
            && self.broker_reference.is_none()
            && self.broker_reference_like.is_none()
            && self.broker_reference_is_null.is_none()
            && self.incoterm.is_none()
            && self.incoterm_like.is_none()
            && self.service_scope.is_none()
            && self.service_scope_like.is_none()
            && self.handling_flags.is_none()
            && self.handling_flags_like.is_none()
            && self.status.is_none()
            && self.status_like.is_none()
            && self.declared_value.is_none()
            && self.declared_value_from.is_none()
            && self.declared_value_to.is_none()
            && self.freight_amount.is_none()
            && self.freight_amount_from.is_none()
            && self.freight_amount_to.is_none()
            && self.insurance_rate.is_none()
            && self.insurance_rate_from.is_none()
            && self.insurance_rate_to.is_none()
            && self.insurance_rate_is_null.is_none()
            && self.insured_amount.is_none()
            && self.insured_amount_from.is_none()
            && self.insured_amount_to.is_none()
            && self.insured_amount_is_null.is_none()
            && self.gross_weight_kg.is_none()
            && self.gross_weight_kg_from.is_none()
            && self.gross_weight_kg_to.is_none()
            && self.volume_cbm.is_none()
            && self.volume_cbm_from.is_none()
            && self.volume_cbm_to.is_none()
            && self.volume_cbm_is_null.is_none()
            && self.notification_flags.is_none()
            && self.notification_flags_from.is_none()
            && self.notification_flags_to.is_none()
            && self.is_hazardous.is_none()
            && self.is_hazardous_from.is_none()
            && self.is_hazardous_to.is_none()
            && self.marks_and_numbers.is_none()
            && self.marks_and_numbers_like.is_none()
            && self.marks_and_numbers_is_null.is_none()
            && self.goods_description.is_none()
            && self.goods_description_like.is_none()
            && self.packing_list.is_none()
            && self.packing_list_like.is_none()
            && self.packing_list_is_null.is_none()
            && self.terms_and_conditions.is_none()
            && self.terms_and_conditions_like.is_none()
            && self.terms_and_conditions_is_null.is_none()
            && self.booked_at.is_none()
            && self.booked_at_from.is_none()
            && self.booked_at_to.is_none()
            && self.updated_at.is_none()
            && self.updated_at_from.is_none()
            && self.updated_at_to.is_none()
    }
}
