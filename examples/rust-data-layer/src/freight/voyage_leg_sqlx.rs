// Code generated by tpl from freight.voyage_leg. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.voyage_leg` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::voyage_leg::{VoyageLeg, VoyageLegFilter, VoyageLegKey, VoyageLegOrder};
use super::voyage_leg_repository::VoyageLegRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.voyage_leg";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `vessel_imo`, `voyage_number`, `leg_sequence`, `port_id`, `eta`, `etd`, `ata`, `berth_shift_time`, `pilot_required`, `moves_discharged`, `moves_loaded`, `moves_total` FROM `voyage_leg`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `vessel_imo`, `voyage_number`, `leg_sequence`, `port_id`, `eta`, `etd`, `ata`, `berth_shift_time`, `pilot_required`, `moves_discharged`, `moves_loaded`, `moves_total` FROM `voyage_leg` WHERE `vessel_imo` = ? AND `voyage_number` = ? AND `leg_sequence` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `voyage_leg` (`vessel_imo`, `voyage_number`, `leg_sequence`, `port_id`, `eta`, `etd`, `ata`, `berth_shift_time`, `pilot_required`, `moves_discharged`, `moves_loaded`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `voyage_leg` SET `port_id` = ?, `eta` = ?, `etd` = ?, `ata` = ?, `berth_shift_time` = ?, `pilot_required` = ?, `moves_discharged` = ?, `moves_loaded` = ? WHERE `vessel_imo` = ? AND `voyage_number` = ? AND `leg_sequence` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `voyage_leg` WHERE `vessel_imo` = ? AND `voyage_number` = ? AND `leg_sequence` = ?";

/// The `sqlx` data layer of `freight.voyage_leg`.
#[derive(Debug, Clone)]
pub struct VoyageLegSqlx {
    pool: sqlx::MySqlPool,
}

impl VoyageLegSqlx {
    /// Builds the data layer of `freight.voyage_leg` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl VoyageLegRepository for VoyageLegSqlx {
    async fn create(&self, row: &mut VoyageLeg) -> Result<(), Error> {
        sqlx::query(INSERT)
            .bind(&row.vessel_imo)
            .bind(&row.voyage_number)
            .bind(row.leg_sequence)
            .bind(row.port_id)
            .bind(row.eta)
            .bind(row.etd)
            .bind(row.ata)
            .bind(row.berth_shift_time)
            .bind(row.pilot_required)
            .bind(row.moves_discharged)
            .bind(row.moves_loaded)
            .execute(&self.pool)
            .await?;
        Ok(())
    }

    async fn read(&self, key: &VoyageLegKey) -> Result<VoyageLeg, Error> {
        sqlx::query_as::<_, VoyageLeg>(READ)
            .bind(&key.vessel_imo)
            .bind(&key.voyage_number)
            .bind(key.leg_sequence)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &VoyageLeg) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(row.port_id)
            .bind(row.eta)
            .bind(row.etd)
            .bind(row.ata)
            .bind(row.berth_shift_time)
            .bind(row.pilot_required)
            .bind(row.moves_discharged)
            .bind(row.moves_loaded)
            .bind(&row.vessel_imo)
            .bind(&row.voyage_number)
            .bind(row.leg_sequence)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &VoyageLegKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(&key.vessel_imo)
            .bind(&key.voyage_number)
            .bind(key.leg_sequence)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: VoyageLegFilter,
        order: &[VoyageLegOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<VoyageLeg>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.vessel_imo {
                conditions
                    .push("`vessel_imo` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.vessel_imo_like {
                conditions
                    .push("`vessel_imo` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.voyage_number {
                conditions
                    .push("`voyage_number` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.voyage_number_like {
                conditions
                    .push("`voyage_number` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.leg_sequence {
                conditions
                    .push("`leg_sequence` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.leg_sequence_from {
                conditions
                    .push("`leg_sequence` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.leg_sequence_to {
                conditions
                    .push("`leg_sequence` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.port_id {
                conditions
                    .push("`port_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.port_id_from {
                conditions
                    .push("`port_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.port_id_to {
                conditions
                    .push("`port_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.eta {
                conditions
                    .push("`eta` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.eta_from {
                conditions
                    .push("`eta` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.eta_to {
                conditions
                    .push("`eta` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.etd {
                conditions
                    .push("`etd` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.etd_from {
                conditions
                    .push("`etd` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.etd_to {
                conditions
                    .push("`etd` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.ata {
                conditions
                    .push("`ata` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.ata_from {
                conditions
                    .push("`ata` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.ata_to {
                conditions
                    .push("`ata` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.ata_is_null {
                conditions.push(if is_null {
                    "`ata` IS NULL"
                } else {
                    "`ata` IS NOT NULL"
                });
            }
            if let Some(value) = filter.berth_shift_time {
                conditions
                    .push("`berth_shift_time` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.berth_shift_time_from {
                conditions
                    .push("`berth_shift_time` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.berth_shift_time_to {
                conditions
                    .push("`berth_shift_time` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.berth_shift_time_is_null {
                conditions.push(if is_null {
                    "`berth_shift_time` IS NULL"
                } else {
                    "`berth_shift_time` IS NOT NULL"
                });
            }
            if let Some(value) = filter.pilot_required {
                conditions
                    .push("`pilot_required` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.pilot_required_from {
                conditions
                    .push("`pilot_required` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.pilot_required_to {
                conditions
                    .push("`pilot_required` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.moves_discharged {
                conditions
                    .push("`moves_discharged` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.moves_discharged_from {
                conditions
                    .push("`moves_discharged` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.moves_discharged_to {
                conditions
                    .push("`moves_discharged` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.moves_loaded {
                conditions
                    .push("`moves_loaded` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.moves_loaded_from {
                conditions
                    .push("`moves_loaded` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.moves_loaded_to {
                conditions
                    .push("`moves_loaded` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.moves_total {
                conditions
                    .push("`moves_total` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.moves_total_from {
                conditions
                    .push("`moves_total` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.moves_total_to {
                conditions
                    .push("`moves_total` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.moves_total_is_null {
                conditions.push(if is_null {
                    "`moves_total` IS NULL"
                } else {
                    "`moves_total` IS NOT NULL"
                });
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<VoyageLeg>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
