// Code generated by tpl from freight.voyage_leg. DO NOT EDIT.

//! The `freight.voyage_leg` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.voyage_leg`.
///
/// Escalas de cada viagem, pela ordem da rotação
#[derive(Debug, Clone)]
pub struct VoyageLeg {
    /// `vessel_imo`, declared `char(7)` NOT NULL.
    ///
    /// It is part of the primary key.
    pub vessel_imo: String,
    /// `voyage_number`, declared `varchar(12)` NOT NULL.
    ///
    /// It is part of the primary key.
    pub voyage_number: String,
    /// `leg_sequence`, declared `tinyint(3) unsigned` NOT NULL.
    ///
    /// It is part of the primary key.
    ///
    /// Rotation position, 1 for the first call
    pub leg_sequence: u8,
    /// `port_id`, declared `smallint(5) unsigned` NOT NULL.
    pub port_id: u16,
    /// `eta`, declared `datetime` NOT NULL.
    pub eta: sqlx::types::chrono::NaiveDateTime,
    /// `etd`, declared `datetime` NOT NULL.
    pub etd: sqlx::types::chrono::NaiveDateTime,
    /// `ata`, declared `datetime(6)` NULL.
    ///
    /// Actual arrival, microsecond precision as reported by AIS
    pub ata: Option<sqlx::types::chrono::NaiveDateTime>,
    /// `berth_shift_time`, declared `time(3)` NULL.
    ///
    /// Time spent shifting between berths during the call
    pub berth_shift_time: Option<sqlx::mysql::types::MySqlTime>,
    /// `pilot_required`, declared `tinyint(1)` NOT NULL.
    pub pilot_required: i8,
    /// `moves_discharged`, declared `smallint(5) unsigned` NOT NULL.
    pub moves_discharged: u16,
    /// `moves_loaded`, declared `smallint(5) unsigned` NOT NULL.
    pub moves_loaded: u16,
    /// `moves_total`, declared `smallint(5) unsigned` NULL VIRTUAL GENERATED AS (`moves_discharged` + `moves_loaded`).
    ///
    /// Virtual generated column: total crane moves for the call
    pub moves_total: Option<u16>,
}

impl VoyageLeg {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> VoyageLegKey {
        VoyageLegKey {
            vessel_imo: self.vessel_imo.clone(),
            voyage_number: self.voyage_number.clone(),
            leg_sequence: self.leg_sequence,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for VoyageLeg {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            vessel_imo: row.try_get("vessel_imo")?,
            voyage_number: row.try_get("voyage_number")?,
            leg_sequence: row.try_get("leg_sequence")?,
            port_id: row.try_get("port_id")?,
            eta: row.try_get("eta")?,
            etd: row.try_get("etd")?,
            ata: row.try_get("ata")?,
            berth_shift_time: row.try_get("berth_shift_time")?,
            pilot_required: row.try_get("pilot_required")?,
            moves_discharged: row.try_get("moves_discharged")?,
            moves_loaded: row.try_get("moves_loaded")?,
            moves_total: row.try_get("moves_total")?,
        })
    }
}

/// The primary key of `freight.voyage_leg`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct VoyageLegKey {
    /// `vessel_imo`, declared `char(7)`.
    pub vessel_imo: String,
    /// `voyage_number`, declared `varchar(12)`.
    pub voyage_number: String,
    /// `leg_sequence`, declared `tinyint(3) unsigned`.
    pub leg_sequence: u8,
}

/// One column of `freight.voyage_leg`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: VoyageLegColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum VoyageLegColumn {
    /// `vessel_imo`, declared `char(7)`.
    VesselImo,
    /// `voyage_number`, declared `varchar(12)`.
    VoyageNumber,
    /// `leg_sequence`, declared `tinyint(3) unsigned`.
    LegSequence,
    /// `port_id`, declared `smallint(5) unsigned`.
    PortId,
    /// `eta`, declared `datetime`.
    Eta,
    /// `etd`, declared `datetime`.
    Etd,
    /// `ata`, declared `datetime(6)`.
    Ata,
    /// `berth_shift_time`, declared `time(3)`.
    BerthShiftTime,
    /// `pilot_required`, declared `tinyint(1)`.
    PilotRequired,
    /// `moves_discharged`, declared `smallint(5) unsigned`.
    MovesDischarged,
    /// `moves_loaded`, declared `smallint(5) unsigned`.
    MovesLoaded,
    /// `moves_total`, declared `smallint(5) unsigned`.
    MovesTotal,
}

impl VoyageLegColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::VesselImo => "`vessel_imo`",
            Self::VoyageNumber => "`voyage_number`",
            Self::LegSequence => "`leg_sequence`",
            Self::PortId => "`port_id`",
            Self::Eta => "`eta`",
            Self::Etd => "`etd`",
            Self::Ata => "`ata`",
            Self::BerthShiftTime => "`berth_shift_time`",
            Self::PilotRequired => "`pilot_required`",
            Self::MovesDischarged => "`moves_discharged`",
            Self::MovesLoaded => "`moves_loaded`",
            Self::MovesTotal => "`moves_total`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::VesselImo => "vessel_imo",
            Self::VoyageNumber => "voyage_number",
            Self::LegSequence => "leg_sequence",
            Self::PortId => "port_id",
            Self::Eta => "eta",
            Self::Etd => "etd",
            Self::Ata => "ata",
            Self::BerthShiftTime => "berth_shift_time",
            Self::PilotRequired => "pilot_required",
            Self::MovesDischarged => "moves_discharged",
            Self::MovesLoaded => "moves_loaded",
            Self::MovesTotal => "moves_total",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::VesselImo,
            Self::VoyageNumber,
            Self::LegSequence,
            Self::PortId,
            Self::Eta,
            Self::Etd,
            Self::Ata,
            Self::BerthShiftTime,
            Self::PilotRequired,
            Self::MovesDischarged,
            Self::MovesLoaded,
            Self::MovesTotal,
        ]
    }
}

impl std::fmt::Display for VoyageLegColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for VoyageLegColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "vessel_imo" => Ok(Self::VesselImo),
            "voyage_number" => Ok(Self::VoyageNumber),
            "leg_sequence" => Ok(Self::LegSequence),
            "port_id" => Ok(Self::PortId),
            "eta" => Ok(Self::Eta),
            "etd" => Ok(Self::Etd),
            "ata" => Ok(Self::Ata),
            "berth_shift_time" => Ok(Self::BerthShiftTime),
            "pilot_required" => Ok(Self::PilotRequired),
            "moves_discharged" => Ok(Self::MovesDischarged),
            "moves_loaded" => Ok(Self::MovesLoaded),
            "moves_total" => Ok(Self::MovesTotal),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.voyage_leg",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.voyage_leg`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct VoyageLegOrder {
    /// The column to order by.
    pub column: VoyageLegColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.voyage_leg`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct VoyageLegFilter {
    /// Where set, requires `vessel_imo` to equal it.
    pub vessel_imo: Option<String>,
    /// Where set, requires `vessel_imo` to match it, as `LIKE` matches.
    pub vessel_imo_like: Option<String>,
    /// Where set, requires `voyage_number` to equal it.
    pub voyage_number: Option<String>,
    /// Where set, requires `voyage_number` to match it, as `LIKE` matches.
    pub voyage_number_like: Option<String>,
    /// Where set, requires `leg_sequence` to equal it.
    pub leg_sequence: Option<u8>,
    /// Where set, requires `leg_sequence` to be at least it.
    pub leg_sequence_from: Option<u8>,
    /// Where set, requires `leg_sequence` to be at most it.
    pub leg_sequence_to: Option<u8>,
    /// Where set, requires `port_id` to equal it.
    pub port_id: Option<u16>,
    /// Where set, requires `port_id` to be at least it.
    pub port_id_from: Option<u16>,
    /// Where set, requires `port_id` to be at most it.
    pub port_id_to: Option<u16>,
    /// Where set, requires `eta` to equal it.
    pub eta: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `eta` to be at least it.
    pub eta_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `eta` to be at most it.
    pub eta_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `etd` to equal it.
    pub etd: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `etd` to be at least it.
    pub etd_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `etd` to be at most it.
    pub etd_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `ata` to equal it.
    pub ata: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `ata` to be at least it.
    pub ata_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `ata` to be at most it.
    pub ata_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `ata` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub ata_is_null: Option<bool>,
    /// Where set, requires `berth_shift_time` to equal it.
    pub berth_shift_time: Option<sqlx::mysql::types::MySqlTime>,
    /// Where set, requires `berth_shift_time` to be at least it.
    pub berth_shift_time_from: Option<sqlx::mysql::types::MySqlTime>,
    /// Where set, requires `berth_shift_time` to be at most it.
    pub berth_shift_time_to: Option<sqlx::mysql::types::MySqlTime>,
    /// Where set, requires `berth_shift_time` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub berth_shift_time_is_null: Option<bool>,
    /// Where set, requires `pilot_required` to equal it.
    pub pilot_required: Option<i8>,
    /// Where set, requires `pilot_required` to be at least it.
    pub pilot_required_from: Option<i8>,
    /// Where set, requires `pilot_required` to be at most it.
    pub pilot_required_to: Option<i8>,
    /// Where set, requires `moves_discharged` to equal it.
    pub moves_discharged: Option<u16>,
    /// Where set, requires `moves_discharged` to be at least it.
    pub moves_discharged_from: Option<u16>,
    /// Where set, requires `moves_discharged` to be at most it.
    pub moves_discharged_to: Option<u16>,
    /// Where set, requires `moves_loaded` to equal it.
    pub moves_loaded: Option<u16>,
    /// Where set, requires `moves_loaded` to be at least it.
    pub moves_loaded_from: Option<u16>,
    /// Where set, requires `moves_loaded` to be at most it.
    pub moves_loaded_to: Option<u16>,
    /// Where set, requires `moves_total` to equal it.
    pub moves_total: Option<u16>,
    /// Where set, requires `moves_total` to be at least it.
    pub moves_total_from: Option<u16>,
    /// Where set, requires `moves_total` to be at most it.
    pub moves_total_to: Option<u16>,
    /// Where set, requires `moves_total` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub moves_total_is_null: Option<bool>,
}

impl VoyageLegFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.vessel_imo.is_none()
            && self.vessel_imo_like.is_none()
            && self.voyage_number.is_none()
            && self.voyage_number_like.is_none()
            && self.leg_sequence.is_none()
            && self.leg_sequence_from.is_none()
            && self.leg_sequence_to.is_none()
            && self.port_id.is_none()
            && self.port_id_from.is_none()
            && self.port_id_to.is_none()
            && self.eta.is_none()
            && self.eta_from.is_none()
            && self.eta_to.is_none()
            && self.etd.is_none()
            && self.etd_from.is_none()
            && self.etd_to.is_none()
            && self.ata.is_none()
            && self.ata_from.is_none()
            && self.ata_to.is_none()
            && self.ata_is_null.is_none()
            && self.berth_shift_time.is_none()
            && self.berth_shift_time_from.is_none()
            && self.berth_shift_time_to.is_none()
            && self.berth_shift_time_is_null.is_none()
            && self.pilot_required.is_none()
            && self.pilot_required_from.is_none()
            && self.pilot_required_to.is_none()
            && self.moves_discharged.is_none()
            && self.moves_discharged_from.is_none()
            && self.moves_discharged_to.is_none()
            && self.moves_loaded.is_none()
            && self.moves_loaded_from.is_none()
            && self.moves_loaded_to.is_none()
            && self.moves_total.is_none()
            && self.moves_total_from.is_none()
            && self.moves_total_to.is_none()
            && self.moves_total_is_null.is_none()
    }
}
