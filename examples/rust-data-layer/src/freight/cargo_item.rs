// Code generated by tpl from freight.cargo_item. DO NOT EDIT.

//! The `freight.cargo_item` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.cargo_item`.
///
/// Linhas de mercadoria de cada expedição
#[derive(Debug, Clone)]
pub struct CargoItem {
    /// `cargo_item_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub cargo_item_id: u64,
    /// `consignment_id`, declared `bigint(20) unsigned` NOT NULL.
    ///
    /// It is part of a unique index.
    pub consignment_id: u64,
    /// `container_id`, declared `bigint(20) unsigned` NULL.
    ///
    /// NULL while the item is loose cargo awaiting consolidation
    pub container_id: Option<u64>,
    /// `line_number`, declared `smallint(5) unsigned` NOT NULL.
    ///
    /// It is part of a unique index.
    pub line_number: u16,
    /// `hs_code`, declared `varchar(12)` NOT NULL.
    ///
    /// Harmonised System commodity code
    pub hs_code: String,
    /// `description`, declared `text` NOT NULL.
    ///
    /// Commercial description; indexed FULLTEXT for the tariff classification desk
    pub description: String,
    /// `package_kind`, declared `enum('Pallet','Crate','Drum','Bale','Carton','Big bag','Roll','Loose')` NOT NULL.
    pub package_kind: String,
    /// `package_count`, declared `mediumint(8) unsigned` NOT NULL.
    pub package_count: u32,
    /// `gross_weight_kg`, declared `decimal(12,3)` NOT NULL.
    pub gross_weight_kg: super::Decimal,
    /// `net_weight_kg`, declared `decimal(12,3)` NOT NULL.
    pub net_weight_kg: super::Decimal,
    /// `volume_cbm`, declared `decimal(10,3)` NOT NULL.
    pub volume_cbm: super::Decimal,
    /// `chargeable_weight_kg`, declared `decimal(12,3)` NULL VIRTUAL GENERATED AS (greatest(`gross_weight_kg`,`volume_cbm` * 167)).
    ///
    /// Virtual generated column: IATA-style volumetric rule at 167 kg per cubic metre
    pub chargeable_weight_kg: Option<super::Decimal>,
    /// `imdg_class`, declared `enum('Not regulated','Class 3, Flammable liquids','Class 8, Corrosives','Class 9, Miscellaneous dangerous goods')` NOT NULL.
    ///
    /// Note the commas inside three of the members
    pub imdg_class: String,
    /// `un_number`, declared `smallint(5) unsigned` NULL.
    ///
    /// UN dangerous goods number, 1 to 3550
    pub un_number: Option<u16>,
    /// `origin_country`, declared `char(2)` NOT NULL.
    pub origin_country: String,
    /// `unit_value`, declared `decimal(12,2)` NOT NULL.
    pub unit_value: super::Decimal,
    /// `line_value`, declared `decimal(14,2)` NULL STORED GENERATED AS (round(`unit_value` * `package_count`,2)).
    ///
    /// Stored generated column: value declared for this line
    pub line_value: Option<super::Decimal>,
}

impl CargoItem {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> CargoItemKey {
        CargoItemKey {
            cargo_item_id: self.cargo_item_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for CargoItem {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            cargo_item_id: row.try_get("cargo_item_id")?,
            consignment_id: row.try_get("consignment_id")?,
            container_id: row.try_get("container_id")?,
            line_number: row.try_get("line_number")?,
            hs_code: row.try_get("hs_code")?,
            description: row.try_get("description")?,
            package_kind: row.try_get("package_kind")?,
            package_count: row.try_get("package_count")?,
            gross_weight_kg: row.try_get("gross_weight_kg")?,
            net_weight_kg: row.try_get("net_weight_kg")?,
            volume_cbm: row.try_get("volume_cbm")?,
            chargeable_weight_kg: row.try_get("chargeable_weight_kg")?,
            imdg_class: row.try_get("imdg_class")?,
            un_number: row.try_get("un_number")?,
            origin_country: row.try_get("origin_country")?,
            unit_value: row.try_get("unit_value")?,
            line_value: row.try_get("line_value")?,
        })
    }
}

/// The primary key of `freight.cargo_item`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct CargoItemKey {
    /// `cargo_item_id`, declared `bigint(20) unsigned`.
    pub cargo_item_id: u64,
}

/// One column of `freight.cargo_item`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: CargoItemColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum CargoItemColumn {
    /// `cargo_item_id`, declared `bigint(20) unsigned`.
    CargoItemId,
    /// `consignment_id`, declared `bigint(20) unsigned`.
    ConsignmentId,
    /// `container_id`, declared `bigint(20) unsigned`.
    ContainerId,
    /// `line_number`, declared `smallint(5) unsigned`.
    LineNumber,
    /// `hs_code`, declared `varchar(12)`.
    HsCode,
    /// `description`, declared `text`.
    Description,
    /// `package_kind`, declared `enum('Pallet','Crate','Drum','Bale','Carton','Big bag','Roll','Loose')`.
    PackageKind,
    /// `package_count`, declared `mediumint(8) unsigned`.
    PackageCount,
    /// `gross_weight_kg`, declared `decimal(12,3)`.
    GrossWeightKg,
    /// `net_weight_kg`, declared `decimal(12,3)`.
    NetWeightKg,
    /// `volume_cbm`, declared `decimal(10,3)`.
    VolumeCbm,
    /// `chargeable_weight_kg`, declared `decimal(12,3)`.
    ChargeableWeightKg,
    /// `imdg_class`, declared `enum('Not regulated','Class 3, Flammable liquids','Class 8, Corrosives','Class 9, Miscellaneous dangerous goods')`.
    ImdgClass,
    /// `un_number`, declared `smallint(5) unsigned`.
    UnNumber,
    /// `origin_country`, declared `char(2)`.
    OriginCountry,
    /// `unit_value`, declared `decimal(12,2)`.
    UnitValue,
    /// `line_value`, declared `decimal(14,2)`.
    LineValue,
}

impl CargoItemColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::CargoItemId => "`cargo_item_id`",
            Self::ConsignmentId => "`consignment_id`",
            Self::ContainerId => "`container_id`",
            Self::LineNumber => "`line_number`",
            Self::HsCode => "`hs_code`",
            Self::Description => "`description`",
            Self::PackageKind => "`package_kind`",
            Self::PackageCount => "`package_count`",
            Self::GrossWeightKg => "`gross_weight_kg`",
            Self::NetWeightKg => "`net_weight_kg`",
            Self::VolumeCbm => "`volume_cbm`",
            Self::ChargeableWeightKg => "`chargeable_weight_kg`",
            Self::ImdgClass => "`imdg_class`",
            Self::UnNumber => "`un_number`",
            Self::OriginCountry => "`origin_country`",
            Self::UnitValue => "`unit_value`",
            Self::LineValue => "`line_value`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::CargoItemId => "cargo_item_id",
            Self::ConsignmentId => "consignment_id",
            Self::ContainerId => "container_id",
            Self::LineNumber => "line_number",
            Self::HsCode => "hs_code",
            Self::Description => "description",
            Self::PackageKind => "package_kind",
            Self::PackageCount => "package_count",
            Self::GrossWeightKg => "gross_weight_kg",
            Self::NetWeightKg => "net_weight_kg",
            Self::VolumeCbm => "volume_cbm",
            Self::ChargeableWeightKg => "chargeable_weight_kg",
            Self::ImdgClass => "imdg_class",
            Self::UnNumber => "un_number",
            Self::OriginCountry => "origin_country",
            Self::UnitValue => "unit_value",
            Self::LineValue => "line_value",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::CargoItemId,
            Self::ConsignmentId,
            Self::ContainerId,
            Self::LineNumber,
            Self::HsCode,
            Self::Description,
            Self::PackageKind,
            Self::PackageCount,
            Self::GrossWeightKg,
            Self::NetWeightKg,
            Self::VolumeCbm,
            Self::ChargeableWeightKg,
            Self::ImdgClass,
            Self::UnNumber,
            Self::OriginCountry,
            Self::UnitValue,
            Self::LineValue,
        ]
    }
}

impl std::fmt::Display for CargoItemColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for CargoItemColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "cargo_item_id" => Ok(Self::CargoItemId),
            "consignment_id" => Ok(Self::ConsignmentId),
            "container_id" => Ok(Self::ContainerId),
            "line_number" => Ok(Self::LineNumber),
            "hs_code" => Ok(Self::HsCode),
            "description" => Ok(Self::Description),
            "package_kind" => Ok(Self::PackageKind),
            "package_count" => Ok(Self::PackageCount),
            "gross_weight_kg" => Ok(Self::GrossWeightKg),
            "net_weight_kg" => Ok(Self::NetWeightKg),
            "volume_cbm" => Ok(Self::VolumeCbm),
            "chargeable_weight_kg" => Ok(Self::ChargeableWeightKg),
            "imdg_class" => Ok(Self::ImdgClass),
            "un_number" => Ok(Self::UnNumber),
            "origin_country" => Ok(Self::OriginCountry),
            "unit_value" => Ok(Self::UnitValue),
            "line_value" => Ok(Self::LineValue),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.cargo_item",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.cargo_item`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct CargoItemOrder {
    /// The column to order by.
    pub column: CargoItemColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.cargo_item`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct CargoItemFilter {
    /// Where set, requires `cargo_item_id` to equal it.
    pub cargo_item_id: Option<u64>,
    /// Where set, requires `cargo_item_id` to be at least it.
    pub cargo_item_id_from: Option<u64>,
    /// Where set, requires `cargo_item_id` to be at most it.
    pub cargo_item_id_to: Option<u64>,
    /// Where set, requires `consignment_id` to equal it.
    pub consignment_id: Option<u64>,
    /// Where set, requires `consignment_id` to be at least it.
    pub consignment_id_from: Option<u64>,
    /// Where set, requires `consignment_id` to be at most it.
    pub consignment_id_to: Option<u64>,
    /// Where set, requires `container_id` to equal it.
    pub container_id: Option<u64>,
    /// Where set, requires `container_id` to be at least it.
    pub container_id_from: Option<u64>,
    /// Where set, requires `container_id` to be at most it.
    pub container_id_to: Option<u64>,
    /// Where set, requires `container_id` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub container_id_is_null: Option<bool>,
    /// Where set, requires `line_number` to equal it.
    pub line_number: Option<u16>,
    /// Where set, requires `line_number` to be at least it.
    pub line_number_from: Option<u16>,
    /// Where set, requires `line_number` to be at most it.
    pub line_number_to: Option<u16>,
    /// Where set, requires `hs_code` to equal it.
    pub hs_code: Option<String>,
    /// Where set, requires `hs_code` to match it, as `LIKE` matches.
    pub hs_code_like: Option<String>,
    /// Where set, requires `description` to equal it.
    pub description: Option<String>,
    /// Where set, requires `description` to match it, as `LIKE` matches.
    pub description_like: Option<String>,
    /// Where set, requires `package_kind` to equal it.
    pub package_kind: Option<String>,
    /// Where set, requires `package_kind` to match it, as `LIKE` matches.
    pub package_kind_like: Option<String>,
    /// Where set, requires `package_count` to equal it.
    pub package_count: Option<u32>,
    /// Where set, requires `package_count` to be at least it.
    pub package_count_from: Option<u32>,
    /// Where set, requires `package_count` to be at most it.
    pub package_count_to: Option<u32>,
    /// Where set, requires `gross_weight_kg` to equal it.
    pub gross_weight_kg: Option<super::Decimal>,
    /// Where set, requires `gross_weight_kg` to be at least it.
    pub gross_weight_kg_from: Option<super::Decimal>,
    /// Where set, requires `gross_weight_kg` to be at most it.
    pub gross_weight_kg_to: Option<super::Decimal>,
    /// Where set, requires `net_weight_kg` to equal it.
    pub net_weight_kg: Option<super::Decimal>,
    /// Where set, requires `net_weight_kg` to be at least it.
    pub net_weight_kg_from: Option<super::Decimal>,
    /// Where set, requires `net_weight_kg` to be at most it.
    pub net_weight_kg_to: Option<super::Decimal>,
    /// Where set, requires `volume_cbm` to equal it.
    pub volume_cbm: Option<super::Decimal>,
    /// Where set, requires `volume_cbm` to be at least it.
    pub volume_cbm_from: Option<super::Decimal>,
    /// Where set, requires `volume_cbm` to be at most it.
    pub volume_cbm_to: Option<super::Decimal>,
    /// Where set, requires `chargeable_weight_kg` to equal it.
    pub chargeable_weight_kg: Option<super::Decimal>,
    /// Where set, requires `chargeable_weight_kg` to be at least it.
    pub chargeable_weight_kg_from: Option<super::Decimal>,
    /// Where set, requires `chargeable_weight_kg` to be at most it.
    pub chargeable_weight_kg_to: Option<super::Decimal>,
    /// Where set, requires `chargeable_weight_kg` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub chargeable_weight_kg_is_null: Option<bool>,
    /// Where set, requires `imdg_class` to equal it.
    pub imdg_class: Option<String>,
    /// Where set, requires `imdg_class` to match it, as `LIKE` matches.
    pub imdg_class_like: Option<String>,
    /// Where set, requires `un_number` to equal it.
    pub un_number: Option<u16>,
    /// Where set, requires `un_number` to be at least it.
    pub un_number_from: Option<u16>,
    /// Where set, requires `un_number` to be at most it.
    pub un_number_to: Option<u16>,
    /// Where set, requires `un_number` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub un_number_is_null: Option<bool>,
    /// Where set, requires `origin_country` to equal it.
    pub origin_country: Option<String>,
    /// Where set, requires `origin_country` to match it, as `LIKE` matches.
    pub origin_country_like: Option<String>,
    /// Where set, requires `unit_value` to equal it.
    pub unit_value: Option<super::Decimal>,
    /// Where set, requires `unit_value` to be at least it.
    pub unit_value_from: Option<super::Decimal>,
    /// Where set, requires `unit_value` to be at most it.
    pub unit_value_to: Option<super::Decimal>,
    /// Where set, requires `line_value` to equal it.
    pub line_value: Option<super::Decimal>,
    /// Where set, requires `line_value` to be at least it.
    pub line_value_from: Option<super::Decimal>,
    /// Where set, requires `line_value` to be at most it.
    pub line_value_to: Option<super::Decimal>,
    /// Where set, requires `line_value` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub line_value_is_null: Option<bool>,
}

impl CargoItemFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.cargo_item_id.is_none()
            && self.cargo_item_id_from.is_none()
            && self.cargo_item_id_to.is_none()
            && self.consignment_id.is_none()
            && self.consignment_id_from.is_none()
            && self.consignment_id_to.is_none()
            && self.container_id.is_none()
            && self.container_id_from.is_none()
            && self.container_id_to.is_none()
            && self.container_id_is_null.is_none()
            && self.line_number.is_none()
            && self.line_number_from.is_none()
            && self.line_number_to.is_none()
            && self.hs_code.is_none()
            && self.hs_code_like.is_none()
            && self.description.is_none()
            && self.description_like.is_none()
            && self.package_kind.is_none()
            && self.package_kind_like.is_none()
            && self.package_count.is_none()
            && self.package_count_from.is_none()
            && self.package_count_to.is_none()
            && self.gross_weight_kg.is_none()
            && self.gross_weight_kg_from.is_none()
            && self.gross_weight_kg_to.is_none()
            && self.net_weight_kg.is_none()
            && self.net_weight_kg_from.is_none()
            && self.net_weight_kg_to.is_none()
            && self.volume_cbm.is_none()
            && self.volume_cbm_from.is_none()
            && self.volume_cbm_to.is_none()
            && self.chargeable_weight_kg.is_none()
            && self.chargeable_weight_kg_from.is_none()
            && self.chargeable_weight_kg_to.is_none()
            && self.chargeable_weight_kg_is_null.is_none()
            && self.imdg_class.is_none()
            && self.imdg_class_like.is_none()
            && self.un_number.is_none()
            && self.un_number_from.is_none()
            && self.un_number_to.is_none()
            && self.un_number_is_null.is_none()
            && self.origin_country.is_none()
            && self.origin_country_like.is_none()
            && self.unit_value.is_none()
            && self.unit_value_from.is_none()
            && self.unit_value_to.is_none()
            && self.line_value.is_none()
            && self.line_value_from.is_none()
            && self.line_value_to.is_none()
            && self.line_value_is_null.is_none()
    }
}
