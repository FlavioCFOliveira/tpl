// Code generated by tpl from freight.tariff. DO NOT EDIT.

//! The `freight.tariff` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.tariff`.
///
/// Tabela de fretes com versionamento de sistema: uma tarifa substituída continua consultável
#[derive(Debug, Clone)]
pub struct Tariff {
    /// `tariff_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub tariff_id: u32,
    /// `trade_lane`, declared `varchar(40)` NOT NULL.
    ///
    /// e.g. North Europe – West Africa
    pub trade_lane: String,
    /// `origin_region`, declared `varchar(40)` NOT NULL.
    pub origin_region: String,
    /// `destination_region`, declared `varchar(40)` NOT NULL.
    pub destination_region: String,
    /// `equipment_iso_code`, declared `char(4)` NOT NULL.
    pub equipment_iso_code: String,
    /// `basis`, declared `enum('Per container','Per freight tonne','Per cubic metre','Per shipment','Per bill of lading')` NOT NULL.
    pub basis: String,
    /// `currency`, declared `char(3)` NOT NULL.
    pub currency: String,
    /// `base_rate`, declared `decimal(11,2)` NOT NULL.
    pub base_rate: super::Decimal,
    /// `bunker_surcharge`, declared `decimal(11,2)` NOT NULL.
    pub bunker_surcharge: super::Decimal,
    /// `congestion_surcharge`, declared `decimal(11,2)` NOT NULL.
    pub congestion_surcharge: super::Decimal,
    /// `all_in_rate`, declared `decimal(11,2)` NULL STORED GENERATED AS (`base_rate` + `bunker_surcharge` + `congestion_surcharge`).
    ///
    /// Stored generated column: what the customer is quoted
    pub all_in_rate: Option<super::Decimal>,
    /// `valid_from`, declared `date` NOT NULL.
    pub valid_from: sqlx::types::chrono::NaiveDate,
    /// `valid_until`, declared `date` NOT NULL.
    pub valid_until: sqlx::types::chrono::NaiveDate,
}

impl Tariff {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> TariffKey {
        TariffKey {
            tariff_id: self.tariff_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Tariff {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            tariff_id: row.try_get("tariff_id")?,
            trade_lane: row.try_get("trade_lane")?,
            origin_region: row.try_get("origin_region")?,
            destination_region: row.try_get("destination_region")?,
            equipment_iso_code: row.try_get("equipment_iso_code")?,
            basis: row.try_get("basis")?,
            currency: row.try_get("currency")?,
            base_rate: row.try_get("base_rate")?,
            bunker_surcharge: row.try_get("bunker_surcharge")?,
            congestion_surcharge: row.try_get("congestion_surcharge")?,
            all_in_rate: row.try_get("all_in_rate")?,
            valid_from: row.try_get("valid_from")?,
            valid_until: row.try_get("valid_until")?,
        })
    }
}

/// The primary key of `freight.tariff`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct TariffKey {
    /// `tariff_id`, declared `int(10) unsigned`.
    pub tariff_id: u32,
}

/// One column of `freight.tariff`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: TariffColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum TariffColumn {
    /// `tariff_id`, declared `int(10) unsigned`.
    TariffId,
    /// `trade_lane`, declared `varchar(40)`.
    TradeLane,
    /// `origin_region`, declared `varchar(40)`.
    OriginRegion,
    /// `destination_region`, declared `varchar(40)`.
    DestinationRegion,
    /// `equipment_iso_code`, declared `char(4)`.
    EquipmentIsoCode,
    /// `basis`, declared `enum('Per container','Per freight tonne','Per cubic metre','Per shipment','Per bill of lading')`.
    Basis,
    /// `currency`, declared `char(3)`.
    Currency,
    /// `base_rate`, declared `decimal(11,2)`.
    BaseRate,
    /// `bunker_surcharge`, declared `decimal(11,2)`.
    BunkerSurcharge,
    /// `congestion_surcharge`, declared `decimal(11,2)`.
    CongestionSurcharge,
    /// `all_in_rate`, declared `decimal(11,2)`.
    AllInRate,
    /// `valid_from`, declared `date`.
    ValidFrom,
    /// `valid_until`, declared `date`.
    ValidUntil,
}

impl TariffColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::TariffId => "`tariff_id`",
            Self::TradeLane => "`trade_lane`",
            Self::OriginRegion => "`origin_region`",
            Self::DestinationRegion => "`destination_region`",
            Self::EquipmentIsoCode => "`equipment_iso_code`",
            Self::Basis => "`basis`",
            Self::Currency => "`currency`",
            Self::BaseRate => "`base_rate`",
            Self::BunkerSurcharge => "`bunker_surcharge`",
            Self::CongestionSurcharge => "`congestion_surcharge`",
            Self::AllInRate => "`all_in_rate`",
            Self::ValidFrom => "`valid_from`",
            Self::ValidUntil => "`valid_until`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::TariffId => "tariff_id",
            Self::TradeLane => "trade_lane",
            Self::OriginRegion => "origin_region",
            Self::DestinationRegion => "destination_region",
            Self::EquipmentIsoCode => "equipment_iso_code",
            Self::Basis => "basis",
            Self::Currency => "currency",
            Self::BaseRate => "base_rate",
            Self::BunkerSurcharge => "bunker_surcharge",
            Self::CongestionSurcharge => "congestion_surcharge",
            Self::AllInRate => "all_in_rate",
            Self::ValidFrom => "valid_from",
            Self::ValidUntil => "valid_until",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::TariffId,
            Self::TradeLane,
            Self::OriginRegion,
            Self::DestinationRegion,
            Self::EquipmentIsoCode,
            Self::Basis,
            Self::Currency,
            Self::BaseRate,
            Self::BunkerSurcharge,
            Self::CongestionSurcharge,
            Self::AllInRate,
            Self::ValidFrom,
            Self::ValidUntil,
        ]
    }
}

impl std::fmt::Display for TariffColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for TariffColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "tariff_id" => Ok(Self::TariffId),
            "trade_lane" => Ok(Self::TradeLane),
            "origin_region" => Ok(Self::OriginRegion),
            "destination_region" => Ok(Self::DestinationRegion),
            "equipment_iso_code" => Ok(Self::EquipmentIsoCode),
            "basis" => Ok(Self::Basis),
            "currency" => Ok(Self::Currency),
            "base_rate" => Ok(Self::BaseRate),
            "bunker_surcharge" => Ok(Self::BunkerSurcharge),
            "congestion_surcharge" => Ok(Self::CongestionSurcharge),
            "all_in_rate" => Ok(Self::AllInRate),
            "valid_from" => Ok(Self::ValidFrom),
            "valid_until" => Ok(Self::ValidUntil),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.tariff",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.tariff`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct TariffOrder {
    /// The column to order by.
    pub column: TariffColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.tariff`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct TariffFilter {
    /// Where set, requires `tariff_id` to equal it.
    pub tariff_id: Option<u32>,
    /// Where set, requires `tariff_id` to be at least it.
    pub tariff_id_from: Option<u32>,
    /// Where set, requires `tariff_id` to be at most it.
    pub tariff_id_to: Option<u32>,
    /// Where set, requires `trade_lane` to equal it.
    pub trade_lane: Option<String>,
    /// Where set, requires `trade_lane` to match it, as `LIKE` matches.
    pub trade_lane_like: Option<String>,
    /// Where set, requires `origin_region` to equal it.
    pub origin_region: Option<String>,
    /// Where set, requires `origin_region` to match it, as `LIKE` matches.
    pub origin_region_like: Option<String>,
    /// Where set, requires `destination_region` to equal it.
    pub destination_region: Option<String>,
    /// Where set, requires `destination_region` to match it, as `LIKE` matches.
    pub destination_region_like: Option<String>,
    /// Where set, requires `equipment_iso_code` to equal it.
    pub equipment_iso_code: Option<String>,
    /// Where set, requires `equipment_iso_code` to match it, as `LIKE` matches.
    pub equipment_iso_code_like: Option<String>,
    /// Where set, requires `basis` to equal it.
    pub basis: Option<String>,
    /// Where set, requires `basis` to match it, as `LIKE` matches.
    pub basis_like: Option<String>,
    /// Where set, requires `currency` to equal it.
    pub currency: Option<String>,
    /// Where set, requires `currency` to match it, as `LIKE` matches.
    pub currency_like: Option<String>,
    /// Where set, requires `base_rate` to equal it.
    pub base_rate: Option<super::Decimal>,
    /// Where set, requires `base_rate` to be at least it.
    pub base_rate_from: Option<super::Decimal>,
    /// Where set, requires `base_rate` to be at most it.
    pub base_rate_to: Option<super::Decimal>,
    /// Where set, requires `bunker_surcharge` to equal it.
    pub bunker_surcharge: Option<super::Decimal>,
    /// Where set, requires `bunker_surcharge` to be at least it.
    pub bunker_surcharge_from: Option<super::Decimal>,
    /// Where set, requires `bunker_surcharge` to be at most it.
    pub bunker_surcharge_to: Option<super::Decimal>,
    /// Where set, requires `congestion_surcharge` to equal it.
    pub congestion_surcharge: Option<super::Decimal>,
    /// Where set, requires `congestion_surcharge` to be at least it.
    pub congestion_surcharge_from: Option<super::Decimal>,
    /// Where set, requires `congestion_surcharge` to be at most it.
    pub congestion_surcharge_to: Option<super::Decimal>,
    /// Where set, requires `all_in_rate` to equal it.
    pub all_in_rate: Option<super::Decimal>,
    /// Where set, requires `all_in_rate` to be at least it.
    pub all_in_rate_from: Option<super::Decimal>,
    /// Where set, requires `all_in_rate` to be at most it.
    pub all_in_rate_to: Option<super::Decimal>,
    /// Where set, requires `all_in_rate` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub all_in_rate_is_null: Option<bool>,
    /// Where set, requires `valid_from` to equal it.
    pub valid_from: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `valid_from` to be at least it.
    pub valid_from_from: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `valid_from` to be at most it.
    pub valid_from_to: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `valid_until` to equal it.
    pub valid_until: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `valid_until` to be at least it.
    pub valid_until_from: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `valid_until` to be at most it.
    pub valid_until_to: Option<sqlx::types::chrono::NaiveDate>,
}

impl TariffFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.tariff_id.is_none()
            && self.tariff_id_from.is_none()
            && self.tariff_id_to.is_none()
            && self.trade_lane.is_none()
            && self.trade_lane_like.is_none()
            && self.origin_region.is_none()
            && self.origin_region_like.is_none()
            && self.destination_region.is_none()
            && self.destination_region_like.is_none()
            && self.equipment_iso_code.is_none()
            && self.equipment_iso_code_like.is_none()
            && self.basis.is_none()
            && self.basis_like.is_none()
            && self.currency.is_none()
            && self.currency_like.is_none()
            && self.base_rate.is_none()
            && self.base_rate_from.is_none()
            && self.base_rate_to.is_none()
            && self.bunker_surcharge.is_none()
            && self.bunker_surcharge_from.is_none()
            && self.bunker_surcharge_to.is_none()
            && self.congestion_surcharge.is_none()
            && self.congestion_surcharge_from.is_none()
            && self.congestion_surcharge_to.is_none()
            && self.all_in_rate.is_none()
            && self.all_in_rate_from.is_none()
            && self.all_in_rate_to.is_none()
            && self.all_in_rate_is_null.is_none()
            && self.valid_from.is_none()
            && self.valid_from_from.is_none()
            && self.valid_from_to.is_none()
            && self.valid_until.is_none()
            && self.valid_until_from.is_none()
            && self.valid_until_to.is_none()
    }
}
