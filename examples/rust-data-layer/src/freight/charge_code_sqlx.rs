// Code generated by tpl from freight.charge_code. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.charge_code` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::charge_code::{ChargeCode, ChargeCodeFilter, ChargeCodeKey, ChargeCodeOrder};
use super::charge_code_repository::ChargeCodeRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.charge_code";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `charge_code`, `description`, `is_freight`, `vat_rate`, `gl_account` FROM `charge_code`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `charge_code`, `description`, `is_freight`, `vat_rate`, `gl_account` FROM `charge_code` WHERE `charge_code` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `charge_code` (`charge_code`, `description`, `is_freight`, `vat_rate`, `gl_account`) VALUES (?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `charge_code` SET `description` = ?, `is_freight` = ?, `vat_rate` = ?, `gl_account` = ? WHERE `charge_code` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `charge_code` WHERE `charge_code` = ?";

/// The `sqlx` data layer of `freight.charge_code`.
#[derive(Debug, Clone)]
pub struct ChargeCodeSqlx {
    pool: sqlx::MySqlPool,
}

impl ChargeCodeSqlx {
    /// Builds the data layer of `freight.charge_code` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl ChargeCodeRepository for ChargeCodeSqlx {
    async fn create(&self, row: &mut ChargeCode) -> Result<(), Error> {
        sqlx::query(INSERT)
            .bind(&row.charge_code)
            .bind(&row.description)
            .bind(row.is_freight)
            .bind(&row.vat_rate)
            .bind(&row.gl_account)
            .execute(&self.pool)
            .await?;
        Ok(())
    }

    async fn read(&self, key: &ChargeCodeKey) -> Result<ChargeCode, Error> {
        sqlx::query_as::<_, ChargeCode>(READ)
            .bind(&key.charge_code)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &ChargeCode) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.description)
            .bind(row.is_freight)
            .bind(&row.vat_rate)
            .bind(&row.gl_account)
            .bind(&row.charge_code)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &ChargeCodeKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(&key.charge_code)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: ChargeCodeFilter,
        order: &[ChargeCodeOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<ChargeCode>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.charge_code {
                conditions
                    .push("`charge_code` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.charge_code_like {
                conditions
                    .push("`charge_code` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.description {
                conditions
                    .push("`description` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.description_like {
                conditions
                    .push("`description` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.is_freight {
                conditions
                    .push("`is_freight` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.is_freight_from {
                conditions
                    .push("`is_freight` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.is_freight_to {
                conditions
                    .push("`is_freight` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.vat_rate {
                conditions
                    .push("`vat_rate` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.vat_rate_from {
                conditions
                    .push("`vat_rate` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.vat_rate_to {
                conditions
                    .push("`vat_rate` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gl_account {
                conditions
                    .push("`gl_account` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gl_account_like {
                conditions
                    .push("`gl_account` LIKE ")
                    .push_bind_unseparated(value);
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<ChargeCode>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
