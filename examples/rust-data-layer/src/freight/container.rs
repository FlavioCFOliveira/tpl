// Code generated by tpl from freight.container. DO NOT EDIT.

//! The `freight.container` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.container`.
///
/// Contentores afectos a cada expedição, com massa bruta verificada (VGM)
#[derive(Debug, Clone)]
pub struct Container {
    /// `container_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub container_id: u64,
    /// `equipment_no`, declared `char(11)` NOT NULL.
    ///
    /// It is part of a unique index.
    ///
    /// ISO 6346 equipment number: four-letter prefix, six digits, one check digit
    pub equipment_no: String,
    /// `consignment_id`, declared `bigint(20) unsigned` NOT NULL.
    ///
    /// It is part of a unique index.
    pub consignment_id: u64,
    /// `iso_code`, declared `char(4)` NOT NULL.
    ///
    /// Defaults to the standard dry twenty-foot box
    pub iso_code: String,
    /// `seal_number`, declared `varchar(15)` NULL.
    pub seal_number: Option<String>,
    /// `tare_weight_kg`, declared `decimal(8,2)` NOT NULL.
    pub tare_weight_kg: super::Decimal,
    /// `cargo_weight_kg`, declared `decimal(10,3)` NOT NULL.
    pub cargo_weight_kg: super::Decimal,
    /// `vgm_kg`, declared `decimal(10,3)` NULL STORED GENERATED AS (`tare_weight_kg` + `cargo_weight_kg`).
    ///
    /// Stored generated column: SOLAS verified gross mass
    pub vgm_kg: Option<super::Decimal>,
    /// `vgm_method`, declared `enum('Method 1 — weighbridge','Method 2 — calculated','Not yet verified')` NOT NULL.
    pub vgm_method: String,
    /// `setpoint_celsius`, declared `decimal(4,1)` NULL.
    ///
    /// Reefer setpoint; NULL for a dry box
    pub setpoint_celsius: Option<super::Decimal>,
    /// `humidity_pct`, declared `tinyint(3) unsigned` NULL.
    pub humidity_pct: Option<u8>,
    /// `vent_setting`, declared `varchar(12)` NULL.
    pub vent_setting: Option<String>,
    /// `stow_position`, declared `varchar(8)` NULL.
    ///
    /// Bay-row-tier as loaded, e.g. 0340882
    pub stow_position: Option<String>,
    /// `loaded_at`, declared `datetime(6)` NULL.
    pub loaded_at: Option<sqlx::types::chrono::NaiveDateTime>,
}

impl Container {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> ContainerKey {
        ContainerKey {
            container_id: self.container_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for Container {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            container_id: row.try_get("container_id")?,
            equipment_no: row.try_get("equipment_no")?,
            consignment_id: row.try_get("consignment_id")?,
            iso_code: row.try_get("iso_code")?,
            seal_number: row.try_get("seal_number")?,
            tare_weight_kg: row.try_get("tare_weight_kg")?,
            cargo_weight_kg: row.try_get("cargo_weight_kg")?,
            vgm_kg: row.try_get("vgm_kg")?,
            vgm_method: row.try_get("vgm_method")?,
            setpoint_celsius: row.try_get("setpoint_celsius")?,
            humidity_pct: row.try_get("humidity_pct")?,
            vent_setting: row.try_get("vent_setting")?,
            stow_position: row.try_get("stow_position")?,
            loaded_at: row.try_get("loaded_at")?,
        })
    }
}

/// The primary key of `freight.container`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct ContainerKey {
    /// `container_id`, declared `bigint(20) unsigned`.
    pub container_id: u64,
}

/// One column of `freight.container`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: ContainerColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum ContainerColumn {
    /// `container_id`, declared `bigint(20) unsigned`.
    ContainerId,
    /// `equipment_no`, declared `char(11)`.
    EquipmentNo,
    /// `consignment_id`, declared `bigint(20) unsigned`.
    ConsignmentId,
    /// `iso_code`, declared `char(4)`.
    IsoCode,
    /// `seal_number`, declared `varchar(15)`.
    SealNumber,
    /// `tare_weight_kg`, declared `decimal(8,2)`.
    TareWeightKg,
    /// `cargo_weight_kg`, declared `decimal(10,3)`.
    CargoWeightKg,
    /// `vgm_kg`, declared `decimal(10,3)`.
    VgmKg,
    /// `vgm_method`, declared `enum('Method 1 — weighbridge','Method 2 — calculated','Not yet verified')`.
    VgmMethod,
    /// `setpoint_celsius`, declared `decimal(4,1)`.
    SetpointCelsius,
    /// `humidity_pct`, declared `tinyint(3) unsigned`.
    HumidityPct,
    /// `vent_setting`, declared `varchar(12)`.
    VentSetting,
    /// `stow_position`, declared `varchar(8)`.
    StowPosition,
    /// `loaded_at`, declared `datetime(6)`.
    LoadedAt,
}

impl ContainerColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::ContainerId => "`container_id`",
            Self::EquipmentNo => "`equipment_no`",
            Self::ConsignmentId => "`consignment_id`",
            Self::IsoCode => "`iso_code`",
            Self::SealNumber => "`seal_number`",
            Self::TareWeightKg => "`tare_weight_kg`",
            Self::CargoWeightKg => "`cargo_weight_kg`",
            Self::VgmKg => "`vgm_kg`",
            Self::VgmMethod => "`vgm_method`",
            Self::SetpointCelsius => "`setpoint_celsius`",
            Self::HumidityPct => "`humidity_pct`",
            Self::VentSetting => "`vent_setting`",
            Self::StowPosition => "`stow_position`",
            Self::LoadedAt => "`loaded_at`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::ContainerId => "container_id",
            Self::EquipmentNo => "equipment_no",
            Self::ConsignmentId => "consignment_id",
            Self::IsoCode => "iso_code",
            Self::SealNumber => "seal_number",
            Self::TareWeightKg => "tare_weight_kg",
            Self::CargoWeightKg => "cargo_weight_kg",
            Self::VgmKg => "vgm_kg",
            Self::VgmMethod => "vgm_method",
            Self::SetpointCelsius => "setpoint_celsius",
            Self::HumidityPct => "humidity_pct",
            Self::VentSetting => "vent_setting",
            Self::StowPosition => "stow_position",
            Self::LoadedAt => "loaded_at",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::ContainerId,
            Self::EquipmentNo,
            Self::ConsignmentId,
            Self::IsoCode,
            Self::SealNumber,
            Self::TareWeightKg,
            Self::CargoWeightKg,
            Self::VgmKg,
            Self::VgmMethod,
            Self::SetpointCelsius,
            Self::HumidityPct,
            Self::VentSetting,
            Self::StowPosition,
            Self::LoadedAt,
        ]
    }
}

impl std::fmt::Display for ContainerColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for ContainerColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "container_id" => Ok(Self::ContainerId),
            "equipment_no" => Ok(Self::EquipmentNo),
            "consignment_id" => Ok(Self::ConsignmentId),
            "iso_code" => Ok(Self::IsoCode),
            "seal_number" => Ok(Self::SealNumber),
            "tare_weight_kg" => Ok(Self::TareWeightKg),
            "cargo_weight_kg" => Ok(Self::CargoWeightKg),
            "vgm_kg" => Ok(Self::VgmKg),
            "vgm_method" => Ok(Self::VgmMethod),
            "setpoint_celsius" => Ok(Self::SetpointCelsius),
            "humidity_pct" => Ok(Self::HumidityPct),
            "vent_setting" => Ok(Self::VentSetting),
            "stow_position" => Ok(Self::StowPosition),
            "loaded_at" => Ok(Self::LoadedAt),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.container",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.container`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct ContainerOrder {
    /// The column to order by.
    pub column: ContainerColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.container`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct ContainerFilter {
    /// Where set, requires `container_id` to equal it.
    pub container_id: Option<u64>,
    /// Where set, requires `container_id` to be at least it.
    pub container_id_from: Option<u64>,
    /// Where set, requires `container_id` to be at most it.
    pub container_id_to: Option<u64>,
    /// Where set, requires `equipment_no` to equal it.
    pub equipment_no: Option<String>,
    /// Where set, requires `equipment_no` to match it, as `LIKE` matches.
    pub equipment_no_like: Option<String>,
    /// Where set, requires `consignment_id` to equal it.
    pub consignment_id: Option<u64>,
    /// Where set, requires `consignment_id` to be at least it.
    pub consignment_id_from: Option<u64>,
    /// Where set, requires `consignment_id` to be at most it.
    pub consignment_id_to: Option<u64>,
    /// Where set, requires `iso_code` to equal it.
    pub iso_code: Option<String>,
    /// Where set, requires `iso_code` to match it, as `LIKE` matches.
    pub iso_code_like: Option<String>,
    /// Where set, requires `seal_number` to equal it.
    pub seal_number: Option<String>,
    /// Where set, requires `seal_number` to match it, as `LIKE` matches.
    pub seal_number_like: Option<String>,
    /// Where set, requires `seal_number` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub seal_number_is_null: Option<bool>,
    /// Where set, requires `tare_weight_kg` to equal it.
    pub tare_weight_kg: Option<super::Decimal>,
    /// Where set, requires `tare_weight_kg` to be at least it.
    pub tare_weight_kg_from: Option<super::Decimal>,
    /// Where set, requires `tare_weight_kg` to be at most it.
    pub tare_weight_kg_to: Option<super::Decimal>,
    /// Where set, requires `cargo_weight_kg` to equal it.
    pub cargo_weight_kg: Option<super::Decimal>,
    /// Where set, requires `cargo_weight_kg` to be at least it.
    pub cargo_weight_kg_from: Option<super::Decimal>,
    /// Where set, requires `cargo_weight_kg` to be at most it.
    pub cargo_weight_kg_to: Option<super::Decimal>,
    /// Where set, requires `vgm_kg` to equal it.
    pub vgm_kg: Option<super::Decimal>,
    /// Where set, requires `vgm_kg` to be at least it.
    pub vgm_kg_from: Option<super::Decimal>,
    /// Where set, requires `vgm_kg` to be at most it.
    pub vgm_kg_to: Option<super::Decimal>,
    /// Where set, requires `vgm_kg` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub vgm_kg_is_null: Option<bool>,
    /// Where set, requires `vgm_method` to equal it.
    pub vgm_method: Option<String>,
    /// Where set, requires `vgm_method` to match it, as `LIKE` matches.
    pub vgm_method_like: Option<String>,
    /// Where set, requires `setpoint_celsius` to equal it.
    pub setpoint_celsius: Option<super::Decimal>,
    /// Where set, requires `setpoint_celsius` to be at least it.
    pub setpoint_celsius_from: Option<super::Decimal>,
    /// Where set, requires `setpoint_celsius` to be at most it.
    pub setpoint_celsius_to: Option<super::Decimal>,
    /// Where set, requires `setpoint_celsius` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub setpoint_celsius_is_null: Option<bool>,
    /// Where set, requires `humidity_pct` to equal it.
    pub humidity_pct: Option<u8>,
    /// Where set, requires `humidity_pct` to be at least it.
    pub humidity_pct_from: Option<u8>,
    /// Where set, requires `humidity_pct` to be at most it.
    pub humidity_pct_to: Option<u8>,
    /// Where set, requires `humidity_pct` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub humidity_pct_is_null: Option<bool>,
    /// Where set, requires `vent_setting` to equal it.
    pub vent_setting: Option<String>,
    /// Where set, requires `vent_setting` to match it, as `LIKE` matches.
    pub vent_setting_like: Option<String>,
    /// Where set, requires `vent_setting` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub vent_setting_is_null: Option<bool>,
    /// Where set, requires `stow_position` to equal it.
    pub stow_position: Option<String>,
    /// Where set, requires `stow_position` to match it, as `LIKE` matches.
    pub stow_position_like: Option<String>,
    /// Where set, requires `stow_position` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub stow_position_is_null: Option<bool>,
    /// Where set, requires `loaded_at` to equal it.
    pub loaded_at: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `loaded_at` to be at least it.
    pub loaded_at_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `loaded_at` to be at most it.
    pub loaded_at_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `loaded_at` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub loaded_at_is_null: Option<bool>,
}

impl ContainerFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.container_id.is_none()
            && self.container_id_from.is_none()
            && self.container_id_to.is_none()
            && self.equipment_no.is_none()
            && self.equipment_no_like.is_none()
            && self.consignment_id.is_none()
            && self.consignment_id_from.is_none()
            && self.consignment_id_to.is_none()
            && self.iso_code.is_none()
            && self.iso_code_like.is_none()
            && self.seal_number.is_none()
            && self.seal_number_like.is_none()
            && self.seal_number_is_null.is_none()
            && self.tare_weight_kg.is_none()
            && self.tare_weight_kg_from.is_none()
            && self.tare_weight_kg_to.is_none()
            && self.cargo_weight_kg.is_none()
            && self.cargo_weight_kg_from.is_none()
            && self.cargo_weight_kg_to.is_none()
            && self.vgm_kg.is_none()
            && self.vgm_kg_from.is_none()
            && self.vgm_kg_to.is_none()
            && self.vgm_kg_is_null.is_none()
            && self.vgm_method.is_none()
            && self.vgm_method_like.is_none()
            && self.setpoint_celsius.is_none()
            && self.setpoint_celsius_from.is_none()
            && self.setpoint_celsius_to.is_none()
            && self.setpoint_celsius_is_null.is_none()
            && self.humidity_pct.is_none()
            && self.humidity_pct_from.is_none()
            && self.humidity_pct_to.is_none()
            && self.humidity_pct_is_null.is_none()
            && self.vent_setting.is_none()
            && self.vent_setting_like.is_none()
            && self.vent_setting_is_null.is_none()
            && self.stow_position.is_none()
            && self.stow_position_like.is_none()
            && self.stow_position_is_null.is_none()
            && self.loaded_at.is_none()
            && self.loaded_at_from.is_none()
            && self.loaded_at_to.is_none()
            && self.loaded_at_is_null.is_none()
    }
}
