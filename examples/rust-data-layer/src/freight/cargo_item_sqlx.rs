// Code generated by tpl from freight.cargo_item. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.cargo_item` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::cargo_item::{CargoItem, CargoItemFilter, CargoItemKey, CargoItemOrder};
use super::cargo_item_repository::CargoItemRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.cargo_item";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `cargo_item_id`, `consignment_id`, `container_id`, `line_number`, `hs_code`, `description`, `package_kind`, `package_count`, `gross_weight_kg`, `net_weight_kg`, `volume_cbm`, `chargeable_weight_kg`, `imdg_class`, `un_number`, `origin_country`, `unit_value`, `line_value` FROM `cargo_item`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `cargo_item_id`, `consignment_id`, `container_id`, `line_number`, `hs_code`, `description`, `package_kind`, `package_count`, `gross_weight_kg`, `net_weight_kg`, `volume_cbm`, `chargeable_weight_kg`, `imdg_class`, `un_number`, `origin_country`, `unit_value`, `line_value` FROM `cargo_item` WHERE `cargo_item_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `cargo_item` (`consignment_id`, `container_id`, `line_number`, `hs_code`, `description`, `package_kind`, `package_count`, `gross_weight_kg`, `net_weight_kg`, `volume_cbm`, `imdg_class`, `un_number`, `origin_country`, `unit_value`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `cargo_item` SET `consignment_id` = ?, `container_id` = ?, `line_number` = ?, `hs_code` = ?, `description` = ?, `package_kind` = ?, `package_count` = ?, `gross_weight_kg` = ?, `net_weight_kg` = ?, `volume_cbm` = ?, `imdg_class` = ?, `un_number` = ?, `origin_country` = ?, `unit_value` = ? WHERE `cargo_item_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `cargo_item` WHERE `cargo_item_id` = ?";

/// The `sqlx` data layer of `freight.cargo_item`.
#[derive(Debug, Clone)]
pub struct CargoItemSqlx {
    pool: sqlx::MySqlPool,
}

impl CargoItemSqlx {
    /// Builds the data layer of `freight.cargo_item` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl CargoItemRepository for CargoItemSqlx {
    async fn create(&self, row: &mut CargoItem) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(row.consignment_id)
            .bind(row.container_id)
            .bind(row.line_number)
            .bind(&row.hs_code)
            .bind(&row.description)
            .bind(&row.package_kind)
            .bind(row.package_count)
            .bind(&row.gross_weight_kg)
            .bind(&row.net_weight_kg)
            .bind(&row.volume_cbm)
            .bind(&row.imdg_class)
            .bind(row.un_number)
            .bind(&row.origin_country)
            .bind(&row.unit_value)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.cargo_item_id = assigned;
        Ok(())
    }

    async fn read(&self, key: &CargoItemKey) -> Result<CargoItem, Error> {
        sqlx::query_as::<_, CargoItem>(READ)
            .bind(key.cargo_item_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &CargoItem) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(row.consignment_id)
            .bind(row.container_id)
            .bind(row.line_number)
            .bind(&row.hs_code)
            .bind(&row.description)
            .bind(&row.package_kind)
            .bind(row.package_count)
            .bind(&row.gross_weight_kg)
            .bind(&row.net_weight_kg)
            .bind(&row.volume_cbm)
            .bind(&row.imdg_class)
            .bind(row.un_number)
            .bind(&row.origin_country)
            .bind(&row.unit_value)
            .bind(row.cargo_item_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &CargoItemKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.cargo_item_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: CargoItemFilter,
        order: &[CargoItemOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<CargoItem>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.cargo_item_id {
                conditions
                    .push("`cargo_item_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.cargo_item_id_from {
                conditions
                    .push("`cargo_item_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.cargo_item_id_to {
                conditions
                    .push("`cargo_item_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.consignment_id {
                conditions
                    .push("`consignment_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.consignment_id_from {
                conditions
                    .push("`consignment_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.consignment_id_to {
                conditions
                    .push("`consignment_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.container_id {
                conditions
                    .push("`container_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.container_id_from {
                conditions
                    .push("`container_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.container_id_to {
                conditions
                    .push("`container_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.container_id_is_null {
                conditions.push(if is_null {
                    "`container_id` IS NULL"
                } else {
                    "`container_id` IS NOT NULL"
                });
            }
            if let Some(value) = filter.line_number {
                conditions
                    .push("`line_number` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.line_number_from {
                conditions
                    .push("`line_number` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.line_number_to {
                conditions
                    .push("`line_number` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.hs_code {
                conditions
                    .push("`hs_code` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.hs_code_like {
                conditions
                    .push("`hs_code` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.description {
                conditions
                    .push("`description` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.description_like {
                conditions
                    .push("`description` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.package_kind {
                conditions
                    .push("`package_kind` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.package_kind_like {
                conditions
                    .push("`package_kind` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.package_count {
                conditions
                    .push("`package_count` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.package_count_from {
                conditions
                    .push("`package_count` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.package_count_to {
                conditions
                    .push("`package_count` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gross_weight_kg {
                conditions
                    .push("`gross_weight_kg` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gross_weight_kg_from {
                conditions
                    .push("`gross_weight_kg` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gross_weight_kg_to {
                conditions
                    .push("`gross_weight_kg` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.net_weight_kg {
                conditions
                    .push("`net_weight_kg` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.net_weight_kg_from {
                conditions
                    .push("`net_weight_kg` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.net_weight_kg_to {
                conditions
                    .push("`net_weight_kg` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.volume_cbm {
                conditions
                    .push("`volume_cbm` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.volume_cbm_from {
                conditions
                    .push("`volume_cbm` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.volume_cbm_to {
                conditions
                    .push("`volume_cbm` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.chargeable_weight_kg {
                conditions
                    .push("`chargeable_weight_kg` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.chargeable_weight_kg_from {
                conditions
                    .push("`chargeable_weight_kg` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.chargeable_weight_kg_to {
                conditions
                    .push("`chargeable_weight_kg` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.chargeable_weight_kg_is_null {
                conditions.push(if is_null {
                    "`chargeable_weight_kg` IS NULL"
                } else {
                    "`chargeable_weight_kg` IS NOT NULL"
                });
            }
            if let Some(value) = filter.imdg_class {
                conditions
                    .push("`imdg_class` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.imdg_class_like {
                conditions
                    .push("`imdg_class` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.un_number {
                conditions
                    .push("`un_number` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.un_number_from {
                conditions
                    .push("`un_number` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.un_number_to {
                conditions
                    .push("`un_number` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.un_number_is_null {
                conditions.push(if is_null {
                    "`un_number` IS NULL"
                } else {
                    "`un_number` IS NOT NULL"
                });
            }
            if let Some(value) = filter.origin_country {
                conditions
                    .push("`origin_country` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.origin_country_like {
                conditions
                    .push("`origin_country` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.unit_value {
                conditions
                    .push("`unit_value` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.unit_value_from {
                conditions
                    .push("`unit_value` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.unit_value_to {
                conditions
                    .push("`unit_value` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.line_value {
                conditions
                    .push("`line_value` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.line_value_from {
                conditions
                    .push("`line_value` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.line_value_to {
                conditions
                    .push("`line_value` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.line_value_is_null {
                conditions.push(if is_null {
                    "`line_value` IS NULL"
                } else {
                    "`line_value` IS NOT NULL"
                });
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<CargoItem>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
