// Code generated by tpl from freight.legacy_edi_field. DO NOT EDIT.

//! The `freight.legacy_edi_field` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.legacy_edi_field`.
///
/// CASOS-LIMITE DE IDENTIFICADORES — mapeamento EDIFACT herdado; os nomes das colunas são deliberadamente hostis ao parser
#[derive(Debug, Clone)]
pub struct LegacyEdiField {
    /// `mapping_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub mapping_id: u16,
    /// `segment_tag`, declared `char(3)` NOT NULL.
    ///
    /// It is part of a unique index.
    ///
    /// EDIFACT segment tag, e.g. BGM, NAD, CNI
    pub segment_tag: String,
    /// `back`tick`, declared `varchar(48)` NULL.
    ///
    /// Identifier containing an embedded backtick, doubled when quoted
    pub back_tick: Option<String>,
    /// `space in name`, declared `varchar(48)` NULL.
    ///
    /// Identifier containing a space
    pub space_in_name: Option<String>,
    /// `select`, declared `varchar(48)` NULL.
    ///
    /// Identifier that is a reserved word
    pub select: Option<String>,
    /// `posição`, declared `varchar(48)` NULL.
    ///
    /// Identifier containing non-ASCII letters
    pub posição: Option<String>,
    /// `Mixed Case Column`, declared `varchar(48)` NULL.
    ///
    /// Identifier mixing case and spaces
    pub mixed_case_column: Option<String>,
    /// `retired_on`, declared `date` NULL.
    pub retired_on: Option<sqlx::types::chrono::NaiveDate>,
}

impl LegacyEdiField {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> LegacyEdiFieldKey {
        LegacyEdiFieldKey {
            mapping_id: self.mapping_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for LegacyEdiField {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            mapping_id: row.try_get("mapping_id")?,
            segment_tag: row.try_get("segment_tag")?,
            back_tick: row.try_get("back`tick")?,
            space_in_name: row.try_get("space in name")?,
            select: row.try_get("select")?,
            posição: row.try_get("posição")?,
            mixed_case_column: row.try_get("Mixed Case Column")?,
            retired_on: row.try_get("retired_on")?,
        })
    }
}

/// The primary key of `freight.legacy_edi_field`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct LegacyEdiFieldKey {
    /// `mapping_id`, declared `smallint(5) unsigned`.
    pub mapping_id: u16,
}

/// One column of `freight.legacy_edi_field`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: LegacyEdiFieldColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum LegacyEdiFieldColumn {
    /// `mapping_id`, declared `smallint(5) unsigned`.
    MappingId,
    /// `segment_tag`, declared `char(3)`.
    SegmentTag,
    /// `back`tick`, declared `varchar(48)`.
    BackTick,
    /// `space in name`, declared `varchar(48)`.
    SpaceInName,
    /// `select`, declared `varchar(48)`.
    Select,
    /// `posição`, declared `varchar(48)`.
    Posição,
    /// `Mixed Case Column`, declared `varchar(48)`.
    MixedCaseColumn,
    /// `retired_on`, declared `date`.
    RetiredOn,
}

impl LegacyEdiFieldColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::MappingId => "`mapping_id`",
            Self::SegmentTag => "`segment_tag`",
            Self::BackTick => "`back``tick`",
            Self::SpaceInName => "`space in name`",
            Self::Select => "`select`",
            Self::Posição => "`posição`",
            Self::MixedCaseColumn => "`Mixed Case Column`",
            Self::RetiredOn => "`retired_on`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::MappingId => "mapping_id",
            Self::SegmentTag => "segment_tag",
            Self::BackTick => "back`tick",
            Self::SpaceInName => "space in name",
            Self::Select => "select",
            Self::Posição => "posição",
            Self::MixedCaseColumn => "Mixed Case Column",
            Self::RetiredOn => "retired_on",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::MappingId,
            Self::SegmentTag,
            Self::BackTick,
            Self::SpaceInName,
            Self::Select,
            Self::Posição,
            Self::MixedCaseColumn,
            Self::RetiredOn,
        ]
    }
}

impl std::fmt::Display for LegacyEdiFieldColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for LegacyEdiFieldColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "mapping_id" => Ok(Self::MappingId),
            "segment_tag" => Ok(Self::SegmentTag),
            "back`tick" => Ok(Self::BackTick),
            "space in name" => Ok(Self::SpaceInName),
            "select" => Ok(Self::Select),
            "posição" => Ok(Self::Posição),
            "Mixed Case Column" => Ok(Self::MixedCaseColumn),
            "retired_on" => Ok(Self::RetiredOn),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.legacy_edi_field",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.legacy_edi_field`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct LegacyEdiFieldOrder {
    /// The column to order by.
    pub column: LegacyEdiFieldColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.legacy_edi_field`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct LegacyEdiFieldFilter {
    /// Where set, requires `mapping_id` to equal it.
    pub mapping_id: Option<u16>,
    /// Where set, requires `mapping_id` to be at least it.
    pub mapping_id_from: Option<u16>,
    /// Where set, requires `mapping_id` to be at most it.
    pub mapping_id_to: Option<u16>,
    /// Where set, requires `segment_tag` to equal it.
    pub segment_tag: Option<String>,
    /// Where set, requires `segment_tag` to match it, as `LIKE` matches.
    pub segment_tag_like: Option<String>,
    /// Where set, requires `back`tick` to equal it.
    pub back_tick: Option<String>,
    /// Where set, requires `back`tick` to match it, as `LIKE` matches.
    pub back_tick_like: Option<String>,
    /// Where set, requires `back`tick` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub back_tick_is_null: Option<bool>,
    /// Where set, requires `space in name` to equal it.
    pub space_in_name: Option<String>,
    /// Where set, requires `space in name` to match it, as `LIKE` matches.
    pub space_in_name_like: Option<String>,
    /// Where set, requires `space in name` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub space_in_name_is_null: Option<bool>,
    /// Where set, requires `select` to equal it.
    pub select: Option<String>,
    /// Where set, requires `select` to match it, as `LIKE` matches.
    pub select_like: Option<String>,
    /// Where set, requires `select` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub select_is_null: Option<bool>,
    /// Where set, requires `posição` to equal it.
    pub posição: Option<String>,
    /// Where set, requires `posição` to match it, as `LIKE` matches.
    pub posição_like: Option<String>,
    /// Where set, requires `posição` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub posição_is_null: Option<bool>,
    /// Where set, requires `Mixed Case Column` to equal it.
    pub mixed_case_column: Option<String>,
    /// Where set, requires `Mixed Case Column` to match it, as `LIKE` matches.
    pub mixed_case_column_like: Option<String>,
    /// Where set, requires `Mixed Case Column` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub mixed_case_column_is_null: Option<bool>,
    /// Where set, requires `retired_on` to equal it.
    pub retired_on: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `retired_on` to be at least it.
    pub retired_on_from: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `retired_on` to be at most it.
    pub retired_on_to: Option<sqlx::types::chrono::NaiveDate>,
    /// Where set, requires `retired_on` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub retired_on_is_null: Option<bool>,
}

impl LegacyEdiFieldFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.mapping_id.is_none()
            && self.mapping_id_from.is_none()
            && self.mapping_id_to.is_none()
            && self.segment_tag.is_none()
            && self.segment_tag_like.is_none()
            && self.back_tick.is_none()
            && self.back_tick_like.is_none()
            && self.back_tick_is_null.is_none()
            && self.space_in_name.is_none()
            && self.space_in_name_like.is_none()
            && self.space_in_name_is_null.is_none()
            && self.select.is_none()
            && self.select_like.is_none()
            && self.select_is_null.is_none()
            && self.posição.is_none()
            && self.posição_like.is_none()
            && self.posição_is_null.is_none()
            && self.mixed_case_column.is_none()
            && self.mixed_case_column_like.is_none()
            && self.mixed_case_column_is_null.is_none()
            && self.retired_on.is_none()
            && self.retired_on_from.is_none()
            && self.retired_on_to.is_none()
            && self.retired_on_is_null.is_none()
    }
}
