// Code generated by tpl from freight.port_facility. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.port_facility` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::port_facility::{PortFacility, PortFacilityFilter, PortFacilityKey, PortFacilityOrder};
use super::port_facility_repository::PortFacilityRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.port_facility";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `facility_id`, `port_id`, `designation`, `facility_kind`, `quay_length_m`, `crane_count`, `gate_opens_at`, `gate_closes_at`, `berth_outline`, `approach_channel`, `anchorage_points`, `fairway_network`, `restricted_areas`, `reference_marker`, `survey_footprint`, `notices_to_mariners` FROM `port_facility`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `facility_id`, `port_id`, `designation`, `facility_kind`, `quay_length_m`, `crane_count`, `gate_opens_at`, `gate_closes_at`, `berth_outline`, `approach_channel`, `anchorage_points`, `fairway_network`, `restricted_areas`, `reference_marker`, `survey_footprint`, `notices_to_mariners` FROM `port_facility` WHERE `facility_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `port_facility` (`port_id`, `designation`, `facility_kind`, `quay_length_m`, `crane_count`, `gate_opens_at`, `gate_closes_at`, `berth_outline`, `approach_channel`, `anchorage_points`, `fairway_network`, `restricted_areas`, `reference_marker`, `survey_footprint`, `notices_to_mariners`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `port_facility` SET `port_id` = ?, `designation` = ?, `facility_kind` = ?, `quay_length_m` = ?, `crane_count` = ?, `gate_opens_at` = ?, `gate_closes_at` = ?, `berth_outline` = ?, `approach_channel` = ?, `anchorage_points` = ?, `fairway_network` = ?, `restricted_areas` = ?, `reference_marker` = ?, `survey_footprint` = ?, `notices_to_mariners` = ? WHERE `facility_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `port_facility` WHERE `facility_id` = ?";

/// The `sqlx` data layer of `freight.port_facility`.
#[derive(Debug, Clone)]
pub struct PortFacilitySqlx {
    pool: sqlx::MySqlPool,
}

impl PortFacilitySqlx {
    /// Builds the data layer of `freight.port_facility` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl PortFacilityRepository for PortFacilitySqlx {
    async fn create(&self, row: &mut PortFacility) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(row.port_id)
            .bind(&row.designation)
            .bind(&row.facility_kind)
            .bind(&row.quay_length_m)
            .bind(row.crane_count)
            .bind(row.gate_opens_at)
            .bind(row.gate_closes_at)
            .bind(&row.berth_outline)
            .bind(&row.approach_channel)
            .bind(&row.anchorage_points)
            .bind(&row.fairway_network)
            .bind(&row.restricted_areas)
            .bind(&row.reference_marker)
            .bind(&row.survey_footprint)
            .bind(&row.notices_to_mariners)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.facility_id =
            u32::try_from(assigned).map_err(|source| Error::AssignedKey {
                table: TABLE,
                column: "facility_id",
                source,
            })?;
        Ok(())
    }

    async fn read(&self, key: &PortFacilityKey) -> Result<PortFacility, Error> {
        sqlx::query_as::<_, PortFacility>(READ)
            .bind(key.facility_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &PortFacility) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(row.port_id)
            .bind(&row.designation)
            .bind(&row.facility_kind)
            .bind(&row.quay_length_m)
            .bind(row.crane_count)
            .bind(row.gate_opens_at)
            .bind(row.gate_closes_at)
            .bind(&row.berth_outline)
            .bind(&row.approach_channel)
            .bind(&row.anchorage_points)
            .bind(&row.fairway_network)
            .bind(&row.restricted_areas)
            .bind(&row.reference_marker)
            .bind(&row.survey_footprint)
            .bind(&row.notices_to_mariners)
            .bind(row.facility_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &PortFacilityKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.facility_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: PortFacilityFilter,
        order: &[PortFacilityOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<PortFacility>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.facility_id {
                conditions
                    .push("`facility_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.facility_id_from {
                conditions
                    .push("`facility_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.facility_id_to {
                conditions
                    .push("`facility_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.port_id {
                conditions
                    .push("`port_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.port_id_from {
                conditions
                    .push("`port_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.port_id_to {
                conditions
                    .push("`port_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.designation {
                conditions
                    .push("`designation` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.designation_like {
                conditions
                    .push("`designation` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.facility_kind {
                conditions
                    .push("`facility_kind` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.facility_kind_like {
                conditions
                    .push("`facility_kind` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.quay_length_m {
                conditions
                    .push("`quay_length_m` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.quay_length_m_from {
                conditions
                    .push("`quay_length_m` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.quay_length_m_to {
                conditions
                    .push("`quay_length_m` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.crane_count {
                conditions
                    .push("`crane_count` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.crane_count_from {
                conditions
                    .push("`crane_count` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.crane_count_to {
                conditions
                    .push("`crane_count` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gate_opens_at {
                conditions
                    .push("`gate_opens_at` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gate_opens_at_from {
                conditions
                    .push("`gate_opens_at` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gate_opens_at_to {
                conditions
                    .push("`gate_opens_at` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gate_closes_at {
                conditions
                    .push("`gate_closes_at` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gate_closes_at_from {
                conditions
                    .push("`gate_closes_at` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gate_closes_at_to {
                conditions
                    .push("`gate_closes_at` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.berth_outline {
                conditions
                    .push("`berth_outline` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.approach_channel {
                conditions
                    .push("`approach_channel` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.approach_channel_is_null {
                conditions.push(if is_null {
                    "`approach_channel` IS NULL"
                } else {
                    "`approach_channel` IS NOT NULL"
                });
            }
            if let Some(value) = filter.anchorage_points {
                conditions
                    .push("`anchorage_points` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.anchorage_points_is_null {
                conditions.push(if is_null {
                    "`anchorage_points` IS NULL"
                } else {
                    "`anchorage_points` IS NOT NULL"
                });
            }
            if let Some(value) = filter.fairway_network {
                conditions
                    .push("`fairway_network` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.fairway_network_is_null {
                conditions.push(if is_null {
                    "`fairway_network` IS NULL"
                } else {
                    "`fairway_network` IS NOT NULL"
                });
            }
            if let Some(value) = filter.restricted_areas {
                conditions
                    .push("`restricted_areas` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.restricted_areas_is_null {
                conditions.push(if is_null {
                    "`restricted_areas` IS NULL"
                } else {
                    "`restricted_areas` IS NOT NULL"
                });
            }
            if let Some(value) = filter.reference_marker {
                conditions
                    .push("`reference_marker` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.reference_marker_is_null {
                conditions.push(if is_null {
                    "`reference_marker` IS NULL"
                } else {
                    "`reference_marker` IS NOT NULL"
                });
            }
            if let Some(value) = filter.survey_footprint {
                conditions
                    .push("`survey_footprint` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.survey_footprint_is_null {
                conditions.push(if is_null {
                    "`survey_footprint` IS NULL"
                } else {
                    "`survey_footprint` IS NOT NULL"
                });
            }
            if let Some(value) = filter.notices_to_mariners {
                conditions
                    .push("`notices_to_mariners` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.notices_to_mariners_is_null {
                conditions.push(if is_null {
                    "`notices_to_mariners` IS NULL"
                } else {
                    "`notices_to_mariners` IS NOT NULL"
                });
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<PortFacility>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
