// Code generated by tpl from freight.customs_declaration. DO NOT EDIT.

//! The `freight.customs_declaration` row, the key that addresses it, the columns an
//! ordering may name, and the filter a search takes.

/// One row of `freight.customs_declaration`.
///
/// Declarações aduaneiras associadas às expedições
#[derive(Debug, Clone)]
pub struct CustomsDeclaration {
    /// `declaration_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    ///
    /// It is part of the primary key.
    pub declaration_id: u64,
    /// `consignment_id`, declared `bigint(20) unsigned` NOT NULL.
    pub consignment_id: u64,
    /// `clearance_port_id`, declared `smallint(5) unsigned` NULL.
    ///
    /// NULL once the port record is retired; the declaration itself must survive
    pub clearance_port_id: Option<u16>,
    /// `mrn`, declared `varchar(21)` NOT NULL.
    ///
    /// It is part of a unique index.
    ///
    /// Movement Reference Number issued by the customs office
    pub mrn: String,
    /// `procedure_code`, declared `char(4)` NOT NULL.
    pub procedure_code: String,
    /// `regime`, declared `enum('Export','Import','Transit T1','Transit T2','Temporary admission','Re-export')` NOT NULL.
    pub regime: String,
    /// `status`, declared `enum('Draft','Lodged','Under control','Released','Rejected')` NOT NULL.
    pub status: String,
    /// `lodged_at`, declared `datetime(6)` NULL.
    pub lodged_at: Option<sqlx::types::chrono::NaiveDateTime>,
    /// `released_at`, declared `datetime(6)` NULL.
    pub released_at: Option<sqlx::types::chrono::NaiveDateTime>,
    /// `duty_amount`, declared `decimal(12,2)` NOT NULL.
    pub duty_amount: super::Decimal,
    /// `vat_amount`, declared `decimal(12,2)` NOT NULL.
    pub vat_amount: super::Decimal,
    /// `total_payable`, declared `decimal(12,2)` NULL STORED GENERATED AS (`duty_amount` + `vat_amount`).
    pub total_payable: Option<super::Decimal>,
    /// `officer_remarks`, declared `varchar(500)` NULL.
    pub officer_remarks: Option<String>,
}

impl CustomsDeclaration {
    /// The primary key of the row.
    #[must_use]
    pub fn key(&self) -> CustomsDeclarationKey {
        CustomsDeclarationKey {
            declaration_id: self.declaration_id,
        }
    }
}

impl<'r> sqlx::FromRow<'r, sqlx::mysql::MySqlRow> for CustomsDeclaration {
    fn from_row(row: &'r sqlx::mysql::MySqlRow) -> Result<Self, sqlx::Error> {
        use sqlx::Row as _;

        Ok(Self {
            declaration_id: row.try_get("declaration_id")?,
            consignment_id: row.try_get("consignment_id")?,
            clearance_port_id: row.try_get("clearance_port_id")?,
            mrn: row.try_get("mrn")?,
            procedure_code: row.try_get("procedure_code")?,
            regime: row.try_get("regime")?,
            status: row.try_get("status")?,
            lodged_at: row.try_get("lodged_at")?,
            released_at: row.try_get("released_at")?,
            duty_amount: row.try_get("duty_amount")?,
            vat_amount: row.try_get("vat_amount")?,
            total_payable: row.try_get("total_payable")?,
            officer_remarks: row.try_get("officer_remarks")?,
        })
    }
}

/// The primary key of `freight.customs_declaration`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct CustomsDeclarationKey {
    /// `declaration_id`, declared `bigint(20) unsigned`.
    pub declaration_id: u64,
}

/// One column of `freight.customs_declaration`.
///
/// A search accepts an ordering term over these variants and over nothing
/// else, so an ordering can carry no SQL of its own: [`as_sql`] answers from a
/// closed `match` with a quoted identifier the template wrote, and
/// [`FromStr`] is the one door a name a caller supplied can come through.
///
/// [`as_sql`]: CustomsDeclarationColumn::as_sql
/// [`FromStr`]: std::str::FromStr
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum CustomsDeclarationColumn {
    /// `declaration_id`, declared `bigint(20) unsigned`.
    DeclarationId,
    /// `consignment_id`, declared `bigint(20) unsigned`.
    ConsignmentId,
    /// `clearance_port_id`, declared `smallint(5) unsigned`.
    ClearancePortId,
    /// `mrn`, declared `varchar(21)`.
    Mrn,
    /// `procedure_code`, declared `char(4)`.
    ProcedureCode,
    /// `regime`, declared `enum('Export','Import','Transit T1','Transit T2','Temporary admission','Re-export')`.
    Regime,
    /// `status`, declared `enum('Draft','Lodged','Under control','Released','Rejected')`.
    Status,
    /// `lodged_at`, declared `datetime(6)`.
    LodgedAt,
    /// `released_at`, declared `datetime(6)`.
    ReleasedAt,
    /// `duty_amount`, declared `decimal(12,2)`.
    DutyAmount,
    /// `vat_amount`, declared `decimal(12,2)`.
    VatAmount,
    /// `total_payable`, declared `decimal(12,2)`.
    TotalPayable,
    /// `officer_remarks`, declared `varchar(500)`.
    OfficerRemarks,
}

impl CustomsDeclarationColumn {
    /// The column as a statement's text names it: quoted for MariaDB.
    #[must_use]
    pub const fn as_sql(self) -> &'static str {
        match self {
            Self::DeclarationId => "`declaration_id`",
            Self::ConsignmentId => "`consignment_id`",
            Self::ClearancePortId => "`clearance_port_id`",
            Self::Mrn => "`mrn`",
            Self::ProcedureCode => "`procedure_code`",
            Self::Regime => "`regime`",
            Self::Status => "`status`",
            Self::LodgedAt => "`lodged_at`",
            Self::ReleasedAt => "`released_at`",
            Self::DutyAmount => "`duty_amount`",
            Self::VatAmount => "`vat_amount`",
            Self::TotalPayable => "`total_payable`",
            Self::OfficerRemarks => "`officer_remarks`",
        }
    }

    /// The column as the catalogue names it, unquoted.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::DeclarationId => "declaration_id",
            Self::ConsignmentId => "consignment_id",
            Self::ClearancePortId => "clearance_port_id",
            Self::Mrn => "mrn",
            Self::ProcedureCode => "procedure_code",
            Self::Regime => "regime",
            Self::Status => "status",
            Self::LodgedAt => "lodged_at",
            Self::ReleasedAt => "released_at",
            Self::DutyAmount => "duty_amount",
            Self::VatAmount => "vat_amount",
            Self::TotalPayable => "total_payable",
            Self::OfficerRemarks => "officer_remarks",
        }
    }

    /// Every column of the table, in catalogue order.
    #[must_use]
    pub const fn all() -> &'static [Self] {
        &[
            Self::DeclarationId,
            Self::ConsignmentId,
            Self::ClearancePortId,
            Self::Mrn,
            Self::ProcedureCode,
            Self::Regime,
            Self::Status,
            Self::LodgedAt,
            Self::ReleasedAt,
            Self::DutyAmount,
            Self::VatAmount,
            Self::TotalPayable,
            Self::OfficerRemarks,
        ]
    }
}

impl std::fmt::Display for CustomsDeclarationColumn {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        formatter.write_str(self.name())
    }
}

impl std::str::FromStr for CustomsDeclarationColumn {
    type Err = super::Error;

    /// Reads a column a caller supplied as text.
    ///
    /// # Errors
    ///
    /// Returns [`super::Error::UnknownColumn`] for every name the table does
    /// not carry. That refusal is what keeps a caller's text out of a
    /// statement.
    fn from_str(name: &str) -> Result<Self, Self::Err> {
        match name {
            "declaration_id" => Ok(Self::DeclarationId),
            "consignment_id" => Ok(Self::ConsignmentId),
            "clearance_port_id" => Ok(Self::ClearancePortId),
            "mrn" => Ok(Self::Mrn),
            "procedure_code" => Ok(Self::ProcedureCode),
            "regime" => Ok(Self::Regime),
            "status" => Ok(Self::Status),
            "lodged_at" => Ok(Self::LodgedAt),
            "released_at" => Ok(Self::ReleasedAt),
            "duty_amount" => Ok(Self::DutyAmount),
            "vat_amount" => Ok(Self::VatAmount),
            "total_payable" => Ok(Self::TotalPayable),
            "officer_remarks" => Ok(Self::OfficerRemarks),
            _ => Err(super::Error::UnknownColumn {
                table: "freight.customs_declaration",
                name: name.to_owned(),
            }),
        }
    }
}

/// One ordering term of a search over `freight.customs_declaration`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct CustomsDeclarationOrder {
    /// The column to order by.
    pub column: CustomsDeclarationColumn,
    /// The direction to order in.
    pub direction: super::Direction,
}

/// What narrows a search over `freight.customs_declaration`.
///
/// Every field is optional: a field left `None` adds no condition, and the
/// conditions a search does build are joined with `AND`. [`Default`] is the
/// filter that matches every row.
#[derive(Debug, Clone, Default)]
pub struct CustomsDeclarationFilter {
    /// Where set, requires `declaration_id` to equal it.
    pub declaration_id: Option<u64>,
    /// Where set, requires `declaration_id` to be at least it.
    pub declaration_id_from: Option<u64>,
    /// Where set, requires `declaration_id` to be at most it.
    pub declaration_id_to: Option<u64>,
    /// Where set, requires `consignment_id` to equal it.
    pub consignment_id: Option<u64>,
    /// Where set, requires `consignment_id` to be at least it.
    pub consignment_id_from: Option<u64>,
    /// Where set, requires `consignment_id` to be at most it.
    pub consignment_id_to: Option<u64>,
    /// Where set, requires `clearance_port_id` to equal it.
    pub clearance_port_id: Option<u16>,
    /// Where set, requires `clearance_port_id` to be at least it.
    pub clearance_port_id_from: Option<u16>,
    /// Where set, requires `clearance_port_id` to be at most it.
    pub clearance_port_id_to: Option<u16>,
    /// Where set, requires `clearance_port_id` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub clearance_port_id_is_null: Option<bool>,
    /// Where set, requires `mrn` to equal it.
    pub mrn: Option<String>,
    /// Where set, requires `mrn` to match it, as `LIKE` matches.
    pub mrn_like: Option<String>,
    /// Where set, requires `procedure_code` to equal it.
    pub procedure_code: Option<String>,
    /// Where set, requires `procedure_code` to match it, as `LIKE` matches.
    pub procedure_code_like: Option<String>,
    /// Where set, requires `regime` to equal it.
    pub regime: Option<String>,
    /// Where set, requires `regime` to match it, as `LIKE` matches.
    pub regime_like: Option<String>,
    /// Where set, requires `status` to equal it.
    pub status: Option<String>,
    /// Where set, requires `status` to match it, as `LIKE` matches.
    pub status_like: Option<String>,
    /// Where set, requires `lodged_at` to equal it.
    pub lodged_at: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `lodged_at` to be at least it.
    pub lodged_at_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `lodged_at` to be at most it.
    pub lodged_at_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `lodged_at` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub lodged_at_is_null: Option<bool>,
    /// Where set, requires `released_at` to equal it.
    pub released_at: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `released_at` to be at least it.
    pub released_at_from: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `released_at` to be at most it.
    pub released_at_to: Option<sqlx::types::chrono::NaiveDateTime>,
    /// Where set, requires `released_at` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub released_at_is_null: Option<bool>,
    /// Where set, requires `duty_amount` to equal it.
    pub duty_amount: Option<super::Decimal>,
    /// Where set, requires `duty_amount` to be at least it.
    pub duty_amount_from: Option<super::Decimal>,
    /// Where set, requires `duty_amount` to be at most it.
    pub duty_amount_to: Option<super::Decimal>,
    /// Where set, requires `vat_amount` to equal it.
    pub vat_amount: Option<super::Decimal>,
    /// Where set, requires `vat_amount` to be at least it.
    pub vat_amount_from: Option<super::Decimal>,
    /// Where set, requires `vat_amount` to be at most it.
    pub vat_amount_to: Option<super::Decimal>,
    /// Where set, requires `total_payable` to equal it.
    pub total_payable: Option<super::Decimal>,
    /// Where set, requires `total_payable` to be at least it.
    pub total_payable_from: Option<super::Decimal>,
    /// Where set, requires `total_payable` to be at most it.
    pub total_payable_to: Option<super::Decimal>,
    /// Where set, requires `total_payable` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub total_payable_is_null: Option<bool>,
    /// Where set, requires `officer_remarks` to equal it.
    pub officer_remarks: Option<String>,
    /// Where set, requires `officer_remarks` to match it, as `LIKE` matches.
    pub officer_remarks_like: Option<String>,
    /// Where set, requires `officer_remarks` to be `NULL` when `true` and not to be `NULL` when `false`.
    pub officer_remarks_is_null: Option<bool>,
}

impl CustomsDeclarationFilter {
    /// Is no field set?
    ///
    /// A search over an empty filter builds no `WHERE` clause and matches
    /// every row of the table.
    #[must_use]
    pub fn is_empty(&self) -> bool {
        self.declaration_id.is_none()
            && self.declaration_id_from.is_none()
            && self.declaration_id_to.is_none()
            && self.consignment_id.is_none()
            && self.consignment_id_from.is_none()
            && self.consignment_id_to.is_none()
            && self.clearance_port_id.is_none()
            && self.clearance_port_id_from.is_none()
            && self.clearance_port_id_to.is_none()
            && self.clearance_port_id_is_null.is_none()
            && self.mrn.is_none()
            && self.mrn_like.is_none()
            && self.procedure_code.is_none()
            && self.procedure_code_like.is_none()
            && self.regime.is_none()
            && self.regime_like.is_none()
            && self.status.is_none()
            && self.status_like.is_none()
            && self.lodged_at.is_none()
            && self.lodged_at_from.is_none()
            && self.lodged_at_to.is_none()
            && self.lodged_at_is_null.is_none()
            && self.released_at.is_none()
            && self.released_at_from.is_none()
            && self.released_at_to.is_none()
            && self.released_at_is_null.is_none()
            && self.duty_amount.is_none()
            && self.duty_amount_from.is_none()
            && self.duty_amount_to.is_none()
            && self.vat_amount.is_none()
            && self.vat_amount_from.is_none()
            && self.vat_amount_to.is_none()
            && self.total_payable.is_none()
            && self.total_payable_from.is_none()
            && self.total_payable_to.is_none()
            && self.total_payable_is_null.is_none()
            && self.officer_remarks.is_none()
            && self.officer_remarks_like.is_none()
            && self.officer_remarks_is_null.is_none()
    }
}
