// Code generated by tpl from freight.container. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.container` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::container::{Container, ContainerFilter, ContainerKey, ContainerOrder};
use super::container_repository::ContainerRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.container";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `container_id`, `equipment_no`, `consignment_id`, `iso_code`, `seal_number`, `tare_weight_kg`, `cargo_weight_kg`, `vgm_kg`, `vgm_method`, `setpoint_celsius`, `humidity_pct`, `vent_setting`, `stow_position`, `loaded_at` FROM `container`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `container_id`, `equipment_no`, `consignment_id`, `iso_code`, `seal_number`, `tare_weight_kg`, `cargo_weight_kg`, `vgm_kg`, `vgm_method`, `setpoint_celsius`, `humidity_pct`, `vent_setting`, `stow_position`, `loaded_at` FROM `container` WHERE `container_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `container` (`equipment_no`, `consignment_id`, `iso_code`, `seal_number`, `tare_weight_kg`, `cargo_weight_kg`, `vgm_method`, `setpoint_celsius`, `humidity_pct`, `vent_setting`, `stow_position`, `loaded_at`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `container` SET `equipment_no` = ?, `consignment_id` = ?, `iso_code` = ?, `seal_number` = ?, `tare_weight_kg` = ?, `cargo_weight_kg` = ?, `vgm_method` = ?, `setpoint_celsius` = ?, `humidity_pct` = ?, `vent_setting` = ?, `stow_position` = ?, `loaded_at` = ? WHERE `container_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `container` WHERE `container_id` = ?";

/// The `sqlx` data layer of `freight.container`.
#[derive(Debug, Clone)]
pub struct ContainerSqlx {
    pool: sqlx::MySqlPool,
}

impl ContainerSqlx {
    /// Builds the data layer of `freight.container` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl ContainerRepository for ContainerSqlx {
    async fn create(&self, row: &mut Container) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(&row.equipment_no)
            .bind(row.consignment_id)
            .bind(&row.iso_code)
            .bind(&row.seal_number)
            .bind(&row.tare_weight_kg)
            .bind(&row.cargo_weight_kg)
            .bind(&row.vgm_method)
            .bind(&row.setpoint_celsius)
            .bind(row.humidity_pct)
            .bind(&row.vent_setting)
            .bind(&row.stow_position)
            .bind(row.loaded_at)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.container_id = assigned;
        Ok(())
    }

    async fn read(&self, key: &ContainerKey) -> Result<Container, Error> {
        sqlx::query_as::<_, Container>(READ)
            .bind(key.container_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &Container) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.equipment_no)
            .bind(row.consignment_id)
            .bind(&row.iso_code)
            .bind(&row.seal_number)
            .bind(&row.tare_weight_kg)
            .bind(&row.cargo_weight_kg)
            .bind(&row.vgm_method)
            .bind(&row.setpoint_celsius)
            .bind(row.humidity_pct)
            .bind(&row.vent_setting)
            .bind(&row.stow_position)
            .bind(row.loaded_at)
            .bind(row.container_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &ContainerKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.container_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: ContainerFilter,
        order: &[ContainerOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<Container>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.container_id {
                conditions
                    .push("`container_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.container_id_from {
                conditions
                    .push("`container_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.container_id_to {
                conditions
                    .push("`container_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.equipment_no {
                conditions
                    .push("`equipment_no` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.equipment_no_like {
                conditions
                    .push("`equipment_no` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.consignment_id {
                conditions
                    .push("`consignment_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.consignment_id_from {
                conditions
                    .push("`consignment_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.consignment_id_to {
                conditions
                    .push("`consignment_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.iso_code {
                conditions
                    .push("`iso_code` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.iso_code_like {
                conditions
                    .push("`iso_code` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.seal_number {
                conditions
                    .push("`seal_number` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.seal_number_like {
                conditions
                    .push("`seal_number` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.seal_number_is_null {
                conditions.push(if is_null {
                    "`seal_number` IS NULL"
                } else {
                    "`seal_number` IS NOT NULL"
                });
            }
            if let Some(value) = filter.tare_weight_kg {
                conditions
                    .push("`tare_weight_kg` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.tare_weight_kg_from {
                conditions
                    .push("`tare_weight_kg` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.tare_weight_kg_to {
                conditions
                    .push("`tare_weight_kg` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.cargo_weight_kg {
                conditions
                    .push("`cargo_weight_kg` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.cargo_weight_kg_from {
                conditions
                    .push("`cargo_weight_kg` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.cargo_weight_kg_to {
                conditions
                    .push("`cargo_weight_kg` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.vgm_kg {
                conditions
                    .push("`vgm_kg` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.vgm_kg_from {
                conditions
                    .push("`vgm_kg` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.vgm_kg_to {
                conditions
                    .push("`vgm_kg` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.vgm_kg_is_null {
                conditions.push(if is_null {
                    "`vgm_kg` IS NULL"
                } else {
                    "`vgm_kg` IS NOT NULL"
                });
            }
            if let Some(value) = filter.vgm_method {
                conditions
                    .push("`vgm_method` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.vgm_method_like {
                conditions
                    .push("`vgm_method` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.setpoint_celsius {
                conditions
                    .push("`setpoint_celsius` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.setpoint_celsius_from {
                conditions
                    .push("`setpoint_celsius` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.setpoint_celsius_to {
                conditions
                    .push("`setpoint_celsius` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.setpoint_celsius_is_null {
                conditions.push(if is_null {
                    "`setpoint_celsius` IS NULL"
                } else {
                    "`setpoint_celsius` IS NOT NULL"
                });
            }
            if let Some(value) = filter.humidity_pct {
                conditions
                    .push("`humidity_pct` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.humidity_pct_from {
                conditions
                    .push("`humidity_pct` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.humidity_pct_to {
                conditions
                    .push("`humidity_pct` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.humidity_pct_is_null {
                conditions.push(if is_null {
                    "`humidity_pct` IS NULL"
                } else {
                    "`humidity_pct` IS NOT NULL"
                });
            }
            if let Some(value) = filter.vent_setting {
                conditions
                    .push("`vent_setting` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.vent_setting_like {
                conditions
                    .push("`vent_setting` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.vent_setting_is_null {
                conditions.push(if is_null {
                    "`vent_setting` IS NULL"
                } else {
                    "`vent_setting` IS NOT NULL"
                });
            }
            if let Some(value) = filter.stow_position {
                conditions
                    .push("`stow_position` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.stow_position_like {
                conditions
                    .push("`stow_position` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.stow_position_is_null {
                conditions.push(if is_null {
                    "`stow_position` IS NULL"
                } else {
                    "`stow_position` IS NOT NULL"
                });
            }
            if let Some(value) = filter.loaded_at {
                conditions
                    .push("`loaded_at` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.loaded_at_from {
                conditions
                    .push("`loaded_at` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.loaded_at_to {
                conditions
                    .push("`loaded_at` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.loaded_at_is_null {
                conditions.push(if is_null {
                    "`loaded_at` IS NULL"
                } else {
                    "`loaded_at` IS NOT NULL"
                });
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<Container>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
