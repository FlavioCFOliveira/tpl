// Code generated by tpl from freight.charge. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.charge` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::charge::{Charge, ChargeFilter, ChargeKey, ChargeOrder};
use super::charge_repository::ChargeRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.charge";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `charge_id`, `consignment_id`, `charge_code`, `quantity`, `unit_amount`, `currency`, `fx_rate_to_eur`, `net_amount`, `payer`, `invoiced_on`, `invoice_number` FROM `charge`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `charge_id`, `consignment_id`, `charge_code`, `quantity`, `unit_amount`, `currency`, `fx_rate_to_eur`, `net_amount`, `payer`, `invoiced_on`, `invoice_number` FROM `charge` WHERE `charge_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `charge` (`consignment_id`, `charge_code`, `quantity`, `unit_amount`, `currency`, `fx_rate_to_eur`, `payer`, `invoiced_on`, `invoice_number`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `charge` SET `consignment_id` = ?, `charge_code` = ?, `quantity` = ?, `unit_amount` = ?, `currency` = ?, `fx_rate_to_eur` = ?, `payer` = ?, `invoiced_on` = ?, `invoice_number` = ? WHERE `charge_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `charge` WHERE `charge_id` = ?";

/// The `sqlx` data layer of `freight.charge`.
#[derive(Debug, Clone)]
pub struct ChargeSqlx {
    pool: sqlx::MySqlPool,
}

impl ChargeSqlx {
    /// Builds the data layer of `freight.charge` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl ChargeRepository for ChargeSqlx {
    async fn create(&self, row: &mut Charge) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(row.consignment_id)
            .bind(&row.charge_code)
            .bind(&row.quantity)
            .bind(&row.unit_amount)
            .bind(&row.currency)
            .bind(&row.fx_rate_to_eur)
            .bind(&row.payer)
            .bind(row.invoiced_on)
            .bind(&row.invoice_number)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.charge_id = assigned;
        Ok(())
    }

    async fn read(&self, key: &ChargeKey) -> Result<Charge, Error> {
        sqlx::query_as::<_, Charge>(READ)
            .bind(key.charge_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &Charge) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(row.consignment_id)
            .bind(&row.charge_code)
            .bind(&row.quantity)
            .bind(&row.unit_amount)
            .bind(&row.currency)
            .bind(&row.fx_rate_to_eur)
            .bind(&row.payer)
            .bind(row.invoiced_on)
            .bind(&row.invoice_number)
            .bind(row.charge_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &ChargeKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.charge_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: ChargeFilter,
        order: &[ChargeOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<Charge>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.charge_id {
                conditions
                    .push("`charge_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.charge_id_from {
                conditions
                    .push("`charge_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.charge_id_to {
                conditions
                    .push("`charge_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.consignment_id {
                conditions
                    .push("`consignment_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.consignment_id_from {
                conditions
                    .push("`consignment_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.consignment_id_to {
                conditions
                    .push("`consignment_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.charge_code {
                conditions
                    .push("`charge_code` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.charge_code_like {
                conditions
                    .push("`charge_code` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.quantity {
                conditions
                    .push("`quantity` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.quantity_from {
                conditions
                    .push("`quantity` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.quantity_to {
                conditions
                    .push("`quantity` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.unit_amount {
                conditions
                    .push("`unit_amount` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.unit_amount_from {
                conditions
                    .push("`unit_amount` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.unit_amount_to {
                conditions
                    .push("`unit_amount` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.currency {
                conditions
                    .push("`currency` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.currency_like {
                conditions
                    .push("`currency` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.fx_rate_to_eur {
                conditions
                    .push("`fx_rate_to_eur` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.fx_rate_to_eur_from {
                conditions
                    .push("`fx_rate_to_eur` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.fx_rate_to_eur_to {
                conditions
                    .push("`fx_rate_to_eur` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.net_amount {
                conditions
                    .push("`net_amount` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.net_amount_from {
                conditions
                    .push("`net_amount` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.net_amount_to {
                conditions
                    .push("`net_amount` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.net_amount_is_null {
                conditions.push(if is_null {
                    "`net_amount` IS NULL"
                } else {
                    "`net_amount` IS NOT NULL"
                });
            }
            if let Some(value) = filter.payer {
                conditions
                    .push("`payer` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.payer_like {
                conditions
                    .push("`payer` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.invoiced_on {
                conditions
                    .push("`invoiced_on` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.invoiced_on_from {
                conditions
                    .push("`invoiced_on` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.invoiced_on_to {
                conditions
                    .push("`invoiced_on` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.invoiced_on_is_null {
                conditions.push(if is_null {
                    "`invoiced_on` IS NULL"
                } else {
                    "`invoiced_on` IS NOT NULL"
                });
            }
            if let Some(value) = filter.invoice_number {
                conditions
                    .push("`invoice_number` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.invoice_number_like {
                conditions
                    .push("`invoice_number` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.invoice_number_is_null {
                conditions.push(if is_null {
                    "`invoice_number` IS NULL"
                } else {
                    "`invoice_number` IS NOT NULL"
                });
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<Charge>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
