// Code generated by tpl from freight.legacy_edi_field. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.legacy_edi_field` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::legacy_edi_field::{LegacyEdiField, LegacyEdiFieldFilter, LegacyEdiFieldKey, LegacyEdiFieldOrder};
use super::legacy_edi_field_repository::LegacyEdiFieldRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.legacy_edi_field";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `mapping_id`, `segment_tag`, `back``tick`, `space in name`, `select`, `posição`, `Mixed Case Column`, `retired_on` FROM `legacy_edi_field`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `mapping_id`, `segment_tag`, `back``tick`, `space in name`, `select`, `posição`, `Mixed Case Column`, `retired_on` FROM `legacy_edi_field` WHERE `mapping_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `legacy_edi_field` (`segment_tag`, `back``tick`, `space in name`, `select`, `posição`, `Mixed Case Column`, `retired_on`) VALUES (?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `legacy_edi_field` SET `segment_tag` = ?, `back``tick` = ?, `space in name` = ?, `select` = ?, `posição` = ?, `Mixed Case Column` = ?, `retired_on` = ? WHERE `mapping_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `legacy_edi_field` WHERE `mapping_id` = ?";

/// The `sqlx` data layer of `freight.legacy_edi_field`.
#[derive(Debug, Clone)]
pub struct LegacyEdiFieldSqlx {
    pool: sqlx::MySqlPool,
}

impl LegacyEdiFieldSqlx {
    /// Builds the data layer of `freight.legacy_edi_field` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl LegacyEdiFieldRepository for LegacyEdiFieldSqlx {
    async fn create(&self, row: &mut LegacyEdiField) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(&row.segment_tag)
            .bind(&row.back_tick)
            .bind(&row.space_in_name)
            .bind(&row.select)
            .bind(&row.posição)
            .bind(&row.mixed_case_column)
            .bind(row.retired_on)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.mapping_id =
            u16::try_from(assigned).map_err(|source| Error::AssignedKey {
                table: TABLE,
                column: "mapping_id",
                source,
            })?;
        Ok(())
    }

    async fn read(&self, key: &LegacyEdiFieldKey) -> Result<LegacyEdiField, Error> {
        sqlx::query_as::<_, LegacyEdiField>(READ)
            .bind(key.mapping_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &LegacyEdiField) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.segment_tag)
            .bind(&row.back_tick)
            .bind(&row.space_in_name)
            .bind(&row.select)
            .bind(&row.posição)
            .bind(&row.mixed_case_column)
            .bind(row.retired_on)
            .bind(row.mapping_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &LegacyEdiFieldKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.mapping_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: LegacyEdiFieldFilter,
        order: &[LegacyEdiFieldOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<LegacyEdiField>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.mapping_id {
                conditions
                    .push("`mapping_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.mapping_id_from {
                conditions
                    .push("`mapping_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.mapping_id_to {
                conditions
                    .push("`mapping_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.segment_tag {
                conditions
                    .push("`segment_tag` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.segment_tag_like {
                conditions
                    .push("`segment_tag` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.back_tick {
                conditions
                    .push("`back``tick` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.back_tick_like {
                conditions
                    .push("`back``tick` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.back_tick_is_null {
                conditions.push(if is_null {
                    "`back``tick` IS NULL"
                } else {
                    "`back``tick` IS NOT NULL"
                });
            }
            if let Some(value) = filter.space_in_name {
                conditions
                    .push("`space in name` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.space_in_name_like {
                conditions
                    .push("`space in name` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.space_in_name_is_null {
                conditions.push(if is_null {
                    "`space in name` IS NULL"
                } else {
                    "`space in name` IS NOT NULL"
                });
            }
            if let Some(value) = filter.select {
                conditions
                    .push("`select` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.select_like {
                conditions
                    .push("`select` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.select_is_null {
                conditions.push(if is_null {
                    "`select` IS NULL"
                } else {
                    "`select` IS NOT NULL"
                });
            }
            if let Some(value) = filter.posição {
                conditions
                    .push("`posição` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.posição_like {
                conditions
                    .push("`posição` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.posição_is_null {
                conditions.push(if is_null {
                    "`posição` IS NULL"
                } else {
                    "`posição` IS NOT NULL"
                });
            }
            if let Some(value) = filter.mixed_case_column {
                conditions
                    .push("`Mixed Case Column` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.mixed_case_column_like {
                conditions
                    .push("`Mixed Case Column` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.mixed_case_column_is_null {
                conditions.push(if is_null {
                    "`Mixed Case Column` IS NULL"
                } else {
                    "`Mixed Case Column` IS NOT NULL"
                });
            }
            if let Some(value) = filter.retired_on {
                conditions
                    .push("`retired_on` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.retired_on_from {
                conditions
                    .push("`retired_on` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.retired_on_to {
                conditions
                    .push("`retired_on` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.retired_on_is_null {
                conditions.push(if is_null {
                    "`retired_on` IS NULL"
                } else {
                    "`retired_on` IS NOT NULL"
                });
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<LegacyEdiField>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
