// Code generated by tpl from freight.voyage. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.voyage` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::voyage::{Voyage, VoyageFilter, VoyageKey, VoyageOrder};
use super::voyage_repository::VoyageRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.voyage";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `vessel_imo`, `voyage_number`, `service_code`, `etd`, `eta`, `atd`, `status`, `bunker_price_usd_t`, `slot_allocation_teu`, `utilisation_pct` FROM `voyage`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `vessel_imo`, `voyage_number`, `service_code`, `etd`, `eta`, `atd`, `status`, `bunker_price_usd_t`, `slot_allocation_teu`, `utilisation_pct` FROM `voyage` WHERE `vessel_imo` = ? AND `voyage_number` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `voyage` (`vessel_imo`, `voyage_number`, `service_code`, `etd`, `eta`, `atd`, `status`, `bunker_price_usd_t`, `slot_allocation_teu`, `utilisation_pct`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `voyage` SET `service_code` = ?, `etd` = ?, `eta` = ?, `atd` = ?, `status` = ?, `bunker_price_usd_t` = ?, `slot_allocation_teu` = ?, `utilisation_pct` = ? WHERE `vessel_imo` = ? AND `voyage_number` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `voyage` WHERE `vessel_imo` = ? AND `voyage_number` = ?";

/// The `sqlx` data layer of `freight.voyage`.
#[derive(Debug, Clone)]
pub struct VoyageSqlx {
    pool: sqlx::MySqlPool,
}

impl VoyageSqlx {
    /// Builds the data layer of `freight.voyage` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl VoyageRepository for VoyageSqlx {
    async fn create(&self, row: &mut Voyage) -> Result<(), Error> {
        sqlx::query(INSERT)
            .bind(&row.vessel_imo)
            .bind(&row.voyage_number)
            .bind(&row.service_code)
            .bind(row.etd)
            .bind(row.eta)
            .bind(row.atd)
            .bind(&row.status)
            .bind(&row.bunker_price_usd_t)
            .bind(row.slot_allocation_teu)
            .bind(&row.utilisation_pct)
            .execute(&self.pool)
            .await?;
        Ok(())
    }

    async fn read(&self, key: &VoyageKey) -> Result<Voyage, Error> {
        sqlx::query_as::<_, Voyage>(READ)
            .bind(&key.vessel_imo)
            .bind(&key.voyage_number)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &Voyage) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.service_code)
            .bind(row.etd)
            .bind(row.eta)
            .bind(row.atd)
            .bind(&row.status)
            .bind(&row.bunker_price_usd_t)
            .bind(row.slot_allocation_teu)
            .bind(&row.utilisation_pct)
            .bind(&row.vessel_imo)
            .bind(&row.voyage_number)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &VoyageKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(&key.vessel_imo)
            .bind(&key.voyage_number)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: VoyageFilter,
        order: &[VoyageOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<Voyage>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.vessel_imo {
                conditions
                    .push("`vessel_imo` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.vessel_imo_like {
                conditions
                    .push("`vessel_imo` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.voyage_number {
                conditions
                    .push("`voyage_number` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.voyage_number_like {
                conditions
                    .push("`voyage_number` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.service_code {
                conditions
                    .push("`service_code` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.service_code_like {
                conditions
                    .push("`service_code` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.etd {
                conditions
                    .push("`etd` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.etd_from {
                conditions
                    .push("`etd` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.etd_to {
                conditions
                    .push("`etd` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.eta {
                conditions
                    .push("`eta` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.eta_from {
                conditions
                    .push("`eta` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.eta_to {
                conditions
                    .push("`eta` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.atd {
                conditions
                    .push("`atd` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.atd_from {
                conditions
                    .push("`atd` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.atd_to {
                conditions
                    .push("`atd` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.atd_is_null {
                conditions.push(if is_null {
                    "`atd` IS NULL"
                } else {
                    "`atd` IS NOT NULL"
                });
            }
            if let Some(value) = filter.status {
                conditions
                    .push("`status` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.status_like {
                conditions
                    .push("`status` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.bunker_price_usd_t {
                conditions
                    .push("`bunker_price_usd_t` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.bunker_price_usd_t_from {
                conditions
                    .push("`bunker_price_usd_t` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.bunker_price_usd_t_to {
                conditions
                    .push("`bunker_price_usd_t` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.bunker_price_usd_t_is_null {
                conditions.push(if is_null {
                    "`bunker_price_usd_t` IS NULL"
                } else {
                    "`bunker_price_usd_t` IS NOT NULL"
                });
            }
            if let Some(value) = filter.slot_allocation_teu {
                conditions
                    .push("`slot_allocation_teu` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.slot_allocation_teu_from {
                conditions
                    .push("`slot_allocation_teu` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.slot_allocation_teu_to {
                conditions
                    .push("`slot_allocation_teu` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.utilisation_pct {
                conditions
                    .push("`utilisation_pct` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.utilisation_pct_from {
                conditions
                    .push("`utilisation_pct` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.utilisation_pct_to {
                conditions
                    .push("`utilisation_pct` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.utilisation_pct_is_null {
                conditions.push(if is_null {
                    "`utilisation_pct` IS NULL"
                } else {
                    "`utilisation_pct` IS NOT NULL"
                });
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<Voyage>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
