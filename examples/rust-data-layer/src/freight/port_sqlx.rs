// Code generated by tpl from freight.port. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.port` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::port::{Port, PortFilter, PortKey, PortOrder};
use super::port_repository::PortRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.port";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `port_id`, `un_locode`, `name`, `country_code`, `local_name`, `timezone_name`, `utc_offset_minutes`, `berth_count`, `max_draught_m`, `tidal_range_m`, `annual_throughput_teu`, `customs_round_clock`, `harbour_dues_currency`, `approach_notes` FROM `port`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `port_id`, `un_locode`, `name`, `country_code`, `local_name`, `timezone_name`, `utc_offset_minutes`, `berth_count`, `max_draught_m`, `tidal_range_m`, `annual_throughput_teu`, `customs_round_clock`, `harbour_dues_currency`, `approach_notes` FROM `port` WHERE `port_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `port` (`un_locode`, `name`, `country_code`, `local_name`, `timezone_name`, `utc_offset_minutes`, `berth_count`, `max_draught_m`, `tidal_range_m`, `annual_throughput_teu`, `customs_round_clock`, `harbour_dues_currency`, `approach_notes`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `port` SET `un_locode` = ?, `name` = ?, `country_code` = ?, `local_name` = ?, `timezone_name` = ?, `utc_offset_minutes` = ?, `berth_count` = ?, `max_draught_m` = ?, `tidal_range_m` = ?, `annual_throughput_teu` = ?, `customs_round_clock` = ?, `harbour_dues_currency` = ?, `approach_notes` = ? WHERE `port_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `port` WHERE `port_id` = ?";

/// The `sqlx` data layer of `freight.port`.
#[derive(Debug, Clone)]
pub struct PortSqlx {
    pool: sqlx::MySqlPool,
}

impl PortSqlx {
    /// Builds the data layer of `freight.port` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl PortRepository for PortSqlx {
    async fn create(&self, row: &mut Port) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(&row.un_locode)
            .bind(&row.name)
            .bind(&row.country_code)
            .bind(&row.local_name)
            .bind(&row.timezone_name)
            .bind(row.utc_offset_minutes)
            .bind(row.berth_count)
            .bind(&row.max_draught_m)
            .bind(row.tidal_range_m)
            .bind(row.annual_throughput_teu)
            .bind(row.customs_round_clock)
            .bind(&row.harbour_dues_currency)
            .bind(&row.approach_notes)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.port_id =
            u16::try_from(assigned).map_err(|source| Error::AssignedKey {
                table: TABLE,
                column: "port_id",
                source,
            })?;
        Ok(())
    }

    async fn read(&self, key: &PortKey) -> Result<Port, Error> {
        sqlx::query_as::<_, Port>(READ)
            .bind(key.port_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &Port) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.un_locode)
            .bind(&row.name)
            .bind(&row.country_code)
            .bind(&row.local_name)
            .bind(&row.timezone_name)
            .bind(row.utc_offset_minutes)
            .bind(row.berth_count)
            .bind(&row.max_draught_m)
            .bind(row.tidal_range_m)
            .bind(row.annual_throughput_teu)
            .bind(row.customs_round_clock)
            .bind(&row.harbour_dues_currency)
            .bind(&row.approach_notes)
            .bind(row.port_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &PortKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.port_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: PortFilter,
        order: &[PortOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<Port>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.port_id {
                conditions
                    .push("`port_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.port_id_from {
                conditions
                    .push("`port_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.port_id_to {
                conditions
                    .push("`port_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.un_locode {
                conditions
                    .push("`un_locode` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.un_locode_like {
                conditions
                    .push("`un_locode` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.name {
                conditions
                    .push("`name` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.name_like {
                conditions
                    .push("`name` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.country_code {
                conditions
                    .push("`country_code` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.country_code_like {
                conditions
                    .push("`country_code` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.local_name {
                conditions
                    .push("`local_name` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.local_name_like {
                conditions
                    .push("`local_name` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.local_name_is_null {
                conditions.push(if is_null {
                    "`local_name` IS NULL"
                } else {
                    "`local_name` IS NOT NULL"
                });
            }
            if let Some(value) = filter.timezone_name {
                conditions
                    .push("`timezone_name` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.timezone_name_like {
                conditions
                    .push("`timezone_name` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.utc_offset_minutes {
                conditions
                    .push("`utc_offset_minutes` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.utc_offset_minutes_from {
                conditions
                    .push("`utc_offset_minutes` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.utc_offset_minutes_to {
                conditions
                    .push("`utc_offset_minutes` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.berth_count {
                conditions
                    .push("`berth_count` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.berth_count_from {
                conditions
                    .push("`berth_count` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.berth_count_to {
                conditions
                    .push("`berth_count` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.max_draught_m {
                conditions
                    .push("`max_draught_m` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.max_draught_m_from {
                conditions
                    .push("`max_draught_m` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.max_draught_m_to {
                conditions
                    .push("`max_draught_m` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.tidal_range_m {
                conditions
                    .push("`tidal_range_m` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.tidal_range_m_from {
                conditions
                    .push("`tidal_range_m` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.tidal_range_m_to {
                conditions
                    .push("`tidal_range_m` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.tidal_range_m_is_null {
                conditions.push(if is_null {
                    "`tidal_range_m` IS NULL"
                } else {
                    "`tidal_range_m` IS NOT NULL"
                });
            }
            if let Some(value) = filter.annual_throughput_teu {
                conditions
                    .push("`annual_throughput_teu` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.annual_throughput_teu_from {
                conditions
                    .push("`annual_throughput_teu` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.annual_throughput_teu_to {
                conditions
                    .push("`annual_throughput_teu` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.annual_throughput_teu_is_null {
                conditions.push(if is_null {
                    "`annual_throughput_teu` IS NULL"
                } else {
                    "`annual_throughput_teu` IS NOT NULL"
                });
            }
            if let Some(value) = filter.customs_round_clock {
                conditions
                    .push("`customs_round_clock` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.customs_round_clock_from {
                conditions
                    .push("`customs_round_clock` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.customs_round_clock_to {
                conditions
                    .push("`customs_round_clock` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.harbour_dues_currency {
                conditions
                    .push("`harbour_dues_currency` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.harbour_dues_currency_like {
                conditions
                    .push("`harbour_dues_currency` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.approach_notes {
                conditions
                    .push("`approach_notes` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.approach_notes_like {
                conditions
                    .push("`approach_notes` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.approach_notes_is_null {
                conditions.push(if is_null {
                    "`approach_notes` IS NULL"
                } else {
                    "`approach_notes` IS NOT NULL"
                });
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<Port>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
