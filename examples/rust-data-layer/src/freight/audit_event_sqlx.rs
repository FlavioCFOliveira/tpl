// Code generated by tpl from freight.audit_event. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.audit_event` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::audit_event::{AuditEvent, AuditEventFilter, AuditEventKey, AuditEventOrder};
use super::audit_event_repository::AuditEventRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.audit_event";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `audit_event_id`, `entity_name`, `entity_key`, `action`, `trigger_timing`, `changed_column`, `old_value`, `new_value`, `actor`, `client_ipv4`, `client_ipv6`, `correlation_uuid`, `observed_at`, `detail` FROM `audit_event`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `audit_event_id`, `entity_name`, `entity_key`, `action`, `trigger_timing`, `changed_column`, `old_value`, `new_value`, `actor`, `client_ipv4`, `client_ipv6`, `correlation_uuid`, `observed_at`, `detail` FROM `audit_event` WHERE `audit_event_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `audit_event` (`entity_name`, `entity_key`, `action`, `trigger_timing`, `changed_column`, `old_value`, `new_value`, `actor`, `client_ipv4`, `client_ipv6`, `correlation_uuid`, `observed_at`, `detail`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `audit_event` SET `entity_name` = ?, `entity_key` = ?, `action` = ?, `trigger_timing` = ?, `changed_column` = ?, `old_value` = ?, `new_value` = ?, `actor` = ?, `client_ipv4` = ?, `client_ipv6` = ?, `correlation_uuid` = ?, `observed_at` = ?, `detail` = ? WHERE `audit_event_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `audit_event` WHERE `audit_event_id` = ?";

/// The `sqlx` data layer of `freight.audit_event`.
#[derive(Debug, Clone)]
pub struct AuditEventSqlx {
    pool: sqlx::MySqlPool,
}

impl AuditEventSqlx {
    /// Builds the data layer of `freight.audit_event` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl AuditEventRepository for AuditEventSqlx {
    async fn create(&self, row: &mut AuditEvent) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(&row.entity_name)
            .bind(&row.entity_key)
            .bind(&row.action)
            .bind(&row.trigger_timing)
            .bind(&row.changed_column)
            .bind(&row.old_value)
            .bind(&row.new_value)
            .bind(&row.actor)
            .bind(row.client_ipv4)
            .bind(row.client_ipv6)
            .bind(&row.correlation_uuid)
            .bind(row.observed_at)
            .bind(&row.detail)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.audit_event_id = assigned;
        Ok(())
    }

    async fn read(&self, key: &AuditEventKey) -> Result<AuditEvent, Error> {
        sqlx::query_as::<_, AuditEvent>(READ)
            .bind(key.audit_event_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &AuditEvent) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.entity_name)
            .bind(&row.entity_key)
            .bind(&row.action)
            .bind(&row.trigger_timing)
            .bind(&row.changed_column)
            .bind(&row.old_value)
            .bind(&row.new_value)
            .bind(&row.actor)
            .bind(row.client_ipv4)
            .bind(row.client_ipv6)
            .bind(&row.correlation_uuid)
            .bind(row.observed_at)
            .bind(&row.detail)
            .bind(row.audit_event_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &AuditEventKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.audit_event_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: AuditEventFilter,
        order: &[AuditEventOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<AuditEvent>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.audit_event_id {
                conditions
                    .push("`audit_event_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.audit_event_id_from {
                conditions
                    .push("`audit_event_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.audit_event_id_to {
                conditions
                    .push("`audit_event_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.entity_name {
                conditions
                    .push("`entity_name` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.entity_name_like {
                conditions
                    .push("`entity_name` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.entity_key {
                conditions
                    .push("`entity_key` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.entity_key_like {
                conditions
                    .push("`entity_key` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.action {
                conditions
                    .push("`action` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.action_like {
                conditions
                    .push("`action` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.trigger_timing {
                conditions
                    .push("`trigger_timing` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.trigger_timing_like {
                conditions
                    .push("`trigger_timing` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.changed_column {
                conditions
                    .push("`changed_column` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.changed_column_like {
                conditions
                    .push("`changed_column` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.changed_column_is_null {
                conditions.push(if is_null {
                    "`changed_column` IS NULL"
                } else {
                    "`changed_column` IS NOT NULL"
                });
            }
            if let Some(value) = filter.old_value {
                conditions
                    .push("`old_value` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.old_value_like {
                conditions
                    .push("`old_value` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.old_value_is_null {
                conditions.push(if is_null {
                    "`old_value` IS NULL"
                } else {
                    "`old_value` IS NOT NULL"
                });
            }
            if let Some(value) = filter.new_value {
                conditions
                    .push("`new_value` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.new_value_like {
                conditions
                    .push("`new_value` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.new_value_is_null {
                conditions.push(if is_null {
                    "`new_value` IS NULL"
                } else {
                    "`new_value` IS NOT NULL"
                });
            }
            if let Some(value) = filter.actor {
                conditions
                    .push("`actor` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.actor_like {
                conditions
                    .push("`actor` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.client_ipv4 {
                conditions
                    .push("`client_ipv4` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.client_ipv4_is_null {
                conditions.push(if is_null {
                    "`client_ipv4` IS NULL"
                } else {
                    "`client_ipv4` IS NOT NULL"
                });
            }
            if let Some(value) = filter.client_ipv6 {
                conditions
                    .push("`client_ipv6` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.client_ipv6_is_null {
                conditions.push(if is_null {
                    "`client_ipv6` IS NULL"
                } else {
                    "`client_ipv6` IS NOT NULL"
                });
            }
            if let Some(value) = filter.correlation_uuid {
                conditions
                    .push("`correlation_uuid` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.correlation_uuid_is_null {
                conditions.push(if is_null {
                    "`correlation_uuid` IS NULL"
                } else {
                    "`correlation_uuid` IS NOT NULL"
                });
            }
            if let Some(value) = filter.observed_at {
                conditions
                    .push("`observed_at` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.observed_at_from {
                conditions
                    .push("`observed_at` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.observed_at_to {
                conditions
                    .push("`observed_at` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.detail {
                conditions
                    .push("`detail` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.detail_like {
                conditions
                    .push("`detail` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.detail_is_null {
                conditions.push(if is_null {
                    "`detail` IS NULL"
                } else {
                    "`detail` IS NOT NULL"
                });
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<AuditEvent>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
