// Code generated by tpl from freight.tariff. DO NOT EDIT.

//! The `sqlx` implementation of the `freight.tariff` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::tariff::{Tariff, TariffFilter, TariffKey, TariffOrder};
use super::tariff_repository::TariffRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "freight.tariff";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `tariff_id`, `trade_lane`, `origin_region`, `destination_region`, `equipment_iso_code`, `basis`, `currency`, `base_rate`, `bunker_surcharge`, `congestion_surcharge`, `all_in_rate`, `valid_from`, `valid_until` FROM `tariff`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `tariff_id`, `trade_lane`, `origin_region`, `destination_region`, `equipment_iso_code`, `basis`, `currency`, `base_rate`, `bunker_surcharge`, `congestion_surcharge`, `all_in_rate`, `valid_from`, `valid_until` FROM `tariff` WHERE `tariff_id` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `tariff` (`trade_lane`, `origin_region`, `destination_region`, `equipment_iso_code`, `basis`, `currency`, `base_rate`, `bunker_surcharge`, `congestion_surcharge`, `valid_from`, `valid_until`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `tariff` SET `trade_lane` = ?, `origin_region` = ?, `destination_region` = ?, `equipment_iso_code` = ?, `basis` = ?, `currency` = ?, `base_rate` = ?, `bunker_surcharge` = ?, `congestion_surcharge` = ?, `valid_from` = ?, `valid_until` = ? WHERE `tariff_id` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `tariff` WHERE `tariff_id` = ?";

/// The `sqlx` data layer of `freight.tariff`.
#[derive(Debug, Clone)]
pub struct TariffSqlx {
    pool: sqlx::MySqlPool,
}

impl TariffSqlx {
    /// Builds the data layer of `freight.tariff` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl TariffRepository for TariffSqlx {
    async fn create(&self, row: &mut Tariff) -> Result<(), Error> {
        let assigned = sqlx::query(INSERT)
            .bind(&row.trade_lane)
            .bind(&row.origin_region)
            .bind(&row.destination_region)
            .bind(&row.equipment_iso_code)
            .bind(&row.basis)
            .bind(&row.currency)
            .bind(&row.base_rate)
            .bind(&row.bunker_surcharge)
            .bind(&row.congestion_surcharge)
            .bind(row.valid_from)
            .bind(row.valid_until)
            .execute(&self.pool)
            .await?
            .last_insert_id();
        row.tariff_id =
            u32::try_from(assigned).map_err(|source| Error::AssignedKey {
                table: TABLE,
                column: "tariff_id",
                source,
            })?;
        Ok(())
    }

    async fn read(&self, key: &TariffKey) -> Result<Tariff, Error> {
        sqlx::query_as::<_, Tariff>(READ)
            .bind(key.tariff_id)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &Tariff) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.trade_lane)
            .bind(&row.origin_region)
            .bind(&row.destination_region)
            .bind(&row.equipment_iso_code)
            .bind(&row.basis)
            .bind(&row.currency)
            .bind(&row.base_rate)
            .bind(&row.bunker_surcharge)
            .bind(&row.congestion_surcharge)
            .bind(row.valid_from)
            .bind(row.valid_until)
            .bind(row.tariff_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &TariffKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(key.tariff_id)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: TariffFilter,
        order: &[TariffOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<Tariff>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.tariff_id {
                conditions
                    .push("`tariff_id` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.tariff_id_from {
                conditions
                    .push("`tariff_id` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.tariff_id_to {
                conditions
                    .push("`tariff_id` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.trade_lane {
                conditions
                    .push("`trade_lane` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.trade_lane_like {
                conditions
                    .push("`trade_lane` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.origin_region {
                conditions
                    .push("`origin_region` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.origin_region_like {
                conditions
                    .push("`origin_region` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.destination_region {
                conditions
                    .push("`destination_region` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.destination_region_like {
                conditions
                    .push("`destination_region` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.equipment_iso_code {
                conditions
                    .push("`equipment_iso_code` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.equipment_iso_code_like {
                conditions
                    .push("`equipment_iso_code` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.basis {
                conditions
                    .push("`basis` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.basis_like {
                conditions
                    .push("`basis` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.currency {
                conditions
                    .push("`currency` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.currency_like {
                conditions
                    .push("`currency` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.base_rate {
                conditions
                    .push("`base_rate` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.base_rate_from {
                conditions
                    .push("`base_rate` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.base_rate_to {
                conditions
                    .push("`base_rate` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.bunker_surcharge {
                conditions
                    .push("`bunker_surcharge` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.bunker_surcharge_from {
                conditions
                    .push("`bunker_surcharge` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.bunker_surcharge_to {
                conditions
                    .push("`bunker_surcharge` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.congestion_surcharge {
                conditions
                    .push("`congestion_surcharge` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.congestion_surcharge_from {
                conditions
                    .push("`congestion_surcharge` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.congestion_surcharge_to {
                conditions
                    .push("`congestion_surcharge` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.all_in_rate {
                conditions
                    .push("`all_in_rate` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.all_in_rate_from {
                conditions
                    .push("`all_in_rate` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.all_in_rate_to {
                conditions
                    .push("`all_in_rate` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.all_in_rate_is_null {
                conditions.push(if is_null {
                    "`all_in_rate` IS NULL"
                } else {
                    "`all_in_rate` IS NOT NULL"
                });
            }
            if let Some(value) = filter.valid_from {
                conditions
                    .push("`valid_from` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.valid_from_from {
                conditions
                    .push("`valid_from` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.valid_from_to {
                conditions
                    .push("`valid_from` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.valid_until {
                conditions
                    .push("`valid_until` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.valid_until_from {
                conditions
                    .push("`valid_until` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.valid_until_to {
                conditions
                    .push("`valid_until` <= ")
                    .push_bind_unseparated(value);
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<Tariff>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
