// Code generated by tpl from world.country. DO NOT EDIT.

//! The `sqlx` implementation of the `world.country` data layer.

use sqlx::{MySql, QueryBuilder};

use super::Error;
use super::country::{Country, CountryFilter, CountryKey, CountryOrder};
use super::country_repository::CountryRepository;

/// The table every statement of this module addresses, as an error names it.
const TABLE: &str = "world.country";

/// Selects every column of the table, in catalogue order.
const SELECT: &str = "SELECT `Code`, `Name`, `Continent`, `Region`, `SurfaceArea`, `IndepYear`, `Population`, `LifeExpectancy`, `GNP`, `GNPOld`, `LocalName`, `GovernmentForm`, `HeadOfState`, `Capital`, `Code2` FROM `country`";

/// Selects the one row a primary key names.
const READ: &str = "SELECT `Code`, `Name`, `Continent`, `Region`, `SurfaceArea`, `IndepYear`, `Population`, `LifeExpectancy`, `GNP`, `GNPOld`, `LocalName`, `GovernmentForm`, `HeadOfState`, `Capital`, `Code2` FROM `country` WHERE `Code` = ?";

/// Inserts one row.
///
/// It names neither a generated column nor an auto-increment key, because the
/// server writes both.
const INSERT: &str = "INSERT INTO `country` (`Code`, `Name`, `Continent`, `Region`, `SurfaceArea`, `IndepYear`, `Population`, `LifeExpectancy`, `GNP`, `GNPOld`, `LocalName`, `GovernmentForm`, `HeadOfState`, `Capital`, `Code2`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/// Writes every column that is neither generated nor part of the key.
const UPDATE: &str = "UPDATE `country` SET `Name` = ?, `Continent` = ?, `Region` = ?, `SurfaceArea` = ?, `IndepYear` = ?, `Population` = ?, `LifeExpectancy` = ?, `GNP` = ?, `GNPOld` = ?, `LocalName` = ?, `GovernmentForm` = ?, `HeadOfState` = ?, `Capital` = ?, `Code2` = ? WHERE `Code` = ?";

/// Removes the one row a primary key names.
const DELETE: &str = "DELETE FROM `country` WHERE `Code` = ?";

/// The `sqlx` data layer of `world.country`.
#[derive(Debug, Clone)]
pub struct CountrySqlx {
    pool: sqlx::MySqlPool,
}

impl CountrySqlx {
    /// Builds the data layer of `world.country` over `pool`.
    ///
    /// The pool must address the schema [`SCHEMA`] names, because the
    /// statements above address the table unqualified. [`connect`] is the pool
    /// that carries it.
    ///
    /// [`SCHEMA`]: super::SCHEMA
    /// [`connect`]: super::connect
    #[must_use]
    pub fn new(pool: sqlx::MySqlPool) -> Self {
        Self { pool }
    }
}

impl CountryRepository for CountrySqlx {
    async fn create(&self, row: &mut Country) -> Result<(), Error> {
        sqlx::query(INSERT)
            .bind(&row.code)
            .bind(&row.name)
            .bind(&row.continent)
            .bind(&row.region)
            .bind(&row.surface_area)
            .bind(row.indep_year)
            .bind(row.population)
            .bind(&row.life_expectancy)
            .bind(&row.gnp)
            .bind(&row.gnp_old)
            .bind(&row.local_name)
            .bind(&row.government_form)
            .bind(&row.head_of_state)
            .bind(row.capital)
            .bind(&row.code2)
            .execute(&self.pool)
            .await?;
        Ok(())
    }

    async fn read(&self, key: &CountryKey) -> Result<Country, Error> {
        sqlx::query_as::<_, Country>(READ)
            .bind(&key.code)
            .fetch_optional(&self.pool)
            .await?
            .ok_or(Error::NotFound { table: TABLE })
    }

    async fn update(&self, row: &Country) -> Result<u64, Error> {
        let outcome = sqlx::query(UPDATE)
            .bind(&row.name)
            .bind(&row.continent)
            .bind(&row.region)
            .bind(&row.surface_area)
            .bind(row.indep_year)
            .bind(row.population)
            .bind(&row.life_expectancy)
            .bind(&row.gnp)
            .bind(&row.gnp_old)
            .bind(&row.local_name)
            .bind(&row.government_form)
            .bind(&row.head_of_state)
            .bind(row.capital)
            .bind(&row.code2)
            .bind(&row.code)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn delete(&self, key: &CountryKey) -> Result<u64, Error> {
        let outcome = sqlx::query(DELETE)
            .bind(&key.code)
            .execute(&self.pool)
            .await?;

        Ok(outcome.rows_affected())
    }

    async fn search(
        &self,
        filter: CountryFilter,
        order: &[CountryOrder],
        limit: Option<u64>,
        offset: Option<u64>,
    ) -> Result<Vec<Country>, Error> {
        let mut builder: QueryBuilder<MySql> = QueryBuilder::new(SELECT);

        if !filter.is_empty() {
            builder.push(" WHERE ");
            let mut conditions = builder.separated(" AND ");

            if let Some(value) = filter.code {
                conditions
                    .push("`Code` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.code_like {
                conditions
                    .push("`Code` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.name {
                conditions
                    .push("`Name` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.name_like {
                conditions
                    .push("`Name` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.continent {
                conditions
                    .push("`Continent` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.continent_like {
                conditions
                    .push("`Continent` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.region {
                conditions
                    .push("`Region` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.region_like {
                conditions
                    .push("`Region` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.surface_area {
                conditions
                    .push("`SurfaceArea` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.surface_area_from {
                conditions
                    .push("`SurfaceArea` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.surface_area_to {
                conditions
                    .push("`SurfaceArea` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.indep_year {
                conditions
                    .push("`IndepYear` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.indep_year_from {
                conditions
                    .push("`IndepYear` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.indep_year_to {
                conditions
                    .push("`IndepYear` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.indep_year_is_null {
                conditions.push(if is_null {
                    "`IndepYear` IS NULL"
                } else {
                    "`IndepYear` IS NOT NULL"
                });
            }
            if let Some(value) = filter.population {
                conditions
                    .push("`Population` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.population_from {
                conditions
                    .push("`Population` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.population_to {
                conditions
                    .push("`Population` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.life_expectancy {
                conditions
                    .push("`LifeExpectancy` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.life_expectancy_from {
                conditions
                    .push("`LifeExpectancy` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.life_expectancy_to {
                conditions
                    .push("`LifeExpectancy` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.life_expectancy_is_null {
                conditions.push(if is_null {
                    "`LifeExpectancy` IS NULL"
                } else {
                    "`LifeExpectancy` IS NOT NULL"
                });
            }
            if let Some(value) = filter.gnp {
                conditions
                    .push("`GNP` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gnp_from {
                conditions
                    .push("`GNP` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gnp_to {
                conditions
                    .push("`GNP` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.gnp_is_null {
                conditions.push(if is_null {
                    "`GNP` IS NULL"
                } else {
                    "`GNP` IS NOT NULL"
                });
            }
            if let Some(value) = filter.gnp_old {
                conditions
                    .push("`GNPOld` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gnp_old_from {
                conditions
                    .push("`GNPOld` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.gnp_old_to {
                conditions
                    .push("`GNPOld` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.gnp_old_is_null {
                conditions.push(if is_null {
                    "`GNPOld` IS NULL"
                } else {
                    "`GNPOld` IS NOT NULL"
                });
            }
            if let Some(value) = filter.local_name {
                conditions
                    .push("`LocalName` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.local_name_like {
                conditions
                    .push("`LocalName` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.government_form {
                conditions
                    .push("`GovernmentForm` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.government_form_like {
                conditions
                    .push("`GovernmentForm` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.head_of_state {
                conditions
                    .push("`HeadOfState` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.head_of_state_like {
                conditions
                    .push("`HeadOfState` LIKE ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.head_of_state_is_null {
                conditions.push(if is_null {
                    "`HeadOfState` IS NULL"
                } else {
                    "`HeadOfState` IS NOT NULL"
                });
            }
            if let Some(value) = filter.capital {
                conditions
                    .push("`Capital` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.capital_from {
                conditions
                    .push("`Capital` >= ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.capital_to {
                conditions
                    .push("`Capital` <= ")
                    .push_bind_unseparated(value);
            }
            if let Some(is_null) = filter.capital_is_null {
                conditions.push(if is_null {
                    "`Capital` IS NULL"
                } else {
                    "`Capital` IS NOT NULL"
                });
            }
            if let Some(value) = filter.code2 {
                conditions
                    .push("`Code2` = ")
                    .push_bind_unseparated(value);
            }
            if let Some(value) = filter.code2_like {
                conditions
                    .push("`Code2` LIKE ")
                    .push_bind_unseparated(value);
            }
        }

        if !order.is_empty() {
            builder.push(" ORDER BY ");
            for (position, term) in order.iter().enumerate() {
                if position > 0 {
                    builder.push(", ");
                }
                builder.push(term.column.as_sql());
                builder.push(" ");
                builder.push(term.direction.as_sql());
            }
        }

        if let Some(limit) = limit {
            builder.push(" LIMIT ").push_bind(limit);

            if let Some(offset) = offset {
                builder.push(" OFFSET ").push_bind(offset);
            }
        }

        builder
            .build_query_as::<Country>()
            .fetch_all(&self.pool)
            .await
            .map_err(Error::from)
    }
}
