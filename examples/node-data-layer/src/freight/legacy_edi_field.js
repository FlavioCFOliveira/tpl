// Code generated by tpl from freight.legacy_edi_field. DO NOT EDIT.

/**
 * The `freight.legacy_edi_field` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

// The table's own comment, as the catalogue carries it:
// CASOS-LIMITE DE IDENTIFICADORES — mapeamento EDIFACT herdado; os nomes das colunas são deliberadamente hostis ao parser

/**
 * One row of `freight.legacy_edi_field`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} LegacyEdiField
 * @property {number} mappingId `mapping_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
 *   It is part of the primary key.
 * @property {string} segmentTag `segment_tag`, declared `char(3)` NOT NULL.
 *   It is part of a unique index.
 *   Comment: EDIFACT segment tag, e.g. BGM, NAD, CNI
 * @property {string | null} backTick `back`tick`, declared `varchar(48)` NULL.
 *   Comment: Identifier containing an embedded backtick, doubled when quoted
 * @property {string | null} spaceInName `space in name`, declared `varchar(48)` NULL.
 *   Comment: Identifier containing a space
 * @property {string | null} select `select`, declared `varchar(48)` NULL.
 *   Comment: Identifier that is a reserved word
 * @property {string | null} posição `posição`, declared `varchar(48)` NULL.
 *   Comment: Identifier containing non-ASCII letters
 * @property {string | null} mixedCaseColumn `Mixed Case Column`, declared `varchar(48)` NULL.
 *   Comment: Identifier mixing case and spaces
 * @property {Date | null} retiredOn `retired_on`, declared `date` NULL.
 */

/**
 * The primary key of `freight.legacy_edi_field`.
 *
 * @typedef {object} LegacyEdiFieldKey
 * @property {number} mappingId `mapping_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
 */

/**
 * One column of `freight.legacy_edi_field`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link LegacyEdiFieldColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const LegacyEdiFieldColumns = Object.freeze(
  /** @type {const} */ ({
    /** `mapping_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT. */
    MAPPING_ID: "mapping_id",
    /** `segment_tag`, declared `char(3)` NOT NULL. */
    SEGMENT_TAG: "segment_tag",
    /** `back`tick`, declared `varchar(48)` NULL. */
    BACK_TICK: "back`tick",
    /** `space in name`, declared `varchar(48)` NULL. */
    SPACE_IN_NAME: "space in name",
    /** `select`, declared `varchar(48)` NULL. */
    SELECT: "select",
    /** `posição`, declared `varchar(48)` NULL. */
    POSIçãO: "posição",
    /** `Mixed Case Column`, declared `varchar(48)` NULL. */
    MIXED_CASE_COLUMN: "Mixed Case Column",
    /** `retired_on`, declared `date` NULL. */
    RETIRED_ON: "retired_on",
  }),
);

/**
 * A column of `freight.legacy_edi_field` an ordering may name.
 *
 * @typedef {(typeof LegacyEdiFieldColumns)[keyof typeof LegacyEdiFieldColumns]} LegacyEdiFieldColumn
 */

/**
 * The one place a column of `freight.legacy_edi_field` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<LegacyEdiFieldColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "mapping_id": "`mapping_id`",
  "segment_tag": "`segment_tag`",
  "back`tick": "`back``tick`",
  "space in name": "`space in name`",
  "select": "`select`",
  "posição": "`posição`",
  "Mixed Case Column": "`Mixed Case Column`",
  "retired_on": "`retired_on`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {LegacyEdiFieldColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function legacyEdiFieldColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("freight.legacy_edi_field", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `freight.legacy_edi_field`.
 *
 * @typedef {object} LegacyEdiFieldOrder
 * @property {LegacyEdiFieldColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `freight.legacy_edi_field`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} LegacyEdiFieldFilter
 * @property {number} [mappingId] where set, requires `mapping_id` to equal it.
 * @property {number} [mappingIdFrom] where set, requires `mapping_id` to be at least it.
 * @property {number} [mappingIdTo] where set, requires `mapping_id` to be at most it.
 * @property {string} [segmentTag] where set, requires `segment_tag` to equal it.
 * @property {string} [segmentTagLike] where set, requires `segment_tag` to match it, as LIKE matches.
 * @property {string} [backTick] where set, requires `back`tick` to equal it.
 * @property {string} [backTickLike] where set, requires `back`tick` to match it, as LIKE matches.
 * @property {boolean} [backTickIsNull] where true, requires `back`tick` to be NULL; where false, requires it not to be.
 * @property {string} [spaceInName] where set, requires `space in name` to equal it.
 * @property {string} [spaceInNameLike] where set, requires `space in name` to match it, as LIKE matches.
 * @property {boolean} [spaceInNameIsNull] where true, requires `space in name` to be NULL; where false, requires it not to be.
 * @property {string} [select] where set, requires `select` to equal it.
 * @property {string} [selectLike] where set, requires `select` to match it, as LIKE matches.
 * @property {boolean} [selectIsNull] where true, requires `select` to be NULL; where false, requires it not to be.
 * @property {string} [posição] where set, requires `posição` to equal it.
 * @property {string} [posiçãoLike] where set, requires `posição` to match it, as LIKE matches.
 * @property {boolean} [posiçãoIsNull] where true, requires `posição` to be NULL; where false, requires it not to be.
 * @property {string} [mixedCaseColumn] where set, requires `Mixed Case Column` to equal it.
 * @property {string} [mixedCaseColumnLike] where set, requires `Mixed Case Column` to match it, as LIKE matches.
 * @property {boolean} [mixedCaseColumnIsNull] where true, requires `Mixed Case Column` to be NULL; where false, requires it not to be.
 * @property {Date} [retiredOn] where set, requires `retired_on` to equal it.
 * @property {Date} [retiredOnFrom] where set, requires `retired_on` to be at least it.
 * @property {Date} [retiredOnTo] where set, requires `retired_on` to be at most it.
 * @property {boolean} [retiredOnIsNull] where true, requires `retired_on` to be NULL; where false, requires it not to be.
 */
