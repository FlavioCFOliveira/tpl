// Code generated by tpl from freight.voyage. DO NOT EDIT.

/**
 * The mysql2 implementation of the `freight.voyage` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { Voyage, VoyageFilter, VoyageKey, VoyageOrder } from './voyage.js' */

import { NotFound, directionSql, failure } from './core.js';
import { voyageColumnSql } from './voyage.js';
import { VoyageRepository } from './voyage_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "freight.voyage";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `vessel_imo`, `voyage_number`, `service_code`, `etd`, `eta`, `atd`, `status`, `bunker_price_usd_t`, `slot_allocation_teu`, `utilisation_pct` FROM `voyage`";

/** Selects the one row a primary key names. */
const READ = "SELECT `vessel_imo`, `voyage_number`, `service_code`, `etd`, `eta`, `atd`, `status`, `bunker_price_usd_t`, `slot_allocation_teu`, `utilisation_pct` FROM `voyage` WHERE `vessel_imo` = ? AND `voyage_number` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `voyage` (`vessel_imo`, `voyage_number`, `service_code`, `etd`, `eta`, `atd`, `status`, `bunker_price_usd_t`, `slot_allocation_teu`, `utilisation_pct`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `voyage` SET `service_code` = ?, `etd` = ?, `eta` = ?, `atd` = ?, `status` = ?, `bunker_price_usd_t` = ?, `slot_allocation_teu` = ?, `utilisation_pct` = ? WHERE `vessel_imo` = ? AND `voyage_number` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `voyage` WHERE `vessel_imo` = ? AND `voyage_number` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `Voyage` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {Voyage}
 */
function toRow(values) {
  return {
    vesselImo: values[0],
    voyageNumber: values[1],
    serviceCode: values[2],
    etd: values[3],
    eta: values[4],
    atd: values[5],
    status: values[6],
    bunkerPriceUsdT: values[7],
    slotAllocationTeu: values[8],
    utilisationPct: values[9],
  };
}

/**
 * The mysql2 data layer of `freight.voyage`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class VoyageMysql2 extends VoyageRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `freight.voyage` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The whole key is taken from `row`, which the caller therefore fills.
   *
   * @override
   * @param {Voyage} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.vesselImo,
      row.voyageNumber,
      row.serviceCode,
      row.etd,
      row.eta,
      row.atd,
      row.status,
      row.bunkerPriceUsdT,
      row.slotAllocationTeu,
      row.utilisationPct,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {VoyageKey} key
   * @returns {Promise<Voyage>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.vesselImo,
      key.voyageNumber,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {Voyage} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.serviceCode,
      row.etd,
      row.eta,
      row.atd,
      row.status,
      row.bunkerPriceUsdT,
      row.slotAllocationTeu,
      row.utilisationPct,
      row.vesselImo,
      row.voyageNumber,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {VoyageKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.vesselImo,
      key.voyageNumber,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {VoyageFilter} criteria
   * @param {readonly VoyageOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<Voyage[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.vesselImo !== undefined) {
      conditions.push("`vessel_imo` = ?");
      values.push(criteria.vesselImo);
    }
    if (criteria.vesselImoLike !== undefined) {
      conditions.push("`vessel_imo` LIKE ?");
      values.push(criteria.vesselImoLike);
    }
    if (criteria.voyageNumber !== undefined) {
      conditions.push("`voyage_number` = ?");
      values.push(criteria.voyageNumber);
    }
    if (criteria.voyageNumberLike !== undefined) {
      conditions.push("`voyage_number` LIKE ?");
      values.push(criteria.voyageNumberLike);
    }
    if (criteria.serviceCode !== undefined) {
      conditions.push("`service_code` = ?");
      values.push(criteria.serviceCode);
    }
    if (criteria.serviceCodeLike !== undefined) {
      conditions.push("`service_code` LIKE ?");
      values.push(criteria.serviceCodeLike);
    }
    if (criteria.etd !== undefined) {
      conditions.push("`etd` = ?");
      values.push(criteria.etd);
    }
    if (criteria.etdFrom !== undefined) {
      conditions.push("`etd` >= ?");
      values.push(criteria.etdFrom);
    }
    if (criteria.etdTo !== undefined) {
      conditions.push("`etd` <= ?");
      values.push(criteria.etdTo);
    }
    if (criteria.eta !== undefined) {
      conditions.push("`eta` = ?");
      values.push(criteria.eta);
    }
    if (criteria.etaFrom !== undefined) {
      conditions.push("`eta` >= ?");
      values.push(criteria.etaFrom);
    }
    if (criteria.etaTo !== undefined) {
      conditions.push("`eta` <= ?");
      values.push(criteria.etaTo);
    }
    if (criteria.atd !== undefined) {
      conditions.push("`atd` = ?");
      values.push(criteria.atd);
    }
    if (criteria.atdFrom !== undefined) {
      conditions.push("`atd` >= ?");
      values.push(criteria.atdFrom);
    }
    if (criteria.atdTo !== undefined) {
      conditions.push("`atd` <= ?");
      values.push(criteria.atdTo);
    }
    if (criteria.atdIsNull !== undefined) {
      conditions.push(
        criteria.atdIsNull
          ? "`atd` IS NULL"
          : "`atd` IS NOT NULL",
      );
    }
    if (criteria.status !== undefined) {
      conditions.push("`status` = ?");
      values.push(criteria.status);
    }
    if (criteria.statusLike !== undefined) {
      conditions.push("`status` LIKE ?");
      values.push(criteria.statusLike);
    }
    if (criteria.bunkerPriceUsdT !== undefined) {
      conditions.push("`bunker_price_usd_t` = ?");
      values.push(criteria.bunkerPriceUsdT);
    }
    if (criteria.bunkerPriceUsdTFrom !== undefined) {
      conditions.push("`bunker_price_usd_t` >= ?");
      values.push(criteria.bunkerPriceUsdTFrom);
    }
    if (criteria.bunkerPriceUsdTTo !== undefined) {
      conditions.push("`bunker_price_usd_t` <= ?");
      values.push(criteria.bunkerPriceUsdTTo);
    }
    if (criteria.bunkerPriceUsdTIsNull !== undefined) {
      conditions.push(
        criteria.bunkerPriceUsdTIsNull
          ? "`bunker_price_usd_t` IS NULL"
          : "`bunker_price_usd_t` IS NOT NULL",
      );
    }
    if (criteria.slotAllocationTeu !== undefined) {
      conditions.push("`slot_allocation_teu` = ?");
      values.push(criteria.slotAllocationTeu);
    }
    if (criteria.slotAllocationTeuFrom !== undefined) {
      conditions.push("`slot_allocation_teu` >= ?");
      values.push(criteria.slotAllocationTeuFrom);
    }
    if (criteria.slotAllocationTeuTo !== undefined) {
      conditions.push("`slot_allocation_teu` <= ?");
      values.push(criteria.slotAllocationTeuTo);
    }
    if (criteria.utilisationPct !== undefined) {
      conditions.push("`utilisation_pct` = ?");
      values.push(criteria.utilisationPct);
    }
    if (criteria.utilisationPctFrom !== undefined) {
      conditions.push("`utilisation_pct` >= ?");
      values.push(criteria.utilisationPctFrom);
    }
    if (criteria.utilisationPctTo !== undefined) {
      conditions.push("`utilisation_pct` <= ?");
      values.push(criteria.utilisationPctTo);
    }
    if (criteria.utilisationPctIsNull !== undefined) {
      conditions.push(
        criteria.utilisationPctIsNull
          ? "`utilisation_pct` IS NULL"
          : "`utilisation_pct` IS NOT NULL",
      );
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              voyageColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
