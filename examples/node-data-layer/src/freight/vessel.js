// Code generated by tpl from freight.vessel. DO NOT EDIT.

/**
 * The `freight.vessel` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

// The table's own comment, as the catalogue carries it:
// Navios porta-contentores em serviço ou fretados

/**
 * One row of `freight.vessel`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} Vessel
 * @property {string} imoNumber `imo_number`, declared `char(7)` NOT NULL.
 *   It is part of the primary key.
 *   Comment: IMO ship identification number, seven digits, check digit included
 * @property {string} name `name`, declared `varchar(100)` NOT NULL.
 *   Comment: Registered name; changes on sale
 * @property {string} callSign `call_sign`, declared `varchar(10)` NOT NULL.
 * @property {number | null} mmsi `mmsi`, declared `int(10) unsigned` NULL.
 *   It is part of a unique index.
 *   Comment: AIS Maritime Mobile Service Identity, nine digits
 * @property {string} flagState `flag_state`, declared `char(2)` NOT NULL.
 * @property {string} classSociety `class_society`, declared `enum('Lloyd''s Register','DNV','Bureau Veritas','ClassNK','American Bureau of Shipping','RINA')` NOT NULL.
 *   Comment: Classification society; note the apostrophe in the first member
 * @property {number | null} hullNumber `hull_number`, declared `mediumint(9)` NULL.
 *   Comment: Builder yard number; signed, as some yards used negative rework codes
 * @property {number} capacityTeu `capacity_teu`, declared `mediumint(8) unsigned` NOT NULL.
 *   Comment: Nominal intake in twenty-foot equivalent units
 * @property {number} reeferPlugs `reefer_plugs`, declared `smallint(5) unsigned` NOT NULL.
 * @property {number} deadweightTonnes `deadweight_tonnes`, declared `int(10) unsigned` NOT NULL.
 * @property {string} grossTonnage `gross_tonnage`, declared `decimal(10,2)` NOT NULL.
 *   Comment: ITC 1969 gross tonnage
 * @property {string | null} netTonnage `net_tonnage`, declared `decimal(10,2)` NULL.
 *   Comment: Declared as NUMERIC to observe how the catalogue reports the synonym
 * @property {number} serviceSpeedKnots `service_speed_knots`, declared `float` NOT NULL.
 * @property {number | null} ballastCapacityM3 `ballast_capacity_m3`, declared `double` NULL.
 * @property {string} loaM `loa_m`, declared `decimal(6,2)` NOT NULL.
 *   Comment: Length overall, metres
 * @property {string} beamM `beam_m`, declared `decimal(5,2)` NOT NULL.
 * @property {string} designDraughtM `design_draught_m`, declared `decimal(5,2)` NOT NULL.
 * @property {number} yearBuilt `year_built`, declared `year(4)` NOT NULL.
 * @property {number} crewComplement `crew_complement`, declared `tinyint(3) unsigned` NOT NULL.
 * @property {string} lifetimeDistanceNm `lifetime_distance_nm`, declared `bigint(20) unsigned` NOT NULL.
 *   Comment: Cumulative logged distance, nautical miles
 * @property {number} inService `in_service`, declared `tinyint(1)` NOT NULL.
 *   Comment: Declared BOOLEAN; MariaDB stores TINYINT(1)
 * @property {Buffer} scrubberFitted `scrubber_fitted`, declared `bit(1)` NOT NULL.
 * @property {string | null} particularsSheet `particulars_sheet`, declared `mediumtext` NULL.
 *   Comment: Full ship particulars as supplied by the owner
 */

/**
 * The primary key of `freight.vessel`.
 *
 * @typedef {object} VesselKey
 * @property {string} imoNumber `imo_number`, declared `char(7)` NOT NULL.
 */

/**
 * One column of `freight.vessel`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link VesselColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const VesselColumns = Object.freeze(
  /** @type {const} */ ({
    /** `imo_number`, declared `char(7)` NOT NULL. */
    IMO_NUMBER: "imo_number",
    /** `name`, declared `varchar(100)` NOT NULL. */
    NAME: "name",
    /** `call_sign`, declared `varchar(10)` NOT NULL. */
    CALL_SIGN: "call_sign",
    /** `mmsi`, declared `int(10) unsigned` NULL. */
    MMSI: "mmsi",
    /** `flag_state`, declared `char(2)` NOT NULL. */
    FLAG_STATE: "flag_state",
    /** `class_society`, declared `enum('Lloyd''s Register','DNV','Bureau Veritas','ClassNK','American Bureau of Shipping','RINA')` NOT NULL. */
    CLASS_SOCIETY: "class_society",
    /** `hull_number`, declared `mediumint(9)` NULL. */
    HULL_NUMBER: "hull_number",
    /** `capacity_teu`, declared `mediumint(8) unsigned` NOT NULL. */
    CAPACITY_TEU: "capacity_teu",
    /** `reefer_plugs`, declared `smallint(5) unsigned` NOT NULL. */
    REEFER_PLUGS: "reefer_plugs",
    /** `deadweight_tonnes`, declared `int(10) unsigned` NOT NULL. */
    DEADWEIGHT_TONNES: "deadweight_tonnes",
    /** `gross_tonnage`, declared `decimal(10,2)` NOT NULL. */
    GROSS_TONNAGE: "gross_tonnage",
    /** `net_tonnage`, declared `decimal(10,2)` NULL. */
    NET_TONNAGE: "net_tonnage",
    /** `service_speed_knots`, declared `float` NOT NULL. */
    SERVICE_SPEED_KNOTS: "service_speed_knots",
    /** `ballast_capacity_m3`, declared `double` NULL. */
    BALLAST_CAPACITY_M3: "ballast_capacity_m3",
    /** `loa_m`, declared `decimal(6,2)` NOT NULL. */
    LOA_M: "loa_m",
    /** `beam_m`, declared `decimal(5,2)` NOT NULL. */
    BEAM_M: "beam_m",
    /** `design_draught_m`, declared `decimal(5,2)` NOT NULL. */
    DESIGN_DRAUGHT_M: "design_draught_m",
    /** `year_built`, declared `year(4)` NOT NULL. */
    YEAR_BUILT: "year_built",
    /** `crew_complement`, declared `tinyint(3) unsigned` NOT NULL. */
    CREW_COMPLEMENT: "crew_complement",
    /** `lifetime_distance_nm`, declared `bigint(20) unsigned` NOT NULL. */
    LIFETIME_DISTANCE_NM: "lifetime_distance_nm",
    /** `in_service`, declared `tinyint(1)` NOT NULL. */
    IN_SERVICE: "in_service",
    /** `scrubber_fitted`, declared `bit(1)` NOT NULL. */
    SCRUBBER_FITTED: "scrubber_fitted",
    /** `particulars_sheet`, declared `mediumtext` NULL. */
    PARTICULARS_SHEET: "particulars_sheet",
  }),
);

/**
 * A column of `freight.vessel` an ordering may name.
 *
 * @typedef {(typeof VesselColumns)[keyof typeof VesselColumns]} VesselColumn
 */

/**
 * The one place a column of `freight.vessel` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<VesselColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "imo_number": "`imo_number`",
  "name": "`name`",
  "call_sign": "`call_sign`",
  "mmsi": "`mmsi`",
  "flag_state": "`flag_state`",
  "class_society": "`class_society`",
  "hull_number": "`hull_number`",
  "capacity_teu": "`capacity_teu`",
  "reefer_plugs": "`reefer_plugs`",
  "deadweight_tonnes": "`deadweight_tonnes`",
  "gross_tonnage": "`gross_tonnage`",
  "net_tonnage": "`net_tonnage`",
  "service_speed_knots": "`service_speed_knots`",
  "ballast_capacity_m3": "`ballast_capacity_m3`",
  "loa_m": "`loa_m`",
  "beam_m": "`beam_m`",
  "design_draught_m": "`design_draught_m`",
  "year_built": "`year_built`",
  "crew_complement": "`crew_complement`",
  "lifetime_distance_nm": "`lifetime_distance_nm`",
  "in_service": "`in_service`",
  "scrubber_fitted": "`scrubber_fitted`",
  "particulars_sheet": "`particulars_sheet`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {VesselColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function vesselColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("freight.vessel", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `freight.vessel`.
 *
 * @typedef {object} VesselOrder
 * @property {VesselColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `freight.vessel`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} VesselFilter
 * @property {string} [imoNumber] where set, requires `imo_number` to equal it.
 * @property {string} [imoNumberLike] where set, requires `imo_number` to match it, as LIKE matches.
 * @property {string} [name] where set, requires `name` to equal it.
 * @property {string} [nameLike] where set, requires `name` to match it, as LIKE matches.
 * @property {string} [callSign] where set, requires `call_sign` to equal it.
 * @property {string} [callSignLike] where set, requires `call_sign` to match it, as LIKE matches.
 * @property {number} [mmsi] where set, requires `mmsi` to equal it.
 * @property {number} [mmsiFrom] where set, requires `mmsi` to be at least it.
 * @property {number} [mmsiTo] where set, requires `mmsi` to be at most it.
 * @property {boolean} [mmsiIsNull] where true, requires `mmsi` to be NULL; where false, requires it not to be.
 * @property {string} [flagState] where set, requires `flag_state` to equal it.
 * @property {string} [flagStateLike] where set, requires `flag_state` to match it, as LIKE matches.
 * @property {string} [classSociety] where set, requires `class_society` to equal it.
 * @property {string} [classSocietyLike] where set, requires `class_society` to match it, as LIKE matches.
 * @property {number} [hullNumber] where set, requires `hull_number` to equal it.
 * @property {number} [hullNumberFrom] where set, requires `hull_number` to be at least it.
 * @property {number} [hullNumberTo] where set, requires `hull_number` to be at most it.
 * @property {boolean} [hullNumberIsNull] where true, requires `hull_number` to be NULL; where false, requires it not to be.
 * @property {number} [capacityTeu] where set, requires `capacity_teu` to equal it.
 * @property {number} [capacityTeuFrom] where set, requires `capacity_teu` to be at least it.
 * @property {number} [capacityTeuTo] where set, requires `capacity_teu` to be at most it.
 * @property {number} [reeferPlugs] where set, requires `reefer_plugs` to equal it.
 * @property {number} [reeferPlugsFrom] where set, requires `reefer_plugs` to be at least it.
 * @property {number} [reeferPlugsTo] where set, requires `reefer_plugs` to be at most it.
 * @property {number} [deadweightTonnes] where set, requires `deadweight_tonnes` to equal it.
 * @property {number} [deadweightTonnesFrom] where set, requires `deadweight_tonnes` to be at least it.
 * @property {number} [deadweightTonnesTo] where set, requires `deadweight_tonnes` to be at most it.
 * @property {string} [grossTonnage] where set, requires `gross_tonnage` to equal it.
 * @property {string} [grossTonnageFrom] where set, requires `gross_tonnage` to be at least it.
 * @property {string} [grossTonnageTo] where set, requires `gross_tonnage` to be at most it.
 * @property {string} [netTonnage] where set, requires `net_tonnage` to equal it.
 * @property {string} [netTonnageFrom] where set, requires `net_tonnage` to be at least it.
 * @property {string} [netTonnageTo] where set, requires `net_tonnage` to be at most it.
 * @property {boolean} [netTonnageIsNull] where true, requires `net_tonnage` to be NULL; where false, requires it not to be.
 * @property {number} [serviceSpeedKnots] where set, requires `service_speed_knots` to equal it.
 * @property {number} [serviceSpeedKnotsFrom] where set, requires `service_speed_knots` to be at least it.
 * @property {number} [serviceSpeedKnotsTo] where set, requires `service_speed_knots` to be at most it.
 * @property {number} [ballastCapacityM3] where set, requires `ballast_capacity_m3` to equal it.
 * @property {number} [ballastCapacityM3From] where set, requires `ballast_capacity_m3` to be at least it.
 * @property {number} [ballastCapacityM3To] where set, requires `ballast_capacity_m3` to be at most it.
 * @property {boolean} [ballastCapacityM3IsNull] where true, requires `ballast_capacity_m3` to be NULL; where false, requires it not to be.
 * @property {string} [loaM] where set, requires `loa_m` to equal it.
 * @property {string} [loaMFrom] where set, requires `loa_m` to be at least it.
 * @property {string} [loaMTo] where set, requires `loa_m` to be at most it.
 * @property {string} [beamM] where set, requires `beam_m` to equal it.
 * @property {string} [beamMFrom] where set, requires `beam_m` to be at least it.
 * @property {string} [beamMTo] where set, requires `beam_m` to be at most it.
 * @property {string} [designDraughtM] where set, requires `design_draught_m` to equal it.
 * @property {string} [designDraughtMFrom] where set, requires `design_draught_m` to be at least it.
 * @property {string} [designDraughtMTo] where set, requires `design_draught_m` to be at most it.
 * @property {number} [yearBuilt] where set, requires `year_built` to equal it.
 * @property {number} [yearBuiltFrom] where set, requires `year_built` to be at least it.
 * @property {number} [yearBuiltTo] where set, requires `year_built` to be at most it.
 * @property {number} [crewComplement] where set, requires `crew_complement` to equal it.
 * @property {number} [crewComplementFrom] where set, requires `crew_complement` to be at least it.
 * @property {number} [crewComplementTo] where set, requires `crew_complement` to be at most it.
 * @property {string} [lifetimeDistanceNm] where set, requires `lifetime_distance_nm` to equal it.
 * @property {string} [lifetimeDistanceNmFrom] where set, requires `lifetime_distance_nm` to be at least it.
 * @property {string} [lifetimeDistanceNmTo] where set, requires `lifetime_distance_nm` to be at most it.
 * @property {number} [inService] where set, requires `in_service` to equal it.
 * @property {number} [inServiceFrom] where set, requires `in_service` to be at least it.
 * @property {number} [inServiceTo] where set, requires `in_service` to be at most it.
 * @property {Buffer} [scrubberFitted] where set, requires `scrubber_fitted` to equal it.
 * @property {Buffer} [scrubberFittedFrom] where set, requires `scrubber_fitted` to be at least it.
 * @property {Buffer} [scrubberFittedTo] where set, requires `scrubber_fitted` to be at most it.
 * @property {string} [particularsSheet] where set, requires `particulars_sheet` to equal it.
 * @property {string} [particularsSheetLike] where set, requires `particulars_sheet` to match it, as LIKE matches.
 * @property {boolean} [particularsSheetIsNull] where true, requires `particulars_sheet` to be NULL; where false, requires it not to be.
 */
