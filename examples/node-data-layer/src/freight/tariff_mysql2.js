// Code generated by tpl from freight.tariff. DO NOT EDIT.

/**
 * The mysql2 implementation of the `freight.tariff` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { Tariff, TariffFilter, TariffKey, TariffOrder } from './tariff.js' */

import { NotFound, directionSql, failure } from './core.js';
import { tariffColumnSql } from './tariff.js';
import { TariffRepository } from './tariff_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "freight.tariff";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `tariff_id`, `trade_lane`, `origin_region`, `destination_region`, `equipment_iso_code`, `basis`, `currency`, `base_rate`, `bunker_surcharge`, `congestion_surcharge`, `all_in_rate`, `valid_from`, `valid_until` FROM `tariff`";

/** Selects the one row a primary key names. */
const READ = "SELECT `tariff_id`, `trade_lane`, `origin_region`, `destination_region`, `equipment_iso_code`, `basis`, `currency`, `base_rate`, `bunker_surcharge`, `congestion_surcharge`, `all_in_rate`, `valid_from`, `valid_until` FROM `tariff` WHERE `tariff_id` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `tariff` (`trade_lane`, `origin_region`, `destination_region`, `equipment_iso_code`, `basis`, `currency`, `base_rate`, `bunker_surcharge`, `congestion_surcharge`, `valid_from`, `valid_until`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `tariff` SET `trade_lane` = ?, `origin_region` = ?, `destination_region` = ?, `equipment_iso_code` = ?, `basis` = ?, `currency` = ?, `base_rate` = ?, `bunker_surcharge` = ?, `congestion_surcharge` = ?, `valid_from` = ?, `valid_until` = ? WHERE `tariff_id` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `tariff` WHERE `tariff_id` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `Tariff` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {Tariff}
 */
function toRow(values) {
  return {
    tariffId: values[0],
    tradeLane: values[1],
    originRegion: values[2],
    destinationRegion: values[3],
    equipmentIsoCode: values[4],
    basis: values[5],
    currency: values[6],
    baseRate: values[7],
    bunkerSurcharge: values[8],
    congestionSurcharge: values[9],
    allInRate: values[10],
    validFrom: values[11],
    validUntil: values[12],
  };
}

/**
 * The mysql2 data layer of `freight.tariff`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class TariffMysql2 extends TariffRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `freight.tariff` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The key is assigned by the server and is written back into `row`.
   *
   * @override
   * @param {Tariff} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.tradeLane,
      row.originRegion,
      row.destinationRegion,
      row.equipmentIsoCode,
      row.basis,
      row.currency,
      row.baseRate,
      row.bunkerSurcharge,
      row.congestionSurcharge,
      row.validFrom,
      row.validUntil,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
      const { insertId } = /** @type {ResultSetHeader} */ (result);
      row.tariffId = insertId;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {TariffKey} key
   * @returns {Promise<Tariff>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.tariffId,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {Tariff} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.tradeLane,
      row.originRegion,
      row.destinationRegion,
      row.equipmentIsoCode,
      row.basis,
      row.currency,
      row.baseRate,
      row.bunkerSurcharge,
      row.congestionSurcharge,
      row.validFrom,
      row.validUntil,
      row.tariffId,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {TariffKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.tariffId,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {TariffFilter} criteria
   * @param {readonly TariffOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<Tariff[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.tariffId !== undefined) {
      conditions.push("`tariff_id` = ?");
      values.push(criteria.tariffId);
    }
    if (criteria.tariffIdFrom !== undefined) {
      conditions.push("`tariff_id` >= ?");
      values.push(criteria.tariffIdFrom);
    }
    if (criteria.tariffIdTo !== undefined) {
      conditions.push("`tariff_id` <= ?");
      values.push(criteria.tariffIdTo);
    }
    if (criteria.tradeLane !== undefined) {
      conditions.push("`trade_lane` = ?");
      values.push(criteria.tradeLane);
    }
    if (criteria.tradeLaneLike !== undefined) {
      conditions.push("`trade_lane` LIKE ?");
      values.push(criteria.tradeLaneLike);
    }
    if (criteria.originRegion !== undefined) {
      conditions.push("`origin_region` = ?");
      values.push(criteria.originRegion);
    }
    if (criteria.originRegionLike !== undefined) {
      conditions.push("`origin_region` LIKE ?");
      values.push(criteria.originRegionLike);
    }
    if (criteria.destinationRegion !== undefined) {
      conditions.push("`destination_region` = ?");
      values.push(criteria.destinationRegion);
    }
    if (criteria.destinationRegionLike !== undefined) {
      conditions.push("`destination_region` LIKE ?");
      values.push(criteria.destinationRegionLike);
    }
    if (criteria.equipmentIsoCode !== undefined) {
      conditions.push("`equipment_iso_code` = ?");
      values.push(criteria.equipmentIsoCode);
    }
    if (criteria.equipmentIsoCodeLike !== undefined) {
      conditions.push("`equipment_iso_code` LIKE ?");
      values.push(criteria.equipmentIsoCodeLike);
    }
    if (criteria.basis !== undefined) {
      conditions.push("`basis` = ?");
      values.push(criteria.basis);
    }
    if (criteria.basisLike !== undefined) {
      conditions.push("`basis` LIKE ?");
      values.push(criteria.basisLike);
    }
    if (criteria.currency !== undefined) {
      conditions.push("`currency` = ?");
      values.push(criteria.currency);
    }
    if (criteria.currencyLike !== undefined) {
      conditions.push("`currency` LIKE ?");
      values.push(criteria.currencyLike);
    }
    if (criteria.baseRate !== undefined) {
      conditions.push("`base_rate` = ?");
      values.push(criteria.baseRate);
    }
    if (criteria.baseRateFrom !== undefined) {
      conditions.push("`base_rate` >= ?");
      values.push(criteria.baseRateFrom);
    }
    if (criteria.baseRateTo !== undefined) {
      conditions.push("`base_rate` <= ?");
      values.push(criteria.baseRateTo);
    }
    if (criteria.bunkerSurcharge !== undefined) {
      conditions.push("`bunker_surcharge` = ?");
      values.push(criteria.bunkerSurcharge);
    }
    if (criteria.bunkerSurchargeFrom !== undefined) {
      conditions.push("`bunker_surcharge` >= ?");
      values.push(criteria.bunkerSurchargeFrom);
    }
    if (criteria.bunkerSurchargeTo !== undefined) {
      conditions.push("`bunker_surcharge` <= ?");
      values.push(criteria.bunkerSurchargeTo);
    }
    if (criteria.congestionSurcharge !== undefined) {
      conditions.push("`congestion_surcharge` = ?");
      values.push(criteria.congestionSurcharge);
    }
    if (criteria.congestionSurchargeFrom !== undefined) {
      conditions.push("`congestion_surcharge` >= ?");
      values.push(criteria.congestionSurchargeFrom);
    }
    if (criteria.congestionSurchargeTo !== undefined) {
      conditions.push("`congestion_surcharge` <= ?");
      values.push(criteria.congestionSurchargeTo);
    }
    if (criteria.allInRate !== undefined) {
      conditions.push("`all_in_rate` = ?");
      values.push(criteria.allInRate);
    }
    if (criteria.allInRateFrom !== undefined) {
      conditions.push("`all_in_rate` >= ?");
      values.push(criteria.allInRateFrom);
    }
    if (criteria.allInRateTo !== undefined) {
      conditions.push("`all_in_rate` <= ?");
      values.push(criteria.allInRateTo);
    }
    if (criteria.allInRateIsNull !== undefined) {
      conditions.push(
        criteria.allInRateIsNull
          ? "`all_in_rate` IS NULL"
          : "`all_in_rate` IS NOT NULL",
      );
    }
    if (criteria.validFrom !== undefined) {
      conditions.push("`valid_from` = ?");
      values.push(criteria.validFrom);
    }
    if (criteria.validFromFrom !== undefined) {
      conditions.push("`valid_from` >= ?");
      values.push(criteria.validFromFrom);
    }
    if (criteria.validFromTo !== undefined) {
      conditions.push("`valid_from` <= ?");
      values.push(criteria.validFromTo);
    }
    if (criteria.validUntil !== undefined) {
      conditions.push("`valid_until` = ?");
      values.push(criteria.validUntil);
    }
    if (criteria.validUntilFrom !== undefined) {
      conditions.push("`valid_until` >= ?");
      values.push(criteria.validUntilFrom);
    }
    if (criteria.validUntilTo !== undefined) {
      conditions.push("`valid_until` <= ?");
      values.push(criteria.validUntilTo);
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              tariffColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
