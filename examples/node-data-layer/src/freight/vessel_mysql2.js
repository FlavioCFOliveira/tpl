// Code generated by tpl from freight.vessel. DO NOT EDIT.

/**
 * The mysql2 implementation of the `freight.vessel` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { Vessel, VesselFilter, VesselKey, VesselOrder } from './vessel.js' */

import { NotFound, directionSql, failure } from './core.js';
import { vesselColumnSql } from './vessel.js';
import { VesselRepository } from './vessel_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "freight.vessel";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `imo_number`, `name`, `call_sign`, `mmsi`, `flag_state`, `class_society`, `hull_number`, `capacity_teu`, `reefer_plugs`, `deadweight_tonnes`, `gross_tonnage`, `net_tonnage`, `service_speed_knots`, `ballast_capacity_m3`, `loa_m`, `beam_m`, `design_draught_m`, `year_built`, `crew_complement`, `lifetime_distance_nm`, `in_service`, `scrubber_fitted`, `particulars_sheet` FROM `vessel`";

/** Selects the one row a primary key names. */
const READ = "SELECT `imo_number`, `name`, `call_sign`, `mmsi`, `flag_state`, `class_society`, `hull_number`, `capacity_teu`, `reefer_plugs`, `deadweight_tonnes`, `gross_tonnage`, `net_tonnage`, `service_speed_knots`, `ballast_capacity_m3`, `loa_m`, `beam_m`, `design_draught_m`, `year_built`, `crew_complement`, `lifetime_distance_nm`, `in_service`, `scrubber_fitted`, `particulars_sheet` FROM `vessel` WHERE `imo_number` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `vessel` (`imo_number`, `name`, `call_sign`, `mmsi`, `flag_state`, `class_society`, `hull_number`, `capacity_teu`, `reefer_plugs`, `deadweight_tonnes`, `gross_tonnage`, `net_tonnage`, `service_speed_knots`, `ballast_capacity_m3`, `loa_m`, `beam_m`, `design_draught_m`, `year_built`, `crew_complement`, `lifetime_distance_nm`, `in_service`, `scrubber_fitted`, `particulars_sheet`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `vessel` SET `name` = ?, `call_sign` = ?, `mmsi` = ?, `flag_state` = ?, `class_society` = ?, `hull_number` = ?, `capacity_teu` = ?, `reefer_plugs` = ?, `deadweight_tonnes` = ?, `gross_tonnage` = ?, `net_tonnage` = ?, `service_speed_knots` = ?, `ballast_capacity_m3` = ?, `loa_m` = ?, `beam_m` = ?, `design_draught_m` = ?, `year_built` = ?, `crew_complement` = ?, `lifetime_distance_nm` = ?, `in_service` = ?, `scrubber_fitted` = ?, `particulars_sheet` = ? WHERE `imo_number` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `vessel` WHERE `imo_number` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `Vessel` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {Vessel}
 */
function toRow(values) {
  return {
    imoNumber: values[0],
    name: values[1],
    callSign: values[2],
    mmsi: values[3],
    flagState: values[4],
    classSociety: values[5],
    hullNumber: values[6],
    capacityTeu: values[7],
    reeferPlugs: values[8],
    deadweightTonnes: values[9],
    grossTonnage: values[10],
    netTonnage: values[11],
    serviceSpeedKnots: values[12],
    ballastCapacityM3: values[13],
    loaM: values[14],
    beamM: values[15],
    designDraughtM: values[16],
    yearBuilt: values[17],
    crewComplement: values[18],
    lifetimeDistanceNm: values[19],
    inService: values[20],
    scrubberFitted: values[21],
    particularsSheet: values[22],
  };
}

/**
 * The mysql2 data layer of `freight.vessel`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class VesselMysql2 extends VesselRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `freight.vessel` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The whole key is taken from `row`, which the caller therefore fills.
   *
   * @override
   * @param {Vessel} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.imoNumber,
      row.name,
      row.callSign,
      row.mmsi,
      row.flagState,
      row.classSociety,
      row.hullNumber,
      row.capacityTeu,
      row.reeferPlugs,
      row.deadweightTonnes,
      row.grossTonnage,
      row.netTonnage,
      row.serviceSpeedKnots,
      row.ballastCapacityM3,
      row.loaM,
      row.beamM,
      row.designDraughtM,
      row.yearBuilt,
      row.crewComplement,
      row.lifetimeDistanceNm,
      row.inService,
      row.scrubberFitted,
      row.particularsSheet,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {VesselKey} key
   * @returns {Promise<Vessel>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.imoNumber,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {Vessel} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.name,
      row.callSign,
      row.mmsi,
      row.flagState,
      row.classSociety,
      row.hullNumber,
      row.capacityTeu,
      row.reeferPlugs,
      row.deadweightTonnes,
      row.grossTonnage,
      row.netTonnage,
      row.serviceSpeedKnots,
      row.ballastCapacityM3,
      row.loaM,
      row.beamM,
      row.designDraughtM,
      row.yearBuilt,
      row.crewComplement,
      row.lifetimeDistanceNm,
      row.inService,
      row.scrubberFitted,
      row.particularsSheet,
      row.imoNumber,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {VesselKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.imoNumber,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {VesselFilter} criteria
   * @param {readonly VesselOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<Vessel[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.imoNumber !== undefined) {
      conditions.push("`imo_number` = ?");
      values.push(criteria.imoNumber);
    }
    if (criteria.imoNumberLike !== undefined) {
      conditions.push("`imo_number` LIKE ?");
      values.push(criteria.imoNumberLike);
    }
    if (criteria.name !== undefined) {
      conditions.push("`name` = ?");
      values.push(criteria.name);
    }
    if (criteria.nameLike !== undefined) {
      conditions.push("`name` LIKE ?");
      values.push(criteria.nameLike);
    }
    if (criteria.callSign !== undefined) {
      conditions.push("`call_sign` = ?");
      values.push(criteria.callSign);
    }
    if (criteria.callSignLike !== undefined) {
      conditions.push("`call_sign` LIKE ?");
      values.push(criteria.callSignLike);
    }
    if (criteria.mmsi !== undefined) {
      conditions.push("`mmsi` = ?");
      values.push(criteria.mmsi);
    }
    if (criteria.mmsiFrom !== undefined) {
      conditions.push("`mmsi` >= ?");
      values.push(criteria.mmsiFrom);
    }
    if (criteria.mmsiTo !== undefined) {
      conditions.push("`mmsi` <= ?");
      values.push(criteria.mmsiTo);
    }
    if (criteria.mmsiIsNull !== undefined) {
      conditions.push(
        criteria.mmsiIsNull
          ? "`mmsi` IS NULL"
          : "`mmsi` IS NOT NULL",
      );
    }
    if (criteria.flagState !== undefined) {
      conditions.push("`flag_state` = ?");
      values.push(criteria.flagState);
    }
    if (criteria.flagStateLike !== undefined) {
      conditions.push("`flag_state` LIKE ?");
      values.push(criteria.flagStateLike);
    }
    if (criteria.classSociety !== undefined) {
      conditions.push("`class_society` = ?");
      values.push(criteria.classSociety);
    }
    if (criteria.classSocietyLike !== undefined) {
      conditions.push("`class_society` LIKE ?");
      values.push(criteria.classSocietyLike);
    }
    if (criteria.hullNumber !== undefined) {
      conditions.push("`hull_number` = ?");
      values.push(criteria.hullNumber);
    }
    if (criteria.hullNumberFrom !== undefined) {
      conditions.push("`hull_number` >= ?");
      values.push(criteria.hullNumberFrom);
    }
    if (criteria.hullNumberTo !== undefined) {
      conditions.push("`hull_number` <= ?");
      values.push(criteria.hullNumberTo);
    }
    if (criteria.hullNumberIsNull !== undefined) {
      conditions.push(
        criteria.hullNumberIsNull
          ? "`hull_number` IS NULL"
          : "`hull_number` IS NOT NULL",
      );
    }
    if (criteria.capacityTeu !== undefined) {
      conditions.push("`capacity_teu` = ?");
      values.push(criteria.capacityTeu);
    }
    if (criteria.capacityTeuFrom !== undefined) {
      conditions.push("`capacity_teu` >= ?");
      values.push(criteria.capacityTeuFrom);
    }
    if (criteria.capacityTeuTo !== undefined) {
      conditions.push("`capacity_teu` <= ?");
      values.push(criteria.capacityTeuTo);
    }
    if (criteria.reeferPlugs !== undefined) {
      conditions.push("`reefer_plugs` = ?");
      values.push(criteria.reeferPlugs);
    }
    if (criteria.reeferPlugsFrom !== undefined) {
      conditions.push("`reefer_plugs` >= ?");
      values.push(criteria.reeferPlugsFrom);
    }
    if (criteria.reeferPlugsTo !== undefined) {
      conditions.push("`reefer_plugs` <= ?");
      values.push(criteria.reeferPlugsTo);
    }
    if (criteria.deadweightTonnes !== undefined) {
      conditions.push("`deadweight_tonnes` = ?");
      values.push(criteria.deadweightTonnes);
    }
    if (criteria.deadweightTonnesFrom !== undefined) {
      conditions.push("`deadweight_tonnes` >= ?");
      values.push(criteria.deadweightTonnesFrom);
    }
    if (criteria.deadweightTonnesTo !== undefined) {
      conditions.push("`deadweight_tonnes` <= ?");
      values.push(criteria.deadweightTonnesTo);
    }
    if (criteria.grossTonnage !== undefined) {
      conditions.push("`gross_tonnage` = ?");
      values.push(criteria.grossTonnage);
    }
    if (criteria.grossTonnageFrom !== undefined) {
      conditions.push("`gross_tonnage` >= ?");
      values.push(criteria.grossTonnageFrom);
    }
    if (criteria.grossTonnageTo !== undefined) {
      conditions.push("`gross_tonnage` <= ?");
      values.push(criteria.grossTonnageTo);
    }
    if (criteria.netTonnage !== undefined) {
      conditions.push("`net_tonnage` = ?");
      values.push(criteria.netTonnage);
    }
    if (criteria.netTonnageFrom !== undefined) {
      conditions.push("`net_tonnage` >= ?");
      values.push(criteria.netTonnageFrom);
    }
    if (criteria.netTonnageTo !== undefined) {
      conditions.push("`net_tonnage` <= ?");
      values.push(criteria.netTonnageTo);
    }
    if (criteria.netTonnageIsNull !== undefined) {
      conditions.push(
        criteria.netTonnageIsNull
          ? "`net_tonnage` IS NULL"
          : "`net_tonnage` IS NOT NULL",
      );
    }
    if (criteria.serviceSpeedKnots !== undefined) {
      conditions.push("`service_speed_knots` = ?");
      values.push(criteria.serviceSpeedKnots);
    }
    if (criteria.serviceSpeedKnotsFrom !== undefined) {
      conditions.push("`service_speed_knots` >= ?");
      values.push(criteria.serviceSpeedKnotsFrom);
    }
    if (criteria.serviceSpeedKnotsTo !== undefined) {
      conditions.push("`service_speed_knots` <= ?");
      values.push(criteria.serviceSpeedKnotsTo);
    }
    if (criteria.ballastCapacityM3 !== undefined) {
      conditions.push("`ballast_capacity_m3` = ?");
      values.push(criteria.ballastCapacityM3);
    }
    if (criteria.ballastCapacityM3From !== undefined) {
      conditions.push("`ballast_capacity_m3` >= ?");
      values.push(criteria.ballastCapacityM3From);
    }
    if (criteria.ballastCapacityM3To !== undefined) {
      conditions.push("`ballast_capacity_m3` <= ?");
      values.push(criteria.ballastCapacityM3To);
    }
    if (criteria.ballastCapacityM3IsNull !== undefined) {
      conditions.push(
        criteria.ballastCapacityM3IsNull
          ? "`ballast_capacity_m3` IS NULL"
          : "`ballast_capacity_m3` IS NOT NULL",
      );
    }
    if (criteria.loaM !== undefined) {
      conditions.push("`loa_m` = ?");
      values.push(criteria.loaM);
    }
    if (criteria.loaMFrom !== undefined) {
      conditions.push("`loa_m` >= ?");
      values.push(criteria.loaMFrom);
    }
    if (criteria.loaMTo !== undefined) {
      conditions.push("`loa_m` <= ?");
      values.push(criteria.loaMTo);
    }
    if (criteria.beamM !== undefined) {
      conditions.push("`beam_m` = ?");
      values.push(criteria.beamM);
    }
    if (criteria.beamMFrom !== undefined) {
      conditions.push("`beam_m` >= ?");
      values.push(criteria.beamMFrom);
    }
    if (criteria.beamMTo !== undefined) {
      conditions.push("`beam_m` <= ?");
      values.push(criteria.beamMTo);
    }
    if (criteria.designDraughtM !== undefined) {
      conditions.push("`design_draught_m` = ?");
      values.push(criteria.designDraughtM);
    }
    if (criteria.designDraughtMFrom !== undefined) {
      conditions.push("`design_draught_m` >= ?");
      values.push(criteria.designDraughtMFrom);
    }
    if (criteria.designDraughtMTo !== undefined) {
      conditions.push("`design_draught_m` <= ?");
      values.push(criteria.designDraughtMTo);
    }
    if (criteria.yearBuilt !== undefined) {
      conditions.push("`year_built` = ?");
      values.push(criteria.yearBuilt);
    }
    if (criteria.yearBuiltFrom !== undefined) {
      conditions.push("`year_built` >= ?");
      values.push(criteria.yearBuiltFrom);
    }
    if (criteria.yearBuiltTo !== undefined) {
      conditions.push("`year_built` <= ?");
      values.push(criteria.yearBuiltTo);
    }
    if (criteria.crewComplement !== undefined) {
      conditions.push("`crew_complement` = ?");
      values.push(criteria.crewComplement);
    }
    if (criteria.crewComplementFrom !== undefined) {
      conditions.push("`crew_complement` >= ?");
      values.push(criteria.crewComplementFrom);
    }
    if (criteria.crewComplementTo !== undefined) {
      conditions.push("`crew_complement` <= ?");
      values.push(criteria.crewComplementTo);
    }
    if (criteria.lifetimeDistanceNm !== undefined) {
      conditions.push("`lifetime_distance_nm` = ?");
      values.push(criteria.lifetimeDistanceNm);
    }
    if (criteria.lifetimeDistanceNmFrom !== undefined) {
      conditions.push("`lifetime_distance_nm` >= ?");
      values.push(criteria.lifetimeDistanceNmFrom);
    }
    if (criteria.lifetimeDistanceNmTo !== undefined) {
      conditions.push("`lifetime_distance_nm` <= ?");
      values.push(criteria.lifetimeDistanceNmTo);
    }
    if (criteria.inService !== undefined) {
      conditions.push("`in_service` = ?");
      values.push(criteria.inService);
    }
    if (criteria.inServiceFrom !== undefined) {
      conditions.push("`in_service` >= ?");
      values.push(criteria.inServiceFrom);
    }
    if (criteria.inServiceTo !== undefined) {
      conditions.push("`in_service` <= ?");
      values.push(criteria.inServiceTo);
    }
    if (criteria.scrubberFitted !== undefined) {
      conditions.push("`scrubber_fitted` = ?");
      values.push(criteria.scrubberFitted);
    }
    if (criteria.scrubberFittedFrom !== undefined) {
      conditions.push("`scrubber_fitted` >= ?");
      values.push(criteria.scrubberFittedFrom);
    }
    if (criteria.scrubberFittedTo !== undefined) {
      conditions.push("`scrubber_fitted` <= ?");
      values.push(criteria.scrubberFittedTo);
    }
    if (criteria.particularsSheet !== undefined) {
      conditions.push("`particulars_sheet` = ?");
      values.push(criteria.particularsSheet);
    }
    if (criteria.particularsSheetLike !== undefined) {
      conditions.push("`particulars_sheet` LIKE ?");
      values.push(criteria.particularsSheetLike);
    }
    if (criteria.particularsSheetIsNull !== undefined) {
      conditions.push(
        criteria.particularsSheetIsNull
          ? "`particulars_sheet` IS NULL"
          : "`particulars_sheet` IS NOT NULL",
      );
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              vesselColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
