// Code generated by tpl from freight.cargo_item. DO NOT EDIT.

/**
 * The `freight.cargo_item` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

// The table's own comment, as the catalogue carries it:
// Linhas de mercadoria de cada expedição

/**
 * One row of `freight.cargo_item`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} CargoItem
 * @property {string} cargoItemId `cargo_item_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
 *   It is part of the primary key.
 * @property {string} consignmentId `consignment_id`, declared `bigint(20) unsigned` NOT NULL.
 *   It is part of a unique index.
 * @property {string | null} containerId `container_id`, declared `bigint(20) unsigned` NULL.
 *   Comment: NULL while the item is loose cargo awaiting consolidation
 * @property {number} lineNumber `line_number`, declared `smallint(5) unsigned` NOT NULL.
 *   It is part of a unique index.
 * @property {string} hsCode `hs_code`, declared `varchar(12)` NOT NULL.
 *   Comment: Harmonised System commodity code
 * @property {string} description `description`, declared `text` NOT NULL.
 *   Comment: Commercial description; indexed FULLTEXT for the tariff classification desk
 * @property {string} packageKind `package_kind`, declared `enum('Pallet','Crate','Drum','Bale','Carton','Big bag','Roll','Loose')` NOT NULL.
 * @property {number} packageCount `package_count`, declared `mediumint(8) unsigned` NOT NULL.
 * @property {string} grossWeightKg `gross_weight_kg`, declared `decimal(12,3)` NOT NULL.
 * @property {string} netWeightKg `net_weight_kg`, declared `decimal(12,3)` NOT NULL.
 * @property {string} volumeCbm `volume_cbm`, declared `decimal(10,3)` NOT NULL.
 * @property {string | null} chargeableWeightKg `chargeable_weight_kg`, declared `decimal(12,3)` NULL VIRTUAL GENERATED AS (greatest(`gross_weight_kg`,`volume_cbm` * 167)).
 *   Comment: Virtual generated column: IATA-style volumetric rule at 167 kg per cubic metre
 * @property {string} imdgClass `imdg_class`, declared `enum('Not regulated','Class 3, Flammable liquids','Class 8, Corrosives','Class 9, Miscellaneous dangerous goods')` NOT NULL.
 *   Comment: Note the commas inside three of the members
 * @property {number | null} unNumber `un_number`, declared `smallint(5) unsigned` NULL.
 *   Comment: UN dangerous goods number, 1 to 3550
 * @property {string} originCountry `origin_country`, declared `char(2)` NOT NULL.
 * @property {string} unitValue `unit_value`, declared `decimal(12,2)` NOT NULL.
 * @property {string | null} lineValue `line_value`, declared `decimal(14,2)` NULL STORED GENERATED AS (round(`unit_value` * `package_count`,2)).
 *   Comment: Stored generated column: value declared for this line
 */

/**
 * The primary key of `freight.cargo_item`.
 *
 * @typedef {object} CargoItemKey
 * @property {string} cargoItemId `cargo_item_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
 */

/**
 * One column of `freight.cargo_item`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link CargoItemColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const CargoItemColumns = Object.freeze(
  /** @type {const} */ ({
    /** `cargo_item_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT. */
    CARGO_ITEM_ID: "cargo_item_id",
    /** `consignment_id`, declared `bigint(20) unsigned` NOT NULL. */
    CONSIGNMENT_ID: "consignment_id",
    /** `container_id`, declared `bigint(20) unsigned` NULL. */
    CONTAINER_ID: "container_id",
    /** `line_number`, declared `smallint(5) unsigned` NOT NULL. */
    LINE_NUMBER: "line_number",
    /** `hs_code`, declared `varchar(12)` NOT NULL. */
    HS_CODE: "hs_code",
    /** `description`, declared `text` NOT NULL. */
    DESCRIPTION: "description",
    /** `package_kind`, declared `enum('Pallet','Crate','Drum','Bale','Carton','Big bag','Roll','Loose')` NOT NULL. */
    PACKAGE_KIND: "package_kind",
    /** `package_count`, declared `mediumint(8) unsigned` NOT NULL. */
    PACKAGE_COUNT: "package_count",
    /** `gross_weight_kg`, declared `decimal(12,3)` NOT NULL. */
    GROSS_WEIGHT_KG: "gross_weight_kg",
    /** `net_weight_kg`, declared `decimal(12,3)` NOT NULL. */
    NET_WEIGHT_KG: "net_weight_kg",
    /** `volume_cbm`, declared `decimal(10,3)` NOT NULL. */
    VOLUME_CBM: "volume_cbm",
    /** `chargeable_weight_kg`, declared `decimal(12,3)` NULL VIRTUAL GENERATED AS (greatest(`gross_weight_kg`,`volume_cbm` * 167)). */
    CHARGEABLE_WEIGHT_KG: "chargeable_weight_kg",
    /** `imdg_class`, declared `enum('Not regulated','Class 3, Flammable liquids','Class 8, Corrosives','Class 9, Miscellaneous dangerous goods')` NOT NULL. */
    IMDG_CLASS: "imdg_class",
    /** `un_number`, declared `smallint(5) unsigned` NULL. */
    UN_NUMBER: "un_number",
    /** `origin_country`, declared `char(2)` NOT NULL. */
    ORIGIN_COUNTRY: "origin_country",
    /** `unit_value`, declared `decimal(12,2)` NOT NULL. */
    UNIT_VALUE: "unit_value",
    /** `line_value`, declared `decimal(14,2)` NULL STORED GENERATED AS (round(`unit_value` * `package_count`,2)). */
    LINE_VALUE: "line_value",
  }),
);

/**
 * A column of `freight.cargo_item` an ordering may name.
 *
 * @typedef {(typeof CargoItemColumns)[keyof typeof CargoItemColumns]} CargoItemColumn
 */

/**
 * The one place a column of `freight.cargo_item` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<CargoItemColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "cargo_item_id": "`cargo_item_id`",
  "consignment_id": "`consignment_id`",
  "container_id": "`container_id`",
  "line_number": "`line_number`",
  "hs_code": "`hs_code`",
  "description": "`description`",
  "package_kind": "`package_kind`",
  "package_count": "`package_count`",
  "gross_weight_kg": "`gross_weight_kg`",
  "net_weight_kg": "`net_weight_kg`",
  "volume_cbm": "`volume_cbm`",
  "chargeable_weight_kg": "`chargeable_weight_kg`",
  "imdg_class": "`imdg_class`",
  "un_number": "`un_number`",
  "origin_country": "`origin_country`",
  "unit_value": "`unit_value`",
  "line_value": "`line_value`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {CargoItemColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function cargoItemColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("freight.cargo_item", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `freight.cargo_item`.
 *
 * @typedef {object} CargoItemOrder
 * @property {CargoItemColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `freight.cargo_item`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} CargoItemFilter
 * @property {string} [cargoItemId] where set, requires `cargo_item_id` to equal it.
 * @property {string} [cargoItemIdFrom] where set, requires `cargo_item_id` to be at least it.
 * @property {string} [cargoItemIdTo] where set, requires `cargo_item_id` to be at most it.
 * @property {string} [consignmentId] where set, requires `consignment_id` to equal it.
 * @property {string} [consignmentIdFrom] where set, requires `consignment_id` to be at least it.
 * @property {string} [consignmentIdTo] where set, requires `consignment_id` to be at most it.
 * @property {string} [containerId] where set, requires `container_id` to equal it.
 * @property {string} [containerIdFrom] where set, requires `container_id` to be at least it.
 * @property {string} [containerIdTo] where set, requires `container_id` to be at most it.
 * @property {boolean} [containerIdIsNull] where true, requires `container_id` to be NULL; where false, requires it not to be.
 * @property {number} [lineNumber] where set, requires `line_number` to equal it.
 * @property {number} [lineNumberFrom] where set, requires `line_number` to be at least it.
 * @property {number} [lineNumberTo] where set, requires `line_number` to be at most it.
 * @property {string} [hsCode] where set, requires `hs_code` to equal it.
 * @property {string} [hsCodeLike] where set, requires `hs_code` to match it, as LIKE matches.
 * @property {string} [description] where set, requires `description` to equal it.
 * @property {string} [descriptionLike] where set, requires `description` to match it, as LIKE matches.
 * @property {string} [packageKind] where set, requires `package_kind` to equal it.
 * @property {string} [packageKindLike] where set, requires `package_kind` to match it, as LIKE matches.
 * @property {number} [packageCount] where set, requires `package_count` to equal it.
 * @property {number} [packageCountFrom] where set, requires `package_count` to be at least it.
 * @property {number} [packageCountTo] where set, requires `package_count` to be at most it.
 * @property {string} [grossWeightKg] where set, requires `gross_weight_kg` to equal it.
 * @property {string} [grossWeightKgFrom] where set, requires `gross_weight_kg` to be at least it.
 * @property {string} [grossWeightKgTo] where set, requires `gross_weight_kg` to be at most it.
 * @property {string} [netWeightKg] where set, requires `net_weight_kg` to equal it.
 * @property {string} [netWeightKgFrom] where set, requires `net_weight_kg` to be at least it.
 * @property {string} [netWeightKgTo] where set, requires `net_weight_kg` to be at most it.
 * @property {string} [volumeCbm] where set, requires `volume_cbm` to equal it.
 * @property {string} [volumeCbmFrom] where set, requires `volume_cbm` to be at least it.
 * @property {string} [volumeCbmTo] where set, requires `volume_cbm` to be at most it.
 * @property {string} [chargeableWeightKg] where set, requires `chargeable_weight_kg` to equal it.
 * @property {string} [chargeableWeightKgFrom] where set, requires `chargeable_weight_kg` to be at least it.
 * @property {string} [chargeableWeightKgTo] where set, requires `chargeable_weight_kg` to be at most it.
 * @property {boolean} [chargeableWeightKgIsNull] where true, requires `chargeable_weight_kg` to be NULL; where false, requires it not to be.
 * @property {string} [imdgClass] where set, requires `imdg_class` to equal it.
 * @property {string} [imdgClassLike] where set, requires `imdg_class` to match it, as LIKE matches.
 * @property {number} [unNumber] where set, requires `un_number` to equal it.
 * @property {number} [unNumberFrom] where set, requires `un_number` to be at least it.
 * @property {number} [unNumberTo] where set, requires `un_number` to be at most it.
 * @property {boolean} [unNumberIsNull] where true, requires `un_number` to be NULL; where false, requires it not to be.
 * @property {string} [originCountry] where set, requires `origin_country` to equal it.
 * @property {string} [originCountryLike] where set, requires `origin_country` to match it, as LIKE matches.
 * @property {string} [unitValue] where set, requires `unit_value` to equal it.
 * @property {string} [unitValueFrom] where set, requires `unit_value` to be at least it.
 * @property {string} [unitValueTo] where set, requires `unit_value` to be at most it.
 * @property {string} [lineValue] where set, requires `line_value` to equal it.
 * @property {string} [lineValueFrom] where set, requires `line_value` to be at least it.
 * @property {string} [lineValueTo] where set, requires `line_value` to be at most it.
 * @property {boolean} [lineValueIsNull] where true, requires `line_value` to be NULL; where false, requires it not to be.
 */
