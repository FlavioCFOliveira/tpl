// Code generated by tpl from freight.voyage_leg. DO NOT EDIT.

/**
 * The mysql2 implementation of the `freight.voyage_leg` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { VoyageLeg, VoyageLegFilter, VoyageLegKey, VoyageLegOrder } from './voyage_leg.js' */

import { NotFound, directionSql, failure } from './core.js';
import { voyageLegColumnSql } from './voyage_leg.js';
import { VoyageLegRepository } from './voyage_leg_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "freight.voyage_leg";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `vessel_imo`, `voyage_number`, `leg_sequence`, `port_id`, `eta`, `etd`, `ata`, `berth_shift_time`, `pilot_required`, `moves_discharged`, `moves_loaded`, `moves_total` FROM `voyage_leg`";

/** Selects the one row a primary key names. */
const READ = "SELECT `vessel_imo`, `voyage_number`, `leg_sequence`, `port_id`, `eta`, `etd`, `ata`, `berth_shift_time`, `pilot_required`, `moves_discharged`, `moves_loaded`, `moves_total` FROM `voyage_leg` WHERE `vessel_imo` = ? AND `voyage_number` = ? AND `leg_sequence` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `voyage_leg` (`vessel_imo`, `voyage_number`, `leg_sequence`, `port_id`, `eta`, `etd`, `ata`, `berth_shift_time`, `pilot_required`, `moves_discharged`, `moves_loaded`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `voyage_leg` SET `port_id` = ?, `eta` = ?, `etd` = ?, `ata` = ?, `berth_shift_time` = ?, `pilot_required` = ?, `moves_discharged` = ?, `moves_loaded` = ? WHERE `vessel_imo` = ? AND `voyage_number` = ? AND `leg_sequence` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `voyage_leg` WHERE `vessel_imo` = ? AND `voyage_number` = ? AND `leg_sequence` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `VoyageLeg` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {VoyageLeg}
 */
function toRow(values) {
  return {
    vesselImo: values[0],
    voyageNumber: values[1],
    legSequence: values[2],
    portId: values[3],
    eta: values[4],
    etd: values[5],
    ata: values[6],
    berthShiftTime: values[7],
    pilotRequired: values[8],
    movesDischarged: values[9],
    movesLoaded: values[10],
    movesTotal: values[11],
  };
}

/**
 * The mysql2 data layer of `freight.voyage_leg`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class VoyageLegMysql2 extends VoyageLegRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `freight.voyage_leg` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The whole key is taken from `row`, which the caller therefore fills.
   *
   * @override
   * @param {VoyageLeg} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.vesselImo,
      row.voyageNumber,
      row.legSequence,
      row.portId,
      row.eta,
      row.etd,
      row.ata,
      row.berthShiftTime,
      row.pilotRequired,
      row.movesDischarged,
      row.movesLoaded,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {VoyageLegKey} key
   * @returns {Promise<VoyageLeg>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.vesselImo,
      key.voyageNumber,
      key.legSequence,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {VoyageLeg} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.portId,
      row.eta,
      row.etd,
      row.ata,
      row.berthShiftTime,
      row.pilotRequired,
      row.movesDischarged,
      row.movesLoaded,
      row.vesselImo,
      row.voyageNumber,
      row.legSequence,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {VoyageLegKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.vesselImo,
      key.voyageNumber,
      key.legSequence,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {VoyageLegFilter} criteria
   * @param {readonly VoyageLegOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<VoyageLeg[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.vesselImo !== undefined) {
      conditions.push("`vessel_imo` = ?");
      values.push(criteria.vesselImo);
    }
    if (criteria.vesselImoLike !== undefined) {
      conditions.push("`vessel_imo` LIKE ?");
      values.push(criteria.vesselImoLike);
    }
    if (criteria.voyageNumber !== undefined) {
      conditions.push("`voyage_number` = ?");
      values.push(criteria.voyageNumber);
    }
    if (criteria.voyageNumberLike !== undefined) {
      conditions.push("`voyage_number` LIKE ?");
      values.push(criteria.voyageNumberLike);
    }
    if (criteria.legSequence !== undefined) {
      conditions.push("`leg_sequence` = ?");
      values.push(criteria.legSequence);
    }
    if (criteria.legSequenceFrom !== undefined) {
      conditions.push("`leg_sequence` >= ?");
      values.push(criteria.legSequenceFrom);
    }
    if (criteria.legSequenceTo !== undefined) {
      conditions.push("`leg_sequence` <= ?");
      values.push(criteria.legSequenceTo);
    }
    if (criteria.portId !== undefined) {
      conditions.push("`port_id` = ?");
      values.push(criteria.portId);
    }
    if (criteria.portIdFrom !== undefined) {
      conditions.push("`port_id` >= ?");
      values.push(criteria.portIdFrom);
    }
    if (criteria.portIdTo !== undefined) {
      conditions.push("`port_id` <= ?");
      values.push(criteria.portIdTo);
    }
    if (criteria.eta !== undefined) {
      conditions.push("`eta` = ?");
      values.push(criteria.eta);
    }
    if (criteria.etaFrom !== undefined) {
      conditions.push("`eta` >= ?");
      values.push(criteria.etaFrom);
    }
    if (criteria.etaTo !== undefined) {
      conditions.push("`eta` <= ?");
      values.push(criteria.etaTo);
    }
    if (criteria.etd !== undefined) {
      conditions.push("`etd` = ?");
      values.push(criteria.etd);
    }
    if (criteria.etdFrom !== undefined) {
      conditions.push("`etd` >= ?");
      values.push(criteria.etdFrom);
    }
    if (criteria.etdTo !== undefined) {
      conditions.push("`etd` <= ?");
      values.push(criteria.etdTo);
    }
    if (criteria.ata !== undefined) {
      conditions.push("`ata` = ?");
      values.push(criteria.ata);
    }
    if (criteria.ataFrom !== undefined) {
      conditions.push("`ata` >= ?");
      values.push(criteria.ataFrom);
    }
    if (criteria.ataTo !== undefined) {
      conditions.push("`ata` <= ?");
      values.push(criteria.ataTo);
    }
    if (criteria.ataIsNull !== undefined) {
      conditions.push(
        criteria.ataIsNull
          ? "`ata` IS NULL"
          : "`ata` IS NOT NULL",
      );
    }
    if (criteria.berthShiftTime !== undefined) {
      conditions.push("`berth_shift_time` = ?");
      values.push(criteria.berthShiftTime);
    }
    if (criteria.berthShiftTimeFrom !== undefined) {
      conditions.push("`berth_shift_time` >= ?");
      values.push(criteria.berthShiftTimeFrom);
    }
    if (criteria.berthShiftTimeTo !== undefined) {
      conditions.push("`berth_shift_time` <= ?");
      values.push(criteria.berthShiftTimeTo);
    }
    if (criteria.berthShiftTimeIsNull !== undefined) {
      conditions.push(
        criteria.berthShiftTimeIsNull
          ? "`berth_shift_time` IS NULL"
          : "`berth_shift_time` IS NOT NULL",
      );
    }
    if (criteria.pilotRequired !== undefined) {
      conditions.push("`pilot_required` = ?");
      values.push(criteria.pilotRequired);
    }
    if (criteria.pilotRequiredFrom !== undefined) {
      conditions.push("`pilot_required` >= ?");
      values.push(criteria.pilotRequiredFrom);
    }
    if (criteria.pilotRequiredTo !== undefined) {
      conditions.push("`pilot_required` <= ?");
      values.push(criteria.pilotRequiredTo);
    }
    if (criteria.movesDischarged !== undefined) {
      conditions.push("`moves_discharged` = ?");
      values.push(criteria.movesDischarged);
    }
    if (criteria.movesDischargedFrom !== undefined) {
      conditions.push("`moves_discharged` >= ?");
      values.push(criteria.movesDischargedFrom);
    }
    if (criteria.movesDischargedTo !== undefined) {
      conditions.push("`moves_discharged` <= ?");
      values.push(criteria.movesDischargedTo);
    }
    if (criteria.movesLoaded !== undefined) {
      conditions.push("`moves_loaded` = ?");
      values.push(criteria.movesLoaded);
    }
    if (criteria.movesLoadedFrom !== undefined) {
      conditions.push("`moves_loaded` >= ?");
      values.push(criteria.movesLoadedFrom);
    }
    if (criteria.movesLoadedTo !== undefined) {
      conditions.push("`moves_loaded` <= ?");
      values.push(criteria.movesLoadedTo);
    }
    if (criteria.movesTotal !== undefined) {
      conditions.push("`moves_total` = ?");
      values.push(criteria.movesTotal);
    }
    if (criteria.movesTotalFrom !== undefined) {
      conditions.push("`moves_total` >= ?");
      values.push(criteria.movesTotalFrom);
    }
    if (criteria.movesTotalTo !== undefined) {
      conditions.push("`moves_total` <= ?");
      values.push(criteria.movesTotalTo);
    }
    if (criteria.movesTotalIsNull !== undefined) {
      conditions.push(
        criteria.movesTotalIsNull
          ? "`moves_total` IS NULL"
          : "`moves_total` IS NOT NULL",
      );
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              voyageLegColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
