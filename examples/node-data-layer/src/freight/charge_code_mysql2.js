// Code generated by tpl from freight.charge_code. DO NOT EDIT.

/**
 * The mysql2 implementation of the `freight.charge_code` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { ChargeCode, ChargeCodeFilter, ChargeCodeKey, ChargeCodeOrder } from './charge_code.js' */

import { NotFound, directionSql, failure } from './core.js';
import { chargeCodeColumnSql } from './charge_code.js';
import { ChargeCodeRepository } from './charge_code_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "freight.charge_code";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `charge_code`, `description`, `is_freight`, `vat_rate`, `gl_account` FROM `charge_code`";

/** Selects the one row a primary key names. */
const READ = "SELECT `charge_code`, `description`, `is_freight`, `vat_rate`, `gl_account` FROM `charge_code` WHERE `charge_code` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `charge_code` (`charge_code`, `description`, `is_freight`, `vat_rate`, `gl_account`) VALUES (?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `charge_code` SET `description` = ?, `is_freight` = ?, `vat_rate` = ?, `gl_account` = ? WHERE `charge_code` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `charge_code` WHERE `charge_code` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `ChargeCode` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {ChargeCode}
 */
function toRow(values) {
  return {
    chargeCode: values[0],
    description: values[1],
    isFreight: values[2],
    vatRate: values[3],
    glAccount: values[4],
  };
}

/**
 * The mysql2 data layer of `freight.charge_code`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class ChargeCodeMysql2 extends ChargeCodeRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `freight.charge_code` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The whole key is taken from `row`, which the caller therefore fills.
   *
   * @override
   * @param {ChargeCode} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.chargeCode,
      row.description,
      row.isFreight,
      row.vatRate,
      row.glAccount,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {ChargeCodeKey} key
   * @returns {Promise<ChargeCode>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.chargeCode,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {ChargeCode} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.description,
      row.isFreight,
      row.vatRate,
      row.glAccount,
      row.chargeCode,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {ChargeCodeKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.chargeCode,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {ChargeCodeFilter} criteria
   * @param {readonly ChargeCodeOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<ChargeCode[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.chargeCode !== undefined) {
      conditions.push("`charge_code` = ?");
      values.push(criteria.chargeCode);
    }
    if (criteria.chargeCodeLike !== undefined) {
      conditions.push("`charge_code` LIKE ?");
      values.push(criteria.chargeCodeLike);
    }
    if (criteria.description !== undefined) {
      conditions.push("`description` = ?");
      values.push(criteria.description);
    }
    if (criteria.descriptionLike !== undefined) {
      conditions.push("`description` LIKE ?");
      values.push(criteria.descriptionLike);
    }
    if (criteria.isFreight !== undefined) {
      conditions.push("`is_freight` = ?");
      values.push(criteria.isFreight);
    }
    if (criteria.isFreightFrom !== undefined) {
      conditions.push("`is_freight` >= ?");
      values.push(criteria.isFreightFrom);
    }
    if (criteria.isFreightTo !== undefined) {
      conditions.push("`is_freight` <= ?");
      values.push(criteria.isFreightTo);
    }
    if (criteria.vatRate !== undefined) {
      conditions.push("`vat_rate` = ?");
      values.push(criteria.vatRate);
    }
    if (criteria.vatRateFrom !== undefined) {
      conditions.push("`vat_rate` >= ?");
      values.push(criteria.vatRateFrom);
    }
    if (criteria.vatRateTo !== undefined) {
      conditions.push("`vat_rate` <= ?");
      values.push(criteria.vatRateTo);
    }
    if (criteria.glAccount !== undefined) {
      conditions.push("`gl_account` = ?");
      values.push(criteria.glAccount);
    }
    if (criteria.glAccountLike !== undefined) {
      conditions.push("`gl_account` LIKE ?");
      values.push(criteria.glAccountLike);
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              chargeCodeColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
