// Code generated by tpl from freight.tariff. DO NOT EDIT.

/**
 * The `freight.tariff` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

// The table's own comment, as the catalogue carries it:
// Tabela de fretes com versionamento de sistema: uma tarifa substituída continua consultável

/**
 * One row of `freight.tariff`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} Tariff
 * @property {number} tariffId `tariff_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT.
 *   It is part of the primary key.
 * @property {string} tradeLane `trade_lane`, declared `varchar(40)` NOT NULL.
 *   Comment: e.g. North Europe – West Africa
 * @property {string} originRegion `origin_region`, declared `varchar(40)` NOT NULL.
 * @property {string} destinationRegion `destination_region`, declared `varchar(40)` NOT NULL.
 * @property {string} equipmentIsoCode `equipment_iso_code`, declared `char(4)` NOT NULL.
 * @property {string} basis `basis`, declared `enum('Per container','Per freight tonne','Per cubic metre','Per shipment','Per bill of lading')` NOT NULL.
 * @property {string} currency `currency`, declared `char(3)` NOT NULL.
 * @property {string} baseRate `base_rate`, declared `decimal(11,2)` NOT NULL.
 * @property {string} bunkerSurcharge `bunker_surcharge`, declared `decimal(11,2)` NOT NULL.
 * @property {string} congestionSurcharge `congestion_surcharge`, declared `decimal(11,2)` NOT NULL.
 * @property {string | null} allInRate `all_in_rate`, declared `decimal(11,2)` NULL STORED GENERATED AS (`base_rate` + `bunker_surcharge` + `congestion_surcharge`).
 *   Comment: Stored generated column: what the customer is quoted
 * @property {Date} validFrom `valid_from`, declared `date` NOT NULL.
 * @property {Date} validUntil `valid_until`, declared `date` NOT NULL.
 */

/**
 * The primary key of `freight.tariff`.
 *
 * @typedef {object} TariffKey
 * @property {number} tariffId `tariff_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT.
 */

/**
 * One column of `freight.tariff`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link TariffColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const TariffColumns = Object.freeze(
  /** @type {const} */ ({
    /** `tariff_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT. */
    TARIFF_ID: "tariff_id",
    /** `trade_lane`, declared `varchar(40)` NOT NULL. */
    TRADE_LANE: "trade_lane",
    /** `origin_region`, declared `varchar(40)` NOT NULL. */
    ORIGIN_REGION: "origin_region",
    /** `destination_region`, declared `varchar(40)` NOT NULL. */
    DESTINATION_REGION: "destination_region",
    /** `equipment_iso_code`, declared `char(4)` NOT NULL. */
    EQUIPMENT_ISO_CODE: "equipment_iso_code",
    /** `basis`, declared `enum('Per container','Per freight tonne','Per cubic metre','Per shipment','Per bill of lading')` NOT NULL. */
    BASIS: "basis",
    /** `currency`, declared `char(3)` NOT NULL. */
    CURRENCY: "currency",
    /** `base_rate`, declared `decimal(11,2)` NOT NULL. */
    BASE_RATE: "base_rate",
    /** `bunker_surcharge`, declared `decimal(11,2)` NOT NULL. */
    BUNKER_SURCHARGE: "bunker_surcharge",
    /** `congestion_surcharge`, declared `decimal(11,2)` NOT NULL. */
    CONGESTION_SURCHARGE: "congestion_surcharge",
    /** `all_in_rate`, declared `decimal(11,2)` NULL STORED GENERATED AS (`base_rate` + `bunker_surcharge` + `congestion_surcharge`). */
    ALL_IN_RATE: "all_in_rate",
    /** `valid_from`, declared `date` NOT NULL. */
    VALID_FROM: "valid_from",
    /** `valid_until`, declared `date` NOT NULL. */
    VALID_UNTIL: "valid_until",
  }),
);

/**
 * A column of `freight.tariff` an ordering may name.
 *
 * @typedef {(typeof TariffColumns)[keyof typeof TariffColumns]} TariffColumn
 */

/**
 * The one place a column of `freight.tariff` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<TariffColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "tariff_id": "`tariff_id`",
  "trade_lane": "`trade_lane`",
  "origin_region": "`origin_region`",
  "destination_region": "`destination_region`",
  "equipment_iso_code": "`equipment_iso_code`",
  "basis": "`basis`",
  "currency": "`currency`",
  "base_rate": "`base_rate`",
  "bunker_surcharge": "`bunker_surcharge`",
  "congestion_surcharge": "`congestion_surcharge`",
  "all_in_rate": "`all_in_rate`",
  "valid_from": "`valid_from`",
  "valid_until": "`valid_until`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {TariffColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function tariffColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("freight.tariff", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `freight.tariff`.
 *
 * @typedef {object} TariffOrder
 * @property {TariffColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `freight.tariff`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} TariffFilter
 * @property {number} [tariffId] where set, requires `tariff_id` to equal it.
 * @property {number} [tariffIdFrom] where set, requires `tariff_id` to be at least it.
 * @property {number} [tariffIdTo] where set, requires `tariff_id` to be at most it.
 * @property {string} [tradeLane] where set, requires `trade_lane` to equal it.
 * @property {string} [tradeLaneLike] where set, requires `trade_lane` to match it, as LIKE matches.
 * @property {string} [originRegion] where set, requires `origin_region` to equal it.
 * @property {string} [originRegionLike] where set, requires `origin_region` to match it, as LIKE matches.
 * @property {string} [destinationRegion] where set, requires `destination_region` to equal it.
 * @property {string} [destinationRegionLike] where set, requires `destination_region` to match it, as LIKE matches.
 * @property {string} [equipmentIsoCode] where set, requires `equipment_iso_code` to equal it.
 * @property {string} [equipmentIsoCodeLike] where set, requires `equipment_iso_code` to match it, as LIKE matches.
 * @property {string} [basis] where set, requires `basis` to equal it.
 * @property {string} [basisLike] where set, requires `basis` to match it, as LIKE matches.
 * @property {string} [currency] where set, requires `currency` to equal it.
 * @property {string} [currencyLike] where set, requires `currency` to match it, as LIKE matches.
 * @property {string} [baseRate] where set, requires `base_rate` to equal it.
 * @property {string} [baseRateFrom] where set, requires `base_rate` to be at least it.
 * @property {string} [baseRateTo] where set, requires `base_rate` to be at most it.
 * @property {string} [bunkerSurcharge] where set, requires `bunker_surcharge` to equal it.
 * @property {string} [bunkerSurchargeFrom] where set, requires `bunker_surcharge` to be at least it.
 * @property {string} [bunkerSurchargeTo] where set, requires `bunker_surcharge` to be at most it.
 * @property {string} [congestionSurcharge] where set, requires `congestion_surcharge` to equal it.
 * @property {string} [congestionSurchargeFrom] where set, requires `congestion_surcharge` to be at least it.
 * @property {string} [congestionSurchargeTo] where set, requires `congestion_surcharge` to be at most it.
 * @property {string} [allInRate] where set, requires `all_in_rate` to equal it.
 * @property {string} [allInRateFrom] where set, requires `all_in_rate` to be at least it.
 * @property {string} [allInRateTo] where set, requires `all_in_rate` to be at most it.
 * @property {boolean} [allInRateIsNull] where true, requires `all_in_rate` to be NULL; where false, requires it not to be.
 * @property {Date} [validFrom] where set, requires `valid_from` to equal it.
 * @property {Date} [validFromFrom] where set, requires `valid_from` to be at least it.
 * @property {Date} [validFromTo] where set, requires `valid_from` to be at most it.
 * @property {Date} [validUntil] where set, requires `valid_until` to equal it.
 * @property {Date} [validUntilFrom] where set, requires `valid_until` to be at least it.
 * @property {Date} [validUntilTo] where set, requires `valid_until` to be at most it.
 */
