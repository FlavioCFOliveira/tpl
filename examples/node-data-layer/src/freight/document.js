// Code generated by tpl from freight.document. DO NOT EDIT.

/**
 * The `freight.document` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

// The table's own comment, as the catalogue carries it:
// Documentos digitalizados e assinados que acompanham cada expedição

/**
 * One row of `freight.document`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} Document
 * @property {string} documentId `document_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
 *   It is part of the primary key.
 * @property {string | null} declarationId `declaration_id`, declared `bigint(20) unsigned` NULL.
 *   Comment: NULL for a document filed against the consignment rather than a declaration
 * @property {string} documentKind `document_kind`, declared `enum('Bill of lading','Commercial invoice','Packing list','Certificate of origin','Phytosanitary certificate','Dangerous goods declaration','Delivery order')` NOT NULL.
 * @property {string} fileName `file_name`, declared `varchar(255)` NOT NULL.
 * @property {string} mediaType `media_type`, declared `varchar(120)` NOT NULL.
 * @property {number} byteSize `byte_size`, declared `int(10) unsigned` NOT NULL.
 * @property {Buffer} contentSha256 `content_sha256`, declared `binary(32)` NOT NULL.
 *   It is part of a unique index.
 *   Comment: Fixed-width binary: SHA-256 of the stored bytes
 * @property {Buffer | null} detachedSignature `detached_signature`, declared `varbinary(512)` NULL.
 *   Comment: Variable-width binary: CMS detached signature
 * @property {Buffer | null} signatureThumbnail `signature_thumbnail`, declared `tinyblob` NULL.
 *   Comment: Rendered signature strip, always small
 * @property {Buffer | null} pagePreview `page_preview`, declared `blob` NULL.
 *   Comment: First-page raster preview
 * @property {Buffer | null} scannedOriginal `scanned_original`, declared `mediumblob` NULL.
 *   Comment: Full colour scan of the paper original
 * @property {Buffer | null} archiveBundle `archive_bundle`, declared `longblob` NULL.
 *   Comment: Signed archival package retained for the statutory period
 * @property {string} uploadedBy `uploaded_by`, declared `varchar(80)` NOT NULL.
 * @property {Date} uploadedAt `uploaded_at`, declared `datetime(6)` NOT NULL.
 * @property {Date} retentionUntil `retention_until`, declared `date` NOT NULL.
 *   Comment: Expression default: ten-year statutory retention
 */

/**
 * The primary key of `freight.document`.
 *
 * @typedef {object} DocumentKey
 * @property {string} documentId `document_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
 */

/**
 * One column of `freight.document`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link DocumentColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const DocumentColumns = Object.freeze(
  /** @type {const} */ ({
    /** `document_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT. */
    DOCUMENT_ID: "document_id",
    /** `declaration_id`, declared `bigint(20) unsigned` NULL. */
    DECLARATION_ID: "declaration_id",
    /** `document_kind`, declared `enum('Bill of lading','Commercial invoice','Packing list','Certificate of origin','Phytosanitary certificate','Dangerous goods declaration','Delivery order')` NOT NULL. */
    DOCUMENT_KIND: "document_kind",
    /** `file_name`, declared `varchar(255)` NOT NULL. */
    FILE_NAME: "file_name",
    /** `media_type`, declared `varchar(120)` NOT NULL. */
    MEDIA_TYPE: "media_type",
    /** `byte_size`, declared `int(10) unsigned` NOT NULL. */
    BYTE_SIZE: "byte_size",
    /** `content_sha256`, declared `binary(32)` NOT NULL. */
    CONTENT_SHA256: "content_sha256",
    /** `detached_signature`, declared `varbinary(512)` NULL. */
    DETACHED_SIGNATURE: "detached_signature",
    /** `signature_thumbnail`, declared `tinyblob` NULL. */
    SIGNATURE_THUMBNAIL: "signature_thumbnail",
    /** `page_preview`, declared `blob` NULL. */
    PAGE_PREVIEW: "page_preview",
    /** `scanned_original`, declared `mediumblob` NULL. */
    SCANNED_ORIGINAL: "scanned_original",
    /** `archive_bundle`, declared `longblob` NULL. */
    ARCHIVE_BUNDLE: "archive_bundle",
    /** `uploaded_by`, declared `varchar(80)` NOT NULL. */
    UPLOADED_BY: "uploaded_by",
    /** `uploaded_at`, declared `datetime(6)` NOT NULL. */
    UPLOADED_AT: "uploaded_at",
    /** `retention_until`, declared `date` NOT NULL. */
    RETENTION_UNTIL: "retention_until",
  }),
);

/**
 * A column of `freight.document` an ordering may name.
 *
 * @typedef {(typeof DocumentColumns)[keyof typeof DocumentColumns]} DocumentColumn
 */

/**
 * The one place a column of `freight.document` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<DocumentColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "document_id": "`document_id`",
  "declaration_id": "`declaration_id`",
  "document_kind": "`document_kind`",
  "file_name": "`file_name`",
  "media_type": "`media_type`",
  "byte_size": "`byte_size`",
  "content_sha256": "`content_sha256`",
  "detached_signature": "`detached_signature`",
  "signature_thumbnail": "`signature_thumbnail`",
  "page_preview": "`page_preview`",
  "scanned_original": "`scanned_original`",
  "archive_bundle": "`archive_bundle`",
  "uploaded_by": "`uploaded_by`",
  "uploaded_at": "`uploaded_at`",
  "retention_until": "`retention_until`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {DocumentColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function documentColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("freight.document", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `freight.document`.
 *
 * @typedef {object} DocumentOrder
 * @property {DocumentColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `freight.document`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} DocumentFilter
 * @property {string} [documentId] where set, requires `document_id` to equal it.
 * @property {string} [documentIdFrom] where set, requires `document_id` to be at least it.
 * @property {string} [documentIdTo] where set, requires `document_id` to be at most it.
 * @property {string} [declarationId] where set, requires `declaration_id` to equal it.
 * @property {string} [declarationIdFrom] where set, requires `declaration_id` to be at least it.
 * @property {string} [declarationIdTo] where set, requires `declaration_id` to be at most it.
 * @property {boolean} [declarationIdIsNull] where true, requires `declaration_id` to be NULL; where false, requires it not to be.
 * @property {string} [documentKind] where set, requires `document_kind` to equal it.
 * @property {string} [documentKindLike] where set, requires `document_kind` to match it, as LIKE matches.
 * @property {string} [fileName] where set, requires `file_name` to equal it.
 * @property {string} [fileNameLike] where set, requires `file_name` to match it, as LIKE matches.
 * @property {string} [mediaType] where set, requires `media_type` to equal it.
 * @property {string} [mediaTypeLike] where set, requires `media_type` to match it, as LIKE matches.
 * @property {number} [byteSize] where set, requires `byte_size` to equal it.
 * @property {number} [byteSizeFrom] where set, requires `byte_size` to be at least it.
 * @property {number} [byteSizeTo] where set, requires `byte_size` to be at most it.
 * @property {Buffer} [contentSha256] where set, requires `content_sha256` to equal it.
 * @property {Buffer} [detachedSignature] where set, requires `detached_signature` to equal it.
 * @property {boolean} [detachedSignatureIsNull] where true, requires `detached_signature` to be NULL; where false, requires it not to be.
 * @property {Buffer} [signatureThumbnail] where set, requires `signature_thumbnail` to equal it.
 * @property {boolean} [signatureThumbnailIsNull] where true, requires `signature_thumbnail` to be NULL; where false, requires it not to be.
 * @property {Buffer} [pagePreview] where set, requires `page_preview` to equal it.
 * @property {boolean} [pagePreviewIsNull] where true, requires `page_preview` to be NULL; where false, requires it not to be.
 * @property {Buffer} [scannedOriginal] where set, requires `scanned_original` to equal it.
 * @property {boolean} [scannedOriginalIsNull] where true, requires `scanned_original` to be NULL; where false, requires it not to be.
 * @property {Buffer} [archiveBundle] where set, requires `archive_bundle` to equal it.
 * @property {boolean} [archiveBundleIsNull] where true, requires `archive_bundle` to be NULL; where false, requires it not to be.
 * @property {string} [uploadedBy] where set, requires `uploaded_by` to equal it.
 * @property {string} [uploadedByLike] where set, requires `uploaded_by` to match it, as LIKE matches.
 * @property {Date} [uploadedAt] where set, requires `uploaded_at` to equal it.
 * @property {Date} [uploadedAtFrom] where set, requires `uploaded_at` to be at least it.
 * @property {Date} [uploadedAtTo] where set, requires `uploaded_at` to be at most it.
 * @property {Date} [retentionUntil] where set, requires `retention_until` to equal it.
 * @property {Date} [retentionUntilFrom] where set, requires `retention_until` to be at least it.
 * @property {Date} [retentionUntilTo] where set, requires `retention_until` to be at most it.
 */
