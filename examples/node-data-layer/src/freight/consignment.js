// Code generated by tpl from freight.consignment. DO NOT EDIT.

/**
 * The `freight.consignment` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

// The table's own comment, as the catalogue carries it:
// Expedições reservadas, do rascunho à entrega

/**
 * One row of `freight.consignment`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} Consignment
 * @property {string} consignmentId `consignment_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
 *   It is part of the primary key.
 * @property {string} bookingReference `booking_reference`, declared `varchar(16)` NOT NULL.
 *   It is part of a unique index.
 *   Comment: Drawn from booking_reference_seq, prefixed BKG-
 * @property {string | null} billOfLadingNo `bill_of_lading_no`, declared `varchar(20)` NULL.
 *   It is part of a unique index.
 *   Comment: Issued only once the cargo is on board
 * @property {number} shipperId `shipper_id`, declared `int(10) unsigned` NOT NULL.
 * @property {number | null} consigneeId `consignee_id`, declared `int(10) unsigned` NULL.
 *   Comment: NULL for a "to order" bill of lading, which names no consignee at issue
 * @property {string | null} vesselImo `vessel_imo`, declared `char(7)` NULL.
 * @property {string | null} voyageNumber `voyage_number`, declared `varchar(12)` NULL.
 * @property {string} originLocode `origin_locode`, declared `char(5)` NOT NULL.
 * @property {string} destinationLocode `destination_locode`, declared `char(5)` NOT NULL.
 * @property {string} invoiceCurrency `invoice_currency`, declared `char(3)` NOT NULL.
 *   Comment: Literal default
 * @property {Date} paymentDueOn `payment_due_on`, declared `date` NOT NULL.
 *   Comment: Expression default: standard net-30 terms
 * @property {string | null} hsCodeOverride `hs_code_override`, declared `varchar(12)` NULL.
 *   Comment: Explicit DEFAULT NULL
 * @property {string} shipperReference `shipper_reference`, declared `varchar(64)` NOT NULL.
 *   Comment: No DEFAULT clause at all, and NOT NULL: every booking arrives with the customer reference
 * @property {string | null} brokerReference `broker_reference`, declared `varchar(64)` NULL.
 *   Comment: No DEFAULT clause and nullable, to contrast with the explicit DEFAULT NULL above
 * @property {string} incoterm `incoterm`, declared `enum('EXW','FCA','FAS','FOB','CFR','CIF','CPT','CIP','DAP','DPU','DDP')` NOT NULL.
 * @property {string} serviceScope `service_scope`, declared `enum('Port to port','Door to door','Door to port','Port to door')` NOT NULL.
 * @property {string} handlingFlags `handling_flags`, declared `set('Fragile','Stackable','Temperature controlled','Lloyd''s Register survey required','Out of gauge','Direct delivery')` NOT NULL.
 *   Comment: Note the apostrophe in the fourth member; a SET member may not contain a comma
 * @property {string} status `status`, declared `enum('Draft','Booked','Loaded','In transit','Discharged','Delivered','Cancelled')` NOT NULL.
 * @property {string} declaredValue `declared_value`, declared `decimal(14,2)` NOT NULL.
 * @property {string} freightAmount `freight_amount`, declared `decimal(12,2)` NOT NULL.
 * @property {string | null} insuranceRate `insurance_rate`, declared `decimal(6,4)` NULL.
 *   Comment: Rate applied to declared_value, e.g. 0.0035
 * @property {string | null} insuredAmount `insured_amount`, declared `decimal(14,2)` NULL VIRTUAL GENERATED AS (round(`declared_value` * coalesce(`insurance_rate`,0),2)).
 *   Comment: Virtual generated column: derived cover, never stored
 * @property {string} grossWeightKg `gross_weight_kg`, declared `decimal(12,3)` NOT NULL.
 *   Comment: Sum of the cargo items, kilograms
 * @property {string | null} volumeCbm `volume_cbm`, declared `decimal(10,3)` NULL.
 *   Comment: Cubic metres
 * @property {Buffer} notificationFlags `notification_flags`, declared `bit(8)` NOT NULL.
 * @property {number} isHazardous `is_hazardous`, declared `tinyint(1)` NOT NULL.
 * @property {string | null} marksAndNumbers `marks_and_numbers`, declared `tinytext` NULL.
 *   Comment: Shipping marks stencilled on the packages
 * @property {string} goodsDescription `goods_description`, declared `text` NOT NULL.
 *   Comment: Description of goods as it will print on the bill of lading
 * @property {string | null} packingList `packing_list`, declared `mediumtext` NULL.
 * @property {string | null} termsAndConditions `terms_and_conditions`, declared `longtext` NULL.
 *   Comment: Carrier terms in force at the moment of booking
 * @property {Date} bookedAt `booked_at`, declared `datetime(6)` NOT NULL.
 * @property {Date} updatedAt `updated_at`, declared `timestamp(3)` NOT NULL.
 */

/**
 * The primary key of `freight.consignment`.
 *
 * @typedef {object} ConsignmentKey
 * @property {string} consignmentId `consignment_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
 */

/**
 * One column of `freight.consignment`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link ConsignmentColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const ConsignmentColumns = Object.freeze(
  /** @type {const} */ ({
    /** `consignment_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT. */
    CONSIGNMENT_ID: "consignment_id",
    /** `booking_reference`, declared `varchar(16)` NOT NULL. */
    BOOKING_REFERENCE: "booking_reference",
    /** `bill_of_lading_no`, declared `varchar(20)` NULL. */
    BILL_OF_LADING_NO: "bill_of_lading_no",
    /** `shipper_id`, declared `int(10) unsigned` NOT NULL. */
    SHIPPER_ID: "shipper_id",
    /** `consignee_id`, declared `int(10) unsigned` NULL. */
    CONSIGNEE_ID: "consignee_id",
    /** `vessel_imo`, declared `char(7)` NULL. */
    VESSEL_IMO: "vessel_imo",
    /** `voyage_number`, declared `varchar(12)` NULL. */
    VOYAGE_NUMBER: "voyage_number",
    /** `origin_locode`, declared `char(5)` NOT NULL. */
    ORIGIN_LOCODE: "origin_locode",
    /** `destination_locode`, declared `char(5)` NOT NULL. */
    DESTINATION_LOCODE: "destination_locode",
    /** `invoice_currency`, declared `char(3)` NOT NULL. */
    INVOICE_CURRENCY: "invoice_currency",
    /** `payment_due_on`, declared `date` NOT NULL. */
    PAYMENT_DUE_ON: "payment_due_on",
    /** `hs_code_override`, declared `varchar(12)` NULL. */
    HS_CODE_OVERRIDE: "hs_code_override",
    /** `shipper_reference`, declared `varchar(64)` NOT NULL. */
    SHIPPER_REFERENCE: "shipper_reference",
    /** `broker_reference`, declared `varchar(64)` NULL. */
    BROKER_REFERENCE: "broker_reference",
    /** `incoterm`, declared `enum('EXW','FCA','FAS','FOB','CFR','CIF','CPT','CIP','DAP','DPU','DDP')` NOT NULL. */
    INCOTERM: "incoterm",
    /** `service_scope`, declared `enum('Port to port','Door to door','Door to port','Port to door')` NOT NULL. */
    SERVICE_SCOPE: "service_scope",
    /** `handling_flags`, declared `set('Fragile','Stackable','Temperature controlled','Lloyd''s Register survey required','Out of gauge','Direct delivery')` NOT NULL. */
    HANDLING_FLAGS: "handling_flags",
    /** `status`, declared `enum('Draft','Booked','Loaded','In transit','Discharged','Delivered','Cancelled')` NOT NULL. */
    STATUS: "status",
    /** `declared_value`, declared `decimal(14,2)` NOT NULL. */
    DECLARED_VALUE: "declared_value",
    /** `freight_amount`, declared `decimal(12,2)` NOT NULL. */
    FREIGHT_AMOUNT: "freight_amount",
    /** `insurance_rate`, declared `decimal(6,4)` NULL. */
    INSURANCE_RATE: "insurance_rate",
    /** `insured_amount`, declared `decimal(14,2)` NULL VIRTUAL GENERATED AS (round(`declared_value` * coalesce(`insurance_rate`,0),2)). */
    INSURED_AMOUNT: "insured_amount",
    /** `gross_weight_kg`, declared `decimal(12,3)` NOT NULL. */
    GROSS_WEIGHT_KG: "gross_weight_kg",
    /** `volume_cbm`, declared `decimal(10,3)` NULL. */
    VOLUME_CBM: "volume_cbm",
    /** `notification_flags`, declared `bit(8)` NOT NULL. */
    NOTIFICATION_FLAGS: "notification_flags",
    /** `is_hazardous`, declared `tinyint(1)` NOT NULL. */
    IS_HAZARDOUS: "is_hazardous",
    /** `marks_and_numbers`, declared `tinytext` NULL. */
    MARKS_AND_NUMBERS: "marks_and_numbers",
    /** `goods_description`, declared `text` NOT NULL. */
    GOODS_DESCRIPTION: "goods_description",
    /** `packing_list`, declared `mediumtext` NULL. */
    PACKING_LIST: "packing_list",
    /** `terms_and_conditions`, declared `longtext` NULL. */
    TERMS_AND_CONDITIONS: "terms_and_conditions",
    /** `booked_at`, declared `datetime(6)` NOT NULL. */
    BOOKED_AT: "booked_at",
    /** `updated_at`, declared `timestamp(3)` NOT NULL. */
    UPDATED_AT: "updated_at",
  }),
);

/**
 * A column of `freight.consignment` an ordering may name.
 *
 * @typedef {(typeof ConsignmentColumns)[keyof typeof ConsignmentColumns]} ConsignmentColumn
 */

/**
 * The one place a column of `freight.consignment` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<ConsignmentColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "consignment_id": "`consignment_id`",
  "booking_reference": "`booking_reference`",
  "bill_of_lading_no": "`bill_of_lading_no`",
  "shipper_id": "`shipper_id`",
  "consignee_id": "`consignee_id`",
  "vessel_imo": "`vessel_imo`",
  "voyage_number": "`voyage_number`",
  "origin_locode": "`origin_locode`",
  "destination_locode": "`destination_locode`",
  "invoice_currency": "`invoice_currency`",
  "payment_due_on": "`payment_due_on`",
  "hs_code_override": "`hs_code_override`",
  "shipper_reference": "`shipper_reference`",
  "broker_reference": "`broker_reference`",
  "incoterm": "`incoterm`",
  "service_scope": "`service_scope`",
  "handling_flags": "`handling_flags`",
  "status": "`status`",
  "declared_value": "`declared_value`",
  "freight_amount": "`freight_amount`",
  "insurance_rate": "`insurance_rate`",
  "insured_amount": "`insured_amount`",
  "gross_weight_kg": "`gross_weight_kg`",
  "volume_cbm": "`volume_cbm`",
  "notification_flags": "`notification_flags`",
  "is_hazardous": "`is_hazardous`",
  "marks_and_numbers": "`marks_and_numbers`",
  "goods_description": "`goods_description`",
  "packing_list": "`packing_list`",
  "terms_and_conditions": "`terms_and_conditions`",
  "booked_at": "`booked_at`",
  "updated_at": "`updated_at`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {ConsignmentColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function consignmentColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("freight.consignment", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `freight.consignment`.
 *
 * @typedef {object} ConsignmentOrder
 * @property {ConsignmentColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `freight.consignment`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} ConsignmentFilter
 * @property {string} [consignmentId] where set, requires `consignment_id` to equal it.
 * @property {string} [consignmentIdFrom] where set, requires `consignment_id` to be at least it.
 * @property {string} [consignmentIdTo] where set, requires `consignment_id` to be at most it.
 * @property {string} [bookingReference] where set, requires `booking_reference` to equal it.
 * @property {string} [bookingReferenceLike] where set, requires `booking_reference` to match it, as LIKE matches.
 * @property {string} [billOfLadingNo] where set, requires `bill_of_lading_no` to equal it.
 * @property {string} [billOfLadingNoLike] where set, requires `bill_of_lading_no` to match it, as LIKE matches.
 * @property {boolean} [billOfLadingNoIsNull] where true, requires `bill_of_lading_no` to be NULL; where false, requires it not to be.
 * @property {number} [shipperId] where set, requires `shipper_id` to equal it.
 * @property {number} [shipperIdFrom] where set, requires `shipper_id` to be at least it.
 * @property {number} [shipperIdTo] where set, requires `shipper_id` to be at most it.
 * @property {number} [consigneeId] where set, requires `consignee_id` to equal it.
 * @property {number} [consigneeIdFrom] where set, requires `consignee_id` to be at least it.
 * @property {number} [consigneeIdTo] where set, requires `consignee_id` to be at most it.
 * @property {boolean} [consigneeIdIsNull] where true, requires `consignee_id` to be NULL; where false, requires it not to be.
 * @property {string} [vesselImo] where set, requires `vessel_imo` to equal it.
 * @property {string} [vesselImoLike] where set, requires `vessel_imo` to match it, as LIKE matches.
 * @property {boolean} [vesselImoIsNull] where true, requires `vessel_imo` to be NULL; where false, requires it not to be.
 * @property {string} [voyageNumber] where set, requires `voyage_number` to equal it.
 * @property {string} [voyageNumberLike] where set, requires `voyage_number` to match it, as LIKE matches.
 * @property {boolean} [voyageNumberIsNull] where true, requires `voyage_number` to be NULL; where false, requires it not to be.
 * @property {string} [originLocode] where set, requires `origin_locode` to equal it.
 * @property {string} [originLocodeLike] where set, requires `origin_locode` to match it, as LIKE matches.
 * @property {string} [destinationLocode] where set, requires `destination_locode` to equal it.
 * @property {string} [destinationLocodeLike] where set, requires `destination_locode` to match it, as LIKE matches.
 * @property {string} [invoiceCurrency] where set, requires `invoice_currency` to equal it.
 * @property {string} [invoiceCurrencyLike] where set, requires `invoice_currency` to match it, as LIKE matches.
 * @property {Date} [paymentDueOn] where set, requires `payment_due_on` to equal it.
 * @property {Date} [paymentDueOnFrom] where set, requires `payment_due_on` to be at least it.
 * @property {Date} [paymentDueOnTo] where set, requires `payment_due_on` to be at most it.
 * @property {string} [hsCodeOverride] where set, requires `hs_code_override` to equal it.
 * @property {string} [hsCodeOverrideLike] where set, requires `hs_code_override` to match it, as LIKE matches.
 * @property {boolean} [hsCodeOverrideIsNull] where true, requires `hs_code_override` to be NULL; where false, requires it not to be.
 * @property {string} [shipperReference] where set, requires `shipper_reference` to equal it.
 * @property {string} [shipperReferenceLike] where set, requires `shipper_reference` to match it, as LIKE matches.
 * @property {string} [brokerReference] where set, requires `broker_reference` to equal it.
 * @property {string} [brokerReferenceLike] where set, requires `broker_reference` to match it, as LIKE matches.
 * @property {boolean} [brokerReferenceIsNull] where true, requires `broker_reference` to be NULL; where false, requires it not to be.
 * @property {string} [incoterm] where set, requires `incoterm` to equal it.
 * @property {string} [incotermLike] where set, requires `incoterm` to match it, as LIKE matches.
 * @property {string} [serviceScope] where set, requires `service_scope` to equal it.
 * @property {string} [serviceScopeLike] where set, requires `service_scope` to match it, as LIKE matches.
 * @property {string} [handlingFlags] where set, requires `handling_flags` to equal it.
 * @property {string} [handlingFlagsLike] where set, requires `handling_flags` to match it, as LIKE matches.
 * @property {string} [status] where set, requires `status` to equal it.
 * @property {string} [statusLike] where set, requires `status` to match it, as LIKE matches.
 * @property {string} [declaredValue] where set, requires `declared_value` to equal it.
 * @property {string} [declaredValueFrom] where set, requires `declared_value` to be at least it.
 * @property {string} [declaredValueTo] where set, requires `declared_value` to be at most it.
 * @property {string} [freightAmount] where set, requires `freight_amount` to equal it.
 * @property {string} [freightAmountFrom] where set, requires `freight_amount` to be at least it.
 * @property {string} [freightAmountTo] where set, requires `freight_amount` to be at most it.
 * @property {string} [insuranceRate] where set, requires `insurance_rate` to equal it.
 * @property {string} [insuranceRateFrom] where set, requires `insurance_rate` to be at least it.
 * @property {string} [insuranceRateTo] where set, requires `insurance_rate` to be at most it.
 * @property {boolean} [insuranceRateIsNull] where true, requires `insurance_rate` to be NULL; where false, requires it not to be.
 * @property {string} [insuredAmount] where set, requires `insured_amount` to equal it.
 * @property {string} [insuredAmountFrom] where set, requires `insured_amount` to be at least it.
 * @property {string} [insuredAmountTo] where set, requires `insured_amount` to be at most it.
 * @property {boolean} [insuredAmountIsNull] where true, requires `insured_amount` to be NULL; where false, requires it not to be.
 * @property {string} [grossWeightKg] where set, requires `gross_weight_kg` to equal it.
 * @property {string} [grossWeightKgFrom] where set, requires `gross_weight_kg` to be at least it.
 * @property {string} [grossWeightKgTo] where set, requires `gross_weight_kg` to be at most it.
 * @property {string} [volumeCbm] where set, requires `volume_cbm` to equal it.
 * @property {string} [volumeCbmFrom] where set, requires `volume_cbm` to be at least it.
 * @property {string} [volumeCbmTo] where set, requires `volume_cbm` to be at most it.
 * @property {boolean} [volumeCbmIsNull] where true, requires `volume_cbm` to be NULL; where false, requires it not to be.
 * @property {Buffer} [notificationFlags] where set, requires `notification_flags` to equal it.
 * @property {Buffer} [notificationFlagsFrom] where set, requires `notification_flags` to be at least it.
 * @property {Buffer} [notificationFlagsTo] where set, requires `notification_flags` to be at most it.
 * @property {number} [isHazardous] where set, requires `is_hazardous` to equal it.
 * @property {number} [isHazardousFrom] where set, requires `is_hazardous` to be at least it.
 * @property {number} [isHazardousTo] where set, requires `is_hazardous` to be at most it.
 * @property {string} [marksAndNumbers] where set, requires `marks_and_numbers` to equal it.
 * @property {string} [marksAndNumbersLike] where set, requires `marks_and_numbers` to match it, as LIKE matches.
 * @property {boolean} [marksAndNumbersIsNull] where true, requires `marks_and_numbers` to be NULL; where false, requires it not to be.
 * @property {string} [goodsDescription] where set, requires `goods_description` to equal it.
 * @property {string} [goodsDescriptionLike] where set, requires `goods_description` to match it, as LIKE matches.
 * @property {string} [packingList] where set, requires `packing_list` to equal it.
 * @property {string} [packingListLike] where set, requires `packing_list` to match it, as LIKE matches.
 * @property {boolean} [packingListIsNull] where true, requires `packing_list` to be NULL; where false, requires it not to be.
 * @property {string} [termsAndConditions] where set, requires `terms_and_conditions` to equal it.
 * @property {string} [termsAndConditionsLike] where set, requires `terms_and_conditions` to match it, as LIKE matches.
 * @property {boolean} [termsAndConditionsIsNull] where true, requires `terms_and_conditions` to be NULL; where false, requires it not to be.
 * @property {Date} [bookedAt] where set, requires `booked_at` to equal it.
 * @property {Date} [bookedAtFrom] where set, requires `booked_at` to be at least it.
 * @property {Date} [bookedAtTo] where set, requires `booked_at` to be at most it.
 * @property {Date} [updatedAt] where set, requires `updated_at` to equal it.
 * @property {Date} [updatedAtFrom] where set, requires `updated_at` to be at least it.
 * @property {Date} [updatedAtTo] where set, requires `updated_at` to be at most it.
 */
