// Code generated by tpl from freight.charge. DO NOT EDIT.

/**
 * The mysql2 implementation of the `freight.charge` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { Charge, ChargeFilter, ChargeKey, ChargeOrder } from './charge.js' */

import { NotFound, directionSql, failure } from './core.js';
import { chargeColumnSql } from './charge.js';
import { ChargeRepository } from './charge_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "freight.charge";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `charge_id`, `consignment_id`, `charge_code`, `quantity`, `unit_amount`, `currency`, `fx_rate_to_eur`, `net_amount`, `payer`, `invoiced_on`, `invoice_number` FROM `charge`";

/** Selects the one row a primary key names. */
const READ = "SELECT `charge_id`, `consignment_id`, `charge_code`, `quantity`, `unit_amount`, `currency`, `fx_rate_to_eur`, `net_amount`, `payer`, `invoiced_on`, `invoice_number` FROM `charge` WHERE `charge_id` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `charge` (`consignment_id`, `charge_code`, `quantity`, `unit_amount`, `currency`, `fx_rate_to_eur`, `payer`, `invoiced_on`, `invoice_number`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `charge` SET `consignment_id` = ?, `charge_code` = ?, `quantity` = ?, `unit_amount` = ?, `currency` = ?, `fx_rate_to_eur` = ?, `payer` = ?, `invoiced_on` = ?, `invoice_number` = ? WHERE `charge_id` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `charge` WHERE `charge_id` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `Charge` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {Charge}
 */
function toRow(values) {
  return {
    chargeId: values[0],
    consignmentId: values[1],
    chargeCode: values[2],
    quantity: values[3],
    unitAmount: values[4],
    currency: values[5],
    fxRateToEur: values[6],
    netAmount: values[7],
    payer: values[8],
    invoicedOn: values[9],
    invoiceNumber: values[10],
  };
}

/**
 * The mysql2 data layer of `freight.charge`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class ChargeMysql2 extends ChargeRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `freight.charge` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The key is assigned by the server and is written back into `row`.
   *
   * @override
   * @param {Charge} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.consignmentId,
      row.chargeCode,
      row.quantity,
      row.unitAmount,
      row.currency,
      row.fxRateToEur,
      row.payer,
      row.invoicedOn,
      row.invoiceNumber,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
      const { insertId } = /** @type {ResultSetHeader} */ (result);
      // The key is a BIGINT and every row of this package carries one as a
      // `string`, because `connect` sets `bigNumberStrings`. mysql2 reports
      // `insertId` as a `number` whatever the column is, so it is converted
      // here rather than left to widen the row's type.
      row.chargeId = String(insertId);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {ChargeKey} key
   * @returns {Promise<Charge>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.chargeId,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {Charge} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.consignmentId,
      row.chargeCode,
      row.quantity,
      row.unitAmount,
      row.currency,
      row.fxRateToEur,
      row.payer,
      row.invoicedOn,
      row.invoiceNumber,
      row.chargeId,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {ChargeKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.chargeId,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {ChargeFilter} criteria
   * @param {readonly ChargeOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<Charge[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.chargeId !== undefined) {
      conditions.push("`charge_id` = ?");
      values.push(criteria.chargeId);
    }
    if (criteria.chargeIdFrom !== undefined) {
      conditions.push("`charge_id` >= ?");
      values.push(criteria.chargeIdFrom);
    }
    if (criteria.chargeIdTo !== undefined) {
      conditions.push("`charge_id` <= ?");
      values.push(criteria.chargeIdTo);
    }
    if (criteria.consignmentId !== undefined) {
      conditions.push("`consignment_id` = ?");
      values.push(criteria.consignmentId);
    }
    if (criteria.consignmentIdFrom !== undefined) {
      conditions.push("`consignment_id` >= ?");
      values.push(criteria.consignmentIdFrom);
    }
    if (criteria.consignmentIdTo !== undefined) {
      conditions.push("`consignment_id` <= ?");
      values.push(criteria.consignmentIdTo);
    }
    if (criteria.chargeCode !== undefined) {
      conditions.push("`charge_code` = ?");
      values.push(criteria.chargeCode);
    }
    if (criteria.chargeCodeLike !== undefined) {
      conditions.push("`charge_code` LIKE ?");
      values.push(criteria.chargeCodeLike);
    }
    if (criteria.quantity !== undefined) {
      conditions.push("`quantity` = ?");
      values.push(criteria.quantity);
    }
    if (criteria.quantityFrom !== undefined) {
      conditions.push("`quantity` >= ?");
      values.push(criteria.quantityFrom);
    }
    if (criteria.quantityTo !== undefined) {
      conditions.push("`quantity` <= ?");
      values.push(criteria.quantityTo);
    }
    if (criteria.unitAmount !== undefined) {
      conditions.push("`unit_amount` = ?");
      values.push(criteria.unitAmount);
    }
    if (criteria.unitAmountFrom !== undefined) {
      conditions.push("`unit_amount` >= ?");
      values.push(criteria.unitAmountFrom);
    }
    if (criteria.unitAmountTo !== undefined) {
      conditions.push("`unit_amount` <= ?");
      values.push(criteria.unitAmountTo);
    }
    if (criteria.currency !== undefined) {
      conditions.push("`currency` = ?");
      values.push(criteria.currency);
    }
    if (criteria.currencyLike !== undefined) {
      conditions.push("`currency` LIKE ?");
      values.push(criteria.currencyLike);
    }
    if (criteria.fxRateToEur !== undefined) {
      conditions.push("`fx_rate_to_eur` = ?");
      values.push(criteria.fxRateToEur);
    }
    if (criteria.fxRateToEurFrom !== undefined) {
      conditions.push("`fx_rate_to_eur` >= ?");
      values.push(criteria.fxRateToEurFrom);
    }
    if (criteria.fxRateToEurTo !== undefined) {
      conditions.push("`fx_rate_to_eur` <= ?");
      values.push(criteria.fxRateToEurTo);
    }
    if (criteria.netAmount !== undefined) {
      conditions.push("`net_amount` = ?");
      values.push(criteria.netAmount);
    }
    if (criteria.netAmountFrom !== undefined) {
      conditions.push("`net_amount` >= ?");
      values.push(criteria.netAmountFrom);
    }
    if (criteria.netAmountTo !== undefined) {
      conditions.push("`net_amount` <= ?");
      values.push(criteria.netAmountTo);
    }
    if (criteria.netAmountIsNull !== undefined) {
      conditions.push(
        criteria.netAmountIsNull
          ? "`net_amount` IS NULL"
          : "`net_amount` IS NOT NULL",
      );
    }
    if (criteria.payer !== undefined) {
      conditions.push("`payer` = ?");
      values.push(criteria.payer);
    }
    if (criteria.payerLike !== undefined) {
      conditions.push("`payer` LIKE ?");
      values.push(criteria.payerLike);
    }
    if (criteria.invoicedOn !== undefined) {
      conditions.push("`invoiced_on` = ?");
      values.push(criteria.invoicedOn);
    }
    if (criteria.invoicedOnFrom !== undefined) {
      conditions.push("`invoiced_on` >= ?");
      values.push(criteria.invoicedOnFrom);
    }
    if (criteria.invoicedOnTo !== undefined) {
      conditions.push("`invoiced_on` <= ?");
      values.push(criteria.invoicedOnTo);
    }
    if (criteria.invoicedOnIsNull !== undefined) {
      conditions.push(
        criteria.invoicedOnIsNull
          ? "`invoiced_on` IS NULL"
          : "`invoiced_on` IS NOT NULL",
      );
    }
    if (criteria.invoiceNumber !== undefined) {
      conditions.push("`invoice_number` = ?");
      values.push(criteria.invoiceNumber);
    }
    if (criteria.invoiceNumberLike !== undefined) {
      conditions.push("`invoice_number` LIKE ?");
      values.push(criteria.invoiceNumberLike);
    }
    if (criteria.invoiceNumberIsNull !== undefined) {
      conditions.push(
        criteria.invoiceNumberIsNull
          ? "`invoice_number` IS NULL"
          : "`invoice_number` IS NOT NULL",
      );
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              chargeColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
