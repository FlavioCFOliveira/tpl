// Code generated by tpl from freight.port_facility. DO NOT EDIT.

/**
 * The `freight.port_facility` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

// The table's own comment, as the catalogue carries it:
// Terminais e postos de acostagem, com a respectiva geometria — 港湾施設

/**
 * One row of `freight.port_facility`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} PortFacility
 * @property {number} facilityId `facility_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT.
 *   It is part of the primary key.
 * @property {number} portId `port_id`, declared `smallint(5) unsigned` NOT NULL.
 *   It is part of a unique index.
 * @property {string} designation `designation`, declared `varchar(80)` NOT NULL.
 *   It is part of a unique index.
 *   Comment: Terminal or berth designation, e.g. "Alcântara Norte"
 * @property {string} facilityKind `facility_kind`, declared `enum('Container terminal','Ro-Ro ramp','Bulk quay','Liquid bulk jetty','Reefer terminal','Lay-by berth')` NOT NULL.
 * @property {string} quayLengthM `quay_length_m`, declared `decimal(7,2)` NOT NULL.
 *   Comment: Continuous quay length in metres
 * @property {number} craneCount `crane_count`, declared `tinyint(3) unsigned` NOT NULL.
 *   Comment: Ship-to-shore gantry cranes
 * @property {string} gateOpensAt `gate_opens_at`, declared `time` NOT NULL.
 *   Comment: Landside gate opening, local time
 * @property {string} gateClosesAt `gate_closes_at`, declared `time` NOT NULL.
 *   Comment: Landside gate closing, local time
 * @property {Buffer} berthOutline `berth_outline`, declared `polygon` NOT NULL.
 *   Comment: Berth footprint as surveyed
 * @property {Buffer | null} approachChannel `approach_channel`, declared `linestring` NULL.
 *   Comment: Centreline of the dredged approach
 * @property {Buffer | null} anchoragePoints `anchorage_points`, declared `multipoint` NULL.
 *   Comment: Designated waiting anchorages
 * @property {Buffer | null} fairwayNetwork `fairway_network`, declared `multilinestring` NULL.
 *   Comment: Buoyed fairways serving the facility
 * @property {Buffer | null} restrictedAreas `restricted_areas`, declared `multipolygon` NULL.
 *   Comment: Areas closed to commercial traffic
 * @property {Buffer | null} referenceMarker `reference_marker`, declared `point` NULL.
 *   Comment: Single published reference position
 * @property {Buffer | null} surveyFootprint `survey_footprint`, declared `geometry` NULL.
 *   Comment: Latest survey outline, type not fixed in advance
 * @property {Buffer | null} noticesToMariners `notices_to_mariners`, declared `geometrycollection` NULL.
 *   Comment: Mixed geometry accompanying current notices
 */

/**
 * The primary key of `freight.port_facility`.
 *
 * @typedef {object} PortFacilityKey
 * @property {number} facilityId `facility_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT.
 */

/**
 * One column of `freight.port_facility`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link PortFacilityColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const PortFacilityColumns = Object.freeze(
  /** @type {const} */ ({
    /** `facility_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT. */
    FACILITY_ID: "facility_id",
    /** `port_id`, declared `smallint(5) unsigned` NOT NULL. */
    PORT_ID: "port_id",
    /** `designation`, declared `varchar(80)` NOT NULL. */
    DESIGNATION: "designation",
    /** `facility_kind`, declared `enum('Container terminal','Ro-Ro ramp','Bulk quay','Liquid bulk jetty','Reefer terminal','Lay-by berth')` NOT NULL. */
    FACILITY_KIND: "facility_kind",
    /** `quay_length_m`, declared `decimal(7,2)` NOT NULL. */
    QUAY_LENGTH_M: "quay_length_m",
    /** `crane_count`, declared `tinyint(3) unsigned` NOT NULL. */
    CRANE_COUNT: "crane_count",
    /** `gate_opens_at`, declared `time` NOT NULL. */
    GATE_OPENS_AT: "gate_opens_at",
    /** `gate_closes_at`, declared `time` NOT NULL. */
    GATE_CLOSES_AT: "gate_closes_at",
    /** `berth_outline`, declared `polygon` NOT NULL. */
    BERTH_OUTLINE: "berth_outline",
    /** `approach_channel`, declared `linestring` NULL. */
    APPROACH_CHANNEL: "approach_channel",
    /** `anchorage_points`, declared `multipoint` NULL. */
    ANCHORAGE_POINTS: "anchorage_points",
    /** `fairway_network`, declared `multilinestring` NULL. */
    FAIRWAY_NETWORK: "fairway_network",
    /** `restricted_areas`, declared `multipolygon` NULL. */
    RESTRICTED_AREAS: "restricted_areas",
    /** `reference_marker`, declared `point` NULL. */
    REFERENCE_MARKER: "reference_marker",
    /** `survey_footprint`, declared `geometry` NULL. */
    SURVEY_FOOTPRINT: "survey_footprint",
    /** `notices_to_mariners`, declared `geometrycollection` NULL. */
    NOTICES_TO_MARINERS: "notices_to_mariners",
  }),
);

/**
 * A column of `freight.port_facility` an ordering may name.
 *
 * @typedef {(typeof PortFacilityColumns)[keyof typeof PortFacilityColumns]} PortFacilityColumn
 */

/**
 * The one place a column of `freight.port_facility` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<PortFacilityColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "facility_id": "`facility_id`",
  "port_id": "`port_id`",
  "designation": "`designation`",
  "facility_kind": "`facility_kind`",
  "quay_length_m": "`quay_length_m`",
  "crane_count": "`crane_count`",
  "gate_opens_at": "`gate_opens_at`",
  "gate_closes_at": "`gate_closes_at`",
  "berth_outline": "`berth_outline`",
  "approach_channel": "`approach_channel`",
  "anchorage_points": "`anchorage_points`",
  "fairway_network": "`fairway_network`",
  "restricted_areas": "`restricted_areas`",
  "reference_marker": "`reference_marker`",
  "survey_footprint": "`survey_footprint`",
  "notices_to_mariners": "`notices_to_mariners`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {PortFacilityColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function portFacilityColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("freight.port_facility", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `freight.port_facility`.
 *
 * @typedef {object} PortFacilityOrder
 * @property {PortFacilityColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `freight.port_facility`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} PortFacilityFilter
 * @property {number} [facilityId] where set, requires `facility_id` to equal it.
 * @property {number} [facilityIdFrom] where set, requires `facility_id` to be at least it.
 * @property {number} [facilityIdTo] where set, requires `facility_id` to be at most it.
 * @property {number} [portId] where set, requires `port_id` to equal it.
 * @property {number} [portIdFrom] where set, requires `port_id` to be at least it.
 * @property {number} [portIdTo] where set, requires `port_id` to be at most it.
 * @property {string} [designation] where set, requires `designation` to equal it.
 * @property {string} [designationLike] where set, requires `designation` to match it, as LIKE matches.
 * @property {string} [facilityKind] where set, requires `facility_kind` to equal it.
 * @property {string} [facilityKindLike] where set, requires `facility_kind` to match it, as LIKE matches.
 * @property {string} [quayLengthM] where set, requires `quay_length_m` to equal it.
 * @property {string} [quayLengthMFrom] where set, requires `quay_length_m` to be at least it.
 * @property {string} [quayLengthMTo] where set, requires `quay_length_m` to be at most it.
 * @property {number} [craneCount] where set, requires `crane_count` to equal it.
 * @property {number} [craneCountFrom] where set, requires `crane_count` to be at least it.
 * @property {number} [craneCountTo] where set, requires `crane_count` to be at most it.
 * @property {string} [gateOpensAt] where set, requires `gate_opens_at` to equal it.
 * @property {string} [gateOpensAtFrom] where set, requires `gate_opens_at` to be at least it.
 * @property {string} [gateOpensAtTo] where set, requires `gate_opens_at` to be at most it.
 * @property {string} [gateClosesAt] where set, requires `gate_closes_at` to equal it.
 * @property {string} [gateClosesAtFrom] where set, requires `gate_closes_at` to be at least it.
 * @property {string} [gateClosesAtTo] where set, requires `gate_closes_at` to be at most it.
 * @property {Buffer} [berthOutline] where set, requires `berth_outline` to equal it.
 * @property {Buffer} [approachChannel] where set, requires `approach_channel` to equal it.
 * @property {boolean} [approachChannelIsNull] where true, requires `approach_channel` to be NULL; where false, requires it not to be.
 * @property {Buffer} [anchoragePoints] where set, requires `anchorage_points` to equal it.
 * @property {boolean} [anchoragePointsIsNull] where true, requires `anchorage_points` to be NULL; where false, requires it not to be.
 * @property {Buffer} [fairwayNetwork] where set, requires `fairway_network` to equal it.
 * @property {boolean} [fairwayNetworkIsNull] where true, requires `fairway_network` to be NULL; where false, requires it not to be.
 * @property {Buffer} [restrictedAreas] where set, requires `restricted_areas` to equal it.
 * @property {boolean} [restrictedAreasIsNull] where true, requires `restricted_areas` to be NULL; where false, requires it not to be.
 * @property {Buffer} [referenceMarker] where set, requires `reference_marker` to equal it.
 * @property {boolean} [referenceMarkerIsNull] where true, requires `reference_marker` to be NULL; where false, requires it not to be.
 * @property {Buffer} [surveyFootprint] where set, requires `survey_footprint` to equal it.
 * @property {boolean} [surveyFootprintIsNull] where true, requires `survey_footprint` to be NULL; where false, requires it not to be.
 * @property {Buffer} [noticesToMariners] where set, requires `notices_to_mariners` to equal it.
 * @property {boolean} [noticesToMarinersIsNull] where true, requires `notices_to_mariners` to be NULL; where false, requires it not to be.
 */
