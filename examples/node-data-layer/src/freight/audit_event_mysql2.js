// Code generated by tpl from freight.audit_event. DO NOT EDIT.

/**
 * The mysql2 implementation of the `freight.audit_event` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { AuditEvent, AuditEventFilter, AuditEventKey, AuditEventOrder } from './audit_event.js' */

import { NotFound, directionSql, failure } from './core.js';
import { auditEventColumnSql } from './audit_event.js';
import { AuditEventRepository } from './audit_event_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "freight.audit_event";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `audit_event_id`, `entity_name`, `entity_key`, `action`, `trigger_timing`, `changed_column`, `old_value`, `new_value`, `actor`, `client_ipv4`, `client_ipv6`, `correlation_uuid`, `observed_at`, `detail` FROM `audit_event`";

/** Selects the one row a primary key names. */
const READ = "SELECT `audit_event_id`, `entity_name`, `entity_key`, `action`, `trigger_timing`, `changed_column`, `old_value`, `new_value`, `actor`, `client_ipv4`, `client_ipv6`, `correlation_uuid`, `observed_at`, `detail` FROM `audit_event` WHERE `audit_event_id` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `audit_event` (`entity_name`, `entity_key`, `action`, `trigger_timing`, `changed_column`, `old_value`, `new_value`, `actor`, `client_ipv4`, `client_ipv6`, `correlation_uuid`, `observed_at`, `detail`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `audit_event` SET `entity_name` = ?, `entity_key` = ?, `action` = ?, `trigger_timing` = ?, `changed_column` = ?, `old_value` = ?, `new_value` = ?, `actor` = ?, `client_ipv4` = ?, `client_ipv6` = ?, `correlation_uuid` = ?, `observed_at` = ?, `detail` = ? WHERE `audit_event_id` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `audit_event` WHERE `audit_event_id` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `AuditEvent` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {AuditEvent}
 */
function toRow(values) {
  return {
    auditEventId: values[0],
    entityName: values[1],
    entityKey: values[2],
    action: values[3],
    triggerTiming: values[4],
    changedColumn: values[5],
    oldValue: values[6],
    newValue: values[7],
    actor: values[8],
    clientIpv4: values[9],
    clientIpv6: values[10],
    correlationUuid: values[11],
    observedAt: values[12],
    detail: values[13],
  };
}

/**
 * The mysql2 data layer of `freight.audit_event`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class AuditEventMysql2 extends AuditEventRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `freight.audit_event` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The key is assigned by the server and is written back into `row`.
   *
   * @override
   * @param {AuditEvent} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.entityName,
      row.entityKey,
      row.action,
      row.triggerTiming,
      row.changedColumn,
      row.oldValue,
      row.newValue,
      row.actor,
      row.clientIpv4,
      row.clientIpv6,
      row.correlationUuid,
      row.observedAt,
      row.detail,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
      const { insertId } = /** @type {ResultSetHeader} */ (result);
      // The key is a BIGINT and every row of this package carries one as a
      // `string`, because `connect` sets `bigNumberStrings`. mysql2 reports
      // `insertId` as a `number` whatever the column is, so it is converted
      // here rather than left to widen the row's type.
      row.auditEventId = String(insertId);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {AuditEventKey} key
   * @returns {Promise<AuditEvent>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.auditEventId,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {AuditEvent} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.entityName,
      row.entityKey,
      row.action,
      row.triggerTiming,
      row.changedColumn,
      row.oldValue,
      row.newValue,
      row.actor,
      row.clientIpv4,
      row.clientIpv6,
      row.correlationUuid,
      row.observedAt,
      row.detail,
      row.auditEventId,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {AuditEventKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.auditEventId,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {AuditEventFilter} criteria
   * @param {readonly AuditEventOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<AuditEvent[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.auditEventId !== undefined) {
      conditions.push("`audit_event_id` = ?");
      values.push(criteria.auditEventId);
    }
    if (criteria.auditEventIdFrom !== undefined) {
      conditions.push("`audit_event_id` >= ?");
      values.push(criteria.auditEventIdFrom);
    }
    if (criteria.auditEventIdTo !== undefined) {
      conditions.push("`audit_event_id` <= ?");
      values.push(criteria.auditEventIdTo);
    }
    if (criteria.entityName !== undefined) {
      conditions.push("`entity_name` = ?");
      values.push(criteria.entityName);
    }
    if (criteria.entityNameLike !== undefined) {
      conditions.push("`entity_name` LIKE ?");
      values.push(criteria.entityNameLike);
    }
    if (criteria.entityKey !== undefined) {
      conditions.push("`entity_key` = ?");
      values.push(criteria.entityKey);
    }
    if (criteria.entityKeyLike !== undefined) {
      conditions.push("`entity_key` LIKE ?");
      values.push(criteria.entityKeyLike);
    }
    if (criteria.action !== undefined) {
      conditions.push("`action` = ?");
      values.push(criteria.action);
    }
    if (criteria.actionLike !== undefined) {
      conditions.push("`action` LIKE ?");
      values.push(criteria.actionLike);
    }
    if (criteria.triggerTiming !== undefined) {
      conditions.push("`trigger_timing` = ?");
      values.push(criteria.triggerTiming);
    }
    if (criteria.triggerTimingLike !== undefined) {
      conditions.push("`trigger_timing` LIKE ?");
      values.push(criteria.triggerTimingLike);
    }
    if (criteria.changedColumn !== undefined) {
      conditions.push("`changed_column` = ?");
      values.push(criteria.changedColumn);
    }
    if (criteria.changedColumnLike !== undefined) {
      conditions.push("`changed_column` LIKE ?");
      values.push(criteria.changedColumnLike);
    }
    if (criteria.changedColumnIsNull !== undefined) {
      conditions.push(
        criteria.changedColumnIsNull
          ? "`changed_column` IS NULL"
          : "`changed_column` IS NOT NULL",
      );
    }
    if (criteria.oldValue !== undefined) {
      conditions.push("`old_value` = ?");
      values.push(criteria.oldValue);
    }
    if (criteria.oldValueLike !== undefined) {
      conditions.push("`old_value` LIKE ?");
      values.push(criteria.oldValueLike);
    }
    if (criteria.oldValueIsNull !== undefined) {
      conditions.push(
        criteria.oldValueIsNull
          ? "`old_value` IS NULL"
          : "`old_value` IS NOT NULL",
      );
    }
    if (criteria.newValue !== undefined) {
      conditions.push("`new_value` = ?");
      values.push(criteria.newValue);
    }
    if (criteria.newValueLike !== undefined) {
      conditions.push("`new_value` LIKE ?");
      values.push(criteria.newValueLike);
    }
    if (criteria.newValueIsNull !== undefined) {
      conditions.push(
        criteria.newValueIsNull
          ? "`new_value` IS NULL"
          : "`new_value` IS NOT NULL",
      );
    }
    if (criteria.actor !== undefined) {
      conditions.push("`actor` = ?");
      values.push(criteria.actor);
    }
    if (criteria.actorLike !== undefined) {
      conditions.push("`actor` LIKE ?");
      values.push(criteria.actorLike);
    }
    if (criteria.clientIpv4 !== undefined) {
      conditions.push("`client_ipv4` = ?");
      values.push(criteria.clientIpv4);
    }
    if (criteria.clientIpv4IsNull !== undefined) {
      conditions.push(
        criteria.clientIpv4IsNull
          ? "`client_ipv4` IS NULL"
          : "`client_ipv4` IS NOT NULL",
      );
    }
    if (criteria.clientIpv6 !== undefined) {
      conditions.push("`client_ipv6` = ?");
      values.push(criteria.clientIpv6);
    }
    if (criteria.clientIpv6IsNull !== undefined) {
      conditions.push(
        criteria.clientIpv6IsNull
          ? "`client_ipv6` IS NULL"
          : "`client_ipv6` IS NOT NULL",
      );
    }
    if (criteria.correlationUuid !== undefined) {
      conditions.push("`correlation_uuid` = ?");
      values.push(criteria.correlationUuid);
    }
    if (criteria.correlationUuidIsNull !== undefined) {
      conditions.push(
        criteria.correlationUuidIsNull
          ? "`correlation_uuid` IS NULL"
          : "`correlation_uuid` IS NOT NULL",
      );
    }
    if (criteria.observedAt !== undefined) {
      conditions.push("`observed_at` = ?");
      values.push(criteria.observedAt);
    }
    if (criteria.observedAtFrom !== undefined) {
      conditions.push("`observed_at` >= ?");
      values.push(criteria.observedAtFrom);
    }
    if (criteria.observedAtTo !== undefined) {
      conditions.push("`observed_at` <= ?");
      values.push(criteria.observedAtTo);
    }
    if (criteria.detail !== undefined) {
      conditions.push("`detail` = ?");
      values.push(criteria.detail);
    }
    if (criteria.detailLike !== undefined) {
      conditions.push("`detail` LIKE ?");
      values.push(criteria.detailLike);
    }
    if (criteria.detailIsNull !== undefined) {
      conditions.push(
        criteria.detailIsNull
          ? "`detail` IS NULL"
          : "`detail` IS NOT NULL",
      );
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              auditEventColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
