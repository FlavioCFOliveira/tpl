// Code generated by tpl from freight.document. DO NOT EDIT.

/**
 * The mysql2 implementation of the `freight.document` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { Document, DocumentFilter, DocumentKey, DocumentOrder } from './document.js' */

import { NotFound, directionSql, failure } from './core.js';
import { documentColumnSql } from './document.js';
import { DocumentRepository } from './document_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "freight.document";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `document_id`, `declaration_id`, `document_kind`, `file_name`, `media_type`, `byte_size`, `content_sha256`, `detached_signature`, `signature_thumbnail`, `page_preview`, `scanned_original`, `archive_bundle`, `uploaded_by`, `uploaded_at`, `retention_until` FROM `document`";

/** Selects the one row a primary key names. */
const READ = "SELECT `document_id`, `declaration_id`, `document_kind`, `file_name`, `media_type`, `byte_size`, `content_sha256`, `detached_signature`, `signature_thumbnail`, `page_preview`, `scanned_original`, `archive_bundle`, `uploaded_by`, `uploaded_at`, `retention_until` FROM `document` WHERE `document_id` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `document` (`declaration_id`, `document_kind`, `file_name`, `media_type`, `byte_size`, `content_sha256`, `detached_signature`, `signature_thumbnail`, `page_preview`, `scanned_original`, `archive_bundle`, `uploaded_by`, `uploaded_at`, `retention_until`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `document` SET `declaration_id` = ?, `document_kind` = ?, `file_name` = ?, `media_type` = ?, `byte_size` = ?, `content_sha256` = ?, `detached_signature` = ?, `signature_thumbnail` = ?, `page_preview` = ?, `scanned_original` = ?, `archive_bundle` = ?, `uploaded_by` = ?, `uploaded_at` = ?, `retention_until` = ? WHERE `document_id` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `document` WHERE `document_id` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `Document` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {Document}
 */
function toRow(values) {
  return {
    documentId: values[0],
    declarationId: values[1],
    documentKind: values[2],
    fileName: values[3],
    mediaType: values[4],
    byteSize: values[5],
    contentSha256: values[6],
    detachedSignature: values[7],
    signatureThumbnail: values[8],
    pagePreview: values[9],
    scannedOriginal: values[10],
    archiveBundle: values[11],
    uploadedBy: values[12],
    uploadedAt: values[13],
    retentionUntil: values[14],
  };
}

/**
 * The mysql2 data layer of `freight.document`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class DocumentMysql2 extends DocumentRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `freight.document` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The key is assigned by the server and is written back into `row`.
   *
   * @override
   * @param {Document} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.declarationId,
      row.documentKind,
      row.fileName,
      row.mediaType,
      row.byteSize,
      row.contentSha256,
      row.detachedSignature,
      row.signatureThumbnail,
      row.pagePreview,
      row.scannedOriginal,
      row.archiveBundle,
      row.uploadedBy,
      row.uploadedAt,
      row.retentionUntil,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
      const { insertId } = /** @type {ResultSetHeader} */ (result);
      // The key is a BIGINT and every row of this package carries one as a
      // `string`, because `connect` sets `bigNumberStrings`. mysql2 reports
      // `insertId` as a `number` whatever the column is, so it is converted
      // here rather than left to widen the row's type.
      row.documentId = String(insertId);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {DocumentKey} key
   * @returns {Promise<Document>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.documentId,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {Document} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.declarationId,
      row.documentKind,
      row.fileName,
      row.mediaType,
      row.byteSize,
      row.contentSha256,
      row.detachedSignature,
      row.signatureThumbnail,
      row.pagePreview,
      row.scannedOriginal,
      row.archiveBundle,
      row.uploadedBy,
      row.uploadedAt,
      row.retentionUntil,
      row.documentId,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {DocumentKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.documentId,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {DocumentFilter} criteria
   * @param {readonly DocumentOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<Document[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.documentId !== undefined) {
      conditions.push("`document_id` = ?");
      values.push(criteria.documentId);
    }
    if (criteria.documentIdFrom !== undefined) {
      conditions.push("`document_id` >= ?");
      values.push(criteria.documentIdFrom);
    }
    if (criteria.documentIdTo !== undefined) {
      conditions.push("`document_id` <= ?");
      values.push(criteria.documentIdTo);
    }
    if (criteria.declarationId !== undefined) {
      conditions.push("`declaration_id` = ?");
      values.push(criteria.declarationId);
    }
    if (criteria.declarationIdFrom !== undefined) {
      conditions.push("`declaration_id` >= ?");
      values.push(criteria.declarationIdFrom);
    }
    if (criteria.declarationIdTo !== undefined) {
      conditions.push("`declaration_id` <= ?");
      values.push(criteria.declarationIdTo);
    }
    if (criteria.declarationIdIsNull !== undefined) {
      conditions.push(
        criteria.declarationIdIsNull
          ? "`declaration_id` IS NULL"
          : "`declaration_id` IS NOT NULL",
      );
    }
    if (criteria.documentKind !== undefined) {
      conditions.push("`document_kind` = ?");
      values.push(criteria.documentKind);
    }
    if (criteria.documentKindLike !== undefined) {
      conditions.push("`document_kind` LIKE ?");
      values.push(criteria.documentKindLike);
    }
    if (criteria.fileName !== undefined) {
      conditions.push("`file_name` = ?");
      values.push(criteria.fileName);
    }
    if (criteria.fileNameLike !== undefined) {
      conditions.push("`file_name` LIKE ?");
      values.push(criteria.fileNameLike);
    }
    if (criteria.mediaType !== undefined) {
      conditions.push("`media_type` = ?");
      values.push(criteria.mediaType);
    }
    if (criteria.mediaTypeLike !== undefined) {
      conditions.push("`media_type` LIKE ?");
      values.push(criteria.mediaTypeLike);
    }
    if (criteria.byteSize !== undefined) {
      conditions.push("`byte_size` = ?");
      values.push(criteria.byteSize);
    }
    if (criteria.byteSizeFrom !== undefined) {
      conditions.push("`byte_size` >= ?");
      values.push(criteria.byteSizeFrom);
    }
    if (criteria.byteSizeTo !== undefined) {
      conditions.push("`byte_size` <= ?");
      values.push(criteria.byteSizeTo);
    }
    if (criteria.contentSha256 !== undefined) {
      conditions.push("`content_sha256` = ?");
      values.push(criteria.contentSha256);
    }
    if (criteria.detachedSignature !== undefined) {
      conditions.push("`detached_signature` = ?");
      values.push(criteria.detachedSignature);
    }
    if (criteria.detachedSignatureIsNull !== undefined) {
      conditions.push(
        criteria.detachedSignatureIsNull
          ? "`detached_signature` IS NULL"
          : "`detached_signature` IS NOT NULL",
      );
    }
    if (criteria.signatureThumbnail !== undefined) {
      conditions.push("`signature_thumbnail` = ?");
      values.push(criteria.signatureThumbnail);
    }
    if (criteria.signatureThumbnailIsNull !== undefined) {
      conditions.push(
        criteria.signatureThumbnailIsNull
          ? "`signature_thumbnail` IS NULL"
          : "`signature_thumbnail` IS NOT NULL",
      );
    }
    if (criteria.pagePreview !== undefined) {
      conditions.push("`page_preview` = ?");
      values.push(criteria.pagePreview);
    }
    if (criteria.pagePreviewIsNull !== undefined) {
      conditions.push(
        criteria.pagePreviewIsNull
          ? "`page_preview` IS NULL"
          : "`page_preview` IS NOT NULL",
      );
    }
    if (criteria.scannedOriginal !== undefined) {
      conditions.push("`scanned_original` = ?");
      values.push(criteria.scannedOriginal);
    }
    if (criteria.scannedOriginalIsNull !== undefined) {
      conditions.push(
        criteria.scannedOriginalIsNull
          ? "`scanned_original` IS NULL"
          : "`scanned_original` IS NOT NULL",
      );
    }
    if (criteria.archiveBundle !== undefined) {
      conditions.push("`archive_bundle` = ?");
      values.push(criteria.archiveBundle);
    }
    if (criteria.archiveBundleIsNull !== undefined) {
      conditions.push(
        criteria.archiveBundleIsNull
          ? "`archive_bundle` IS NULL"
          : "`archive_bundle` IS NOT NULL",
      );
    }
    if (criteria.uploadedBy !== undefined) {
      conditions.push("`uploaded_by` = ?");
      values.push(criteria.uploadedBy);
    }
    if (criteria.uploadedByLike !== undefined) {
      conditions.push("`uploaded_by` LIKE ?");
      values.push(criteria.uploadedByLike);
    }
    if (criteria.uploadedAt !== undefined) {
      conditions.push("`uploaded_at` = ?");
      values.push(criteria.uploadedAt);
    }
    if (criteria.uploadedAtFrom !== undefined) {
      conditions.push("`uploaded_at` >= ?");
      values.push(criteria.uploadedAtFrom);
    }
    if (criteria.uploadedAtTo !== undefined) {
      conditions.push("`uploaded_at` <= ?");
      values.push(criteria.uploadedAtTo);
    }
    if (criteria.retentionUntil !== undefined) {
      conditions.push("`retention_until` = ?");
      values.push(criteria.retentionUntil);
    }
    if (criteria.retentionUntilFrom !== undefined) {
      conditions.push("`retention_until` >= ?");
      values.push(criteria.retentionUntilFrom);
    }
    if (criteria.retentionUntilTo !== undefined) {
      conditions.push("`retention_until` <= ?");
      values.push(criteria.retentionUntilTo);
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              documentColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
