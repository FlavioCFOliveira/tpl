// Code generated by tpl from freight.voyage. DO NOT EDIT.

/**
 * The `freight.voyage` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

// The table's own comment, as the catalogue carries it:
// Viagens programadas por navio e número de viagem

/**
 * One row of `freight.voyage`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} Voyage
 * @property {string} vesselImo `vessel_imo`, declared `char(7)` NOT NULL.
 *   It is part of the primary key.
 * @property {string} voyageNumber `voyage_number`, declared `varchar(12)` NOT NULL.
 *   It is part of the primary key.
 *   Comment: Carrier voyage number, e.g. 2026-014W; W and E denote direction
 * @property {string} serviceCode `service_code`, declared `char(4)` NOT NULL.
 *   Comment: Liner service the voyage is operated under
 * @property {Date} etd `etd`, declared `datetime` NOT NULL.
 *   Comment: Estimated time of departure from the first load port
 * @property {Date} eta `eta`, declared `datetime` NOT NULL.
 *   Comment: Estimated time of arrival at the last discharge port
 * @property {Date | null} atd `atd`, declared `datetime` NULL.
 *   Comment: Actual time of departure; NULL until the vessel sails
 * @property {string} status `status`, declared `enum('Planned','Announced','Sailing','Completed','Cancelled')` NOT NULL.
 * @property {string | null} bunkerPriceUsdT `bunker_price_usd_t`, declared `decimal(9,2)` NULL.
 *   Comment: VLSFO price used for the bunker adjustment factor
 * @property {number} slotAllocationTeu `slot_allocation_teu`, declared `mediumint(8) unsigned` NOT NULL.
 * @property {string | null} utilisationPct `utilisation_pct`, declared `decimal(5,2)` NULL.
 */

/**
 * The primary key of `freight.voyage`.
 *
 * @typedef {object} VoyageKey
 * @property {string} vesselImo `vessel_imo`, declared `char(7)` NOT NULL.
 * @property {string} voyageNumber `voyage_number`, declared `varchar(12)` NOT NULL.
 */

/**
 * One column of `freight.voyage`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link VoyageColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const VoyageColumns = Object.freeze(
  /** @type {const} */ ({
    /** `vessel_imo`, declared `char(7)` NOT NULL. */
    VESSEL_IMO: "vessel_imo",
    /** `voyage_number`, declared `varchar(12)` NOT NULL. */
    VOYAGE_NUMBER: "voyage_number",
    /** `service_code`, declared `char(4)` NOT NULL. */
    SERVICE_CODE: "service_code",
    /** `etd`, declared `datetime` NOT NULL. */
    ETD: "etd",
    /** `eta`, declared `datetime` NOT NULL. */
    ETA: "eta",
    /** `atd`, declared `datetime` NULL. */
    ATD: "atd",
    /** `status`, declared `enum('Planned','Announced','Sailing','Completed','Cancelled')` NOT NULL. */
    STATUS: "status",
    /** `bunker_price_usd_t`, declared `decimal(9,2)` NULL. */
    BUNKER_PRICE_USD_T: "bunker_price_usd_t",
    /** `slot_allocation_teu`, declared `mediumint(8) unsigned` NOT NULL. */
    SLOT_ALLOCATION_TEU: "slot_allocation_teu",
    /** `utilisation_pct`, declared `decimal(5,2)` NULL. */
    UTILISATION_PCT: "utilisation_pct",
  }),
);

/**
 * A column of `freight.voyage` an ordering may name.
 *
 * @typedef {(typeof VoyageColumns)[keyof typeof VoyageColumns]} VoyageColumn
 */

/**
 * The one place a column of `freight.voyage` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<VoyageColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "vessel_imo": "`vessel_imo`",
  "voyage_number": "`voyage_number`",
  "service_code": "`service_code`",
  "etd": "`etd`",
  "eta": "`eta`",
  "atd": "`atd`",
  "status": "`status`",
  "bunker_price_usd_t": "`bunker_price_usd_t`",
  "slot_allocation_teu": "`slot_allocation_teu`",
  "utilisation_pct": "`utilisation_pct`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {VoyageColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function voyageColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("freight.voyage", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `freight.voyage`.
 *
 * @typedef {object} VoyageOrder
 * @property {VoyageColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `freight.voyage`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} VoyageFilter
 * @property {string} [vesselImo] where set, requires `vessel_imo` to equal it.
 * @property {string} [vesselImoLike] where set, requires `vessel_imo` to match it, as LIKE matches.
 * @property {string} [voyageNumber] where set, requires `voyage_number` to equal it.
 * @property {string} [voyageNumberLike] where set, requires `voyage_number` to match it, as LIKE matches.
 * @property {string} [serviceCode] where set, requires `service_code` to equal it.
 * @property {string} [serviceCodeLike] where set, requires `service_code` to match it, as LIKE matches.
 * @property {Date} [etd] where set, requires `etd` to equal it.
 * @property {Date} [etdFrom] where set, requires `etd` to be at least it.
 * @property {Date} [etdTo] where set, requires `etd` to be at most it.
 * @property {Date} [eta] where set, requires `eta` to equal it.
 * @property {Date} [etaFrom] where set, requires `eta` to be at least it.
 * @property {Date} [etaTo] where set, requires `eta` to be at most it.
 * @property {Date} [atd] where set, requires `atd` to equal it.
 * @property {Date} [atdFrom] where set, requires `atd` to be at least it.
 * @property {Date} [atdTo] where set, requires `atd` to be at most it.
 * @property {boolean} [atdIsNull] where true, requires `atd` to be NULL; where false, requires it not to be.
 * @property {string} [status] where set, requires `status` to equal it.
 * @property {string} [statusLike] where set, requires `status` to match it, as LIKE matches.
 * @property {string} [bunkerPriceUsdT] where set, requires `bunker_price_usd_t` to equal it.
 * @property {string} [bunkerPriceUsdTFrom] where set, requires `bunker_price_usd_t` to be at least it.
 * @property {string} [bunkerPriceUsdTTo] where set, requires `bunker_price_usd_t` to be at most it.
 * @property {boolean} [bunkerPriceUsdTIsNull] where true, requires `bunker_price_usd_t` to be NULL; where false, requires it not to be.
 * @property {number} [slotAllocationTeu] where set, requires `slot_allocation_teu` to equal it.
 * @property {number} [slotAllocationTeuFrom] where set, requires `slot_allocation_teu` to be at least it.
 * @property {number} [slotAllocationTeuTo] where set, requires `slot_allocation_teu` to be at most it.
 * @property {string} [utilisationPct] where set, requires `utilisation_pct` to equal it.
 * @property {string} [utilisationPctFrom] where set, requires `utilisation_pct` to be at least it.
 * @property {string} [utilisationPctTo] where set, requires `utilisation_pct` to be at most it.
 * @property {boolean} [utilisationPctIsNull] where true, requires `utilisation_pct` to be NULL; where false, requires it not to be.
 */
