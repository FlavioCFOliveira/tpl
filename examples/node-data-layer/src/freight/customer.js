// Code generated by tpl from freight.customer. DO NOT EDIT.

/**
 * The `freight.customer` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

// The table's own comment, as the catalogue carries it:
// Clientes expedidores e destinatários, com limite de crédito e preferências do portal

/**
 * One row of `freight.customer`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} Customer
 * @property {number} customerId `customer_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT.
 *   It is part of the primary key.
 * @property {string} legalName `legal_name`, declared `varchar(160)` NOT NULL.
 *   Comment: Registered company name as it appears on the bill of lading
 * @property {string | null} tradingName `trading_name`, declared `varchar(160)` NULL.
 * @property {string} vatNumber `vat_number`, declared `varchar(20)` NOT NULL.
 *   It is part of a unique index.
 * @property {string | null} eoriNumber `eori_number`, declared `varchar(17)` NULL.
 *   Comment: Economic Operators Registration and Identification number
 * @property {string} countryCode `country_code`, declared `char(2)` NOT NULL.
 *   It is part of a unique index.
 * @property {string | null} legacyAccountCode `legacy_account_code`, declared `varchar(24)` NULL INVISIBLE.
 *   Comment: INVISIBLE: account code from the 1998 AS/400 ledger, kept for reconciliation only
 * @property {string} portalUuid `portal_uuid`, declared `uuid` NOT NULL.
 *   It is part of a unique index.
 *   Comment: Opaque identifier exposed to the customer portal
 * @property {string | null} lastLoginIpv4 `last_login_ipv4`, declared `inet4` NULL.
 * @property {string | null} lastLoginIpv6 `last_login_ipv6`, declared `inet6` NULL.
 * @property {string} creditLimitEur `credit_limit_eur`, declared `decimal(14,2)` NOT NULL.
 * @property {string} outstandingEur `outstanding_eur`, declared `decimal(14,2)` NOT NULL.
 * @property {string | null} creditHeadroomEur `credit_headroom_eur`, declared `decimal(14,2)` NULL STORED GENERATED AS (`credit_limit_eur` - `outstanding_eur`).
 *   Comment: Stored generated column: what the sales desk is allowed to book today
 * @property {string | null} riskScore `risk_score`, declared `decimal(5,2)` NULL.
 *   Comment: Internal credit risk, 0.00 best to 100.00 worst
 * @property {number} paymentTermsDays `payment_terms_days`, declared `smallint(5) unsigned` NOT NULL.
 * @property {string | null} preferences `preferences`, declared `longtext` NULL.
 *   Comment: Portal preferences; MariaDB implements JSON as LONGTEXT plus a json_valid CHECK
 * @property {Buffer} notificationFlags `notification_flags`, declared `bit(8)` NOT NULL.
 *   Comment: Bit 0 e-mail, bit 1 SMS, bit 2 EDI, bit 3 portal
 * @property {number} isActive `is_active`, declared `tinyint(1)` NOT NULL.
 * @property {Date} onboardedOn `onboarded_on`, declared `date` NOT NULL.
 * @property {Date} reviewDueOn `review_due_on`, declared `date` NOT NULL.
 *   Comment: Expression default: annual KYC review
 * @property {string | null} contactNote `contact_note`, declared `varchar(255)` NULL.
 *   Comment: Column charset differs from the table default: utf8mb3 from the legacy CRM import
 * @property {Date} createdAt `created_at`, declared `datetime(6)` NOT NULL.
 * @property {Date} updatedAt `updated_at`, declared `timestamp` NOT NULL.
 */

/**
 * The primary key of `freight.customer`.
 *
 * @typedef {object} CustomerKey
 * @property {number} customerId `customer_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT.
 */

/**
 * One column of `freight.customer`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link CustomerColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const CustomerColumns = Object.freeze(
  /** @type {const} */ ({
    /** `customer_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT. */
    CUSTOMER_ID: "customer_id",
    /** `legal_name`, declared `varchar(160)` NOT NULL. */
    LEGAL_NAME: "legal_name",
    /** `trading_name`, declared `varchar(160)` NULL. */
    TRADING_NAME: "trading_name",
    /** `vat_number`, declared `varchar(20)` NOT NULL. */
    VAT_NUMBER: "vat_number",
    /** `eori_number`, declared `varchar(17)` NULL. */
    EORI_NUMBER: "eori_number",
    /** `country_code`, declared `char(2)` NOT NULL. */
    COUNTRY_CODE: "country_code",
    /** `legacy_account_code`, declared `varchar(24)` NULL INVISIBLE. */
    LEGACY_ACCOUNT_CODE: "legacy_account_code",
    /** `portal_uuid`, declared `uuid` NOT NULL. */
    PORTAL_UUID: "portal_uuid",
    /** `last_login_ipv4`, declared `inet4` NULL. */
    LAST_LOGIN_IPV4: "last_login_ipv4",
    /** `last_login_ipv6`, declared `inet6` NULL. */
    LAST_LOGIN_IPV6: "last_login_ipv6",
    /** `credit_limit_eur`, declared `decimal(14,2)` NOT NULL. */
    CREDIT_LIMIT_EUR: "credit_limit_eur",
    /** `outstanding_eur`, declared `decimal(14,2)` NOT NULL. */
    OUTSTANDING_EUR: "outstanding_eur",
    /** `credit_headroom_eur`, declared `decimal(14,2)` NULL STORED GENERATED AS (`credit_limit_eur` - `outstanding_eur`). */
    CREDIT_HEADROOM_EUR: "credit_headroom_eur",
    /** `risk_score`, declared `decimal(5,2)` NULL. */
    RISK_SCORE: "risk_score",
    /** `payment_terms_days`, declared `smallint(5) unsigned` NOT NULL. */
    PAYMENT_TERMS_DAYS: "payment_terms_days",
    /** `preferences`, declared `longtext` NULL. */
    PREFERENCES: "preferences",
    /** `notification_flags`, declared `bit(8)` NOT NULL. */
    NOTIFICATION_FLAGS: "notification_flags",
    /** `is_active`, declared `tinyint(1)` NOT NULL. */
    IS_ACTIVE: "is_active",
    /** `onboarded_on`, declared `date` NOT NULL. */
    ONBOARDED_ON: "onboarded_on",
    /** `review_due_on`, declared `date` NOT NULL. */
    REVIEW_DUE_ON: "review_due_on",
    /** `contact_note`, declared `varchar(255)` NULL. */
    CONTACT_NOTE: "contact_note",
    /** `created_at`, declared `datetime(6)` NOT NULL. */
    CREATED_AT: "created_at",
    /** `updated_at`, declared `timestamp` NOT NULL. */
    UPDATED_AT: "updated_at",
  }),
);

/**
 * A column of `freight.customer` an ordering may name.
 *
 * @typedef {(typeof CustomerColumns)[keyof typeof CustomerColumns]} CustomerColumn
 */

/**
 * The one place a column of `freight.customer` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<CustomerColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "customer_id": "`customer_id`",
  "legal_name": "`legal_name`",
  "trading_name": "`trading_name`",
  "vat_number": "`vat_number`",
  "eori_number": "`eori_number`",
  "country_code": "`country_code`",
  "legacy_account_code": "`legacy_account_code`",
  "portal_uuid": "`portal_uuid`",
  "last_login_ipv4": "`last_login_ipv4`",
  "last_login_ipv6": "`last_login_ipv6`",
  "credit_limit_eur": "`credit_limit_eur`",
  "outstanding_eur": "`outstanding_eur`",
  "credit_headroom_eur": "`credit_headroom_eur`",
  "risk_score": "`risk_score`",
  "payment_terms_days": "`payment_terms_days`",
  "preferences": "`preferences`",
  "notification_flags": "`notification_flags`",
  "is_active": "`is_active`",
  "onboarded_on": "`onboarded_on`",
  "review_due_on": "`review_due_on`",
  "contact_note": "`contact_note`",
  "created_at": "`created_at`",
  "updated_at": "`updated_at`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {CustomerColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function customerColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("freight.customer", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `freight.customer`.
 *
 * @typedef {object} CustomerOrder
 * @property {CustomerColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `freight.customer`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} CustomerFilter
 * @property {number} [customerId] where set, requires `customer_id` to equal it.
 * @property {number} [customerIdFrom] where set, requires `customer_id` to be at least it.
 * @property {number} [customerIdTo] where set, requires `customer_id` to be at most it.
 * @property {string} [legalName] where set, requires `legal_name` to equal it.
 * @property {string} [legalNameLike] where set, requires `legal_name` to match it, as LIKE matches.
 * @property {string} [tradingName] where set, requires `trading_name` to equal it.
 * @property {string} [tradingNameLike] where set, requires `trading_name` to match it, as LIKE matches.
 * @property {boolean} [tradingNameIsNull] where true, requires `trading_name` to be NULL; where false, requires it not to be.
 * @property {string} [vatNumber] where set, requires `vat_number` to equal it.
 * @property {string} [vatNumberLike] where set, requires `vat_number` to match it, as LIKE matches.
 * @property {string} [eoriNumber] where set, requires `eori_number` to equal it.
 * @property {string} [eoriNumberLike] where set, requires `eori_number` to match it, as LIKE matches.
 * @property {boolean} [eoriNumberIsNull] where true, requires `eori_number` to be NULL; where false, requires it not to be.
 * @property {string} [countryCode] where set, requires `country_code` to equal it.
 * @property {string} [countryCodeLike] where set, requires `country_code` to match it, as LIKE matches.
 * @property {string} [legacyAccountCode] where set, requires `legacy_account_code` to equal it.
 * @property {string} [legacyAccountCodeLike] where set, requires `legacy_account_code` to match it, as LIKE matches.
 * @property {boolean} [legacyAccountCodeIsNull] where true, requires `legacy_account_code` to be NULL; where false, requires it not to be.
 * @property {string} [portalUuid] where set, requires `portal_uuid` to equal it.
 * @property {string} [lastLoginIpv4] where set, requires `last_login_ipv4` to equal it.
 * @property {boolean} [lastLoginIpv4IsNull] where true, requires `last_login_ipv4` to be NULL; where false, requires it not to be.
 * @property {string} [lastLoginIpv6] where set, requires `last_login_ipv6` to equal it.
 * @property {boolean} [lastLoginIpv6IsNull] where true, requires `last_login_ipv6` to be NULL; where false, requires it not to be.
 * @property {string} [creditLimitEur] where set, requires `credit_limit_eur` to equal it.
 * @property {string} [creditLimitEurFrom] where set, requires `credit_limit_eur` to be at least it.
 * @property {string} [creditLimitEurTo] where set, requires `credit_limit_eur` to be at most it.
 * @property {string} [outstandingEur] where set, requires `outstanding_eur` to equal it.
 * @property {string} [outstandingEurFrom] where set, requires `outstanding_eur` to be at least it.
 * @property {string} [outstandingEurTo] where set, requires `outstanding_eur` to be at most it.
 * @property {string} [creditHeadroomEur] where set, requires `credit_headroom_eur` to equal it.
 * @property {string} [creditHeadroomEurFrom] where set, requires `credit_headroom_eur` to be at least it.
 * @property {string} [creditHeadroomEurTo] where set, requires `credit_headroom_eur` to be at most it.
 * @property {boolean} [creditHeadroomEurIsNull] where true, requires `credit_headroom_eur` to be NULL; where false, requires it not to be.
 * @property {string} [riskScore] where set, requires `risk_score` to equal it.
 * @property {string} [riskScoreFrom] where set, requires `risk_score` to be at least it.
 * @property {string} [riskScoreTo] where set, requires `risk_score` to be at most it.
 * @property {boolean} [riskScoreIsNull] where true, requires `risk_score` to be NULL; where false, requires it not to be.
 * @property {number} [paymentTermsDays] where set, requires `payment_terms_days` to equal it.
 * @property {number} [paymentTermsDaysFrom] where set, requires `payment_terms_days` to be at least it.
 * @property {number} [paymentTermsDaysTo] where set, requires `payment_terms_days` to be at most it.
 * @property {string} [preferences] where set, requires `preferences` to equal it.
 * @property {string} [preferencesLike] where set, requires `preferences` to match it, as LIKE matches.
 * @property {boolean} [preferencesIsNull] where true, requires `preferences` to be NULL; where false, requires it not to be.
 * @property {Buffer} [notificationFlags] where set, requires `notification_flags` to equal it.
 * @property {Buffer} [notificationFlagsFrom] where set, requires `notification_flags` to be at least it.
 * @property {Buffer} [notificationFlagsTo] where set, requires `notification_flags` to be at most it.
 * @property {number} [isActive] where set, requires `is_active` to equal it.
 * @property {number} [isActiveFrom] where set, requires `is_active` to be at least it.
 * @property {number} [isActiveTo] where set, requires `is_active` to be at most it.
 * @property {Date} [onboardedOn] where set, requires `onboarded_on` to equal it.
 * @property {Date} [onboardedOnFrom] where set, requires `onboarded_on` to be at least it.
 * @property {Date} [onboardedOnTo] where set, requires `onboarded_on` to be at most it.
 * @property {Date} [reviewDueOn] where set, requires `review_due_on` to equal it.
 * @property {Date} [reviewDueOnFrom] where set, requires `review_due_on` to be at least it.
 * @property {Date} [reviewDueOnTo] where set, requires `review_due_on` to be at most it.
 * @property {string} [contactNote] where set, requires `contact_note` to equal it.
 * @property {string} [contactNoteLike] where set, requires `contact_note` to match it, as LIKE matches.
 * @property {boolean} [contactNoteIsNull] where true, requires `contact_note` to be NULL; where false, requires it not to be.
 * @property {Date} [createdAt] where set, requires `created_at` to equal it.
 * @property {Date} [createdAtFrom] where set, requires `created_at` to be at least it.
 * @property {Date} [createdAtTo] where set, requires `created_at` to be at most it.
 * @property {Date} [updatedAt] where set, requires `updated_at` to equal it.
 * @property {Date} [updatedAtFrom] where set, requires `updated_at` to be at least it.
 * @property {Date} [updatedAtTo] where set, requires `updated_at` to be at most it.
 */
