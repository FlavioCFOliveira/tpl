// Code generated by tpl from freight.container. DO NOT EDIT.

/**
 * The mysql2 implementation of the `freight.container` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { Container, ContainerFilter, ContainerKey, ContainerOrder } from './container.js' */

import { NotFound, directionSql, failure } from './core.js';
import { containerColumnSql } from './container.js';
import { ContainerRepository } from './container_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "freight.container";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `container_id`, `equipment_no`, `consignment_id`, `iso_code`, `seal_number`, `tare_weight_kg`, `cargo_weight_kg`, `vgm_kg`, `vgm_method`, `setpoint_celsius`, `humidity_pct`, `vent_setting`, `stow_position`, `loaded_at` FROM `container`";

/** Selects the one row a primary key names. */
const READ = "SELECT `container_id`, `equipment_no`, `consignment_id`, `iso_code`, `seal_number`, `tare_weight_kg`, `cargo_weight_kg`, `vgm_kg`, `vgm_method`, `setpoint_celsius`, `humidity_pct`, `vent_setting`, `stow_position`, `loaded_at` FROM `container` WHERE `container_id` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `container` (`equipment_no`, `consignment_id`, `iso_code`, `seal_number`, `tare_weight_kg`, `cargo_weight_kg`, `vgm_method`, `setpoint_celsius`, `humidity_pct`, `vent_setting`, `stow_position`, `loaded_at`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `container` SET `equipment_no` = ?, `consignment_id` = ?, `iso_code` = ?, `seal_number` = ?, `tare_weight_kg` = ?, `cargo_weight_kg` = ?, `vgm_method` = ?, `setpoint_celsius` = ?, `humidity_pct` = ?, `vent_setting` = ?, `stow_position` = ?, `loaded_at` = ? WHERE `container_id` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `container` WHERE `container_id` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `Container` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {Container}
 */
function toRow(values) {
  return {
    containerId: values[0],
    equipmentNo: values[1],
    consignmentId: values[2],
    isoCode: values[3],
    sealNumber: values[4],
    tareWeightKg: values[5],
    cargoWeightKg: values[6],
    vgmKg: values[7],
    vgmMethod: values[8],
    setpointCelsius: values[9],
    humidityPct: values[10],
    ventSetting: values[11],
    stowPosition: values[12],
    loadedAt: values[13],
  };
}

/**
 * The mysql2 data layer of `freight.container`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class ContainerMysql2 extends ContainerRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `freight.container` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The key is assigned by the server and is written back into `row`.
   *
   * @override
   * @param {Container} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.equipmentNo,
      row.consignmentId,
      row.isoCode,
      row.sealNumber,
      row.tareWeightKg,
      row.cargoWeightKg,
      row.vgmMethod,
      row.setpointCelsius,
      row.humidityPct,
      row.ventSetting,
      row.stowPosition,
      row.loadedAt,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
      const { insertId } = /** @type {ResultSetHeader} */ (result);
      // The key is a BIGINT and every row of this package carries one as a
      // `string`, because `connect` sets `bigNumberStrings`. mysql2 reports
      // `insertId` as a `number` whatever the column is, so it is converted
      // here rather than left to widen the row's type.
      row.containerId = String(insertId);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {ContainerKey} key
   * @returns {Promise<Container>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.containerId,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {Container} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.equipmentNo,
      row.consignmentId,
      row.isoCode,
      row.sealNumber,
      row.tareWeightKg,
      row.cargoWeightKg,
      row.vgmMethod,
      row.setpointCelsius,
      row.humidityPct,
      row.ventSetting,
      row.stowPosition,
      row.loadedAt,
      row.containerId,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {ContainerKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.containerId,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {ContainerFilter} criteria
   * @param {readonly ContainerOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<Container[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.containerId !== undefined) {
      conditions.push("`container_id` = ?");
      values.push(criteria.containerId);
    }
    if (criteria.containerIdFrom !== undefined) {
      conditions.push("`container_id` >= ?");
      values.push(criteria.containerIdFrom);
    }
    if (criteria.containerIdTo !== undefined) {
      conditions.push("`container_id` <= ?");
      values.push(criteria.containerIdTo);
    }
    if (criteria.equipmentNo !== undefined) {
      conditions.push("`equipment_no` = ?");
      values.push(criteria.equipmentNo);
    }
    if (criteria.equipmentNoLike !== undefined) {
      conditions.push("`equipment_no` LIKE ?");
      values.push(criteria.equipmentNoLike);
    }
    if (criteria.consignmentId !== undefined) {
      conditions.push("`consignment_id` = ?");
      values.push(criteria.consignmentId);
    }
    if (criteria.consignmentIdFrom !== undefined) {
      conditions.push("`consignment_id` >= ?");
      values.push(criteria.consignmentIdFrom);
    }
    if (criteria.consignmentIdTo !== undefined) {
      conditions.push("`consignment_id` <= ?");
      values.push(criteria.consignmentIdTo);
    }
    if (criteria.isoCode !== undefined) {
      conditions.push("`iso_code` = ?");
      values.push(criteria.isoCode);
    }
    if (criteria.isoCodeLike !== undefined) {
      conditions.push("`iso_code` LIKE ?");
      values.push(criteria.isoCodeLike);
    }
    if (criteria.sealNumber !== undefined) {
      conditions.push("`seal_number` = ?");
      values.push(criteria.sealNumber);
    }
    if (criteria.sealNumberLike !== undefined) {
      conditions.push("`seal_number` LIKE ?");
      values.push(criteria.sealNumberLike);
    }
    if (criteria.sealNumberIsNull !== undefined) {
      conditions.push(
        criteria.sealNumberIsNull
          ? "`seal_number` IS NULL"
          : "`seal_number` IS NOT NULL",
      );
    }
    if (criteria.tareWeightKg !== undefined) {
      conditions.push("`tare_weight_kg` = ?");
      values.push(criteria.tareWeightKg);
    }
    if (criteria.tareWeightKgFrom !== undefined) {
      conditions.push("`tare_weight_kg` >= ?");
      values.push(criteria.tareWeightKgFrom);
    }
    if (criteria.tareWeightKgTo !== undefined) {
      conditions.push("`tare_weight_kg` <= ?");
      values.push(criteria.tareWeightKgTo);
    }
    if (criteria.cargoWeightKg !== undefined) {
      conditions.push("`cargo_weight_kg` = ?");
      values.push(criteria.cargoWeightKg);
    }
    if (criteria.cargoWeightKgFrom !== undefined) {
      conditions.push("`cargo_weight_kg` >= ?");
      values.push(criteria.cargoWeightKgFrom);
    }
    if (criteria.cargoWeightKgTo !== undefined) {
      conditions.push("`cargo_weight_kg` <= ?");
      values.push(criteria.cargoWeightKgTo);
    }
    if (criteria.vgmKg !== undefined) {
      conditions.push("`vgm_kg` = ?");
      values.push(criteria.vgmKg);
    }
    if (criteria.vgmKgFrom !== undefined) {
      conditions.push("`vgm_kg` >= ?");
      values.push(criteria.vgmKgFrom);
    }
    if (criteria.vgmKgTo !== undefined) {
      conditions.push("`vgm_kg` <= ?");
      values.push(criteria.vgmKgTo);
    }
    if (criteria.vgmKgIsNull !== undefined) {
      conditions.push(
        criteria.vgmKgIsNull
          ? "`vgm_kg` IS NULL"
          : "`vgm_kg` IS NOT NULL",
      );
    }
    if (criteria.vgmMethod !== undefined) {
      conditions.push("`vgm_method` = ?");
      values.push(criteria.vgmMethod);
    }
    if (criteria.vgmMethodLike !== undefined) {
      conditions.push("`vgm_method` LIKE ?");
      values.push(criteria.vgmMethodLike);
    }
    if (criteria.setpointCelsius !== undefined) {
      conditions.push("`setpoint_celsius` = ?");
      values.push(criteria.setpointCelsius);
    }
    if (criteria.setpointCelsiusFrom !== undefined) {
      conditions.push("`setpoint_celsius` >= ?");
      values.push(criteria.setpointCelsiusFrom);
    }
    if (criteria.setpointCelsiusTo !== undefined) {
      conditions.push("`setpoint_celsius` <= ?");
      values.push(criteria.setpointCelsiusTo);
    }
    if (criteria.setpointCelsiusIsNull !== undefined) {
      conditions.push(
        criteria.setpointCelsiusIsNull
          ? "`setpoint_celsius` IS NULL"
          : "`setpoint_celsius` IS NOT NULL",
      );
    }
    if (criteria.humidityPct !== undefined) {
      conditions.push("`humidity_pct` = ?");
      values.push(criteria.humidityPct);
    }
    if (criteria.humidityPctFrom !== undefined) {
      conditions.push("`humidity_pct` >= ?");
      values.push(criteria.humidityPctFrom);
    }
    if (criteria.humidityPctTo !== undefined) {
      conditions.push("`humidity_pct` <= ?");
      values.push(criteria.humidityPctTo);
    }
    if (criteria.humidityPctIsNull !== undefined) {
      conditions.push(
        criteria.humidityPctIsNull
          ? "`humidity_pct` IS NULL"
          : "`humidity_pct` IS NOT NULL",
      );
    }
    if (criteria.ventSetting !== undefined) {
      conditions.push("`vent_setting` = ?");
      values.push(criteria.ventSetting);
    }
    if (criteria.ventSettingLike !== undefined) {
      conditions.push("`vent_setting` LIKE ?");
      values.push(criteria.ventSettingLike);
    }
    if (criteria.ventSettingIsNull !== undefined) {
      conditions.push(
        criteria.ventSettingIsNull
          ? "`vent_setting` IS NULL"
          : "`vent_setting` IS NOT NULL",
      );
    }
    if (criteria.stowPosition !== undefined) {
      conditions.push("`stow_position` = ?");
      values.push(criteria.stowPosition);
    }
    if (criteria.stowPositionLike !== undefined) {
      conditions.push("`stow_position` LIKE ?");
      values.push(criteria.stowPositionLike);
    }
    if (criteria.stowPositionIsNull !== undefined) {
      conditions.push(
        criteria.stowPositionIsNull
          ? "`stow_position` IS NULL"
          : "`stow_position` IS NOT NULL",
      );
    }
    if (criteria.loadedAt !== undefined) {
      conditions.push("`loaded_at` = ?");
      values.push(criteria.loadedAt);
    }
    if (criteria.loadedAtFrom !== undefined) {
      conditions.push("`loaded_at` >= ?");
      values.push(criteria.loadedAtFrom);
    }
    if (criteria.loadedAtTo !== undefined) {
      conditions.push("`loaded_at` <= ?");
      values.push(criteria.loadedAtTo);
    }
    if (criteria.loadedAtIsNull !== undefined) {
      conditions.push(
        criteria.loadedAtIsNull
          ? "`loaded_at` IS NULL"
          : "`loaded_at` IS NOT NULL",
      );
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              containerColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
