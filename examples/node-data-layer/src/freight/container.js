// Code generated by tpl from freight.container. DO NOT EDIT.

/**
 * The `freight.container` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

// The table's own comment, as the catalogue carries it:
// Contentores afectos a cada expedição, com massa bruta verificada (VGM)

/**
 * One row of `freight.container`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} Container
 * @property {string} containerId `container_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
 *   It is part of the primary key.
 * @property {string} equipmentNo `equipment_no`, declared `char(11)` NOT NULL.
 *   It is part of a unique index.
 *   Comment: ISO 6346 equipment number: four-letter prefix, six digits, one check digit
 * @property {string} consignmentId `consignment_id`, declared `bigint(20) unsigned` NOT NULL.
 *   It is part of a unique index.
 * @property {string} isoCode `iso_code`, declared `char(4)` NOT NULL.
 *   Comment: Defaults to the standard dry twenty-foot box
 * @property {string | null} sealNumber `seal_number`, declared `varchar(15)` NULL.
 * @property {string} tareWeightKg `tare_weight_kg`, declared `decimal(8,2)` NOT NULL.
 * @property {string} cargoWeightKg `cargo_weight_kg`, declared `decimal(10,3)` NOT NULL.
 * @property {string | null} vgmKg `vgm_kg`, declared `decimal(10,3)` NULL STORED GENERATED AS (`tare_weight_kg` + `cargo_weight_kg`).
 *   Comment: Stored generated column: SOLAS verified gross mass
 * @property {string} vgmMethod `vgm_method`, declared `enum('Method 1 — weighbridge','Method 2 — calculated','Not yet verified')` NOT NULL.
 * @property {string | null} setpointCelsius `setpoint_celsius`, declared `decimal(4,1)` NULL.
 *   Comment: Reefer setpoint; NULL for a dry box
 * @property {number | null} humidityPct `humidity_pct`, declared `tinyint(3) unsigned` NULL.
 * @property {string | null} ventSetting `vent_setting`, declared `varchar(12)` NULL.
 * @property {string | null} stowPosition `stow_position`, declared `varchar(8)` NULL.
 *   Comment: Bay-row-tier as loaded, e.g. 0340882
 * @property {Date | null} loadedAt `loaded_at`, declared `datetime(6)` NULL.
 */

/**
 * The primary key of `freight.container`.
 *
 * @typedef {object} ContainerKey
 * @property {string} containerId `container_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
 */

/**
 * One column of `freight.container`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link ContainerColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const ContainerColumns = Object.freeze(
  /** @type {const} */ ({
    /** `container_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT. */
    CONTAINER_ID: "container_id",
    /** `equipment_no`, declared `char(11)` NOT NULL. */
    EQUIPMENT_NO: "equipment_no",
    /** `consignment_id`, declared `bigint(20) unsigned` NOT NULL. */
    CONSIGNMENT_ID: "consignment_id",
    /** `iso_code`, declared `char(4)` NOT NULL. */
    ISO_CODE: "iso_code",
    /** `seal_number`, declared `varchar(15)` NULL. */
    SEAL_NUMBER: "seal_number",
    /** `tare_weight_kg`, declared `decimal(8,2)` NOT NULL. */
    TARE_WEIGHT_KG: "tare_weight_kg",
    /** `cargo_weight_kg`, declared `decimal(10,3)` NOT NULL. */
    CARGO_WEIGHT_KG: "cargo_weight_kg",
    /** `vgm_kg`, declared `decimal(10,3)` NULL STORED GENERATED AS (`tare_weight_kg` + `cargo_weight_kg`). */
    VGM_KG: "vgm_kg",
    /** `vgm_method`, declared `enum('Method 1 — weighbridge','Method 2 — calculated','Not yet verified')` NOT NULL. */
    VGM_METHOD: "vgm_method",
    /** `setpoint_celsius`, declared `decimal(4,1)` NULL. */
    SETPOINT_CELSIUS: "setpoint_celsius",
    /** `humidity_pct`, declared `tinyint(3) unsigned` NULL. */
    HUMIDITY_PCT: "humidity_pct",
    /** `vent_setting`, declared `varchar(12)` NULL. */
    VENT_SETTING: "vent_setting",
    /** `stow_position`, declared `varchar(8)` NULL. */
    STOW_POSITION: "stow_position",
    /** `loaded_at`, declared `datetime(6)` NULL. */
    LOADED_AT: "loaded_at",
  }),
);

/**
 * A column of `freight.container` an ordering may name.
 *
 * @typedef {(typeof ContainerColumns)[keyof typeof ContainerColumns]} ContainerColumn
 */

/**
 * The one place a column of `freight.container` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<ContainerColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "container_id": "`container_id`",
  "equipment_no": "`equipment_no`",
  "consignment_id": "`consignment_id`",
  "iso_code": "`iso_code`",
  "seal_number": "`seal_number`",
  "tare_weight_kg": "`tare_weight_kg`",
  "cargo_weight_kg": "`cargo_weight_kg`",
  "vgm_kg": "`vgm_kg`",
  "vgm_method": "`vgm_method`",
  "setpoint_celsius": "`setpoint_celsius`",
  "humidity_pct": "`humidity_pct`",
  "vent_setting": "`vent_setting`",
  "stow_position": "`stow_position`",
  "loaded_at": "`loaded_at`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {ContainerColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function containerColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("freight.container", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `freight.container`.
 *
 * @typedef {object} ContainerOrder
 * @property {ContainerColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `freight.container`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} ContainerFilter
 * @property {string} [containerId] where set, requires `container_id` to equal it.
 * @property {string} [containerIdFrom] where set, requires `container_id` to be at least it.
 * @property {string} [containerIdTo] where set, requires `container_id` to be at most it.
 * @property {string} [equipmentNo] where set, requires `equipment_no` to equal it.
 * @property {string} [equipmentNoLike] where set, requires `equipment_no` to match it, as LIKE matches.
 * @property {string} [consignmentId] where set, requires `consignment_id` to equal it.
 * @property {string} [consignmentIdFrom] where set, requires `consignment_id` to be at least it.
 * @property {string} [consignmentIdTo] where set, requires `consignment_id` to be at most it.
 * @property {string} [isoCode] where set, requires `iso_code` to equal it.
 * @property {string} [isoCodeLike] where set, requires `iso_code` to match it, as LIKE matches.
 * @property {string} [sealNumber] where set, requires `seal_number` to equal it.
 * @property {string} [sealNumberLike] where set, requires `seal_number` to match it, as LIKE matches.
 * @property {boolean} [sealNumberIsNull] where true, requires `seal_number` to be NULL; where false, requires it not to be.
 * @property {string} [tareWeightKg] where set, requires `tare_weight_kg` to equal it.
 * @property {string} [tareWeightKgFrom] where set, requires `tare_weight_kg` to be at least it.
 * @property {string} [tareWeightKgTo] where set, requires `tare_weight_kg` to be at most it.
 * @property {string} [cargoWeightKg] where set, requires `cargo_weight_kg` to equal it.
 * @property {string} [cargoWeightKgFrom] where set, requires `cargo_weight_kg` to be at least it.
 * @property {string} [cargoWeightKgTo] where set, requires `cargo_weight_kg` to be at most it.
 * @property {string} [vgmKg] where set, requires `vgm_kg` to equal it.
 * @property {string} [vgmKgFrom] where set, requires `vgm_kg` to be at least it.
 * @property {string} [vgmKgTo] where set, requires `vgm_kg` to be at most it.
 * @property {boolean} [vgmKgIsNull] where true, requires `vgm_kg` to be NULL; where false, requires it not to be.
 * @property {string} [vgmMethod] where set, requires `vgm_method` to equal it.
 * @property {string} [vgmMethodLike] where set, requires `vgm_method` to match it, as LIKE matches.
 * @property {string} [setpointCelsius] where set, requires `setpoint_celsius` to equal it.
 * @property {string} [setpointCelsiusFrom] where set, requires `setpoint_celsius` to be at least it.
 * @property {string} [setpointCelsiusTo] where set, requires `setpoint_celsius` to be at most it.
 * @property {boolean} [setpointCelsiusIsNull] where true, requires `setpoint_celsius` to be NULL; where false, requires it not to be.
 * @property {number} [humidityPct] where set, requires `humidity_pct` to equal it.
 * @property {number} [humidityPctFrom] where set, requires `humidity_pct` to be at least it.
 * @property {number} [humidityPctTo] where set, requires `humidity_pct` to be at most it.
 * @property {boolean} [humidityPctIsNull] where true, requires `humidity_pct` to be NULL; where false, requires it not to be.
 * @property {string} [ventSetting] where set, requires `vent_setting` to equal it.
 * @property {string} [ventSettingLike] where set, requires `vent_setting` to match it, as LIKE matches.
 * @property {boolean} [ventSettingIsNull] where true, requires `vent_setting` to be NULL; where false, requires it not to be.
 * @property {string} [stowPosition] where set, requires `stow_position` to equal it.
 * @property {string} [stowPositionLike] where set, requires `stow_position` to match it, as LIKE matches.
 * @property {boolean} [stowPositionIsNull] where true, requires `stow_position` to be NULL; where false, requires it not to be.
 * @property {Date} [loadedAt] where set, requires `loaded_at` to equal it.
 * @property {Date} [loadedAtFrom] where set, requires `loaded_at` to be at least it.
 * @property {Date} [loadedAtTo] where set, requires `loaded_at` to be at most it.
 * @property {boolean} [loadedAtIsNull] where true, requires `loaded_at` to be NULL; where false, requires it not to be.
 */
