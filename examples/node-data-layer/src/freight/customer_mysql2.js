// Code generated by tpl from freight.customer. DO NOT EDIT.

/**
 * The mysql2 implementation of the `freight.customer` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { Customer, CustomerFilter, CustomerKey, CustomerOrder } from './customer.js' */

import { NotFound, directionSql, failure } from './core.js';
import { customerColumnSql } from './customer.js';
import { CustomerRepository } from './customer_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "freight.customer";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `customer_id`, `legal_name`, `trading_name`, `vat_number`, `eori_number`, `country_code`, `legacy_account_code`, `portal_uuid`, `last_login_ipv4`, `last_login_ipv6`, `credit_limit_eur`, `outstanding_eur`, `credit_headroom_eur`, `risk_score`, `payment_terms_days`, `preferences`, `notification_flags`, `is_active`, `onboarded_on`, `review_due_on`, `contact_note`, `created_at`, `updated_at` FROM `customer`";

/** Selects the one row a primary key names. */
const READ = "SELECT `customer_id`, `legal_name`, `trading_name`, `vat_number`, `eori_number`, `country_code`, `legacy_account_code`, `portal_uuid`, `last_login_ipv4`, `last_login_ipv6`, `credit_limit_eur`, `outstanding_eur`, `credit_headroom_eur`, `risk_score`, `payment_terms_days`, `preferences`, `notification_flags`, `is_active`, `onboarded_on`, `review_due_on`, `contact_note`, `created_at`, `updated_at` FROM `customer` WHERE `customer_id` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `customer` (`legal_name`, `trading_name`, `vat_number`, `eori_number`, `country_code`, `legacy_account_code`, `portal_uuid`, `last_login_ipv4`, `last_login_ipv6`, `credit_limit_eur`, `outstanding_eur`, `risk_score`, `payment_terms_days`, `preferences`, `notification_flags`, `is_active`, `onboarded_on`, `review_due_on`, `contact_note`, `created_at`, `updated_at`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `customer` SET `legal_name` = ?, `trading_name` = ?, `vat_number` = ?, `eori_number` = ?, `country_code` = ?, `legacy_account_code` = ?, `portal_uuid` = ?, `last_login_ipv4` = ?, `last_login_ipv6` = ?, `credit_limit_eur` = ?, `outstanding_eur` = ?, `risk_score` = ?, `payment_terms_days` = ?, `preferences` = ?, `notification_flags` = ?, `is_active` = ?, `onboarded_on` = ?, `review_due_on` = ?, `contact_note` = ?, `created_at` = ?, `updated_at` = ? WHERE `customer_id` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `customer` WHERE `customer_id` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `Customer` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {Customer}
 */
function toRow(values) {
  return {
    customerId: values[0],
    legalName: values[1],
    tradingName: values[2],
    vatNumber: values[3],
    eoriNumber: values[4],
    countryCode: values[5],
    legacyAccountCode: values[6],
    portalUuid: values[7],
    lastLoginIpv4: values[8],
    lastLoginIpv6: values[9],
    creditLimitEur: values[10],
    outstandingEur: values[11],
    creditHeadroomEur: values[12],
    riskScore: values[13],
    paymentTermsDays: values[14],
    preferences: values[15],
    notificationFlags: values[16],
    isActive: values[17],
    onboardedOn: values[18],
    reviewDueOn: values[19],
    contactNote: values[20],
    createdAt: values[21],
    updatedAt: values[22],
  };
}

/**
 * The mysql2 data layer of `freight.customer`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class CustomerMysql2 extends CustomerRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `freight.customer` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The key is assigned by the server and is written back into `row`.
   *
   * @override
   * @param {Customer} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.legalName,
      row.tradingName,
      row.vatNumber,
      row.eoriNumber,
      row.countryCode,
      row.legacyAccountCode,
      row.portalUuid,
      row.lastLoginIpv4,
      row.lastLoginIpv6,
      row.creditLimitEur,
      row.outstandingEur,
      row.riskScore,
      row.paymentTermsDays,
      row.preferences,
      row.notificationFlags,
      row.isActive,
      row.onboardedOn,
      row.reviewDueOn,
      row.contactNote,
      row.createdAt,
      row.updatedAt,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
      const { insertId } = /** @type {ResultSetHeader} */ (result);
      row.customerId = insertId;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {CustomerKey} key
   * @returns {Promise<Customer>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.customerId,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {Customer} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.legalName,
      row.tradingName,
      row.vatNumber,
      row.eoriNumber,
      row.countryCode,
      row.legacyAccountCode,
      row.portalUuid,
      row.lastLoginIpv4,
      row.lastLoginIpv6,
      row.creditLimitEur,
      row.outstandingEur,
      row.riskScore,
      row.paymentTermsDays,
      row.preferences,
      row.notificationFlags,
      row.isActive,
      row.onboardedOn,
      row.reviewDueOn,
      row.contactNote,
      row.createdAt,
      row.updatedAt,
      row.customerId,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {CustomerKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.customerId,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {CustomerFilter} criteria
   * @param {readonly CustomerOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<Customer[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.customerId !== undefined) {
      conditions.push("`customer_id` = ?");
      values.push(criteria.customerId);
    }
    if (criteria.customerIdFrom !== undefined) {
      conditions.push("`customer_id` >= ?");
      values.push(criteria.customerIdFrom);
    }
    if (criteria.customerIdTo !== undefined) {
      conditions.push("`customer_id` <= ?");
      values.push(criteria.customerIdTo);
    }
    if (criteria.legalName !== undefined) {
      conditions.push("`legal_name` = ?");
      values.push(criteria.legalName);
    }
    if (criteria.legalNameLike !== undefined) {
      conditions.push("`legal_name` LIKE ?");
      values.push(criteria.legalNameLike);
    }
    if (criteria.tradingName !== undefined) {
      conditions.push("`trading_name` = ?");
      values.push(criteria.tradingName);
    }
    if (criteria.tradingNameLike !== undefined) {
      conditions.push("`trading_name` LIKE ?");
      values.push(criteria.tradingNameLike);
    }
    if (criteria.tradingNameIsNull !== undefined) {
      conditions.push(
        criteria.tradingNameIsNull
          ? "`trading_name` IS NULL"
          : "`trading_name` IS NOT NULL",
      );
    }
    if (criteria.vatNumber !== undefined) {
      conditions.push("`vat_number` = ?");
      values.push(criteria.vatNumber);
    }
    if (criteria.vatNumberLike !== undefined) {
      conditions.push("`vat_number` LIKE ?");
      values.push(criteria.vatNumberLike);
    }
    if (criteria.eoriNumber !== undefined) {
      conditions.push("`eori_number` = ?");
      values.push(criteria.eoriNumber);
    }
    if (criteria.eoriNumberLike !== undefined) {
      conditions.push("`eori_number` LIKE ?");
      values.push(criteria.eoriNumberLike);
    }
    if (criteria.eoriNumberIsNull !== undefined) {
      conditions.push(
        criteria.eoriNumberIsNull
          ? "`eori_number` IS NULL"
          : "`eori_number` IS NOT NULL",
      );
    }
    if (criteria.countryCode !== undefined) {
      conditions.push("`country_code` = ?");
      values.push(criteria.countryCode);
    }
    if (criteria.countryCodeLike !== undefined) {
      conditions.push("`country_code` LIKE ?");
      values.push(criteria.countryCodeLike);
    }
    if (criteria.legacyAccountCode !== undefined) {
      conditions.push("`legacy_account_code` = ?");
      values.push(criteria.legacyAccountCode);
    }
    if (criteria.legacyAccountCodeLike !== undefined) {
      conditions.push("`legacy_account_code` LIKE ?");
      values.push(criteria.legacyAccountCodeLike);
    }
    if (criteria.legacyAccountCodeIsNull !== undefined) {
      conditions.push(
        criteria.legacyAccountCodeIsNull
          ? "`legacy_account_code` IS NULL"
          : "`legacy_account_code` IS NOT NULL",
      );
    }
    if (criteria.portalUuid !== undefined) {
      conditions.push("`portal_uuid` = ?");
      values.push(criteria.portalUuid);
    }
    if (criteria.lastLoginIpv4 !== undefined) {
      conditions.push("`last_login_ipv4` = ?");
      values.push(criteria.lastLoginIpv4);
    }
    if (criteria.lastLoginIpv4IsNull !== undefined) {
      conditions.push(
        criteria.lastLoginIpv4IsNull
          ? "`last_login_ipv4` IS NULL"
          : "`last_login_ipv4` IS NOT NULL",
      );
    }
    if (criteria.lastLoginIpv6 !== undefined) {
      conditions.push("`last_login_ipv6` = ?");
      values.push(criteria.lastLoginIpv6);
    }
    if (criteria.lastLoginIpv6IsNull !== undefined) {
      conditions.push(
        criteria.lastLoginIpv6IsNull
          ? "`last_login_ipv6` IS NULL"
          : "`last_login_ipv6` IS NOT NULL",
      );
    }
    if (criteria.creditLimitEur !== undefined) {
      conditions.push("`credit_limit_eur` = ?");
      values.push(criteria.creditLimitEur);
    }
    if (criteria.creditLimitEurFrom !== undefined) {
      conditions.push("`credit_limit_eur` >= ?");
      values.push(criteria.creditLimitEurFrom);
    }
    if (criteria.creditLimitEurTo !== undefined) {
      conditions.push("`credit_limit_eur` <= ?");
      values.push(criteria.creditLimitEurTo);
    }
    if (criteria.outstandingEur !== undefined) {
      conditions.push("`outstanding_eur` = ?");
      values.push(criteria.outstandingEur);
    }
    if (criteria.outstandingEurFrom !== undefined) {
      conditions.push("`outstanding_eur` >= ?");
      values.push(criteria.outstandingEurFrom);
    }
    if (criteria.outstandingEurTo !== undefined) {
      conditions.push("`outstanding_eur` <= ?");
      values.push(criteria.outstandingEurTo);
    }
    if (criteria.creditHeadroomEur !== undefined) {
      conditions.push("`credit_headroom_eur` = ?");
      values.push(criteria.creditHeadroomEur);
    }
    if (criteria.creditHeadroomEurFrom !== undefined) {
      conditions.push("`credit_headroom_eur` >= ?");
      values.push(criteria.creditHeadroomEurFrom);
    }
    if (criteria.creditHeadroomEurTo !== undefined) {
      conditions.push("`credit_headroom_eur` <= ?");
      values.push(criteria.creditHeadroomEurTo);
    }
    if (criteria.creditHeadroomEurIsNull !== undefined) {
      conditions.push(
        criteria.creditHeadroomEurIsNull
          ? "`credit_headroom_eur` IS NULL"
          : "`credit_headroom_eur` IS NOT NULL",
      );
    }
    if (criteria.riskScore !== undefined) {
      conditions.push("`risk_score` = ?");
      values.push(criteria.riskScore);
    }
    if (criteria.riskScoreFrom !== undefined) {
      conditions.push("`risk_score` >= ?");
      values.push(criteria.riskScoreFrom);
    }
    if (criteria.riskScoreTo !== undefined) {
      conditions.push("`risk_score` <= ?");
      values.push(criteria.riskScoreTo);
    }
    if (criteria.riskScoreIsNull !== undefined) {
      conditions.push(
        criteria.riskScoreIsNull
          ? "`risk_score` IS NULL"
          : "`risk_score` IS NOT NULL",
      );
    }
    if (criteria.paymentTermsDays !== undefined) {
      conditions.push("`payment_terms_days` = ?");
      values.push(criteria.paymentTermsDays);
    }
    if (criteria.paymentTermsDaysFrom !== undefined) {
      conditions.push("`payment_terms_days` >= ?");
      values.push(criteria.paymentTermsDaysFrom);
    }
    if (criteria.paymentTermsDaysTo !== undefined) {
      conditions.push("`payment_terms_days` <= ?");
      values.push(criteria.paymentTermsDaysTo);
    }
    if (criteria.preferences !== undefined) {
      conditions.push("`preferences` = ?");
      values.push(criteria.preferences);
    }
    if (criteria.preferencesLike !== undefined) {
      conditions.push("`preferences` LIKE ?");
      values.push(criteria.preferencesLike);
    }
    if (criteria.preferencesIsNull !== undefined) {
      conditions.push(
        criteria.preferencesIsNull
          ? "`preferences` IS NULL"
          : "`preferences` IS NOT NULL",
      );
    }
    if (criteria.notificationFlags !== undefined) {
      conditions.push("`notification_flags` = ?");
      values.push(criteria.notificationFlags);
    }
    if (criteria.notificationFlagsFrom !== undefined) {
      conditions.push("`notification_flags` >= ?");
      values.push(criteria.notificationFlagsFrom);
    }
    if (criteria.notificationFlagsTo !== undefined) {
      conditions.push("`notification_flags` <= ?");
      values.push(criteria.notificationFlagsTo);
    }
    if (criteria.isActive !== undefined) {
      conditions.push("`is_active` = ?");
      values.push(criteria.isActive);
    }
    if (criteria.isActiveFrom !== undefined) {
      conditions.push("`is_active` >= ?");
      values.push(criteria.isActiveFrom);
    }
    if (criteria.isActiveTo !== undefined) {
      conditions.push("`is_active` <= ?");
      values.push(criteria.isActiveTo);
    }
    if (criteria.onboardedOn !== undefined) {
      conditions.push("`onboarded_on` = ?");
      values.push(criteria.onboardedOn);
    }
    if (criteria.onboardedOnFrom !== undefined) {
      conditions.push("`onboarded_on` >= ?");
      values.push(criteria.onboardedOnFrom);
    }
    if (criteria.onboardedOnTo !== undefined) {
      conditions.push("`onboarded_on` <= ?");
      values.push(criteria.onboardedOnTo);
    }
    if (criteria.reviewDueOn !== undefined) {
      conditions.push("`review_due_on` = ?");
      values.push(criteria.reviewDueOn);
    }
    if (criteria.reviewDueOnFrom !== undefined) {
      conditions.push("`review_due_on` >= ?");
      values.push(criteria.reviewDueOnFrom);
    }
    if (criteria.reviewDueOnTo !== undefined) {
      conditions.push("`review_due_on` <= ?");
      values.push(criteria.reviewDueOnTo);
    }
    if (criteria.contactNote !== undefined) {
      conditions.push("`contact_note` = ?");
      values.push(criteria.contactNote);
    }
    if (criteria.contactNoteLike !== undefined) {
      conditions.push("`contact_note` LIKE ?");
      values.push(criteria.contactNoteLike);
    }
    if (criteria.contactNoteIsNull !== undefined) {
      conditions.push(
        criteria.contactNoteIsNull
          ? "`contact_note` IS NULL"
          : "`contact_note` IS NOT NULL",
      );
    }
    if (criteria.createdAt !== undefined) {
      conditions.push("`created_at` = ?");
      values.push(criteria.createdAt);
    }
    if (criteria.createdAtFrom !== undefined) {
      conditions.push("`created_at` >= ?");
      values.push(criteria.createdAtFrom);
    }
    if (criteria.createdAtTo !== undefined) {
      conditions.push("`created_at` <= ?");
      values.push(criteria.createdAtTo);
    }
    if (criteria.updatedAt !== undefined) {
      conditions.push("`updated_at` = ?");
      values.push(criteria.updatedAt);
    }
    if (criteria.updatedAtFrom !== undefined) {
      conditions.push("`updated_at` >= ?");
      values.push(criteria.updatedAtFrom);
    }
    if (criteria.updatedAtTo !== undefined) {
      conditions.push("`updated_at` <= ?");
      values.push(criteria.updatedAtTo);
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              customerColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
