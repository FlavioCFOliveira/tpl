// Code generated by tpl from freight.audit_event. DO NOT EDIT.

/**
 * The `freight.audit_event` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

// The table's own comment, as the catalogue carries it:
// Trilho de auditoria escrito pelos gatilhos; sobrevive à remoção das linhas que regista

/**
 * One row of `freight.audit_event`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} AuditEvent
 * @property {string} auditEventId `audit_event_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
 *   It is part of the primary key.
 * @property {string} entityName `entity_name`, declared `varchar(64)` NOT NULL.
 * @property {string} entityKey `entity_key`, declared `varchar(64)` NOT NULL.
 * @property {string} action `action`, declared `enum('INSERT','UPDATE','DELETE')` NOT NULL.
 * @property {string} triggerTiming `trigger_timing`, declared `enum('BEFORE','AFTER')` NOT NULL.
 * @property {string | null} changedColumn `changed_column`, declared `varchar(64)` NULL.
 * @property {string | null} oldValue `old_value`, declared `varchar(255)` NULL.
 * @property {string | null} newValue `new_value`, declared `varchar(255)` NULL.
 * @property {string} actor `actor`, declared `varchar(96)` NOT NULL.
 * @property {string | null} clientIpv4 `client_ipv4`, declared `inet4` NULL.
 * @property {string | null} clientIpv6 `client_ipv6`, declared `inet6` NULL.
 * @property {string | null} correlationUuid `correlation_uuid`, declared `uuid` NULL.
 * @property {Date} observedAt `observed_at`, declared `datetime(6)` NOT NULL.
 * @property {string | null} detail `detail`, declared `text` NULL.
 */

/**
 * The primary key of `freight.audit_event`.
 *
 * @typedef {object} AuditEventKey
 * @property {string} auditEventId `audit_event_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
 */

/**
 * One column of `freight.audit_event`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link AuditEventColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const AuditEventColumns = Object.freeze(
  /** @type {const} */ ({
    /** `audit_event_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT. */
    AUDIT_EVENT_ID: "audit_event_id",
    /** `entity_name`, declared `varchar(64)` NOT NULL. */
    ENTITY_NAME: "entity_name",
    /** `entity_key`, declared `varchar(64)` NOT NULL. */
    ENTITY_KEY: "entity_key",
    /** `action`, declared `enum('INSERT','UPDATE','DELETE')` NOT NULL. */
    ACTION: "action",
    /** `trigger_timing`, declared `enum('BEFORE','AFTER')` NOT NULL. */
    TRIGGER_TIMING: "trigger_timing",
    /** `changed_column`, declared `varchar(64)` NULL. */
    CHANGED_COLUMN: "changed_column",
    /** `old_value`, declared `varchar(255)` NULL. */
    OLD_VALUE: "old_value",
    /** `new_value`, declared `varchar(255)` NULL. */
    NEW_VALUE: "new_value",
    /** `actor`, declared `varchar(96)` NOT NULL. */
    ACTOR: "actor",
    /** `client_ipv4`, declared `inet4` NULL. */
    CLIENT_IPV4: "client_ipv4",
    /** `client_ipv6`, declared `inet6` NULL. */
    CLIENT_IPV6: "client_ipv6",
    /** `correlation_uuid`, declared `uuid` NULL. */
    CORRELATION_UUID: "correlation_uuid",
    /** `observed_at`, declared `datetime(6)` NOT NULL. */
    OBSERVED_AT: "observed_at",
    /** `detail`, declared `text` NULL. */
    DETAIL: "detail",
  }),
);

/**
 * A column of `freight.audit_event` an ordering may name.
 *
 * @typedef {(typeof AuditEventColumns)[keyof typeof AuditEventColumns]} AuditEventColumn
 */

/**
 * The one place a column of `freight.audit_event` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<AuditEventColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "audit_event_id": "`audit_event_id`",
  "entity_name": "`entity_name`",
  "entity_key": "`entity_key`",
  "action": "`action`",
  "trigger_timing": "`trigger_timing`",
  "changed_column": "`changed_column`",
  "old_value": "`old_value`",
  "new_value": "`new_value`",
  "actor": "`actor`",
  "client_ipv4": "`client_ipv4`",
  "client_ipv6": "`client_ipv6`",
  "correlation_uuid": "`correlation_uuid`",
  "observed_at": "`observed_at`",
  "detail": "`detail`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {AuditEventColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function auditEventColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("freight.audit_event", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `freight.audit_event`.
 *
 * @typedef {object} AuditEventOrder
 * @property {AuditEventColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `freight.audit_event`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} AuditEventFilter
 * @property {string} [auditEventId] where set, requires `audit_event_id` to equal it.
 * @property {string} [auditEventIdFrom] where set, requires `audit_event_id` to be at least it.
 * @property {string} [auditEventIdTo] where set, requires `audit_event_id` to be at most it.
 * @property {string} [entityName] where set, requires `entity_name` to equal it.
 * @property {string} [entityNameLike] where set, requires `entity_name` to match it, as LIKE matches.
 * @property {string} [entityKey] where set, requires `entity_key` to equal it.
 * @property {string} [entityKeyLike] where set, requires `entity_key` to match it, as LIKE matches.
 * @property {string} [action] where set, requires `action` to equal it.
 * @property {string} [actionLike] where set, requires `action` to match it, as LIKE matches.
 * @property {string} [triggerTiming] where set, requires `trigger_timing` to equal it.
 * @property {string} [triggerTimingLike] where set, requires `trigger_timing` to match it, as LIKE matches.
 * @property {string} [changedColumn] where set, requires `changed_column` to equal it.
 * @property {string} [changedColumnLike] where set, requires `changed_column` to match it, as LIKE matches.
 * @property {boolean} [changedColumnIsNull] where true, requires `changed_column` to be NULL; where false, requires it not to be.
 * @property {string} [oldValue] where set, requires `old_value` to equal it.
 * @property {string} [oldValueLike] where set, requires `old_value` to match it, as LIKE matches.
 * @property {boolean} [oldValueIsNull] where true, requires `old_value` to be NULL; where false, requires it not to be.
 * @property {string} [newValue] where set, requires `new_value` to equal it.
 * @property {string} [newValueLike] where set, requires `new_value` to match it, as LIKE matches.
 * @property {boolean} [newValueIsNull] where true, requires `new_value` to be NULL; where false, requires it not to be.
 * @property {string} [actor] where set, requires `actor` to equal it.
 * @property {string} [actorLike] where set, requires `actor` to match it, as LIKE matches.
 * @property {string} [clientIpv4] where set, requires `client_ipv4` to equal it.
 * @property {boolean} [clientIpv4IsNull] where true, requires `client_ipv4` to be NULL; where false, requires it not to be.
 * @property {string} [clientIpv6] where set, requires `client_ipv6` to equal it.
 * @property {boolean} [clientIpv6IsNull] where true, requires `client_ipv6` to be NULL; where false, requires it not to be.
 * @property {string} [correlationUuid] where set, requires `correlation_uuid` to equal it.
 * @property {boolean} [correlationUuidIsNull] where true, requires `correlation_uuid` to be NULL; where false, requires it not to be.
 * @property {Date} [observedAt] where set, requires `observed_at` to equal it.
 * @property {Date} [observedAtFrom] where set, requires `observed_at` to be at least it.
 * @property {Date} [observedAtTo] where set, requires `observed_at` to be at most it.
 * @property {string} [detail] where set, requires `detail` to equal it.
 * @property {string} [detailLike] where set, requires `detail` to match it, as LIKE matches.
 * @property {boolean} [detailIsNull] where true, requires `detail` to be NULL; where false, requires it not to be.
 */
