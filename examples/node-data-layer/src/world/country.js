// Code generated by tpl from world.country. DO NOT EDIT.

/**
 * The `world.country` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

/**
 * One row of `world.country`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} Country
 * @property {string} code `Code`, declared `char(3)` NOT NULL.
 *   It is part of the primary key.
 * @property {string} name `Name`, declared `char(52)` NOT NULL.
 * @property {string} continent `Continent`, declared `enum('Asia','Europe','North America','Africa','Oceania','Antarctica','South America')` NOT NULL.
 * @property {string} region `Region`, declared `char(26)` NOT NULL.
 * @property {string} surfaceArea `SurfaceArea`, declared `decimal(10,2)` NOT NULL.
 * @property {number | null} indepYear `IndepYear`, declared `smallint(6)` NULL.
 * @property {number} population `Population`, declared `int(11)` NOT NULL.
 * @property {string | null} lifeExpectancy `LifeExpectancy`, declared `decimal(3,1)` NULL.
 * @property {string | null} gnp `GNP`, declared `decimal(10,2)` NULL.
 * @property {string | null} gnpOld `GNPOld`, declared `decimal(10,2)` NULL.
 * @property {string} localName `LocalName`, declared `char(45)` NOT NULL.
 * @property {string} governmentForm `GovernmentForm`, declared `char(45)` NOT NULL.
 * @property {string | null} headOfState `HeadOfState`, declared `char(60)` NULL.
 * @property {number | null} capital `Capital`, declared `int(11)` NULL.
 * @property {string} code2 `Code2`, declared `char(2)` NOT NULL.
 */

/**
 * The primary key of `world.country`.
 *
 * @typedef {object} CountryKey
 * @property {string} code `Code`, declared `char(3)` NOT NULL.
 */

/**
 * One column of `world.country`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link CountryColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const CountryColumns = Object.freeze(
  /** @type {const} */ ({
    /** `Code`, declared `char(3)` NOT NULL. */
    CODE: "Code",
    /** `Name`, declared `char(52)` NOT NULL. */
    NAME: "Name",
    /** `Continent`, declared `enum('Asia','Europe','North America','Africa','Oceania','Antarctica','South America')` NOT NULL. */
    CONTINENT: "Continent",
    /** `Region`, declared `char(26)` NOT NULL. */
    REGION: "Region",
    /** `SurfaceArea`, declared `decimal(10,2)` NOT NULL. */
    SURFACE_AREA: "SurfaceArea",
    /** `IndepYear`, declared `smallint(6)` NULL. */
    INDEP_YEAR: "IndepYear",
    /** `Population`, declared `int(11)` NOT NULL. */
    POPULATION: "Population",
    /** `LifeExpectancy`, declared `decimal(3,1)` NULL. */
    LIFE_EXPECTANCY: "LifeExpectancy",
    /** `GNP`, declared `decimal(10,2)` NULL. */
    GNP: "GNP",
    /** `GNPOld`, declared `decimal(10,2)` NULL. */
    GNP_OLD: "GNPOld",
    /** `LocalName`, declared `char(45)` NOT NULL. */
    LOCAL_NAME: "LocalName",
    /** `GovernmentForm`, declared `char(45)` NOT NULL. */
    GOVERNMENT_FORM: "GovernmentForm",
    /** `HeadOfState`, declared `char(60)` NULL. */
    HEAD_OF_STATE: "HeadOfState",
    /** `Capital`, declared `int(11)` NULL. */
    CAPITAL: "Capital",
    /** `Code2`, declared `char(2)` NOT NULL. */
    CODE2: "Code2",
  }),
);

/**
 * A column of `world.country` an ordering may name.
 *
 * @typedef {(typeof CountryColumns)[keyof typeof CountryColumns]} CountryColumn
 */

/**
 * The one place a column of `world.country` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<CountryColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "Code": "`Code`",
  "Name": "`Name`",
  "Continent": "`Continent`",
  "Region": "`Region`",
  "SurfaceArea": "`SurfaceArea`",
  "IndepYear": "`IndepYear`",
  "Population": "`Population`",
  "LifeExpectancy": "`LifeExpectancy`",
  "GNP": "`GNP`",
  "GNPOld": "`GNPOld`",
  "LocalName": "`LocalName`",
  "GovernmentForm": "`GovernmentForm`",
  "HeadOfState": "`HeadOfState`",
  "Capital": "`Capital`",
  "Code2": "`Code2`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {CountryColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function countryColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("world.country", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `world.country`.
 *
 * @typedef {object} CountryOrder
 * @property {CountryColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `world.country`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} CountryFilter
 * @property {string} [code] where set, requires `Code` to equal it.
 * @property {string} [codeLike] where set, requires `Code` to match it, as LIKE matches.
 * @property {string} [name] where set, requires `Name` to equal it.
 * @property {string} [nameLike] where set, requires `Name` to match it, as LIKE matches.
 * @property {string} [continent] where set, requires `Continent` to equal it.
 * @property {string} [continentLike] where set, requires `Continent` to match it, as LIKE matches.
 * @property {string} [region] where set, requires `Region` to equal it.
 * @property {string} [regionLike] where set, requires `Region` to match it, as LIKE matches.
 * @property {string} [surfaceArea] where set, requires `SurfaceArea` to equal it.
 * @property {string} [surfaceAreaFrom] where set, requires `SurfaceArea` to be at least it.
 * @property {string} [surfaceAreaTo] where set, requires `SurfaceArea` to be at most it.
 * @property {number} [indepYear] where set, requires `IndepYear` to equal it.
 * @property {number} [indepYearFrom] where set, requires `IndepYear` to be at least it.
 * @property {number} [indepYearTo] where set, requires `IndepYear` to be at most it.
 * @property {boolean} [indepYearIsNull] where true, requires `IndepYear` to be NULL; where false, requires it not to be.
 * @property {number} [population] where set, requires `Population` to equal it.
 * @property {number} [populationFrom] where set, requires `Population` to be at least it.
 * @property {number} [populationTo] where set, requires `Population` to be at most it.
 * @property {string} [lifeExpectancy] where set, requires `LifeExpectancy` to equal it.
 * @property {string} [lifeExpectancyFrom] where set, requires `LifeExpectancy` to be at least it.
 * @property {string} [lifeExpectancyTo] where set, requires `LifeExpectancy` to be at most it.
 * @property {boolean} [lifeExpectancyIsNull] where true, requires `LifeExpectancy` to be NULL; where false, requires it not to be.
 * @property {string} [gnp] where set, requires `GNP` to equal it.
 * @property {string} [gnpFrom] where set, requires `GNP` to be at least it.
 * @property {string} [gnpTo] where set, requires `GNP` to be at most it.
 * @property {boolean} [gnpIsNull] where true, requires `GNP` to be NULL; where false, requires it not to be.
 * @property {string} [gnpOld] where set, requires `GNPOld` to equal it.
 * @property {string} [gnpOldFrom] where set, requires `GNPOld` to be at least it.
 * @property {string} [gnpOldTo] where set, requires `GNPOld` to be at most it.
 * @property {boolean} [gnpOldIsNull] where true, requires `GNPOld` to be NULL; where false, requires it not to be.
 * @property {string} [localName] where set, requires `LocalName` to equal it.
 * @property {string} [localNameLike] where set, requires `LocalName` to match it, as LIKE matches.
 * @property {string} [governmentForm] where set, requires `GovernmentForm` to equal it.
 * @property {string} [governmentFormLike] where set, requires `GovernmentForm` to match it, as LIKE matches.
 * @property {string} [headOfState] where set, requires `HeadOfState` to equal it.
 * @property {string} [headOfStateLike] where set, requires `HeadOfState` to match it, as LIKE matches.
 * @property {boolean} [headOfStateIsNull] where true, requires `HeadOfState` to be NULL; where false, requires it not to be.
 * @property {number} [capital] where set, requires `Capital` to equal it.
 * @property {number} [capitalFrom] where set, requires `Capital` to be at least it.
 * @property {number} [capitalTo] where set, requires `Capital` to be at most it.
 * @property {boolean} [capitalIsNull] where true, requires `Capital` to be NULL; where false, requires it not to be.
 * @property {string} [code2] where set, requires `Code2` to equal it.
 * @property {string} [code2Like] where set, requires `Code2` to match it, as LIKE matches.
 */
