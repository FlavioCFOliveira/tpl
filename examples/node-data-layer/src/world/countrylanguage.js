// Code generated by tpl from world.countrylanguage. DO NOT EDIT.

/**
 * The `world.countrylanguage` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

/**
 * One row of `world.countrylanguage`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} Countrylanguage
 * @property {string} countryCode `CountryCode`, declared `char(3)` NOT NULL.
 *   It is part of the primary key.
 * @property {string} language `Language`, declared `char(30)` NOT NULL.
 *   It is part of the primary key.
 * @property {string} isOfficial `IsOfficial`, declared `enum('T','F')` NOT NULL.
 * @property {string} percentage `Percentage`, declared `decimal(4,1)` NOT NULL.
 */

/**
 * The primary key of `world.countrylanguage`.
 *
 * @typedef {object} CountrylanguageKey
 * @property {string} countryCode `CountryCode`, declared `char(3)` NOT NULL.
 * @property {string} language `Language`, declared `char(30)` NOT NULL.
 */

/**
 * One column of `world.countrylanguage`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link CountrylanguageColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const CountrylanguageColumns = Object.freeze(
  /** @type {const} */ ({
    /** `CountryCode`, declared `char(3)` NOT NULL. */
    COUNTRY_CODE: "CountryCode",
    /** `Language`, declared `char(30)` NOT NULL. */
    LANGUAGE: "Language",
    /** `IsOfficial`, declared `enum('T','F')` NOT NULL. */
    IS_OFFICIAL: "IsOfficial",
    /** `Percentage`, declared `decimal(4,1)` NOT NULL. */
    PERCENTAGE: "Percentage",
  }),
);

/**
 * A column of `world.countrylanguage` an ordering may name.
 *
 * @typedef {(typeof CountrylanguageColumns)[keyof typeof CountrylanguageColumns]} CountrylanguageColumn
 */

/**
 * The one place a column of `world.countrylanguage` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<CountrylanguageColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "CountryCode": "`CountryCode`",
  "Language": "`Language`",
  "IsOfficial": "`IsOfficial`",
  "Percentage": "`Percentage`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {CountrylanguageColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function countrylanguageColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("world.countrylanguage", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `world.countrylanguage`.
 *
 * @typedef {object} CountrylanguageOrder
 * @property {CountrylanguageColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `world.countrylanguage`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} CountrylanguageFilter
 * @property {string} [countryCode] where set, requires `CountryCode` to equal it.
 * @property {string} [countryCodeLike] where set, requires `CountryCode` to match it, as LIKE matches.
 * @property {string} [language] where set, requires `Language` to equal it.
 * @property {string} [languageLike] where set, requires `Language` to match it, as LIKE matches.
 * @property {string} [isOfficial] where set, requires `IsOfficial` to equal it.
 * @property {string} [isOfficialLike] where set, requires `IsOfficial` to match it, as LIKE matches.
 * @property {string} [percentage] where set, requires `Percentage` to equal it.
 * @property {string} [percentageFrom] where set, requires `Percentage` to be at least it.
 * @property {string} [percentageTo] where set, requires `Percentage` to be at most it.
 */
