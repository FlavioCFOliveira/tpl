// Code generated by tpl from world.country. DO NOT EDIT.

/**
 * The mysql2 implementation of the `world.country` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { Country, CountryFilter, CountryKey, CountryOrder } from './country.js' */

import { NotFound, directionSql, failure } from './core.js';
import { countryColumnSql } from './country.js';
import { CountryRepository } from './country_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "world.country";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `Code`, `Name`, `Continent`, `Region`, `SurfaceArea`, `IndepYear`, `Population`, `LifeExpectancy`, `GNP`, `GNPOld`, `LocalName`, `GovernmentForm`, `HeadOfState`, `Capital`, `Code2` FROM `country`";

/** Selects the one row a primary key names. */
const READ = "SELECT `Code`, `Name`, `Continent`, `Region`, `SurfaceArea`, `IndepYear`, `Population`, `LifeExpectancy`, `GNP`, `GNPOld`, `LocalName`, `GovernmentForm`, `HeadOfState`, `Capital`, `Code2` FROM `country` WHERE `Code` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `country` (`Code`, `Name`, `Continent`, `Region`, `SurfaceArea`, `IndepYear`, `Population`, `LifeExpectancy`, `GNP`, `GNPOld`, `LocalName`, `GovernmentForm`, `HeadOfState`, `Capital`, `Code2`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `country` SET `Name` = ?, `Continent` = ?, `Region` = ?, `SurfaceArea` = ?, `IndepYear` = ?, `Population` = ?, `LifeExpectancy` = ?, `GNP` = ?, `GNPOld` = ?, `LocalName` = ?, `GovernmentForm` = ?, `HeadOfState` = ?, `Capital` = ?, `Code2` = ? WHERE `Code` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `country` WHERE `Code` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `Country` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {Country}
 */
function toRow(values) {
  return {
    code: values[0],
    name: values[1],
    continent: values[2],
    region: values[3],
    surfaceArea: values[4],
    indepYear: values[5],
    population: values[6],
    lifeExpectancy: values[7],
    gnp: values[8],
    gnpOld: values[9],
    localName: values[10],
    governmentForm: values[11],
    headOfState: values[12],
    capital: values[13],
    code2: values[14],
  };
}

/**
 * The mysql2 data layer of `world.country`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class CountryMysql2 extends CountryRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `world.country` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The whole key is taken from `row`, which the caller therefore fills.
   *
   * @override
   * @param {Country} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.code,
      row.name,
      row.continent,
      row.region,
      row.surfaceArea,
      row.indepYear,
      row.population,
      row.lifeExpectancy,
      row.gnp,
      row.gnpOld,
      row.localName,
      row.governmentForm,
      row.headOfState,
      row.capital,
      row.code2,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {CountryKey} key
   * @returns {Promise<Country>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.code,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {Country} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.name,
      row.continent,
      row.region,
      row.surfaceArea,
      row.indepYear,
      row.population,
      row.lifeExpectancy,
      row.gnp,
      row.gnpOld,
      row.localName,
      row.governmentForm,
      row.headOfState,
      row.capital,
      row.code2,
      row.code,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {CountryKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.code,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {CountryFilter} criteria
   * @param {readonly CountryOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<Country[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.code !== undefined) {
      conditions.push("`Code` = ?");
      values.push(criteria.code);
    }
    if (criteria.codeLike !== undefined) {
      conditions.push("`Code` LIKE ?");
      values.push(criteria.codeLike);
    }
    if (criteria.name !== undefined) {
      conditions.push("`Name` = ?");
      values.push(criteria.name);
    }
    if (criteria.nameLike !== undefined) {
      conditions.push("`Name` LIKE ?");
      values.push(criteria.nameLike);
    }
    if (criteria.continent !== undefined) {
      conditions.push("`Continent` = ?");
      values.push(criteria.continent);
    }
    if (criteria.continentLike !== undefined) {
      conditions.push("`Continent` LIKE ?");
      values.push(criteria.continentLike);
    }
    if (criteria.region !== undefined) {
      conditions.push("`Region` = ?");
      values.push(criteria.region);
    }
    if (criteria.regionLike !== undefined) {
      conditions.push("`Region` LIKE ?");
      values.push(criteria.regionLike);
    }
    if (criteria.surfaceArea !== undefined) {
      conditions.push("`SurfaceArea` = ?");
      values.push(criteria.surfaceArea);
    }
    if (criteria.surfaceAreaFrom !== undefined) {
      conditions.push("`SurfaceArea` >= ?");
      values.push(criteria.surfaceAreaFrom);
    }
    if (criteria.surfaceAreaTo !== undefined) {
      conditions.push("`SurfaceArea` <= ?");
      values.push(criteria.surfaceAreaTo);
    }
    if (criteria.indepYear !== undefined) {
      conditions.push("`IndepYear` = ?");
      values.push(criteria.indepYear);
    }
    if (criteria.indepYearFrom !== undefined) {
      conditions.push("`IndepYear` >= ?");
      values.push(criteria.indepYearFrom);
    }
    if (criteria.indepYearTo !== undefined) {
      conditions.push("`IndepYear` <= ?");
      values.push(criteria.indepYearTo);
    }
    if (criteria.indepYearIsNull !== undefined) {
      conditions.push(
        criteria.indepYearIsNull
          ? "`IndepYear` IS NULL"
          : "`IndepYear` IS NOT NULL",
      );
    }
    if (criteria.population !== undefined) {
      conditions.push("`Population` = ?");
      values.push(criteria.population);
    }
    if (criteria.populationFrom !== undefined) {
      conditions.push("`Population` >= ?");
      values.push(criteria.populationFrom);
    }
    if (criteria.populationTo !== undefined) {
      conditions.push("`Population` <= ?");
      values.push(criteria.populationTo);
    }
    if (criteria.lifeExpectancy !== undefined) {
      conditions.push("`LifeExpectancy` = ?");
      values.push(criteria.lifeExpectancy);
    }
    if (criteria.lifeExpectancyFrom !== undefined) {
      conditions.push("`LifeExpectancy` >= ?");
      values.push(criteria.lifeExpectancyFrom);
    }
    if (criteria.lifeExpectancyTo !== undefined) {
      conditions.push("`LifeExpectancy` <= ?");
      values.push(criteria.lifeExpectancyTo);
    }
    if (criteria.lifeExpectancyIsNull !== undefined) {
      conditions.push(
        criteria.lifeExpectancyIsNull
          ? "`LifeExpectancy` IS NULL"
          : "`LifeExpectancy` IS NOT NULL",
      );
    }
    if (criteria.gnp !== undefined) {
      conditions.push("`GNP` = ?");
      values.push(criteria.gnp);
    }
    if (criteria.gnpFrom !== undefined) {
      conditions.push("`GNP` >= ?");
      values.push(criteria.gnpFrom);
    }
    if (criteria.gnpTo !== undefined) {
      conditions.push("`GNP` <= ?");
      values.push(criteria.gnpTo);
    }
    if (criteria.gnpIsNull !== undefined) {
      conditions.push(
        criteria.gnpIsNull
          ? "`GNP` IS NULL"
          : "`GNP` IS NOT NULL",
      );
    }
    if (criteria.gnpOld !== undefined) {
      conditions.push("`GNPOld` = ?");
      values.push(criteria.gnpOld);
    }
    if (criteria.gnpOldFrom !== undefined) {
      conditions.push("`GNPOld` >= ?");
      values.push(criteria.gnpOldFrom);
    }
    if (criteria.gnpOldTo !== undefined) {
      conditions.push("`GNPOld` <= ?");
      values.push(criteria.gnpOldTo);
    }
    if (criteria.gnpOldIsNull !== undefined) {
      conditions.push(
        criteria.gnpOldIsNull
          ? "`GNPOld` IS NULL"
          : "`GNPOld` IS NOT NULL",
      );
    }
    if (criteria.localName !== undefined) {
      conditions.push("`LocalName` = ?");
      values.push(criteria.localName);
    }
    if (criteria.localNameLike !== undefined) {
      conditions.push("`LocalName` LIKE ?");
      values.push(criteria.localNameLike);
    }
    if (criteria.governmentForm !== undefined) {
      conditions.push("`GovernmentForm` = ?");
      values.push(criteria.governmentForm);
    }
    if (criteria.governmentFormLike !== undefined) {
      conditions.push("`GovernmentForm` LIKE ?");
      values.push(criteria.governmentFormLike);
    }
    if (criteria.headOfState !== undefined) {
      conditions.push("`HeadOfState` = ?");
      values.push(criteria.headOfState);
    }
    if (criteria.headOfStateLike !== undefined) {
      conditions.push("`HeadOfState` LIKE ?");
      values.push(criteria.headOfStateLike);
    }
    if (criteria.headOfStateIsNull !== undefined) {
      conditions.push(
        criteria.headOfStateIsNull
          ? "`HeadOfState` IS NULL"
          : "`HeadOfState` IS NOT NULL",
      );
    }
    if (criteria.capital !== undefined) {
      conditions.push("`Capital` = ?");
      values.push(criteria.capital);
    }
    if (criteria.capitalFrom !== undefined) {
      conditions.push("`Capital` >= ?");
      values.push(criteria.capitalFrom);
    }
    if (criteria.capitalTo !== undefined) {
      conditions.push("`Capital` <= ?");
      values.push(criteria.capitalTo);
    }
    if (criteria.capitalIsNull !== undefined) {
      conditions.push(
        criteria.capitalIsNull
          ? "`Capital` IS NULL"
          : "`Capital` IS NOT NULL",
      );
    }
    if (criteria.code2 !== undefined) {
      conditions.push("`Code2` = ?");
      values.push(criteria.code2);
    }
    if (criteria.code2Like !== undefined) {
      conditions.push("`Code2` LIKE ?");
      values.push(criteria.code2Like);
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              countryColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
