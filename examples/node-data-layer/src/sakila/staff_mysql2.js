// Code generated by tpl from sakila.staff. DO NOT EDIT.

/**
 * The mysql2 implementation of the `sakila.staff` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { Staff, StaffFilter, StaffKey, StaffOrder } from './staff.js' */

import { NotFound, directionSql, failure } from './core.js';
import { staffColumnSql } from './staff.js';
import { StaffRepository } from './staff_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "sakila.staff";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `staff_id`, `first_name`, `last_name`, `address_id`, `picture`, `email`, `store_id`, `active`, `username`, `password`, `last_update` FROM `staff`";

/** Selects the one row a primary key names. */
const READ = "SELECT `staff_id`, `first_name`, `last_name`, `address_id`, `picture`, `email`, `store_id`, `active`, `username`, `password`, `last_update` FROM `staff` WHERE `staff_id` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `staff` (`first_name`, `last_name`, `address_id`, `picture`, `email`, `store_id`, `active`, `username`, `password`, `last_update`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `staff` SET `first_name` = ?, `last_name` = ?, `address_id` = ?, `picture` = ?, `email` = ?, `store_id` = ?, `active` = ?, `username` = ?, `password` = ?, `last_update` = ? WHERE `staff_id` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `staff` WHERE `staff_id` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `Staff` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {Staff}
 */
function toRow(values) {
  return {
    staffId: values[0],
    firstName: values[1],
    lastName: values[2],
    addressId: values[3],
    picture: values[4],
    email: values[5],
    storeId: values[6],
    active: values[7],
    username: values[8],
    password: values[9],
    lastUpdate: values[10],
  };
}

/**
 * The mysql2 data layer of `sakila.staff`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class StaffMysql2 extends StaffRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `sakila.staff` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The key is assigned by the server and is written back into `row`.
   *
   * @override
   * @param {Staff} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.firstName,
      row.lastName,
      row.addressId,
      row.picture,
      row.email,
      row.storeId,
      row.active,
      row.username,
      row.password,
      row.lastUpdate,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
      const { insertId } = /** @type {ResultSetHeader} */ (result);
      row.staffId = insertId;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {StaffKey} key
   * @returns {Promise<Staff>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.staffId,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {Staff} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.firstName,
      row.lastName,
      row.addressId,
      row.picture,
      row.email,
      row.storeId,
      row.active,
      row.username,
      row.password,
      row.lastUpdate,
      row.staffId,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {StaffKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.staffId,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {StaffFilter} criteria
   * @param {readonly StaffOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<Staff[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.staffId !== undefined) {
      conditions.push("`staff_id` = ?");
      values.push(criteria.staffId);
    }
    if (criteria.staffIdFrom !== undefined) {
      conditions.push("`staff_id` >= ?");
      values.push(criteria.staffIdFrom);
    }
    if (criteria.staffIdTo !== undefined) {
      conditions.push("`staff_id` <= ?");
      values.push(criteria.staffIdTo);
    }
    if (criteria.firstName !== undefined) {
      conditions.push("`first_name` = ?");
      values.push(criteria.firstName);
    }
    if (criteria.firstNameLike !== undefined) {
      conditions.push("`first_name` LIKE ?");
      values.push(criteria.firstNameLike);
    }
    if (criteria.lastName !== undefined) {
      conditions.push("`last_name` = ?");
      values.push(criteria.lastName);
    }
    if (criteria.lastNameLike !== undefined) {
      conditions.push("`last_name` LIKE ?");
      values.push(criteria.lastNameLike);
    }
    if (criteria.addressId !== undefined) {
      conditions.push("`address_id` = ?");
      values.push(criteria.addressId);
    }
    if (criteria.addressIdFrom !== undefined) {
      conditions.push("`address_id` >= ?");
      values.push(criteria.addressIdFrom);
    }
    if (criteria.addressIdTo !== undefined) {
      conditions.push("`address_id` <= ?");
      values.push(criteria.addressIdTo);
    }
    if (criteria.picture !== undefined) {
      conditions.push("`picture` = ?");
      values.push(criteria.picture);
    }
    if (criteria.pictureIsNull !== undefined) {
      conditions.push(
        criteria.pictureIsNull
          ? "`picture` IS NULL"
          : "`picture` IS NOT NULL",
      );
    }
    if (criteria.email !== undefined) {
      conditions.push("`email` = ?");
      values.push(criteria.email);
    }
    if (criteria.emailLike !== undefined) {
      conditions.push("`email` LIKE ?");
      values.push(criteria.emailLike);
    }
    if (criteria.emailIsNull !== undefined) {
      conditions.push(
        criteria.emailIsNull
          ? "`email` IS NULL"
          : "`email` IS NOT NULL",
      );
    }
    if (criteria.storeId !== undefined) {
      conditions.push("`store_id` = ?");
      values.push(criteria.storeId);
    }
    if (criteria.storeIdFrom !== undefined) {
      conditions.push("`store_id` >= ?");
      values.push(criteria.storeIdFrom);
    }
    if (criteria.storeIdTo !== undefined) {
      conditions.push("`store_id` <= ?");
      values.push(criteria.storeIdTo);
    }
    if (criteria.active !== undefined) {
      conditions.push("`active` = ?");
      values.push(criteria.active);
    }
    if (criteria.activeFrom !== undefined) {
      conditions.push("`active` >= ?");
      values.push(criteria.activeFrom);
    }
    if (criteria.activeTo !== undefined) {
      conditions.push("`active` <= ?");
      values.push(criteria.activeTo);
    }
    if (criteria.username !== undefined) {
      conditions.push("`username` = ?");
      values.push(criteria.username);
    }
    if (criteria.usernameLike !== undefined) {
      conditions.push("`username` LIKE ?");
      values.push(criteria.usernameLike);
    }
    if (criteria.password !== undefined) {
      conditions.push("`password` = ?");
      values.push(criteria.password);
    }
    if (criteria.passwordLike !== undefined) {
      conditions.push("`password` LIKE ?");
      values.push(criteria.passwordLike);
    }
    if (criteria.passwordIsNull !== undefined) {
      conditions.push(
        criteria.passwordIsNull
          ? "`password` IS NULL"
          : "`password` IS NOT NULL",
      );
    }
    if (criteria.lastUpdate !== undefined) {
      conditions.push("`last_update` = ?");
      values.push(criteria.lastUpdate);
    }
    if (criteria.lastUpdateFrom !== undefined) {
      conditions.push("`last_update` >= ?");
      values.push(criteria.lastUpdateFrom);
    }
    if (criteria.lastUpdateTo !== undefined) {
      conditions.push("`last_update` <= ?");
      values.push(criteria.lastUpdateTo);
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              staffColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
