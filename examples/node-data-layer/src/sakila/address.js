// Code generated by tpl from sakila.address. DO NOT EDIT.

/**
 * The `sakila.address` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

/**
 * One row of `sakila.address`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} Address
 * @property {number} addressId `address_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
 *   It is part of the primary key.
 * @property {string} address `address`, declared `varchar(50)` NOT NULL.
 * @property {string | null} address2 `address2`, declared `varchar(50)` NULL.
 * @property {string} district `district`, declared `varchar(20)` NOT NULL.
 * @property {number} cityId `city_id`, declared `smallint(5) unsigned` NOT NULL.
 * @property {string | null} postalCode `postal_code`, declared `varchar(10)` NULL.
 * @property {string} phone `phone`, declared `varchar(20)` NOT NULL.
 * @property {Date} lastUpdate `last_update`, declared `timestamp` NOT NULL.
 */

/**
 * The primary key of `sakila.address`.
 *
 * @typedef {object} AddressKey
 * @property {number} addressId `address_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
 */

/**
 * One column of `sakila.address`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link AddressColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const AddressColumns = Object.freeze(
  /** @type {const} */ ({
    /** `address_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT. */
    ADDRESS_ID: "address_id",
    /** `address`, declared `varchar(50)` NOT NULL. */
    ADDRESS: "address",
    /** `address2`, declared `varchar(50)` NULL. */
    ADDRESS2: "address2",
    /** `district`, declared `varchar(20)` NOT NULL. */
    DISTRICT: "district",
    /** `city_id`, declared `smallint(5) unsigned` NOT NULL. */
    CITY_ID: "city_id",
    /** `postal_code`, declared `varchar(10)` NULL. */
    POSTAL_CODE: "postal_code",
    /** `phone`, declared `varchar(20)` NOT NULL. */
    PHONE: "phone",
    /** `last_update`, declared `timestamp` NOT NULL. */
    LAST_UPDATE: "last_update",
  }),
);

/**
 * A column of `sakila.address` an ordering may name.
 *
 * @typedef {(typeof AddressColumns)[keyof typeof AddressColumns]} AddressColumn
 */

/**
 * The one place a column of `sakila.address` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<AddressColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "address_id": "`address_id`",
  "address": "`address`",
  "address2": "`address2`",
  "district": "`district`",
  "city_id": "`city_id`",
  "postal_code": "`postal_code`",
  "phone": "`phone`",
  "last_update": "`last_update`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {AddressColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function addressColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("sakila.address", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `sakila.address`.
 *
 * @typedef {object} AddressOrder
 * @property {AddressColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `sakila.address`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} AddressFilter
 * @property {number} [addressId] where set, requires `address_id` to equal it.
 * @property {number} [addressIdFrom] where set, requires `address_id` to be at least it.
 * @property {number} [addressIdTo] where set, requires `address_id` to be at most it.
 * @property {string} [address] where set, requires `address` to equal it.
 * @property {string} [addressLike] where set, requires `address` to match it, as LIKE matches.
 * @property {string} [address2] where set, requires `address2` to equal it.
 * @property {string} [address2Like] where set, requires `address2` to match it, as LIKE matches.
 * @property {boolean} [address2IsNull] where true, requires `address2` to be NULL; where false, requires it not to be.
 * @property {string} [district] where set, requires `district` to equal it.
 * @property {string} [districtLike] where set, requires `district` to match it, as LIKE matches.
 * @property {number} [cityId] where set, requires `city_id` to equal it.
 * @property {number} [cityIdFrom] where set, requires `city_id` to be at least it.
 * @property {number} [cityIdTo] where set, requires `city_id` to be at most it.
 * @property {string} [postalCode] where set, requires `postal_code` to equal it.
 * @property {string} [postalCodeLike] where set, requires `postal_code` to match it, as LIKE matches.
 * @property {boolean} [postalCodeIsNull] where true, requires `postal_code` to be NULL; where false, requires it not to be.
 * @property {string} [phone] where set, requires `phone` to equal it.
 * @property {string} [phoneLike] where set, requires `phone` to match it, as LIKE matches.
 * @property {Date} [lastUpdate] where set, requires `last_update` to equal it.
 * @property {Date} [lastUpdateFrom] where set, requires `last_update` to be at least it.
 * @property {Date} [lastUpdateTo] where set, requires `last_update` to be at most it.
 */
