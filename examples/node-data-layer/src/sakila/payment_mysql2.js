// Code generated by tpl from sakila.payment. DO NOT EDIT.

/**
 * The mysql2 implementation of the `sakila.payment` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { Payment, PaymentFilter, PaymentKey, PaymentOrder } from './payment.js' */

import { NotFound, directionSql, failure } from './core.js';
import { paymentColumnSql } from './payment.js';
import { PaymentRepository } from './payment_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "sakila.payment";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `payment_id`, `customer_id`, `staff_id`, `rental_id`, `amount`, `payment_date`, `last_update` FROM `payment`";

/** Selects the one row a primary key names. */
const READ = "SELECT `payment_id`, `customer_id`, `staff_id`, `rental_id`, `amount`, `payment_date`, `last_update` FROM `payment` WHERE `payment_id` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `payment` (`customer_id`, `staff_id`, `rental_id`, `amount`, `payment_date`, `last_update`) VALUES (?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `payment` SET `customer_id` = ?, `staff_id` = ?, `rental_id` = ?, `amount` = ?, `payment_date` = ?, `last_update` = ? WHERE `payment_id` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `payment` WHERE `payment_id` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `Payment` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {Payment}
 */
function toRow(values) {
  return {
    paymentId: values[0],
    customerId: values[1],
    staffId: values[2],
    rentalId: values[3],
    amount: values[4],
    paymentDate: values[5],
    lastUpdate: values[6],
  };
}

/**
 * The mysql2 data layer of `sakila.payment`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class PaymentMysql2 extends PaymentRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `sakila.payment` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The key is assigned by the server and is written back into `row`.
   *
   * @override
   * @param {Payment} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.customerId,
      row.staffId,
      row.rentalId,
      row.amount,
      row.paymentDate,
      row.lastUpdate,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
      const { insertId } = /** @type {ResultSetHeader} */ (result);
      row.paymentId = insertId;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {PaymentKey} key
   * @returns {Promise<Payment>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.paymentId,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {Payment} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.customerId,
      row.staffId,
      row.rentalId,
      row.amount,
      row.paymentDate,
      row.lastUpdate,
      row.paymentId,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {PaymentKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.paymentId,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {PaymentFilter} criteria
   * @param {readonly PaymentOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<Payment[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.paymentId !== undefined) {
      conditions.push("`payment_id` = ?");
      values.push(criteria.paymentId);
    }
    if (criteria.paymentIdFrom !== undefined) {
      conditions.push("`payment_id` >= ?");
      values.push(criteria.paymentIdFrom);
    }
    if (criteria.paymentIdTo !== undefined) {
      conditions.push("`payment_id` <= ?");
      values.push(criteria.paymentIdTo);
    }
    if (criteria.customerId !== undefined) {
      conditions.push("`customer_id` = ?");
      values.push(criteria.customerId);
    }
    if (criteria.customerIdFrom !== undefined) {
      conditions.push("`customer_id` >= ?");
      values.push(criteria.customerIdFrom);
    }
    if (criteria.customerIdTo !== undefined) {
      conditions.push("`customer_id` <= ?");
      values.push(criteria.customerIdTo);
    }
    if (criteria.staffId !== undefined) {
      conditions.push("`staff_id` = ?");
      values.push(criteria.staffId);
    }
    if (criteria.staffIdFrom !== undefined) {
      conditions.push("`staff_id` >= ?");
      values.push(criteria.staffIdFrom);
    }
    if (criteria.staffIdTo !== undefined) {
      conditions.push("`staff_id` <= ?");
      values.push(criteria.staffIdTo);
    }
    if (criteria.rentalId !== undefined) {
      conditions.push("`rental_id` = ?");
      values.push(criteria.rentalId);
    }
    if (criteria.rentalIdFrom !== undefined) {
      conditions.push("`rental_id` >= ?");
      values.push(criteria.rentalIdFrom);
    }
    if (criteria.rentalIdTo !== undefined) {
      conditions.push("`rental_id` <= ?");
      values.push(criteria.rentalIdTo);
    }
    if (criteria.rentalIdIsNull !== undefined) {
      conditions.push(
        criteria.rentalIdIsNull
          ? "`rental_id` IS NULL"
          : "`rental_id` IS NOT NULL",
      );
    }
    if (criteria.amount !== undefined) {
      conditions.push("`amount` = ?");
      values.push(criteria.amount);
    }
    if (criteria.amountFrom !== undefined) {
      conditions.push("`amount` >= ?");
      values.push(criteria.amountFrom);
    }
    if (criteria.amountTo !== undefined) {
      conditions.push("`amount` <= ?");
      values.push(criteria.amountTo);
    }
    if (criteria.paymentDate !== undefined) {
      conditions.push("`payment_date` = ?");
      values.push(criteria.paymentDate);
    }
    if (criteria.paymentDateFrom !== undefined) {
      conditions.push("`payment_date` >= ?");
      values.push(criteria.paymentDateFrom);
    }
    if (criteria.paymentDateTo !== undefined) {
      conditions.push("`payment_date` <= ?");
      values.push(criteria.paymentDateTo);
    }
    if (criteria.lastUpdate !== undefined) {
      conditions.push("`last_update` = ?");
      values.push(criteria.lastUpdate);
    }
    if (criteria.lastUpdateFrom !== undefined) {
      conditions.push("`last_update` >= ?");
      values.push(criteria.lastUpdateFrom);
    }
    if (criteria.lastUpdateTo !== undefined) {
      conditions.push("`last_update` <= ?");
      values.push(criteria.lastUpdateTo);
    }
    if (criteria.lastUpdateIsNull !== undefined) {
      conditions.push(
        criteria.lastUpdateIsNull
          ? "`last_update` IS NULL"
          : "`last_update` IS NOT NULL",
      );
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              paymentColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
