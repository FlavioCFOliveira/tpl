// Code generated by tpl from sakila.film. DO NOT EDIT.

/**
 * The mysql2 implementation of the `sakila.film` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { Film, FilmFilter, FilmKey, FilmOrder } from './film.js' */

import { NotFound, directionSql, failure } from './core.js';
import { filmColumnSql } from './film.js';
import { FilmRepository } from './film_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "sakila.film";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `film_id`, `title`, `description`, `release_year`, `language_id`, `original_language_id`, `rental_duration`, `rental_rate`, `length`, `replacement_cost`, `rating`, `special_features`, `last_update` FROM `film`";

/** Selects the one row a primary key names. */
const READ = "SELECT `film_id`, `title`, `description`, `release_year`, `language_id`, `original_language_id`, `rental_duration`, `rental_rate`, `length`, `replacement_cost`, `rating`, `special_features`, `last_update` FROM `film` WHERE `film_id` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `film` (`title`, `description`, `release_year`, `language_id`, `original_language_id`, `rental_duration`, `rental_rate`, `length`, `replacement_cost`, `rating`, `special_features`, `last_update`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `film` SET `title` = ?, `description` = ?, `release_year` = ?, `language_id` = ?, `original_language_id` = ?, `rental_duration` = ?, `rental_rate` = ?, `length` = ?, `replacement_cost` = ?, `rating` = ?, `special_features` = ?, `last_update` = ? WHERE `film_id` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `film` WHERE `film_id` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `Film` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {Film}
 */
function toRow(values) {
  return {
    filmId: values[0],
    title: values[1],
    description: values[2],
    releaseYear: values[3],
    languageId: values[4],
    originalLanguageId: values[5],
    rentalDuration: values[6],
    rentalRate: values[7],
    length: values[8],
    replacementCost: values[9],
    rating: values[10],
    specialFeatures: values[11],
    lastUpdate: values[12],
  };
}

/**
 * The mysql2 data layer of `sakila.film`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class FilmMysql2 extends FilmRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `sakila.film` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The key is assigned by the server and is written back into `row`.
   *
   * @override
   * @param {Film} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.title,
      row.description,
      row.releaseYear,
      row.languageId,
      row.originalLanguageId,
      row.rentalDuration,
      row.rentalRate,
      row.length,
      row.replacementCost,
      row.rating,
      row.specialFeatures,
      row.lastUpdate,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
      const { insertId } = /** @type {ResultSetHeader} */ (result);
      row.filmId = insertId;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {FilmKey} key
   * @returns {Promise<Film>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.filmId,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {Film} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.title,
      row.description,
      row.releaseYear,
      row.languageId,
      row.originalLanguageId,
      row.rentalDuration,
      row.rentalRate,
      row.length,
      row.replacementCost,
      row.rating,
      row.specialFeatures,
      row.lastUpdate,
      row.filmId,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {FilmKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.filmId,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {FilmFilter} criteria
   * @param {readonly FilmOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<Film[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.filmId !== undefined) {
      conditions.push("`film_id` = ?");
      values.push(criteria.filmId);
    }
    if (criteria.filmIdFrom !== undefined) {
      conditions.push("`film_id` >= ?");
      values.push(criteria.filmIdFrom);
    }
    if (criteria.filmIdTo !== undefined) {
      conditions.push("`film_id` <= ?");
      values.push(criteria.filmIdTo);
    }
    if (criteria.title !== undefined) {
      conditions.push("`title` = ?");
      values.push(criteria.title);
    }
    if (criteria.titleLike !== undefined) {
      conditions.push("`title` LIKE ?");
      values.push(criteria.titleLike);
    }
    if (criteria.description !== undefined) {
      conditions.push("`description` = ?");
      values.push(criteria.description);
    }
    if (criteria.descriptionLike !== undefined) {
      conditions.push("`description` LIKE ?");
      values.push(criteria.descriptionLike);
    }
    if (criteria.descriptionIsNull !== undefined) {
      conditions.push(
        criteria.descriptionIsNull
          ? "`description` IS NULL"
          : "`description` IS NOT NULL",
      );
    }
    if (criteria.releaseYear !== undefined) {
      conditions.push("`release_year` = ?");
      values.push(criteria.releaseYear);
    }
    if (criteria.releaseYearFrom !== undefined) {
      conditions.push("`release_year` >= ?");
      values.push(criteria.releaseYearFrom);
    }
    if (criteria.releaseYearTo !== undefined) {
      conditions.push("`release_year` <= ?");
      values.push(criteria.releaseYearTo);
    }
    if (criteria.releaseYearIsNull !== undefined) {
      conditions.push(
        criteria.releaseYearIsNull
          ? "`release_year` IS NULL"
          : "`release_year` IS NOT NULL",
      );
    }
    if (criteria.languageId !== undefined) {
      conditions.push("`language_id` = ?");
      values.push(criteria.languageId);
    }
    if (criteria.languageIdFrom !== undefined) {
      conditions.push("`language_id` >= ?");
      values.push(criteria.languageIdFrom);
    }
    if (criteria.languageIdTo !== undefined) {
      conditions.push("`language_id` <= ?");
      values.push(criteria.languageIdTo);
    }
    if (criteria.originalLanguageId !== undefined) {
      conditions.push("`original_language_id` = ?");
      values.push(criteria.originalLanguageId);
    }
    if (criteria.originalLanguageIdFrom !== undefined) {
      conditions.push("`original_language_id` >= ?");
      values.push(criteria.originalLanguageIdFrom);
    }
    if (criteria.originalLanguageIdTo !== undefined) {
      conditions.push("`original_language_id` <= ?");
      values.push(criteria.originalLanguageIdTo);
    }
    if (criteria.originalLanguageIdIsNull !== undefined) {
      conditions.push(
        criteria.originalLanguageIdIsNull
          ? "`original_language_id` IS NULL"
          : "`original_language_id` IS NOT NULL",
      );
    }
    if (criteria.rentalDuration !== undefined) {
      conditions.push("`rental_duration` = ?");
      values.push(criteria.rentalDuration);
    }
    if (criteria.rentalDurationFrom !== undefined) {
      conditions.push("`rental_duration` >= ?");
      values.push(criteria.rentalDurationFrom);
    }
    if (criteria.rentalDurationTo !== undefined) {
      conditions.push("`rental_duration` <= ?");
      values.push(criteria.rentalDurationTo);
    }
    if (criteria.rentalRate !== undefined) {
      conditions.push("`rental_rate` = ?");
      values.push(criteria.rentalRate);
    }
    if (criteria.rentalRateFrom !== undefined) {
      conditions.push("`rental_rate` >= ?");
      values.push(criteria.rentalRateFrom);
    }
    if (criteria.rentalRateTo !== undefined) {
      conditions.push("`rental_rate` <= ?");
      values.push(criteria.rentalRateTo);
    }
    if (criteria.length !== undefined) {
      conditions.push("`length` = ?");
      values.push(criteria.length);
    }
    if (criteria.lengthFrom !== undefined) {
      conditions.push("`length` >= ?");
      values.push(criteria.lengthFrom);
    }
    if (criteria.lengthTo !== undefined) {
      conditions.push("`length` <= ?");
      values.push(criteria.lengthTo);
    }
    if (criteria.lengthIsNull !== undefined) {
      conditions.push(
        criteria.lengthIsNull
          ? "`length` IS NULL"
          : "`length` IS NOT NULL",
      );
    }
    if (criteria.replacementCost !== undefined) {
      conditions.push("`replacement_cost` = ?");
      values.push(criteria.replacementCost);
    }
    if (criteria.replacementCostFrom !== undefined) {
      conditions.push("`replacement_cost` >= ?");
      values.push(criteria.replacementCostFrom);
    }
    if (criteria.replacementCostTo !== undefined) {
      conditions.push("`replacement_cost` <= ?");
      values.push(criteria.replacementCostTo);
    }
    if (criteria.rating !== undefined) {
      conditions.push("`rating` = ?");
      values.push(criteria.rating);
    }
    if (criteria.ratingLike !== undefined) {
      conditions.push("`rating` LIKE ?");
      values.push(criteria.ratingLike);
    }
    if (criteria.ratingIsNull !== undefined) {
      conditions.push(
        criteria.ratingIsNull
          ? "`rating` IS NULL"
          : "`rating` IS NOT NULL",
      );
    }
    if (criteria.specialFeatures !== undefined) {
      conditions.push("`special_features` = ?");
      values.push(criteria.specialFeatures);
    }
    if (criteria.specialFeaturesLike !== undefined) {
      conditions.push("`special_features` LIKE ?");
      values.push(criteria.specialFeaturesLike);
    }
    if (criteria.specialFeaturesIsNull !== undefined) {
      conditions.push(
        criteria.specialFeaturesIsNull
          ? "`special_features` IS NULL"
          : "`special_features` IS NOT NULL",
      );
    }
    if (criteria.lastUpdate !== undefined) {
      conditions.push("`last_update` = ?");
      values.push(criteria.lastUpdate);
    }
    if (criteria.lastUpdateFrom !== undefined) {
      conditions.push("`last_update` >= ?");
      values.push(criteria.lastUpdateFrom);
    }
    if (criteria.lastUpdateTo !== undefined) {
      conditions.push("`last_update` <= ?");
      values.push(criteria.lastUpdateTo);
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              filmColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
