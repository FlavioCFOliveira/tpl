// Code generated by tpl from sakila.film. DO NOT EDIT.

/**
 * The `sakila.film` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

/**
 * One row of `sakila.film`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} Film
 * @property {number} filmId `film_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
 *   It is part of the primary key.
 * @property {string} title `title`, declared `varchar(128)` NOT NULL.
 * @property {string | null} description `description`, declared `text` NULL.
 * @property {number | null} releaseYear `release_year`, declared `year(4)` NULL.
 * @property {number} languageId `language_id`, declared `tinyint(3) unsigned` NOT NULL.
 * @property {number | null} originalLanguageId `original_language_id`, declared `tinyint(3) unsigned` NULL.
 * @property {number} rentalDuration `rental_duration`, declared `tinyint(3) unsigned` NOT NULL.
 * @property {string} rentalRate `rental_rate`, declared `decimal(4,2)` NOT NULL.
 * @property {number | null} length `length`, declared `smallint(5) unsigned` NULL.
 * @property {string} replacementCost `replacement_cost`, declared `decimal(5,2)` NOT NULL.
 * @property {string | null} rating `rating`, declared `enum('G','PG','PG-13','R','NC-17')` NULL.
 * @property {string | null} specialFeatures `special_features`, declared `set('Trailers','Commentaries','Deleted Scenes','Behind the Scenes')` NULL.
 * @property {Date} lastUpdate `last_update`, declared `timestamp` NOT NULL.
 */

/**
 * The primary key of `sakila.film`.
 *
 * @typedef {object} FilmKey
 * @property {number} filmId `film_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
 */

/**
 * One column of `sakila.film`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link FilmColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const FilmColumns = Object.freeze(
  /** @type {const} */ ({
    /** `film_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT. */
    FILM_ID: "film_id",
    /** `title`, declared `varchar(128)` NOT NULL. */
    TITLE: "title",
    /** `description`, declared `text` NULL. */
    DESCRIPTION: "description",
    /** `release_year`, declared `year(4)` NULL. */
    RELEASE_YEAR: "release_year",
    /** `language_id`, declared `tinyint(3) unsigned` NOT NULL. */
    LANGUAGE_ID: "language_id",
    /** `original_language_id`, declared `tinyint(3) unsigned` NULL. */
    ORIGINAL_LANGUAGE_ID: "original_language_id",
    /** `rental_duration`, declared `tinyint(3) unsigned` NOT NULL. */
    RENTAL_DURATION: "rental_duration",
    /** `rental_rate`, declared `decimal(4,2)` NOT NULL. */
    RENTAL_RATE: "rental_rate",
    /** `length`, declared `smallint(5) unsigned` NULL. */
    LENGTH: "length",
    /** `replacement_cost`, declared `decimal(5,2)` NOT NULL. */
    REPLACEMENT_COST: "replacement_cost",
    /** `rating`, declared `enum('G','PG','PG-13','R','NC-17')` NULL. */
    RATING: "rating",
    /** `special_features`, declared `set('Trailers','Commentaries','Deleted Scenes','Behind the Scenes')` NULL. */
    SPECIAL_FEATURES: "special_features",
    /** `last_update`, declared `timestamp` NOT NULL. */
    LAST_UPDATE: "last_update",
  }),
);

/**
 * A column of `sakila.film` an ordering may name.
 *
 * @typedef {(typeof FilmColumns)[keyof typeof FilmColumns]} FilmColumn
 */

/**
 * The one place a column of `sakila.film` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<FilmColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "film_id": "`film_id`",
  "title": "`title`",
  "description": "`description`",
  "release_year": "`release_year`",
  "language_id": "`language_id`",
  "original_language_id": "`original_language_id`",
  "rental_duration": "`rental_duration`",
  "rental_rate": "`rental_rate`",
  "length": "`length`",
  "replacement_cost": "`replacement_cost`",
  "rating": "`rating`",
  "special_features": "`special_features`",
  "last_update": "`last_update`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {FilmColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function filmColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("sakila.film", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `sakila.film`.
 *
 * @typedef {object} FilmOrder
 * @property {FilmColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `sakila.film`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} FilmFilter
 * @property {number} [filmId] where set, requires `film_id` to equal it.
 * @property {number} [filmIdFrom] where set, requires `film_id` to be at least it.
 * @property {number} [filmIdTo] where set, requires `film_id` to be at most it.
 * @property {string} [title] where set, requires `title` to equal it.
 * @property {string} [titleLike] where set, requires `title` to match it, as LIKE matches.
 * @property {string} [description] where set, requires `description` to equal it.
 * @property {string} [descriptionLike] where set, requires `description` to match it, as LIKE matches.
 * @property {boolean} [descriptionIsNull] where true, requires `description` to be NULL; where false, requires it not to be.
 * @property {number} [releaseYear] where set, requires `release_year` to equal it.
 * @property {number} [releaseYearFrom] where set, requires `release_year` to be at least it.
 * @property {number} [releaseYearTo] where set, requires `release_year` to be at most it.
 * @property {boolean} [releaseYearIsNull] where true, requires `release_year` to be NULL; where false, requires it not to be.
 * @property {number} [languageId] where set, requires `language_id` to equal it.
 * @property {number} [languageIdFrom] where set, requires `language_id` to be at least it.
 * @property {number} [languageIdTo] where set, requires `language_id` to be at most it.
 * @property {number} [originalLanguageId] where set, requires `original_language_id` to equal it.
 * @property {number} [originalLanguageIdFrom] where set, requires `original_language_id` to be at least it.
 * @property {number} [originalLanguageIdTo] where set, requires `original_language_id` to be at most it.
 * @property {boolean} [originalLanguageIdIsNull] where true, requires `original_language_id` to be NULL; where false, requires it not to be.
 * @property {number} [rentalDuration] where set, requires `rental_duration` to equal it.
 * @property {number} [rentalDurationFrom] where set, requires `rental_duration` to be at least it.
 * @property {number} [rentalDurationTo] where set, requires `rental_duration` to be at most it.
 * @property {string} [rentalRate] where set, requires `rental_rate` to equal it.
 * @property {string} [rentalRateFrom] where set, requires `rental_rate` to be at least it.
 * @property {string} [rentalRateTo] where set, requires `rental_rate` to be at most it.
 * @property {number} [length] where set, requires `length` to equal it.
 * @property {number} [lengthFrom] where set, requires `length` to be at least it.
 * @property {number} [lengthTo] where set, requires `length` to be at most it.
 * @property {boolean} [lengthIsNull] where true, requires `length` to be NULL; where false, requires it not to be.
 * @property {string} [replacementCost] where set, requires `replacement_cost` to equal it.
 * @property {string} [replacementCostFrom] where set, requires `replacement_cost` to be at least it.
 * @property {string} [replacementCostTo] where set, requires `replacement_cost` to be at most it.
 * @property {string} [rating] where set, requires `rating` to equal it.
 * @property {string} [ratingLike] where set, requires `rating` to match it, as LIKE matches.
 * @property {boolean} [ratingIsNull] where true, requires `rating` to be NULL; where false, requires it not to be.
 * @property {string} [specialFeatures] where set, requires `special_features` to equal it.
 * @property {string} [specialFeaturesLike] where set, requires `special_features` to match it, as LIKE matches.
 * @property {boolean} [specialFeaturesIsNull] where true, requires `special_features` to be NULL; where false, requires it not to be.
 * @property {Date} [lastUpdate] where set, requires `last_update` to equal it.
 * @property {Date} [lastUpdateFrom] where set, requires `last_update` to be at least it.
 * @property {Date} [lastUpdateTo] where set, requires `last_update` to be at most it.
 */
