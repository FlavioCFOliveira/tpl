// Code generated by tpl from sakila.address. DO NOT EDIT.

/**
 * The mysql2 implementation of the `sakila.address` data layer.
 *
 * @module
 */

/** @import { Connection, ResultSetHeader, RowDataPacket } from 'mysql2/promise' */
/** @import { BoundValue } from './core.js' */
/** @import { Address, AddressFilter, AddressKey, AddressOrder } from './address.js' */

import { NotFound, directionSql, failure } from './core.js';
import { addressColumnSql } from './address.js';
import { AddressRepository } from './address_repository.js';

/** The table every statement of this module addresses, as an error names it. */
const TABLE = "sakila.address";

/** Selects every column of the table, in catalogue order. */
const SELECT = "SELECT `address_id`, `address`, `address2`, `district`, `city_id`, `postal_code`, `phone`, `last_update` FROM `address`";

/** Selects the one row a primary key names. */
const READ = "SELECT `address_id`, `address`, `address2`, `district`, `city_id`, `postal_code`, `phone`, `last_update` FROM `address` WHERE `address_id` = ?";

/**
 * Inserts one row. It names neither a generated column nor an auto-increment
 * key, because the server writes both.
 */
const INSERT = "INSERT INTO `address` (`address`, `address2`, `district`, `city_id`, `postal_code`, `phone`, `last_update`) VALUES (?, ?, ?, ?, ?, ?, ?)";

/** Writes every column that is neither generated nor part of the key. */
const UPDATE = "UPDATE `address` SET `address` = ?, `address2` = ?, `district` = ?, `city_id` = ?, `postal_code` = ?, `phone` = ?, `last_update` = ? WHERE `address_id` = ?";

/** Removes the one row a primary key names. */
const DELETE = "DELETE FROM `address` WHERE `address_id` = ?";

/**
 * Build a row from one result tuple.
 *
 * The positions are the order `SELECT` names the columns in, which is
 * catalogue order. Every statement below is run with `rowsAsArray`, so a
 * result is an array and never an object: a column named `__proto__` or
 * `constructor` would otherwise reach an object literal as a key, and the
 * first of those sets a prototype instead of defining a property.
 *
 * The values are whatever mysql2 decoded, and the types `Address` declares
 * are this example's record of what that is.
 *
 * @param {RowDataPacket} values
 * @returns {Address}
 */
function toRow(values) {
  return {
    addressId: values[0],
    address: values[1],
    address2: values[2],
    district: values[3],
    cityId: values[4],
    postalCode: values[5],
    phone: values[6],
    lastUpdate: values[7],
  };
}

/**
 * The mysql2 data layer of `sakila.address`.
 *
 * Nothing here commits. mysql2 autocommits until a caller calls
 * `beginTransaction`, so transaction control stays with the caller.
 */
export class AddressMysql2 extends AddressRepository {
  /** @type {Connection} */
  #connection;

  /**
   * Build the data layer of `sakila.address` over `connection`.
   *
   * The connection must address the schema `SCHEMA` names, because the
   * statements above address the table unqualified. `connect` is the
   * connection that carries it.
   *
   * @param {Connection} connection
   */
  constructor(connection) {
    super();
    this.#connection = connection;
  }

  /**
   * Insert `row`.
   *
   * The key is assigned by the server and is written back into `row`.
   *
   * @override
   * @param {Address} row
   * @returns {Promise<void>}
   */
  async create(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.address,
      row.address2,
      row.district,
      row.cityId,
      row.postalCode,
      row.phone,
      row.lastUpdate,
    ];
    try {
      const [result] = await this.#connection.execute(INSERT, values);
      const { insertId } = /** @type {ResultSetHeader} */ (result);
      row.addressId = insertId;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return the row `key` names.
   *
   * @override
   * @param {AddressKey} key
   * @returns {Promise<Address>}
   */
  async read(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.addressId,
    ];
    try {
      const [rows] = await this.#connection.execute(
        { sql: READ, rowsAsArray: true },
        values,
      );
      const found = /** @type {RowDataPacket[]} */ (rows);
      if (found.length === 0) {
        throw new NotFound(TABLE);
      }
      return toRow(found[0]);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Write every writable column of `row` over the row its key names.
   *
   * @override
   * @param {Address} row
   * @returns {Promise<number>}
   */
  async update(row) {
    /** @type {BoundValue[]} */
    const values = [
      row.address,
      row.address2,
      row.district,
      row.cityId,
      row.postalCode,
      row.phone,
      row.lastUpdate,
      row.addressId,
    ];
    try {
      const [result] = await this.#connection.execute(UPDATE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Remove the row `key` names.
   *
   * @override
   * @param {AddressKey} key
   * @returns {Promise<number>}
   */
  async delete(key) {
    /** @type {BoundValue[]} */
    const values = [
      key.addressId,
    ];
    try {
      const [result] = await this.#connection.execute(DELETE, values);
      return /** @type {ResultSetHeader} */ (result).affectedRows;
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }

  /**
   * Return every row matching `criteria`, ordered by the terms of `order`.
   *
   * @override
   * @param {AddressFilter} criteria
   * @param {readonly AddressOrder[]} [order]
   * @param {number | null} [limit]
   * @param {number | null} [offset]
   * @returns {Promise<Address[]>}
   */
  async search(criteria, order = [], limit = null, offset = null) {
    /** @type {string[]} */
    const conditions = [];
    /** @type {BoundValue[]} */
    const values = [];

    if (criteria.addressId !== undefined) {
      conditions.push("`address_id` = ?");
      values.push(criteria.addressId);
    }
    if (criteria.addressIdFrom !== undefined) {
      conditions.push("`address_id` >= ?");
      values.push(criteria.addressIdFrom);
    }
    if (criteria.addressIdTo !== undefined) {
      conditions.push("`address_id` <= ?");
      values.push(criteria.addressIdTo);
    }
    if (criteria.address !== undefined) {
      conditions.push("`address` = ?");
      values.push(criteria.address);
    }
    if (criteria.addressLike !== undefined) {
      conditions.push("`address` LIKE ?");
      values.push(criteria.addressLike);
    }
    if (criteria.address2 !== undefined) {
      conditions.push("`address2` = ?");
      values.push(criteria.address2);
    }
    if (criteria.address2Like !== undefined) {
      conditions.push("`address2` LIKE ?");
      values.push(criteria.address2Like);
    }
    if (criteria.address2IsNull !== undefined) {
      conditions.push(
        criteria.address2IsNull
          ? "`address2` IS NULL"
          : "`address2` IS NOT NULL",
      );
    }
    if (criteria.district !== undefined) {
      conditions.push("`district` = ?");
      values.push(criteria.district);
    }
    if (criteria.districtLike !== undefined) {
      conditions.push("`district` LIKE ?");
      values.push(criteria.districtLike);
    }
    if (criteria.cityId !== undefined) {
      conditions.push("`city_id` = ?");
      values.push(criteria.cityId);
    }
    if (criteria.cityIdFrom !== undefined) {
      conditions.push("`city_id` >= ?");
      values.push(criteria.cityIdFrom);
    }
    if (criteria.cityIdTo !== undefined) {
      conditions.push("`city_id` <= ?");
      values.push(criteria.cityIdTo);
    }
    if (criteria.postalCode !== undefined) {
      conditions.push("`postal_code` = ?");
      values.push(criteria.postalCode);
    }
    if (criteria.postalCodeLike !== undefined) {
      conditions.push("`postal_code` LIKE ?");
      values.push(criteria.postalCodeLike);
    }
    if (criteria.postalCodeIsNull !== undefined) {
      conditions.push(
        criteria.postalCodeIsNull
          ? "`postal_code` IS NULL"
          : "`postal_code` IS NOT NULL",
      );
    }
    if (criteria.phone !== undefined) {
      conditions.push("`phone` = ?");
      values.push(criteria.phone);
    }
    if (criteria.phoneLike !== undefined) {
      conditions.push("`phone` LIKE ?");
      values.push(criteria.phoneLike);
    }
    if (criteria.lastUpdate !== undefined) {
      conditions.push("`last_update` = ?");
      values.push(criteria.lastUpdate);
    }
    if (criteria.lastUpdateFrom !== undefined) {
      conditions.push("`last_update` >= ?");
      values.push(criteria.lastUpdateFrom);
    }
    if (criteria.lastUpdateTo !== undefined) {
      conditions.push("`last_update` <= ?");
      values.push(criteria.lastUpdateTo);
    }

    let sql = SELECT;
    if (conditions.length > 0) {
      sql += ' WHERE ' + conditions.join(' AND ');
    }
    if (order.length > 0) {
      sql +=
        ' ORDER BY ' +
        order
          .map(
            (term) =>
              addressColumnSql(term.column) +
              ' ' +
              directionSql(term.direction ?? 'asc'),
          )
          .join(', ');
    }
    if (limit !== null) {
      sql += ' LIMIT ?';
      values.push(limit);
      if (offset !== null) {
        sql += ' OFFSET ?';
        values.push(offset);
      }
    }

    try {
      const [rows] = await this.#connection.execute(
        { sql, rowsAsArray: true },
        values,
      );
      return /** @type {RowDataPacket[]} */ (rows).map(toRow);
    } catch (cause) {
      throw failure(TABLE, cause);
    }
  }
}
