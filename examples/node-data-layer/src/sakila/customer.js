// Code generated by tpl from sakila.customer. DO NOT EDIT.

/**
 * The `sakila.customer` row, the key that addresses it, the columns an ordering may
 * name, and the filter a search takes.
 *
 * @module
 */

/** @import { Direction } from './core.js' */

import { UnknownColumn } from './core.js';

/**
 * One row of `sakila.customer`.
 *
 * Every property is present on a row. A column that admits `NULL` is typed
 * `T | null`, which is what mysql2 hands back for a `NULL` and what `tsc`
 * makes a caller narrow before reading.
 *
 * @typedef {object} Customer
 * @property {number} customerId `customer_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
 *   It is part of the primary key.
 * @property {number} storeId `store_id`, declared `tinyint(3) unsigned` NOT NULL.
 * @property {string} firstName `first_name`, declared `varchar(45)` NOT NULL.
 * @property {string} lastName `last_name`, declared `varchar(45)` NOT NULL.
 * @property {string | null} email `email`, declared `varchar(50)` NULL.
 * @property {number} addressId `address_id`, declared `smallint(5) unsigned` NOT NULL.
 * @property {number} active `active`, declared `tinyint(1)` NOT NULL.
 * @property {Date} createDate `create_date`, declared `datetime` NOT NULL.
 * @property {Date | null} lastUpdate `last_update`, declared `timestamp` NULL.
 */

/**
 * The primary key of `sakila.customer`.
 *
 * @typedef {object} CustomerKey
 * @property {number} customerId `customer_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
 */

/**
 * One column of `sakila.customer`.
 *
 * A search accepts an ordering term over these values and over nothing else,
 * so an ordering can carry no SQL of its own. A caller may write the column's
 * catalogue name as a string literal instead: {@link CustomerColumn} is the union
 * of the values below, so the two are the same value and the same type.
 */
export const CustomerColumns = Object.freeze(
  /** @type {const} */ ({
    /** `customer_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT. */
    CUSTOMER_ID: "customer_id",
    /** `store_id`, declared `tinyint(3) unsigned` NOT NULL. */
    STORE_ID: "store_id",
    /** `first_name`, declared `varchar(45)` NOT NULL. */
    FIRST_NAME: "first_name",
    /** `last_name`, declared `varchar(45)` NOT NULL. */
    LAST_NAME: "last_name",
    /** `email`, declared `varchar(50)` NULL. */
    EMAIL: "email",
    /** `address_id`, declared `smallint(5) unsigned` NOT NULL. */
    ADDRESS_ID: "address_id",
    /** `active`, declared `tinyint(1)` NOT NULL. */
    ACTIVE: "active",
    /** `create_date`, declared `datetime` NOT NULL. */
    CREATE_DATE: "create_date",
    /** `last_update`, declared `timestamp` NULL. */
    LAST_UPDATE: "last_update",
  }),
);

/**
 * A column of `sakila.customer` an ordering may name.
 *
 * @typedef {(typeof CustomerColumns)[keyof typeof CustomerColumns]} CustomerColumn
 */

/**
 * The one place a column of `sakila.customer` becomes SQL.
 *
 * It is closed over the constants above, and every identifier in it was quoted
 * at render time by `tpl`, so no value a caller supplies can reach a
 * statement's text through an ordering term.
 *
 * @type {Readonly<Record<CustomerColumn, string>>}
 */
const COLUMN_SQL = Object.freeze({
  "customer_id": "`customer_id`",
  "store_id": "`store_id`",
  "first_name": "`first_name`",
  "last_name": "`last_name`",
  "email": "`email`",
  "address_id": "`address_id`",
  "active": "`active`",
  "create_date": "`create_date`",
  "last_update": "`last_update`",
});

/**
 * The column as a statement's text names it: quoted for MariaDB.
 *
 * The lookup is by own property and the refusal is unconditional, so a caller
 * that reached past the type — with an unchecked string, or a value read from
 * a request — is refused here rather than interpolated.
 *
 * @param {CustomerColumn} column
 * @returns {string}
 * @throws {UnknownColumn} for every name the table does not carry.
 */
export function customerColumnSql(column) {
  if (!Object.hasOwn(COLUMN_SQL, column)) {
    throw new UnknownColumn("sakila.customer", String(column));
  }
  return COLUMN_SQL[column];
}

/**
 * One ordering term of a search over `sakila.customer`.
 *
 * @typedef {object} CustomerOrder
 * @property {CustomerColumn} column the column to order by
 * @property {Direction} [direction] the direction to order in. Ascending when absent.
 */

/**
 * What narrows a search over `sakila.customer`.
 *
 * Every property is optional: a property left absent adds no condition, and the
 * conditions a search does build are joined with `AND`. The empty object is the
 * filter that matches every row.
 *
 * A property is absent for *no condition* and is never `null` for *is NULL*.
 * That question is asked through the `…IsNull` property a nullable column also
 * gets, so the two meanings never collide, and it is why no filter property
 * admits `null`.
 *
 * @typedef {object} CustomerFilter
 * @property {number} [customerId] where set, requires `customer_id` to equal it.
 * @property {number} [customerIdFrom] where set, requires `customer_id` to be at least it.
 * @property {number} [customerIdTo] where set, requires `customer_id` to be at most it.
 * @property {number} [storeId] where set, requires `store_id` to equal it.
 * @property {number} [storeIdFrom] where set, requires `store_id` to be at least it.
 * @property {number} [storeIdTo] where set, requires `store_id` to be at most it.
 * @property {string} [firstName] where set, requires `first_name` to equal it.
 * @property {string} [firstNameLike] where set, requires `first_name` to match it, as LIKE matches.
 * @property {string} [lastName] where set, requires `last_name` to equal it.
 * @property {string} [lastNameLike] where set, requires `last_name` to match it, as LIKE matches.
 * @property {string} [email] where set, requires `email` to equal it.
 * @property {string} [emailLike] where set, requires `email` to match it, as LIKE matches.
 * @property {boolean} [emailIsNull] where true, requires `email` to be NULL; where false, requires it not to be.
 * @property {number} [addressId] where set, requires `address_id` to equal it.
 * @property {number} [addressIdFrom] where set, requires `address_id` to be at least it.
 * @property {number} [addressIdTo] where set, requires `address_id` to be at most it.
 * @property {number} [active] where set, requires `active` to equal it.
 * @property {number} [activeFrom] where set, requires `active` to be at least it.
 * @property {number} [activeTo] where set, requires `active` to be at most it.
 * @property {Date} [createDate] where set, requires `create_date` to equal it.
 * @property {Date} [createDateFrom] where set, requires `create_date` to be at least it.
 * @property {Date} [createDateTo] where set, requires `create_date` to be at most it.
 * @property {Date} [lastUpdate] where set, requires `last_update` to equal it.
 * @property {Date} [lastUpdateFrom] where set, requires `last_update` to be at least it.
 * @property {Date} [lastUpdateTo] where set, requires `last_update` to be at most it.
 * @property {boolean} [lastUpdateIsNull] where true, requires `last_update` to be NULL; where false, requires it not to be.
 */
