// Code generated by tpl from sakila.category. DO NOT EDIT.

package sakila

import (
	"time"
)

// Category is one row of sakila.category.
type Category struct {
	// CategoryId is category_id, declared tinyint(3) unsigned NOT NULL AUTO_INCREMENT.
	// It is part of the primary key.
	CategoryId uint8
	// Name is name, declared varchar(25) NOT NULL.
	Name string
	// LastUpdate is last_update, declared timestamp NOT NULL.
	LastUpdate time.Time
}

// CategoryKey is the primary key of sakila.category.
type CategoryKey struct {
	// CategoryId is category_id.
	CategoryId uint8
}

// Key returns the primary key of the row.
func (row *Category) Key() CategoryKey {
	var key CategoryKey
	key.CategoryId = row.CategoryId
	return key
}

// CategoryColumn names a column of sakila.category.
//
// A search accepts an ordering term only over the constants below, so an
// ordering can carry no SQL of its own.
type CategoryColumn string

// The columns of sakila.category, in catalogue order.
const (
	// CategoryColumnCategoryId names category_id.
	CategoryColumnCategoryId CategoryColumn = "category_id"
	// CategoryColumnName names name.
	CategoryColumnName CategoryColumn = "name"
	// CategoryColumnLastUpdate names last_update.
	CategoryColumnLastUpdate CategoryColumn = "last_update"
)

// CategoryOrder is one ordering term of a search over sakila.category.
type CategoryOrder struct {
	// Column is the column to order by.
	Column CategoryColumn
	// Direction is the direction to order in.
	Direction Direction
}

// CategoryFilter narrows a search over sakila.category.
//
// Every field is optional: a field left nil adds no condition, and the
// conditions a search does build are joined with AND.
type CategoryFilter struct {
	// CategoryId, where set, requires category_id to equal it.
	CategoryId *uint8
	// CategoryIdFrom, where set, requires category_id to be at least it.
	CategoryIdFrom *uint8
	// CategoryIdTo, where set, requires category_id to be at most it.
	CategoryIdTo *uint8
	// Name, where set, requires name to equal it.
	Name *string
	// NameLike, where set, requires name to match it, as LIKE matches.
	NameLike *string
	// LastUpdate, where set, requires last_update to equal it.
	LastUpdate *time.Time
	// LastUpdateFrom, where set, requires last_update to be at least it.
	LastUpdateFrom *time.Time
	// LastUpdateTo, where set, requires last_update to be at most it.
	LastUpdateTo *time.Time
}
