// Code generated by tpl from sakila.actor. DO NOT EDIT.

package sakila

import (
	"time"
)

// Actor is one row of sakila.actor.
type Actor struct {
	// ActorId is actor_id, declared smallint(5) unsigned NOT NULL AUTO_INCREMENT.
	// It is part of the primary key.
	ActorId uint16
	// FirstName is first_name, declared varchar(45) NOT NULL.
	FirstName string
	// LastName is last_name, declared varchar(45) NOT NULL.
	LastName string
	// LastUpdate is last_update, declared timestamp NOT NULL.
	LastUpdate time.Time
}

// ActorKey is the primary key of sakila.actor.
type ActorKey struct {
	// ActorId is actor_id.
	ActorId uint16
}

// Key returns the primary key of the row.
func (row *Actor) Key() ActorKey {
	var key ActorKey
	key.ActorId = row.ActorId
	return key
}

// ActorColumn names a column of sakila.actor.
//
// A search accepts an ordering term only over the constants below, so an
// ordering can carry no SQL of its own.
type ActorColumn string

// The columns of sakila.actor, in catalogue order.
const (
	// ActorColumnActorId names actor_id.
	ActorColumnActorId ActorColumn = "actor_id"
	// ActorColumnFirstName names first_name.
	ActorColumnFirstName ActorColumn = "first_name"
	// ActorColumnLastName names last_name.
	ActorColumnLastName ActorColumn = "last_name"
	// ActorColumnLastUpdate names last_update.
	ActorColumnLastUpdate ActorColumn = "last_update"
)

// ActorOrder is one ordering term of a search over sakila.actor.
type ActorOrder struct {
	// Column is the column to order by.
	Column ActorColumn
	// Direction is the direction to order in.
	Direction Direction
}

// ActorFilter narrows a search over sakila.actor.
//
// Every field is optional: a field left nil adds no condition, and the
// conditions a search does build are joined with AND.
type ActorFilter struct {
	// ActorId, where set, requires actor_id to equal it.
	ActorId *uint16
	// ActorIdFrom, where set, requires actor_id to be at least it.
	ActorIdFrom *uint16
	// ActorIdTo, where set, requires actor_id to be at most it.
	ActorIdTo *uint16
	// FirstName, where set, requires first_name to equal it.
	FirstName *string
	// FirstNameLike, where set, requires first_name to match it, as LIKE matches.
	FirstNameLike *string
	// LastName, where set, requires last_name to equal it.
	LastName *string
	// LastNameLike, where set, requires last_name to match it, as LIKE matches.
	LastNameLike *string
	// LastUpdate, where set, requires last_update to equal it.
	LastUpdate *time.Time
	// LastUpdateFrom, where set, requires last_update to be at least it.
	LastUpdateFrom *time.Time
	// LastUpdateTo, where set, requires last_update to be at most it.
	LastUpdateTo *time.Time
}
