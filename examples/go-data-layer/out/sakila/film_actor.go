// Code generated by tpl from sakila.film_actor. DO NOT EDIT.

package sakila

import (
	"time"
)

// FilmActor is one row of sakila.film_actor.
type FilmActor struct {
	// ActorId is actor_id, declared smallint(5) unsigned NOT NULL.
	// It is part of the primary key.
	ActorId uint16
	// FilmId is film_id, declared smallint(5) unsigned NOT NULL.
	// It is part of the primary key.
	FilmId uint16
	// LastUpdate is last_update, declared timestamp NOT NULL.
	LastUpdate time.Time
}

// FilmActorKey is the primary key of sakila.film_actor.
type FilmActorKey struct {
	// ActorId is actor_id.
	ActorId uint16
	// FilmId is film_id.
	FilmId uint16
}

// Key returns the primary key of the row.
func (row *FilmActor) Key() FilmActorKey {
	var key FilmActorKey
	key.ActorId = row.ActorId
	key.FilmId = row.FilmId
	return key
}

// FilmActorColumn names a column of sakila.film_actor.
//
// A search accepts an ordering term only over the constants below, so an
// ordering can carry no SQL of its own.
type FilmActorColumn string

// The columns of sakila.film_actor, in catalogue order.
const (
	// FilmActorColumnActorId names actor_id.
	FilmActorColumnActorId FilmActorColumn = "actor_id"
	// FilmActorColumnFilmId names film_id.
	FilmActorColumnFilmId FilmActorColumn = "film_id"
	// FilmActorColumnLastUpdate names last_update.
	FilmActorColumnLastUpdate FilmActorColumn = "last_update"
)

// FilmActorOrder is one ordering term of a search over sakila.film_actor.
type FilmActorOrder struct {
	// Column is the column to order by.
	Column FilmActorColumn
	// Direction is the direction to order in.
	Direction Direction
}

// FilmActorFilter narrows a search over sakila.film_actor.
//
// Every field is optional: a field left nil adds no condition, and the
// conditions a search does build are joined with AND.
type FilmActorFilter struct {
	// ActorId, where set, requires actor_id to equal it.
	ActorId *uint16
	// ActorIdFrom, where set, requires actor_id to be at least it.
	ActorIdFrom *uint16
	// ActorIdTo, where set, requires actor_id to be at most it.
	ActorIdTo *uint16
	// FilmId, where set, requires film_id to equal it.
	FilmId *uint16
	// FilmIdFrom, where set, requires film_id to be at least it.
	FilmIdFrom *uint16
	// FilmIdTo, where set, requires film_id to be at most it.
	FilmIdTo *uint16
	// LastUpdate, where set, requires last_update to equal it.
	LastUpdate *time.Time
	// LastUpdateFrom, where set, requires last_update to be at least it.
	LastUpdateFrom *time.Time
	// LastUpdateTo, where set, requires last_update to be at most it.
	LastUpdateTo *time.Time
}
