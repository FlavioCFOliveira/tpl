// Code generated by tpl from sakila.language. DO NOT EDIT.

package sakila

import (
	"time"
)

// Language is one row of sakila.language.
type Language struct {
	// LanguageId is language_id, declared tinyint(3) unsigned NOT NULL AUTO_INCREMENT.
	// It is part of the primary key.
	LanguageId uint8
	// Name is name, declared char(20) NOT NULL.
	Name string
	// LastUpdate is last_update, declared timestamp NOT NULL.
	LastUpdate time.Time
}

// LanguageKey is the primary key of sakila.language.
type LanguageKey struct {
	// LanguageId is language_id.
	LanguageId uint8
}

// Key returns the primary key of the row.
func (row *Language) Key() LanguageKey {
	var key LanguageKey
	key.LanguageId = row.LanguageId
	return key
}

// LanguageColumn names a column of sakila.language.
//
// A search accepts an ordering term only over the constants below, so an
// ordering can carry no SQL of its own.
type LanguageColumn string

// The columns of sakila.language, in catalogue order.
const (
	// LanguageColumnLanguageId names language_id.
	LanguageColumnLanguageId LanguageColumn = "language_id"
	// LanguageColumnName names name.
	LanguageColumnName LanguageColumn = "name"
	// LanguageColumnLastUpdate names last_update.
	LanguageColumnLastUpdate LanguageColumn = "last_update"
)

// LanguageOrder is one ordering term of a search over sakila.language.
type LanguageOrder struct {
	// Column is the column to order by.
	Column LanguageColumn
	// Direction is the direction to order in.
	Direction Direction
}

// LanguageFilter narrows a search over sakila.language.
//
// Every field is optional: a field left nil adds no condition, and the
// conditions a search does build are joined with AND.
type LanguageFilter struct {
	// LanguageId, where set, requires language_id to equal it.
	LanguageId *uint8
	// LanguageIdFrom, where set, requires language_id to be at least it.
	LanguageIdFrom *uint8
	// LanguageIdTo, where set, requires language_id to be at most it.
	LanguageIdTo *uint8
	// Name, where set, requires name to equal it.
	Name *string
	// NameLike, where set, requires name to match it, as LIKE matches.
	NameLike *string
	// LastUpdate, where set, requires last_update to equal it.
	LastUpdate *time.Time
	// LastUpdateFrom, where set, requires last_update to be at least it.
	LastUpdateFrom *time.Time
	// LastUpdateTo, where set, requires last_update to be at most it.
	LastUpdateTo *time.Time
}
