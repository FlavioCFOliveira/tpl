// Code generated by tpl from sakila.address. DO NOT EDIT.

package sakila

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// addressSQL is the database/sql implementation of AddressRepository.
type addressSQL struct {
	db *sql.DB
}

// NewAddressRepository builds the data layer of sakila.address over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewAddressRepository(db *sql.DB) AddressRepository {
	return &addressSQL{db: db}
}

// addressSelect lists every column of sakila.address, in catalogue order.
const addressSelect = "SELECT `address_id`, `address`, `address2`, `district`, `city_id`, `postal_code`, `phone`, `last_update` FROM `address`"

// addressRead reads the one row a primary key names.
const addressRead = "SELECT `address_id`, `address`, `address2`, `district`, `city_id`, `postal_code`, `phone`, `last_update` FROM `address` WHERE `address_id` = ?"

// addressInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const addressInsert = "INSERT INTO `address` (`address`, `address2`, `district`, `city_id`, `postal_code`, `phone`, `last_update`) VALUES (?, ?, ?, ?, ?, ?, ?)"

// addressUpdate writes every column that is neither generated nor a key.
const addressUpdate = "UPDATE `address` SET `address` = ?, `address2` = ?, `district` = ?, `city_id` = ?, `postal_code` = ?, `phone` = ?, `last_update` = ? WHERE `address_id` = ?"

// addressDelete removes the one row a primary key names.
const addressDelete = "DELETE FROM `address` WHERE `address_id` = ?"

// Create inserts row.
func (r *addressSQL) Create(ctx context.Context, row *Address) error {
	result, err := r.db.ExecContext(ctx, addressInsert,
		row.Address,
		row.Address2,
		row.District,
		row.CityId,
		row.PostalCode,
		row.Phone,
		row.LastUpdate,
	)
	if err != nil {
		return fmt.Errorf("sakila.address: create: %w", err)
	}
	assigned, err := result.LastInsertId()
	if err != nil {
		return fmt.Errorf("sakila.address: create: reading the key the server assigned: %w", err)
	}
	row.AddressId = uint16(assigned)
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *addressSQL) Read(ctx context.Context, key AddressKey) (*Address, error) {
	var row Address
	err := r.db.QueryRowContext(ctx, addressRead,
		key.AddressId,
	).Scan(
		&row.AddressId,
		&row.Address,
		&row.Address2,
		&row.District,
		&row.CityId,
		&row.PostalCode,
		&row.Phone,
		&row.LastUpdate,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("sakila.address: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("sakila.address: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *addressSQL) Update(ctx context.Context, row *Address) (int64, error) {
	result, err := r.db.ExecContext(ctx, addressUpdate,
		row.Address,
		row.Address2,
		row.District,
		row.CityId,
		row.PostalCode,
		row.Phone,
		row.LastUpdate,
		row.AddressId,
	)
	if err != nil {
		return 0, fmt.Errorf("sakila.address: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("sakila.address: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *addressSQL) Delete(ctx context.Context, key AddressKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, addressDelete,
		key.AddressId,
	)
	if err != nil {
		return 0, fmt.Errorf("sakila.address: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("sakila.address: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *addressSQL) Search(ctx context.Context, filter AddressFilter, order []AddressOrder, limit, offset int) ([]Address, error) {
	conditions := make([]string, 0, 21)
	arguments := make([]any, 0, 23)

	if filter.AddressId != nil {
		conditions = append(conditions, "`address_id` = ?")
		arguments = append(arguments, *filter.AddressId)
	}
	if filter.AddressIdFrom != nil {
		conditions = append(conditions, "`address_id` >= ?")
		arguments = append(arguments, *filter.AddressIdFrom)
	}
	if filter.AddressIdTo != nil {
		conditions = append(conditions, "`address_id` <= ?")
		arguments = append(arguments, *filter.AddressIdTo)
	}
	if filter.Address != nil {
		conditions = append(conditions, "`address` = ?")
		arguments = append(arguments, *filter.Address)
	}
	if filter.AddressLike != nil {
		conditions = append(conditions, "`address` LIKE ?")
		arguments = append(arguments, *filter.AddressLike)
	}
	if filter.Address2 != nil {
		conditions = append(conditions, "`address2` = ?")
		arguments = append(arguments, *filter.Address2)
	}
	if filter.Address2Like != nil {
		conditions = append(conditions, "`address2` LIKE ?")
		arguments = append(arguments, *filter.Address2Like)
	}
	if filter.Address2IsNull != nil {
		if *filter.Address2IsNull {
			conditions = append(conditions, "`address2` IS NULL")
		} else {
			conditions = append(conditions, "`address2` IS NOT NULL")
		}
	}
	if filter.District != nil {
		conditions = append(conditions, "`district` = ?")
		arguments = append(arguments, *filter.District)
	}
	if filter.DistrictLike != nil {
		conditions = append(conditions, "`district` LIKE ?")
		arguments = append(arguments, *filter.DistrictLike)
	}
	if filter.CityId != nil {
		conditions = append(conditions, "`city_id` = ?")
		arguments = append(arguments, *filter.CityId)
	}
	if filter.CityIdFrom != nil {
		conditions = append(conditions, "`city_id` >= ?")
		arguments = append(arguments, *filter.CityIdFrom)
	}
	if filter.CityIdTo != nil {
		conditions = append(conditions, "`city_id` <= ?")
		arguments = append(arguments, *filter.CityIdTo)
	}
	if filter.PostalCode != nil {
		conditions = append(conditions, "`postal_code` = ?")
		arguments = append(arguments, *filter.PostalCode)
	}
	if filter.PostalCodeLike != nil {
		conditions = append(conditions, "`postal_code` LIKE ?")
		arguments = append(arguments, *filter.PostalCodeLike)
	}
	if filter.PostalCodeIsNull != nil {
		if *filter.PostalCodeIsNull {
			conditions = append(conditions, "`postal_code` IS NULL")
		} else {
			conditions = append(conditions, "`postal_code` IS NOT NULL")
		}
	}
	if filter.Phone != nil {
		conditions = append(conditions, "`phone` = ?")
		arguments = append(arguments, *filter.Phone)
	}
	if filter.PhoneLike != nil {
		conditions = append(conditions, "`phone` LIKE ?")
		arguments = append(arguments, *filter.PhoneLike)
	}
	if filter.LastUpdate != nil {
		conditions = append(conditions, "`last_update` = ?")
		arguments = append(arguments, *filter.LastUpdate)
	}
	if filter.LastUpdateFrom != nil {
		conditions = append(conditions, "`last_update` >= ?")
		arguments = append(arguments, *filter.LastUpdateFrom)
	}
	if filter.LastUpdateTo != nil {
		conditions = append(conditions, "`last_update` <= ?")
		arguments = append(arguments, *filter.LastUpdateTo)
	}

	var query strings.Builder
	query.WriteString(addressSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := addressOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("sakila.address: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("sakila.address: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("sakila.address: search: %w", err)
	}
	defer rows.Close()

	var found []Address
	for rows.Next() {
		var row Address
		if err := rows.Scan(
			&row.AddressId,
			&row.Address,
			&row.Address2,
			&row.District,
			&row.CityId,
			&row.PostalCode,
			&row.Phone,
			&row.LastUpdate,
		); err != nil {
			return nil, fmt.Errorf("sakila.address: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("sakila.address: search: %w", err)
	}
	return found, nil
}

// addressOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func addressOrderColumn(column AddressColumn) (string, error) {
	switch column {
	case AddressColumnAddressId:
		return "`address_id`", nil
	case AddressColumnAddress:
		return "`address`", nil
	case AddressColumnAddress2:
		return "`address2`", nil
	case AddressColumnDistrict:
		return "`district`", nil
	case AddressColumnCityId:
		return "`city_id`", nil
	case AddressColumnPostalCode:
		return "`postal_code`", nil
	case AddressColumnPhone:
		return "`phone`", nil
	case AddressColumnLastUpdate:
		return "`last_update`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
