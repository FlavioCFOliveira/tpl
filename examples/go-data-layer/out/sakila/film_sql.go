// Code generated by tpl from sakila.film. DO NOT EDIT.

package sakila

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// filmSQL is the database/sql implementation of FilmRepository.
type filmSQL struct {
	db *sql.DB
}

// NewFilmRepository builds the data layer of sakila.film over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewFilmRepository(db *sql.DB) FilmRepository {
	return &filmSQL{db: db}
}

// filmSelect lists every column of sakila.film, in catalogue order.
const filmSelect = "SELECT `film_id`, `title`, `description`, `release_year`, `language_id`, `original_language_id`, `rental_duration`, `rental_rate`, `length`, `replacement_cost`, `rating`, `special_features`, `last_update` FROM `film`"

// filmRead reads the one row a primary key names.
const filmRead = "SELECT `film_id`, `title`, `description`, `release_year`, `language_id`, `original_language_id`, `rental_duration`, `rental_rate`, `length`, `replacement_cost`, `rating`, `special_features`, `last_update` FROM `film` WHERE `film_id` = ?"

// filmInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const filmInsert = "INSERT INTO `film` (`title`, `description`, `release_year`, `language_id`, `original_language_id`, `rental_duration`, `rental_rate`, `length`, `replacement_cost`, `rating`, `special_features`, `last_update`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"

// filmUpdate writes every column that is neither generated nor a key.
const filmUpdate = "UPDATE `film` SET `title` = ?, `description` = ?, `release_year` = ?, `language_id` = ?, `original_language_id` = ?, `rental_duration` = ?, `rental_rate` = ?, `length` = ?, `replacement_cost` = ?, `rating` = ?, `special_features` = ?, `last_update` = ? WHERE `film_id` = ?"

// filmDelete removes the one row a primary key names.
const filmDelete = "DELETE FROM `film` WHERE `film_id` = ?"

// Create inserts row.
func (r *filmSQL) Create(ctx context.Context, row *Film) error {
	result, err := r.db.ExecContext(ctx, filmInsert,
		row.Title,
		row.Description,
		row.ReleaseYear,
		row.LanguageId,
		row.OriginalLanguageId,
		row.RentalDuration,
		row.RentalRate,
		row.Length,
		row.ReplacementCost,
		row.Rating,
		row.SpecialFeatures,
		row.LastUpdate,
	)
	if err != nil {
		return fmt.Errorf("sakila.film: create: %w", err)
	}
	assigned, err := result.LastInsertId()
	if err != nil {
		return fmt.Errorf("sakila.film: create: reading the key the server assigned: %w", err)
	}
	row.FilmId = uint16(assigned)
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *filmSQL) Read(ctx context.Context, key FilmKey) (*Film, error) {
	var row Film
	err := r.db.QueryRowContext(ctx, filmRead,
		key.FilmId,
	).Scan(
		&row.FilmId,
		&row.Title,
		&row.Description,
		&row.ReleaseYear,
		&row.LanguageId,
		&row.OriginalLanguageId,
		&row.RentalDuration,
		&row.RentalRate,
		&row.Length,
		&row.ReplacementCost,
		&row.Rating,
		&row.SpecialFeatures,
		&row.LastUpdate,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("sakila.film: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("sakila.film: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *filmSQL) Update(ctx context.Context, row *Film) (int64, error) {
	result, err := r.db.ExecContext(ctx, filmUpdate,
		row.Title,
		row.Description,
		row.ReleaseYear,
		row.LanguageId,
		row.OriginalLanguageId,
		row.RentalDuration,
		row.RentalRate,
		row.Length,
		row.ReplacementCost,
		row.Rating,
		row.SpecialFeatures,
		row.LastUpdate,
		row.FilmId,
	)
	if err != nil {
		return 0, fmt.Errorf("sakila.film: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("sakila.film: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *filmSQL) Delete(ctx context.Context, key FilmKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, filmDelete,
		key.FilmId,
	)
	if err != nil {
		return 0, fmt.Errorf("sakila.film: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("sakila.film: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *filmSQL) Search(ctx context.Context, filter FilmFilter, order []FilmOrder, limit, offset int) ([]Film, error) {
	conditions := make([]string, 0, 41)
	arguments := make([]any, 0, 43)

	if filter.FilmId != nil {
		conditions = append(conditions, "`film_id` = ?")
		arguments = append(arguments, *filter.FilmId)
	}
	if filter.FilmIdFrom != nil {
		conditions = append(conditions, "`film_id` >= ?")
		arguments = append(arguments, *filter.FilmIdFrom)
	}
	if filter.FilmIdTo != nil {
		conditions = append(conditions, "`film_id` <= ?")
		arguments = append(arguments, *filter.FilmIdTo)
	}
	if filter.Title != nil {
		conditions = append(conditions, "`title` = ?")
		arguments = append(arguments, *filter.Title)
	}
	if filter.TitleLike != nil {
		conditions = append(conditions, "`title` LIKE ?")
		arguments = append(arguments, *filter.TitleLike)
	}
	if filter.Description != nil {
		conditions = append(conditions, "`description` = ?")
		arguments = append(arguments, *filter.Description)
	}
	if filter.DescriptionLike != nil {
		conditions = append(conditions, "`description` LIKE ?")
		arguments = append(arguments, *filter.DescriptionLike)
	}
	if filter.DescriptionIsNull != nil {
		if *filter.DescriptionIsNull {
			conditions = append(conditions, "`description` IS NULL")
		} else {
			conditions = append(conditions, "`description` IS NOT NULL")
		}
	}
	if filter.ReleaseYear != nil {
		conditions = append(conditions, "`release_year` = ?")
		arguments = append(arguments, *filter.ReleaseYear)
	}
	if filter.ReleaseYearFrom != nil {
		conditions = append(conditions, "`release_year` >= ?")
		arguments = append(arguments, *filter.ReleaseYearFrom)
	}
	if filter.ReleaseYearTo != nil {
		conditions = append(conditions, "`release_year` <= ?")
		arguments = append(arguments, *filter.ReleaseYearTo)
	}
	if filter.ReleaseYearIsNull != nil {
		if *filter.ReleaseYearIsNull {
			conditions = append(conditions, "`release_year` IS NULL")
		} else {
			conditions = append(conditions, "`release_year` IS NOT NULL")
		}
	}
	if filter.LanguageId != nil {
		conditions = append(conditions, "`language_id` = ?")
		arguments = append(arguments, *filter.LanguageId)
	}
	if filter.LanguageIdFrom != nil {
		conditions = append(conditions, "`language_id` >= ?")
		arguments = append(arguments, *filter.LanguageIdFrom)
	}
	if filter.LanguageIdTo != nil {
		conditions = append(conditions, "`language_id` <= ?")
		arguments = append(arguments, *filter.LanguageIdTo)
	}
	if filter.OriginalLanguageId != nil {
		conditions = append(conditions, "`original_language_id` = ?")
		arguments = append(arguments, *filter.OriginalLanguageId)
	}
	if filter.OriginalLanguageIdFrom != nil {
		conditions = append(conditions, "`original_language_id` >= ?")
		arguments = append(arguments, *filter.OriginalLanguageIdFrom)
	}
	if filter.OriginalLanguageIdTo != nil {
		conditions = append(conditions, "`original_language_id` <= ?")
		arguments = append(arguments, *filter.OriginalLanguageIdTo)
	}
	if filter.OriginalLanguageIdIsNull != nil {
		if *filter.OriginalLanguageIdIsNull {
			conditions = append(conditions, "`original_language_id` IS NULL")
		} else {
			conditions = append(conditions, "`original_language_id` IS NOT NULL")
		}
	}
	if filter.RentalDuration != nil {
		conditions = append(conditions, "`rental_duration` = ?")
		arguments = append(arguments, *filter.RentalDuration)
	}
	if filter.RentalDurationFrom != nil {
		conditions = append(conditions, "`rental_duration` >= ?")
		arguments = append(arguments, *filter.RentalDurationFrom)
	}
	if filter.RentalDurationTo != nil {
		conditions = append(conditions, "`rental_duration` <= ?")
		arguments = append(arguments, *filter.RentalDurationTo)
	}
	if filter.RentalRate != nil {
		conditions = append(conditions, "`rental_rate` = ?")
		arguments = append(arguments, *filter.RentalRate)
	}
	if filter.RentalRateFrom != nil {
		conditions = append(conditions, "`rental_rate` >= ?")
		arguments = append(arguments, *filter.RentalRateFrom)
	}
	if filter.RentalRateTo != nil {
		conditions = append(conditions, "`rental_rate` <= ?")
		arguments = append(arguments, *filter.RentalRateTo)
	}
	if filter.Length != nil {
		conditions = append(conditions, "`length` = ?")
		arguments = append(arguments, *filter.Length)
	}
	if filter.LengthFrom != nil {
		conditions = append(conditions, "`length` >= ?")
		arguments = append(arguments, *filter.LengthFrom)
	}
	if filter.LengthTo != nil {
		conditions = append(conditions, "`length` <= ?")
		arguments = append(arguments, *filter.LengthTo)
	}
	if filter.LengthIsNull != nil {
		if *filter.LengthIsNull {
			conditions = append(conditions, "`length` IS NULL")
		} else {
			conditions = append(conditions, "`length` IS NOT NULL")
		}
	}
	if filter.ReplacementCost != nil {
		conditions = append(conditions, "`replacement_cost` = ?")
		arguments = append(arguments, *filter.ReplacementCost)
	}
	if filter.ReplacementCostFrom != nil {
		conditions = append(conditions, "`replacement_cost` >= ?")
		arguments = append(arguments, *filter.ReplacementCostFrom)
	}
	if filter.ReplacementCostTo != nil {
		conditions = append(conditions, "`replacement_cost` <= ?")
		arguments = append(arguments, *filter.ReplacementCostTo)
	}
	if filter.Rating != nil {
		conditions = append(conditions, "`rating` = ?")
		arguments = append(arguments, *filter.Rating)
	}
	if filter.RatingLike != nil {
		conditions = append(conditions, "`rating` LIKE ?")
		arguments = append(arguments, *filter.RatingLike)
	}
	if filter.RatingIsNull != nil {
		if *filter.RatingIsNull {
			conditions = append(conditions, "`rating` IS NULL")
		} else {
			conditions = append(conditions, "`rating` IS NOT NULL")
		}
	}
	if filter.SpecialFeatures != nil {
		conditions = append(conditions, "`special_features` = ?")
		arguments = append(arguments, *filter.SpecialFeatures)
	}
	if filter.SpecialFeaturesLike != nil {
		conditions = append(conditions, "`special_features` LIKE ?")
		arguments = append(arguments, *filter.SpecialFeaturesLike)
	}
	if filter.SpecialFeaturesIsNull != nil {
		if *filter.SpecialFeaturesIsNull {
			conditions = append(conditions, "`special_features` IS NULL")
		} else {
			conditions = append(conditions, "`special_features` IS NOT NULL")
		}
	}
	if filter.LastUpdate != nil {
		conditions = append(conditions, "`last_update` = ?")
		arguments = append(arguments, *filter.LastUpdate)
	}
	if filter.LastUpdateFrom != nil {
		conditions = append(conditions, "`last_update` >= ?")
		arguments = append(arguments, *filter.LastUpdateFrom)
	}
	if filter.LastUpdateTo != nil {
		conditions = append(conditions, "`last_update` <= ?")
		arguments = append(arguments, *filter.LastUpdateTo)
	}

	var query strings.Builder
	query.WriteString(filmSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := filmOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("sakila.film: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("sakila.film: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("sakila.film: search: %w", err)
	}
	defer rows.Close()

	var found []Film
	for rows.Next() {
		var row Film
		if err := rows.Scan(
			&row.FilmId,
			&row.Title,
			&row.Description,
			&row.ReleaseYear,
			&row.LanguageId,
			&row.OriginalLanguageId,
			&row.RentalDuration,
			&row.RentalRate,
			&row.Length,
			&row.ReplacementCost,
			&row.Rating,
			&row.SpecialFeatures,
			&row.LastUpdate,
		); err != nil {
			return nil, fmt.Errorf("sakila.film: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("sakila.film: search: %w", err)
	}
	return found, nil
}

// filmOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func filmOrderColumn(column FilmColumn) (string, error) {
	switch column {
	case FilmColumnFilmId:
		return "`film_id`", nil
	case FilmColumnTitle:
		return "`title`", nil
	case FilmColumnDescription:
		return "`description`", nil
	case FilmColumnReleaseYear:
		return "`release_year`", nil
	case FilmColumnLanguageId:
		return "`language_id`", nil
	case FilmColumnOriginalLanguageId:
		return "`original_language_id`", nil
	case FilmColumnRentalDuration:
		return "`rental_duration`", nil
	case FilmColumnRentalRate:
		return "`rental_rate`", nil
	case FilmColumnLength:
		return "`length`", nil
	case FilmColumnReplacementCost:
		return "`replacement_cost`", nil
	case FilmColumnRating:
		return "`rating`", nil
	case FilmColumnSpecialFeatures:
		return "`special_features`", nil
	case FilmColumnLastUpdate:
		return "`last_update`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
