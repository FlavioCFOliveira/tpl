// Code generated by tpl from sakila.city. DO NOT EDIT.

package sakila

import (
	"time"
)

// City is one row of sakila.city.
type City struct {
	// CityId is city_id, declared smallint(5) unsigned NOT NULL AUTO_INCREMENT.
	// It is part of the primary key.
	CityId uint16
	// City is city, declared varchar(50) NOT NULL.
	City string
	// CountryId is country_id, declared smallint(5) unsigned NOT NULL.
	CountryId uint16
	// LastUpdate is last_update, declared timestamp NOT NULL.
	LastUpdate time.Time
}

// CityKey is the primary key of sakila.city.
type CityKey struct {
	// CityId is city_id.
	CityId uint16
}

// Key returns the primary key of the row.
func (row *City) Key() CityKey {
	var key CityKey
	key.CityId = row.CityId
	return key
}

// CityColumn names a column of sakila.city.
//
// A search accepts an ordering term only over the constants below, so an
// ordering can carry no SQL of its own.
type CityColumn string

// The columns of sakila.city, in catalogue order.
const (
	// CityColumnCityId names city_id.
	CityColumnCityId CityColumn = "city_id"
	// CityColumnCity names city.
	CityColumnCity CityColumn = "city"
	// CityColumnCountryId names country_id.
	CityColumnCountryId CityColumn = "country_id"
	// CityColumnLastUpdate names last_update.
	CityColumnLastUpdate CityColumn = "last_update"
)

// CityOrder is one ordering term of a search over sakila.city.
type CityOrder struct {
	// Column is the column to order by.
	Column CityColumn
	// Direction is the direction to order in.
	Direction Direction
}

// CityFilter narrows a search over sakila.city.
//
// Every field is optional: a field left nil adds no condition, and the
// conditions a search does build are joined with AND.
type CityFilter struct {
	// CityId, where set, requires city_id to equal it.
	CityId *uint16
	// CityIdFrom, where set, requires city_id to be at least it.
	CityIdFrom *uint16
	// CityIdTo, where set, requires city_id to be at most it.
	CityIdTo *uint16
	// City, where set, requires city to equal it.
	City *string
	// CityLike, where set, requires city to match it, as LIKE matches.
	CityLike *string
	// CountryId, where set, requires country_id to equal it.
	CountryId *uint16
	// CountryIdFrom, where set, requires country_id to be at least it.
	CountryIdFrom *uint16
	// CountryIdTo, where set, requires country_id to be at most it.
	CountryIdTo *uint16
	// LastUpdate, where set, requires last_update to equal it.
	LastUpdate *time.Time
	// LastUpdateFrom, where set, requires last_update to be at least it.
	LastUpdateFrom *time.Time
	// LastUpdateTo, where set, requires last_update to be at most it.
	LastUpdateTo *time.Time
}
