// Code generated by tpl from sakila.payment. DO NOT EDIT.

package sakila

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// paymentSQL is the database/sql implementation of PaymentRepository.
type paymentSQL struct {
	db *sql.DB
}

// NewPaymentRepository builds the data layer of sakila.payment over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewPaymentRepository(db *sql.DB) PaymentRepository {
	return &paymentSQL{db: db}
}

// paymentSelect lists every column of sakila.payment, in catalogue order.
const paymentSelect = "SELECT `payment_id`, `customer_id`, `staff_id`, `rental_id`, `amount`, `payment_date`, `last_update` FROM `payment`"

// paymentRead reads the one row a primary key names.
const paymentRead = "SELECT `payment_id`, `customer_id`, `staff_id`, `rental_id`, `amount`, `payment_date`, `last_update` FROM `payment` WHERE `payment_id` = ?"

// paymentInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const paymentInsert = "INSERT INTO `payment` (`customer_id`, `staff_id`, `rental_id`, `amount`, `payment_date`, `last_update`) VALUES (?, ?, ?, ?, ?, ?)"

// paymentUpdate writes every column that is neither generated nor a key.
const paymentUpdate = "UPDATE `payment` SET `customer_id` = ?, `staff_id` = ?, `rental_id` = ?, `amount` = ?, `payment_date` = ?, `last_update` = ? WHERE `payment_id` = ?"

// paymentDelete removes the one row a primary key names.
const paymentDelete = "DELETE FROM `payment` WHERE `payment_id` = ?"

// Create inserts row.
func (r *paymentSQL) Create(ctx context.Context, row *Payment) error {
	result, err := r.db.ExecContext(ctx, paymentInsert,
		row.CustomerId,
		row.StaffId,
		row.RentalId,
		row.Amount,
		row.PaymentDate,
		row.LastUpdate,
	)
	if err != nil {
		return fmt.Errorf("sakila.payment: create: %w", err)
	}
	assigned, err := result.LastInsertId()
	if err != nil {
		return fmt.Errorf("sakila.payment: create: reading the key the server assigned: %w", err)
	}
	row.PaymentId = uint16(assigned)
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *paymentSQL) Read(ctx context.Context, key PaymentKey) (*Payment, error) {
	var row Payment
	err := r.db.QueryRowContext(ctx, paymentRead,
		key.PaymentId,
	).Scan(
		&row.PaymentId,
		&row.CustomerId,
		&row.StaffId,
		&row.RentalId,
		&row.Amount,
		&row.PaymentDate,
		&row.LastUpdate,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("sakila.payment: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("sakila.payment: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *paymentSQL) Update(ctx context.Context, row *Payment) (int64, error) {
	result, err := r.db.ExecContext(ctx, paymentUpdate,
		row.CustomerId,
		row.StaffId,
		row.RentalId,
		row.Amount,
		row.PaymentDate,
		row.LastUpdate,
		row.PaymentId,
	)
	if err != nil {
		return 0, fmt.Errorf("sakila.payment: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("sakila.payment: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *paymentSQL) Delete(ctx context.Context, key PaymentKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, paymentDelete,
		key.PaymentId,
	)
	if err != nil {
		return 0, fmt.Errorf("sakila.payment: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("sakila.payment: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *paymentSQL) Search(ctx context.Context, filter PaymentFilter, order []PaymentOrder, limit, offset int) ([]Payment, error) {
	conditions := make([]string, 0, 23)
	arguments := make([]any, 0, 25)

	if filter.PaymentId != nil {
		conditions = append(conditions, "`payment_id` = ?")
		arguments = append(arguments, *filter.PaymentId)
	}
	if filter.PaymentIdFrom != nil {
		conditions = append(conditions, "`payment_id` >= ?")
		arguments = append(arguments, *filter.PaymentIdFrom)
	}
	if filter.PaymentIdTo != nil {
		conditions = append(conditions, "`payment_id` <= ?")
		arguments = append(arguments, *filter.PaymentIdTo)
	}
	if filter.CustomerId != nil {
		conditions = append(conditions, "`customer_id` = ?")
		arguments = append(arguments, *filter.CustomerId)
	}
	if filter.CustomerIdFrom != nil {
		conditions = append(conditions, "`customer_id` >= ?")
		arguments = append(arguments, *filter.CustomerIdFrom)
	}
	if filter.CustomerIdTo != nil {
		conditions = append(conditions, "`customer_id` <= ?")
		arguments = append(arguments, *filter.CustomerIdTo)
	}
	if filter.StaffId != nil {
		conditions = append(conditions, "`staff_id` = ?")
		arguments = append(arguments, *filter.StaffId)
	}
	if filter.StaffIdFrom != nil {
		conditions = append(conditions, "`staff_id` >= ?")
		arguments = append(arguments, *filter.StaffIdFrom)
	}
	if filter.StaffIdTo != nil {
		conditions = append(conditions, "`staff_id` <= ?")
		arguments = append(arguments, *filter.StaffIdTo)
	}
	if filter.RentalId != nil {
		conditions = append(conditions, "`rental_id` = ?")
		arguments = append(arguments, *filter.RentalId)
	}
	if filter.RentalIdFrom != nil {
		conditions = append(conditions, "`rental_id` >= ?")
		arguments = append(arguments, *filter.RentalIdFrom)
	}
	if filter.RentalIdTo != nil {
		conditions = append(conditions, "`rental_id` <= ?")
		arguments = append(arguments, *filter.RentalIdTo)
	}
	if filter.RentalIdIsNull != nil {
		if *filter.RentalIdIsNull {
			conditions = append(conditions, "`rental_id` IS NULL")
		} else {
			conditions = append(conditions, "`rental_id` IS NOT NULL")
		}
	}
	if filter.Amount != nil {
		conditions = append(conditions, "`amount` = ?")
		arguments = append(arguments, *filter.Amount)
	}
	if filter.AmountFrom != nil {
		conditions = append(conditions, "`amount` >= ?")
		arguments = append(arguments, *filter.AmountFrom)
	}
	if filter.AmountTo != nil {
		conditions = append(conditions, "`amount` <= ?")
		arguments = append(arguments, *filter.AmountTo)
	}
	if filter.PaymentDate != nil {
		conditions = append(conditions, "`payment_date` = ?")
		arguments = append(arguments, *filter.PaymentDate)
	}
	if filter.PaymentDateFrom != nil {
		conditions = append(conditions, "`payment_date` >= ?")
		arguments = append(arguments, *filter.PaymentDateFrom)
	}
	if filter.PaymentDateTo != nil {
		conditions = append(conditions, "`payment_date` <= ?")
		arguments = append(arguments, *filter.PaymentDateTo)
	}
	if filter.LastUpdate != nil {
		conditions = append(conditions, "`last_update` = ?")
		arguments = append(arguments, *filter.LastUpdate)
	}
	if filter.LastUpdateFrom != nil {
		conditions = append(conditions, "`last_update` >= ?")
		arguments = append(arguments, *filter.LastUpdateFrom)
	}
	if filter.LastUpdateTo != nil {
		conditions = append(conditions, "`last_update` <= ?")
		arguments = append(arguments, *filter.LastUpdateTo)
	}
	if filter.LastUpdateIsNull != nil {
		if *filter.LastUpdateIsNull {
			conditions = append(conditions, "`last_update` IS NULL")
		} else {
			conditions = append(conditions, "`last_update` IS NOT NULL")
		}
	}

	var query strings.Builder
	query.WriteString(paymentSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := paymentOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("sakila.payment: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("sakila.payment: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("sakila.payment: search: %w", err)
	}
	defer rows.Close()

	var found []Payment
	for rows.Next() {
		var row Payment
		if err := rows.Scan(
			&row.PaymentId,
			&row.CustomerId,
			&row.StaffId,
			&row.RentalId,
			&row.Amount,
			&row.PaymentDate,
			&row.LastUpdate,
		); err != nil {
			return nil, fmt.Errorf("sakila.payment: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("sakila.payment: search: %w", err)
	}
	return found, nil
}

// paymentOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func paymentOrderColumn(column PaymentColumn) (string, error) {
	switch column {
	case PaymentColumnPaymentId:
		return "`payment_id`", nil
	case PaymentColumnCustomerId:
		return "`customer_id`", nil
	case PaymentColumnStaffId:
		return "`staff_id`", nil
	case PaymentColumnRentalId:
		return "`rental_id`", nil
	case PaymentColumnAmount:
		return "`amount`", nil
	case PaymentColumnPaymentDate:
		return "`payment_date`", nil
	case PaymentColumnLastUpdate:
		return "`last_update`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
