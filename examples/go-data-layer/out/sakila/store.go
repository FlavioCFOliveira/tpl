// Code generated by tpl from sakila.store. DO NOT EDIT.

package sakila

import (
	"time"
)

// Store is one row of sakila.store.
type Store struct {
	// StoreId is store_id, declared tinyint(3) unsigned NOT NULL AUTO_INCREMENT.
	// It is part of the primary key.
	StoreId uint8
	// ManagerStaffId is manager_staff_id, declared tinyint(3) unsigned NOT NULL.
	// It is part of a unique index.
	ManagerStaffId uint8
	// AddressId is address_id, declared smallint(5) unsigned NOT NULL.
	AddressId uint16
	// LastUpdate is last_update, declared timestamp NOT NULL.
	LastUpdate time.Time
}

// StoreKey is the primary key of sakila.store.
type StoreKey struct {
	// StoreId is store_id.
	StoreId uint8
}

// Key returns the primary key of the row.
func (row *Store) Key() StoreKey {
	var key StoreKey
	key.StoreId = row.StoreId
	return key
}

// StoreColumn names a column of sakila.store.
//
// A search accepts an ordering term only over the constants below, so an
// ordering can carry no SQL of its own.
type StoreColumn string

// The columns of sakila.store, in catalogue order.
const (
	// StoreColumnStoreId names store_id.
	StoreColumnStoreId StoreColumn = "store_id"
	// StoreColumnManagerStaffId names manager_staff_id.
	StoreColumnManagerStaffId StoreColumn = "manager_staff_id"
	// StoreColumnAddressId names address_id.
	StoreColumnAddressId StoreColumn = "address_id"
	// StoreColumnLastUpdate names last_update.
	StoreColumnLastUpdate StoreColumn = "last_update"
)

// StoreOrder is one ordering term of a search over sakila.store.
type StoreOrder struct {
	// Column is the column to order by.
	Column StoreColumn
	// Direction is the direction to order in.
	Direction Direction
}

// StoreFilter narrows a search over sakila.store.
//
// Every field is optional: a field left nil adds no condition, and the
// conditions a search does build are joined with AND.
type StoreFilter struct {
	// StoreId, where set, requires store_id to equal it.
	StoreId *uint8
	// StoreIdFrom, where set, requires store_id to be at least it.
	StoreIdFrom *uint8
	// StoreIdTo, where set, requires store_id to be at most it.
	StoreIdTo *uint8
	// ManagerStaffId, where set, requires manager_staff_id to equal it.
	ManagerStaffId *uint8
	// ManagerStaffIdFrom, where set, requires manager_staff_id to be at least it.
	ManagerStaffIdFrom *uint8
	// ManagerStaffIdTo, where set, requires manager_staff_id to be at most it.
	ManagerStaffIdTo *uint8
	// AddressId, where set, requires address_id to equal it.
	AddressId *uint16
	// AddressIdFrom, where set, requires address_id to be at least it.
	AddressIdFrom *uint16
	// AddressIdTo, where set, requires address_id to be at most it.
	AddressIdTo *uint16
	// LastUpdate, where set, requires last_update to equal it.
	LastUpdate *time.Time
	// LastUpdateFrom, where set, requires last_update to be at least it.
	LastUpdateFrom *time.Time
	// LastUpdateTo, where set, requires last_update to be at most it.
	LastUpdateTo *time.Time
}
