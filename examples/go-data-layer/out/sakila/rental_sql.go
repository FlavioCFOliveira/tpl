// Code generated by tpl from sakila.rental. DO NOT EDIT.

package sakila

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// rentalSQL is the database/sql implementation of RentalRepository.
type rentalSQL struct {
	db *sql.DB
}

// NewRentalRepository builds the data layer of sakila.rental over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewRentalRepository(db *sql.DB) RentalRepository {
	return &rentalSQL{db: db}
}

// rentalSelect lists every column of sakila.rental, in catalogue order.
const rentalSelect = "SELECT `rental_id`, `rental_date`, `inventory_id`, `customer_id`, `return_date`, `staff_id`, `last_update` FROM `rental`"

// rentalRead reads the one row a primary key names.
const rentalRead = "SELECT `rental_id`, `rental_date`, `inventory_id`, `customer_id`, `return_date`, `staff_id`, `last_update` FROM `rental` WHERE `rental_id` = ?"

// rentalInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const rentalInsert = "INSERT INTO `rental` (`rental_date`, `inventory_id`, `customer_id`, `return_date`, `staff_id`, `last_update`) VALUES (?, ?, ?, ?, ?, ?)"

// rentalUpdate writes every column that is neither generated nor a key.
const rentalUpdate = "UPDATE `rental` SET `rental_date` = ?, `inventory_id` = ?, `customer_id` = ?, `return_date` = ?, `staff_id` = ?, `last_update` = ? WHERE `rental_id` = ?"

// rentalDelete removes the one row a primary key names.
const rentalDelete = "DELETE FROM `rental` WHERE `rental_id` = ?"

// Create inserts row.
func (r *rentalSQL) Create(ctx context.Context, row *Rental) error {
	result, err := r.db.ExecContext(ctx, rentalInsert,
		row.RentalDate,
		row.InventoryId,
		row.CustomerId,
		row.ReturnDate,
		row.StaffId,
		row.LastUpdate,
	)
	if err != nil {
		return fmt.Errorf("sakila.rental: create: %w", err)
	}
	assigned, err := result.LastInsertId()
	if err != nil {
		return fmt.Errorf("sakila.rental: create: reading the key the server assigned: %w", err)
	}
	row.RentalId = int32(assigned)
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *rentalSQL) Read(ctx context.Context, key RentalKey) (*Rental, error) {
	var row Rental
	err := r.db.QueryRowContext(ctx, rentalRead,
		key.RentalId,
	).Scan(
		&row.RentalId,
		&row.RentalDate,
		&row.InventoryId,
		&row.CustomerId,
		&row.ReturnDate,
		&row.StaffId,
		&row.LastUpdate,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("sakila.rental: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("sakila.rental: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *rentalSQL) Update(ctx context.Context, row *Rental) (int64, error) {
	result, err := r.db.ExecContext(ctx, rentalUpdate,
		row.RentalDate,
		row.InventoryId,
		row.CustomerId,
		row.ReturnDate,
		row.StaffId,
		row.LastUpdate,
		row.RentalId,
	)
	if err != nil {
		return 0, fmt.Errorf("sakila.rental: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("sakila.rental: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *rentalSQL) Delete(ctx context.Context, key RentalKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, rentalDelete,
		key.RentalId,
	)
	if err != nil {
		return 0, fmt.Errorf("sakila.rental: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("sakila.rental: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *rentalSQL) Search(ctx context.Context, filter RentalFilter, order []RentalOrder, limit, offset int) ([]Rental, error) {
	conditions := make([]string, 0, 22)
	arguments := make([]any, 0, 24)

	if filter.RentalId != nil {
		conditions = append(conditions, "`rental_id` = ?")
		arguments = append(arguments, *filter.RentalId)
	}
	if filter.RentalIdFrom != nil {
		conditions = append(conditions, "`rental_id` >= ?")
		arguments = append(arguments, *filter.RentalIdFrom)
	}
	if filter.RentalIdTo != nil {
		conditions = append(conditions, "`rental_id` <= ?")
		arguments = append(arguments, *filter.RentalIdTo)
	}
	if filter.RentalDate != nil {
		conditions = append(conditions, "`rental_date` = ?")
		arguments = append(arguments, *filter.RentalDate)
	}
	if filter.RentalDateFrom != nil {
		conditions = append(conditions, "`rental_date` >= ?")
		arguments = append(arguments, *filter.RentalDateFrom)
	}
	if filter.RentalDateTo != nil {
		conditions = append(conditions, "`rental_date` <= ?")
		arguments = append(arguments, *filter.RentalDateTo)
	}
	if filter.InventoryId != nil {
		conditions = append(conditions, "`inventory_id` = ?")
		arguments = append(arguments, *filter.InventoryId)
	}
	if filter.InventoryIdFrom != nil {
		conditions = append(conditions, "`inventory_id` >= ?")
		arguments = append(arguments, *filter.InventoryIdFrom)
	}
	if filter.InventoryIdTo != nil {
		conditions = append(conditions, "`inventory_id` <= ?")
		arguments = append(arguments, *filter.InventoryIdTo)
	}
	if filter.CustomerId != nil {
		conditions = append(conditions, "`customer_id` = ?")
		arguments = append(arguments, *filter.CustomerId)
	}
	if filter.CustomerIdFrom != nil {
		conditions = append(conditions, "`customer_id` >= ?")
		arguments = append(arguments, *filter.CustomerIdFrom)
	}
	if filter.CustomerIdTo != nil {
		conditions = append(conditions, "`customer_id` <= ?")
		arguments = append(arguments, *filter.CustomerIdTo)
	}
	if filter.ReturnDate != nil {
		conditions = append(conditions, "`return_date` = ?")
		arguments = append(arguments, *filter.ReturnDate)
	}
	if filter.ReturnDateFrom != nil {
		conditions = append(conditions, "`return_date` >= ?")
		arguments = append(arguments, *filter.ReturnDateFrom)
	}
	if filter.ReturnDateTo != nil {
		conditions = append(conditions, "`return_date` <= ?")
		arguments = append(arguments, *filter.ReturnDateTo)
	}
	if filter.ReturnDateIsNull != nil {
		if *filter.ReturnDateIsNull {
			conditions = append(conditions, "`return_date` IS NULL")
		} else {
			conditions = append(conditions, "`return_date` IS NOT NULL")
		}
	}
	if filter.StaffId != nil {
		conditions = append(conditions, "`staff_id` = ?")
		arguments = append(arguments, *filter.StaffId)
	}
	if filter.StaffIdFrom != nil {
		conditions = append(conditions, "`staff_id` >= ?")
		arguments = append(arguments, *filter.StaffIdFrom)
	}
	if filter.StaffIdTo != nil {
		conditions = append(conditions, "`staff_id` <= ?")
		arguments = append(arguments, *filter.StaffIdTo)
	}
	if filter.LastUpdate != nil {
		conditions = append(conditions, "`last_update` = ?")
		arguments = append(arguments, *filter.LastUpdate)
	}
	if filter.LastUpdateFrom != nil {
		conditions = append(conditions, "`last_update` >= ?")
		arguments = append(arguments, *filter.LastUpdateFrom)
	}
	if filter.LastUpdateTo != nil {
		conditions = append(conditions, "`last_update` <= ?")
		arguments = append(arguments, *filter.LastUpdateTo)
	}

	var query strings.Builder
	query.WriteString(rentalSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := rentalOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("sakila.rental: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("sakila.rental: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("sakila.rental: search: %w", err)
	}
	defer rows.Close()

	var found []Rental
	for rows.Next() {
		var row Rental
		if err := rows.Scan(
			&row.RentalId,
			&row.RentalDate,
			&row.InventoryId,
			&row.CustomerId,
			&row.ReturnDate,
			&row.StaffId,
			&row.LastUpdate,
		); err != nil {
			return nil, fmt.Errorf("sakila.rental: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("sakila.rental: search: %w", err)
	}
	return found, nil
}

// rentalOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func rentalOrderColumn(column RentalColumn) (string, error) {
	switch column {
	case RentalColumnRentalId:
		return "`rental_id`", nil
	case RentalColumnRentalDate:
		return "`rental_date`", nil
	case RentalColumnInventoryId:
		return "`inventory_id`", nil
	case RentalColumnCustomerId:
		return "`customer_id`", nil
	case RentalColumnReturnDate:
		return "`return_date`", nil
	case RentalColumnStaffId:
		return "`staff_id`", nil
	case RentalColumnLastUpdate:
		return "`last_update`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
