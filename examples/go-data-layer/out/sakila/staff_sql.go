// Code generated by tpl from sakila.staff. DO NOT EDIT.

package sakila

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// staffSQL is the database/sql implementation of StaffRepository.
type staffSQL struct {
	db *sql.DB
}

// NewStaffRepository builds the data layer of sakila.staff over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewStaffRepository(db *sql.DB) StaffRepository {
	return &staffSQL{db: db}
}

// staffSelect lists every column of sakila.staff, in catalogue order.
const staffSelect = "SELECT `staff_id`, `first_name`, `last_name`, `address_id`, `picture`, `email`, `store_id`, `active`, `username`, `password`, `last_update` FROM `staff`"

// staffRead reads the one row a primary key names.
const staffRead = "SELECT `staff_id`, `first_name`, `last_name`, `address_id`, `picture`, `email`, `store_id`, `active`, `username`, `password`, `last_update` FROM `staff` WHERE `staff_id` = ?"

// staffInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const staffInsert = "INSERT INTO `staff` (`first_name`, `last_name`, `address_id`, `picture`, `email`, `store_id`, `active`, `username`, `password`, `last_update`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"

// staffUpdate writes every column that is neither generated nor a key.
const staffUpdate = "UPDATE `staff` SET `first_name` = ?, `last_name` = ?, `address_id` = ?, `picture` = ?, `email` = ?, `store_id` = ?, `active` = ?, `username` = ?, `password` = ?, `last_update` = ? WHERE `staff_id` = ?"

// staffDelete removes the one row a primary key names.
const staffDelete = "DELETE FROM `staff` WHERE `staff_id` = ?"

// Create inserts row.
func (r *staffSQL) Create(ctx context.Context, row *Staff) error {
	result, err := r.db.ExecContext(ctx, staffInsert,
		row.FirstName,
		row.LastName,
		row.AddressId,
		row.Picture,
		row.Email,
		row.StoreId,
		row.Active,
		row.Username,
		row.Password,
		row.LastUpdate,
	)
	if err != nil {
		return fmt.Errorf("sakila.staff: create: %w", err)
	}
	assigned, err := result.LastInsertId()
	if err != nil {
		return fmt.Errorf("sakila.staff: create: reading the key the server assigned: %w", err)
	}
	row.StaffId = uint8(assigned)
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *staffSQL) Read(ctx context.Context, key StaffKey) (*Staff, error) {
	var row Staff
	err := r.db.QueryRowContext(ctx, staffRead,
		key.StaffId,
	).Scan(
		&row.StaffId,
		&row.FirstName,
		&row.LastName,
		&row.AddressId,
		&row.Picture,
		&row.Email,
		&row.StoreId,
		&row.Active,
		&row.Username,
		&row.Password,
		&row.LastUpdate,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("sakila.staff: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("sakila.staff: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *staffSQL) Update(ctx context.Context, row *Staff) (int64, error) {
	result, err := r.db.ExecContext(ctx, staffUpdate,
		row.FirstName,
		row.LastName,
		row.AddressId,
		row.Picture,
		row.Email,
		row.StoreId,
		row.Active,
		row.Username,
		row.Password,
		row.LastUpdate,
		row.StaffId,
	)
	if err != nil {
		return 0, fmt.Errorf("sakila.staff: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("sakila.staff: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *staffSQL) Delete(ctx context.Context, key StaffKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, staffDelete,
		key.StaffId,
	)
	if err != nil {
		return 0, fmt.Errorf("sakila.staff: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("sakila.staff: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *staffSQL) Search(ctx context.Context, filter StaffFilter, order []StaffOrder, limit, offset int) ([]Staff, error) {
	conditions := make([]string, 0, 29)
	arguments := make([]any, 0, 31)

	if filter.StaffId != nil {
		conditions = append(conditions, "`staff_id` = ?")
		arguments = append(arguments, *filter.StaffId)
	}
	if filter.StaffIdFrom != nil {
		conditions = append(conditions, "`staff_id` >= ?")
		arguments = append(arguments, *filter.StaffIdFrom)
	}
	if filter.StaffIdTo != nil {
		conditions = append(conditions, "`staff_id` <= ?")
		arguments = append(arguments, *filter.StaffIdTo)
	}
	if filter.FirstName != nil {
		conditions = append(conditions, "`first_name` = ?")
		arguments = append(arguments, *filter.FirstName)
	}
	if filter.FirstNameLike != nil {
		conditions = append(conditions, "`first_name` LIKE ?")
		arguments = append(arguments, *filter.FirstNameLike)
	}
	if filter.LastName != nil {
		conditions = append(conditions, "`last_name` = ?")
		arguments = append(arguments, *filter.LastName)
	}
	if filter.LastNameLike != nil {
		conditions = append(conditions, "`last_name` LIKE ?")
		arguments = append(arguments, *filter.LastNameLike)
	}
	if filter.AddressId != nil {
		conditions = append(conditions, "`address_id` = ?")
		arguments = append(arguments, *filter.AddressId)
	}
	if filter.AddressIdFrom != nil {
		conditions = append(conditions, "`address_id` >= ?")
		arguments = append(arguments, *filter.AddressIdFrom)
	}
	if filter.AddressIdTo != nil {
		conditions = append(conditions, "`address_id` <= ?")
		arguments = append(arguments, *filter.AddressIdTo)
	}
	if filter.Picture != nil {
		conditions = append(conditions, "`picture` = ?")
		arguments = append(arguments, filter.Picture)
	}
	if filter.PictureIsNull != nil {
		if *filter.PictureIsNull {
			conditions = append(conditions, "`picture` IS NULL")
		} else {
			conditions = append(conditions, "`picture` IS NOT NULL")
		}
	}
	if filter.Email != nil {
		conditions = append(conditions, "`email` = ?")
		arguments = append(arguments, *filter.Email)
	}
	if filter.EmailLike != nil {
		conditions = append(conditions, "`email` LIKE ?")
		arguments = append(arguments, *filter.EmailLike)
	}
	if filter.EmailIsNull != nil {
		if *filter.EmailIsNull {
			conditions = append(conditions, "`email` IS NULL")
		} else {
			conditions = append(conditions, "`email` IS NOT NULL")
		}
	}
	if filter.StoreId != nil {
		conditions = append(conditions, "`store_id` = ?")
		arguments = append(arguments, *filter.StoreId)
	}
	if filter.StoreIdFrom != nil {
		conditions = append(conditions, "`store_id` >= ?")
		arguments = append(arguments, *filter.StoreIdFrom)
	}
	if filter.StoreIdTo != nil {
		conditions = append(conditions, "`store_id` <= ?")
		arguments = append(arguments, *filter.StoreIdTo)
	}
	if filter.Active != nil {
		conditions = append(conditions, "`active` = ?")
		arguments = append(arguments, *filter.Active)
	}
	if filter.ActiveFrom != nil {
		conditions = append(conditions, "`active` >= ?")
		arguments = append(arguments, *filter.ActiveFrom)
	}
	if filter.ActiveTo != nil {
		conditions = append(conditions, "`active` <= ?")
		arguments = append(arguments, *filter.ActiveTo)
	}
	if filter.Username != nil {
		conditions = append(conditions, "`username` = ?")
		arguments = append(arguments, *filter.Username)
	}
	if filter.UsernameLike != nil {
		conditions = append(conditions, "`username` LIKE ?")
		arguments = append(arguments, *filter.UsernameLike)
	}
	if filter.Password != nil {
		conditions = append(conditions, "`password` = ?")
		arguments = append(arguments, *filter.Password)
	}
	if filter.PasswordLike != nil {
		conditions = append(conditions, "`password` LIKE ?")
		arguments = append(arguments, *filter.PasswordLike)
	}
	if filter.PasswordIsNull != nil {
		if *filter.PasswordIsNull {
			conditions = append(conditions, "`password` IS NULL")
		} else {
			conditions = append(conditions, "`password` IS NOT NULL")
		}
	}
	if filter.LastUpdate != nil {
		conditions = append(conditions, "`last_update` = ?")
		arguments = append(arguments, *filter.LastUpdate)
	}
	if filter.LastUpdateFrom != nil {
		conditions = append(conditions, "`last_update` >= ?")
		arguments = append(arguments, *filter.LastUpdateFrom)
	}
	if filter.LastUpdateTo != nil {
		conditions = append(conditions, "`last_update` <= ?")
		arguments = append(arguments, *filter.LastUpdateTo)
	}

	var query strings.Builder
	query.WriteString(staffSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := staffOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("sakila.staff: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("sakila.staff: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("sakila.staff: search: %w", err)
	}
	defer rows.Close()

	var found []Staff
	for rows.Next() {
		var row Staff
		if err := rows.Scan(
			&row.StaffId,
			&row.FirstName,
			&row.LastName,
			&row.AddressId,
			&row.Picture,
			&row.Email,
			&row.StoreId,
			&row.Active,
			&row.Username,
			&row.Password,
			&row.LastUpdate,
		); err != nil {
			return nil, fmt.Errorf("sakila.staff: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("sakila.staff: search: %w", err)
	}
	return found, nil
}

// staffOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func staffOrderColumn(column StaffColumn) (string, error) {
	switch column {
	case StaffColumnStaffId:
		return "`staff_id`", nil
	case StaffColumnFirstName:
		return "`first_name`", nil
	case StaffColumnLastName:
		return "`last_name`", nil
	case StaffColumnAddressId:
		return "`address_id`", nil
	case StaffColumnPicture:
		return "`picture`", nil
	case StaffColumnEmail:
		return "`email`", nil
	case StaffColumnStoreId:
		return "`store_id`", nil
	case StaffColumnActive:
		return "`active`", nil
	case StaffColumnUsername:
		return "`username`", nil
	case StaffColumnPassword:
		return "`password`", nil
	case StaffColumnLastUpdate:
		return "`last_update`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
