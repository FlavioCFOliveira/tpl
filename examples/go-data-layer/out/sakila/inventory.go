// Code generated by tpl from sakila.inventory. DO NOT EDIT.

package sakila

import (
	"time"
)

// Inventory is one row of sakila.inventory.
type Inventory struct {
	// InventoryId is inventory_id, declared mediumint(8) unsigned NOT NULL AUTO_INCREMENT.
	// It is part of the primary key.
	InventoryId uint32
	// FilmId is film_id, declared smallint(5) unsigned NOT NULL.
	FilmId uint16
	// StoreId is store_id, declared tinyint(3) unsigned NOT NULL.
	StoreId uint8
	// LastUpdate is last_update, declared timestamp NOT NULL.
	LastUpdate time.Time
}

// InventoryKey is the primary key of sakila.inventory.
type InventoryKey struct {
	// InventoryId is inventory_id.
	InventoryId uint32
}

// Key returns the primary key of the row.
func (row *Inventory) Key() InventoryKey {
	var key InventoryKey
	key.InventoryId = row.InventoryId
	return key
}

// InventoryColumn names a column of sakila.inventory.
//
// A search accepts an ordering term only over the constants below, so an
// ordering can carry no SQL of its own.
type InventoryColumn string

// The columns of sakila.inventory, in catalogue order.
const (
	// InventoryColumnInventoryId names inventory_id.
	InventoryColumnInventoryId InventoryColumn = "inventory_id"
	// InventoryColumnFilmId names film_id.
	InventoryColumnFilmId InventoryColumn = "film_id"
	// InventoryColumnStoreId names store_id.
	InventoryColumnStoreId InventoryColumn = "store_id"
	// InventoryColumnLastUpdate names last_update.
	InventoryColumnLastUpdate InventoryColumn = "last_update"
)

// InventoryOrder is one ordering term of a search over sakila.inventory.
type InventoryOrder struct {
	// Column is the column to order by.
	Column InventoryColumn
	// Direction is the direction to order in.
	Direction Direction
}

// InventoryFilter narrows a search over sakila.inventory.
//
// Every field is optional: a field left nil adds no condition, and the
// conditions a search does build are joined with AND.
type InventoryFilter struct {
	// InventoryId, where set, requires inventory_id to equal it.
	InventoryId *uint32
	// InventoryIdFrom, where set, requires inventory_id to be at least it.
	InventoryIdFrom *uint32
	// InventoryIdTo, where set, requires inventory_id to be at most it.
	InventoryIdTo *uint32
	// FilmId, where set, requires film_id to equal it.
	FilmId *uint16
	// FilmIdFrom, where set, requires film_id to be at least it.
	FilmIdFrom *uint16
	// FilmIdTo, where set, requires film_id to be at most it.
	FilmIdTo *uint16
	// StoreId, where set, requires store_id to equal it.
	StoreId *uint8
	// StoreIdFrom, where set, requires store_id to be at least it.
	StoreIdFrom *uint8
	// StoreIdTo, where set, requires store_id to be at most it.
	StoreIdTo *uint8
	// LastUpdate, where set, requires last_update to equal it.
	LastUpdate *time.Time
	// LastUpdateFrom, where set, requires last_update to be at least it.
	LastUpdateFrom *time.Time
	// LastUpdateTo, where set, requires last_update to be at most it.
	LastUpdateTo *time.Time
}
