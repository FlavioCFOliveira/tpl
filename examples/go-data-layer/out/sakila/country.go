// Code generated by tpl from sakila.country. DO NOT EDIT.

package sakila

import (
	"time"
)

// Country is one row of sakila.country.
type Country struct {
	// CountryId is country_id, declared smallint(5) unsigned NOT NULL AUTO_INCREMENT.
	// It is part of the primary key.
	CountryId uint16
	// Country is country, declared varchar(50) NOT NULL.
	Country string
	// LastUpdate is last_update, declared timestamp NOT NULL.
	LastUpdate time.Time
}

// CountryKey is the primary key of sakila.country.
type CountryKey struct {
	// CountryId is country_id.
	CountryId uint16
}

// Key returns the primary key of the row.
func (row *Country) Key() CountryKey {
	var key CountryKey
	key.CountryId = row.CountryId
	return key
}

// CountryColumn names a column of sakila.country.
//
// A search accepts an ordering term only over the constants below, so an
// ordering can carry no SQL of its own.
type CountryColumn string

// The columns of sakila.country, in catalogue order.
const (
	// CountryColumnCountryId names country_id.
	CountryColumnCountryId CountryColumn = "country_id"
	// CountryColumnCountry names country.
	CountryColumnCountry CountryColumn = "country"
	// CountryColumnLastUpdate names last_update.
	CountryColumnLastUpdate CountryColumn = "last_update"
)

// CountryOrder is one ordering term of a search over sakila.country.
type CountryOrder struct {
	// Column is the column to order by.
	Column CountryColumn
	// Direction is the direction to order in.
	Direction Direction
}

// CountryFilter narrows a search over sakila.country.
//
// Every field is optional: a field left nil adds no condition, and the
// conditions a search does build are joined with AND.
type CountryFilter struct {
	// CountryId, where set, requires country_id to equal it.
	CountryId *uint16
	// CountryIdFrom, where set, requires country_id to be at least it.
	CountryIdFrom *uint16
	// CountryIdTo, where set, requires country_id to be at most it.
	CountryIdTo *uint16
	// Country, where set, requires country to equal it.
	Country *string
	// CountryLike, where set, requires country to match it, as LIKE matches.
	CountryLike *string
	// LastUpdate, where set, requires last_update to equal it.
	LastUpdate *time.Time
	// LastUpdateFrom, where set, requires last_update to be at least it.
	LastUpdateFrom *time.Time
	// LastUpdateTo, where set, requires last_update to be at most it.
	LastUpdateTo *time.Time
}
