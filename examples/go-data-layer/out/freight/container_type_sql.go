// Code generated by tpl from freight.container_type. DO NOT EDIT.

package freight

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// containerTypeSQL is the database/sql implementation of ContainerTypeRepository.
type containerTypeSQL struct {
	db *sql.DB
}

// NewContainerTypeRepository builds the data layer of freight.container_type over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewContainerTypeRepository(db *sql.DB) ContainerTypeRepository {
	return &containerTypeSQL{db: db}
}

// containerTypeSelect lists every column of freight.container_type, in catalogue order.
const containerTypeSelect = "SELECT `iso_code`, `description`, `length_ft`, `height_ft_in`, `tare_weight_kg`, `max_payload_kg`, `internal_volume_cbm`, `is_reefer`, `is_tank` FROM `container_type`"

// containerTypeRead reads the one row a primary key names.
const containerTypeRead = "SELECT `iso_code`, `description`, `length_ft`, `height_ft_in`, `tare_weight_kg`, `max_payload_kg`, `internal_volume_cbm`, `is_reefer`, `is_tank` FROM `container_type` WHERE `iso_code` = ?"

// containerTypeInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const containerTypeInsert = "INSERT INTO `container_type` (`iso_code`, `description`, `length_ft`, `height_ft_in`, `tare_weight_kg`, `max_payload_kg`, `internal_volume_cbm`, `is_reefer`, `is_tank`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)"

// containerTypeUpdate writes every column that is neither generated nor a key.
const containerTypeUpdate = "UPDATE `container_type` SET `description` = ?, `length_ft` = ?, `height_ft_in` = ?, `tare_weight_kg` = ?, `max_payload_kg` = ?, `internal_volume_cbm` = ?, `is_reefer` = ?, `is_tank` = ? WHERE `iso_code` = ?"

// containerTypeDelete removes the one row a primary key names.
const containerTypeDelete = "DELETE FROM `container_type` WHERE `iso_code` = ?"

// Create inserts row.
func (r *containerTypeSQL) Create(ctx context.Context, row *ContainerType) error {
	_, err := r.db.ExecContext(ctx, containerTypeInsert,
		row.IsoCode,
		row.Description,
		row.LengthFt,
		row.HeightFtIn,
		row.TareWeightKg,
		row.MaxPayloadKg,
		row.InternalVolumeCbm,
		row.IsReefer,
		row.IsTank,
	)
	if err != nil {
		return fmt.Errorf("freight.container_type: create: %w", err)
	}
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *containerTypeSQL) Read(ctx context.Context, key ContainerTypeKey) (*ContainerType, error) {
	var row ContainerType
	err := r.db.QueryRowContext(ctx, containerTypeRead,
		key.IsoCode,
	).Scan(
		&row.IsoCode,
		&row.Description,
		&row.LengthFt,
		&row.HeightFtIn,
		&row.TareWeightKg,
		&row.MaxPayloadKg,
		&row.InternalVolumeCbm,
		&row.IsReefer,
		&row.IsTank,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("freight.container_type: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("freight.container_type: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *containerTypeSQL) Update(ctx context.Context, row *ContainerType) (int64, error) {
	result, err := r.db.ExecContext(ctx, containerTypeUpdate,
		row.Description,
		row.LengthFt,
		row.HeightFtIn,
		row.TareWeightKg,
		row.MaxPayloadKg,
		row.InternalVolumeCbm,
		row.IsReefer,
		row.IsTank,
		row.IsoCode,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.container_type: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.container_type: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *containerTypeSQL) Delete(ctx context.Context, key ContainerTypeKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, containerTypeDelete,
		key.IsoCode,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.container_type: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.container_type: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *containerTypeSQL) Search(ctx context.Context, filter ContainerTypeFilter, order []ContainerTypeOrder, limit, offset int) ([]ContainerType, error) {
	conditions := make([]string, 0, 24)
	arguments := make([]any, 0, 26)

	if filter.IsoCode != nil {
		conditions = append(conditions, "`iso_code` = ?")
		arguments = append(arguments, *filter.IsoCode)
	}
	if filter.IsoCodeLike != nil {
		conditions = append(conditions, "`iso_code` LIKE ?")
		arguments = append(arguments, *filter.IsoCodeLike)
	}
	if filter.Description != nil {
		conditions = append(conditions, "`description` = ?")
		arguments = append(arguments, *filter.Description)
	}
	if filter.DescriptionLike != nil {
		conditions = append(conditions, "`description` LIKE ?")
		arguments = append(arguments, *filter.DescriptionLike)
	}
	if filter.LengthFt != nil {
		conditions = append(conditions, "`length_ft` = ?")
		arguments = append(arguments, *filter.LengthFt)
	}
	if filter.LengthFtFrom != nil {
		conditions = append(conditions, "`length_ft` >= ?")
		arguments = append(arguments, *filter.LengthFtFrom)
	}
	if filter.LengthFtTo != nil {
		conditions = append(conditions, "`length_ft` <= ?")
		arguments = append(arguments, *filter.LengthFtTo)
	}
	if filter.HeightFtIn != nil {
		conditions = append(conditions, "`height_ft_in` = ?")
		arguments = append(arguments, *filter.HeightFtIn)
	}
	if filter.HeightFtInLike != nil {
		conditions = append(conditions, "`height_ft_in` LIKE ?")
		arguments = append(arguments, *filter.HeightFtInLike)
	}
	if filter.TareWeightKg != nil {
		conditions = append(conditions, "`tare_weight_kg` = ?")
		arguments = append(arguments, *filter.TareWeightKg)
	}
	if filter.TareWeightKgFrom != nil {
		conditions = append(conditions, "`tare_weight_kg` >= ?")
		arguments = append(arguments, *filter.TareWeightKgFrom)
	}
	if filter.TareWeightKgTo != nil {
		conditions = append(conditions, "`tare_weight_kg` <= ?")
		arguments = append(arguments, *filter.TareWeightKgTo)
	}
	if filter.MaxPayloadKg != nil {
		conditions = append(conditions, "`max_payload_kg` = ?")
		arguments = append(arguments, *filter.MaxPayloadKg)
	}
	if filter.MaxPayloadKgFrom != nil {
		conditions = append(conditions, "`max_payload_kg` >= ?")
		arguments = append(arguments, *filter.MaxPayloadKgFrom)
	}
	if filter.MaxPayloadKgTo != nil {
		conditions = append(conditions, "`max_payload_kg` <= ?")
		arguments = append(arguments, *filter.MaxPayloadKgTo)
	}
	if filter.InternalVolumeCbm != nil {
		conditions = append(conditions, "`internal_volume_cbm` = ?")
		arguments = append(arguments, *filter.InternalVolumeCbm)
	}
	if filter.InternalVolumeCbmFrom != nil {
		conditions = append(conditions, "`internal_volume_cbm` >= ?")
		arguments = append(arguments, *filter.InternalVolumeCbmFrom)
	}
	if filter.InternalVolumeCbmTo != nil {
		conditions = append(conditions, "`internal_volume_cbm` <= ?")
		arguments = append(arguments, *filter.InternalVolumeCbmTo)
	}
	if filter.IsReefer != nil {
		conditions = append(conditions, "`is_reefer` = ?")
		arguments = append(arguments, *filter.IsReefer)
	}
	if filter.IsReeferFrom != nil {
		conditions = append(conditions, "`is_reefer` >= ?")
		arguments = append(arguments, *filter.IsReeferFrom)
	}
	if filter.IsReeferTo != nil {
		conditions = append(conditions, "`is_reefer` <= ?")
		arguments = append(arguments, *filter.IsReeferTo)
	}
	if filter.IsTank != nil {
		conditions = append(conditions, "`is_tank` = ?")
		arguments = append(arguments, *filter.IsTank)
	}
	if filter.IsTankFrom != nil {
		conditions = append(conditions, "`is_tank` >= ?")
		arguments = append(arguments, *filter.IsTankFrom)
	}
	if filter.IsTankTo != nil {
		conditions = append(conditions, "`is_tank` <= ?")
		arguments = append(arguments, *filter.IsTankTo)
	}

	var query strings.Builder
	query.WriteString(containerTypeSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := containerTypeOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("freight.container_type: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("freight.container_type: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("freight.container_type: search: %w", err)
	}
	defer rows.Close()

	var found []ContainerType
	for rows.Next() {
		var row ContainerType
		if err := rows.Scan(
			&row.IsoCode,
			&row.Description,
			&row.LengthFt,
			&row.HeightFtIn,
			&row.TareWeightKg,
			&row.MaxPayloadKg,
			&row.InternalVolumeCbm,
			&row.IsReefer,
			&row.IsTank,
		); err != nil {
			return nil, fmt.Errorf("freight.container_type: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("freight.container_type: search: %w", err)
	}
	return found, nil
}

// containerTypeOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func containerTypeOrderColumn(column ContainerTypeColumn) (string, error) {
	switch column {
	case ContainerTypeColumnIsoCode:
		return "`iso_code`", nil
	case ContainerTypeColumnDescription:
		return "`description`", nil
	case ContainerTypeColumnLengthFt:
		return "`length_ft`", nil
	case ContainerTypeColumnHeightFtIn:
		return "`height_ft_in`", nil
	case ContainerTypeColumnTareWeightKg:
		return "`tare_weight_kg`", nil
	case ContainerTypeColumnMaxPayloadKg:
		return "`max_payload_kg`", nil
	case ContainerTypeColumnInternalVolumeCbm:
		return "`internal_volume_cbm`", nil
	case ContainerTypeColumnIsReefer:
		return "`is_reefer`", nil
	case ContainerTypeColumnIsTank:
		return "`is_tank`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
