// Code generated by tpl from freight.container. DO NOT EDIT.

package freight

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// containerSQL is the database/sql implementation of ContainerRepository.
type containerSQL struct {
	db *sql.DB
}

// NewContainerRepository builds the data layer of freight.container over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewContainerRepository(db *sql.DB) ContainerRepository {
	return &containerSQL{db: db}
}

// containerSelect lists every column of freight.container, in catalogue order.
const containerSelect = "SELECT `container_id`, `equipment_no`, `consignment_id`, `iso_code`, `seal_number`, `tare_weight_kg`, `cargo_weight_kg`, `vgm_kg`, `vgm_method`, `setpoint_celsius`, `humidity_pct`, `vent_setting`, `stow_position`, `loaded_at` FROM `container`"

// containerRead reads the one row a primary key names.
const containerRead = "SELECT `container_id`, `equipment_no`, `consignment_id`, `iso_code`, `seal_number`, `tare_weight_kg`, `cargo_weight_kg`, `vgm_kg`, `vgm_method`, `setpoint_celsius`, `humidity_pct`, `vent_setting`, `stow_position`, `loaded_at` FROM `container` WHERE `container_id` = ?"

// containerInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const containerInsert = "INSERT INTO `container` (`equipment_no`, `consignment_id`, `iso_code`, `seal_number`, `tare_weight_kg`, `cargo_weight_kg`, `vgm_method`, `setpoint_celsius`, `humidity_pct`, `vent_setting`, `stow_position`, `loaded_at`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"

// containerUpdate writes every column that is neither generated nor a key.
const containerUpdate = "UPDATE `container` SET `equipment_no` = ?, `consignment_id` = ?, `iso_code` = ?, `seal_number` = ?, `tare_weight_kg` = ?, `cargo_weight_kg` = ?, `vgm_method` = ?, `setpoint_celsius` = ?, `humidity_pct` = ?, `vent_setting` = ?, `stow_position` = ?, `loaded_at` = ? WHERE `container_id` = ?"

// containerDelete removes the one row a primary key names.
const containerDelete = "DELETE FROM `container` WHERE `container_id` = ?"

// Create inserts row.
func (r *containerSQL) Create(ctx context.Context, row *Container) error {
	result, err := r.db.ExecContext(ctx, containerInsert,
		row.EquipmentNo,
		row.ConsignmentId,
		row.IsoCode,
		row.SealNumber,
		row.TareWeightKg,
		row.CargoWeightKg,
		row.VgmMethod,
		row.SetpointCelsius,
		row.HumidityPct,
		row.VentSetting,
		row.StowPosition,
		row.LoadedAt,
	)
	if err != nil {
		return fmt.Errorf("freight.container: create: %w", err)
	}
	assigned, err := result.LastInsertId()
	if err != nil {
		return fmt.Errorf("freight.container: create: reading the key the server assigned: %w", err)
	}
	row.ContainerId = uint64(assigned)
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *containerSQL) Read(ctx context.Context, key ContainerKey) (*Container, error) {
	var row Container
	err := r.db.QueryRowContext(ctx, containerRead,
		key.ContainerId,
	).Scan(
		&row.ContainerId,
		&row.EquipmentNo,
		&row.ConsignmentId,
		&row.IsoCode,
		&row.SealNumber,
		&row.TareWeightKg,
		&row.CargoWeightKg,
		&row.VgmKg,
		&row.VgmMethod,
		&row.SetpointCelsius,
		&row.HumidityPct,
		&row.VentSetting,
		&row.StowPosition,
		&row.LoadedAt,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("freight.container: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("freight.container: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *containerSQL) Update(ctx context.Context, row *Container) (int64, error) {
	result, err := r.db.ExecContext(ctx, containerUpdate,
		row.EquipmentNo,
		row.ConsignmentId,
		row.IsoCode,
		row.SealNumber,
		row.TareWeightKg,
		row.CargoWeightKg,
		row.VgmMethod,
		row.SetpointCelsius,
		row.HumidityPct,
		row.VentSetting,
		row.StowPosition,
		row.LoadedAt,
		row.ContainerId,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.container: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.container: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *containerSQL) Delete(ctx context.Context, key ContainerKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, containerDelete,
		key.ContainerId,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.container: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.container: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *containerSQL) Search(ctx context.Context, filter ContainerFilter, order []ContainerOrder, limit, offset int) ([]Container, error) {
	conditions := make([]string, 0, 43)
	arguments := make([]any, 0, 45)

	if filter.ContainerId != nil {
		conditions = append(conditions, "`container_id` = ?")
		arguments = append(arguments, *filter.ContainerId)
	}
	if filter.ContainerIdFrom != nil {
		conditions = append(conditions, "`container_id` >= ?")
		arguments = append(arguments, *filter.ContainerIdFrom)
	}
	if filter.ContainerIdTo != nil {
		conditions = append(conditions, "`container_id` <= ?")
		arguments = append(arguments, *filter.ContainerIdTo)
	}
	if filter.EquipmentNo != nil {
		conditions = append(conditions, "`equipment_no` = ?")
		arguments = append(arguments, *filter.EquipmentNo)
	}
	if filter.EquipmentNoLike != nil {
		conditions = append(conditions, "`equipment_no` LIKE ?")
		arguments = append(arguments, *filter.EquipmentNoLike)
	}
	if filter.ConsignmentId != nil {
		conditions = append(conditions, "`consignment_id` = ?")
		arguments = append(arguments, *filter.ConsignmentId)
	}
	if filter.ConsignmentIdFrom != nil {
		conditions = append(conditions, "`consignment_id` >= ?")
		arguments = append(arguments, *filter.ConsignmentIdFrom)
	}
	if filter.ConsignmentIdTo != nil {
		conditions = append(conditions, "`consignment_id` <= ?")
		arguments = append(arguments, *filter.ConsignmentIdTo)
	}
	if filter.IsoCode != nil {
		conditions = append(conditions, "`iso_code` = ?")
		arguments = append(arguments, *filter.IsoCode)
	}
	if filter.IsoCodeLike != nil {
		conditions = append(conditions, "`iso_code` LIKE ?")
		arguments = append(arguments, *filter.IsoCodeLike)
	}
	if filter.SealNumber != nil {
		conditions = append(conditions, "`seal_number` = ?")
		arguments = append(arguments, *filter.SealNumber)
	}
	if filter.SealNumberLike != nil {
		conditions = append(conditions, "`seal_number` LIKE ?")
		arguments = append(arguments, *filter.SealNumberLike)
	}
	if filter.SealNumberIsNull != nil {
		if *filter.SealNumberIsNull {
			conditions = append(conditions, "`seal_number` IS NULL")
		} else {
			conditions = append(conditions, "`seal_number` IS NOT NULL")
		}
	}
	if filter.TareWeightKg != nil {
		conditions = append(conditions, "`tare_weight_kg` = ?")
		arguments = append(arguments, *filter.TareWeightKg)
	}
	if filter.TareWeightKgFrom != nil {
		conditions = append(conditions, "`tare_weight_kg` >= ?")
		arguments = append(arguments, *filter.TareWeightKgFrom)
	}
	if filter.TareWeightKgTo != nil {
		conditions = append(conditions, "`tare_weight_kg` <= ?")
		arguments = append(arguments, *filter.TareWeightKgTo)
	}
	if filter.CargoWeightKg != nil {
		conditions = append(conditions, "`cargo_weight_kg` = ?")
		arguments = append(arguments, *filter.CargoWeightKg)
	}
	if filter.CargoWeightKgFrom != nil {
		conditions = append(conditions, "`cargo_weight_kg` >= ?")
		arguments = append(arguments, *filter.CargoWeightKgFrom)
	}
	if filter.CargoWeightKgTo != nil {
		conditions = append(conditions, "`cargo_weight_kg` <= ?")
		arguments = append(arguments, *filter.CargoWeightKgTo)
	}
	if filter.VgmKg != nil {
		conditions = append(conditions, "`vgm_kg` = ?")
		arguments = append(arguments, *filter.VgmKg)
	}
	if filter.VgmKgFrom != nil {
		conditions = append(conditions, "`vgm_kg` >= ?")
		arguments = append(arguments, *filter.VgmKgFrom)
	}
	if filter.VgmKgTo != nil {
		conditions = append(conditions, "`vgm_kg` <= ?")
		arguments = append(arguments, *filter.VgmKgTo)
	}
	if filter.VgmKgIsNull != nil {
		if *filter.VgmKgIsNull {
			conditions = append(conditions, "`vgm_kg` IS NULL")
		} else {
			conditions = append(conditions, "`vgm_kg` IS NOT NULL")
		}
	}
	if filter.VgmMethod != nil {
		conditions = append(conditions, "`vgm_method` = ?")
		arguments = append(arguments, *filter.VgmMethod)
	}
	if filter.VgmMethodLike != nil {
		conditions = append(conditions, "`vgm_method` LIKE ?")
		arguments = append(arguments, *filter.VgmMethodLike)
	}
	if filter.SetpointCelsius != nil {
		conditions = append(conditions, "`setpoint_celsius` = ?")
		arguments = append(arguments, *filter.SetpointCelsius)
	}
	if filter.SetpointCelsiusFrom != nil {
		conditions = append(conditions, "`setpoint_celsius` >= ?")
		arguments = append(arguments, *filter.SetpointCelsiusFrom)
	}
	if filter.SetpointCelsiusTo != nil {
		conditions = append(conditions, "`setpoint_celsius` <= ?")
		arguments = append(arguments, *filter.SetpointCelsiusTo)
	}
	if filter.SetpointCelsiusIsNull != nil {
		if *filter.SetpointCelsiusIsNull {
			conditions = append(conditions, "`setpoint_celsius` IS NULL")
		} else {
			conditions = append(conditions, "`setpoint_celsius` IS NOT NULL")
		}
	}
	if filter.HumidityPct != nil {
		conditions = append(conditions, "`humidity_pct` = ?")
		arguments = append(arguments, *filter.HumidityPct)
	}
	if filter.HumidityPctFrom != nil {
		conditions = append(conditions, "`humidity_pct` >= ?")
		arguments = append(arguments, *filter.HumidityPctFrom)
	}
	if filter.HumidityPctTo != nil {
		conditions = append(conditions, "`humidity_pct` <= ?")
		arguments = append(arguments, *filter.HumidityPctTo)
	}
	if filter.HumidityPctIsNull != nil {
		if *filter.HumidityPctIsNull {
			conditions = append(conditions, "`humidity_pct` IS NULL")
		} else {
			conditions = append(conditions, "`humidity_pct` IS NOT NULL")
		}
	}
	if filter.VentSetting != nil {
		conditions = append(conditions, "`vent_setting` = ?")
		arguments = append(arguments, *filter.VentSetting)
	}
	if filter.VentSettingLike != nil {
		conditions = append(conditions, "`vent_setting` LIKE ?")
		arguments = append(arguments, *filter.VentSettingLike)
	}
	if filter.VentSettingIsNull != nil {
		if *filter.VentSettingIsNull {
			conditions = append(conditions, "`vent_setting` IS NULL")
		} else {
			conditions = append(conditions, "`vent_setting` IS NOT NULL")
		}
	}
	if filter.StowPosition != nil {
		conditions = append(conditions, "`stow_position` = ?")
		arguments = append(arguments, *filter.StowPosition)
	}
	if filter.StowPositionLike != nil {
		conditions = append(conditions, "`stow_position` LIKE ?")
		arguments = append(arguments, *filter.StowPositionLike)
	}
	if filter.StowPositionIsNull != nil {
		if *filter.StowPositionIsNull {
			conditions = append(conditions, "`stow_position` IS NULL")
		} else {
			conditions = append(conditions, "`stow_position` IS NOT NULL")
		}
	}
	if filter.LoadedAt != nil {
		conditions = append(conditions, "`loaded_at` = ?")
		arguments = append(arguments, *filter.LoadedAt)
	}
	if filter.LoadedAtFrom != nil {
		conditions = append(conditions, "`loaded_at` >= ?")
		arguments = append(arguments, *filter.LoadedAtFrom)
	}
	if filter.LoadedAtTo != nil {
		conditions = append(conditions, "`loaded_at` <= ?")
		arguments = append(arguments, *filter.LoadedAtTo)
	}
	if filter.LoadedAtIsNull != nil {
		if *filter.LoadedAtIsNull {
			conditions = append(conditions, "`loaded_at` IS NULL")
		} else {
			conditions = append(conditions, "`loaded_at` IS NOT NULL")
		}
	}

	var query strings.Builder
	query.WriteString(containerSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := containerOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("freight.container: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("freight.container: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("freight.container: search: %w", err)
	}
	defer rows.Close()

	var found []Container
	for rows.Next() {
		var row Container
		if err := rows.Scan(
			&row.ContainerId,
			&row.EquipmentNo,
			&row.ConsignmentId,
			&row.IsoCode,
			&row.SealNumber,
			&row.TareWeightKg,
			&row.CargoWeightKg,
			&row.VgmKg,
			&row.VgmMethod,
			&row.SetpointCelsius,
			&row.HumidityPct,
			&row.VentSetting,
			&row.StowPosition,
			&row.LoadedAt,
		); err != nil {
			return nil, fmt.Errorf("freight.container: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("freight.container: search: %w", err)
	}
	return found, nil
}

// containerOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func containerOrderColumn(column ContainerColumn) (string, error) {
	switch column {
	case ContainerColumnContainerId:
		return "`container_id`", nil
	case ContainerColumnEquipmentNo:
		return "`equipment_no`", nil
	case ContainerColumnConsignmentId:
		return "`consignment_id`", nil
	case ContainerColumnIsoCode:
		return "`iso_code`", nil
	case ContainerColumnSealNumber:
		return "`seal_number`", nil
	case ContainerColumnTareWeightKg:
		return "`tare_weight_kg`", nil
	case ContainerColumnCargoWeightKg:
		return "`cargo_weight_kg`", nil
	case ContainerColumnVgmKg:
		return "`vgm_kg`", nil
	case ContainerColumnVgmMethod:
		return "`vgm_method`", nil
	case ContainerColumnSetpointCelsius:
		return "`setpoint_celsius`", nil
	case ContainerColumnHumidityPct:
		return "`humidity_pct`", nil
	case ContainerColumnVentSetting:
		return "`vent_setting`", nil
	case ContainerColumnStowPosition:
		return "`stow_position`", nil
	case ContainerColumnLoadedAt:
		return "`loaded_at`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
