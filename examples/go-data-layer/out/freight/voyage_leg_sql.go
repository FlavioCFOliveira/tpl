// Code generated by tpl from freight.voyage_leg. DO NOT EDIT.

package freight

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// voyageLegSQL is the database/sql implementation of VoyageLegRepository.
type voyageLegSQL struct {
	db *sql.DB
}

// NewVoyageLegRepository builds the data layer of freight.voyage_leg over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewVoyageLegRepository(db *sql.DB) VoyageLegRepository {
	return &voyageLegSQL{db: db}
}

// voyageLegSelect lists every column of freight.voyage_leg, in catalogue order.
const voyageLegSelect = "SELECT `vessel_imo`, `voyage_number`, `leg_sequence`, `port_id`, `eta`, `etd`, `ata`, `berth_shift_time`, `pilot_required`, `moves_discharged`, `moves_loaded`, `moves_total` FROM `voyage_leg`"

// voyageLegRead reads the one row a primary key names.
const voyageLegRead = "SELECT `vessel_imo`, `voyage_number`, `leg_sequence`, `port_id`, `eta`, `etd`, `ata`, `berth_shift_time`, `pilot_required`, `moves_discharged`, `moves_loaded`, `moves_total` FROM `voyage_leg` WHERE `vessel_imo` = ? AND `voyage_number` = ? AND `leg_sequence` = ?"

// voyageLegInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const voyageLegInsert = "INSERT INTO `voyage_leg` (`vessel_imo`, `voyage_number`, `leg_sequence`, `port_id`, `eta`, `etd`, `ata`, `berth_shift_time`, `pilot_required`, `moves_discharged`, `moves_loaded`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"

// voyageLegUpdate writes every column that is neither generated nor a key.
const voyageLegUpdate = "UPDATE `voyage_leg` SET `port_id` = ?, `eta` = ?, `etd` = ?, `ata` = ?, `berth_shift_time` = ?, `pilot_required` = ?, `moves_discharged` = ?, `moves_loaded` = ? WHERE `vessel_imo` = ? AND `voyage_number` = ? AND `leg_sequence` = ?"

// voyageLegDelete removes the one row a primary key names.
const voyageLegDelete = "DELETE FROM `voyage_leg` WHERE `vessel_imo` = ? AND `voyage_number` = ? AND `leg_sequence` = ?"

// Create inserts row.
func (r *voyageLegSQL) Create(ctx context.Context, row *VoyageLeg) error {
	_, err := r.db.ExecContext(ctx, voyageLegInsert,
		row.VesselImo,
		row.VoyageNumber,
		row.LegSequence,
		row.PortId,
		row.Eta,
		row.Etd,
		row.Ata,
		row.BerthShiftTime,
		row.PilotRequired,
		row.MovesDischarged,
		row.MovesLoaded,
	)
	if err != nil {
		return fmt.Errorf("freight.voyage_leg: create: %w", err)
	}
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *voyageLegSQL) Read(ctx context.Context, key VoyageLegKey) (*VoyageLeg, error) {
	var row VoyageLeg
	err := r.db.QueryRowContext(ctx, voyageLegRead,
		key.VesselImo,
		key.VoyageNumber,
		key.LegSequence,
	).Scan(
		&row.VesselImo,
		&row.VoyageNumber,
		&row.LegSequence,
		&row.PortId,
		&row.Eta,
		&row.Etd,
		&row.Ata,
		&row.BerthShiftTime,
		&row.PilotRequired,
		&row.MovesDischarged,
		&row.MovesLoaded,
		&row.MovesTotal,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("freight.voyage_leg: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("freight.voyage_leg: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *voyageLegSQL) Update(ctx context.Context, row *VoyageLeg) (int64, error) {
	result, err := r.db.ExecContext(ctx, voyageLegUpdate,
		row.PortId,
		row.Eta,
		row.Etd,
		row.Ata,
		row.BerthShiftTime,
		row.PilotRequired,
		row.MovesDischarged,
		row.MovesLoaded,
		row.VesselImo,
		row.VoyageNumber,
		row.LegSequence,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.voyage_leg: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.voyage_leg: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *voyageLegSQL) Delete(ctx context.Context, key VoyageLegKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, voyageLegDelete,
		key.VesselImo,
		key.VoyageNumber,
		key.LegSequence,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.voyage_leg: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.voyage_leg: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *voyageLegSQL) Search(ctx context.Context, filter VoyageLegFilter, order []VoyageLegOrder, limit, offset int) ([]VoyageLeg, error) {
	conditions := make([]string, 0, 37)
	arguments := make([]any, 0, 39)

	if filter.VesselImo != nil {
		conditions = append(conditions, "`vessel_imo` = ?")
		arguments = append(arguments, *filter.VesselImo)
	}
	if filter.VesselImoLike != nil {
		conditions = append(conditions, "`vessel_imo` LIKE ?")
		arguments = append(arguments, *filter.VesselImoLike)
	}
	if filter.VoyageNumber != nil {
		conditions = append(conditions, "`voyage_number` = ?")
		arguments = append(arguments, *filter.VoyageNumber)
	}
	if filter.VoyageNumberLike != nil {
		conditions = append(conditions, "`voyage_number` LIKE ?")
		arguments = append(arguments, *filter.VoyageNumberLike)
	}
	if filter.LegSequence != nil {
		conditions = append(conditions, "`leg_sequence` = ?")
		arguments = append(arguments, *filter.LegSequence)
	}
	if filter.LegSequenceFrom != nil {
		conditions = append(conditions, "`leg_sequence` >= ?")
		arguments = append(arguments, *filter.LegSequenceFrom)
	}
	if filter.LegSequenceTo != nil {
		conditions = append(conditions, "`leg_sequence` <= ?")
		arguments = append(arguments, *filter.LegSequenceTo)
	}
	if filter.PortId != nil {
		conditions = append(conditions, "`port_id` = ?")
		arguments = append(arguments, *filter.PortId)
	}
	if filter.PortIdFrom != nil {
		conditions = append(conditions, "`port_id` >= ?")
		arguments = append(arguments, *filter.PortIdFrom)
	}
	if filter.PortIdTo != nil {
		conditions = append(conditions, "`port_id` <= ?")
		arguments = append(arguments, *filter.PortIdTo)
	}
	if filter.Eta != nil {
		conditions = append(conditions, "`eta` = ?")
		arguments = append(arguments, *filter.Eta)
	}
	if filter.EtaFrom != nil {
		conditions = append(conditions, "`eta` >= ?")
		arguments = append(arguments, *filter.EtaFrom)
	}
	if filter.EtaTo != nil {
		conditions = append(conditions, "`eta` <= ?")
		arguments = append(arguments, *filter.EtaTo)
	}
	if filter.Etd != nil {
		conditions = append(conditions, "`etd` = ?")
		arguments = append(arguments, *filter.Etd)
	}
	if filter.EtdFrom != nil {
		conditions = append(conditions, "`etd` >= ?")
		arguments = append(arguments, *filter.EtdFrom)
	}
	if filter.EtdTo != nil {
		conditions = append(conditions, "`etd` <= ?")
		arguments = append(arguments, *filter.EtdTo)
	}
	if filter.Ata != nil {
		conditions = append(conditions, "`ata` = ?")
		arguments = append(arguments, *filter.Ata)
	}
	if filter.AtaFrom != nil {
		conditions = append(conditions, "`ata` >= ?")
		arguments = append(arguments, *filter.AtaFrom)
	}
	if filter.AtaTo != nil {
		conditions = append(conditions, "`ata` <= ?")
		arguments = append(arguments, *filter.AtaTo)
	}
	if filter.AtaIsNull != nil {
		if *filter.AtaIsNull {
			conditions = append(conditions, "`ata` IS NULL")
		} else {
			conditions = append(conditions, "`ata` IS NOT NULL")
		}
	}
	if filter.BerthShiftTime != nil {
		conditions = append(conditions, "`berth_shift_time` = ?")
		arguments = append(arguments, *filter.BerthShiftTime)
	}
	if filter.BerthShiftTimeFrom != nil {
		conditions = append(conditions, "`berth_shift_time` >= ?")
		arguments = append(arguments, *filter.BerthShiftTimeFrom)
	}
	if filter.BerthShiftTimeTo != nil {
		conditions = append(conditions, "`berth_shift_time` <= ?")
		arguments = append(arguments, *filter.BerthShiftTimeTo)
	}
	if filter.BerthShiftTimeIsNull != nil {
		if *filter.BerthShiftTimeIsNull {
			conditions = append(conditions, "`berth_shift_time` IS NULL")
		} else {
			conditions = append(conditions, "`berth_shift_time` IS NOT NULL")
		}
	}
	if filter.PilotRequired != nil {
		conditions = append(conditions, "`pilot_required` = ?")
		arguments = append(arguments, *filter.PilotRequired)
	}
	if filter.PilotRequiredFrom != nil {
		conditions = append(conditions, "`pilot_required` >= ?")
		arguments = append(arguments, *filter.PilotRequiredFrom)
	}
	if filter.PilotRequiredTo != nil {
		conditions = append(conditions, "`pilot_required` <= ?")
		arguments = append(arguments, *filter.PilotRequiredTo)
	}
	if filter.MovesDischarged != nil {
		conditions = append(conditions, "`moves_discharged` = ?")
		arguments = append(arguments, *filter.MovesDischarged)
	}
	if filter.MovesDischargedFrom != nil {
		conditions = append(conditions, "`moves_discharged` >= ?")
		arguments = append(arguments, *filter.MovesDischargedFrom)
	}
	if filter.MovesDischargedTo != nil {
		conditions = append(conditions, "`moves_discharged` <= ?")
		arguments = append(arguments, *filter.MovesDischargedTo)
	}
	if filter.MovesLoaded != nil {
		conditions = append(conditions, "`moves_loaded` = ?")
		arguments = append(arguments, *filter.MovesLoaded)
	}
	if filter.MovesLoadedFrom != nil {
		conditions = append(conditions, "`moves_loaded` >= ?")
		arguments = append(arguments, *filter.MovesLoadedFrom)
	}
	if filter.MovesLoadedTo != nil {
		conditions = append(conditions, "`moves_loaded` <= ?")
		arguments = append(arguments, *filter.MovesLoadedTo)
	}
	if filter.MovesTotal != nil {
		conditions = append(conditions, "`moves_total` = ?")
		arguments = append(arguments, *filter.MovesTotal)
	}
	if filter.MovesTotalFrom != nil {
		conditions = append(conditions, "`moves_total` >= ?")
		arguments = append(arguments, *filter.MovesTotalFrom)
	}
	if filter.MovesTotalTo != nil {
		conditions = append(conditions, "`moves_total` <= ?")
		arguments = append(arguments, *filter.MovesTotalTo)
	}
	if filter.MovesTotalIsNull != nil {
		if *filter.MovesTotalIsNull {
			conditions = append(conditions, "`moves_total` IS NULL")
		} else {
			conditions = append(conditions, "`moves_total` IS NOT NULL")
		}
	}

	var query strings.Builder
	query.WriteString(voyageLegSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := voyageLegOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("freight.voyage_leg: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("freight.voyage_leg: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("freight.voyage_leg: search: %w", err)
	}
	defer rows.Close()

	var found []VoyageLeg
	for rows.Next() {
		var row VoyageLeg
		if err := rows.Scan(
			&row.VesselImo,
			&row.VoyageNumber,
			&row.LegSequence,
			&row.PortId,
			&row.Eta,
			&row.Etd,
			&row.Ata,
			&row.BerthShiftTime,
			&row.PilotRequired,
			&row.MovesDischarged,
			&row.MovesLoaded,
			&row.MovesTotal,
		); err != nil {
			return nil, fmt.Errorf("freight.voyage_leg: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("freight.voyage_leg: search: %w", err)
	}
	return found, nil
}

// voyageLegOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func voyageLegOrderColumn(column VoyageLegColumn) (string, error) {
	switch column {
	case VoyageLegColumnVesselImo:
		return "`vessel_imo`", nil
	case VoyageLegColumnVoyageNumber:
		return "`voyage_number`", nil
	case VoyageLegColumnLegSequence:
		return "`leg_sequence`", nil
	case VoyageLegColumnPortId:
		return "`port_id`", nil
	case VoyageLegColumnEta:
		return "`eta`", nil
	case VoyageLegColumnEtd:
		return "`etd`", nil
	case VoyageLegColumnAta:
		return "`ata`", nil
	case VoyageLegColumnBerthShiftTime:
		return "`berth_shift_time`", nil
	case VoyageLegColumnPilotRequired:
		return "`pilot_required`", nil
	case VoyageLegColumnMovesDischarged:
		return "`moves_discharged`", nil
	case VoyageLegColumnMovesLoaded:
		return "`moves_loaded`", nil
	case VoyageLegColumnMovesTotal:
		return "`moves_total`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
