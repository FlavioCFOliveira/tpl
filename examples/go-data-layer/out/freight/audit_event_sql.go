// Code generated by tpl from freight.audit_event. DO NOT EDIT.

package freight

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// auditEventSQL is the database/sql implementation of AuditEventRepository.
type auditEventSQL struct {
	db *sql.DB
}

// NewAuditEventRepository builds the data layer of freight.audit_event over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewAuditEventRepository(db *sql.DB) AuditEventRepository {
	return &auditEventSQL{db: db}
}

// auditEventSelect lists every column of freight.audit_event, in catalogue order.
const auditEventSelect = "SELECT `audit_event_id`, `entity_name`, `entity_key`, `action`, `trigger_timing`, `changed_column`, `old_value`, `new_value`, `actor`, `client_ipv4`, `client_ipv6`, `correlation_uuid`, `observed_at`, `detail` FROM `audit_event`"

// auditEventRead reads the one row a primary key names.
const auditEventRead = "SELECT `audit_event_id`, `entity_name`, `entity_key`, `action`, `trigger_timing`, `changed_column`, `old_value`, `new_value`, `actor`, `client_ipv4`, `client_ipv6`, `correlation_uuid`, `observed_at`, `detail` FROM `audit_event` WHERE `audit_event_id` = ?"

// auditEventInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const auditEventInsert = "INSERT INTO `audit_event` (`entity_name`, `entity_key`, `action`, `trigger_timing`, `changed_column`, `old_value`, `new_value`, `actor`, `client_ipv4`, `client_ipv6`, `correlation_uuid`, `observed_at`, `detail`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"

// auditEventUpdate writes every column that is neither generated nor a key.
const auditEventUpdate = "UPDATE `audit_event` SET `entity_name` = ?, `entity_key` = ?, `action` = ?, `trigger_timing` = ?, `changed_column` = ?, `old_value` = ?, `new_value` = ?, `actor` = ?, `client_ipv4` = ?, `client_ipv6` = ?, `correlation_uuid` = ?, `observed_at` = ?, `detail` = ? WHERE `audit_event_id` = ?"

// auditEventDelete removes the one row a primary key names.
const auditEventDelete = "DELETE FROM `audit_event` WHERE `audit_event_id` = ?"

// Create inserts row.
func (r *auditEventSQL) Create(ctx context.Context, row *AuditEvent) error {
	result, err := r.db.ExecContext(ctx, auditEventInsert,
		row.EntityName,
		row.EntityKey,
		row.Action,
		row.TriggerTiming,
		row.ChangedColumn,
		row.OldValue,
		row.NewValue,
		row.Actor,
		row.ClientIpv4,
		row.ClientIpv6,
		row.CorrelationUuid,
		row.ObservedAt,
		row.Detail,
	)
	if err != nil {
		return fmt.Errorf("freight.audit_event: create: %w", err)
	}
	assigned, err := result.LastInsertId()
	if err != nil {
		return fmt.Errorf("freight.audit_event: create: reading the key the server assigned: %w", err)
	}
	row.AuditEventId = uint64(assigned)
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *auditEventSQL) Read(ctx context.Context, key AuditEventKey) (*AuditEvent, error) {
	var row AuditEvent
	err := r.db.QueryRowContext(ctx, auditEventRead,
		key.AuditEventId,
	).Scan(
		&row.AuditEventId,
		&row.EntityName,
		&row.EntityKey,
		&row.Action,
		&row.TriggerTiming,
		&row.ChangedColumn,
		&row.OldValue,
		&row.NewValue,
		&row.Actor,
		&row.ClientIpv4,
		&row.ClientIpv6,
		&row.CorrelationUuid,
		&row.ObservedAt,
		&row.Detail,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("freight.audit_event: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("freight.audit_event: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *auditEventSQL) Update(ctx context.Context, row *AuditEvent) (int64, error) {
	result, err := r.db.ExecContext(ctx, auditEventUpdate,
		row.EntityName,
		row.EntityKey,
		row.Action,
		row.TriggerTiming,
		row.ChangedColumn,
		row.OldValue,
		row.NewValue,
		row.Actor,
		row.ClientIpv4,
		row.ClientIpv6,
		row.CorrelationUuid,
		row.ObservedAt,
		row.Detail,
		row.AuditEventId,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.audit_event: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.audit_event: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *auditEventSQL) Delete(ctx context.Context, key AuditEventKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, auditEventDelete,
		key.AuditEventId,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.audit_event: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.audit_event: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *auditEventSQL) Search(ctx context.Context, filter AuditEventFilter, order []AuditEventOrder, limit, offset int) ([]AuditEvent, error) {
	conditions := make([]string, 0, 34)
	arguments := make([]any, 0, 36)

	if filter.AuditEventId != nil {
		conditions = append(conditions, "`audit_event_id` = ?")
		arguments = append(arguments, *filter.AuditEventId)
	}
	if filter.AuditEventIdFrom != nil {
		conditions = append(conditions, "`audit_event_id` >= ?")
		arguments = append(arguments, *filter.AuditEventIdFrom)
	}
	if filter.AuditEventIdTo != nil {
		conditions = append(conditions, "`audit_event_id` <= ?")
		arguments = append(arguments, *filter.AuditEventIdTo)
	}
	if filter.EntityName != nil {
		conditions = append(conditions, "`entity_name` = ?")
		arguments = append(arguments, *filter.EntityName)
	}
	if filter.EntityNameLike != nil {
		conditions = append(conditions, "`entity_name` LIKE ?")
		arguments = append(arguments, *filter.EntityNameLike)
	}
	if filter.EntityKey != nil {
		conditions = append(conditions, "`entity_key` = ?")
		arguments = append(arguments, *filter.EntityKey)
	}
	if filter.EntityKeyLike != nil {
		conditions = append(conditions, "`entity_key` LIKE ?")
		arguments = append(arguments, *filter.EntityKeyLike)
	}
	if filter.Action != nil {
		conditions = append(conditions, "`action` = ?")
		arguments = append(arguments, *filter.Action)
	}
	if filter.ActionLike != nil {
		conditions = append(conditions, "`action` LIKE ?")
		arguments = append(arguments, *filter.ActionLike)
	}
	if filter.TriggerTiming != nil {
		conditions = append(conditions, "`trigger_timing` = ?")
		arguments = append(arguments, *filter.TriggerTiming)
	}
	if filter.TriggerTimingLike != nil {
		conditions = append(conditions, "`trigger_timing` LIKE ?")
		arguments = append(arguments, *filter.TriggerTimingLike)
	}
	if filter.ChangedColumn != nil {
		conditions = append(conditions, "`changed_column` = ?")
		arguments = append(arguments, *filter.ChangedColumn)
	}
	if filter.ChangedColumnLike != nil {
		conditions = append(conditions, "`changed_column` LIKE ?")
		arguments = append(arguments, *filter.ChangedColumnLike)
	}
	if filter.ChangedColumnIsNull != nil {
		if *filter.ChangedColumnIsNull {
			conditions = append(conditions, "`changed_column` IS NULL")
		} else {
			conditions = append(conditions, "`changed_column` IS NOT NULL")
		}
	}
	if filter.OldValue != nil {
		conditions = append(conditions, "`old_value` = ?")
		arguments = append(arguments, *filter.OldValue)
	}
	if filter.OldValueLike != nil {
		conditions = append(conditions, "`old_value` LIKE ?")
		arguments = append(arguments, *filter.OldValueLike)
	}
	if filter.OldValueIsNull != nil {
		if *filter.OldValueIsNull {
			conditions = append(conditions, "`old_value` IS NULL")
		} else {
			conditions = append(conditions, "`old_value` IS NOT NULL")
		}
	}
	if filter.NewValue != nil {
		conditions = append(conditions, "`new_value` = ?")
		arguments = append(arguments, *filter.NewValue)
	}
	if filter.NewValueLike != nil {
		conditions = append(conditions, "`new_value` LIKE ?")
		arguments = append(arguments, *filter.NewValueLike)
	}
	if filter.NewValueIsNull != nil {
		if *filter.NewValueIsNull {
			conditions = append(conditions, "`new_value` IS NULL")
		} else {
			conditions = append(conditions, "`new_value` IS NOT NULL")
		}
	}
	if filter.Actor != nil {
		conditions = append(conditions, "`actor` = ?")
		arguments = append(arguments, *filter.Actor)
	}
	if filter.ActorLike != nil {
		conditions = append(conditions, "`actor` LIKE ?")
		arguments = append(arguments, *filter.ActorLike)
	}
	if filter.ClientIpv4 != nil {
		conditions = append(conditions, "`client_ipv4` = ?")
		arguments = append(arguments, *filter.ClientIpv4)
	}
	if filter.ClientIpv4IsNull != nil {
		if *filter.ClientIpv4IsNull {
			conditions = append(conditions, "`client_ipv4` IS NULL")
		} else {
			conditions = append(conditions, "`client_ipv4` IS NOT NULL")
		}
	}
	if filter.ClientIpv6 != nil {
		conditions = append(conditions, "`client_ipv6` = ?")
		arguments = append(arguments, *filter.ClientIpv6)
	}
	if filter.ClientIpv6IsNull != nil {
		if *filter.ClientIpv6IsNull {
			conditions = append(conditions, "`client_ipv6` IS NULL")
		} else {
			conditions = append(conditions, "`client_ipv6` IS NOT NULL")
		}
	}
	if filter.CorrelationUuid != nil {
		conditions = append(conditions, "`correlation_uuid` = ?")
		arguments = append(arguments, *filter.CorrelationUuid)
	}
	if filter.CorrelationUuidIsNull != nil {
		if *filter.CorrelationUuidIsNull {
			conditions = append(conditions, "`correlation_uuid` IS NULL")
		} else {
			conditions = append(conditions, "`correlation_uuid` IS NOT NULL")
		}
	}
	if filter.ObservedAt != nil {
		conditions = append(conditions, "`observed_at` = ?")
		arguments = append(arguments, *filter.ObservedAt)
	}
	if filter.ObservedAtFrom != nil {
		conditions = append(conditions, "`observed_at` >= ?")
		arguments = append(arguments, *filter.ObservedAtFrom)
	}
	if filter.ObservedAtTo != nil {
		conditions = append(conditions, "`observed_at` <= ?")
		arguments = append(arguments, *filter.ObservedAtTo)
	}
	if filter.Detail != nil {
		conditions = append(conditions, "`detail` = ?")
		arguments = append(arguments, *filter.Detail)
	}
	if filter.DetailLike != nil {
		conditions = append(conditions, "`detail` LIKE ?")
		arguments = append(arguments, *filter.DetailLike)
	}
	if filter.DetailIsNull != nil {
		if *filter.DetailIsNull {
			conditions = append(conditions, "`detail` IS NULL")
		} else {
			conditions = append(conditions, "`detail` IS NOT NULL")
		}
	}

	var query strings.Builder
	query.WriteString(auditEventSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := auditEventOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("freight.audit_event: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("freight.audit_event: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("freight.audit_event: search: %w", err)
	}
	defer rows.Close()

	var found []AuditEvent
	for rows.Next() {
		var row AuditEvent
		if err := rows.Scan(
			&row.AuditEventId,
			&row.EntityName,
			&row.EntityKey,
			&row.Action,
			&row.TriggerTiming,
			&row.ChangedColumn,
			&row.OldValue,
			&row.NewValue,
			&row.Actor,
			&row.ClientIpv4,
			&row.ClientIpv6,
			&row.CorrelationUuid,
			&row.ObservedAt,
			&row.Detail,
		); err != nil {
			return nil, fmt.Errorf("freight.audit_event: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("freight.audit_event: search: %w", err)
	}
	return found, nil
}

// auditEventOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func auditEventOrderColumn(column AuditEventColumn) (string, error) {
	switch column {
	case AuditEventColumnAuditEventId:
		return "`audit_event_id`", nil
	case AuditEventColumnEntityName:
		return "`entity_name`", nil
	case AuditEventColumnEntityKey:
		return "`entity_key`", nil
	case AuditEventColumnAction:
		return "`action`", nil
	case AuditEventColumnTriggerTiming:
		return "`trigger_timing`", nil
	case AuditEventColumnChangedColumn:
		return "`changed_column`", nil
	case AuditEventColumnOldValue:
		return "`old_value`", nil
	case AuditEventColumnNewValue:
		return "`new_value`", nil
	case AuditEventColumnActor:
		return "`actor`", nil
	case AuditEventColumnClientIpv4:
		return "`client_ipv4`", nil
	case AuditEventColumnClientIpv6:
		return "`client_ipv6`", nil
	case AuditEventColumnCorrelationUuid:
		return "`correlation_uuid`", nil
	case AuditEventColumnObservedAt:
		return "`observed_at`", nil
	case AuditEventColumnDetail:
		return "`detail`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
