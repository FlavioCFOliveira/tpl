// Code generated by tpl from freight.charge_code. DO NOT EDIT.

package freight

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// chargeCodeSQL is the database/sql implementation of ChargeCodeRepository.
type chargeCodeSQL struct {
	db *sql.DB
}

// NewChargeCodeRepository builds the data layer of freight.charge_code over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewChargeCodeRepository(db *sql.DB) ChargeCodeRepository {
	return &chargeCodeSQL{db: db}
}

// chargeCodeSelect lists every column of freight.charge_code, in catalogue order.
const chargeCodeSelect = "SELECT `charge_code`, `description`, `is_freight`, `vat_rate`, `gl_account` FROM `charge_code`"

// chargeCodeRead reads the one row a primary key names.
const chargeCodeRead = "SELECT `charge_code`, `description`, `is_freight`, `vat_rate`, `gl_account` FROM `charge_code` WHERE `charge_code` = ?"

// chargeCodeInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const chargeCodeInsert = "INSERT INTO `charge_code` (`charge_code`, `description`, `is_freight`, `vat_rate`, `gl_account`) VALUES (?, ?, ?, ?, ?)"

// chargeCodeUpdate writes every column that is neither generated nor a key.
const chargeCodeUpdate = "UPDATE `charge_code` SET `description` = ?, `is_freight` = ?, `vat_rate` = ?, `gl_account` = ? WHERE `charge_code` = ?"

// chargeCodeDelete removes the one row a primary key names.
const chargeCodeDelete = "DELETE FROM `charge_code` WHERE `charge_code` = ?"

// Create inserts row.
func (r *chargeCodeSQL) Create(ctx context.Context, row *ChargeCode) error {
	_, err := r.db.ExecContext(ctx, chargeCodeInsert,
		row.ChargeCode,
		row.Description,
		row.IsFreight,
		row.VatRate,
		row.GlAccount,
	)
	if err != nil {
		return fmt.Errorf("freight.charge_code: create: %w", err)
	}
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *chargeCodeSQL) Read(ctx context.Context, key ChargeCodeKey) (*ChargeCode, error) {
	var row ChargeCode
	err := r.db.QueryRowContext(ctx, chargeCodeRead,
		key.ChargeCode,
	).Scan(
		&row.ChargeCode,
		&row.Description,
		&row.IsFreight,
		&row.VatRate,
		&row.GlAccount,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("freight.charge_code: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("freight.charge_code: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *chargeCodeSQL) Update(ctx context.Context, row *ChargeCode) (int64, error) {
	result, err := r.db.ExecContext(ctx, chargeCodeUpdate,
		row.Description,
		row.IsFreight,
		row.VatRate,
		row.GlAccount,
		row.ChargeCode,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.charge_code: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.charge_code: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *chargeCodeSQL) Delete(ctx context.Context, key ChargeCodeKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, chargeCodeDelete,
		key.ChargeCode,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.charge_code: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.charge_code: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *chargeCodeSQL) Search(ctx context.Context, filter ChargeCodeFilter, order []ChargeCodeOrder, limit, offset int) ([]ChargeCode, error) {
	conditions := make([]string, 0, 12)
	arguments := make([]any, 0, 14)

	if filter.ChargeCode != nil {
		conditions = append(conditions, "`charge_code` = ?")
		arguments = append(arguments, *filter.ChargeCode)
	}
	if filter.ChargeCodeLike != nil {
		conditions = append(conditions, "`charge_code` LIKE ?")
		arguments = append(arguments, *filter.ChargeCodeLike)
	}
	if filter.Description != nil {
		conditions = append(conditions, "`description` = ?")
		arguments = append(arguments, *filter.Description)
	}
	if filter.DescriptionLike != nil {
		conditions = append(conditions, "`description` LIKE ?")
		arguments = append(arguments, *filter.DescriptionLike)
	}
	if filter.IsFreight != nil {
		conditions = append(conditions, "`is_freight` = ?")
		arguments = append(arguments, *filter.IsFreight)
	}
	if filter.IsFreightFrom != nil {
		conditions = append(conditions, "`is_freight` >= ?")
		arguments = append(arguments, *filter.IsFreightFrom)
	}
	if filter.IsFreightTo != nil {
		conditions = append(conditions, "`is_freight` <= ?")
		arguments = append(arguments, *filter.IsFreightTo)
	}
	if filter.VatRate != nil {
		conditions = append(conditions, "`vat_rate` = ?")
		arguments = append(arguments, *filter.VatRate)
	}
	if filter.VatRateFrom != nil {
		conditions = append(conditions, "`vat_rate` >= ?")
		arguments = append(arguments, *filter.VatRateFrom)
	}
	if filter.VatRateTo != nil {
		conditions = append(conditions, "`vat_rate` <= ?")
		arguments = append(arguments, *filter.VatRateTo)
	}
	if filter.GlAccount != nil {
		conditions = append(conditions, "`gl_account` = ?")
		arguments = append(arguments, *filter.GlAccount)
	}
	if filter.GlAccountLike != nil {
		conditions = append(conditions, "`gl_account` LIKE ?")
		arguments = append(arguments, *filter.GlAccountLike)
	}

	var query strings.Builder
	query.WriteString(chargeCodeSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := chargeCodeOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("freight.charge_code: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("freight.charge_code: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("freight.charge_code: search: %w", err)
	}
	defer rows.Close()

	var found []ChargeCode
	for rows.Next() {
		var row ChargeCode
		if err := rows.Scan(
			&row.ChargeCode,
			&row.Description,
			&row.IsFreight,
			&row.VatRate,
			&row.GlAccount,
		); err != nil {
			return nil, fmt.Errorf("freight.charge_code: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("freight.charge_code: search: %w", err)
	}
	return found, nil
}

// chargeCodeOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func chargeCodeOrderColumn(column ChargeCodeColumn) (string, error) {
	switch column {
	case ChargeCodeColumnChargeCode:
		return "`charge_code`", nil
	case ChargeCodeColumnDescription:
		return "`description`", nil
	case ChargeCodeColumnIsFreight:
		return "`is_freight`", nil
	case ChargeCodeColumnVatRate:
		return "`vat_rate`", nil
	case ChargeCodeColumnGlAccount:
		return "`gl_account`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
