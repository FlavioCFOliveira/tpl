// Code generated by tpl from freight.customs_declaration. DO NOT EDIT.

package freight

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// customsDeclarationSQL is the database/sql implementation of CustomsDeclarationRepository.
type customsDeclarationSQL struct {
	db *sql.DB
}

// NewCustomsDeclarationRepository builds the data layer of freight.customs_declaration over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewCustomsDeclarationRepository(db *sql.DB) CustomsDeclarationRepository {
	return &customsDeclarationSQL{db: db}
}

// customsDeclarationSelect lists every column of freight.customs_declaration, in catalogue order.
const customsDeclarationSelect = "SELECT `declaration_id`, `consignment_id`, `clearance_port_id`, `mrn`, `procedure_code`, `regime`, `status`, `lodged_at`, `released_at`, `duty_amount`, `vat_amount`, `total_payable`, `officer_remarks` FROM `customs_declaration`"

// customsDeclarationRead reads the one row a primary key names.
const customsDeclarationRead = "SELECT `declaration_id`, `consignment_id`, `clearance_port_id`, `mrn`, `procedure_code`, `regime`, `status`, `lodged_at`, `released_at`, `duty_amount`, `vat_amount`, `total_payable`, `officer_remarks` FROM `customs_declaration` WHERE `declaration_id` = ?"

// customsDeclarationInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const customsDeclarationInsert = "INSERT INTO `customs_declaration` (`consignment_id`, `clearance_port_id`, `mrn`, `procedure_code`, `regime`, `status`, `lodged_at`, `released_at`, `duty_amount`, `vat_amount`, `officer_remarks`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"

// customsDeclarationUpdate writes every column that is neither generated nor a key.
const customsDeclarationUpdate = "UPDATE `customs_declaration` SET `consignment_id` = ?, `clearance_port_id` = ?, `mrn` = ?, `procedure_code` = ?, `regime` = ?, `status` = ?, `lodged_at` = ?, `released_at` = ?, `duty_amount` = ?, `vat_amount` = ?, `officer_remarks` = ? WHERE `declaration_id` = ?"

// customsDeclarationDelete removes the one row a primary key names.
const customsDeclarationDelete = "DELETE FROM `customs_declaration` WHERE `declaration_id` = ?"

// Create inserts row.
func (r *customsDeclarationSQL) Create(ctx context.Context, row *CustomsDeclaration) error {
	result, err := r.db.ExecContext(ctx, customsDeclarationInsert,
		row.ConsignmentId,
		row.ClearancePortId,
		row.Mrn,
		row.ProcedureCode,
		row.Regime,
		row.Status,
		row.LodgedAt,
		row.ReleasedAt,
		row.DutyAmount,
		row.VatAmount,
		row.OfficerRemarks,
	)
	if err != nil {
		return fmt.Errorf("freight.customs_declaration: create: %w", err)
	}
	assigned, err := result.LastInsertId()
	if err != nil {
		return fmt.Errorf("freight.customs_declaration: create: reading the key the server assigned: %w", err)
	}
	row.DeclarationId = uint64(assigned)
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *customsDeclarationSQL) Read(ctx context.Context, key CustomsDeclarationKey) (*CustomsDeclaration, error) {
	var row CustomsDeclaration
	err := r.db.QueryRowContext(ctx, customsDeclarationRead,
		key.DeclarationId,
	).Scan(
		&row.DeclarationId,
		&row.ConsignmentId,
		&row.ClearancePortId,
		&row.Mrn,
		&row.ProcedureCode,
		&row.Regime,
		&row.Status,
		&row.LodgedAt,
		&row.ReleasedAt,
		&row.DutyAmount,
		&row.VatAmount,
		&row.TotalPayable,
		&row.OfficerRemarks,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("freight.customs_declaration: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("freight.customs_declaration: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *customsDeclarationSQL) Update(ctx context.Context, row *CustomsDeclaration) (int64, error) {
	result, err := r.db.ExecContext(ctx, customsDeclarationUpdate,
		row.ConsignmentId,
		row.ClearancePortId,
		row.Mrn,
		row.ProcedureCode,
		row.Regime,
		row.Status,
		row.LodgedAt,
		row.ReleasedAt,
		row.DutyAmount,
		row.VatAmount,
		row.OfficerRemarks,
		row.DeclarationId,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.customs_declaration: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.customs_declaration: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *customsDeclarationSQL) Delete(ctx context.Context, key CustomsDeclarationKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, customsDeclarationDelete,
		key.DeclarationId,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.customs_declaration: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.customs_declaration: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *customsDeclarationSQL) Search(ctx context.Context, filter CustomsDeclarationFilter, order []CustomsDeclarationOrder, limit, offset int) ([]CustomsDeclaration, error) {
	conditions := make([]string, 0, 39)
	arguments := make([]any, 0, 41)

	if filter.DeclarationId != nil {
		conditions = append(conditions, "`declaration_id` = ?")
		arguments = append(arguments, *filter.DeclarationId)
	}
	if filter.DeclarationIdFrom != nil {
		conditions = append(conditions, "`declaration_id` >= ?")
		arguments = append(arguments, *filter.DeclarationIdFrom)
	}
	if filter.DeclarationIdTo != nil {
		conditions = append(conditions, "`declaration_id` <= ?")
		arguments = append(arguments, *filter.DeclarationIdTo)
	}
	if filter.ConsignmentId != nil {
		conditions = append(conditions, "`consignment_id` = ?")
		arguments = append(arguments, *filter.ConsignmentId)
	}
	if filter.ConsignmentIdFrom != nil {
		conditions = append(conditions, "`consignment_id` >= ?")
		arguments = append(arguments, *filter.ConsignmentIdFrom)
	}
	if filter.ConsignmentIdTo != nil {
		conditions = append(conditions, "`consignment_id` <= ?")
		arguments = append(arguments, *filter.ConsignmentIdTo)
	}
	if filter.ClearancePortId != nil {
		conditions = append(conditions, "`clearance_port_id` = ?")
		arguments = append(arguments, *filter.ClearancePortId)
	}
	if filter.ClearancePortIdFrom != nil {
		conditions = append(conditions, "`clearance_port_id` >= ?")
		arguments = append(arguments, *filter.ClearancePortIdFrom)
	}
	if filter.ClearancePortIdTo != nil {
		conditions = append(conditions, "`clearance_port_id` <= ?")
		arguments = append(arguments, *filter.ClearancePortIdTo)
	}
	if filter.ClearancePortIdIsNull != nil {
		if *filter.ClearancePortIdIsNull {
			conditions = append(conditions, "`clearance_port_id` IS NULL")
		} else {
			conditions = append(conditions, "`clearance_port_id` IS NOT NULL")
		}
	}
	if filter.Mrn != nil {
		conditions = append(conditions, "`mrn` = ?")
		arguments = append(arguments, *filter.Mrn)
	}
	if filter.MrnLike != nil {
		conditions = append(conditions, "`mrn` LIKE ?")
		arguments = append(arguments, *filter.MrnLike)
	}
	if filter.ProcedureCode != nil {
		conditions = append(conditions, "`procedure_code` = ?")
		arguments = append(arguments, *filter.ProcedureCode)
	}
	if filter.ProcedureCodeLike != nil {
		conditions = append(conditions, "`procedure_code` LIKE ?")
		arguments = append(arguments, *filter.ProcedureCodeLike)
	}
	if filter.Regime != nil {
		conditions = append(conditions, "`regime` = ?")
		arguments = append(arguments, *filter.Regime)
	}
	if filter.RegimeLike != nil {
		conditions = append(conditions, "`regime` LIKE ?")
		arguments = append(arguments, *filter.RegimeLike)
	}
	if filter.Status != nil {
		conditions = append(conditions, "`status` = ?")
		arguments = append(arguments, *filter.Status)
	}
	if filter.StatusLike != nil {
		conditions = append(conditions, "`status` LIKE ?")
		arguments = append(arguments, *filter.StatusLike)
	}
	if filter.LodgedAt != nil {
		conditions = append(conditions, "`lodged_at` = ?")
		arguments = append(arguments, *filter.LodgedAt)
	}
	if filter.LodgedAtFrom != nil {
		conditions = append(conditions, "`lodged_at` >= ?")
		arguments = append(arguments, *filter.LodgedAtFrom)
	}
	if filter.LodgedAtTo != nil {
		conditions = append(conditions, "`lodged_at` <= ?")
		arguments = append(arguments, *filter.LodgedAtTo)
	}
	if filter.LodgedAtIsNull != nil {
		if *filter.LodgedAtIsNull {
			conditions = append(conditions, "`lodged_at` IS NULL")
		} else {
			conditions = append(conditions, "`lodged_at` IS NOT NULL")
		}
	}
	if filter.ReleasedAt != nil {
		conditions = append(conditions, "`released_at` = ?")
		arguments = append(arguments, *filter.ReleasedAt)
	}
	if filter.ReleasedAtFrom != nil {
		conditions = append(conditions, "`released_at` >= ?")
		arguments = append(arguments, *filter.ReleasedAtFrom)
	}
	if filter.ReleasedAtTo != nil {
		conditions = append(conditions, "`released_at` <= ?")
		arguments = append(arguments, *filter.ReleasedAtTo)
	}
	if filter.ReleasedAtIsNull != nil {
		if *filter.ReleasedAtIsNull {
			conditions = append(conditions, "`released_at` IS NULL")
		} else {
			conditions = append(conditions, "`released_at` IS NOT NULL")
		}
	}
	if filter.DutyAmount != nil {
		conditions = append(conditions, "`duty_amount` = ?")
		arguments = append(arguments, *filter.DutyAmount)
	}
	if filter.DutyAmountFrom != nil {
		conditions = append(conditions, "`duty_amount` >= ?")
		arguments = append(arguments, *filter.DutyAmountFrom)
	}
	if filter.DutyAmountTo != nil {
		conditions = append(conditions, "`duty_amount` <= ?")
		arguments = append(arguments, *filter.DutyAmountTo)
	}
	if filter.VatAmount != nil {
		conditions = append(conditions, "`vat_amount` = ?")
		arguments = append(arguments, *filter.VatAmount)
	}
	if filter.VatAmountFrom != nil {
		conditions = append(conditions, "`vat_amount` >= ?")
		arguments = append(arguments, *filter.VatAmountFrom)
	}
	if filter.VatAmountTo != nil {
		conditions = append(conditions, "`vat_amount` <= ?")
		arguments = append(arguments, *filter.VatAmountTo)
	}
	if filter.TotalPayable != nil {
		conditions = append(conditions, "`total_payable` = ?")
		arguments = append(arguments, *filter.TotalPayable)
	}
	if filter.TotalPayableFrom != nil {
		conditions = append(conditions, "`total_payable` >= ?")
		arguments = append(arguments, *filter.TotalPayableFrom)
	}
	if filter.TotalPayableTo != nil {
		conditions = append(conditions, "`total_payable` <= ?")
		arguments = append(arguments, *filter.TotalPayableTo)
	}
	if filter.TotalPayableIsNull != nil {
		if *filter.TotalPayableIsNull {
			conditions = append(conditions, "`total_payable` IS NULL")
		} else {
			conditions = append(conditions, "`total_payable` IS NOT NULL")
		}
	}
	if filter.OfficerRemarks != nil {
		conditions = append(conditions, "`officer_remarks` = ?")
		arguments = append(arguments, *filter.OfficerRemarks)
	}
	if filter.OfficerRemarksLike != nil {
		conditions = append(conditions, "`officer_remarks` LIKE ?")
		arguments = append(arguments, *filter.OfficerRemarksLike)
	}
	if filter.OfficerRemarksIsNull != nil {
		if *filter.OfficerRemarksIsNull {
			conditions = append(conditions, "`officer_remarks` IS NULL")
		} else {
			conditions = append(conditions, "`officer_remarks` IS NOT NULL")
		}
	}

	var query strings.Builder
	query.WriteString(customsDeclarationSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := customsDeclarationOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("freight.customs_declaration: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("freight.customs_declaration: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("freight.customs_declaration: search: %w", err)
	}
	defer rows.Close()

	var found []CustomsDeclaration
	for rows.Next() {
		var row CustomsDeclaration
		if err := rows.Scan(
			&row.DeclarationId,
			&row.ConsignmentId,
			&row.ClearancePortId,
			&row.Mrn,
			&row.ProcedureCode,
			&row.Regime,
			&row.Status,
			&row.LodgedAt,
			&row.ReleasedAt,
			&row.DutyAmount,
			&row.VatAmount,
			&row.TotalPayable,
			&row.OfficerRemarks,
		); err != nil {
			return nil, fmt.Errorf("freight.customs_declaration: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("freight.customs_declaration: search: %w", err)
	}
	return found, nil
}

// customsDeclarationOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func customsDeclarationOrderColumn(column CustomsDeclarationColumn) (string, error) {
	switch column {
	case CustomsDeclarationColumnDeclarationId:
		return "`declaration_id`", nil
	case CustomsDeclarationColumnConsignmentId:
		return "`consignment_id`", nil
	case CustomsDeclarationColumnClearancePortId:
		return "`clearance_port_id`", nil
	case CustomsDeclarationColumnMrn:
		return "`mrn`", nil
	case CustomsDeclarationColumnProcedureCode:
		return "`procedure_code`", nil
	case CustomsDeclarationColumnRegime:
		return "`regime`", nil
	case CustomsDeclarationColumnStatus:
		return "`status`", nil
	case CustomsDeclarationColumnLodgedAt:
		return "`lodged_at`", nil
	case CustomsDeclarationColumnReleasedAt:
		return "`released_at`", nil
	case CustomsDeclarationColumnDutyAmount:
		return "`duty_amount`", nil
	case CustomsDeclarationColumnVatAmount:
		return "`vat_amount`", nil
	case CustomsDeclarationColumnTotalPayable:
		return "`total_payable`", nil
	case CustomsDeclarationColumnOfficerRemarks:
		return "`officer_remarks`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
