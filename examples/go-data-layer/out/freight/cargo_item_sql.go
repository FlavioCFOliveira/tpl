// Code generated by tpl from freight.cargo_item. DO NOT EDIT.

package freight

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// cargoItemSQL is the database/sql implementation of CargoItemRepository.
type cargoItemSQL struct {
	db *sql.DB
}

// NewCargoItemRepository builds the data layer of freight.cargo_item over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewCargoItemRepository(db *sql.DB) CargoItemRepository {
	return &cargoItemSQL{db: db}
}

// cargoItemSelect lists every column of freight.cargo_item, in catalogue order.
const cargoItemSelect = "SELECT `cargo_item_id`, `consignment_id`, `container_id`, `line_number`, `hs_code`, `description`, `package_kind`, `package_count`, `gross_weight_kg`, `net_weight_kg`, `volume_cbm`, `chargeable_weight_kg`, `imdg_class`, `un_number`, `origin_country`, `unit_value`, `line_value` FROM `cargo_item`"

// cargoItemRead reads the one row a primary key names.
const cargoItemRead = "SELECT `cargo_item_id`, `consignment_id`, `container_id`, `line_number`, `hs_code`, `description`, `package_kind`, `package_count`, `gross_weight_kg`, `net_weight_kg`, `volume_cbm`, `chargeable_weight_kg`, `imdg_class`, `un_number`, `origin_country`, `unit_value`, `line_value` FROM `cargo_item` WHERE `cargo_item_id` = ?"

// cargoItemInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const cargoItemInsert = "INSERT INTO `cargo_item` (`consignment_id`, `container_id`, `line_number`, `hs_code`, `description`, `package_kind`, `package_count`, `gross_weight_kg`, `net_weight_kg`, `volume_cbm`, `imdg_class`, `un_number`, `origin_country`, `unit_value`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"

// cargoItemUpdate writes every column that is neither generated nor a key.
const cargoItemUpdate = "UPDATE `cargo_item` SET `consignment_id` = ?, `container_id` = ?, `line_number` = ?, `hs_code` = ?, `description` = ?, `package_kind` = ?, `package_count` = ?, `gross_weight_kg` = ?, `net_weight_kg` = ?, `volume_cbm` = ?, `imdg_class` = ?, `un_number` = ?, `origin_country` = ?, `unit_value` = ? WHERE `cargo_item_id` = ?"

// cargoItemDelete removes the one row a primary key names.
const cargoItemDelete = "DELETE FROM `cargo_item` WHERE `cargo_item_id` = ?"

// Create inserts row.
func (r *cargoItemSQL) Create(ctx context.Context, row *CargoItem) error {
	result, err := r.db.ExecContext(ctx, cargoItemInsert,
		row.ConsignmentId,
		row.ContainerId,
		row.LineNumber,
		row.HsCode,
		row.Description,
		row.PackageKind,
		row.PackageCount,
		row.GrossWeightKg,
		row.NetWeightKg,
		row.VolumeCbm,
		row.ImdgClass,
		row.UnNumber,
		row.OriginCountry,
		row.UnitValue,
	)
	if err != nil {
		return fmt.Errorf("freight.cargo_item: create: %w", err)
	}
	assigned, err := result.LastInsertId()
	if err != nil {
		return fmt.Errorf("freight.cargo_item: create: reading the key the server assigned: %w", err)
	}
	row.CargoItemId = uint64(assigned)
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *cargoItemSQL) Read(ctx context.Context, key CargoItemKey) (*CargoItem, error) {
	var row CargoItem
	err := r.db.QueryRowContext(ctx, cargoItemRead,
		key.CargoItemId,
	).Scan(
		&row.CargoItemId,
		&row.ConsignmentId,
		&row.ContainerId,
		&row.LineNumber,
		&row.HsCode,
		&row.Description,
		&row.PackageKind,
		&row.PackageCount,
		&row.GrossWeightKg,
		&row.NetWeightKg,
		&row.VolumeCbm,
		&row.ChargeableWeightKg,
		&row.ImdgClass,
		&row.UnNumber,
		&row.OriginCountry,
		&row.UnitValue,
		&row.LineValue,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("freight.cargo_item: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("freight.cargo_item: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *cargoItemSQL) Update(ctx context.Context, row *CargoItem) (int64, error) {
	result, err := r.db.ExecContext(ctx, cargoItemUpdate,
		row.ConsignmentId,
		row.ContainerId,
		row.LineNumber,
		row.HsCode,
		row.Description,
		row.PackageKind,
		row.PackageCount,
		row.GrossWeightKg,
		row.NetWeightKg,
		row.VolumeCbm,
		row.ImdgClass,
		row.UnNumber,
		row.OriginCountry,
		row.UnitValue,
		row.CargoItemId,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.cargo_item: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.cargo_item: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *cargoItemSQL) Delete(ctx context.Context, key CargoItemKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, cargoItemDelete,
		key.CargoItemId,
	)
	if err != nil {
		return 0, fmt.Errorf("freight.cargo_item: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("freight.cargo_item: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *cargoItemSQL) Search(ctx context.Context, filter CargoItemFilter, order []CargoItemOrder, limit, offset int) ([]CargoItem, error) {
	conditions := make([]string, 0, 50)
	arguments := make([]any, 0, 52)

	if filter.CargoItemId != nil {
		conditions = append(conditions, "`cargo_item_id` = ?")
		arguments = append(arguments, *filter.CargoItemId)
	}
	if filter.CargoItemIdFrom != nil {
		conditions = append(conditions, "`cargo_item_id` >= ?")
		arguments = append(arguments, *filter.CargoItemIdFrom)
	}
	if filter.CargoItemIdTo != nil {
		conditions = append(conditions, "`cargo_item_id` <= ?")
		arguments = append(arguments, *filter.CargoItemIdTo)
	}
	if filter.ConsignmentId != nil {
		conditions = append(conditions, "`consignment_id` = ?")
		arguments = append(arguments, *filter.ConsignmentId)
	}
	if filter.ConsignmentIdFrom != nil {
		conditions = append(conditions, "`consignment_id` >= ?")
		arguments = append(arguments, *filter.ConsignmentIdFrom)
	}
	if filter.ConsignmentIdTo != nil {
		conditions = append(conditions, "`consignment_id` <= ?")
		arguments = append(arguments, *filter.ConsignmentIdTo)
	}
	if filter.ContainerId != nil {
		conditions = append(conditions, "`container_id` = ?")
		arguments = append(arguments, *filter.ContainerId)
	}
	if filter.ContainerIdFrom != nil {
		conditions = append(conditions, "`container_id` >= ?")
		arguments = append(arguments, *filter.ContainerIdFrom)
	}
	if filter.ContainerIdTo != nil {
		conditions = append(conditions, "`container_id` <= ?")
		arguments = append(arguments, *filter.ContainerIdTo)
	}
	if filter.ContainerIdIsNull != nil {
		if *filter.ContainerIdIsNull {
			conditions = append(conditions, "`container_id` IS NULL")
		} else {
			conditions = append(conditions, "`container_id` IS NOT NULL")
		}
	}
	if filter.LineNumber != nil {
		conditions = append(conditions, "`line_number` = ?")
		arguments = append(arguments, *filter.LineNumber)
	}
	if filter.LineNumberFrom != nil {
		conditions = append(conditions, "`line_number` >= ?")
		arguments = append(arguments, *filter.LineNumberFrom)
	}
	if filter.LineNumberTo != nil {
		conditions = append(conditions, "`line_number` <= ?")
		arguments = append(arguments, *filter.LineNumberTo)
	}
	if filter.HsCode != nil {
		conditions = append(conditions, "`hs_code` = ?")
		arguments = append(arguments, *filter.HsCode)
	}
	if filter.HsCodeLike != nil {
		conditions = append(conditions, "`hs_code` LIKE ?")
		arguments = append(arguments, *filter.HsCodeLike)
	}
	if filter.Description != nil {
		conditions = append(conditions, "`description` = ?")
		arguments = append(arguments, *filter.Description)
	}
	if filter.DescriptionLike != nil {
		conditions = append(conditions, "`description` LIKE ?")
		arguments = append(arguments, *filter.DescriptionLike)
	}
	if filter.PackageKind != nil {
		conditions = append(conditions, "`package_kind` = ?")
		arguments = append(arguments, *filter.PackageKind)
	}
	if filter.PackageKindLike != nil {
		conditions = append(conditions, "`package_kind` LIKE ?")
		arguments = append(arguments, *filter.PackageKindLike)
	}
	if filter.PackageCount != nil {
		conditions = append(conditions, "`package_count` = ?")
		arguments = append(arguments, *filter.PackageCount)
	}
	if filter.PackageCountFrom != nil {
		conditions = append(conditions, "`package_count` >= ?")
		arguments = append(arguments, *filter.PackageCountFrom)
	}
	if filter.PackageCountTo != nil {
		conditions = append(conditions, "`package_count` <= ?")
		arguments = append(arguments, *filter.PackageCountTo)
	}
	if filter.GrossWeightKg != nil {
		conditions = append(conditions, "`gross_weight_kg` = ?")
		arguments = append(arguments, *filter.GrossWeightKg)
	}
	if filter.GrossWeightKgFrom != nil {
		conditions = append(conditions, "`gross_weight_kg` >= ?")
		arguments = append(arguments, *filter.GrossWeightKgFrom)
	}
	if filter.GrossWeightKgTo != nil {
		conditions = append(conditions, "`gross_weight_kg` <= ?")
		arguments = append(arguments, *filter.GrossWeightKgTo)
	}
	if filter.NetWeightKg != nil {
		conditions = append(conditions, "`net_weight_kg` = ?")
		arguments = append(arguments, *filter.NetWeightKg)
	}
	if filter.NetWeightKgFrom != nil {
		conditions = append(conditions, "`net_weight_kg` >= ?")
		arguments = append(arguments, *filter.NetWeightKgFrom)
	}
	if filter.NetWeightKgTo != nil {
		conditions = append(conditions, "`net_weight_kg` <= ?")
		arguments = append(arguments, *filter.NetWeightKgTo)
	}
	if filter.VolumeCbm != nil {
		conditions = append(conditions, "`volume_cbm` = ?")
		arguments = append(arguments, *filter.VolumeCbm)
	}
	if filter.VolumeCbmFrom != nil {
		conditions = append(conditions, "`volume_cbm` >= ?")
		arguments = append(arguments, *filter.VolumeCbmFrom)
	}
	if filter.VolumeCbmTo != nil {
		conditions = append(conditions, "`volume_cbm` <= ?")
		arguments = append(arguments, *filter.VolumeCbmTo)
	}
	if filter.ChargeableWeightKg != nil {
		conditions = append(conditions, "`chargeable_weight_kg` = ?")
		arguments = append(arguments, *filter.ChargeableWeightKg)
	}
	if filter.ChargeableWeightKgFrom != nil {
		conditions = append(conditions, "`chargeable_weight_kg` >= ?")
		arguments = append(arguments, *filter.ChargeableWeightKgFrom)
	}
	if filter.ChargeableWeightKgTo != nil {
		conditions = append(conditions, "`chargeable_weight_kg` <= ?")
		arguments = append(arguments, *filter.ChargeableWeightKgTo)
	}
	if filter.ChargeableWeightKgIsNull != nil {
		if *filter.ChargeableWeightKgIsNull {
			conditions = append(conditions, "`chargeable_weight_kg` IS NULL")
		} else {
			conditions = append(conditions, "`chargeable_weight_kg` IS NOT NULL")
		}
	}
	if filter.ImdgClass != nil {
		conditions = append(conditions, "`imdg_class` = ?")
		arguments = append(arguments, *filter.ImdgClass)
	}
	if filter.ImdgClassLike != nil {
		conditions = append(conditions, "`imdg_class` LIKE ?")
		arguments = append(arguments, *filter.ImdgClassLike)
	}
	if filter.UnNumber != nil {
		conditions = append(conditions, "`un_number` = ?")
		arguments = append(arguments, *filter.UnNumber)
	}
	if filter.UnNumberFrom != nil {
		conditions = append(conditions, "`un_number` >= ?")
		arguments = append(arguments, *filter.UnNumberFrom)
	}
	if filter.UnNumberTo != nil {
		conditions = append(conditions, "`un_number` <= ?")
		arguments = append(arguments, *filter.UnNumberTo)
	}
	if filter.UnNumberIsNull != nil {
		if *filter.UnNumberIsNull {
			conditions = append(conditions, "`un_number` IS NULL")
		} else {
			conditions = append(conditions, "`un_number` IS NOT NULL")
		}
	}
	if filter.OriginCountry != nil {
		conditions = append(conditions, "`origin_country` = ?")
		arguments = append(arguments, *filter.OriginCountry)
	}
	if filter.OriginCountryLike != nil {
		conditions = append(conditions, "`origin_country` LIKE ?")
		arguments = append(arguments, *filter.OriginCountryLike)
	}
	if filter.UnitValue != nil {
		conditions = append(conditions, "`unit_value` = ?")
		arguments = append(arguments, *filter.UnitValue)
	}
	if filter.UnitValueFrom != nil {
		conditions = append(conditions, "`unit_value` >= ?")
		arguments = append(arguments, *filter.UnitValueFrom)
	}
	if filter.UnitValueTo != nil {
		conditions = append(conditions, "`unit_value` <= ?")
		arguments = append(arguments, *filter.UnitValueTo)
	}
	if filter.LineValue != nil {
		conditions = append(conditions, "`line_value` = ?")
		arguments = append(arguments, *filter.LineValue)
	}
	if filter.LineValueFrom != nil {
		conditions = append(conditions, "`line_value` >= ?")
		arguments = append(arguments, *filter.LineValueFrom)
	}
	if filter.LineValueTo != nil {
		conditions = append(conditions, "`line_value` <= ?")
		arguments = append(arguments, *filter.LineValueTo)
	}
	if filter.LineValueIsNull != nil {
		if *filter.LineValueIsNull {
			conditions = append(conditions, "`line_value` IS NULL")
		} else {
			conditions = append(conditions, "`line_value` IS NOT NULL")
		}
	}

	var query strings.Builder
	query.WriteString(cargoItemSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := cargoItemOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("freight.cargo_item: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("freight.cargo_item: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("freight.cargo_item: search: %w", err)
	}
	defer rows.Close()

	var found []CargoItem
	for rows.Next() {
		var row CargoItem
		if err := rows.Scan(
			&row.CargoItemId,
			&row.ConsignmentId,
			&row.ContainerId,
			&row.LineNumber,
			&row.HsCode,
			&row.Description,
			&row.PackageKind,
			&row.PackageCount,
			&row.GrossWeightKg,
			&row.NetWeightKg,
			&row.VolumeCbm,
			&row.ChargeableWeightKg,
			&row.ImdgClass,
			&row.UnNumber,
			&row.OriginCountry,
			&row.UnitValue,
			&row.LineValue,
		); err != nil {
			return nil, fmt.Errorf("freight.cargo_item: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("freight.cargo_item: search: %w", err)
	}
	return found, nil
}

// cargoItemOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func cargoItemOrderColumn(column CargoItemColumn) (string, error) {
	switch column {
	case CargoItemColumnCargoItemId:
		return "`cargo_item_id`", nil
	case CargoItemColumnConsignmentId:
		return "`consignment_id`", nil
	case CargoItemColumnContainerId:
		return "`container_id`", nil
	case CargoItemColumnLineNumber:
		return "`line_number`", nil
	case CargoItemColumnHsCode:
		return "`hs_code`", nil
	case CargoItemColumnDescription:
		return "`description`", nil
	case CargoItemColumnPackageKind:
		return "`package_kind`", nil
	case CargoItemColumnPackageCount:
		return "`package_count`", nil
	case CargoItemColumnGrossWeightKg:
		return "`gross_weight_kg`", nil
	case CargoItemColumnNetWeightKg:
		return "`net_weight_kg`", nil
	case CargoItemColumnVolumeCbm:
		return "`volume_cbm`", nil
	case CargoItemColumnChargeableWeightKg:
		return "`chargeable_weight_kg`", nil
	case CargoItemColumnImdgClass:
		return "`imdg_class`", nil
	case CargoItemColumnUnNumber:
		return "`un_number`", nil
	case CargoItemColumnOriginCountry:
		return "`origin_country`", nil
	case CargoItemColumnUnitValue:
		return "`unit_value`", nil
	case CargoItemColumnLineValue:
		return "`line_value`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
