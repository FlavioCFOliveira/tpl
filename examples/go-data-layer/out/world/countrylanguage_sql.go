// Code generated by tpl from world.countrylanguage. DO NOT EDIT.

package world

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// countrylanguageSQL is the database/sql implementation of CountrylanguageRepository.
type countrylanguageSQL struct {
	db *sql.DB
}

// NewCountrylanguageRepository builds the data layer of world.countrylanguage over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewCountrylanguageRepository(db *sql.DB) CountrylanguageRepository {
	return &countrylanguageSQL{db: db}
}

// countrylanguageSelect lists every column of world.countrylanguage, in catalogue order.
const countrylanguageSelect = "SELECT `CountryCode`, `Language`, `IsOfficial`, `Percentage` FROM `countrylanguage`"

// countrylanguageRead reads the one row a primary key names.
const countrylanguageRead = "SELECT `CountryCode`, `Language`, `IsOfficial`, `Percentage` FROM `countrylanguage` WHERE `CountryCode` = ? AND `Language` = ?"

// countrylanguageInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const countrylanguageInsert = "INSERT INTO `countrylanguage` (`CountryCode`, `Language`, `IsOfficial`, `Percentage`) VALUES (?, ?, ?, ?)"

// countrylanguageUpdate writes every column that is neither generated nor a key.
const countrylanguageUpdate = "UPDATE `countrylanguage` SET `IsOfficial` = ?, `Percentage` = ? WHERE `CountryCode` = ? AND `Language` = ?"

// countrylanguageDelete removes the one row a primary key names.
const countrylanguageDelete = "DELETE FROM `countrylanguage` WHERE `CountryCode` = ? AND `Language` = ?"

// Create inserts row.
func (r *countrylanguageSQL) Create(ctx context.Context, row *Countrylanguage) error {
	_, err := r.db.ExecContext(ctx, countrylanguageInsert,
		row.CountryCode,
		row.Language,
		row.IsOfficial,
		row.Percentage,
	)
	if err != nil {
		return fmt.Errorf("world.countrylanguage: create: %w", err)
	}
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *countrylanguageSQL) Read(ctx context.Context, key CountrylanguageKey) (*Countrylanguage, error) {
	var row Countrylanguage
	err := r.db.QueryRowContext(ctx, countrylanguageRead,
		key.CountryCode,
		key.Language,
	).Scan(
		&row.CountryCode,
		&row.Language,
		&row.IsOfficial,
		&row.Percentage,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("world.countrylanguage: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("world.countrylanguage: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *countrylanguageSQL) Update(ctx context.Context, row *Countrylanguage) (int64, error) {
	result, err := r.db.ExecContext(ctx, countrylanguageUpdate,
		row.IsOfficial,
		row.Percentage,
		row.CountryCode,
		row.Language,
	)
	if err != nil {
		return 0, fmt.Errorf("world.countrylanguage: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("world.countrylanguage: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *countrylanguageSQL) Delete(ctx context.Context, key CountrylanguageKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, countrylanguageDelete,
		key.CountryCode,
		key.Language,
	)
	if err != nil {
		return 0, fmt.Errorf("world.countrylanguage: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("world.countrylanguage: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *countrylanguageSQL) Search(ctx context.Context, filter CountrylanguageFilter, order []CountrylanguageOrder, limit, offset int) ([]Countrylanguage, error) {
	conditions := make([]string, 0, 9)
	arguments := make([]any, 0, 11)

	if filter.CountryCode != nil {
		conditions = append(conditions, "`CountryCode` = ?")
		arguments = append(arguments, *filter.CountryCode)
	}
	if filter.CountryCodeLike != nil {
		conditions = append(conditions, "`CountryCode` LIKE ?")
		arguments = append(arguments, *filter.CountryCodeLike)
	}
	if filter.Language != nil {
		conditions = append(conditions, "`Language` = ?")
		arguments = append(arguments, *filter.Language)
	}
	if filter.LanguageLike != nil {
		conditions = append(conditions, "`Language` LIKE ?")
		arguments = append(arguments, *filter.LanguageLike)
	}
	if filter.IsOfficial != nil {
		conditions = append(conditions, "`IsOfficial` = ?")
		arguments = append(arguments, *filter.IsOfficial)
	}
	if filter.IsOfficialLike != nil {
		conditions = append(conditions, "`IsOfficial` LIKE ?")
		arguments = append(arguments, *filter.IsOfficialLike)
	}
	if filter.Percentage != nil {
		conditions = append(conditions, "`Percentage` = ?")
		arguments = append(arguments, *filter.Percentage)
	}
	if filter.PercentageFrom != nil {
		conditions = append(conditions, "`Percentage` >= ?")
		arguments = append(arguments, *filter.PercentageFrom)
	}
	if filter.PercentageTo != nil {
		conditions = append(conditions, "`Percentage` <= ?")
		arguments = append(arguments, *filter.PercentageTo)
	}

	var query strings.Builder
	query.WriteString(countrylanguageSelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := countrylanguageOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("world.countrylanguage: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("world.countrylanguage: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("world.countrylanguage: search: %w", err)
	}
	defer rows.Close()

	var found []Countrylanguage
	for rows.Next() {
		var row Countrylanguage
		if err := rows.Scan(
			&row.CountryCode,
			&row.Language,
			&row.IsOfficial,
			&row.Percentage,
		); err != nil {
			return nil, fmt.Errorf("world.countrylanguage: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("world.countrylanguage: search: %w", err)
	}
	return found, nil
}

// countrylanguageOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func countrylanguageOrderColumn(column CountrylanguageColumn) (string, error) {
	switch column {
	case CountrylanguageColumnCountryCode:
		return "`CountryCode`", nil
	case CountrylanguageColumnLanguage:
		return "`Language`", nil
	case CountrylanguageColumnIsOfficial:
		return "`IsOfficial`", nil
	case CountrylanguageColumnPercentage:
		return "`Percentage`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
