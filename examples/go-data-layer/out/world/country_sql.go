// Code generated by tpl from world.country. DO NOT EDIT.

package world

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"strings"
)

// countrySQL is the database/sql implementation of CountryRepository.
type countrySQL struct {
	db *sql.DB
}

// NewCountryRepository builds the data layer of world.country over db.
//
// The pool db must have been opened with parseTime=true where the table
// declares a DATE, DATETIME or TIMESTAMP column, because those columns are
// scanned into time.Time. Open, in schema.go, is the pool that carries it.
func NewCountryRepository(db *sql.DB) CountryRepository {
	return &countrySQL{db: db}
}

// countrySelect lists every column of world.country, in catalogue order.
const countrySelect = "SELECT `Code`, `Name`, `Continent`, `Region`, `SurfaceArea`, `IndepYear`, `Population`, `LifeExpectancy`, `GNP`, `GNPOld`, `LocalName`, `GovernmentForm`, `HeadOfState`, `Capital`, `Code2` FROM `country`"

// countryRead reads the one row a primary key names.
const countryRead = "SELECT `Code`, `Name`, `Continent`, `Region`, `SurfaceArea`, `IndepYear`, `Population`, `LifeExpectancy`, `GNP`, `GNPOld`, `LocalName`, `GovernmentForm`, `HeadOfState`, `Capital`, `Code2` FROM `country` WHERE `Code` = ?"

// countryInsert inserts one row.
//
// It names neither a generated column nor an auto-increment key, because the
// server writes both.
const countryInsert = "INSERT INTO `country` (`Code`, `Name`, `Continent`, `Region`, `SurfaceArea`, `IndepYear`, `Population`, `LifeExpectancy`, `GNP`, `GNPOld`, `LocalName`, `GovernmentForm`, `HeadOfState`, `Capital`, `Code2`) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"

// countryUpdate writes every column that is neither generated nor a key.
const countryUpdate = "UPDATE `country` SET `Name` = ?, `Continent` = ?, `Region` = ?, `SurfaceArea` = ?, `IndepYear` = ?, `Population` = ?, `LifeExpectancy` = ?, `GNP` = ?, `GNPOld` = ?, `LocalName` = ?, `GovernmentForm` = ?, `HeadOfState` = ?, `Capital` = ?, `Code2` = ? WHERE `Code` = ?"

// countryDelete removes the one row a primary key names.
const countryDelete = "DELETE FROM `country` WHERE `Code` = ?"

// Create inserts row.
func (r *countrySQL) Create(ctx context.Context, row *Country) error {
	_, err := r.db.ExecContext(ctx, countryInsert,
		row.Code,
		row.Name,
		row.Continent,
		row.Region,
		row.SurfaceArea,
		row.IndepYear,
		row.Population,
		row.LifeExpectancy,
		row.Gnp,
		row.GnpOld,
		row.LocalName,
		row.GovernmentForm,
		row.HeadOfState,
		row.Capital,
		row.Code2,
	)
	if err != nil {
		return fmt.Errorf("world.country: create: %w", err)
	}
	return nil
}

// Read returns the row carrying key, or an error wrapping ErrNotFound.
func (r *countrySQL) Read(ctx context.Context, key CountryKey) (*Country, error) {
	var row Country
	err := r.db.QueryRowContext(ctx, countryRead,
		key.Code,
	).Scan(
		&row.Code,
		&row.Name,
		&row.Continent,
		&row.Region,
		&row.SurfaceArea,
		&row.IndepYear,
		&row.Population,
		&row.LifeExpectancy,
		&row.Gnp,
		&row.GnpOld,
		&row.LocalName,
		&row.GovernmentForm,
		&row.HeadOfState,
		&row.Capital,
		&row.Code2,
	)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, fmt.Errorf("world.country: read %+v: %w", key, ErrNotFound)
	}
	if err != nil {
		return nil, fmt.Errorf("world.country: read %+v: %w", key, err)
	}
	return &row, nil
}

// Update writes row over the row its key names.
func (r *countrySQL) Update(ctx context.Context, row *Country) (int64, error) {
	result, err := r.db.ExecContext(ctx, countryUpdate,
		row.Name,
		row.Continent,
		row.Region,
		row.SurfaceArea,
		row.IndepYear,
		row.Population,
		row.LifeExpectancy,
		row.Gnp,
		row.GnpOld,
		row.LocalName,
		row.GovernmentForm,
		row.HeadOfState,
		row.Capital,
		row.Code2,
		row.Code,
	)
	if err != nil {
		return 0, fmt.Errorf("world.country: update: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("world.country: update: %w", err)
	}
	return affected, nil
}

// Delete removes the row carrying key.
func (r *countrySQL) Delete(ctx context.Context, key CountryKey) (int64, error) {
	result, err := r.db.ExecContext(ctx, countryDelete,
		key.Code,
	)
	if err != nil {
		return 0, fmt.Errorf("world.country: delete: %w", err)
	}
	affected, err := result.RowsAffected()
	if err != nil {
		return 0, fmt.Errorf("world.country: delete: %w", err)
	}
	return affected, nil
}

// Search returns every row matching filter, ordered by the terms of order.
func (r *countrySQL) Search(ctx context.Context, filter CountryFilter, order []CountryOrder, limit, offset int) ([]Country, error) {
	conditions := make([]string, 0, 43)
	arguments := make([]any, 0, 45)

	if filter.Code != nil {
		conditions = append(conditions, "`Code` = ?")
		arguments = append(arguments, *filter.Code)
	}
	if filter.CodeLike != nil {
		conditions = append(conditions, "`Code` LIKE ?")
		arguments = append(arguments, *filter.CodeLike)
	}
	if filter.Name != nil {
		conditions = append(conditions, "`Name` = ?")
		arguments = append(arguments, *filter.Name)
	}
	if filter.NameLike != nil {
		conditions = append(conditions, "`Name` LIKE ?")
		arguments = append(arguments, *filter.NameLike)
	}
	if filter.Continent != nil {
		conditions = append(conditions, "`Continent` = ?")
		arguments = append(arguments, *filter.Continent)
	}
	if filter.ContinentLike != nil {
		conditions = append(conditions, "`Continent` LIKE ?")
		arguments = append(arguments, *filter.ContinentLike)
	}
	if filter.Region != nil {
		conditions = append(conditions, "`Region` = ?")
		arguments = append(arguments, *filter.Region)
	}
	if filter.RegionLike != nil {
		conditions = append(conditions, "`Region` LIKE ?")
		arguments = append(arguments, *filter.RegionLike)
	}
	if filter.SurfaceArea != nil {
		conditions = append(conditions, "`SurfaceArea` = ?")
		arguments = append(arguments, *filter.SurfaceArea)
	}
	if filter.SurfaceAreaFrom != nil {
		conditions = append(conditions, "`SurfaceArea` >= ?")
		arguments = append(arguments, *filter.SurfaceAreaFrom)
	}
	if filter.SurfaceAreaTo != nil {
		conditions = append(conditions, "`SurfaceArea` <= ?")
		arguments = append(arguments, *filter.SurfaceAreaTo)
	}
	if filter.IndepYear != nil {
		conditions = append(conditions, "`IndepYear` = ?")
		arguments = append(arguments, *filter.IndepYear)
	}
	if filter.IndepYearFrom != nil {
		conditions = append(conditions, "`IndepYear` >= ?")
		arguments = append(arguments, *filter.IndepYearFrom)
	}
	if filter.IndepYearTo != nil {
		conditions = append(conditions, "`IndepYear` <= ?")
		arguments = append(arguments, *filter.IndepYearTo)
	}
	if filter.IndepYearIsNull != nil {
		if *filter.IndepYearIsNull {
			conditions = append(conditions, "`IndepYear` IS NULL")
		} else {
			conditions = append(conditions, "`IndepYear` IS NOT NULL")
		}
	}
	if filter.Population != nil {
		conditions = append(conditions, "`Population` = ?")
		arguments = append(arguments, *filter.Population)
	}
	if filter.PopulationFrom != nil {
		conditions = append(conditions, "`Population` >= ?")
		arguments = append(arguments, *filter.PopulationFrom)
	}
	if filter.PopulationTo != nil {
		conditions = append(conditions, "`Population` <= ?")
		arguments = append(arguments, *filter.PopulationTo)
	}
	if filter.LifeExpectancy != nil {
		conditions = append(conditions, "`LifeExpectancy` = ?")
		arguments = append(arguments, *filter.LifeExpectancy)
	}
	if filter.LifeExpectancyFrom != nil {
		conditions = append(conditions, "`LifeExpectancy` >= ?")
		arguments = append(arguments, *filter.LifeExpectancyFrom)
	}
	if filter.LifeExpectancyTo != nil {
		conditions = append(conditions, "`LifeExpectancy` <= ?")
		arguments = append(arguments, *filter.LifeExpectancyTo)
	}
	if filter.LifeExpectancyIsNull != nil {
		if *filter.LifeExpectancyIsNull {
			conditions = append(conditions, "`LifeExpectancy` IS NULL")
		} else {
			conditions = append(conditions, "`LifeExpectancy` IS NOT NULL")
		}
	}
	if filter.Gnp != nil {
		conditions = append(conditions, "`GNP` = ?")
		arguments = append(arguments, *filter.Gnp)
	}
	if filter.GnpFrom != nil {
		conditions = append(conditions, "`GNP` >= ?")
		arguments = append(arguments, *filter.GnpFrom)
	}
	if filter.GnpTo != nil {
		conditions = append(conditions, "`GNP` <= ?")
		arguments = append(arguments, *filter.GnpTo)
	}
	if filter.GnpIsNull != nil {
		if *filter.GnpIsNull {
			conditions = append(conditions, "`GNP` IS NULL")
		} else {
			conditions = append(conditions, "`GNP` IS NOT NULL")
		}
	}
	if filter.GnpOld != nil {
		conditions = append(conditions, "`GNPOld` = ?")
		arguments = append(arguments, *filter.GnpOld)
	}
	if filter.GnpOldFrom != nil {
		conditions = append(conditions, "`GNPOld` >= ?")
		arguments = append(arguments, *filter.GnpOldFrom)
	}
	if filter.GnpOldTo != nil {
		conditions = append(conditions, "`GNPOld` <= ?")
		arguments = append(arguments, *filter.GnpOldTo)
	}
	if filter.GnpOldIsNull != nil {
		if *filter.GnpOldIsNull {
			conditions = append(conditions, "`GNPOld` IS NULL")
		} else {
			conditions = append(conditions, "`GNPOld` IS NOT NULL")
		}
	}
	if filter.LocalName != nil {
		conditions = append(conditions, "`LocalName` = ?")
		arguments = append(arguments, *filter.LocalName)
	}
	if filter.LocalNameLike != nil {
		conditions = append(conditions, "`LocalName` LIKE ?")
		arguments = append(arguments, *filter.LocalNameLike)
	}
	if filter.GovernmentForm != nil {
		conditions = append(conditions, "`GovernmentForm` = ?")
		arguments = append(arguments, *filter.GovernmentForm)
	}
	if filter.GovernmentFormLike != nil {
		conditions = append(conditions, "`GovernmentForm` LIKE ?")
		arguments = append(arguments, *filter.GovernmentFormLike)
	}
	if filter.HeadOfState != nil {
		conditions = append(conditions, "`HeadOfState` = ?")
		arguments = append(arguments, *filter.HeadOfState)
	}
	if filter.HeadOfStateLike != nil {
		conditions = append(conditions, "`HeadOfState` LIKE ?")
		arguments = append(arguments, *filter.HeadOfStateLike)
	}
	if filter.HeadOfStateIsNull != nil {
		if *filter.HeadOfStateIsNull {
			conditions = append(conditions, "`HeadOfState` IS NULL")
		} else {
			conditions = append(conditions, "`HeadOfState` IS NOT NULL")
		}
	}
	if filter.Capital != nil {
		conditions = append(conditions, "`Capital` = ?")
		arguments = append(arguments, *filter.Capital)
	}
	if filter.CapitalFrom != nil {
		conditions = append(conditions, "`Capital` >= ?")
		arguments = append(arguments, *filter.CapitalFrom)
	}
	if filter.CapitalTo != nil {
		conditions = append(conditions, "`Capital` <= ?")
		arguments = append(arguments, *filter.CapitalTo)
	}
	if filter.CapitalIsNull != nil {
		if *filter.CapitalIsNull {
			conditions = append(conditions, "`Capital` IS NULL")
		} else {
			conditions = append(conditions, "`Capital` IS NOT NULL")
		}
	}
	if filter.Code2 != nil {
		conditions = append(conditions, "`Code2` = ?")
		arguments = append(arguments, *filter.Code2)
	}
	if filter.Code2Like != nil {
		conditions = append(conditions, "`Code2` LIKE ?")
		arguments = append(arguments, *filter.Code2Like)
	}

	var query strings.Builder
	query.WriteString(countrySelect)
	if len(conditions) > 0 {
		query.WriteString(" WHERE ")
		query.WriteString(strings.Join(conditions, " AND "))
	}
	if len(order) > 0 {
		terms := make([]string, 0, len(order))
		for _, term := range order {
			name, err := countryOrderColumn(term.Column)
			if err != nil {
				return nil, fmt.Errorf("world.country: search: %w", err)
			}
			direction, err := term.Direction.keyword()
			if err != nil {
				return nil, fmt.Errorf("world.country: search: %w", err)
			}
			terms = append(terms, name+" "+direction)
		}
		query.WriteString(" ORDER BY ")
		query.WriteString(strings.Join(terms, ", "))
	}
	if limit > 0 {
		query.WriteString(" LIMIT ?")
		arguments = append(arguments, limit)
		if offset > 0 {
			query.WriteString(" OFFSET ?")
			arguments = append(arguments, offset)
		}
	}

	rows, err := r.db.QueryContext(ctx, query.String(), arguments...)
	if err != nil {
		return nil, fmt.Errorf("world.country: search: %w", err)
	}
	defer rows.Close()

	var found []Country
	for rows.Next() {
		var row Country
		if err := rows.Scan(
			&row.Code,
			&row.Name,
			&row.Continent,
			&row.Region,
			&row.SurfaceArea,
			&row.IndepYear,
			&row.Population,
			&row.LifeExpectancy,
			&row.Gnp,
			&row.GnpOld,
			&row.LocalName,
			&row.GovernmentForm,
			&row.HeadOfState,
			&row.Capital,
			&row.Code2,
		); err != nil {
			return nil, fmt.Errorf("world.country: search: %w", err)
		}
		found = append(found, row)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("world.country: search: %w", err)
	}
	return found, nil
}

// countryOrderColumn quotes one column for an ORDER BY term.
//
// A name the table does not carry is refused rather than interpolated, which is
// what keeps an ordering from carrying SQL of its own.
func countryOrderColumn(column CountryColumn) (string, error) {
	switch column {
	case CountryColumnCode:
		return "`Code`", nil
	case CountryColumnName:
		return "`Name`", nil
	case CountryColumnContinent:
		return "`Continent`", nil
	case CountryColumnRegion:
		return "`Region`", nil
	case CountryColumnSurfaceArea:
		return "`SurfaceArea`", nil
	case CountryColumnIndepYear:
		return "`IndepYear`", nil
	case CountryColumnPopulation:
		return "`Population`", nil
	case CountryColumnLifeExpectancy:
		return "`LifeExpectancy`", nil
	case CountryColumnGnp:
		return "`GNP`", nil
	case CountryColumnGnpOld:
		return "`GNPOld`", nil
	case CountryColumnLocalName:
		return "`LocalName`", nil
	case CountryColumnGovernmentForm:
		return "`GovernmentForm`", nil
	case CountryColumnHeadOfState:
		return "`HeadOfState`", nil
	case CountryColumnCapital:
		return "`Capital`", nil
	case CountryColumnCode2:
		return "`Code2`", nil
	}
	return "", fmt.Errorf("%q: %w", string(column), ErrUnknownColumn)
}
