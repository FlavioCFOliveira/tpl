# Code generated by tpl from world.country. DO NOT EDIT.

"""The PyMySQL implementation of the `world.country` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .country import Country, CountryFilter, CountryKey, CountryOrder
from .country_repository import CountryRepository

__all__ = ["CountryPyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "world.country"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `Code`, `Name`, `Continent`, `Region`, `SurfaceArea`, `IndepYear`, `Population`, `LifeExpectancy`, `GNP`, `GNPOld`, `LocalName`, `GovernmentForm`, `HeadOfState`, `Capital`, `Code2` FROM `country`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `Code`, `Name`, `Continent`, `Region`, `SurfaceArea`, `IndepYear`, `Population`, `LifeExpectancy`, `GNP`, `GNPOld`, `LocalName`, `GovernmentForm`, `HeadOfState`, `Capital`, `Code2` FROM `country` WHERE `Code` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `country` (`Code`, `Name`, `Continent`, `Region`, `SurfaceArea`, `IndepYear`, `Population`, `LifeExpectancy`, `GNP`, `GNPOld`, `LocalName`, `GovernmentForm`, `HeadOfState`, `Capital`, `Code2`) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `country` SET `Name` = %s, `Continent` = %s, `Region` = %s, `SurfaceArea` = %s, `IndepYear` = %s, `Population` = %s, `LifeExpectancy` = %s, `GNP` = %s, `GNPOld` = %s, `LocalName` = %s, `GovernmentForm` = %s, `HeadOfState` = %s, `Capital` = %s, `Code2` = %s WHERE `Code` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `country` WHERE `Code` = %s"


def _row(values: tuple[Any, ...]) -> Country:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return Country(
        code=values[0],
        name=values[1],
        continent=values[2],
        region=values[3],
        surface_area=values[4],
        indep_year=values[5],
        population=values[6],
        life_expectancy=values[7],
        gnp=values[8],
        gnp_old=values[9],
        local_name=values[10],
        government_form=values[11],
        head_of_state=values[12],
        capital=values[13],
        code2=values[14],
    )


class CountryPyMySQL:
    """The PyMySQL data layer of `world.country`.

    It names no base class. ``CountryRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `world.country` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: Country) -> None:
        """Insert `row`.

        The whole key is taken from `row`, which the caller therefore fills.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.code,
                    row.name,
                    row.continent,
                    row.region,
                    row.surface_area,
                    row.indep_year,
                    row.population,
                    row.life_expectancy,
                    row.gnp,
                    row.gnp_old,
                    row.local_name,
                    row.government_form,
                    row.head_of_state,
                    row.capital,
                    row.code2,
                ),
            )

    def read(self, key: CountryKey) -> Country:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.code,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: Country) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.name,
                    row.continent,
                    row.region,
                    row.surface_area,
                    row.indep_year,
                    row.population,
                    row.life_expectancy,
                    row.gnp,
                    row.gnp_old,
                    row.local_name,
                    row.government_form,
                    row.head_of_state,
                    row.capital,
                    row.code2,
                    row.code,
                ),
            )
        return affected

    def delete(self, key: CountryKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.code,
                ),
            )
        return affected

    def search(
        self,
        criteria: CountryFilter,
        order: Sequence[CountryOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[Country]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.code is not None:
            conditions.append("`Code` = %s")
            arguments.append(criteria.code)
        if criteria.code_like is not None:
            conditions.append("`Code` LIKE %s")
            arguments.append(criteria.code_like)
        if criteria.name is not None:
            conditions.append("`Name` = %s")
            arguments.append(criteria.name)
        if criteria.name_like is not None:
            conditions.append("`Name` LIKE %s")
            arguments.append(criteria.name_like)
        if criteria.continent is not None:
            conditions.append("`Continent` = %s")
            arguments.append(criteria.continent)
        if criteria.continent_like is not None:
            conditions.append("`Continent` LIKE %s")
            arguments.append(criteria.continent_like)
        if criteria.region is not None:
            conditions.append("`Region` = %s")
            arguments.append(criteria.region)
        if criteria.region_like is not None:
            conditions.append("`Region` LIKE %s")
            arguments.append(criteria.region_like)
        if criteria.surface_area is not None:
            conditions.append("`SurfaceArea` = %s")
            arguments.append(criteria.surface_area)
        if criteria.surface_area_from is not None:
            conditions.append("`SurfaceArea` >= %s")
            arguments.append(criteria.surface_area_from)
        if criteria.surface_area_to is not None:
            conditions.append("`SurfaceArea` <= %s")
            arguments.append(criteria.surface_area_to)
        if criteria.indep_year is not None:
            conditions.append("`IndepYear` = %s")
            arguments.append(criteria.indep_year)
        if criteria.indep_year_from is not None:
            conditions.append("`IndepYear` >= %s")
            arguments.append(criteria.indep_year_from)
        if criteria.indep_year_to is not None:
            conditions.append("`IndepYear` <= %s")
            arguments.append(criteria.indep_year_to)
        if criteria.indep_year_is_null is not None:
            conditions.append(
                "`IndepYear` IS NULL"
                if criteria.indep_year_is_null
                else "`IndepYear` IS NOT NULL"
            )
        if criteria.population is not None:
            conditions.append("`Population` = %s")
            arguments.append(criteria.population)
        if criteria.population_from is not None:
            conditions.append("`Population` >= %s")
            arguments.append(criteria.population_from)
        if criteria.population_to is not None:
            conditions.append("`Population` <= %s")
            arguments.append(criteria.population_to)
        if criteria.life_expectancy is not None:
            conditions.append("`LifeExpectancy` = %s")
            arguments.append(criteria.life_expectancy)
        if criteria.life_expectancy_from is not None:
            conditions.append("`LifeExpectancy` >= %s")
            arguments.append(criteria.life_expectancy_from)
        if criteria.life_expectancy_to is not None:
            conditions.append("`LifeExpectancy` <= %s")
            arguments.append(criteria.life_expectancy_to)
        if criteria.life_expectancy_is_null is not None:
            conditions.append(
                "`LifeExpectancy` IS NULL"
                if criteria.life_expectancy_is_null
                else "`LifeExpectancy` IS NOT NULL"
            )
        if criteria.gnp is not None:
            conditions.append("`GNP` = %s")
            arguments.append(criteria.gnp)
        if criteria.gnp_from is not None:
            conditions.append("`GNP` >= %s")
            arguments.append(criteria.gnp_from)
        if criteria.gnp_to is not None:
            conditions.append("`GNP` <= %s")
            arguments.append(criteria.gnp_to)
        if criteria.gnp_is_null is not None:
            conditions.append(
                "`GNP` IS NULL"
                if criteria.gnp_is_null
                else "`GNP` IS NOT NULL"
            )
        if criteria.gnp_old is not None:
            conditions.append("`GNPOld` = %s")
            arguments.append(criteria.gnp_old)
        if criteria.gnp_old_from is not None:
            conditions.append("`GNPOld` >= %s")
            arguments.append(criteria.gnp_old_from)
        if criteria.gnp_old_to is not None:
            conditions.append("`GNPOld` <= %s")
            arguments.append(criteria.gnp_old_to)
        if criteria.gnp_old_is_null is not None:
            conditions.append(
                "`GNPOld` IS NULL"
                if criteria.gnp_old_is_null
                else "`GNPOld` IS NOT NULL"
            )
        if criteria.local_name is not None:
            conditions.append("`LocalName` = %s")
            arguments.append(criteria.local_name)
        if criteria.local_name_like is not None:
            conditions.append("`LocalName` LIKE %s")
            arguments.append(criteria.local_name_like)
        if criteria.government_form is not None:
            conditions.append("`GovernmentForm` = %s")
            arguments.append(criteria.government_form)
        if criteria.government_form_like is not None:
            conditions.append("`GovernmentForm` LIKE %s")
            arguments.append(criteria.government_form_like)
        if criteria.head_of_state is not None:
            conditions.append("`HeadOfState` = %s")
            arguments.append(criteria.head_of_state)
        if criteria.head_of_state_like is not None:
            conditions.append("`HeadOfState` LIKE %s")
            arguments.append(criteria.head_of_state_like)
        if criteria.head_of_state_is_null is not None:
            conditions.append(
                "`HeadOfState` IS NULL"
                if criteria.head_of_state_is_null
                else "`HeadOfState` IS NOT NULL"
            )
        if criteria.capital is not None:
            conditions.append("`Capital` = %s")
            arguments.append(criteria.capital)
        if criteria.capital_from is not None:
            conditions.append("`Capital` >= %s")
            arguments.append(criteria.capital_from)
        if criteria.capital_to is not None:
            conditions.append("`Capital` <= %s")
            arguments.append(criteria.capital_to)
        if criteria.capital_is_null is not None:
            conditions.append(
                "`Capital` IS NULL"
                if criteria.capital_is_null
                else "`Capital` IS NOT NULL"
            )
        if criteria.code2 is not None:
            conditions.append("`Code2` = %s")
            arguments.append(criteria.code2)
        if criteria.code2_like is not None:
            conditions.append("`Code2` LIKE %s")
            arguments.append(criteria.code2_like)

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: CountryPyMySQL,
    ) -> CountryRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
