# Code generated by tpl from world.countrylanguage. DO NOT EDIT.

"""The `world.countrylanguage` row, the key that addresses it, the columns an ordering may
name, and the filter a search takes.
"""

from __future__ import annotations

import dataclasses
import decimal
import enum
from collections.abc import Mapping
from typing import Final

from ._core import Direction, UnknownColumn

__all__ = [
    "Countrylanguage",
    "CountrylanguageColumn",
    "CountrylanguageFilter",
    "CountrylanguageKey",
    "CountrylanguageOrder",
]


@dataclasses.dataclass
class Countrylanguage:
    """One row of `world.countrylanguage`.

    The dataclass is mutable, because :meth:`create` writes a server-assigned
    key back into it.
    """

    #: `CountryCode`, declared `char(3)` NOT NULL.
    #:
    #: It is part of the primary key.
    country_code: str
    #: `Language`, declared `char(30)` NOT NULL.
    #:
    #: It is part of the primary key.
    language: str
    #: `IsOfficial`, declared `enum('T','F')` NOT NULL.
    is_official: str
    #: `Percentage`, declared `decimal(4,1)` NOT NULL.
    percentage: decimal.Decimal

    def key(self) -> CountrylanguageKey:
        """Return the primary key of the row."""
        return CountrylanguageKey(
            country_code=self.country_code,
            language=self.language,
        )


@dataclasses.dataclass(frozen=True)
class CountrylanguageKey:
    """The primary key of `world.countrylanguage`.

    Frozen, so that it hashes and compares by value. Every type this example's
    mapping produces is hashable, so no key of these three schemas is refused
    for that reason.
    """

    #: `CountryCode`, declared `char(3)` NOT NULL.
    country_code: str
    #: `Language`, declared `char(30)` NOT NULL.
    language: str


class CountrylanguageColumn(enum.Enum):
    """One column of `world.countrylanguage`.

    A search accepts an ordering term over these members and over nothing else,
    so an ordering can carry no SQL of its own: :attr:`sql` answers from a
    closed mapping this template wrote, and :meth:`from_name` is the one door a
    name a caller supplied can come through.

    A member's value is the column's name as the catalogue carries it, which is
    what :meth:`from_name` matches on and what ``member.value`` returns.
    """

    #: `CountryCode`, declared `char(3)` NOT NULL.
    COUNTRY_CODE = "CountryCode"
    #: `Language`, declared `char(30)` NOT NULL.
    LANGUAGE = "Language"
    #: `IsOfficial`, declared `enum('T','F')` NOT NULL.
    IS_OFFICIAL = "IsOfficial"
    #: `Percentage`, declared `decimal(4,1)` NOT NULL.
    PERCENTAGE = "Percentage"

    @property
    def sql(self) -> str:
        """The column as a statement's text names it: quoted for MariaDB."""
        return _COLUMNS[self]

    @classmethod
    def from_name(cls, name: str) -> CountrylanguageColumn:
        """Read a column a caller supplied as text.

        Raises:
            UnknownColumn: for every name the table does not carry. That
                refusal is what keeps a caller's text out of a statement.
        """
        try:
            return cls(name)
        except ValueError:
            raise UnknownColumn("world.countrylanguage", name) from None


#: The one place a column becomes SQL. It is closed over the enumeration above,
#: and every identifier in it was quoted at render time by `tpl`, so no value a
#: caller supplies can reach a statement's text through an ordering term.
_COLUMNS: Final[Mapping[CountrylanguageColumn, str]] = {
    CountrylanguageColumn.COUNTRY_CODE: "`CountryCode`",
    CountrylanguageColumn.LANGUAGE: "`Language`",
    CountrylanguageColumn.IS_OFFICIAL: "`IsOfficial`",
    CountrylanguageColumn.PERCENTAGE: "`Percentage`",
}


@dataclasses.dataclass(frozen=True)
class CountrylanguageOrder:
    """One ordering term of a search over `world.countrylanguage`."""

    #: The column to order by.
    column: CountrylanguageColumn
    #: The direction to order in.
    direction: Direction = Direction.ASCENDING


@dataclasses.dataclass
class CountrylanguageFilter:
    """What narrows a search over `world.countrylanguage`.

    Every field is optional: a field left ``None`` adds no condition, and the
    conditions a search does build are joined with ``AND``. The default
    instance is the filter that matches every row.

    A nullable column's field is ``None`` for *no condition* and never for
    *is NULL*; that question is asked through the ``…_is_null`` field such a
    column also gets, so the two meanings never collide.
    """

    #: Where set, requires `CountryCode` to equal it.
    country_code: str | None = None
    #: Where set, requires `CountryCode` to match it, as LIKE matches.
    country_code_like: str | None = None
    #: Where set, requires `Language` to equal it.
    language: str | None = None
    #: Where set, requires `Language` to match it, as LIKE matches.
    language_like: str | None = None
    #: Where set, requires `IsOfficial` to equal it.
    is_official: str | None = None
    #: Where set, requires `IsOfficial` to match it, as LIKE matches.
    is_official_like: str | None = None
    #: Where set, requires `Percentage` to equal it.
    percentage: decimal.Decimal | None = None
    #: Where set, requires `Percentage` to be at least it.
    percentage_from: decimal.Decimal | None = None
    #: Where set, requires `Percentage` to be at most it.
    percentage_to: decimal.Decimal | None = None

    def is_empty(self) -> bool:
        """Is no field set?

        A search over an empty filter builds no ``WHERE`` clause and matches
        every row of the table.
        """
        return (
            self.country_code is None
            and self.country_code_like is None
            and self.language is None
            and self.language_like is None
            and self.is_official is None
            and self.is_official_like is None
            and self.percentage is None
            and self.percentage_from is None
            and self.percentage_to is None
        )
