# Code generated by tpl from freight.container. DO NOT EDIT.

"""The PyMySQL implementation of the `freight.container` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .container import Container, ContainerFilter, ContainerKey, ContainerOrder
from .container_repository import ContainerRepository

__all__ = ["ContainerPyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "freight.container"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `container_id`, `equipment_no`, `consignment_id`, `iso_code`, `seal_number`, `tare_weight_kg`, `cargo_weight_kg`, `vgm_kg`, `vgm_method`, `setpoint_celsius`, `humidity_pct`, `vent_setting`, `stow_position`, `loaded_at` FROM `container`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `container_id`, `equipment_no`, `consignment_id`, `iso_code`, `seal_number`, `tare_weight_kg`, `cargo_weight_kg`, `vgm_kg`, `vgm_method`, `setpoint_celsius`, `humidity_pct`, `vent_setting`, `stow_position`, `loaded_at` FROM `container` WHERE `container_id` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `container` (`equipment_no`, `consignment_id`, `iso_code`, `seal_number`, `tare_weight_kg`, `cargo_weight_kg`, `vgm_method`, `setpoint_celsius`, `humidity_pct`, `vent_setting`, `stow_position`, `loaded_at`) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `container` SET `equipment_no` = %s, `consignment_id` = %s, `iso_code` = %s, `seal_number` = %s, `tare_weight_kg` = %s, `cargo_weight_kg` = %s, `vgm_method` = %s, `setpoint_celsius` = %s, `humidity_pct` = %s, `vent_setting` = %s, `stow_position` = %s, `loaded_at` = %s WHERE `container_id` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `container` WHERE `container_id` = %s"


def _row(values: tuple[Any, ...]) -> Container:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return Container(
        container_id=values[0],
        equipment_no=values[1],
        consignment_id=values[2],
        iso_code=values[3],
        seal_number=values[4],
        tare_weight_kg=values[5],
        cargo_weight_kg=values[6],
        vgm_kg=values[7],
        vgm_method=values[8],
        setpoint_celsius=values[9],
        humidity_pct=values[10],
        vent_setting=values[11],
        stow_position=values[12],
        loaded_at=values[13],
    )


class ContainerPyMySQL:
    """The PyMySQL data layer of `freight.container`.

    It names no base class. ``ContainerRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `freight.container` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: Container) -> None:
        """Insert `row`.

        The key is assigned by the server and is written back into `row`.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.equipment_no,
                    row.consignment_id,
                    row.iso_code,
                    row.seal_number,
                    row.tare_weight_kg,
                    row.cargo_weight_kg,
                    row.vgm_method,
                    row.setpoint_celsius,
                    row.humidity_pct,
                    row.vent_setting,
                    row.stow_position,
                    row.loaded_at,
                ),
            )
            row.container_id = cursor.lastrowid

    def read(self, key: ContainerKey) -> Container:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.container_id,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: Container) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.equipment_no,
                    row.consignment_id,
                    row.iso_code,
                    row.seal_number,
                    row.tare_weight_kg,
                    row.cargo_weight_kg,
                    row.vgm_method,
                    row.setpoint_celsius,
                    row.humidity_pct,
                    row.vent_setting,
                    row.stow_position,
                    row.loaded_at,
                    row.container_id,
                ),
            )
        return affected

    def delete(self, key: ContainerKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.container_id,
                ),
            )
        return affected

    def search(
        self,
        criteria: ContainerFilter,
        order: Sequence[ContainerOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[Container]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.container_id is not None:
            conditions.append("`container_id` = %s")
            arguments.append(criteria.container_id)
        if criteria.container_id_from is not None:
            conditions.append("`container_id` >= %s")
            arguments.append(criteria.container_id_from)
        if criteria.container_id_to is not None:
            conditions.append("`container_id` <= %s")
            arguments.append(criteria.container_id_to)
        if criteria.equipment_no is not None:
            conditions.append("`equipment_no` = %s")
            arguments.append(criteria.equipment_no)
        if criteria.equipment_no_like is not None:
            conditions.append("`equipment_no` LIKE %s")
            arguments.append(criteria.equipment_no_like)
        if criteria.consignment_id is not None:
            conditions.append("`consignment_id` = %s")
            arguments.append(criteria.consignment_id)
        if criteria.consignment_id_from is not None:
            conditions.append("`consignment_id` >= %s")
            arguments.append(criteria.consignment_id_from)
        if criteria.consignment_id_to is not None:
            conditions.append("`consignment_id` <= %s")
            arguments.append(criteria.consignment_id_to)
        if criteria.iso_code is not None:
            conditions.append("`iso_code` = %s")
            arguments.append(criteria.iso_code)
        if criteria.iso_code_like is not None:
            conditions.append("`iso_code` LIKE %s")
            arguments.append(criteria.iso_code_like)
        if criteria.seal_number is not None:
            conditions.append("`seal_number` = %s")
            arguments.append(criteria.seal_number)
        if criteria.seal_number_like is not None:
            conditions.append("`seal_number` LIKE %s")
            arguments.append(criteria.seal_number_like)
        if criteria.seal_number_is_null is not None:
            conditions.append(
                "`seal_number` IS NULL"
                if criteria.seal_number_is_null
                else "`seal_number` IS NOT NULL"
            )
        if criteria.tare_weight_kg is not None:
            conditions.append("`tare_weight_kg` = %s")
            arguments.append(criteria.tare_weight_kg)
        if criteria.tare_weight_kg_from is not None:
            conditions.append("`tare_weight_kg` >= %s")
            arguments.append(criteria.tare_weight_kg_from)
        if criteria.tare_weight_kg_to is not None:
            conditions.append("`tare_weight_kg` <= %s")
            arguments.append(criteria.tare_weight_kg_to)
        if criteria.cargo_weight_kg is not None:
            conditions.append("`cargo_weight_kg` = %s")
            arguments.append(criteria.cargo_weight_kg)
        if criteria.cargo_weight_kg_from is not None:
            conditions.append("`cargo_weight_kg` >= %s")
            arguments.append(criteria.cargo_weight_kg_from)
        if criteria.cargo_weight_kg_to is not None:
            conditions.append("`cargo_weight_kg` <= %s")
            arguments.append(criteria.cargo_weight_kg_to)
        if criteria.vgm_kg is not None:
            conditions.append("`vgm_kg` = %s")
            arguments.append(criteria.vgm_kg)
        if criteria.vgm_kg_from is not None:
            conditions.append("`vgm_kg` >= %s")
            arguments.append(criteria.vgm_kg_from)
        if criteria.vgm_kg_to is not None:
            conditions.append("`vgm_kg` <= %s")
            arguments.append(criteria.vgm_kg_to)
        if criteria.vgm_kg_is_null is not None:
            conditions.append(
                "`vgm_kg` IS NULL"
                if criteria.vgm_kg_is_null
                else "`vgm_kg` IS NOT NULL"
            )
        if criteria.vgm_method is not None:
            conditions.append("`vgm_method` = %s")
            arguments.append(criteria.vgm_method)
        if criteria.vgm_method_like is not None:
            conditions.append("`vgm_method` LIKE %s")
            arguments.append(criteria.vgm_method_like)
        if criteria.setpoint_celsius is not None:
            conditions.append("`setpoint_celsius` = %s")
            arguments.append(criteria.setpoint_celsius)
        if criteria.setpoint_celsius_from is not None:
            conditions.append("`setpoint_celsius` >= %s")
            arguments.append(criteria.setpoint_celsius_from)
        if criteria.setpoint_celsius_to is not None:
            conditions.append("`setpoint_celsius` <= %s")
            arguments.append(criteria.setpoint_celsius_to)
        if criteria.setpoint_celsius_is_null is not None:
            conditions.append(
                "`setpoint_celsius` IS NULL"
                if criteria.setpoint_celsius_is_null
                else "`setpoint_celsius` IS NOT NULL"
            )
        if criteria.humidity_pct is not None:
            conditions.append("`humidity_pct` = %s")
            arguments.append(criteria.humidity_pct)
        if criteria.humidity_pct_from is not None:
            conditions.append("`humidity_pct` >= %s")
            arguments.append(criteria.humidity_pct_from)
        if criteria.humidity_pct_to is not None:
            conditions.append("`humidity_pct` <= %s")
            arguments.append(criteria.humidity_pct_to)
        if criteria.humidity_pct_is_null is not None:
            conditions.append(
                "`humidity_pct` IS NULL"
                if criteria.humidity_pct_is_null
                else "`humidity_pct` IS NOT NULL"
            )
        if criteria.vent_setting is not None:
            conditions.append("`vent_setting` = %s")
            arguments.append(criteria.vent_setting)
        if criteria.vent_setting_like is not None:
            conditions.append("`vent_setting` LIKE %s")
            arguments.append(criteria.vent_setting_like)
        if criteria.vent_setting_is_null is not None:
            conditions.append(
                "`vent_setting` IS NULL"
                if criteria.vent_setting_is_null
                else "`vent_setting` IS NOT NULL"
            )
        if criteria.stow_position is not None:
            conditions.append("`stow_position` = %s")
            arguments.append(criteria.stow_position)
        if criteria.stow_position_like is not None:
            conditions.append("`stow_position` LIKE %s")
            arguments.append(criteria.stow_position_like)
        if criteria.stow_position_is_null is not None:
            conditions.append(
                "`stow_position` IS NULL"
                if criteria.stow_position_is_null
                else "`stow_position` IS NOT NULL"
            )
        if criteria.loaded_at is not None:
            conditions.append("`loaded_at` = %s")
            arguments.append(criteria.loaded_at)
        if criteria.loaded_at_from is not None:
            conditions.append("`loaded_at` >= %s")
            arguments.append(criteria.loaded_at_from)
        if criteria.loaded_at_to is not None:
            conditions.append("`loaded_at` <= %s")
            arguments.append(criteria.loaded_at_to)
        if criteria.loaded_at_is_null is not None:
            conditions.append(
                "`loaded_at` IS NULL"
                if criteria.loaded_at_is_null
                else "`loaded_at` IS NOT NULL"
            )

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: ContainerPyMySQL,
    ) -> ContainerRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
