# Code generated by tpl from freight.charge. DO NOT EDIT.

"""The PyMySQL implementation of the `freight.charge` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .charge import Charge, ChargeFilter, ChargeKey, ChargeOrder
from .charge_repository import ChargeRepository

__all__ = ["ChargePyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "freight.charge"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `charge_id`, `consignment_id`, `charge_code`, `quantity`, `unit_amount`, `currency`, `fx_rate_to_eur`, `net_amount`, `payer`, `invoiced_on`, `invoice_number` FROM `charge`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `charge_id`, `consignment_id`, `charge_code`, `quantity`, `unit_amount`, `currency`, `fx_rate_to_eur`, `net_amount`, `payer`, `invoiced_on`, `invoice_number` FROM `charge` WHERE `charge_id` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `charge` (`consignment_id`, `charge_code`, `quantity`, `unit_amount`, `currency`, `fx_rate_to_eur`, `payer`, `invoiced_on`, `invoice_number`) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `charge` SET `consignment_id` = %s, `charge_code` = %s, `quantity` = %s, `unit_amount` = %s, `currency` = %s, `fx_rate_to_eur` = %s, `payer` = %s, `invoiced_on` = %s, `invoice_number` = %s WHERE `charge_id` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `charge` WHERE `charge_id` = %s"


def _row(values: tuple[Any, ...]) -> Charge:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return Charge(
        charge_id=values[0],
        consignment_id=values[1],
        charge_code=values[2],
        quantity=values[3],
        unit_amount=values[4],
        currency=values[5],
        fx_rate_to_eur=values[6],
        net_amount=values[7],
        payer=values[8],
        invoiced_on=values[9],
        invoice_number=values[10],
    )


class ChargePyMySQL:
    """The PyMySQL data layer of `freight.charge`.

    It names no base class. ``ChargeRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `freight.charge` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: Charge) -> None:
        """Insert `row`.

        The key is assigned by the server and is written back into `row`.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.consignment_id,
                    row.charge_code,
                    row.quantity,
                    row.unit_amount,
                    row.currency,
                    row.fx_rate_to_eur,
                    row.payer,
                    row.invoiced_on,
                    row.invoice_number,
                ),
            )
            row.charge_id = cursor.lastrowid

    def read(self, key: ChargeKey) -> Charge:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.charge_id,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: Charge) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.consignment_id,
                    row.charge_code,
                    row.quantity,
                    row.unit_amount,
                    row.currency,
                    row.fx_rate_to_eur,
                    row.payer,
                    row.invoiced_on,
                    row.invoice_number,
                    row.charge_id,
                ),
            )
        return affected

    def delete(self, key: ChargeKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.charge_id,
                ),
            )
        return affected

    def search(
        self,
        criteria: ChargeFilter,
        order: Sequence[ChargeOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[Charge]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.charge_id is not None:
            conditions.append("`charge_id` = %s")
            arguments.append(criteria.charge_id)
        if criteria.charge_id_from is not None:
            conditions.append("`charge_id` >= %s")
            arguments.append(criteria.charge_id_from)
        if criteria.charge_id_to is not None:
            conditions.append("`charge_id` <= %s")
            arguments.append(criteria.charge_id_to)
        if criteria.consignment_id is not None:
            conditions.append("`consignment_id` = %s")
            arguments.append(criteria.consignment_id)
        if criteria.consignment_id_from is not None:
            conditions.append("`consignment_id` >= %s")
            arguments.append(criteria.consignment_id_from)
        if criteria.consignment_id_to is not None:
            conditions.append("`consignment_id` <= %s")
            arguments.append(criteria.consignment_id_to)
        if criteria.charge_code is not None:
            conditions.append("`charge_code` = %s")
            arguments.append(criteria.charge_code)
        if criteria.charge_code_like is not None:
            conditions.append("`charge_code` LIKE %s")
            arguments.append(criteria.charge_code_like)
        if criteria.quantity is not None:
            conditions.append("`quantity` = %s")
            arguments.append(criteria.quantity)
        if criteria.quantity_from is not None:
            conditions.append("`quantity` >= %s")
            arguments.append(criteria.quantity_from)
        if criteria.quantity_to is not None:
            conditions.append("`quantity` <= %s")
            arguments.append(criteria.quantity_to)
        if criteria.unit_amount is not None:
            conditions.append("`unit_amount` = %s")
            arguments.append(criteria.unit_amount)
        if criteria.unit_amount_from is not None:
            conditions.append("`unit_amount` >= %s")
            arguments.append(criteria.unit_amount_from)
        if criteria.unit_amount_to is not None:
            conditions.append("`unit_amount` <= %s")
            arguments.append(criteria.unit_amount_to)
        if criteria.currency is not None:
            conditions.append("`currency` = %s")
            arguments.append(criteria.currency)
        if criteria.currency_like is not None:
            conditions.append("`currency` LIKE %s")
            arguments.append(criteria.currency_like)
        if criteria.fx_rate_to_eur is not None:
            conditions.append("`fx_rate_to_eur` = %s")
            arguments.append(criteria.fx_rate_to_eur)
        if criteria.fx_rate_to_eur_from is not None:
            conditions.append("`fx_rate_to_eur` >= %s")
            arguments.append(criteria.fx_rate_to_eur_from)
        if criteria.fx_rate_to_eur_to is not None:
            conditions.append("`fx_rate_to_eur` <= %s")
            arguments.append(criteria.fx_rate_to_eur_to)
        if criteria.net_amount is not None:
            conditions.append("`net_amount` = %s")
            arguments.append(criteria.net_amount)
        if criteria.net_amount_from is not None:
            conditions.append("`net_amount` >= %s")
            arguments.append(criteria.net_amount_from)
        if criteria.net_amount_to is not None:
            conditions.append("`net_amount` <= %s")
            arguments.append(criteria.net_amount_to)
        if criteria.net_amount_is_null is not None:
            conditions.append(
                "`net_amount` IS NULL"
                if criteria.net_amount_is_null
                else "`net_amount` IS NOT NULL"
            )
        if criteria.payer is not None:
            conditions.append("`payer` = %s")
            arguments.append(criteria.payer)
        if criteria.payer_like is not None:
            conditions.append("`payer` LIKE %s")
            arguments.append(criteria.payer_like)
        if criteria.invoiced_on is not None:
            conditions.append("`invoiced_on` = %s")
            arguments.append(criteria.invoiced_on)
        if criteria.invoiced_on_from is not None:
            conditions.append("`invoiced_on` >= %s")
            arguments.append(criteria.invoiced_on_from)
        if criteria.invoiced_on_to is not None:
            conditions.append("`invoiced_on` <= %s")
            arguments.append(criteria.invoiced_on_to)
        if criteria.invoiced_on_is_null is not None:
            conditions.append(
                "`invoiced_on` IS NULL"
                if criteria.invoiced_on_is_null
                else "`invoiced_on` IS NOT NULL"
            )
        if criteria.invoice_number is not None:
            conditions.append("`invoice_number` = %s")
            arguments.append(criteria.invoice_number)
        if criteria.invoice_number_like is not None:
            conditions.append("`invoice_number` LIKE %s")
            arguments.append(criteria.invoice_number_like)
        if criteria.invoice_number_is_null is not None:
            conditions.append(
                "`invoice_number` IS NULL"
                if criteria.invoice_number_is_null
                else "`invoice_number` IS NOT NULL"
            )

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: ChargePyMySQL,
    ) -> ChargeRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
