# Code generated by tpl from freight.voyage. DO NOT EDIT.

"""The PyMySQL implementation of the `freight.voyage` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .voyage import Voyage, VoyageFilter, VoyageKey, VoyageOrder
from .voyage_repository import VoyageRepository

__all__ = ["VoyagePyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "freight.voyage"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `vessel_imo`, `voyage_number`, `service_code`, `etd`, `eta`, `atd`, `status`, `bunker_price_usd_t`, `slot_allocation_teu`, `utilisation_pct` FROM `voyage`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `vessel_imo`, `voyage_number`, `service_code`, `etd`, `eta`, `atd`, `status`, `bunker_price_usd_t`, `slot_allocation_teu`, `utilisation_pct` FROM `voyage` WHERE `vessel_imo` = %s AND `voyage_number` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `voyage` (`vessel_imo`, `voyage_number`, `service_code`, `etd`, `eta`, `atd`, `status`, `bunker_price_usd_t`, `slot_allocation_teu`, `utilisation_pct`) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `voyage` SET `service_code` = %s, `etd` = %s, `eta` = %s, `atd` = %s, `status` = %s, `bunker_price_usd_t` = %s, `slot_allocation_teu` = %s, `utilisation_pct` = %s WHERE `vessel_imo` = %s AND `voyage_number` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `voyage` WHERE `vessel_imo` = %s AND `voyage_number` = %s"


def _row(values: tuple[Any, ...]) -> Voyage:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return Voyage(
        vessel_imo=values[0],
        voyage_number=values[1],
        service_code=values[2],
        etd=values[3],
        eta=values[4],
        atd=values[5],
        status=values[6],
        bunker_price_usd_t=values[7],
        slot_allocation_teu=values[8],
        utilisation_pct=values[9],
    )


class VoyagePyMySQL:
    """The PyMySQL data layer of `freight.voyage`.

    It names no base class. ``VoyageRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `freight.voyage` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: Voyage) -> None:
        """Insert `row`.

        The whole key is taken from `row`, which the caller therefore fills.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.vessel_imo,
                    row.voyage_number,
                    row.service_code,
                    row.etd,
                    row.eta,
                    row.atd,
                    row.status,
                    row.bunker_price_usd_t,
                    row.slot_allocation_teu,
                    row.utilisation_pct,
                ),
            )

    def read(self, key: VoyageKey) -> Voyage:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.vessel_imo,
                    key.voyage_number,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: Voyage) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.service_code,
                    row.etd,
                    row.eta,
                    row.atd,
                    row.status,
                    row.bunker_price_usd_t,
                    row.slot_allocation_teu,
                    row.utilisation_pct,
                    row.vessel_imo,
                    row.voyage_number,
                ),
            )
        return affected

    def delete(self, key: VoyageKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.vessel_imo,
                    key.voyage_number,
                ),
            )
        return affected

    def search(
        self,
        criteria: VoyageFilter,
        order: Sequence[VoyageOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[Voyage]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.vessel_imo is not None:
            conditions.append("`vessel_imo` = %s")
            arguments.append(criteria.vessel_imo)
        if criteria.vessel_imo_like is not None:
            conditions.append("`vessel_imo` LIKE %s")
            arguments.append(criteria.vessel_imo_like)
        if criteria.voyage_number is not None:
            conditions.append("`voyage_number` = %s")
            arguments.append(criteria.voyage_number)
        if criteria.voyage_number_like is not None:
            conditions.append("`voyage_number` LIKE %s")
            arguments.append(criteria.voyage_number_like)
        if criteria.service_code is not None:
            conditions.append("`service_code` = %s")
            arguments.append(criteria.service_code)
        if criteria.service_code_like is not None:
            conditions.append("`service_code` LIKE %s")
            arguments.append(criteria.service_code_like)
        if criteria.etd is not None:
            conditions.append("`etd` = %s")
            arguments.append(criteria.etd)
        if criteria.etd_from is not None:
            conditions.append("`etd` >= %s")
            arguments.append(criteria.etd_from)
        if criteria.etd_to is not None:
            conditions.append("`etd` <= %s")
            arguments.append(criteria.etd_to)
        if criteria.eta is not None:
            conditions.append("`eta` = %s")
            arguments.append(criteria.eta)
        if criteria.eta_from is not None:
            conditions.append("`eta` >= %s")
            arguments.append(criteria.eta_from)
        if criteria.eta_to is not None:
            conditions.append("`eta` <= %s")
            arguments.append(criteria.eta_to)
        if criteria.atd is not None:
            conditions.append("`atd` = %s")
            arguments.append(criteria.atd)
        if criteria.atd_from is not None:
            conditions.append("`atd` >= %s")
            arguments.append(criteria.atd_from)
        if criteria.atd_to is not None:
            conditions.append("`atd` <= %s")
            arguments.append(criteria.atd_to)
        if criteria.atd_is_null is not None:
            conditions.append(
                "`atd` IS NULL"
                if criteria.atd_is_null
                else "`atd` IS NOT NULL"
            )
        if criteria.status is not None:
            conditions.append("`status` = %s")
            arguments.append(criteria.status)
        if criteria.status_like is not None:
            conditions.append("`status` LIKE %s")
            arguments.append(criteria.status_like)
        if criteria.bunker_price_usd_t is not None:
            conditions.append("`bunker_price_usd_t` = %s")
            arguments.append(criteria.bunker_price_usd_t)
        if criteria.bunker_price_usd_t_from is not None:
            conditions.append("`bunker_price_usd_t` >= %s")
            arguments.append(criteria.bunker_price_usd_t_from)
        if criteria.bunker_price_usd_t_to is not None:
            conditions.append("`bunker_price_usd_t` <= %s")
            arguments.append(criteria.bunker_price_usd_t_to)
        if criteria.bunker_price_usd_t_is_null is not None:
            conditions.append(
                "`bunker_price_usd_t` IS NULL"
                if criteria.bunker_price_usd_t_is_null
                else "`bunker_price_usd_t` IS NOT NULL"
            )
        if criteria.slot_allocation_teu is not None:
            conditions.append("`slot_allocation_teu` = %s")
            arguments.append(criteria.slot_allocation_teu)
        if criteria.slot_allocation_teu_from is not None:
            conditions.append("`slot_allocation_teu` >= %s")
            arguments.append(criteria.slot_allocation_teu_from)
        if criteria.slot_allocation_teu_to is not None:
            conditions.append("`slot_allocation_teu` <= %s")
            arguments.append(criteria.slot_allocation_teu_to)
        if criteria.utilisation_pct is not None:
            conditions.append("`utilisation_pct` = %s")
            arguments.append(criteria.utilisation_pct)
        if criteria.utilisation_pct_from is not None:
            conditions.append("`utilisation_pct` >= %s")
            arguments.append(criteria.utilisation_pct_from)
        if criteria.utilisation_pct_to is not None:
            conditions.append("`utilisation_pct` <= %s")
            arguments.append(criteria.utilisation_pct_to)
        if criteria.utilisation_pct_is_null is not None:
            conditions.append(
                "`utilisation_pct` IS NULL"
                if criteria.utilisation_pct_is_null
                else "`utilisation_pct` IS NOT NULL"
            )

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: VoyagePyMySQL,
    ) -> VoyageRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
