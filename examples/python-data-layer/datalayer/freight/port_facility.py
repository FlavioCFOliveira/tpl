# Code generated by tpl from freight.port_facility. DO NOT EDIT.

"""The `freight.port_facility` row, the key that addresses it, the columns an ordering may
name, and the filter a search takes.
"""

from __future__ import annotations

import dataclasses
import datetime
import decimal
import enum
from collections.abc import Mapping
from typing import Final

from ._core import Direction, UnknownColumn

__all__ = [
    "PortFacility",
    "PortFacilityColumn",
    "PortFacilityFilter",
    "PortFacilityKey",
    "PortFacilityOrder",
]


@dataclasses.dataclass
class PortFacility:
    """One row of `freight.port_facility`.

    The dataclass is mutable, because :meth:`create` writes a server-assigned
    key back into it.
    """

    # The table's own comment, as the catalogue carries it:
    # Terminais e postos de acostagem, com a respectiva geometria — 港湾施設

    #: `facility_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT.
    #:
    #: It is part of the primary key.
    facility_id: int
    #: `port_id`, declared `smallint(5) unsigned` NOT NULL.
    #:
    #: It is part of a unique index.
    port_id: int
    #: `designation`, declared `varchar(80)` NOT NULL.
    #:
    #: It is part of a unique index.
    #:
    #: Terminal or berth designation, e.g. "Alcântara Norte"
    designation: str
    #: `facility_kind`, declared `enum('Container terminal','Ro-Ro ramp','Bulk quay','Liquid bulk jetty','Reefer terminal','Lay-by berth')` NOT NULL.
    facility_kind: str
    #: `quay_length_m`, declared `decimal(7,2)` NOT NULL.
    #:
    #: Continuous quay length in metres
    quay_length_m: decimal.Decimal
    #: `crane_count`, declared `tinyint(3) unsigned` NOT NULL.
    #:
    #: Ship-to-shore gantry cranes
    crane_count: int
    #: `gate_opens_at`, declared `time` NOT NULL.
    #:
    #: Landside gate opening, local time
    gate_opens_at: datetime.timedelta
    #: `gate_closes_at`, declared `time` NOT NULL.
    #:
    #: Landside gate closing, local time
    gate_closes_at: datetime.timedelta
    #: `berth_outline`, declared `polygon` NOT NULL.
    #:
    #: Berth footprint as surveyed
    berth_outline: bytes
    #: `approach_channel`, declared `linestring` NULL.
    #:
    #: Centreline of the dredged approach
    approach_channel: bytes | None
    #: `anchorage_points`, declared `multipoint` NULL.
    #:
    #: Designated waiting anchorages
    anchorage_points: bytes | None
    #: `fairway_network`, declared `multilinestring` NULL.
    #:
    #: Buoyed fairways serving the facility
    fairway_network: bytes | None
    #: `restricted_areas`, declared `multipolygon` NULL.
    #:
    #: Areas closed to commercial traffic
    restricted_areas: bytes | None
    #: `reference_marker`, declared `point` NULL.
    #:
    #: Single published reference position
    reference_marker: bytes | None
    #: `survey_footprint`, declared `geometry` NULL.
    #:
    #: Latest survey outline, type not fixed in advance
    survey_footprint: bytes | None
    #: `notices_to_mariners`, declared `geometrycollection` NULL.
    #:
    #: Mixed geometry accompanying current notices
    notices_to_mariners: bytes | None

    def key(self) -> PortFacilityKey:
        """Return the primary key of the row."""
        return PortFacilityKey(
            facility_id=self.facility_id,
        )


@dataclasses.dataclass(frozen=True)
class PortFacilityKey:
    """The primary key of `freight.port_facility`.

    Frozen, so that it hashes and compares by value. Every type this example's
    mapping produces is hashable, so no key of these three schemas is refused
    for that reason.
    """

    #: `facility_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT.
    facility_id: int


class PortFacilityColumn(enum.Enum):
    """One column of `freight.port_facility`.

    A search accepts an ordering term over these members and over nothing else,
    so an ordering can carry no SQL of its own: :attr:`sql` answers from a
    closed mapping this template wrote, and :meth:`from_name` is the one door a
    name a caller supplied can come through.

    A member's value is the column's name as the catalogue carries it, which is
    what :meth:`from_name` matches on and what ``member.value`` returns.
    """

    #: `facility_id`, declared `int(10) unsigned` NOT NULL AUTO_INCREMENT.
    FACILITY_ID = "facility_id"
    #: `port_id`, declared `smallint(5) unsigned` NOT NULL.
    PORT_ID = "port_id"
    #: `designation`, declared `varchar(80)` NOT NULL.
    DESIGNATION = "designation"
    #: `facility_kind`, declared `enum('Container terminal','Ro-Ro ramp','Bulk quay','Liquid bulk jetty','Reefer terminal','Lay-by berth')` NOT NULL.
    FACILITY_KIND = "facility_kind"
    #: `quay_length_m`, declared `decimal(7,2)` NOT NULL.
    QUAY_LENGTH_M = "quay_length_m"
    #: `crane_count`, declared `tinyint(3) unsigned` NOT NULL.
    CRANE_COUNT = "crane_count"
    #: `gate_opens_at`, declared `time` NOT NULL.
    GATE_OPENS_AT = "gate_opens_at"
    #: `gate_closes_at`, declared `time` NOT NULL.
    GATE_CLOSES_AT = "gate_closes_at"
    #: `berth_outline`, declared `polygon` NOT NULL.
    BERTH_OUTLINE = "berth_outline"
    #: `approach_channel`, declared `linestring` NULL.
    APPROACH_CHANNEL = "approach_channel"
    #: `anchorage_points`, declared `multipoint` NULL.
    ANCHORAGE_POINTS = "anchorage_points"
    #: `fairway_network`, declared `multilinestring` NULL.
    FAIRWAY_NETWORK = "fairway_network"
    #: `restricted_areas`, declared `multipolygon` NULL.
    RESTRICTED_AREAS = "restricted_areas"
    #: `reference_marker`, declared `point` NULL.
    REFERENCE_MARKER = "reference_marker"
    #: `survey_footprint`, declared `geometry` NULL.
    SURVEY_FOOTPRINT = "survey_footprint"
    #: `notices_to_mariners`, declared `geometrycollection` NULL.
    NOTICES_TO_MARINERS = "notices_to_mariners"

    @property
    def sql(self) -> str:
        """The column as a statement's text names it: quoted for MariaDB."""
        return _COLUMNS[self]

    @classmethod
    def from_name(cls, name: str) -> PortFacilityColumn:
        """Read a column a caller supplied as text.

        Raises:
            UnknownColumn: for every name the table does not carry. That
                refusal is what keeps a caller's text out of a statement.
        """
        try:
            return cls(name)
        except ValueError:
            raise UnknownColumn("freight.port_facility", name) from None


#: The one place a column becomes SQL. It is closed over the enumeration above,
#: and every identifier in it was quoted at render time by `tpl`, so no value a
#: caller supplies can reach a statement's text through an ordering term.
_COLUMNS: Final[Mapping[PortFacilityColumn, str]] = {
    PortFacilityColumn.FACILITY_ID: "`facility_id`",
    PortFacilityColumn.PORT_ID: "`port_id`",
    PortFacilityColumn.DESIGNATION: "`designation`",
    PortFacilityColumn.FACILITY_KIND: "`facility_kind`",
    PortFacilityColumn.QUAY_LENGTH_M: "`quay_length_m`",
    PortFacilityColumn.CRANE_COUNT: "`crane_count`",
    PortFacilityColumn.GATE_OPENS_AT: "`gate_opens_at`",
    PortFacilityColumn.GATE_CLOSES_AT: "`gate_closes_at`",
    PortFacilityColumn.BERTH_OUTLINE: "`berth_outline`",
    PortFacilityColumn.APPROACH_CHANNEL: "`approach_channel`",
    PortFacilityColumn.ANCHORAGE_POINTS: "`anchorage_points`",
    PortFacilityColumn.FAIRWAY_NETWORK: "`fairway_network`",
    PortFacilityColumn.RESTRICTED_AREAS: "`restricted_areas`",
    PortFacilityColumn.REFERENCE_MARKER: "`reference_marker`",
    PortFacilityColumn.SURVEY_FOOTPRINT: "`survey_footprint`",
    PortFacilityColumn.NOTICES_TO_MARINERS: "`notices_to_mariners`",
}


@dataclasses.dataclass(frozen=True)
class PortFacilityOrder:
    """One ordering term of a search over `freight.port_facility`."""

    #: The column to order by.
    column: PortFacilityColumn
    #: The direction to order in.
    direction: Direction = Direction.ASCENDING


@dataclasses.dataclass
class PortFacilityFilter:
    """What narrows a search over `freight.port_facility`.

    Every field is optional: a field left ``None`` adds no condition, and the
    conditions a search does build are joined with ``AND``. The default
    instance is the filter that matches every row.

    A nullable column's field is ``None`` for *no condition* and never for
    *is NULL*; that question is asked through the ``…_is_null`` field such a
    column also gets, so the two meanings never collide.
    """

    #: Where set, requires `facility_id` to equal it.
    facility_id: int | None = None
    #: Where set, requires `facility_id` to be at least it.
    facility_id_from: int | None = None
    #: Where set, requires `facility_id` to be at most it.
    facility_id_to: int | None = None
    #: Where set, requires `port_id` to equal it.
    port_id: int | None = None
    #: Where set, requires `port_id` to be at least it.
    port_id_from: int | None = None
    #: Where set, requires `port_id` to be at most it.
    port_id_to: int | None = None
    #: Where set, requires `designation` to equal it.
    designation: str | None = None
    #: Where set, requires `designation` to match it, as LIKE matches.
    designation_like: str | None = None
    #: Where set, requires `facility_kind` to equal it.
    facility_kind: str | None = None
    #: Where set, requires `facility_kind` to match it, as LIKE matches.
    facility_kind_like: str | None = None
    #: Where set, requires `quay_length_m` to equal it.
    quay_length_m: decimal.Decimal | None = None
    #: Where set, requires `quay_length_m` to be at least it.
    quay_length_m_from: decimal.Decimal | None = None
    #: Where set, requires `quay_length_m` to be at most it.
    quay_length_m_to: decimal.Decimal | None = None
    #: Where set, requires `crane_count` to equal it.
    crane_count: int | None = None
    #: Where set, requires `crane_count` to be at least it.
    crane_count_from: int | None = None
    #: Where set, requires `crane_count` to be at most it.
    crane_count_to: int | None = None
    #: Where set, requires `gate_opens_at` to equal it.
    gate_opens_at: datetime.timedelta | None = None
    #: Where set, requires `gate_opens_at` to be at least it.
    gate_opens_at_from: datetime.timedelta | None = None
    #: Where set, requires `gate_opens_at` to be at most it.
    gate_opens_at_to: datetime.timedelta | None = None
    #: Where set, requires `gate_closes_at` to equal it.
    gate_closes_at: datetime.timedelta | None = None
    #: Where set, requires `gate_closes_at` to be at least it.
    gate_closes_at_from: datetime.timedelta | None = None
    #: Where set, requires `gate_closes_at` to be at most it.
    gate_closes_at_to: datetime.timedelta | None = None
    #: Where set, requires `berth_outline` to equal it.
    berth_outline: bytes | None = None
    #: Where set, requires `approach_channel` to equal it.
    approach_channel: bytes | None = None
    #: Where True, requires `approach_channel` to be NULL; where False, requires it not to be.
    approach_channel_is_null: bool | None = None
    #: Where set, requires `anchorage_points` to equal it.
    anchorage_points: bytes | None = None
    #: Where True, requires `anchorage_points` to be NULL; where False, requires it not to be.
    anchorage_points_is_null: bool | None = None
    #: Where set, requires `fairway_network` to equal it.
    fairway_network: bytes | None = None
    #: Where True, requires `fairway_network` to be NULL; where False, requires it not to be.
    fairway_network_is_null: bool | None = None
    #: Where set, requires `restricted_areas` to equal it.
    restricted_areas: bytes | None = None
    #: Where True, requires `restricted_areas` to be NULL; where False, requires it not to be.
    restricted_areas_is_null: bool | None = None
    #: Where set, requires `reference_marker` to equal it.
    reference_marker: bytes | None = None
    #: Where True, requires `reference_marker` to be NULL; where False, requires it not to be.
    reference_marker_is_null: bool | None = None
    #: Where set, requires `survey_footprint` to equal it.
    survey_footprint: bytes | None = None
    #: Where True, requires `survey_footprint` to be NULL; where False, requires it not to be.
    survey_footprint_is_null: bool | None = None
    #: Where set, requires `notices_to_mariners` to equal it.
    notices_to_mariners: bytes | None = None
    #: Where True, requires `notices_to_mariners` to be NULL; where False, requires it not to be.
    notices_to_mariners_is_null: bool | None = None

    def is_empty(self) -> bool:
        """Is no field set?

        A search over an empty filter builds no ``WHERE`` clause and matches
        every row of the table.
        """
        return (
            self.facility_id is None
            and self.facility_id_from is None
            and self.facility_id_to is None
            and self.port_id is None
            and self.port_id_from is None
            and self.port_id_to is None
            and self.designation is None
            and self.designation_like is None
            and self.facility_kind is None
            and self.facility_kind_like is None
            and self.quay_length_m is None
            and self.quay_length_m_from is None
            and self.quay_length_m_to is None
            and self.crane_count is None
            and self.crane_count_from is None
            and self.crane_count_to is None
            and self.gate_opens_at is None
            and self.gate_opens_at_from is None
            and self.gate_opens_at_to is None
            and self.gate_closes_at is None
            and self.gate_closes_at_from is None
            and self.gate_closes_at_to is None
            and self.berth_outline is None
            and self.approach_channel is None
            and self.approach_channel_is_null is None
            and self.anchorage_points is None
            and self.anchorage_points_is_null is None
            and self.fairway_network is None
            and self.fairway_network_is_null is None
            and self.restricted_areas is None
            and self.restricted_areas_is_null is None
            and self.reference_marker is None
            and self.reference_marker_is_null is None
            and self.survey_footprint is None
            and self.survey_footprint_is_null is None
            and self.notices_to_mariners is None
            and self.notices_to_mariners_is_null is None
        )
