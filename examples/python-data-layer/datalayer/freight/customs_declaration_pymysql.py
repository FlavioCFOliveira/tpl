# Code generated by tpl from freight.customs_declaration. DO NOT EDIT.

"""The PyMySQL implementation of the `freight.customs_declaration` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .customs_declaration import CustomsDeclaration, CustomsDeclarationFilter, CustomsDeclarationKey, CustomsDeclarationOrder
from .customs_declaration_repository import CustomsDeclarationRepository

__all__ = ["CustomsDeclarationPyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "freight.customs_declaration"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `declaration_id`, `consignment_id`, `clearance_port_id`, `mrn`, `procedure_code`, `regime`, `status`, `lodged_at`, `released_at`, `duty_amount`, `vat_amount`, `total_payable`, `officer_remarks` FROM `customs_declaration`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `declaration_id`, `consignment_id`, `clearance_port_id`, `mrn`, `procedure_code`, `regime`, `status`, `lodged_at`, `released_at`, `duty_amount`, `vat_amount`, `total_payable`, `officer_remarks` FROM `customs_declaration` WHERE `declaration_id` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `customs_declaration` (`consignment_id`, `clearance_port_id`, `mrn`, `procedure_code`, `regime`, `status`, `lodged_at`, `released_at`, `duty_amount`, `vat_amount`, `officer_remarks`) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `customs_declaration` SET `consignment_id` = %s, `clearance_port_id` = %s, `mrn` = %s, `procedure_code` = %s, `regime` = %s, `status` = %s, `lodged_at` = %s, `released_at` = %s, `duty_amount` = %s, `vat_amount` = %s, `officer_remarks` = %s WHERE `declaration_id` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `customs_declaration` WHERE `declaration_id` = %s"


def _row(values: tuple[Any, ...]) -> CustomsDeclaration:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return CustomsDeclaration(
        declaration_id=values[0],
        consignment_id=values[1],
        clearance_port_id=values[2],
        mrn=values[3],
        procedure_code=values[4],
        regime=values[5],
        status=values[6],
        lodged_at=values[7],
        released_at=values[8],
        duty_amount=values[9],
        vat_amount=values[10],
        total_payable=values[11],
        officer_remarks=values[12],
    )


class CustomsDeclarationPyMySQL:
    """The PyMySQL data layer of `freight.customs_declaration`.

    It names no base class. ``CustomsDeclarationRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `freight.customs_declaration` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: CustomsDeclaration) -> None:
        """Insert `row`.

        The key is assigned by the server and is written back into `row`.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.consignment_id,
                    row.clearance_port_id,
                    row.mrn,
                    row.procedure_code,
                    row.regime,
                    row.status,
                    row.lodged_at,
                    row.released_at,
                    row.duty_amount,
                    row.vat_amount,
                    row.officer_remarks,
                ),
            )
            row.declaration_id = cursor.lastrowid

    def read(self, key: CustomsDeclarationKey) -> CustomsDeclaration:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.declaration_id,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: CustomsDeclaration) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.consignment_id,
                    row.clearance_port_id,
                    row.mrn,
                    row.procedure_code,
                    row.regime,
                    row.status,
                    row.lodged_at,
                    row.released_at,
                    row.duty_amount,
                    row.vat_amount,
                    row.officer_remarks,
                    row.declaration_id,
                ),
            )
        return affected

    def delete(self, key: CustomsDeclarationKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.declaration_id,
                ),
            )
        return affected

    def search(
        self,
        criteria: CustomsDeclarationFilter,
        order: Sequence[CustomsDeclarationOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[CustomsDeclaration]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.declaration_id is not None:
            conditions.append("`declaration_id` = %s")
            arguments.append(criteria.declaration_id)
        if criteria.declaration_id_from is not None:
            conditions.append("`declaration_id` >= %s")
            arguments.append(criteria.declaration_id_from)
        if criteria.declaration_id_to is not None:
            conditions.append("`declaration_id` <= %s")
            arguments.append(criteria.declaration_id_to)
        if criteria.consignment_id is not None:
            conditions.append("`consignment_id` = %s")
            arguments.append(criteria.consignment_id)
        if criteria.consignment_id_from is not None:
            conditions.append("`consignment_id` >= %s")
            arguments.append(criteria.consignment_id_from)
        if criteria.consignment_id_to is not None:
            conditions.append("`consignment_id` <= %s")
            arguments.append(criteria.consignment_id_to)
        if criteria.clearance_port_id is not None:
            conditions.append("`clearance_port_id` = %s")
            arguments.append(criteria.clearance_port_id)
        if criteria.clearance_port_id_from is not None:
            conditions.append("`clearance_port_id` >= %s")
            arguments.append(criteria.clearance_port_id_from)
        if criteria.clearance_port_id_to is not None:
            conditions.append("`clearance_port_id` <= %s")
            arguments.append(criteria.clearance_port_id_to)
        if criteria.clearance_port_id_is_null is not None:
            conditions.append(
                "`clearance_port_id` IS NULL"
                if criteria.clearance_port_id_is_null
                else "`clearance_port_id` IS NOT NULL"
            )
        if criteria.mrn is not None:
            conditions.append("`mrn` = %s")
            arguments.append(criteria.mrn)
        if criteria.mrn_like is not None:
            conditions.append("`mrn` LIKE %s")
            arguments.append(criteria.mrn_like)
        if criteria.procedure_code is not None:
            conditions.append("`procedure_code` = %s")
            arguments.append(criteria.procedure_code)
        if criteria.procedure_code_like is not None:
            conditions.append("`procedure_code` LIKE %s")
            arguments.append(criteria.procedure_code_like)
        if criteria.regime is not None:
            conditions.append("`regime` = %s")
            arguments.append(criteria.regime)
        if criteria.regime_like is not None:
            conditions.append("`regime` LIKE %s")
            arguments.append(criteria.regime_like)
        if criteria.status is not None:
            conditions.append("`status` = %s")
            arguments.append(criteria.status)
        if criteria.status_like is not None:
            conditions.append("`status` LIKE %s")
            arguments.append(criteria.status_like)
        if criteria.lodged_at is not None:
            conditions.append("`lodged_at` = %s")
            arguments.append(criteria.lodged_at)
        if criteria.lodged_at_from is not None:
            conditions.append("`lodged_at` >= %s")
            arguments.append(criteria.lodged_at_from)
        if criteria.lodged_at_to is not None:
            conditions.append("`lodged_at` <= %s")
            arguments.append(criteria.lodged_at_to)
        if criteria.lodged_at_is_null is not None:
            conditions.append(
                "`lodged_at` IS NULL"
                if criteria.lodged_at_is_null
                else "`lodged_at` IS NOT NULL"
            )
        if criteria.released_at is not None:
            conditions.append("`released_at` = %s")
            arguments.append(criteria.released_at)
        if criteria.released_at_from is not None:
            conditions.append("`released_at` >= %s")
            arguments.append(criteria.released_at_from)
        if criteria.released_at_to is not None:
            conditions.append("`released_at` <= %s")
            arguments.append(criteria.released_at_to)
        if criteria.released_at_is_null is not None:
            conditions.append(
                "`released_at` IS NULL"
                if criteria.released_at_is_null
                else "`released_at` IS NOT NULL"
            )
        if criteria.duty_amount is not None:
            conditions.append("`duty_amount` = %s")
            arguments.append(criteria.duty_amount)
        if criteria.duty_amount_from is not None:
            conditions.append("`duty_amount` >= %s")
            arguments.append(criteria.duty_amount_from)
        if criteria.duty_amount_to is not None:
            conditions.append("`duty_amount` <= %s")
            arguments.append(criteria.duty_amount_to)
        if criteria.vat_amount is not None:
            conditions.append("`vat_amount` = %s")
            arguments.append(criteria.vat_amount)
        if criteria.vat_amount_from is not None:
            conditions.append("`vat_amount` >= %s")
            arguments.append(criteria.vat_amount_from)
        if criteria.vat_amount_to is not None:
            conditions.append("`vat_amount` <= %s")
            arguments.append(criteria.vat_amount_to)
        if criteria.total_payable is not None:
            conditions.append("`total_payable` = %s")
            arguments.append(criteria.total_payable)
        if criteria.total_payable_from is not None:
            conditions.append("`total_payable` >= %s")
            arguments.append(criteria.total_payable_from)
        if criteria.total_payable_to is not None:
            conditions.append("`total_payable` <= %s")
            arguments.append(criteria.total_payable_to)
        if criteria.total_payable_is_null is not None:
            conditions.append(
                "`total_payable` IS NULL"
                if criteria.total_payable_is_null
                else "`total_payable` IS NOT NULL"
            )
        if criteria.officer_remarks is not None:
            conditions.append("`officer_remarks` = %s")
            arguments.append(criteria.officer_remarks)
        if criteria.officer_remarks_like is not None:
            conditions.append("`officer_remarks` LIKE %s")
            arguments.append(criteria.officer_remarks_like)
        if criteria.officer_remarks_is_null is not None:
            conditions.append(
                "`officer_remarks` IS NULL"
                if criteria.officer_remarks_is_null
                else "`officer_remarks` IS NOT NULL"
            )

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: CustomsDeclarationPyMySQL,
    ) -> CustomsDeclarationRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
