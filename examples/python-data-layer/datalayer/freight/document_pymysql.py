# Code generated by tpl from freight.document. DO NOT EDIT.

"""The PyMySQL implementation of the `freight.document` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .document import Document, DocumentFilter, DocumentKey, DocumentOrder
from .document_repository import DocumentRepository

__all__ = ["DocumentPyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "freight.document"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `document_id`, `declaration_id`, `document_kind`, `file_name`, `media_type`, `byte_size`, `content_sha256`, `detached_signature`, `signature_thumbnail`, `page_preview`, `scanned_original`, `archive_bundle`, `uploaded_by`, `uploaded_at`, `retention_until` FROM `document`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `document_id`, `declaration_id`, `document_kind`, `file_name`, `media_type`, `byte_size`, `content_sha256`, `detached_signature`, `signature_thumbnail`, `page_preview`, `scanned_original`, `archive_bundle`, `uploaded_by`, `uploaded_at`, `retention_until` FROM `document` WHERE `document_id` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `document` (`declaration_id`, `document_kind`, `file_name`, `media_type`, `byte_size`, `content_sha256`, `detached_signature`, `signature_thumbnail`, `page_preview`, `scanned_original`, `archive_bundle`, `uploaded_by`, `uploaded_at`, `retention_until`) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `document` SET `declaration_id` = %s, `document_kind` = %s, `file_name` = %s, `media_type` = %s, `byte_size` = %s, `content_sha256` = %s, `detached_signature` = %s, `signature_thumbnail` = %s, `page_preview` = %s, `scanned_original` = %s, `archive_bundle` = %s, `uploaded_by` = %s, `uploaded_at` = %s, `retention_until` = %s WHERE `document_id` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `document` WHERE `document_id` = %s"


def _row(values: tuple[Any, ...]) -> Document:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return Document(
        document_id=values[0],
        declaration_id=values[1],
        document_kind=values[2],
        file_name=values[3],
        media_type=values[4],
        byte_size=values[5],
        content_sha256=values[6],
        detached_signature=values[7],
        signature_thumbnail=values[8],
        page_preview=values[9],
        scanned_original=values[10],
        archive_bundle=values[11],
        uploaded_by=values[12],
        uploaded_at=values[13],
        retention_until=values[14],
    )


class DocumentPyMySQL:
    """The PyMySQL data layer of `freight.document`.

    It names no base class. ``DocumentRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `freight.document` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: Document) -> None:
        """Insert `row`.

        The key is assigned by the server and is written back into `row`.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.declaration_id,
                    row.document_kind,
                    row.file_name,
                    row.media_type,
                    row.byte_size,
                    row.content_sha256,
                    row.detached_signature,
                    row.signature_thumbnail,
                    row.page_preview,
                    row.scanned_original,
                    row.archive_bundle,
                    row.uploaded_by,
                    row.uploaded_at,
                    row.retention_until,
                ),
            )
            row.document_id = cursor.lastrowid

    def read(self, key: DocumentKey) -> Document:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.document_id,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: Document) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.declaration_id,
                    row.document_kind,
                    row.file_name,
                    row.media_type,
                    row.byte_size,
                    row.content_sha256,
                    row.detached_signature,
                    row.signature_thumbnail,
                    row.page_preview,
                    row.scanned_original,
                    row.archive_bundle,
                    row.uploaded_by,
                    row.uploaded_at,
                    row.retention_until,
                    row.document_id,
                ),
            )
        return affected

    def delete(self, key: DocumentKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.document_id,
                ),
            )
        return affected

    def search(
        self,
        criteria: DocumentFilter,
        order: Sequence[DocumentOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[Document]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.document_id is not None:
            conditions.append("`document_id` = %s")
            arguments.append(criteria.document_id)
        if criteria.document_id_from is not None:
            conditions.append("`document_id` >= %s")
            arguments.append(criteria.document_id_from)
        if criteria.document_id_to is not None:
            conditions.append("`document_id` <= %s")
            arguments.append(criteria.document_id_to)
        if criteria.declaration_id is not None:
            conditions.append("`declaration_id` = %s")
            arguments.append(criteria.declaration_id)
        if criteria.declaration_id_from is not None:
            conditions.append("`declaration_id` >= %s")
            arguments.append(criteria.declaration_id_from)
        if criteria.declaration_id_to is not None:
            conditions.append("`declaration_id` <= %s")
            arguments.append(criteria.declaration_id_to)
        if criteria.declaration_id_is_null is not None:
            conditions.append(
                "`declaration_id` IS NULL"
                if criteria.declaration_id_is_null
                else "`declaration_id` IS NOT NULL"
            )
        if criteria.document_kind is not None:
            conditions.append("`document_kind` = %s")
            arguments.append(criteria.document_kind)
        if criteria.document_kind_like is not None:
            conditions.append("`document_kind` LIKE %s")
            arguments.append(criteria.document_kind_like)
        if criteria.file_name is not None:
            conditions.append("`file_name` = %s")
            arguments.append(criteria.file_name)
        if criteria.file_name_like is not None:
            conditions.append("`file_name` LIKE %s")
            arguments.append(criteria.file_name_like)
        if criteria.media_type is not None:
            conditions.append("`media_type` = %s")
            arguments.append(criteria.media_type)
        if criteria.media_type_like is not None:
            conditions.append("`media_type` LIKE %s")
            arguments.append(criteria.media_type_like)
        if criteria.byte_size is not None:
            conditions.append("`byte_size` = %s")
            arguments.append(criteria.byte_size)
        if criteria.byte_size_from is not None:
            conditions.append("`byte_size` >= %s")
            arguments.append(criteria.byte_size_from)
        if criteria.byte_size_to is not None:
            conditions.append("`byte_size` <= %s")
            arguments.append(criteria.byte_size_to)
        if criteria.content_sha256 is not None:
            conditions.append("`content_sha256` = %s")
            arguments.append(criteria.content_sha256)
        if criteria.detached_signature is not None:
            conditions.append("`detached_signature` = %s")
            arguments.append(criteria.detached_signature)
        if criteria.detached_signature_is_null is not None:
            conditions.append(
                "`detached_signature` IS NULL"
                if criteria.detached_signature_is_null
                else "`detached_signature` IS NOT NULL"
            )
        if criteria.signature_thumbnail is not None:
            conditions.append("`signature_thumbnail` = %s")
            arguments.append(criteria.signature_thumbnail)
        if criteria.signature_thumbnail_is_null is not None:
            conditions.append(
                "`signature_thumbnail` IS NULL"
                if criteria.signature_thumbnail_is_null
                else "`signature_thumbnail` IS NOT NULL"
            )
        if criteria.page_preview is not None:
            conditions.append("`page_preview` = %s")
            arguments.append(criteria.page_preview)
        if criteria.page_preview_is_null is not None:
            conditions.append(
                "`page_preview` IS NULL"
                if criteria.page_preview_is_null
                else "`page_preview` IS NOT NULL"
            )
        if criteria.scanned_original is not None:
            conditions.append("`scanned_original` = %s")
            arguments.append(criteria.scanned_original)
        if criteria.scanned_original_is_null is not None:
            conditions.append(
                "`scanned_original` IS NULL"
                if criteria.scanned_original_is_null
                else "`scanned_original` IS NOT NULL"
            )
        if criteria.archive_bundle is not None:
            conditions.append("`archive_bundle` = %s")
            arguments.append(criteria.archive_bundle)
        if criteria.archive_bundle_is_null is not None:
            conditions.append(
                "`archive_bundle` IS NULL"
                if criteria.archive_bundle_is_null
                else "`archive_bundle` IS NOT NULL"
            )
        if criteria.uploaded_by is not None:
            conditions.append("`uploaded_by` = %s")
            arguments.append(criteria.uploaded_by)
        if criteria.uploaded_by_like is not None:
            conditions.append("`uploaded_by` LIKE %s")
            arguments.append(criteria.uploaded_by_like)
        if criteria.uploaded_at is not None:
            conditions.append("`uploaded_at` = %s")
            arguments.append(criteria.uploaded_at)
        if criteria.uploaded_at_from is not None:
            conditions.append("`uploaded_at` >= %s")
            arguments.append(criteria.uploaded_at_from)
        if criteria.uploaded_at_to is not None:
            conditions.append("`uploaded_at` <= %s")
            arguments.append(criteria.uploaded_at_to)
        if criteria.retention_until is not None:
            conditions.append("`retention_until` = %s")
            arguments.append(criteria.retention_until)
        if criteria.retention_until_from is not None:
            conditions.append("`retention_until` >= %s")
            arguments.append(criteria.retention_until_from)
        if criteria.retention_until_to is not None:
            conditions.append("`retention_until` <= %s")
            arguments.append(criteria.retention_until_to)

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: DocumentPyMySQL,
    ) -> DocumentRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
