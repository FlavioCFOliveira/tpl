# Code generated by tpl from freight.document. DO NOT EDIT.

"""The `freight.document` row, the key that addresses it, the columns an ordering may
name, and the filter a search takes.
"""

from __future__ import annotations

import dataclasses
import datetime
import enum
from collections.abc import Mapping
from typing import Final

from ._core import Direction, UnknownColumn

__all__ = [
    "Document",
    "DocumentColumn",
    "DocumentFilter",
    "DocumentKey",
    "DocumentOrder",
]


@dataclasses.dataclass
class Document:
    """One row of `freight.document`.

    The dataclass is mutable, because :meth:`create` writes a server-assigned
    key back into it.
    """

    # The table's own comment, as the catalogue carries it:
    # Documentos digitalizados e assinados que acompanham cada expedição

    #: `document_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    #:
    #: It is part of the primary key.
    document_id: int
    #: `declaration_id`, declared `bigint(20) unsigned` NULL.
    #:
    #: NULL for a document filed against the consignment rather than a declaration
    declaration_id: int | None
    #: `document_kind`, declared `enum('Bill of lading','Commercial invoice','Packing list','Certificate of origin','Phytosanitary certificate','Dangerous goods declaration','Delivery order')` NOT NULL.
    document_kind: str
    #: `file_name`, declared `varchar(255)` NOT NULL.
    file_name: str
    #: `media_type`, declared `varchar(120)` NOT NULL.
    media_type: str
    #: `byte_size`, declared `int(10) unsigned` NOT NULL.
    byte_size: int
    #: `content_sha256`, declared `binary(32)` NOT NULL.
    #:
    #: It is part of a unique index.
    #:
    #: Fixed-width binary: SHA-256 of the stored bytes
    content_sha256: bytes
    #: `detached_signature`, declared `varbinary(512)` NULL.
    #:
    #: Variable-width binary: CMS detached signature
    detached_signature: bytes | None
    #: `signature_thumbnail`, declared `tinyblob` NULL.
    #:
    #: Rendered signature strip, always small
    signature_thumbnail: bytes | None
    #: `page_preview`, declared `blob` NULL.
    #:
    #: First-page raster preview
    page_preview: bytes | None
    #: `scanned_original`, declared `mediumblob` NULL.
    #:
    #: Full colour scan of the paper original
    scanned_original: bytes | None
    #: `archive_bundle`, declared `longblob` NULL.
    #:
    #: Signed archival package retained for the statutory period
    archive_bundle: bytes | None
    #: `uploaded_by`, declared `varchar(80)` NOT NULL.
    uploaded_by: str
    #: `uploaded_at`, declared `datetime(6)` NOT NULL.
    uploaded_at: datetime.datetime
    #: `retention_until`, declared `date` NOT NULL.
    #:
    #: Expression default: ten-year statutory retention
    retention_until: datetime.date

    def key(self) -> DocumentKey:
        """Return the primary key of the row."""
        return DocumentKey(
            document_id=self.document_id,
        )


@dataclasses.dataclass(frozen=True)
class DocumentKey:
    """The primary key of `freight.document`.

    Frozen, so that it hashes and compares by value. Every type this example's
    mapping produces is hashable, so no key of these three schemas is refused
    for that reason.
    """

    #: `document_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    document_id: int


class DocumentColumn(enum.Enum):
    """One column of `freight.document`.

    A search accepts an ordering term over these members and over nothing else,
    so an ordering can carry no SQL of its own: :attr:`sql` answers from a
    closed mapping this template wrote, and :meth:`from_name` is the one door a
    name a caller supplied can come through.

    A member's value is the column's name as the catalogue carries it, which is
    what :meth:`from_name` matches on and what ``member.value`` returns.
    """

    #: `document_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    DOCUMENT_ID = "document_id"
    #: `declaration_id`, declared `bigint(20) unsigned` NULL.
    DECLARATION_ID = "declaration_id"
    #: `document_kind`, declared `enum('Bill of lading','Commercial invoice','Packing list','Certificate of origin','Phytosanitary certificate','Dangerous goods declaration','Delivery order')` NOT NULL.
    DOCUMENT_KIND = "document_kind"
    #: `file_name`, declared `varchar(255)` NOT NULL.
    FILE_NAME = "file_name"
    #: `media_type`, declared `varchar(120)` NOT NULL.
    MEDIA_TYPE = "media_type"
    #: `byte_size`, declared `int(10) unsigned` NOT NULL.
    BYTE_SIZE = "byte_size"
    #: `content_sha256`, declared `binary(32)` NOT NULL.
    CONTENT_SHA256 = "content_sha256"
    #: `detached_signature`, declared `varbinary(512)` NULL.
    DETACHED_SIGNATURE = "detached_signature"
    #: `signature_thumbnail`, declared `tinyblob` NULL.
    SIGNATURE_THUMBNAIL = "signature_thumbnail"
    #: `page_preview`, declared `blob` NULL.
    PAGE_PREVIEW = "page_preview"
    #: `scanned_original`, declared `mediumblob` NULL.
    SCANNED_ORIGINAL = "scanned_original"
    #: `archive_bundle`, declared `longblob` NULL.
    ARCHIVE_BUNDLE = "archive_bundle"
    #: `uploaded_by`, declared `varchar(80)` NOT NULL.
    UPLOADED_BY = "uploaded_by"
    #: `uploaded_at`, declared `datetime(6)` NOT NULL.
    UPLOADED_AT = "uploaded_at"
    #: `retention_until`, declared `date` NOT NULL.
    RETENTION_UNTIL = "retention_until"

    @property
    def sql(self) -> str:
        """The column as a statement's text names it: quoted for MariaDB."""
        return _COLUMNS[self]

    @classmethod
    def from_name(cls, name: str) -> DocumentColumn:
        """Read a column a caller supplied as text.

        Raises:
            UnknownColumn: for every name the table does not carry. That
                refusal is what keeps a caller's text out of a statement.
        """
        try:
            return cls(name)
        except ValueError:
            raise UnknownColumn("freight.document", name) from None


#: The one place a column becomes SQL. It is closed over the enumeration above,
#: and every identifier in it was quoted at render time by `tpl`, so no value a
#: caller supplies can reach a statement's text through an ordering term.
_COLUMNS: Final[Mapping[DocumentColumn, str]] = {
    DocumentColumn.DOCUMENT_ID: "`document_id`",
    DocumentColumn.DECLARATION_ID: "`declaration_id`",
    DocumentColumn.DOCUMENT_KIND: "`document_kind`",
    DocumentColumn.FILE_NAME: "`file_name`",
    DocumentColumn.MEDIA_TYPE: "`media_type`",
    DocumentColumn.BYTE_SIZE: "`byte_size`",
    DocumentColumn.CONTENT_SHA256: "`content_sha256`",
    DocumentColumn.DETACHED_SIGNATURE: "`detached_signature`",
    DocumentColumn.SIGNATURE_THUMBNAIL: "`signature_thumbnail`",
    DocumentColumn.PAGE_PREVIEW: "`page_preview`",
    DocumentColumn.SCANNED_ORIGINAL: "`scanned_original`",
    DocumentColumn.ARCHIVE_BUNDLE: "`archive_bundle`",
    DocumentColumn.UPLOADED_BY: "`uploaded_by`",
    DocumentColumn.UPLOADED_AT: "`uploaded_at`",
    DocumentColumn.RETENTION_UNTIL: "`retention_until`",
}


@dataclasses.dataclass(frozen=True)
class DocumentOrder:
    """One ordering term of a search over `freight.document`."""

    #: The column to order by.
    column: DocumentColumn
    #: The direction to order in.
    direction: Direction = Direction.ASCENDING


@dataclasses.dataclass
class DocumentFilter:
    """What narrows a search over `freight.document`.

    Every field is optional: a field left ``None`` adds no condition, and the
    conditions a search does build are joined with ``AND``. The default
    instance is the filter that matches every row.

    A nullable column's field is ``None`` for *no condition* and never for
    *is NULL*; that question is asked through the ``…_is_null`` field such a
    column also gets, so the two meanings never collide.
    """

    #: Where set, requires `document_id` to equal it.
    document_id: int | None = None
    #: Where set, requires `document_id` to be at least it.
    document_id_from: int | None = None
    #: Where set, requires `document_id` to be at most it.
    document_id_to: int | None = None
    #: Where set, requires `declaration_id` to equal it.
    declaration_id: int | None = None
    #: Where set, requires `declaration_id` to be at least it.
    declaration_id_from: int | None = None
    #: Where set, requires `declaration_id` to be at most it.
    declaration_id_to: int | None = None
    #: Where True, requires `declaration_id` to be NULL; where False, requires it not to be.
    declaration_id_is_null: bool | None = None
    #: Where set, requires `document_kind` to equal it.
    document_kind: str | None = None
    #: Where set, requires `document_kind` to match it, as LIKE matches.
    document_kind_like: str | None = None
    #: Where set, requires `file_name` to equal it.
    file_name: str | None = None
    #: Where set, requires `file_name` to match it, as LIKE matches.
    file_name_like: str | None = None
    #: Where set, requires `media_type` to equal it.
    media_type: str | None = None
    #: Where set, requires `media_type` to match it, as LIKE matches.
    media_type_like: str | None = None
    #: Where set, requires `byte_size` to equal it.
    byte_size: int | None = None
    #: Where set, requires `byte_size` to be at least it.
    byte_size_from: int | None = None
    #: Where set, requires `byte_size` to be at most it.
    byte_size_to: int | None = None
    #: Where set, requires `content_sha256` to equal it.
    content_sha256: bytes | None = None
    #: Where set, requires `detached_signature` to equal it.
    detached_signature: bytes | None = None
    #: Where True, requires `detached_signature` to be NULL; where False, requires it not to be.
    detached_signature_is_null: bool | None = None
    #: Where set, requires `signature_thumbnail` to equal it.
    signature_thumbnail: bytes | None = None
    #: Where True, requires `signature_thumbnail` to be NULL; where False, requires it not to be.
    signature_thumbnail_is_null: bool | None = None
    #: Where set, requires `page_preview` to equal it.
    page_preview: bytes | None = None
    #: Where True, requires `page_preview` to be NULL; where False, requires it not to be.
    page_preview_is_null: bool | None = None
    #: Where set, requires `scanned_original` to equal it.
    scanned_original: bytes | None = None
    #: Where True, requires `scanned_original` to be NULL; where False, requires it not to be.
    scanned_original_is_null: bool | None = None
    #: Where set, requires `archive_bundle` to equal it.
    archive_bundle: bytes | None = None
    #: Where True, requires `archive_bundle` to be NULL; where False, requires it not to be.
    archive_bundle_is_null: bool | None = None
    #: Where set, requires `uploaded_by` to equal it.
    uploaded_by: str | None = None
    #: Where set, requires `uploaded_by` to match it, as LIKE matches.
    uploaded_by_like: str | None = None
    #: Where set, requires `uploaded_at` to equal it.
    uploaded_at: datetime.datetime | None = None
    #: Where set, requires `uploaded_at` to be at least it.
    uploaded_at_from: datetime.datetime | None = None
    #: Where set, requires `uploaded_at` to be at most it.
    uploaded_at_to: datetime.datetime | None = None
    #: Where set, requires `retention_until` to equal it.
    retention_until: datetime.date | None = None
    #: Where set, requires `retention_until` to be at least it.
    retention_until_from: datetime.date | None = None
    #: Where set, requires `retention_until` to be at most it.
    retention_until_to: datetime.date | None = None

    def is_empty(self) -> bool:
        """Is no field set?

        A search over an empty filter builds no ``WHERE`` clause and matches
        every row of the table.
        """
        return (
            self.document_id is None
            and self.document_id_from is None
            and self.document_id_to is None
            and self.declaration_id is None
            and self.declaration_id_from is None
            and self.declaration_id_to is None
            and self.declaration_id_is_null is None
            and self.document_kind is None
            and self.document_kind_like is None
            and self.file_name is None
            and self.file_name_like is None
            and self.media_type is None
            and self.media_type_like is None
            and self.byte_size is None
            and self.byte_size_from is None
            and self.byte_size_to is None
            and self.content_sha256 is None
            and self.detached_signature is None
            and self.detached_signature_is_null is None
            and self.signature_thumbnail is None
            and self.signature_thumbnail_is_null is None
            and self.page_preview is None
            and self.page_preview_is_null is None
            and self.scanned_original is None
            and self.scanned_original_is_null is None
            and self.archive_bundle is None
            and self.archive_bundle_is_null is None
            and self.uploaded_by is None
            and self.uploaded_by_like is None
            and self.uploaded_at is None
            and self.uploaded_at_from is None
            and self.uploaded_at_to is None
            and self.retention_until is None
            and self.retention_until_from is None
            and self.retention_until_to is None
        )
