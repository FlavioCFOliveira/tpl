# Code generated by tpl from freight.customer. DO NOT EDIT.

"""The PyMySQL implementation of the `freight.customer` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .customer import Customer, CustomerFilter, CustomerKey, CustomerOrder
from .customer_repository import CustomerRepository

__all__ = ["CustomerPyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "freight.customer"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `customer_id`, `legal_name`, `trading_name`, `vat_number`, `eori_number`, `country_code`, `legacy_account_code`, `portal_uuid`, `last_login_ipv4`, `last_login_ipv6`, `credit_limit_eur`, `outstanding_eur`, `credit_headroom_eur`, `risk_score`, `payment_terms_days`, `preferences`, `notification_flags`, `is_active`, `onboarded_on`, `review_due_on`, `contact_note`, `created_at`, `updated_at` FROM `customer`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `customer_id`, `legal_name`, `trading_name`, `vat_number`, `eori_number`, `country_code`, `legacy_account_code`, `portal_uuid`, `last_login_ipv4`, `last_login_ipv6`, `credit_limit_eur`, `outstanding_eur`, `credit_headroom_eur`, `risk_score`, `payment_terms_days`, `preferences`, `notification_flags`, `is_active`, `onboarded_on`, `review_due_on`, `contact_note`, `created_at`, `updated_at` FROM `customer` WHERE `customer_id` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `customer` (`legal_name`, `trading_name`, `vat_number`, `eori_number`, `country_code`, `legacy_account_code`, `portal_uuid`, `last_login_ipv4`, `last_login_ipv6`, `credit_limit_eur`, `outstanding_eur`, `risk_score`, `payment_terms_days`, `preferences`, `notification_flags`, `is_active`, `onboarded_on`, `review_due_on`, `contact_note`, `created_at`, `updated_at`) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `customer` SET `legal_name` = %s, `trading_name` = %s, `vat_number` = %s, `eori_number` = %s, `country_code` = %s, `legacy_account_code` = %s, `portal_uuid` = %s, `last_login_ipv4` = %s, `last_login_ipv6` = %s, `credit_limit_eur` = %s, `outstanding_eur` = %s, `risk_score` = %s, `payment_terms_days` = %s, `preferences` = %s, `notification_flags` = %s, `is_active` = %s, `onboarded_on` = %s, `review_due_on` = %s, `contact_note` = %s, `created_at` = %s, `updated_at` = %s WHERE `customer_id` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `customer` WHERE `customer_id` = %s"


def _row(values: tuple[Any, ...]) -> Customer:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return Customer(
        customer_id=values[0],
        legal_name=values[1],
        trading_name=values[2],
        vat_number=values[3],
        eori_number=values[4],
        country_code=values[5],
        legacy_account_code=values[6],
        portal_uuid=values[7],
        last_login_ipv4=values[8],
        last_login_ipv6=values[9],
        credit_limit_eur=values[10],
        outstanding_eur=values[11],
        credit_headroom_eur=values[12],
        risk_score=values[13],
        payment_terms_days=values[14],
        preferences=values[15],
        notification_flags=values[16],
        is_active=values[17],
        onboarded_on=values[18],
        review_due_on=values[19],
        contact_note=values[20],
        created_at=values[21],
        updated_at=values[22],
    )


class CustomerPyMySQL:
    """The PyMySQL data layer of `freight.customer`.

    It names no base class. ``CustomerRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `freight.customer` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: Customer) -> None:
        """Insert `row`.

        The key is assigned by the server and is written back into `row`.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.legal_name,
                    row.trading_name,
                    row.vat_number,
                    row.eori_number,
                    row.country_code,
                    row.legacy_account_code,
                    row.portal_uuid,
                    row.last_login_ipv4,
                    row.last_login_ipv6,
                    row.credit_limit_eur,
                    row.outstanding_eur,
                    row.risk_score,
                    row.payment_terms_days,
                    row.preferences,
                    row.notification_flags,
                    row.is_active,
                    row.onboarded_on,
                    row.review_due_on,
                    row.contact_note,
                    row.created_at,
                    row.updated_at,
                ),
            )
            row.customer_id = cursor.lastrowid

    def read(self, key: CustomerKey) -> Customer:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.customer_id,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: Customer) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.legal_name,
                    row.trading_name,
                    row.vat_number,
                    row.eori_number,
                    row.country_code,
                    row.legacy_account_code,
                    row.portal_uuid,
                    row.last_login_ipv4,
                    row.last_login_ipv6,
                    row.credit_limit_eur,
                    row.outstanding_eur,
                    row.risk_score,
                    row.payment_terms_days,
                    row.preferences,
                    row.notification_flags,
                    row.is_active,
                    row.onboarded_on,
                    row.review_due_on,
                    row.contact_note,
                    row.created_at,
                    row.updated_at,
                    row.customer_id,
                ),
            )
        return affected

    def delete(self, key: CustomerKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.customer_id,
                ),
            )
        return affected

    def search(
        self,
        criteria: CustomerFilter,
        order: Sequence[CustomerOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[Customer]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.customer_id is not None:
            conditions.append("`customer_id` = %s")
            arguments.append(criteria.customer_id)
        if criteria.customer_id_from is not None:
            conditions.append("`customer_id` >= %s")
            arguments.append(criteria.customer_id_from)
        if criteria.customer_id_to is not None:
            conditions.append("`customer_id` <= %s")
            arguments.append(criteria.customer_id_to)
        if criteria.legal_name is not None:
            conditions.append("`legal_name` = %s")
            arguments.append(criteria.legal_name)
        if criteria.legal_name_like is not None:
            conditions.append("`legal_name` LIKE %s")
            arguments.append(criteria.legal_name_like)
        if criteria.trading_name is not None:
            conditions.append("`trading_name` = %s")
            arguments.append(criteria.trading_name)
        if criteria.trading_name_like is not None:
            conditions.append("`trading_name` LIKE %s")
            arguments.append(criteria.trading_name_like)
        if criteria.trading_name_is_null is not None:
            conditions.append(
                "`trading_name` IS NULL"
                if criteria.trading_name_is_null
                else "`trading_name` IS NOT NULL"
            )
        if criteria.vat_number is not None:
            conditions.append("`vat_number` = %s")
            arguments.append(criteria.vat_number)
        if criteria.vat_number_like is not None:
            conditions.append("`vat_number` LIKE %s")
            arguments.append(criteria.vat_number_like)
        if criteria.eori_number is not None:
            conditions.append("`eori_number` = %s")
            arguments.append(criteria.eori_number)
        if criteria.eori_number_like is not None:
            conditions.append("`eori_number` LIKE %s")
            arguments.append(criteria.eori_number_like)
        if criteria.eori_number_is_null is not None:
            conditions.append(
                "`eori_number` IS NULL"
                if criteria.eori_number_is_null
                else "`eori_number` IS NOT NULL"
            )
        if criteria.country_code is not None:
            conditions.append("`country_code` = %s")
            arguments.append(criteria.country_code)
        if criteria.country_code_like is not None:
            conditions.append("`country_code` LIKE %s")
            arguments.append(criteria.country_code_like)
        if criteria.legacy_account_code is not None:
            conditions.append("`legacy_account_code` = %s")
            arguments.append(criteria.legacy_account_code)
        if criteria.legacy_account_code_like is not None:
            conditions.append("`legacy_account_code` LIKE %s")
            arguments.append(criteria.legacy_account_code_like)
        if criteria.legacy_account_code_is_null is not None:
            conditions.append(
                "`legacy_account_code` IS NULL"
                if criteria.legacy_account_code_is_null
                else "`legacy_account_code` IS NOT NULL"
            )
        if criteria.portal_uuid is not None:
            conditions.append("`portal_uuid` = %s")
            arguments.append(criteria.portal_uuid)
        if criteria.last_login_ipv4 is not None:
            conditions.append("`last_login_ipv4` = %s")
            arguments.append(criteria.last_login_ipv4)
        if criteria.last_login_ipv4_is_null is not None:
            conditions.append(
                "`last_login_ipv4` IS NULL"
                if criteria.last_login_ipv4_is_null
                else "`last_login_ipv4` IS NOT NULL"
            )
        if criteria.last_login_ipv6 is not None:
            conditions.append("`last_login_ipv6` = %s")
            arguments.append(criteria.last_login_ipv6)
        if criteria.last_login_ipv6_is_null is not None:
            conditions.append(
                "`last_login_ipv6` IS NULL"
                if criteria.last_login_ipv6_is_null
                else "`last_login_ipv6` IS NOT NULL"
            )
        if criteria.credit_limit_eur is not None:
            conditions.append("`credit_limit_eur` = %s")
            arguments.append(criteria.credit_limit_eur)
        if criteria.credit_limit_eur_from is not None:
            conditions.append("`credit_limit_eur` >= %s")
            arguments.append(criteria.credit_limit_eur_from)
        if criteria.credit_limit_eur_to is not None:
            conditions.append("`credit_limit_eur` <= %s")
            arguments.append(criteria.credit_limit_eur_to)
        if criteria.outstanding_eur is not None:
            conditions.append("`outstanding_eur` = %s")
            arguments.append(criteria.outstanding_eur)
        if criteria.outstanding_eur_from is not None:
            conditions.append("`outstanding_eur` >= %s")
            arguments.append(criteria.outstanding_eur_from)
        if criteria.outstanding_eur_to is not None:
            conditions.append("`outstanding_eur` <= %s")
            arguments.append(criteria.outstanding_eur_to)
        if criteria.credit_headroom_eur is not None:
            conditions.append("`credit_headroom_eur` = %s")
            arguments.append(criteria.credit_headroom_eur)
        if criteria.credit_headroom_eur_from is not None:
            conditions.append("`credit_headroom_eur` >= %s")
            arguments.append(criteria.credit_headroom_eur_from)
        if criteria.credit_headroom_eur_to is not None:
            conditions.append("`credit_headroom_eur` <= %s")
            arguments.append(criteria.credit_headroom_eur_to)
        if criteria.credit_headroom_eur_is_null is not None:
            conditions.append(
                "`credit_headroom_eur` IS NULL"
                if criteria.credit_headroom_eur_is_null
                else "`credit_headroom_eur` IS NOT NULL"
            )
        if criteria.risk_score is not None:
            conditions.append("`risk_score` = %s")
            arguments.append(criteria.risk_score)
        if criteria.risk_score_from is not None:
            conditions.append("`risk_score` >= %s")
            arguments.append(criteria.risk_score_from)
        if criteria.risk_score_to is not None:
            conditions.append("`risk_score` <= %s")
            arguments.append(criteria.risk_score_to)
        if criteria.risk_score_is_null is not None:
            conditions.append(
                "`risk_score` IS NULL"
                if criteria.risk_score_is_null
                else "`risk_score` IS NOT NULL"
            )
        if criteria.payment_terms_days is not None:
            conditions.append("`payment_terms_days` = %s")
            arguments.append(criteria.payment_terms_days)
        if criteria.payment_terms_days_from is not None:
            conditions.append("`payment_terms_days` >= %s")
            arguments.append(criteria.payment_terms_days_from)
        if criteria.payment_terms_days_to is not None:
            conditions.append("`payment_terms_days` <= %s")
            arguments.append(criteria.payment_terms_days_to)
        if criteria.preferences is not None:
            conditions.append("`preferences` = %s")
            arguments.append(criteria.preferences)
        if criteria.preferences_like is not None:
            conditions.append("`preferences` LIKE %s")
            arguments.append(criteria.preferences_like)
        if criteria.preferences_is_null is not None:
            conditions.append(
                "`preferences` IS NULL"
                if criteria.preferences_is_null
                else "`preferences` IS NOT NULL"
            )
        if criteria.notification_flags is not None:
            conditions.append("`notification_flags` = %s")
            arguments.append(criteria.notification_flags)
        if criteria.notification_flags_from is not None:
            conditions.append("`notification_flags` >= %s")
            arguments.append(criteria.notification_flags_from)
        if criteria.notification_flags_to is not None:
            conditions.append("`notification_flags` <= %s")
            arguments.append(criteria.notification_flags_to)
        if criteria.is_active is not None:
            conditions.append("`is_active` = %s")
            arguments.append(criteria.is_active)
        if criteria.is_active_from is not None:
            conditions.append("`is_active` >= %s")
            arguments.append(criteria.is_active_from)
        if criteria.is_active_to is not None:
            conditions.append("`is_active` <= %s")
            arguments.append(criteria.is_active_to)
        if criteria.onboarded_on is not None:
            conditions.append("`onboarded_on` = %s")
            arguments.append(criteria.onboarded_on)
        if criteria.onboarded_on_from is not None:
            conditions.append("`onboarded_on` >= %s")
            arguments.append(criteria.onboarded_on_from)
        if criteria.onboarded_on_to is not None:
            conditions.append("`onboarded_on` <= %s")
            arguments.append(criteria.onboarded_on_to)
        if criteria.review_due_on is not None:
            conditions.append("`review_due_on` = %s")
            arguments.append(criteria.review_due_on)
        if criteria.review_due_on_from is not None:
            conditions.append("`review_due_on` >= %s")
            arguments.append(criteria.review_due_on_from)
        if criteria.review_due_on_to is not None:
            conditions.append("`review_due_on` <= %s")
            arguments.append(criteria.review_due_on_to)
        if criteria.contact_note is not None:
            conditions.append("`contact_note` = %s")
            arguments.append(criteria.contact_note)
        if criteria.contact_note_like is not None:
            conditions.append("`contact_note` LIKE %s")
            arguments.append(criteria.contact_note_like)
        if criteria.contact_note_is_null is not None:
            conditions.append(
                "`contact_note` IS NULL"
                if criteria.contact_note_is_null
                else "`contact_note` IS NOT NULL"
            )
        if criteria.created_at is not None:
            conditions.append("`created_at` = %s")
            arguments.append(criteria.created_at)
        if criteria.created_at_from is not None:
            conditions.append("`created_at` >= %s")
            arguments.append(criteria.created_at_from)
        if criteria.created_at_to is not None:
            conditions.append("`created_at` <= %s")
            arguments.append(criteria.created_at_to)
        if criteria.updated_at is not None:
            conditions.append("`updated_at` = %s")
            arguments.append(criteria.updated_at)
        if criteria.updated_at_from is not None:
            conditions.append("`updated_at` >= %s")
            arguments.append(criteria.updated_at_from)
        if criteria.updated_at_to is not None:
            conditions.append("`updated_at` <= %s")
            arguments.append(criteria.updated_at_to)

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: CustomerPyMySQL,
    ) -> CustomerRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
