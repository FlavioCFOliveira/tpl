# Code generated by tpl from freight.audit_event. DO NOT EDIT.

"""The PyMySQL implementation of the `freight.audit_event` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .audit_event import AuditEvent, AuditEventFilter, AuditEventKey, AuditEventOrder
from .audit_event_repository import AuditEventRepository

__all__ = ["AuditEventPyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "freight.audit_event"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `audit_event_id`, `entity_name`, `entity_key`, `action`, `trigger_timing`, `changed_column`, `old_value`, `new_value`, `actor`, `client_ipv4`, `client_ipv6`, `correlation_uuid`, `observed_at`, `detail` FROM `audit_event`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `audit_event_id`, `entity_name`, `entity_key`, `action`, `trigger_timing`, `changed_column`, `old_value`, `new_value`, `actor`, `client_ipv4`, `client_ipv6`, `correlation_uuid`, `observed_at`, `detail` FROM `audit_event` WHERE `audit_event_id` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `audit_event` (`entity_name`, `entity_key`, `action`, `trigger_timing`, `changed_column`, `old_value`, `new_value`, `actor`, `client_ipv4`, `client_ipv6`, `correlation_uuid`, `observed_at`, `detail`) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `audit_event` SET `entity_name` = %s, `entity_key` = %s, `action` = %s, `trigger_timing` = %s, `changed_column` = %s, `old_value` = %s, `new_value` = %s, `actor` = %s, `client_ipv4` = %s, `client_ipv6` = %s, `correlation_uuid` = %s, `observed_at` = %s, `detail` = %s WHERE `audit_event_id` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `audit_event` WHERE `audit_event_id` = %s"


def _row(values: tuple[Any, ...]) -> AuditEvent:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return AuditEvent(
        audit_event_id=values[0],
        entity_name=values[1],
        entity_key=values[2],
        action=values[3],
        trigger_timing=values[4],
        changed_column=values[5],
        old_value=values[6],
        new_value=values[7],
        actor=values[8],
        client_ipv4=values[9],
        client_ipv6=values[10],
        correlation_uuid=values[11],
        observed_at=values[12],
        detail=values[13],
    )


class AuditEventPyMySQL:
    """The PyMySQL data layer of `freight.audit_event`.

    It names no base class. ``AuditEventRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `freight.audit_event` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: AuditEvent) -> None:
        """Insert `row`.

        The key is assigned by the server and is written back into `row`.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.entity_name,
                    row.entity_key,
                    row.action,
                    row.trigger_timing,
                    row.changed_column,
                    row.old_value,
                    row.new_value,
                    row.actor,
                    row.client_ipv4,
                    row.client_ipv6,
                    row.correlation_uuid,
                    row.observed_at,
                    row.detail,
                ),
            )
            row.audit_event_id = cursor.lastrowid

    def read(self, key: AuditEventKey) -> AuditEvent:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.audit_event_id,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: AuditEvent) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.entity_name,
                    row.entity_key,
                    row.action,
                    row.trigger_timing,
                    row.changed_column,
                    row.old_value,
                    row.new_value,
                    row.actor,
                    row.client_ipv4,
                    row.client_ipv6,
                    row.correlation_uuid,
                    row.observed_at,
                    row.detail,
                    row.audit_event_id,
                ),
            )
        return affected

    def delete(self, key: AuditEventKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.audit_event_id,
                ),
            )
        return affected

    def search(
        self,
        criteria: AuditEventFilter,
        order: Sequence[AuditEventOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[AuditEvent]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.audit_event_id is not None:
            conditions.append("`audit_event_id` = %s")
            arguments.append(criteria.audit_event_id)
        if criteria.audit_event_id_from is not None:
            conditions.append("`audit_event_id` >= %s")
            arguments.append(criteria.audit_event_id_from)
        if criteria.audit_event_id_to is not None:
            conditions.append("`audit_event_id` <= %s")
            arguments.append(criteria.audit_event_id_to)
        if criteria.entity_name is not None:
            conditions.append("`entity_name` = %s")
            arguments.append(criteria.entity_name)
        if criteria.entity_name_like is not None:
            conditions.append("`entity_name` LIKE %s")
            arguments.append(criteria.entity_name_like)
        if criteria.entity_key is not None:
            conditions.append("`entity_key` = %s")
            arguments.append(criteria.entity_key)
        if criteria.entity_key_like is not None:
            conditions.append("`entity_key` LIKE %s")
            arguments.append(criteria.entity_key_like)
        if criteria.action is not None:
            conditions.append("`action` = %s")
            arguments.append(criteria.action)
        if criteria.action_like is not None:
            conditions.append("`action` LIKE %s")
            arguments.append(criteria.action_like)
        if criteria.trigger_timing is not None:
            conditions.append("`trigger_timing` = %s")
            arguments.append(criteria.trigger_timing)
        if criteria.trigger_timing_like is not None:
            conditions.append("`trigger_timing` LIKE %s")
            arguments.append(criteria.trigger_timing_like)
        if criteria.changed_column is not None:
            conditions.append("`changed_column` = %s")
            arguments.append(criteria.changed_column)
        if criteria.changed_column_like is not None:
            conditions.append("`changed_column` LIKE %s")
            arguments.append(criteria.changed_column_like)
        if criteria.changed_column_is_null is not None:
            conditions.append(
                "`changed_column` IS NULL"
                if criteria.changed_column_is_null
                else "`changed_column` IS NOT NULL"
            )
        if criteria.old_value is not None:
            conditions.append("`old_value` = %s")
            arguments.append(criteria.old_value)
        if criteria.old_value_like is not None:
            conditions.append("`old_value` LIKE %s")
            arguments.append(criteria.old_value_like)
        if criteria.old_value_is_null is not None:
            conditions.append(
                "`old_value` IS NULL"
                if criteria.old_value_is_null
                else "`old_value` IS NOT NULL"
            )
        if criteria.new_value is not None:
            conditions.append("`new_value` = %s")
            arguments.append(criteria.new_value)
        if criteria.new_value_like is not None:
            conditions.append("`new_value` LIKE %s")
            arguments.append(criteria.new_value_like)
        if criteria.new_value_is_null is not None:
            conditions.append(
                "`new_value` IS NULL"
                if criteria.new_value_is_null
                else "`new_value` IS NOT NULL"
            )
        if criteria.actor is not None:
            conditions.append("`actor` = %s")
            arguments.append(criteria.actor)
        if criteria.actor_like is not None:
            conditions.append("`actor` LIKE %s")
            arguments.append(criteria.actor_like)
        if criteria.client_ipv4 is not None:
            conditions.append("`client_ipv4` = %s")
            arguments.append(criteria.client_ipv4)
        if criteria.client_ipv4_is_null is not None:
            conditions.append(
                "`client_ipv4` IS NULL"
                if criteria.client_ipv4_is_null
                else "`client_ipv4` IS NOT NULL"
            )
        if criteria.client_ipv6 is not None:
            conditions.append("`client_ipv6` = %s")
            arguments.append(criteria.client_ipv6)
        if criteria.client_ipv6_is_null is not None:
            conditions.append(
                "`client_ipv6` IS NULL"
                if criteria.client_ipv6_is_null
                else "`client_ipv6` IS NOT NULL"
            )
        if criteria.correlation_uuid is not None:
            conditions.append("`correlation_uuid` = %s")
            arguments.append(criteria.correlation_uuid)
        if criteria.correlation_uuid_is_null is not None:
            conditions.append(
                "`correlation_uuid` IS NULL"
                if criteria.correlation_uuid_is_null
                else "`correlation_uuid` IS NOT NULL"
            )
        if criteria.observed_at is not None:
            conditions.append("`observed_at` = %s")
            arguments.append(criteria.observed_at)
        if criteria.observed_at_from is not None:
            conditions.append("`observed_at` >= %s")
            arguments.append(criteria.observed_at_from)
        if criteria.observed_at_to is not None:
            conditions.append("`observed_at` <= %s")
            arguments.append(criteria.observed_at_to)
        if criteria.detail is not None:
            conditions.append("`detail` = %s")
            arguments.append(criteria.detail)
        if criteria.detail_like is not None:
            conditions.append("`detail` LIKE %s")
            arguments.append(criteria.detail_like)
        if criteria.detail_is_null is not None:
            conditions.append(
                "`detail` IS NULL"
                if criteria.detail_is_null
                else "`detail` IS NOT NULL"
            )

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: AuditEventPyMySQL,
    ) -> AuditEventRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
