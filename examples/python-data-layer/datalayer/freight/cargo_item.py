# Code generated by tpl from freight.cargo_item. DO NOT EDIT.

"""The `freight.cargo_item` row, the key that addresses it, the columns an ordering may
name, and the filter a search takes.
"""

from __future__ import annotations

import dataclasses
import decimal
import enum
from collections.abc import Mapping
from typing import Final

from ._core import Direction, UnknownColumn

__all__ = [
    "CargoItem",
    "CargoItemColumn",
    "CargoItemFilter",
    "CargoItemKey",
    "CargoItemOrder",
]


@dataclasses.dataclass
class CargoItem:
    """One row of `freight.cargo_item`.

    The dataclass is mutable, because :meth:`create` writes a server-assigned
    key back into it.
    """

    # The table's own comment, as the catalogue carries it:
    # Linhas de mercadoria de cada expedição

    #: `cargo_item_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    #:
    #: It is part of the primary key.
    cargo_item_id: int
    #: `consignment_id`, declared `bigint(20) unsigned` NOT NULL.
    #:
    #: It is part of a unique index.
    consignment_id: int
    #: `container_id`, declared `bigint(20) unsigned` NULL.
    #:
    #: NULL while the item is loose cargo awaiting consolidation
    container_id: int | None
    #: `line_number`, declared `smallint(5) unsigned` NOT NULL.
    #:
    #: It is part of a unique index.
    line_number: int
    #: `hs_code`, declared `varchar(12)` NOT NULL.
    #:
    #: Harmonised System commodity code
    hs_code: str
    #: `description`, declared `text` NOT NULL.
    #:
    #: Commercial description; indexed FULLTEXT for the tariff classification desk
    description: str
    #: `package_kind`, declared `enum('Pallet','Crate','Drum','Bale','Carton','Big bag','Roll','Loose')` NOT NULL.
    package_kind: str
    #: `package_count`, declared `mediumint(8) unsigned` NOT NULL.
    package_count: int
    #: `gross_weight_kg`, declared `decimal(12,3)` NOT NULL.
    gross_weight_kg: decimal.Decimal
    #: `net_weight_kg`, declared `decimal(12,3)` NOT NULL.
    net_weight_kg: decimal.Decimal
    #: `volume_cbm`, declared `decimal(10,3)` NOT NULL.
    volume_cbm: decimal.Decimal
    #: `chargeable_weight_kg`, declared `decimal(12,3)` NULL VIRTUAL GENERATED AS (greatest(`gross_weight_kg`,`volume_cbm` * 167)).
    #:
    #: Virtual generated column: IATA-style volumetric rule at 167 kg per cubic metre
    chargeable_weight_kg: decimal.Decimal | None
    #: `imdg_class`, declared `enum('Not regulated','Class 3, Flammable liquids','Class 8, Corrosives','Class 9, Miscellaneous dangerous goods')` NOT NULL.
    #:
    #: Note the commas inside three of the members
    imdg_class: str
    #: `un_number`, declared `smallint(5) unsigned` NULL.
    #:
    #: UN dangerous goods number, 1 to 3550
    un_number: int | None
    #: `origin_country`, declared `char(2)` NOT NULL.
    origin_country: str
    #: `unit_value`, declared `decimal(12,2)` NOT NULL.
    unit_value: decimal.Decimal
    #: `line_value`, declared `decimal(14,2)` NULL STORED GENERATED AS (round(`unit_value` * `package_count`,2)).
    #:
    #: Stored generated column: value declared for this line
    line_value: decimal.Decimal | None

    def key(self) -> CargoItemKey:
        """Return the primary key of the row."""
        return CargoItemKey(
            cargo_item_id=self.cargo_item_id,
        )


@dataclasses.dataclass(frozen=True)
class CargoItemKey:
    """The primary key of `freight.cargo_item`.

    Frozen, so that it hashes and compares by value. Every type this example's
    mapping produces is hashable, so no key of these three schemas is refused
    for that reason.
    """

    #: `cargo_item_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    cargo_item_id: int


class CargoItemColumn(enum.Enum):
    """One column of `freight.cargo_item`.

    A search accepts an ordering term over these members and over nothing else,
    so an ordering can carry no SQL of its own: :attr:`sql` answers from a
    closed mapping this template wrote, and :meth:`from_name` is the one door a
    name a caller supplied can come through.

    A member's value is the column's name as the catalogue carries it, which is
    what :meth:`from_name` matches on and what ``member.value`` returns.
    """

    #: `cargo_item_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    CARGO_ITEM_ID = "cargo_item_id"
    #: `consignment_id`, declared `bigint(20) unsigned` NOT NULL.
    CONSIGNMENT_ID = "consignment_id"
    #: `container_id`, declared `bigint(20) unsigned` NULL.
    CONTAINER_ID = "container_id"
    #: `line_number`, declared `smallint(5) unsigned` NOT NULL.
    LINE_NUMBER = "line_number"
    #: `hs_code`, declared `varchar(12)` NOT NULL.
    HS_CODE = "hs_code"
    #: `description`, declared `text` NOT NULL.
    DESCRIPTION = "description"
    #: `package_kind`, declared `enum('Pallet','Crate','Drum','Bale','Carton','Big bag','Roll','Loose')` NOT NULL.
    PACKAGE_KIND = "package_kind"
    #: `package_count`, declared `mediumint(8) unsigned` NOT NULL.
    PACKAGE_COUNT = "package_count"
    #: `gross_weight_kg`, declared `decimal(12,3)` NOT NULL.
    GROSS_WEIGHT_KG = "gross_weight_kg"
    #: `net_weight_kg`, declared `decimal(12,3)` NOT NULL.
    NET_WEIGHT_KG = "net_weight_kg"
    #: `volume_cbm`, declared `decimal(10,3)` NOT NULL.
    VOLUME_CBM = "volume_cbm"
    #: `chargeable_weight_kg`, declared `decimal(12,3)` NULL VIRTUAL GENERATED AS (greatest(`gross_weight_kg`,`volume_cbm` * 167)).
    CHARGEABLE_WEIGHT_KG = "chargeable_weight_kg"
    #: `imdg_class`, declared `enum('Not regulated','Class 3, Flammable liquids','Class 8, Corrosives','Class 9, Miscellaneous dangerous goods')` NOT NULL.
    IMDG_CLASS = "imdg_class"
    #: `un_number`, declared `smallint(5) unsigned` NULL.
    UN_NUMBER = "un_number"
    #: `origin_country`, declared `char(2)` NOT NULL.
    ORIGIN_COUNTRY = "origin_country"
    #: `unit_value`, declared `decimal(12,2)` NOT NULL.
    UNIT_VALUE = "unit_value"
    #: `line_value`, declared `decimal(14,2)` NULL STORED GENERATED AS (round(`unit_value` * `package_count`,2)).
    LINE_VALUE = "line_value"

    @property
    def sql(self) -> str:
        """The column as a statement's text names it: quoted for MariaDB."""
        return _COLUMNS[self]

    @classmethod
    def from_name(cls, name: str) -> CargoItemColumn:
        """Read a column a caller supplied as text.

        Raises:
            UnknownColumn: for every name the table does not carry. That
                refusal is what keeps a caller's text out of a statement.
        """
        try:
            return cls(name)
        except ValueError:
            raise UnknownColumn("freight.cargo_item", name) from None


#: The one place a column becomes SQL. It is closed over the enumeration above,
#: and every identifier in it was quoted at render time by `tpl`, so no value a
#: caller supplies can reach a statement's text through an ordering term.
_COLUMNS: Final[Mapping[CargoItemColumn, str]] = {
    CargoItemColumn.CARGO_ITEM_ID: "`cargo_item_id`",
    CargoItemColumn.CONSIGNMENT_ID: "`consignment_id`",
    CargoItemColumn.CONTAINER_ID: "`container_id`",
    CargoItemColumn.LINE_NUMBER: "`line_number`",
    CargoItemColumn.HS_CODE: "`hs_code`",
    CargoItemColumn.DESCRIPTION: "`description`",
    CargoItemColumn.PACKAGE_KIND: "`package_kind`",
    CargoItemColumn.PACKAGE_COUNT: "`package_count`",
    CargoItemColumn.GROSS_WEIGHT_KG: "`gross_weight_kg`",
    CargoItemColumn.NET_WEIGHT_KG: "`net_weight_kg`",
    CargoItemColumn.VOLUME_CBM: "`volume_cbm`",
    CargoItemColumn.CHARGEABLE_WEIGHT_KG: "`chargeable_weight_kg`",
    CargoItemColumn.IMDG_CLASS: "`imdg_class`",
    CargoItemColumn.UN_NUMBER: "`un_number`",
    CargoItemColumn.ORIGIN_COUNTRY: "`origin_country`",
    CargoItemColumn.UNIT_VALUE: "`unit_value`",
    CargoItemColumn.LINE_VALUE: "`line_value`",
}


@dataclasses.dataclass(frozen=True)
class CargoItemOrder:
    """One ordering term of a search over `freight.cargo_item`."""

    #: The column to order by.
    column: CargoItemColumn
    #: The direction to order in.
    direction: Direction = Direction.ASCENDING


@dataclasses.dataclass
class CargoItemFilter:
    """What narrows a search over `freight.cargo_item`.

    Every field is optional: a field left ``None`` adds no condition, and the
    conditions a search does build are joined with ``AND``. The default
    instance is the filter that matches every row.

    A nullable column's field is ``None`` for *no condition* and never for
    *is NULL*; that question is asked through the ``…_is_null`` field such a
    column also gets, so the two meanings never collide.
    """

    #: Where set, requires `cargo_item_id` to equal it.
    cargo_item_id: int | None = None
    #: Where set, requires `cargo_item_id` to be at least it.
    cargo_item_id_from: int | None = None
    #: Where set, requires `cargo_item_id` to be at most it.
    cargo_item_id_to: int | None = None
    #: Where set, requires `consignment_id` to equal it.
    consignment_id: int | None = None
    #: Where set, requires `consignment_id` to be at least it.
    consignment_id_from: int | None = None
    #: Where set, requires `consignment_id` to be at most it.
    consignment_id_to: int | None = None
    #: Where set, requires `container_id` to equal it.
    container_id: int | None = None
    #: Where set, requires `container_id` to be at least it.
    container_id_from: int | None = None
    #: Where set, requires `container_id` to be at most it.
    container_id_to: int | None = None
    #: Where True, requires `container_id` to be NULL; where False, requires it not to be.
    container_id_is_null: bool | None = None
    #: Where set, requires `line_number` to equal it.
    line_number: int | None = None
    #: Where set, requires `line_number` to be at least it.
    line_number_from: int | None = None
    #: Where set, requires `line_number` to be at most it.
    line_number_to: int | None = None
    #: Where set, requires `hs_code` to equal it.
    hs_code: str | None = None
    #: Where set, requires `hs_code` to match it, as LIKE matches.
    hs_code_like: str | None = None
    #: Where set, requires `description` to equal it.
    description: str | None = None
    #: Where set, requires `description` to match it, as LIKE matches.
    description_like: str | None = None
    #: Where set, requires `package_kind` to equal it.
    package_kind: str | None = None
    #: Where set, requires `package_kind` to match it, as LIKE matches.
    package_kind_like: str | None = None
    #: Where set, requires `package_count` to equal it.
    package_count: int | None = None
    #: Where set, requires `package_count` to be at least it.
    package_count_from: int | None = None
    #: Where set, requires `package_count` to be at most it.
    package_count_to: int | None = None
    #: Where set, requires `gross_weight_kg` to equal it.
    gross_weight_kg: decimal.Decimal | None = None
    #: Where set, requires `gross_weight_kg` to be at least it.
    gross_weight_kg_from: decimal.Decimal | None = None
    #: Where set, requires `gross_weight_kg` to be at most it.
    gross_weight_kg_to: decimal.Decimal | None = None
    #: Where set, requires `net_weight_kg` to equal it.
    net_weight_kg: decimal.Decimal | None = None
    #: Where set, requires `net_weight_kg` to be at least it.
    net_weight_kg_from: decimal.Decimal | None = None
    #: Where set, requires `net_weight_kg` to be at most it.
    net_weight_kg_to: decimal.Decimal | None = None
    #: Where set, requires `volume_cbm` to equal it.
    volume_cbm: decimal.Decimal | None = None
    #: Where set, requires `volume_cbm` to be at least it.
    volume_cbm_from: decimal.Decimal | None = None
    #: Where set, requires `volume_cbm` to be at most it.
    volume_cbm_to: decimal.Decimal | None = None
    #: Where set, requires `chargeable_weight_kg` to equal it.
    chargeable_weight_kg: decimal.Decimal | None = None
    #: Where set, requires `chargeable_weight_kg` to be at least it.
    chargeable_weight_kg_from: decimal.Decimal | None = None
    #: Where set, requires `chargeable_weight_kg` to be at most it.
    chargeable_weight_kg_to: decimal.Decimal | None = None
    #: Where True, requires `chargeable_weight_kg` to be NULL; where False, requires it not to be.
    chargeable_weight_kg_is_null: bool | None = None
    #: Where set, requires `imdg_class` to equal it.
    imdg_class: str | None = None
    #: Where set, requires `imdg_class` to match it, as LIKE matches.
    imdg_class_like: str | None = None
    #: Where set, requires `un_number` to equal it.
    un_number: int | None = None
    #: Where set, requires `un_number` to be at least it.
    un_number_from: int | None = None
    #: Where set, requires `un_number` to be at most it.
    un_number_to: int | None = None
    #: Where True, requires `un_number` to be NULL; where False, requires it not to be.
    un_number_is_null: bool | None = None
    #: Where set, requires `origin_country` to equal it.
    origin_country: str | None = None
    #: Where set, requires `origin_country` to match it, as LIKE matches.
    origin_country_like: str | None = None
    #: Where set, requires `unit_value` to equal it.
    unit_value: decimal.Decimal | None = None
    #: Where set, requires `unit_value` to be at least it.
    unit_value_from: decimal.Decimal | None = None
    #: Where set, requires `unit_value` to be at most it.
    unit_value_to: decimal.Decimal | None = None
    #: Where set, requires `line_value` to equal it.
    line_value: decimal.Decimal | None = None
    #: Where set, requires `line_value` to be at least it.
    line_value_from: decimal.Decimal | None = None
    #: Where set, requires `line_value` to be at most it.
    line_value_to: decimal.Decimal | None = None
    #: Where True, requires `line_value` to be NULL; where False, requires it not to be.
    line_value_is_null: bool | None = None

    def is_empty(self) -> bool:
        """Is no field set?

        A search over an empty filter builds no ``WHERE`` clause and matches
        every row of the table.
        """
        return (
            self.cargo_item_id is None
            and self.cargo_item_id_from is None
            and self.cargo_item_id_to is None
            and self.consignment_id is None
            and self.consignment_id_from is None
            and self.consignment_id_to is None
            and self.container_id is None
            and self.container_id_from is None
            and self.container_id_to is None
            and self.container_id_is_null is None
            and self.line_number is None
            and self.line_number_from is None
            and self.line_number_to is None
            and self.hs_code is None
            and self.hs_code_like is None
            and self.description is None
            and self.description_like is None
            and self.package_kind is None
            and self.package_kind_like is None
            and self.package_count is None
            and self.package_count_from is None
            and self.package_count_to is None
            and self.gross_weight_kg is None
            and self.gross_weight_kg_from is None
            and self.gross_weight_kg_to is None
            and self.net_weight_kg is None
            and self.net_weight_kg_from is None
            and self.net_weight_kg_to is None
            and self.volume_cbm is None
            and self.volume_cbm_from is None
            and self.volume_cbm_to is None
            and self.chargeable_weight_kg is None
            and self.chargeable_weight_kg_from is None
            and self.chargeable_weight_kg_to is None
            and self.chargeable_weight_kg_is_null is None
            and self.imdg_class is None
            and self.imdg_class_like is None
            and self.un_number is None
            and self.un_number_from is None
            and self.un_number_to is None
            and self.un_number_is_null is None
            and self.origin_country is None
            and self.origin_country_like is None
            and self.unit_value is None
            and self.unit_value_from is None
            and self.unit_value_to is None
            and self.line_value is None
            and self.line_value_from is None
            and self.line_value_to is None
            and self.line_value_is_null is None
        )
