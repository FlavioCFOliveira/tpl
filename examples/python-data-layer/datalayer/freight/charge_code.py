# Code generated by tpl from freight.charge_code. DO NOT EDIT.

"""The `freight.charge_code` row, the key that addresses it, the columns an ordering may
name, and the filter a search takes.
"""

from __future__ import annotations

import dataclasses
import decimal
import enum
from collections.abc import Mapping
from typing import Final

from ._core import Direction, UnknownColumn

__all__ = [
    "ChargeCode",
    "ChargeCodeColumn",
    "ChargeCodeFilter",
    "ChargeCodeKey",
    "ChargeCodeOrder",
]


@dataclasses.dataclass
class ChargeCode:
    """One row of `freight.charge_code`.

    The dataclass is mutable, because :meth:`create` writes a server-assigned
    key back into it.
    """

    # The table's own comment, as the catalogue carries it:
    # Códigos de encargo utilizados na facturação

    #: `charge_code`, declared `varchar(8)` NOT NULL.
    #:
    #: It is part of the primary key.
    #:
    #: Industry charge code, e.g. BAF, THC, ISPS
    charge_code: str
    #: `description`, declared `varchar(100)` NOT NULL.
    description: str
    #: `is_freight`, declared `tinyint(1)` NOT NULL.
    #:
    #: Counts towards ocean freight rather than local charges
    is_freight: int
    #: `vat_rate`, declared `decimal(5,4)` NOT NULL.
    vat_rate: decimal.Decimal
    #: `gl_account`, declared `varchar(12)` NOT NULL.
    gl_account: str

    def key(self) -> ChargeCodeKey:
        """Return the primary key of the row."""
        return ChargeCodeKey(
            charge_code=self.charge_code,
        )


@dataclasses.dataclass(frozen=True)
class ChargeCodeKey:
    """The primary key of `freight.charge_code`.

    Frozen, so that it hashes and compares by value. Every type this example's
    mapping produces is hashable, so no key of these three schemas is refused
    for that reason.
    """

    #: `charge_code`, declared `varchar(8)` NOT NULL.
    charge_code: str


class ChargeCodeColumn(enum.Enum):
    """One column of `freight.charge_code`.

    A search accepts an ordering term over these members and over nothing else,
    so an ordering can carry no SQL of its own: :attr:`sql` answers from a
    closed mapping this template wrote, and :meth:`from_name` is the one door a
    name a caller supplied can come through.

    A member's value is the column's name as the catalogue carries it, which is
    what :meth:`from_name` matches on and what ``member.value`` returns.
    """

    #: `charge_code`, declared `varchar(8)` NOT NULL.
    CHARGE_CODE = "charge_code"
    #: `description`, declared `varchar(100)` NOT NULL.
    DESCRIPTION = "description"
    #: `is_freight`, declared `tinyint(1)` NOT NULL.
    IS_FREIGHT = "is_freight"
    #: `vat_rate`, declared `decimal(5,4)` NOT NULL.
    VAT_RATE = "vat_rate"
    #: `gl_account`, declared `varchar(12)` NOT NULL.
    GL_ACCOUNT = "gl_account"

    @property
    def sql(self) -> str:
        """The column as a statement's text names it: quoted for MariaDB."""
        return _COLUMNS[self]

    @classmethod
    def from_name(cls, name: str) -> ChargeCodeColumn:
        """Read a column a caller supplied as text.

        Raises:
            UnknownColumn: for every name the table does not carry. That
                refusal is what keeps a caller's text out of a statement.
        """
        try:
            return cls(name)
        except ValueError:
            raise UnknownColumn("freight.charge_code", name) from None


#: The one place a column becomes SQL. It is closed over the enumeration above,
#: and every identifier in it was quoted at render time by `tpl`, so no value a
#: caller supplies can reach a statement's text through an ordering term.
_COLUMNS: Final[Mapping[ChargeCodeColumn, str]] = {
    ChargeCodeColumn.CHARGE_CODE: "`charge_code`",
    ChargeCodeColumn.DESCRIPTION: "`description`",
    ChargeCodeColumn.IS_FREIGHT: "`is_freight`",
    ChargeCodeColumn.VAT_RATE: "`vat_rate`",
    ChargeCodeColumn.GL_ACCOUNT: "`gl_account`",
}


@dataclasses.dataclass(frozen=True)
class ChargeCodeOrder:
    """One ordering term of a search over `freight.charge_code`."""

    #: The column to order by.
    column: ChargeCodeColumn
    #: The direction to order in.
    direction: Direction = Direction.ASCENDING


@dataclasses.dataclass
class ChargeCodeFilter:
    """What narrows a search over `freight.charge_code`.

    Every field is optional: a field left ``None`` adds no condition, and the
    conditions a search does build are joined with ``AND``. The default
    instance is the filter that matches every row.

    A nullable column's field is ``None`` for *no condition* and never for
    *is NULL*; that question is asked through the ``…_is_null`` field such a
    column also gets, so the two meanings never collide.
    """

    #: Where set, requires `charge_code` to equal it.
    charge_code: str | None = None
    #: Where set, requires `charge_code` to match it, as LIKE matches.
    charge_code_like: str | None = None
    #: Where set, requires `description` to equal it.
    description: str | None = None
    #: Where set, requires `description` to match it, as LIKE matches.
    description_like: str | None = None
    #: Where set, requires `is_freight` to equal it.
    is_freight: int | None = None
    #: Where set, requires `is_freight` to be at least it.
    is_freight_from: int | None = None
    #: Where set, requires `is_freight` to be at most it.
    is_freight_to: int | None = None
    #: Where set, requires `vat_rate` to equal it.
    vat_rate: decimal.Decimal | None = None
    #: Where set, requires `vat_rate` to be at least it.
    vat_rate_from: decimal.Decimal | None = None
    #: Where set, requires `vat_rate` to be at most it.
    vat_rate_to: decimal.Decimal | None = None
    #: Where set, requires `gl_account` to equal it.
    gl_account: str | None = None
    #: Where set, requires `gl_account` to match it, as LIKE matches.
    gl_account_like: str | None = None

    def is_empty(self) -> bool:
        """Is no field set?

        A search over an empty filter builds no ``WHERE`` clause and matches
        every row of the table.
        """
        return (
            self.charge_code is None
            and self.charge_code_like is None
            and self.description is None
            and self.description_like is None
            and self.is_freight is None
            and self.is_freight_from is None
            and self.is_freight_to is None
            and self.vat_rate is None
            and self.vat_rate_from is None
            and self.vat_rate_to is None
            and self.gl_account is None
            and self.gl_account_like is None
        )
