# Code generated by tpl from freight.container_type. DO NOT EDIT.

"""The `freight.container_type` row, the key that addresses it, the columns an ordering may
name, and the filter a search takes.
"""

from __future__ import annotations

import dataclasses
import decimal
import enum
from collections.abc import Mapping
from typing import Final

from ._core import Direction, UnknownColumn

__all__ = [
    "ContainerType",
    "ContainerTypeColumn",
    "ContainerTypeFilter",
    "ContainerTypeKey",
    "ContainerTypeOrder",
]


@dataclasses.dataclass
class ContainerType:
    """One row of `freight.container_type`.

    The dataclass is mutable, because :meth:`create` writes a server-assigned
    key back into it.
    """

    # The table's own comment, as the catalogue carries it:
    # Tipos de contentor normalizados ISO 6346

    #: `iso_code`, declared `char(4)` NOT NULL.
    #:
    #: It is part of the primary key.
    #:
    #: ISO 6346 size-and-type code, e.g. 22G1
    iso_code: str
    #: `description`, declared `varchar(80)` NOT NULL.
    description: str
    #: `length_ft`, declared `tinyint(3) unsigned` NOT NULL.
    length_ft: int
    #: `height_ft_in`, declared `varchar(6)` NOT NULL.
    #:
    #: Nominal external height, feet and inches
    height_ft_in: str
    #: `tare_weight_kg`, declared `decimal(8,2)` NOT NULL.
    tare_weight_kg: decimal.Decimal
    #: `max_payload_kg`, declared `decimal(8,2)` NOT NULL.
    max_payload_kg: decimal.Decimal
    #: `internal_volume_cbm`, declared `decimal(7,3)` NOT NULL.
    internal_volume_cbm: decimal.Decimal
    #: `is_reefer`, declared `tinyint(1)` NOT NULL.
    is_reefer: int
    #: `is_tank`, declared `tinyint(1)` NOT NULL.
    is_tank: int

    def key(self) -> ContainerTypeKey:
        """Return the primary key of the row."""
        return ContainerTypeKey(
            iso_code=self.iso_code,
        )


@dataclasses.dataclass(frozen=True)
class ContainerTypeKey:
    """The primary key of `freight.container_type`.

    Frozen, so that it hashes and compares by value. Every type this example's
    mapping produces is hashable, so no key of these three schemas is refused
    for that reason.
    """

    #: `iso_code`, declared `char(4)` NOT NULL.
    iso_code: str


class ContainerTypeColumn(enum.Enum):
    """One column of `freight.container_type`.

    A search accepts an ordering term over these members and over nothing else,
    so an ordering can carry no SQL of its own: :attr:`sql` answers from a
    closed mapping this template wrote, and :meth:`from_name` is the one door a
    name a caller supplied can come through.

    A member's value is the column's name as the catalogue carries it, which is
    what :meth:`from_name` matches on and what ``member.value`` returns.
    """

    #: `iso_code`, declared `char(4)` NOT NULL.
    ISO_CODE = "iso_code"
    #: `description`, declared `varchar(80)` NOT NULL.
    DESCRIPTION = "description"
    #: `length_ft`, declared `tinyint(3) unsigned` NOT NULL.
    LENGTH_FT = "length_ft"
    #: `height_ft_in`, declared `varchar(6)` NOT NULL.
    HEIGHT_FT_IN = "height_ft_in"
    #: `tare_weight_kg`, declared `decimal(8,2)` NOT NULL.
    TARE_WEIGHT_KG = "tare_weight_kg"
    #: `max_payload_kg`, declared `decimal(8,2)` NOT NULL.
    MAX_PAYLOAD_KG = "max_payload_kg"
    #: `internal_volume_cbm`, declared `decimal(7,3)` NOT NULL.
    INTERNAL_VOLUME_CBM = "internal_volume_cbm"
    #: `is_reefer`, declared `tinyint(1)` NOT NULL.
    IS_REEFER = "is_reefer"
    #: `is_tank`, declared `tinyint(1)` NOT NULL.
    IS_TANK = "is_tank"

    @property
    def sql(self) -> str:
        """The column as a statement's text names it: quoted for MariaDB."""
        return _COLUMNS[self]

    @classmethod
    def from_name(cls, name: str) -> ContainerTypeColumn:
        """Read a column a caller supplied as text.

        Raises:
            UnknownColumn: for every name the table does not carry. That
                refusal is what keeps a caller's text out of a statement.
        """
        try:
            return cls(name)
        except ValueError:
            raise UnknownColumn("freight.container_type", name) from None


#: The one place a column becomes SQL. It is closed over the enumeration above,
#: and every identifier in it was quoted at render time by `tpl`, so no value a
#: caller supplies can reach a statement's text through an ordering term.
_COLUMNS: Final[Mapping[ContainerTypeColumn, str]] = {
    ContainerTypeColumn.ISO_CODE: "`iso_code`",
    ContainerTypeColumn.DESCRIPTION: "`description`",
    ContainerTypeColumn.LENGTH_FT: "`length_ft`",
    ContainerTypeColumn.HEIGHT_FT_IN: "`height_ft_in`",
    ContainerTypeColumn.TARE_WEIGHT_KG: "`tare_weight_kg`",
    ContainerTypeColumn.MAX_PAYLOAD_KG: "`max_payload_kg`",
    ContainerTypeColumn.INTERNAL_VOLUME_CBM: "`internal_volume_cbm`",
    ContainerTypeColumn.IS_REEFER: "`is_reefer`",
    ContainerTypeColumn.IS_TANK: "`is_tank`",
}


@dataclasses.dataclass(frozen=True)
class ContainerTypeOrder:
    """One ordering term of a search over `freight.container_type`."""

    #: The column to order by.
    column: ContainerTypeColumn
    #: The direction to order in.
    direction: Direction = Direction.ASCENDING


@dataclasses.dataclass
class ContainerTypeFilter:
    """What narrows a search over `freight.container_type`.

    Every field is optional: a field left ``None`` adds no condition, and the
    conditions a search does build are joined with ``AND``. The default
    instance is the filter that matches every row.

    A nullable column's field is ``None`` for *no condition* and never for
    *is NULL*; that question is asked through the ``…_is_null`` field such a
    column also gets, so the two meanings never collide.
    """

    #: Where set, requires `iso_code` to equal it.
    iso_code: str | None = None
    #: Where set, requires `iso_code` to match it, as LIKE matches.
    iso_code_like: str | None = None
    #: Where set, requires `description` to equal it.
    description: str | None = None
    #: Where set, requires `description` to match it, as LIKE matches.
    description_like: str | None = None
    #: Where set, requires `length_ft` to equal it.
    length_ft: int | None = None
    #: Where set, requires `length_ft` to be at least it.
    length_ft_from: int | None = None
    #: Where set, requires `length_ft` to be at most it.
    length_ft_to: int | None = None
    #: Where set, requires `height_ft_in` to equal it.
    height_ft_in: str | None = None
    #: Where set, requires `height_ft_in` to match it, as LIKE matches.
    height_ft_in_like: str | None = None
    #: Where set, requires `tare_weight_kg` to equal it.
    tare_weight_kg: decimal.Decimal | None = None
    #: Where set, requires `tare_weight_kg` to be at least it.
    tare_weight_kg_from: decimal.Decimal | None = None
    #: Where set, requires `tare_weight_kg` to be at most it.
    tare_weight_kg_to: decimal.Decimal | None = None
    #: Where set, requires `max_payload_kg` to equal it.
    max_payload_kg: decimal.Decimal | None = None
    #: Where set, requires `max_payload_kg` to be at least it.
    max_payload_kg_from: decimal.Decimal | None = None
    #: Where set, requires `max_payload_kg` to be at most it.
    max_payload_kg_to: decimal.Decimal | None = None
    #: Where set, requires `internal_volume_cbm` to equal it.
    internal_volume_cbm: decimal.Decimal | None = None
    #: Where set, requires `internal_volume_cbm` to be at least it.
    internal_volume_cbm_from: decimal.Decimal | None = None
    #: Where set, requires `internal_volume_cbm` to be at most it.
    internal_volume_cbm_to: decimal.Decimal | None = None
    #: Where set, requires `is_reefer` to equal it.
    is_reefer: int | None = None
    #: Where set, requires `is_reefer` to be at least it.
    is_reefer_from: int | None = None
    #: Where set, requires `is_reefer` to be at most it.
    is_reefer_to: int | None = None
    #: Where set, requires `is_tank` to equal it.
    is_tank: int | None = None
    #: Where set, requires `is_tank` to be at least it.
    is_tank_from: int | None = None
    #: Where set, requires `is_tank` to be at most it.
    is_tank_to: int | None = None

    def is_empty(self) -> bool:
        """Is no field set?

        A search over an empty filter builds no ``WHERE`` clause and matches
        every row of the table.
        """
        return (
            self.iso_code is None
            and self.iso_code_like is None
            and self.description is None
            and self.description_like is None
            and self.length_ft is None
            and self.length_ft_from is None
            and self.length_ft_to is None
            and self.height_ft_in is None
            and self.height_ft_in_like is None
            and self.tare_weight_kg is None
            and self.tare_weight_kg_from is None
            and self.tare_weight_kg_to is None
            and self.max_payload_kg is None
            and self.max_payload_kg_from is None
            and self.max_payload_kg_to is None
            and self.internal_volume_cbm is None
            and self.internal_volume_cbm_from is None
            and self.internal_volume_cbm_to is None
            and self.is_reefer is None
            and self.is_reefer_from is None
            and self.is_reefer_to is None
            and self.is_tank is None
            and self.is_tank_from is None
            and self.is_tank_to is None
        )
