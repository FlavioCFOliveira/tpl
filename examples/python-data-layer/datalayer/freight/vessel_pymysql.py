# Code generated by tpl from freight.vessel. DO NOT EDIT.

"""The PyMySQL implementation of the `freight.vessel` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .vessel import Vessel, VesselFilter, VesselKey, VesselOrder
from .vessel_repository import VesselRepository

__all__ = ["VesselPyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "freight.vessel"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `imo_number`, `name`, `call_sign`, `mmsi`, `flag_state`, `class_society`, `hull_number`, `capacity_teu`, `reefer_plugs`, `deadweight_tonnes`, `gross_tonnage`, `net_tonnage`, `service_speed_knots`, `ballast_capacity_m3`, `loa_m`, `beam_m`, `design_draught_m`, `year_built`, `crew_complement`, `lifetime_distance_nm`, `in_service`, `scrubber_fitted`, `particulars_sheet` FROM `vessel`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `imo_number`, `name`, `call_sign`, `mmsi`, `flag_state`, `class_society`, `hull_number`, `capacity_teu`, `reefer_plugs`, `deadweight_tonnes`, `gross_tonnage`, `net_tonnage`, `service_speed_knots`, `ballast_capacity_m3`, `loa_m`, `beam_m`, `design_draught_m`, `year_built`, `crew_complement`, `lifetime_distance_nm`, `in_service`, `scrubber_fitted`, `particulars_sheet` FROM `vessel` WHERE `imo_number` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `vessel` (`imo_number`, `name`, `call_sign`, `mmsi`, `flag_state`, `class_society`, `hull_number`, `capacity_teu`, `reefer_plugs`, `deadweight_tonnes`, `gross_tonnage`, `net_tonnage`, `service_speed_knots`, `ballast_capacity_m3`, `loa_m`, `beam_m`, `design_draught_m`, `year_built`, `crew_complement`, `lifetime_distance_nm`, `in_service`, `scrubber_fitted`, `particulars_sheet`) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `vessel` SET `name` = %s, `call_sign` = %s, `mmsi` = %s, `flag_state` = %s, `class_society` = %s, `hull_number` = %s, `capacity_teu` = %s, `reefer_plugs` = %s, `deadweight_tonnes` = %s, `gross_tonnage` = %s, `net_tonnage` = %s, `service_speed_knots` = %s, `ballast_capacity_m3` = %s, `loa_m` = %s, `beam_m` = %s, `design_draught_m` = %s, `year_built` = %s, `crew_complement` = %s, `lifetime_distance_nm` = %s, `in_service` = %s, `scrubber_fitted` = %s, `particulars_sheet` = %s WHERE `imo_number` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `vessel` WHERE `imo_number` = %s"


def _row(values: tuple[Any, ...]) -> Vessel:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return Vessel(
        imo_number=values[0],
        name=values[1],
        call_sign=values[2],
        mmsi=values[3],
        flag_state=values[4],
        class_society=values[5],
        hull_number=values[6],
        capacity_teu=values[7],
        reefer_plugs=values[8],
        deadweight_tonnes=values[9],
        gross_tonnage=values[10],
        net_tonnage=values[11],
        service_speed_knots=values[12],
        ballast_capacity_m3=values[13],
        loa_m=values[14],
        beam_m=values[15],
        design_draught_m=values[16],
        year_built=values[17],
        crew_complement=values[18],
        lifetime_distance_nm=values[19],
        in_service=values[20],
        scrubber_fitted=values[21],
        particulars_sheet=values[22],
    )


class VesselPyMySQL:
    """The PyMySQL data layer of `freight.vessel`.

    It names no base class. ``VesselRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `freight.vessel` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: Vessel) -> None:
        """Insert `row`.

        The whole key is taken from `row`, which the caller therefore fills.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.imo_number,
                    row.name,
                    row.call_sign,
                    row.mmsi,
                    row.flag_state,
                    row.class_society,
                    row.hull_number,
                    row.capacity_teu,
                    row.reefer_plugs,
                    row.deadweight_tonnes,
                    row.gross_tonnage,
                    row.net_tonnage,
                    row.service_speed_knots,
                    row.ballast_capacity_m3,
                    row.loa_m,
                    row.beam_m,
                    row.design_draught_m,
                    row.year_built,
                    row.crew_complement,
                    row.lifetime_distance_nm,
                    row.in_service,
                    row.scrubber_fitted,
                    row.particulars_sheet,
                ),
            )

    def read(self, key: VesselKey) -> Vessel:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.imo_number,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: Vessel) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.name,
                    row.call_sign,
                    row.mmsi,
                    row.flag_state,
                    row.class_society,
                    row.hull_number,
                    row.capacity_teu,
                    row.reefer_plugs,
                    row.deadweight_tonnes,
                    row.gross_tonnage,
                    row.net_tonnage,
                    row.service_speed_knots,
                    row.ballast_capacity_m3,
                    row.loa_m,
                    row.beam_m,
                    row.design_draught_m,
                    row.year_built,
                    row.crew_complement,
                    row.lifetime_distance_nm,
                    row.in_service,
                    row.scrubber_fitted,
                    row.particulars_sheet,
                    row.imo_number,
                ),
            )
        return affected

    def delete(self, key: VesselKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.imo_number,
                ),
            )
        return affected

    def search(
        self,
        criteria: VesselFilter,
        order: Sequence[VesselOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[Vessel]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.imo_number is not None:
            conditions.append("`imo_number` = %s")
            arguments.append(criteria.imo_number)
        if criteria.imo_number_like is not None:
            conditions.append("`imo_number` LIKE %s")
            arguments.append(criteria.imo_number_like)
        if criteria.name is not None:
            conditions.append("`name` = %s")
            arguments.append(criteria.name)
        if criteria.name_like is not None:
            conditions.append("`name` LIKE %s")
            arguments.append(criteria.name_like)
        if criteria.call_sign is not None:
            conditions.append("`call_sign` = %s")
            arguments.append(criteria.call_sign)
        if criteria.call_sign_like is not None:
            conditions.append("`call_sign` LIKE %s")
            arguments.append(criteria.call_sign_like)
        if criteria.mmsi is not None:
            conditions.append("`mmsi` = %s")
            arguments.append(criteria.mmsi)
        if criteria.mmsi_from is not None:
            conditions.append("`mmsi` >= %s")
            arguments.append(criteria.mmsi_from)
        if criteria.mmsi_to is not None:
            conditions.append("`mmsi` <= %s")
            arguments.append(criteria.mmsi_to)
        if criteria.mmsi_is_null is not None:
            conditions.append(
                "`mmsi` IS NULL"
                if criteria.mmsi_is_null
                else "`mmsi` IS NOT NULL"
            )
        if criteria.flag_state is not None:
            conditions.append("`flag_state` = %s")
            arguments.append(criteria.flag_state)
        if criteria.flag_state_like is not None:
            conditions.append("`flag_state` LIKE %s")
            arguments.append(criteria.flag_state_like)
        if criteria.class_society is not None:
            conditions.append("`class_society` = %s")
            arguments.append(criteria.class_society)
        if criteria.class_society_like is not None:
            conditions.append("`class_society` LIKE %s")
            arguments.append(criteria.class_society_like)
        if criteria.hull_number is not None:
            conditions.append("`hull_number` = %s")
            arguments.append(criteria.hull_number)
        if criteria.hull_number_from is not None:
            conditions.append("`hull_number` >= %s")
            arguments.append(criteria.hull_number_from)
        if criteria.hull_number_to is not None:
            conditions.append("`hull_number` <= %s")
            arguments.append(criteria.hull_number_to)
        if criteria.hull_number_is_null is not None:
            conditions.append(
                "`hull_number` IS NULL"
                if criteria.hull_number_is_null
                else "`hull_number` IS NOT NULL"
            )
        if criteria.capacity_teu is not None:
            conditions.append("`capacity_teu` = %s")
            arguments.append(criteria.capacity_teu)
        if criteria.capacity_teu_from is not None:
            conditions.append("`capacity_teu` >= %s")
            arguments.append(criteria.capacity_teu_from)
        if criteria.capacity_teu_to is not None:
            conditions.append("`capacity_teu` <= %s")
            arguments.append(criteria.capacity_teu_to)
        if criteria.reefer_plugs is not None:
            conditions.append("`reefer_plugs` = %s")
            arguments.append(criteria.reefer_plugs)
        if criteria.reefer_plugs_from is not None:
            conditions.append("`reefer_plugs` >= %s")
            arguments.append(criteria.reefer_plugs_from)
        if criteria.reefer_plugs_to is not None:
            conditions.append("`reefer_plugs` <= %s")
            arguments.append(criteria.reefer_plugs_to)
        if criteria.deadweight_tonnes is not None:
            conditions.append("`deadweight_tonnes` = %s")
            arguments.append(criteria.deadweight_tonnes)
        if criteria.deadweight_tonnes_from is not None:
            conditions.append("`deadweight_tonnes` >= %s")
            arguments.append(criteria.deadweight_tonnes_from)
        if criteria.deadweight_tonnes_to is not None:
            conditions.append("`deadweight_tonnes` <= %s")
            arguments.append(criteria.deadweight_tonnes_to)
        if criteria.gross_tonnage is not None:
            conditions.append("`gross_tonnage` = %s")
            arguments.append(criteria.gross_tonnage)
        if criteria.gross_tonnage_from is not None:
            conditions.append("`gross_tonnage` >= %s")
            arguments.append(criteria.gross_tonnage_from)
        if criteria.gross_tonnage_to is not None:
            conditions.append("`gross_tonnage` <= %s")
            arguments.append(criteria.gross_tonnage_to)
        if criteria.net_tonnage is not None:
            conditions.append("`net_tonnage` = %s")
            arguments.append(criteria.net_tonnage)
        if criteria.net_tonnage_from is not None:
            conditions.append("`net_tonnage` >= %s")
            arguments.append(criteria.net_tonnage_from)
        if criteria.net_tonnage_to is not None:
            conditions.append("`net_tonnage` <= %s")
            arguments.append(criteria.net_tonnage_to)
        if criteria.net_tonnage_is_null is not None:
            conditions.append(
                "`net_tonnage` IS NULL"
                if criteria.net_tonnage_is_null
                else "`net_tonnage` IS NOT NULL"
            )
        if criteria.service_speed_knots is not None:
            conditions.append("`service_speed_knots` = %s")
            arguments.append(criteria.service_speed_knots)
        if criteria.service_speed_knots_from is not None:
            conditions.append("`service_speed_knots` >= %s")
            arguments.append(criteria.service_speed_knots_from)
        if criteria.service_speed_knots_to is not None:
            conditions.append("`service_speed_knots` <= %s")
            arguments.append(criteria.service_speed_knots_to)
        if criteria.ballast_capacity_m3 is not None:
            conditions.append("`ballast_capacity_m3` = %s")
            arguments.append(criteria.ballast_capacity_m3)
        if criteria.ballast_capacity_m3_from is not None:
            conditions.append("`ballast_capacity_m3` >= %s")
            arguments.append(criteria.ballast_capacity_m3_from)
        if criteria.ballast_capacity_m3_to is not None:
            conditions.append("`ballast_capacity_m3` <= %s")
            arguments.append(criteria.ballast_capacity_m3_to)
        if criteria.ballast_capacity_m3_is_null is not None:
            conditions.append(
                "`ballast_capacity_m3` IS NULL"
                if criteria.ballast_capacity_m3_is_null
                else "`ballast_capacity_m3` IS NOT NULL"
            )
        if criteria.loa_m is not None:
            conditions.append("`loa_m` = %s")
            arguments.append(criteria.loa_m)
        if criteria.loa_m_from is not None:
            conditions.append("`loa_m` >= %s")
            arguments.append(criteria.loa_m_from)
        if criteria.loa_m_to is not None:
            conditions.append("`loa_m` <= %s")
            arguments.append(criteria.loa_m_to)
        if criteria.beam_m is not None:
            conditions.append("`beam_m` = %s")
            arguments.append(criteria.beam_m)
        if criteria.beam_m_from is not None:
            conditions.append("`beam_m` >= %s")
            arguments.append(criteria.beam_m_from)
        if criteria.beam_m_to is not None:
            conditions.append("`beam_m` <= %s")
            arguments.append(criteria.beam_m_to)
        if criteria.design_draught_m is not None:
            conditions.append("`design_draught_m` = %s")
            arguments.append(criteria.design_draught_m)
        if criteria.design_draught_m_from is not None:
            conditions.append("`design_draught_m` >= %s")
            arguments.append(criteria.design_draught_m_from)
        if criteria.design_draught_m_to is not None:
            conditions.append("`design_draught_m` <= %s")
            arguments.append(criteria.design_draught_m_to)
        if criteria.year_built is not None:
            conditions.append("`year_built` = %s")
            arguments.append(criteria.year_built)
        if criteria.year_built_from is not None:
            conditions.append("`year_built` >= %s")
            arguments.append(criteria.year_built_from)
        if criteria.year_built_to is not None:
            conditions.append("`year_built` <= %s")
            arguments.append(criteria.year_built_to)
        if criteria.crew_complement is not None:
            conditions.append("`crew_complement` = %s")
            arguments.append(criteria.crew_complement)
        if criteria.crew_complement_from is not None:
            conditions.append("`crew_complement` >= %s")
            arguments.append(criteria.crew_complement_from)
        if criteria.crew_complement_to is not None:
            conditions.append("`crew_complement` <= %s")
            arguments.append(criteria.crew_complement_to)
        if criteria.lifetime_distance_nm is not None:
            conditions.append("`lifetime_distance_nm` = %s")
            arguments.append(criteria.lifetime_distance_nm)
        if criteria.lifetime_distance_nm_from is not None:
            conditions.append("`lifetime_distance_nm` >= %s")
            arguments.append(criteria.lifetime_distance_nm_from)
        if criteria.lifetime_distance_nm_to is not None:
            conditions.append("`lifetime_distance_nm` <= %s")
            arguments.append(criteria.lifetime_distance_nm_to)
        if criteria.in_service is not None:
            conditions.append("`in_service` = %s")
            arguments.append(criteria.in_service)
        if criteria.in_service_from is not None:
            conditions.append("`in_service` >= %s")
            arguments.append(criteria.in_service_from)
        if criteria.in_service_to is not None:
            conditions.append("`in_service` <= %s")
            arguments.append(criteria.in_service_to)
        if criteria.scrubber_fitted is not None:
            conditions.append("`scrubber_fitted` = %s")
            arguments.append(criteria.scrubber_fitted)
        if criteria.scrubber_fitted_from is not None:
            conditions.append("`scrubber_fitted` >= %s")
            arguments.append(criteria.scrubber_fitted_from)
        if criteria.scrubber_fitted_to is not None:
            conditions.append("`scrubber_fitted` <= %s")
            arguments.append(criteria.scrubber_fitted_to)
        if criteria.particulars_sheet is not None:
            conditions.append("`particulars_sheet` = %s")
            arguments.append(criteria.particulars_sheet)
        if criteria.particulars_sheet_like is not None:
            conditions.append("`particulars_sheet` LIKE %s")
            arguments.append(criteria.particulars_sheet_like)
        if criteria.particulars_sheet_is_null is not None:
            conditions.append(
                "`particulars_sheet` IS NULL"
                if criteria.particulars_sheet_is_null
                else "`particulars_sheet` IS NOT NULL"
            )

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: VesselPyMySQL,
    ) -> VesselRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
