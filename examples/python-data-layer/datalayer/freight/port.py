# Code generated by tpl from freight.port. DO NOT EDIT.

"""The `freight.port` row, the key that addresses it, the columns an ordering may
name, and the filter a search takes.
"""

from __future__ import annotations

import dataclasses
import decimal
import enum
from collections.abc import Mapping
from typing import Final

from ._core import Direction, UnknownColumn

__all__ = [
    "Port",
    "PortColumn",
    "PortFilter",
    "PortKey",
    "PortOrder",
]


@dataclasses.dataclass
class Port:
    """One row of `freight.port`.

    The dataclass is mutable, because :meth:`create` writes a server-assigned
    key back into it.
    """

    # The table's own comment, as the catalogue carries it:
    # Portos marítimos, respectivos códigos UN/LOCODE e características de acostagem — ver anexo técnico do manual de operações

    #: `port_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    #:
    #: It is part of the primary key.
    #:
    #: Surrogate key; UN/LOCODE is the natural key
    port_id: int
    #: `un_locode`, declared `char(5)` NOT NULL.
    #:
    #: It is part of a unique index.
    #:
    #: UN/LOCODE, e.g. PTLIS — two-letter country plus three-letter location
    un_locode: str
    #: `name`, declared `varchar(120)` NOT NULL.
    #:
    #: Port name as published by the port authority
    name: str
    #: `country_code`, declared `char(2)` NOT NULL.
    #:
    #: ISO 3166-1 alpha-2; ASCII by definition, so stored as ASCII
    country_code: str
    #: `local_name`, declared `varchar(120)` NULL.
    #:
    #: Name in the local language — binary collation, since Ålesund and Alesund are different ports
    local_name: str | None
    #: `timezone_name`, declared `varchar(64)` NOT NULL.
    #:
    #: IANA time zone identifier
    timezone_name: str
    #: `utc_offset_minutes`, declared `smallint(6)` NOT NULL.
    #:
    #: Standard-time offset in minutes; negative west of Greenwich
    utc_offset_minutes: int
    #: `berth_count`, declared `tinyint(3) unsigned` NOT NULL.
    #:
    #: Number of commercial berths
    berth_count: int
    #: `max_draught_m`, declared `decimal(5,2)` NOT NULL.
    #:
    #: Maximum permitted draught at the deepest berth, in metres
    max_draught_m: decimal.Decimal
    #: `tidal_range_m`, declared `float` NULL.
    #:
    #: Mean spring tidal range, in metres
    tidal_range_m: float | None
    #: `annual_throughput_teu`, declared `int(10) unsigned` NULL.
    #:
    #: Container throughput for the last reported calendar year
    annual_throughput_teu: int | None
    #: `customs_round_clock`, declared `bit(1)` NOT NULL.
    #:
    #: Customs desk staffed 24/7
    customs_round_clock: bytes
    #: `harbour_dues_currency`, declared `char(3)` NOT NULL.
    #:
    #: ISO 4217 code in which harbour dues are invoiced
    harbour_dues_currency: str
    #: `approach_notes`, declared `text` NULL.
    #:
    #: Free text from the pilot book
    approach_notes: str | None

    def key(self) -> PortKey:
        """Return the primary key of the row."""
        return PortKey(
            port_id=self.port_id,
        )


@dataclasses.dataclass(frozen=True)
class PortKey:
    """The primary key of `freight.port`.

    Frozen, so that it hashes and compares by value. Every type this example's
    mapping produces is hashable, so no key of these three schemas is refused
    for that reason.
    """

    #: `port_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    port_id: int


class PortColumn(enum.Enum):
    """One column of `freight.port`.

    A search accepts an ordering term over these members and over nothing else,
    so an ordering can carry no SQL of its own: :attr:`sql` answers from a
    closed mapping this template wrote, and :meth:`from_name` is the one door a
    name a caller supplied can come through.

    A member's value is the column's name as the catalogue carries it, which is
    what :meth:`from_name` matches on and what ``member.value`` returns.
    """

    #: `port_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    PORT_ID = "port_id"
    #: `un_locode`, declared `char(5)` NOT NULL.
    UN_LOCODE = "un_locode"
    #: `name`, declared `varchar(120)` NOT NULL.
    NAME = "name"
    #: `country_code`, declared `char(2)` NOT NULL.
    COUNTRY_CODE = "country_code"
    #: `local_name`, declared `varchar(120)` NULL.
    LOCAL_NAME = "local_name"
    #: `timezone_name`, declared `varchar(64)` NOT NULL.
    TIMEZONE_NAME = "timezone_name"
    #: `utc_offset_minutes`, declared `smallint(6)` NOT NULL.
    UTC_OFFSET_MINUTES = "utc_offset_minutes"
    #: `berth_count`, declared `tinyint(3) unsigned` NOT NULL.
    BERTH_COUNT = "berth_count"
    #: `max_draught_m`, declared `decimal(5,2)` NOT NULL.
    MAX_DRAUGHT_M = "max_draught_m"
    #: `tidal_range_m`, declared `float` NULL.
    TIDAL_RANGE_M = "tidal_range_m"
    #: `annual_throughput_teu`, declared `int(10) unsigned` NULL.
    ANNUAL_THROUGHPUT_TEU = "annual_throughput_teu"
    #: `customs_round_clock`, declared `bit(1)` NOT NULL.
    CUSTOMS_ROUND_CLOCK = "customs_round_clock"
    #: `harbour_dues_currency`, declared `char(3)` NOT NULL.
    HARBOUR_DUES_CURRENCY = "harbour_dues_currency"
    #: `approach_notes`, declared `text` NULL.
    APPROACH_NOTES = "approach_notes"

    @property
    def sql(self) -> str:
        """The column as a statement's text names it: quoted for MariaDB."""
        return _COLUMNS[self]

    @classmethod
    def from_name(cls, name: str) -> PortColumn:
        """Read a column a caller supplied as text.

        Raises:
            UnknownColumn: for every name the table does not carry. That
                refusal is what keeps a caller's text out of a statement.
        """
        try:
            return cls(name)
        except ValueError:
            raise UnknownColumn("freight.port", name) from None


#: The one place a column becomes SQL. It is closed over the enumeration above,
#: and every identifier in it was quoted at render time by `tpl`, so no value a
#: caller supplies can reach a statement's text through an ordering term.
_COLUMNS: Final[Mapping[PortColumn, str]] = {
    PortColumn.PORT_ID: "`port_id`",
    PortColumn.UN_LOCODE: "`un_locode`",
    PortColumn.NAME: "`name`",
    PortColumn.COUNTRY_CODE: "`country_code`",
    PortColumn.LOCAL_NAME: "`local_name`",
    PortColumn.TIMEZONE_NAME: "`timezone_name`",
    PortColumn.UTC_OFFSET_MINUTES: "`utc_offset_minutes`",
    PortColumn.BERTH_COUNT: "`berth_count`",
    PortColumn.MAX_DRAUGHT_M: "`max_draught_m`",
    PortColumn.TIDAL_RANGE_M: "`tidal_range_m`",
    PortColumn.ANNUAL_THROUGHPUT_TEU: "`annual_throughput_teu`",
    PortColumn.CUSTOMS_ROUND_CLOCK: "`customs_round_clock`",
    PortColumn.HARBOUR_DUES_CURRENCY: "`harbour_dues_currency`",
    PortColumn.APPROACH_NOTES: "`approach_notes`",
}


@dataclasses.dataclass(frozen=True)
class PortOrder:
    """One ordering term of a search over `freight.port`."""

    #: The column to order by.
    column: PortColumn
    #: The direction to order in.
    direction: Direction = Direction.ASCENDING


@dataclasses.dataclass
class PortFilter:
    """What narrows a search over `freight.port`.

    Every field is optional: a field left ``None`` adds no condition, and the
    conditions a search does build are joined with ``AND``. The default
    instance is the filter that matches every row.

    A nullable column's field is ``None`` for *no condition* and never for
    *is NULL*; that question is asked through the ``…_is_null`` field such a
    column also gets, so the two meanings never collide.
    """

    #: Where set, requires `port_id` to equal it.
    port_id: int | None = None
    #: Where set, requires `port_id` to be at least it.
    port_id_from: int | None = None
    #: Where set, requires `port_id` to be at most it.
    port_id_to: int | None = None
    #: Where set, requires `un_locode` to equal it.
    un_locode: str | None = None
    #: Where set, requires `un_locode` to match it, as LIKE matches.
    un_locode_like: str | None = None
    #: Where set, requires `name` to equal it.
    name: str | None = None
    #: Where set, requires `name` to match it, as LIKE matches.
    name_like: str | None = None
    #: Where set, requires `country_code` to equal it.
    country_code: str | None = None
    #: Where set, requires `country_code` to match it, as LIKE matches.
    country_code_like: str | None = None
    #: Where set, requires `local_name` to equal it.
    local_name: str | None = None
    #: Where set, requires `local_name` to match it, as LIKE matches.
    local_name_like: str | None = None
    #: Where True, requires `local_name` to be NULL; where False, requires it not to be.
    local_name_is_null: bool | None = None
    #: Where set, requires `timezone_name` to equal it.
    timezone_name: str | None = None
    #: Where set, requires `timezone_name` to match it, as LIKE matches.
    timezone_name_like: str | None = None
    #: Where set, requires `utc_offset_minutes` to equal it.
    utc_offset_minutes: int | None = None
    #: Where set, requires `utc_offset_minutes` to be at least it.
    utc_offset_minutes_from: int | None = None
    #: Where set, requires `utc_offset_minutes` to be at most it.
    utc_offset_minutes_to: int | None = None
    #: Where set, requires `berth_count` to equal it.
    berth_count: int | None = None
    #: Where set, requires `berth_count` to be at least it.
    berth_count_from: int | None = None
    #: Where set, requires `berth_count` to be at most it.
    berth_count_to: int | None = None
    #: Where set, requires `max_draught_m` to equal it.
    max_draught_m: decimal.Decimal | None = None
    #: Where set, requires `max_draught_m` to be at least it.
    max_draught_m_from: decimal.Decimal | None = None
    #: Where set, requires `max_draught_m` to be at most it.
    max_draught_m_to: decimal.Decimal | None = None
    #: Where set, requires `tidal_range_m` to equal it.
    tidal_range_m: float | None = None
    #: Where set, requires `tidal_range_m` to be at least it.
    tidal_range_m_from: float | None = None
    #: Where set, requires `tidal_range_m` to be at most it.
    tidal_range_m_to: float | None = None
    #: Where True, requires `tidal_range_m` to be NULL; where False, requires it not to be.
    tidal_range_m_is_null: bool | None = None
    #: Where set, requires `annual_throughput_teu` to equal it.
    annual_throughput_teu: int | None = None
    #: Where set, requires `annual_throughput_teu` to be at least it.
    annual_throughput_teu_from: int | None = None
    #: Where set, requires `annual_throughput_teu` to be at most it.
    annual_throughput_teu_to: int | None = None
    #: Where True, requires `annual_throughput_teu` to be NULL; where False, requires it not to be.
    annual_throughput_teu_is_null: bool | None = None
    #: Where set, requires `customs_round_clock` to equal it.
    customs_round_clock: bytes | None = None
    #: Where set, requires `customs_round_clock` to be at least it.
    customs_round_clock_from: bytes | None = None
    #: Where set, requires `customs_round_clock` to be at most it.
    customs_round_clock_to: bytes | None = None
    #: Where set, requires `harbour_dues_currency` to equal it.
    harbour_dues_currency: str | None = None
    #: Where set, requires `harbour_dues_currency` to match it, as LIKE matches.
    harbour_dues_currency_like: str | None = None
    #: Where set, requires `approach_notes` to equal it.
    approach_notes: str | None = None
    #: Where set, requires `approach_notes` to match it, as LIKE matches.
    approach_notes_like: str | None = None
    #: Where True, requires `approach_notes` to be NULL; where False, requires it not to be.
    approach_notes_is_null: bool | None = None

    def is_empty(self) -> bool:
        """Is no field set?

        A search over an empty filter builds no ``WHERE`` clause and matches
        every row of the table.
        """
        return (
            self.port_id is None
            and self.port_id_from is None
            and self.port_id_to is None
            and self.un_locode is None
            and self.un_locode_like is None
            and self.name is None
            and self.name_like is None
            and self.country_code is None
            and self.country_code_like is None
            and self.local_name is None
            and self.local_name_like is None
            and self.local_name_is_null is None
            and self.timezone_name is None
            and self.timezone_name_like is None
            and self.utc_offset_minutes is None
            and self.utc_offset_minutes_from is None
            and self.utc_offset_minutes_to is None
            and self.berth_count is None
            and self.berth_count_from is None
            and self.berth_count_to is None
            and self.max_draught_m is None
            and self.max_draught_m_from is None
            and self.max_draught_m_to is None
            and self.tidal_range_m is None
            and self.tidal_range_m_from is None
            and self.tidal_range_m_to is None
            and self.tidal_range_m_is_null is None
            and self.annual_throughput_teu is None
            and self.annual_throughput_teu_from is None
            and self.annual_throughput_teu_to is None
            and self.annual_throughput_teu_is_null is None
            and self.customs_round_clock is None
            and self.customs_round_clock_from is None
            and self.customs_round_clock_to is None
            and self.harbour_dues_currency is None
            and self.harbour_dues_currency_like is None
            and self.approach_notes is None
            and self.approach_notes_like is None
            and self.approach_notes_is_null is None
        )
