# Code generated by tpl from freight.audit_event. DO NOT EDIT.

"""The `freight.audit_event` row, the key that addresses it, the columns an ordering may
name, and the filter a search takes.
"""

from __future__ import annotations

import dataclasses
import datetime
import enum
from collections.abc import Mapping
from typing import Final

from ._core import Direction, UnknownColumn

__all__ = [
    "AuditEvent",
    "AuditEventColumn",
    "AuditEventFilter",
    "AuditEventKey",
    "AuditEventOrder",
]


@dataclasses.dataclass
class AuditEvent:
    """One row of `freight.audit_event`.

    The dataclass is mutable, because :meth:`create` writes a server-assigned
    key back into it.
    """

    # The table's own comment, as the catalogue carries it:
    # Trilho de auditoria escrito pelos gatilhos; sobrevive à remoção das linhas que regista

    #: `audit_event_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    #:
    #: It is part of the primary key.
    audit_event_id: int
    #: `entity_name`, declared `varchar(64)` NOT NULL.
    entity_name: str
    #: `entity_key`, declared `varchar(64)` NOT NULL.
    entity_key: str
    #: `action`, declared `enum('INSERT','UPDATE','DELETE')` NOT NULL.
    action: str
    #: `trigger_timing`, declared `enum('BEFORE','AFTER')` NOT NULL.
    trigger_timing: str
    #: `changed_column`, declared `varchar(64)` NULL.
    changed_column: str | None
    #: `old_value`, declared `varchar(255)` NULL.
    old_value: str | None
    #: `new_value`, declared `varchar(255)` NULL.
    new_value: str | None
    #: `actor`, declared `varchar(96)` NOT NULL.
    actor: str
    #: `client_ipv4`, declared `inet4` NULL.
    client_ipv4: str | None
    #: `client_ipv6`, declared `inet6` NULL.
    client_ipv6: str | None
    #: `correlation_uuid`, declared `uuid` NULL.
    correlation_uuid: str | None
    #: `observed_at`, declared `datetime(6)` NOT NULL.
    observed_at: datetime.datetime
    #: `detail`, declared `text` NULL.
    detail: str | None

    def key(self) -> AuditEventKey:
        """Return the primary key of the row."""
        return AuditEventKey(
            audit_event_id=self.audit_event_id,
        )


@dataclasses.dataclass(frozen=True)
class AuditEventKey:
    """The primary key of `freight.audit_event`.

    Frozen, so that it hashes and compares by value. Every type this example's
    mapping produces is hashable, so no key of these three schemas is refused
    for that reason.
    """

    #: `audit_event_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    audit_event_id: int


class AuditEventColumn(enum.Enum):
    """One column of `freight.audit_event`.

    A search accepts an ordering term over these members and over nothing else,
    so an ordering can carry no SQL of its own: :attr:`sql` answers from a
    closed mapping this template wrote, and :meth:`from_name` is the one door a
    name a caller supplied can come through.

    A member's value is the column's name as the catalogue carries it, which is
    what :meth:`from_name` matches on and what ``member.value`` returns.
    """

    #: `audit_event_id`, declared `bigint(20) unsigned` NOT NULL AUTO_INCREMENT.
    AUDIT_EVENT_ID = "audit_event_id"
    #: `entity_name`, declared `varchar(64)` NOT NULL.
    ENTITY_NAME = "entity_name"
    #: `entity_key`, declared `varchar(64)` NOT NULL.
    ENTITY_KEY = "entity_key"
    #: `action`, declared `enum('INSERT','UPDATE','DELETE')` NOT NULL.
    ACTION = "action"
    #: `trigger_timing`, declared `enum('BEFORE','AFTER')` NOT NULL.
    TRIGGER_TIMING = "trigger_timing"
    #: `changed_column`, declared `varchar(64)` NULL.
    CHANGED_COLUMN = "changed_column"
    #: `old_value`, declared `varchar(255)` NULL.
    OLD_VALUE = "old_value"
    #: `new_value`, declared `varchar(255)` NULL.
    NEW_VALUE = "new_value"
    #: `actor`, declared `varchar(96)` NOT NULL.
    ACTOR = "actor"
    #: `client_ipv4`, declared `inet4` NULL.
    CLIENT_IPV4 = "client_ipv4"
    #: `client_ipv6`, declared `inet6` NULL.
    CLIENT_IPV6 = "client_ipv6"
    #: `correlation_uuid`, declared `uuid` NULL.
    CORRELATION_UUID = "correlation_uuid"
    #: `observed_at`, declared `datetime(6)` NOT NULL.
    OBSERVED_AT = "observed_at"
    #: `detail`, declared `text` NULL.
    DETAIL = "detail"

    @property
    def sql(self) -> str:
        """The column as a statement's text names it: quoted for MariaDB."""
        return _COLUMNS[self]

    @classmethod
    def from_name(cls, name: str) -> AuditEventColumn:
        """Read a column a caller supplied as text.

        Raises:
            UnknownColumn: for every name the table does not carry. That
                refusal is what keeps a caller's text out of a statement.
        """
        try:
            return cls(name)
        except ValueError:
            raise UnknownColumn("freight.audit_event", name) from None


#: The one place a column becomes SQL. It is closed over the enumeration above,
#: and every identifier in it was quoted at render time by `tpl`, so no value a
#: caller supplies can reach a statement's text through an ordering term.
_COLUMNS: Final[Mapping[AuditEventColumn, str]] = {
    AuditEventColumn.AUDIT_EVENT_ID: "`audit_event_id`",
    AuditEventColumn.ENTITY_NAME: "`entity_name`",
    AuditEventColumn.ENTITY_KEY: "`entity_key`",
    AuditEventColumn.ACTION: "`action`",
    AuditEventColumn.TRIGGER_TIMING: "`trigger_timing`",
    AuditEventColumn.CHANGED_COLUMN: "`changed_column`",
    AuditEventColumn.OLD_VALUE: "`old_value`",
    AuditEventColumn.NEW_VALUE: "`new_value`",
    AuditEventColumn.ACTOR: "`actor`",
    AuditEventColumn.CLIENT_IPV4: "`client_ipv4`",
    AuditEventColumn.CLIENT_IPV6: "`client_ipv6`",
    AuditEventColumn.CORRELATION_UUID: "`correlation_uuid`",
    AuditEventColumn.OBSERVED_AT: "`observed_at`",
    AuditEventColumn.DETAIL: "`detail`",
}


@dataclasses.dataclass(frozen=True)
class AuditEventOrder:
    """One ordering term of a search over `freight.audit_event`."""

    #: The column to order by.
    column: AuditEventColumn
    #: The direction to order in.
    direction: Direction = Direction.ASCENDING


@dataclasses.dataclass
class AuditEventFilter:
    """What narrows a search over `freight.audit_event`.

    Every field is optional: a field left ``None`` adds no condition, and the
    conditions a search does build are joined with ``AND``. The default
    instance is the filter that matches every row.

    A nullable column's field is ``None`` for *no condition* and never for
    *is NULL*; that question is asked through the ``…_is_null`` field such a
    column also gets, so the two meanings never collide.
    """

    #: Where set, requires `audit_event_id` to equal it.
    audit_event_id: int | None = None
    #: Where set, requires `audit_event_id` to be at least it.
    audit_event_id_from: int | None = None
    #: Where set, requires `audit_event_id` to be at most it.
    audit_event_id_to: int | None = None
    #: Where set, requires `entity_name` to equal it.
    entity_name: str | None = None
    #: Where set, requires `entity_name` to match it, as LIKE matches.
    entity_name_like: str | None = None
    #: Where set, requires `entity_key` to equal it.
    entity_key: str | None = None
    #: Where set, requires `entity_key` to match it, as LIKE matches.
    entity_key_like: str | None = None
    #: Where set, requires `action` to equal it.
    action: str | None = None
    #: Where set, requires `action` to match it, as LIKE matches.
    action_like: str | None = None
    #: Where set, requires `trigger_timing` to equal it.
    trigger_timing: str | None = None
    #: Where set, requires `trigger_timing` to match it, as LIKE matches.
    trigger_timing_like: str | None = None
    #: Where set, requires `changed_column` to equal it.
    changed_column: str | None = None
    #: Where set, requires `changed_column` to match it, as LIKE matches.
    changed_column_like: str | None = None
    #: Where True, requires `changed_column` to be NULL; where False, requires it not to be.
    changed_column_is_null: bool | None = None
    #: Where set, requires `old_value` to equal it.
    old_value: str | None = None
    #: Where set, requires `old_value` to match it, as LIKE matches.
    old_value_like: str | None = None
    #: Where True, requires `old_value` to be NULL; where False, requires it not to be.
    old_value_is_null: bool | None = None
    #: Where set, requires `new_value` to equal it.
    new_value: str | None = None
    #: Where set, requires `new_value` to match it, as LIKE matches.
    new_value_like: str | None = None
    #: Where True, requires `new_value` to be NULL; where False, requires it not to be.
    new_value_is_null: bool | None = None
    #: Where set, requires `actor` to equal it.
    actor: str | None = None
    #: Where set, requires `actor` to match it, as LIKE matches.
    actor_like: str | None = None
    #: Where set, requires `client_ipv4` to equal it.
    client_ipv4: str | None = None
    #: Where True, requires `client_ipv4` to be NULL; where False, requires it not to be.
    client_ipv4_is_null: bool | None = None
    #: Where set, requires `client_ipv6` to equal it.
    client_ipv6: str | None = None
    #: Where True, requires `client_ipv6` to be NULL; where False, requires it not to be.
    client_ipv6_is_null: bool | None = None
    #: Where set, requires `correlation_uuid` to equal it.
    correlation_uuid: str | None = None
    #: Where True, requires `correlation_uuid` to be NULL; where False, requires it not to be.
    correlation_uuid_is_null: bool | None = None
    #: Where set, requires `observed_at` to equal it.
    observed_at: datetime.datetime | None = None
    #: Where set, requires `observed_at` to be at least it.
    observed_at_from: datetime.datetime | None = None
    #: Where set, requires `observed_at` to be at most it.
    observed_at_to: datetime.datetime | None = None
    #: Where set, requires `detail` to equal it.
    detail: str | None = None
    #: Where set, requires `detail` to match it, as LIKE matches.
    detail_like: str | None = None
    #: Where True, requires `detail` to be NULL; where False, requires it not to be.
    detail_is_null: bool | None = None

    def is_empty(self) -> bool:
        """Is no field set?

        A search over an empty filter builds no ``WHERE`` clause and matches
        every row of the table.
        """
        return (
            self.audit_event_id is None
            and self.audit_event_id_from is None
            and self.audit_event_id_to is None
            and self.entity_name is None
            and self.entity_name_like is None
            and self.entity_key is None
            and self.entity_key_like is None
            and self.action is None
            and self.action_like is None
            and self.trigger_timing is None
            and self.trigger_timing_like is None
            and self.changed_column is None
            and self.changed_column_like is None
            and self.changed_column_is_null is None
            and self.old_value is None
            and self.old_value_like is None
            and self.old_value_is_null is None
            and self.new_value is None
            and self.new_value_like is None
            and self.new_value_is_null is None
            and self.actor is None
            and self.actor_like is None
            and self.client_ipv4 is None
            and self.client_ipv4_is_null is None
            and self.client_ipv6 is None
            and self.client_ipv6_is_null is None
            and self.correlation_uuid is None
            and self.correlation_uuid_is_null is None
            and self.observed_at is None
            and self.observed_at_from is None
            and self.observed_at_to is None
            and self.detail is None
            and self.detail_like is None
            and self.detail_is_null is None
        )
