# Code generated by tpl from freight.charge_code. DO NOT EDIT.

"""The PyMySQL implementation of the `freight.charge_code` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .charge_code import ChargeCode, ChargeCodeFilter, ChargeCodeKey, ChargeCodeOrder
from .charge_code_repository import ChargeCodeRepository

__all__ = ["ChargeCodePyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "freight.charge_code"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `charge_code`, `description`, `is_freight`, `vat_rate`, `gl_account` FROM `charge_code`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `charge_code`, `description`, `is_freight`, `vat_rate`, `gl_account` FROM `charge_code` WHERE `charge_code` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `charge_code` (`charge_code`, `description`, `is_freight`, `vat_rate`, `gl_account`) VALUES (%s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `charge_code` SET `description` = %s, `is_freight` = %s, `vat_rate` = %s, `gl_account` = %s WHERE `charge_code` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `charge_code` WHERE `charge_code` = %s"


def _row(values: tuple[Any, ...]) -> ChargeCode:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return ChargeCode(
        charge_code=values[0],
        description=values[1],
        is_freight=values[2],
        vat_rate=values[3],
        gl_account=values[4],
    )


class ChargeCodePyMySQL:
    """The PyMySQL data layer of `freight.charge_code`.

    It names no base class. ``ChargeCodeRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `freight.charge_code` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: ChargeCode) -> None:
        """Insert `row`.

        The whole key is taken from `row`, which the caller therefore fills.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.charge_code,
                    row.description,
                    row.is_freight,
                    row.vat_rate,
                    row.gl_account,
                ),
            )

    def read(self, key: ChargeCodeKey) -> ChargeCode:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.charge_code,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: ChargeCode) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.description,
                    row.is_freight,
                    row.vat_rate,
                    row.gl_account,
                    row.charge_code,
                ),
            )
        return affected

    def delete(self, key: ChargeCodeKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.charge_code,
                ),
            )
        return affected

    def search(
        self,
        criteria: ChargeCodeFilter,
        order: Sequence[ChargeCodeOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[ChargeCode]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.charge_code is not None:
            conditions.append("`charge_code` = %s")
            arguments.append(criteria.charge_code)
        if criteria.charge_code_like is not None:
            conditions.append("`charge_code` LIKE %s")
            arguments.append(criteria.charge_code_like)
        if criteria.description is not None:
            conditions.append("`description` = %s")
            arguments.append(criteria.description)
        if criteria.description_like is not None:
            conditions.append("`description` LIKE %s")
            arguments.append(criteria.description_like)
        if criteria.is_freight is not None:
            conditions.append("`is_freight` = %s")
            arguments.append(criteria.is_freight)
        if criteria.is_freight_from is not None:
            conditions.append("`is_freight` >= %s")
            arguments.append(criteria.is_freight_from)
        if criteria.is_freight_to is not None:
            conditions.append("`is_freight` <= %s")
            arguments.append(criteria.is_freight_to)
        if criteria.vat_rate is not None:
            conditions.append("`vat_rate` = %s")
            arguments.append(criteria.vat_rate)
        if criteria.vat_rate_from is not None:
            conditions.append("`vat_rate` >= %s")
            arguments.append(criteria.vat_rate_from)
        if criteria.vat_rate_to is not None:
            conditions.append("`vat_rate` <= %s")
            arguments.append(criteria.vat_rate_to)
        if criteria.gl_account is not None:
            conditions.append("`gl_account` = %s")
            arguments.append(criteria.gl_account)
        if criteria.gl_account_like is not None:
            conditions.append("`gl_account` LIKE %s")
            arguments.append(criteria.gl_account_like)

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: ChargeCodePyMySQL,
    ) -> ChargeCodeRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
