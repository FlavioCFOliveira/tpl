# Code generated by tpl from freight.voyage_leg. DO NOT EDIT.

"""The PyMySQL implementation of the `freight.voyage_leg` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .voyage_leg import VoyageLeg, VoyageLegFilter, VoyageLegKey, VoyageLegOrder
from .voyage_leg_repository import VoyageLegRepository

__all__ = ["VoyageLegPyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "freight.voyage_leg"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `vessel_imo`, `voyage_number`, `leg_sequence`, `port_id`, `eta`, `etd`, `ata`, `berth_shift_time`, `pilot_required`, `moves_discharged`, `moves_loaded`, `moves_total` FROM `voyage_leg`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `vessel_imo`, `voyage_number`, `leg_sequence`, `port_id`, `eta`, `etd`, `ata`, `berth_shift_time`, `pilot_required`, `moves_discharged`, `moves_loaded`, `moves_total` FROM `voyage_leg` WHERE `vessel_imo` = %s AND `voyage_number` = %s AND `leg_sequence` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `voyage_leg` (`vessel_imo`, `voyage_number`, `leg_sequence`, `port_id`, `eta`, `etd`, `ata`, `berth_shift_time`, `pilot_required`, `moves_discharged`, `moves_loaded`) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `voyage_leg` SET `port_id` = %s, `eta` = %s, `etd` = %s, `ata` = %s, `berth_shift_time` = %s, `pilot_required` = %s, `moves_discharged` = %s, `moves_loaded` = %s WHERE `vessel_imo` = %s AND `voyage_number` = %s AND `leg_sequence` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `voyage_leg` WHERE `vessel_imo` = %s AND `voyage_number` = %s AND `leg_sequence` = %s"


def _row(values: tuple[Any, ...]) -> VoyageLeg:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return VoyageLeg(
        vessel_imo=values[0],
        voyage_number=values[1],
        leg_sequence=values[2],
        port_id=values[3],
        eta=values[4],
        etd=values[5],
        ata=values[6],
        berth_shift_time=values[7],
        pilot_required=values[8],
        moves_discharged=values[9],
        moves_loaded=values[10],
        moves_total=values[11],
    )


class VoyageLegPyMySQL:
    """The PyMySQL data layer of `freight.voyage_leg`.

    It names no base class. ``VoyageLegRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `freight.voyage_leg` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: VoyageLeg) -> None:
        """Insert `row`.

        The whole key is taken from `row`, which the caller therefore fills.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.vessel_imo,
                    row.voyage_number,
                    row.leg_sequence,
                    row.port_id,
                    row.eta,
                    row.etd,
                    row.ata,
                    row.berth_shift_time,
                    row.pilot_required,
                    row.moves_discharged,
                    row.moves_loaded,
                ),
            )

    def read(self, key: VoyageLegKey) -> VoyageLeg:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.vessel_imo,
                    key.voyage_number,
                    key.leg_sequence,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: VoyageLeg) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.port_id,
                    row.eta,
                    row.etd,
                    row.ata,
                    row.berth_shift_time,
                    row.pilot_required,
                    row.moves_discharged,
                    row.moves_loaded,
                    row.vessel_imo,
                    row.voyage_number,
                    row.leg_sequence,
                ),
            )
        return affected

    def delete(self, key: VoyageLegKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.vessel_imo,
                    key.voyage_number,
                    key.leg_sequence,
                ),
            )
        return affected

    def search(
        self,
        criteria: VoyageLegFilter,
        order: Sequence[VoyageLegOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[VoyageLeg]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.vessel_imo is not None:
            conditions.append("`vessel_imo` = %s")
            arguments.append(criteria.vessel_imo)
        if criteria.vessel_imo_like is not None:
            conditions.append("`vessel_imo` LIKE %s")
            arguments.append(criteria.vessel_imo_like)
        if criteria.voyage_number is not None:
            conditions.append("`voyage_number` = %s")
            arguments.append(criteria.voyage_number)
        if criteria.voyage_number_like is not None:
            conditions.append("`voyage_number` LIKE %s")
            arguments.append(criteria.voyage_number_like)
        if criteria.leg_sequence is not None:
            conditions.append("`leg_sequence` = %s")
            arguments.append(criteria.leg_sequence)
        if criteria.leg_sequence_from is not None:
            conditions.append("`leg_sequence` >= %s")
            arguments.append(criteria.leg_sequence_from)
        if criteria.leg_sequence_to is not None:
            conditions.append("`leg_sequence` <= %s")
            arguments.append(criteria.leg_sequence_to)
        if criteria.port_id is not None:
            conditions.append("`port_id` = %s")
            arguments.append(criteria.port_id)
        if criteria.port_id_from is not None:
            conditions.append("`port_id` >= %s")
            arguments.append(criteria.port_id_from)
        if criteria.port_id_to is not None:
            conditions.append("`port_id` <= %s")
            arguments.append(criteria.port_id_to)
        if criteria.eta is not None:
            conditions.append("`eta` = %s")
            arguments.append(criteria.eta)
        if criteria.eta_from is not None:
            conditions.append("`eta` >= %s")
            arguments.append(criteria.eta_from)
        if criteria.eta_to is not None:
            conditions.append("`eta` <= %s")
            arguments.append(criteria.eta_to)
        if criteria.etd is not None:
            conditions.append("`etd` = %s")
            arguments.append(criteria.etd)
        if criteria.etd_from is not None:
            conditions.append("`etd` >= %s")
            arguments.append(criteria.etd_from)
        if criteria.etd_to is not None:
            conditions.append("`etd` <= %s")
            arguments.append(criteria.etd_to)
        if criteria.ata is not None:
            conditions.append("`ata` = %s")
            arguments.append(criteria.ata)
        if criteria.ata_from is not None:
            conditions.append("`ata` >= %s")
            arguments.append(criteria.ata_from)
        if criteria.ata_to is not None:
            conditions.append("`ata` <= %s")
            arguments.append(criteria.ata_to)
        if criteria.ata_is_null is not None:
            conditions.append(
                "`ata` IS NULL"
                if criteria.ata_is_null
                else "`ata` IS NOT NULL"
            )
        if criteria.berth_shift_time is not None:
            conditions.append("`berth_shift_time` = %s")
            arguments.append(criteria.berth_shift_time)
        if criteria.berth_shift_time_from is not None:
            conditions.append("`berth_shift_time` >= %s")
            arguments.append(criteria.berth_shift_time_from)
        if criteria.berth_shift_time_to is not None:
            conditions.append("`berth_shift_time` <= %s")
            arguments.append(criteria.berth_shift_time_to)
        if criteria.berth_shift_time_is_null is not None:
            conditions.append(
                "`berth_shift_time` IS NULL"
                if criteria.berth_shift_time_is_null
                else "`berth_shift_time` IS NOT NULL"
            )
        if criteria.pilot_required is not None:
            conditions.append("`pilot_required` = %s")
            arguments.append(criteria.pilot_required)
        if criteria.pilot_required_from is not None:
            conditions.append("`pilot_required` >= %s")
            arguments.append(criteria.pilot_required_from)
        if criteria.pilot_required_to is not None:
            conditions.append("`pilot_required` <= %s")
            arguments.append(criteria.pilot_required_to)
        if criteria.moves_discharged is not None:
            conditions.append("`moves_discharged` = %s")
            arguments.append(criteria.moves_discharged)
        if criteria.moves_discharged_from is not None:
            conditions.append("`moves_discharged` >= %s")
            arguments.append(criteria.moves_discharged_from)
        if criteria.moves_discharged_to is not None:
            conditions.append("`moves_discharged` <= %s")
            arguments.append(criteria.moves_discharged_to)
        if criteria.moves_loaded is not None:
            conditions.append("`moves_loaded` = %s")
            arguments.append(criteria.moves_loaded)
        if criteria.moves_loaded_from is not None:
            conditions.append("`moves_loaded` >= %s")
            arguments.append(criteria.moves_loaded_from)
        if criteria.moves_loaded_to is not None:
            conditions.append("`moves_loaded` <= %s")
            arguments.append(criteria.moves_loaded_to)
        if criteria.moves_total is not None:
            conditions.append("`moves_total` = %s")
            arguments.append(criteria.moves_total)
        if criteria.moves_total_from is not None:
            conditions.append("`moves_total` >= %s")
            arguments.append(criteria.moves_total_from)
        if criteria.moves_total_to is not None:
            conditions.append("`moves_total` <= %s")
            arguments.append(criteria.moves_total_to)
        if criteria.moves_total_is_null is not None:
            conditions.append(
                "`moves_total` IS NULL"
                if criteria.moves_total_is_null
                else "`moves_total` IS NOT NULL"
            )

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: VoyageLegPyMySQL,
    ) -> VoyageLegRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
