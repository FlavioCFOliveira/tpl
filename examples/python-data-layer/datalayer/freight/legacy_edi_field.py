# Code generated by tpl from freight.legacy_edi_field. DO NOT EDIT.

"""The `freight.legacy_edi_field` row, the key that addresses it, the columns an ordering may
name, and the filter a search takes.
"""

from __future__ import annotations

import dataclasses
import datetime
import enum
from collections.abc import Mapping
from typing import Final

from ._core import Direction, UnknownColumn

__all__ = [
    "LegacyEdiField",
    "LegacyEdiFieldColumn",
    "LegacyEdiFieldFilter",
    "LegacyEdiFieldKey",
    "LegacyEdiFieldOrder",
]


@dataclasses.dataclass
class LegacyEdiField:
    """One row of `freight.legacy_edi_field`.

    The dataclass is mutable, because :meth:`create` writes a server-assigned
    key back into it.
    """

    # The table's own comment, as the catalogue carries it:
    # CASOS-LIMITE DE IDENTIFICADORES — mapeamento EDIFACT herdado; os nomes das colunas são deliberadamente hostis ao parser

    #: `mapping_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    #:
    #: It is part of the primary key.
    mapping_id: int
    #: `segment_tag`, declared `char(3)` NOT NULL.
    #:
    #: It is part of a unique index.
    #:
    #: EDIFACT segment tag, e.g. BGM, NAD, CNI
    segment_tag: str
    #: `back`tick`, declared `varchar(48)` NULL.
    #:
    #: Identifier containing an embedded backtick, doubled when quoted
    back_tick: str | None
    #: `space in name`, declared `varchar(48)` NULL.
    #:
    #: Identifier containing a space
    space_in_name: str | None
    #: `select`, declared `varchar(48)` NULL.
    #:
    #: Identifier that is a reserved word
    select: str | None
    #: `posição`, declared `varchar(48)` NULL.
    #:
    #: Identifier containing non-ASCII letters
    posição: str | None
    #: `Mixed Case Column`, declared `varchar(48)` NULL.
    #:
    #: Identifier mixing case and spaces
    mixed_case_column: str | None
    #: `retired_on`, declared `date` NULL.
    retired_on: datetime.date | None

    def key(self) -> LegacyEdiFieldKey:
        """Return the primary key of the row."""
        return LegacyEdiFieldKey(
            mapping_id=self.mapping_id,
        )


@dataclasses.dataclass(frozen=True)
class LegacyEdiFieldKey:
    """The primary key of `freight.legacy_edi_field`.

    Frozen, so that it hashes and compares by value. Every type this example's
    mapping produces is hashable, so no key of these three schemas is refused
    for that reason.
    """

    #: `mapping_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    mapping_id: int


class LegacyEdiFieldColumn(enum.Enum):
    """One column of `freight.legacy_edi_field`.

    A search accepts an ordering term over these members and over nothing else,
    so an ordering can carry no SQL of its own: :attr:`sql` answers from a
    closed mapping this template wrote, and :meth:`from_name` is the one door a
    name a caller supplied can come through.

    A member's value is the column's name as the catalogue carries it, which is
    what :meth:`from_name` matches on and what ``member.value`` returns.
    """

    #: `mapping_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    MAPPING_ID = "mapping_id"
    #: `segment_tag`, declared `char(3)` NOT NULL.
    SEGMENT_TAG = "segment_tag"
    #: `back`tick`, declared `varchar(48)` NULL.
    BACK_TICK = "back`tick"
    #: `space in name`, declared `varchar(48)` NULL.
    SPACE_IN_NAME = "space in name"
    #: `select`, declared `varchar(48)` NULL.
    SELECT = "select"
    #: `posição`, declared `varchar(48)` NULL.
    POSIçãO = "posição"
    #: `Mixed Case Column`, declared `varchar(48)` NULL.
    MIXED_CASE_COLUMN = "Mixed Case Column"
    #: `retired_on`, declared `date` NULL.
    RETIRED_ON = "retired_on"

    @property
    def sql(self) -> str:
        """The column as a statement's text names it: quoted for MariaDB."""
        return _COLUMNS[self]

    @classmethod
    def from_name(cls, name: str) -> LegacyEdiFieldColumn:
        """Read a column a caller supplied as text.

        Raises:
            UnknownColumn: for every name the table does not carry. That
                refusal is what keeps a caller's text out of a statement.
        """
        try:
            return cls(name)
        except ValueError:
            raise UnknownColumn("freight.legacy_edi_field", name) from None


#: The one place a column becomes SQL. It is closed over the enumeration above,
#: and every identifier in it was quoted at render time by `tpl`, so no value a
#: caller supplies can reach a statement's text through an ordering term.
_COLUMNS: Final[Mapping[LegacyEdiFieldColumn, str]] = {
    LegacyEdiFieldColumn.MAPPING_ID: "`mapping_id`",
    LegacyEdiFieldColumn.SEGMENT_TAG: "`segment_tag`",
    LegacyEdiFieldColumn.BACK_TICK: "`back``tick`",
    LegacyEdiFieldColumn.SPACE_IN_NAME: "`space in name`",
    LegacyEdiFieldColumn.SELECT: "`select`",
    LegacyEdiFieldColumn.POSIçãO: "`posição`",
    LegacyEdiFieldColumn.MIXED_CASE_COLUMN: "`Mixed Case Column`",
    LegacyEdiFieldColumn.RETIRED_ON: "`retired_on`",
}


@dataclasses.dataclass(frozen=True)
class LegacyEdiFieldOrder:
    """One ordering term of a search over `freight.legacy_edi_field`."""

    #: The column to order by.
    column: LegacyEdiFieldColumn
    #: The direction to order in.
    direction: Direction = Direction.ASCENDING


@dataclasses.dataclass
class LegacyEdiFieldFilter:
    """What narrows a search over `freight.legacy_edi_field`.

    Every field is optional: a field left ``None`` adds no condition, and the
    conditions a search does build are joined with ``AND``. The default
    instance is the filter that matches every row.

    A nullable column's field is ``None`` for *no condition* and never for
    *is NULL*; that question is asked through the ``…_is_null`` field such a
    column also gets, so the two meanings never collide.
    """

    #: Where set, requires `mapping_id` to equal it.
    mapping_id: int | None = None
    #: Where set, requires `mapping_id` to be at least it.
    mapping_id_from: int | None = None
    #: Where set, requires `mapping_id` to be at most it.
    mapping_id_to: int | None = None
    #: Where set, requires `segment_tag` to equal it.
    segment_tag: str | None = None
    #: Where set, requires `segment_tag` to match it, as LIKE matches.
    segment_tag_like: str | None = None
    #: Where set, requires `back`tick` to equal it.
    back_tick: str | None = None
    #: Where set, requires `back`tick` to match it, as LIKE matches.
    back_tick_like: str | None = None
    #: Where True, requires `back`tick` to be NULL; where False, requires it not to be.
    back_tick_is_null: bool | None = None
    #: Where set, requires `space in name` to equal it.
    space_in_name: str | None = None
    #: Where set, requires `space in name` to match it, as LIKE matches.
    space_in_name_like: str | None = None
    #: Where True, requires `space in name` to be NULL; where False, requires it not to be.
    space_in_name_is_null: bool | None = None
    #: Where set, requires `select` to equal it.
    select: str | None = None
    #: Where set, requires `select` to match it, as LIKE matches.
    select_like: str | None = None
    #: Where True, requires `select` to be NULL; where False, requires it not to be.
    select_is_null: bool | None = None
    #: Where set, requires `posição` to equal it.
    posição: str | None = None
    #: Where set, requires `posição` to match it, as LIKE matches.
    posição_like: str | None = None
    #: Where True, requires `posição` to be NULL; where False, requires it not to be.
    posição_is_null: bool | None = None
    #: Where set, requires `Mixed Case Column` to equal it.
    mixed_case_column: str | None = None
    #: Where set, requires `Mixed Case Column` to match it, as LIKE matches.
    mixed_case_column_like: str | None = None
    #: Where True, requires `Mixed Case Column` to be NULL; where False, requires it not to be.
    mixed_case_column_is_null: bool | None = None
    #: Where set, requires `retired_on` to equal it.
    retired_on: datetime.date | None = None
    #: Where set, requires `retired_on` to be at least it.
    retired_on_from: datetime.date | None = None
    #: Where set, requires `retired_on` to be at most it.
    retired_on_to: datetime.date | None = None
    #: Where True, requires `retired_on` to be NULL; where False, requires it not to be.
    retired_on_is_null: bool | None = None

    def is_empty(self) -> bool:
        """Is no field set?

        A search over an empty filter builds no ``WHERE`` clause and matches
        every row of the table.
        """
        return (
            self.mapping_id is None
            and self.mapping_id_from is None
            and self.mapping_id_to is None
            and self.segment_tag is None
            and self.segment_tag_like is None
            and self.back_tick is None
            and self.back_tick_like is None
            and self.back_tick_is_null is None
            and self.space_in_name is None
            and self.space_in_name_like is None
            and self.space_in_name_is_null is None
            and self.select is None
            and self.select_like is None
            and self.select_is_null is None
            and self.posição is None
            and self.posição_like is None
            and self.posição_is_null is None
            and self.mixed_case_column is None
            and self.mixed_case_column_like is None
            and self.mixed_case_column_is_null is None
            and self.retired_on is None
            and self.retired_on_from is None
            and self.retired_on_to is None
            and self.retired_on_is_null is None
        )
