# Code generated by tpl from sakila.address. DO NOT EDIT.

"""The `sakila.address` row, the key that addresses it, the columns an ordering may
name, and the filter a search takes.
"""

from __future__ import annotations

import dataclasses
import datetime
import enum
from collections.abc import Mapping
from typing import Final

from ._core import Direction, UnknownColumn

__all__ = [
    "Address",
    "AddressColumn",
    "AddressFilter",
    "AddressKey",
    "AddressOrder",
]


@dataclasses.dataclass
class Address:
    """One row of `sakila.address`.

    The dataclass is mutable, because :meth:`create` writes a server-assigned
    key back into it.
    """

    #: `address_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    #:
    #: It is part of the primary key.
    address_id: int
    #: `address`, declared `varchar(50)` NOT NULL.
    address: str
    #: `address2`, declared `varchar(50)` NULL.
    address2: str | None
    #: `district`, declared `varchar(20)` NOT NULL.
    district: str
    #: `city_id`, declared `smallint(5) unsigned` NOT NULL.
    city_id: int
    #: `postal_code`, declared `varchar(10)` NULL.
    postal_code: str | None
    #: `phone`, declared `varchar(20)` NOT NULL.
    phone: str
    #: `last_update`, declared `timestamp` NOT NULL.
    last_update: datetime.datetime

    def key(self) -> AddressKey:
        """Return the primary key of the row."""
        return AddressKey(
            address_id=self.address_id,
        )


@dataclasses.dataclass(frozen=True)
class AddressKey:
    """The primary key of `sakila.address`.

    Frozen, so that it hashes and compares by value. Every type this example's
    mapping produces is hashable, so no key of these three schemas is refused
    for that reason.
    """

    #: `address_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    address_id: int


class AddressColumn(enum.Enum):
    """One column of `sakila.address`.

    A search accepts an ordering term over these members and over nothing else,
    so an ordering can carry no SQL of its own: :attr:`sql` answers from a
    closed mapping this template wrote, and :meth:`from_name` is the one door a
    name a caller supplied can come through.

    A member's value is the column's name as the catalogue carries it, which is
    what :meth:`from_name` matches on and what ``member.value`` returns.
    """

    #: `address_id`, declared `smallint(5) unsigned` NOT NULL AUTO_INCREMENT.
    ADDRESS_ID = "address_id"
    #: `address`, declared `varchar(50)` NOT NULL.
    ADDRESS = "address"
    #: `address2`, declared `varchar(50)` NULL.
    ADDRESS2 = "address2"
    #: `district`, declared `varchar(20)` NOT NULL.
    DISTRICT = "district"
    #: `city_id`, declared `smallint(5) unsigned` NOT NULL.
    CITY_ID = "city_id"
    #: `postal_code`, declared `varchar(10)` NULL.
    POSTAL_CODE = "postal_code"
    #: `phone`, declared `varchar(20)` NOT NULL.
    PHONE = "phone"
    #: `last_update`, declared `timestamp` NOT NULL.
    LAST_UPDATE = "last_update"

    @property
    def sql(self) -> str:
        """The column as a statement's text names it: quoted for MariaDB."""
        return _COLUMNS[self]

    @classmethod
    def from_name(cls, name: str) -> AddressColumn:
        """Read a column a caller supplied as text.

        Raises:
            UnknownColumn: for every name the table does not carry. That
                refusal is what keeps a caller's text out of a statement.
        """
        try:
            return cls(name)
        except ValueError:
            raise UnknownColumn("sakila.address", name) from None


#: The one place a column becomes SQL. It is closed over the enumeration above,
#: and every identifier in it was quoted at render time by `tpl`, so no value a
#: caller supplies can reach a statement's text through an ordering term.
_COLUMNS: Final[Mapping[AddressColumn, str]] = {
    AddressColumn.ADDRESS_ID: "`address_id`",
    AddressColumn.ADDRESS: "`address`",
    AddressColumn.ADDRESS2: "`address2`",
    AddressColumn.DISTRICT: "`district`",
    AddressColumn.CITY_ID: "`city_id`",
    AddressColumn.POSTAL_CODE: "`postal_code`",
    AddressColumn.PHONE: "`phone`",
    AddressColumn.LAST_UPDATE: "`last_update`",
}


@dataclasses.dataclass(frozen=True)
class AddressOrder:
    """One ordering term of a search over `sakila.address`."""

    #: The column to order by.
    column: AddressColumn
    #: The direction to order in.
    direction: Direction = Direction.ASCENDING


@dataclasses.dataclass
class AddressFilter:
    """What narrows a search over `sakila.address`.

    Every field is optional: a field left ``None`` adds no condition, and the
    conditions a search does build are joined with ``AND``. The default
    instance is the filter that matches every row.

    A nullable column's field is ``None`` for *no condition* and never for
    *is NULL*; that question is asked through the ``…_is_null`` field such a
    column also gets, so the two meanings never collide.
    """

    #: Where set, requires `address_id` to equal it.
    address_id: int | None = None
    #: Where set, requires `address_id` to be at least it.
    address_id_from: int | None = None
    #: Where set, requires `address_id` to be at most it.
    address_id_to: int | None = None
    #: Where set, requires `address` to equal it.
    address: str | None = None
    #: Where set, requires `address` to match it, as LIKE matches.
    address_like: str | None = None
    #: Where set, requires `address2` to equal it.
    address2: str | None = None
    #: Where set, requires `address2` to match it, as LIKE matches.
    address2_like: str | None = None
    #: Where True, requires `address2` to be NULL; where False, requires it not to be.
    address2_is_null: bool | None = None
    #: Where set, requires `district` to equal it.
    district: str | None = None
    #: Where set, requires `district` to match it, as LIKE matches.
    district_like: str | None = None
    #: Where set, requires `city_id` to equal it.
    city_id: int | None = None
    #: Where set, requires `city_id` to be at least it.
    city_id_from: int | None = None
    #: Where set, requires `city_id` to be at most it.
    city_id_to: int | None = None
    #: Where set, requires `postal_code` to equal it.
    postal_code: str | None = None
    #: Where set, requires `postal_code` to match it, as LIKE matches.
    postal_code_like: str | None = None
    #: Where True, requires `postal_code` to be NULL; where False, requires it not to be.
    postal_code_is_null: bool | None = None
    #: Where set, requires `phone` to equal it.
    phone: str | None = None
    #: Where set, requires `phone` to match it, as LIKE matches.
    phone_like: str | None = None
    #: Where set, requires `last_update` to equal it.
    last_update: datetime.datetime | None = None
    #: Where set, requires `last_update` to be at least it.
    last_update_from: datetime.datetime | None = None
    #: Where set, requires `last_update` to be at most it.
    last_update_to: datetime.datetime | None = None

    def is_empty(self) -> bool:
        """Is no field set?

        A search over an empty filter builds no ``WHERE`` clause and matches
        every row of the table.
        """
        return (
            self.address_id is None
            and self.address_id_from is None
            and self.address_id_to is None
            and self.address is None
            and self.address_like is None
            and self.address2 is None
            and self.address2_like is None
            and self.address2_is_null is None
            and self.district is None
            and self.district_like is None
            and self.city_id is None
            and self.city_id_from is None
            and self.city_id_to is None
            and self.postal_code is None
            and self.postal_code_like is None
            and self.postal_code_is_null is None
            and self.phone is None
            and self.phone_like is None
            and self.last_update is None
            and self.last_update_from is None
            and self.last_update_to is None
        )
