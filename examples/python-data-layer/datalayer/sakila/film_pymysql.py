# Code generated by tpl from sakila.film. DO NOT EDIT.

"""The PyMySQL implementation of the `sakila.film` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .film import Film, FilmFilter, FilmKey, FilmOrder
from .film_repository import FilmRepository

__all__ = ["FilmPyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "sakila.film"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `film_id`, `title`, `description`, `release_year`, `language_id`, `original_language_id`, `rental_duration`, `rental_rate`, `length`, `replacement_cost`, `rating`, `special_features`, `last_update` FROM `film`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `film_id`, `title`, `description`, `release_year`, `language_id`, `original_language_id`, `rental_duration`, `rental_rate`, `length`, `replacement_cost`, `rating`, `special_features`, `last_update` FROM `film` WHERE `film_id` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `film` (`title`, `description`, `release_year`, `language_id`, `original_language_id`, `rental_duration`, `rental_rate`, `length`, `replacement_cost`, `rating`, `special_features`, `last_update`) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `film` SET `title` = %s, `description` = %s, `release_year` = %s, `language_id` = %s, `original_language_id` = %s, `rental_duration` = %s, `rental_rate` = %s, `length` = %s, `replacement_cost` = %s, `rating` = %s, `special_features` = %s, `last_update` = %s WHERE `film_id` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `film` WHERE `film_id` = %s"


def _row(values: tuple[Any, ...]) -> Film:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return Film(
        film_id=values[0],
        title=values[1],
        description=values[2],
        release_year=values[3],
        language_id=values[4],
        original_language_id=values[5],
        rental_duration=values[6],
        rental_rate=values[7],
        length=values[8],
        replacement_cost=values[9],
        rating=values[10],
        special_features=values[11],
        last_update=values[12],
    )


class FilmPyMySQL:
    """The PyMySQL data layer of `sakila.film`.

    It names no base class. ``FilmRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `sakila.film` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: Film) -> None:
        """Insert `row`.

        The key is assigned by the server and is written back into `row`.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.title,
                    row.description,
                    row.release_year,
                    row.language_id,
                    row.original_language_id,
                    row.rental_duration,
                    row.rental_rate,
                    row.length,
                    row.replacement_cost,
                    row.rating,
                    row.special_features,
                    row.last_update,
                ),
            )
            row.film_id = cursor.lastrowid

    def read(self, key: FilmKey) -> Film:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.film_id,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: Film) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.title,
                    row.description,
                    row.release_year,
                    row.language_id,
                    row.original_language_id,
                    row.rental_duration,
                    row.rental_rate,
                    row.length,
                    row.replacement_cost,
                    row.rating,
                    row.special_features,
                    row.last_update,
                    row.film_id,
                ),
            )
        return affected

    def delete(self, key: FilmKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.film_id,
                ),
            )
        return affected

    def search(
        self,
        criteria: FilmFilter,
        order: Sequence[FilmOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[Film]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.film_id is not None:
            conditions.append("`film_id` = %s")
            arguments.append(criteria.film_id)
        if criteria.film_id_from is not None:
            conditions.append("`film_id` >= %s")
            arguments.append(criteria.film_id_from)
        if criteria.film_id_to is not None:
            conditions.append("`film_id` <= %s")
            arguments.append(criteria.film_id_to)
        if criteria.title is not None:
            conditions.append("`title` = %s")
            arguments.append(criteria.title)
        if criteria.title_like is not None:
            conditions.append("`title` LIKE %s")
            arguments.append(criteria.title_like)
        if criteria.description is not None:
            conditions.append("`description` = %s")
            arguments.append(criteria.description)
        if criteria.description_like is not None:
            conditions.append("`description` LIKE %s")
            arguments.append(criteria.description_like)
        if criteria.description_is_null is not None:
            conditions.append(
                "`description` IS NULL"
                if criteria.description_is_null
                else "`description` IS NOT NULL"
            )
        if criteria.release_year is not None:
            conditions.append("`release_year` = %s")
            arguments.append(criteria.release_year)
        if criteria.release_year_from is not None:
            conditions.append("`release_year` >= %s")
            arguments.append(criteria.release_year_from)
        if criteria.release_year_to is not None:
            conditions.append("`release_year` <= %s")
            arguments.append(criteria.release_year_to)
        if criteria.release_year_is_null is not None:
            conditions.append(
                "`release_year` IS NULL"
                if criteria.release_year_is_null
                else "`release_year` IS NOT NULL"
            )
        if criteria.language_id is not None:
            conditions.append("`language_id` = %s")
            arguments.append(criteria.language_id)
        if criteria.language_id_from is not None:
            conditions.append("`language_id` >= %s")
            arguments.append(criteria.language_id_from)
        if criteria.language_id_to is not None:
            conditions.append("`language_id` <= %s")
            arguments.append(criteria.language_id_to)
        if criteria.original_language_id is not None:
            conditions.append("`original_language_id` = %s")
            arguments.append(criteria.original_language_id)
        if criteria.original_language_id_from is not None:
            conditions.append("`original_language_id` >= %s")
            arguments.append(criteria.original_language_id_from)
        if criteria.original_language_id_to is not None:
            conditions.append("`original_language_id` <= %s")
            arguments.append(criteria.original_language_id_to)
        if criteria.original_language_id_is_null is not None:
            conditions.append(
                "`original_language_id` IS NULL"
                if criteria.original_language_id_is_null
                else "`original_language_id` IS NOT NULL"
            )
        if criteria.rental_duration is not None:
            conditions.append("`rental_duration` = %s")
            arguments.append(criteria.rental_duration)
        if criteria.rental_duration_from is not None:
            conditions.append("`rental_duration` >= %s")
            arguments.append(criteria.rental_duration_from)
        if criteria.rental_duration_to is not None:
            conditions.append("`rental_duration` <= %s")
            arguments.append(criteria.rental_duration_to)
        if criteria.rental_rate is not None:
            conditions.append("`rental_rate` = %s")
            arguments.append(criteria.rental_rate)
        if criteria.rental_rate_from is not None:
            conditions.append("`rental_rate` >= %s")
            arguments.append(criteria.rental_rate_from)
        if criteria.rental_rate_to is not None:
            conditions.append("`rental_rate` <= %s")
            arguments.append(criteria.rental_rate_to)
        if criteria.length is not None:
            conditions.append("`length` = %s")
            arguments.append(criteria.length)
        if criteria.length_from is not None:
            conditions.append("`length` >= %s")
            arguments.append(criteria.length_from)
        if criteria.length_to is not None:
            conditions.append("`length` <= %s")
            arguments.append(criteria.length_to)
        if criteria.length_is_null is not None:
            conditions.append(
                "`length` IS NULL"
                if criteria.length_is_null
                else "`length` IS NOT NULL"
            )
        if criteria.replacement_cost is not None:
            conditions.append("`replacement_cost` = %s")
            arguments.append(criteria.replacement_cost)
        if criteria.replacement_cost_from is not None:
            conditions.append("`replacement_cost` >= %s")
            arguments.append(criteria.replacement_cost_from)
        if criteria.replacement_cost_to is not None:
            conditions.append("`replacement_cost` <= %s")
            arguments.append(criteria.replacement_cost_to)
        if criteria.rating is not None:
            conditions.append("`rating` = %s")
            arguments.append(criteria.rating)
        if criteria.rating_like is not None:
            conditions.append("`rating` LIKE %s")
            arguments.append(criteria.rating_like)
        if criteria.rating_is_null is not None:
            conditions.append(
                "`rating` IS NULL"
                if criteria.rating_is_null
                else "`rating` IS NOT NULL"
            )
        if criteria.special_features is not None:
            conditions.append("`special_features` = %s")
            arguments.append(criteria.special_features)
        if criteria.special_features_like is not None:
            conditions.append("`special_features` LIKE %s")
            arguments.append(criteria.special_features_like)
        if criteria.special_features_is_null is not None:
            conditions.append(
                "`special_features` IS NULL"
                if criteria.special_features_is_null
                else "`special_features` IS NOT NULL"
            )
        if criteria.last_update is not None:
            conditions.append("`last_update` = %s")
            arguments.append(criteria.last_update)
        if criteria.last_update_from is not None:
            conditions.append("`last_update` >= %s")
            arguments.append(criteria.last_update_from)
        if criteria.last_update_to is not None:
            conditions.append("`last_update` <= %s")
            arguments.append(criteria.last_update_to)

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: FilmPyMySQL,
    ) -> FilmRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
