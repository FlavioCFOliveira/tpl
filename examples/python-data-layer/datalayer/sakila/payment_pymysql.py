# Code generated by tpl from sakila.payment. DO NOT EDIT.

"""The PyMySQL implementation of the `sakila.payment` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .payment import Payment, PaymentFilter, PaymentKey, PaymentOrder
from .payment_repository import PaymentRepository

__all__ = ["PaymentPyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "sakila.payment"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `payment_id`, `customer_id`, `staff_id`, `rental_id`, `amount`, `payment_date`, `last_update` FROM `payment`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `payment_id`, `customer_id`, `staff_id`, `rental_id`, `amount`, `payment_date`, `last_update` FROM `payment` WHERE `payment_id` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `payment` (`customer_id`, `staff_id`, `rental_id`, `amount`, `payment_date`, `last_update`) VALUES (%s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `payment` SET `customer_id` = %s, `staff_id` = %s, `rental_id` = %s, `amount` = %s, `payment_date` = %s, `last_update` = %s WHERE `payment_id` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `payment` WHERE `payment_id` = %s"


def _row(values: tuple[Any, ...]) -> Payment:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return Payment(
        payment_id=values[0],
        customer_id=values[1],
        staff_id=values[2],
        rental_id=values[3],
        amount=values[4],
        payment_date=values[5],
        last_update=values[6],
    )


class PaymentPyMySQL:
    """The PyMySQL data layer of `sakila.payment`.

    It names no base class. ``PaymentRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `sakila.payment` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: Payment) -> None:
        """Insert `row`.

        The key is assigned by the server and is written back into `row`.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.customer_id,
                    row.staff_id,
                    row.rental_id,
                    row.amount,
                    row.payment_date,
                    row.last_update,
                ),
            )
            row.payment_id = cursor.lastrowid

    def read(self, key: PaymentKey) -> Payment:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.payment_id,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: Payment) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.customer_id,
                    row.staff_id,
                    row.rental_id,
                    row.amount,
                    row.payment_date,
                    row.last_update,
                    row.payment_id,
                ),
            )
        return affected

    def delete(self, key: PaymentKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.payment_id,
                ),
            )
        return affected

    def search(
        self,
        criteria: PaymentFilter,
        order: Sequence[PaymentOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[Payment]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.payment_id is not None:
            conditions.append("`payment_id` = %s")
            arguments.append(criteria.payment_id)
        if criteria.payment_id_from is not None:
            conditions.append("`payment_id` >= %s")
            arguments.append(criteria.payment_id_from)
        if criteria.payment_id_to is not None:
            conditions.append("`payment_id` <= %s")
            arguments.append(criteria.payment_id_to)
        if criteria.customer_id is not None:
            conditions.append("`customer_id` = %s")
            arguments.append(criteria.customer_id)
        if criteria.customer_id_from is not None:
            conditions.append("`customer_id` >= %s")
            arguments.append(criteria.customer_id_from)
        if criteria.customer_id_to is not None:
            conditions.append("`customer_id` <= %s")
            arguments.append(criteria.customer_id_to)
        if criteria.staff_id is not None:
            conditions.append("`staff_id` = %s")
            arguments.append(criteria.staff_id)
        if criteria.staff_id_from is not None:
            conditions.append("`staff_id` >= %s")
            arguments.append(criteria.staff_id_from)
        if criteria.staff_id_to is not None:
            conditions.append("`staff_id` <= %s")
            arguments.append(criteria.staff_id_to)
        if criteria.rental_id is not None:
            conditions.append("`rental_id` = %s")
            arguments.append(criteria.rental_id)
        if criteria.rental_id_from is not None:
            conditions.append("`rental_id` >= %s")
            arguments.append(criteria.rental_id_from)
        if criteria.rental_id_to is not None:
            conditions.append("`rental_id` <= %s")
            arguments.append(criteria.rental_id_to)
        if criteria.rental_id_is_null is not None:
            conditions.append(
                "`rental_id` IS NULL"
                if criteria.rental_id_is_null
                else "`rental_id` IS NOT NULL"
            )
        if criteria.amount is not None:
            conditions.append("`amount` = %s")
            arguments.append(criteria.amount)
        if criteria.amount_from is not None:
            conditions.append("`amount` >= %s")
            arguments.append(criteria.amount_from)
        if criteria.amount_to is not None:
            conditions.append("`amount` <= %s")
            arguments.append(criteria.amount_to)
        if criteria.payment_date is not None:
            conditions.append("`payment_date` = %s")
            arguments.append(criteria.payment_date)
        if criteria.payment_date_from is not None:
            conditions.append("`payment_date` >= %s")
            arguments.append(criteria.payment_date_from)
        if criteria.payment_date_to is not None:
            conditions.append("`payment_date` <= %s")
            arguments.append(criteria.payment_date_to)
        if criteria.last_update is not None:
            conditions.append("`last_update` = %s")
            arguments.append(criteria.last_update)
        if criteria.last_update_from is not None:
            conditions.append("`last_update` >= %s")
            arguments.append(criteria.last_update_from)
        if criteria.last_update_to is not None:
            conditions.append("`last_update` <= %s")
            arguments.append(criteria.last_update_to)
        if criteria.last_update_is_null is not None:
            conditions.append(
                "`last_update` IS NULL"
                if criteria.last_update_is_null
                else "`last_update` IS NOT NULL"
            )

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: PaymentPyMySQL,
    ) -> PaymentRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
