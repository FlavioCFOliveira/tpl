# Code generated by tpl from sakila.staff. DO NOT EDIT.

"""The `sakila.staff` row, the key that addresses it, the columns an ordering may
name, and the filter a search takes.
"""

from __future__ import annotations

import dataclasses
import datetime
import enum
from collections.abc import Mapping
from typing import Final

from ._core import Direction, UnknownColumn

__all__ = [
    "Staff",
    "StaffColumn",
    "StaffFilter",
    "StaffKey",
    "StaffOrder",
]


@dataclasses.dataclass
class Staff:
    """One row of `sakila.staff`.

    The dataclass is mutable, because :meth:`create` writes a server-assigned
    key back into it.
    """

    #: `staff_id`, declared `tinyint(3) unsigned` NOT NULL AUTO_INCREMENT.
    #:
    #: It is part of the primary key.
    staff_id: int
    #: `first_name`, declared `varchar(45)` NOT NULL.
    first_name: str
    #: `last_name`, declared `varchar(45)` NOT NULL.
    last_name: str
    #: `address_id`, declared `smallint(5) unsigned` NOT NULL.
    address_id: int
    #: `picture`, declared `blob` NULL.
    picture: bytes | None
    #: `email`, declared `varchar(50)` NULL.
    email: str | None
    #: `store_id`, declared `tinyint(3) unsigned` NOT NULL.
    store_id: int
    #: `active`, declared `tinyint(1)` NOT NULL.
    active: int
    #: `username`, declared `varchar(16)` NOT NULL.
    username: str
    #: `password`, declared `varchar(40)` NULL.
    password: str | None
    #: `last_update`, declared `timestamp` NOT NULL.
    last_update: datetime.datetime

    def key(self) -> StaffKey:
        """Return the primary key of the row."""
        return StaffKey(
            staff_id=self.staff_id,
        )


@dataclasses.dataclass(frozen=True)
class StaffKey:
    """The primary key of `sakila.staff`.

    Frozen, so that it hashes and compares by value. Every type this example's
    mapping produces is hashable, so no key of these three schemas is refused
    for that reason.
    """

    #: `staff_id`, declared `tinyint(3) unsigned` NOT NULL AUTO_INCREMENT.
    staff_id: int


class StaffColumn(enum.Enum):
    """One column of `sakila.staff`.

    A search accepts an ordering term over these members and over nothing else,
    so an ordering can carry no SQL of its own: :attr:`sql` answers from a
    closed mapping this template wrote, and :meth:`from_name` is the one door a
    name a caller supplied can come through.

    A member's value is the column's name as the catalogue carries it, which is
    what :meth:`from_name` matches on and what ``member.value`` returns.
    """

    #: `staff_id`, declared `tinyint(3) unsigned` NOT NULL AUTO_INCREMENT.
    STAFF_ID = "staff_id"
    #: `first_name`, declared `varchar(45)` NOT NULL.
    FIRST_NAME = "first_name"
    #: `last_name`, declared `varchar(45)` NOT NULL.
    LAST_NAME = "last_name"
    #: `address_id`, declared `smallint(5) unsigned` NOT NULL.
    ADDRESS_ID = "address_id"
    #: `picture`, declared `blob` NULL.
    PICTURE = "picture"
    #: `email`, declared `varchar(50)` NULL.
    EMAIL = "email"
    #: `store_id`, declared `tinyint(3) unsigned` NOT NULL.
    STORE_ID = "store_id"
    #: `active`, declared `tinyint(1)` NOT NULL.
    ACTIVE = "active"
    #: `username`, declared `varchar(16)` NOT NULL.
    USERNAME = "username"
    #: `password`, declared `varchar(40)` NULL.
    PASSWORD = "password"
    #: `last_update`, declared `timestamp` NOT NULL.
    LAST_UPDATE = "last_update"

    @property
    def sql(self) -> str:
        """The column as a statement's text names it: quoted for MariaDB."""
        return _COLUMNS[self]

    @classmethod
    def from_name(cls, name: str) -> StaffColumn:
        """Read a column a caller supplied as text.

        Raises:
            UnknownColumn: for every name the table does not carry. That
                refusal is what keeps a caller's text out of a statement.
        """
        try:
            return cls(name)
        except ValueError:
            raise UnknownColumn("sakila.staff", name) from None


#: The one place a column becomes SQL. It is closed over the enumeration above,
#: and every identifier in it was quoted at render time by `tpl`, so no value a
#: caller supplies can reach a statement's text through an ordering term.
_COLUMNS: Final[Mapping[StaffColumn, str]] = {
    StaffColumn.STAFF_ID: "`staff_id`",
    StaffColumn.FIRST_NAME: "`first_name`",
    StaffColumn.LAST_NAME: "`last_name`",
    StaffColumn.ADDRESS_ID: "`address_id`",
    StaffColumn.PICTURE: "`picture`",
    StaffColumn.EMAIL: "`email`",
    StaffColumn.STORE_ID: "`store_id`",
    StaffColumn.ACTIVE: "`active`",
    StaffColumn.USERNAME: "`username`",
    StaffColumn.PASSWORD: "`password`",
    StaffColumn.LAST_UPDATE: "`last_update`",
}


@dataclasses.dataclass(frozen=True)
class StaffOrder:
    """One ordering term of a search over `sakila.staff`."""

    #: The column to order by.
    column: StaffColumn
    #: The direction to order in.
    direction: Direction = Direction.ASCENDING


@dataclasses.dataclass
class StaffFilter:
    """What narrows a search over `sakila.staff`.

    Every field is optional: a field left ``None`` adds no condition, and the
    conditions a search does build are joined with ``AND``. The default
    instance is the filter that matches every row.

    A nullable column's field is ``None`` for *no condition* and never for
    *is NULL*; that question is asked through the ``…_is_null`` field such a
    column also gets, so the two meanings never collide.
    """

    #: Where set, requires `staff_id` to equal it.
    staff_id: int | None = None
    #: Where set, requires `staff_id` to be at least it.
    staff_id_from: int | None = None
    #: Where set, requires `staff_id` to be at most it.
    staff_id_to: int | None = None
    #: Where set, requires `first_name` to equal it.
    first_name: str | None = None
    #: Where set, requires `first_name` to match it, as LIKE matches.
    first_name_like: str | None = None
    #: Where set, requires `last_name` to equal it.
    last_name: str | None = None
    #: Where set, requires `last_name` to match it, as LIKE matches.
    last_name_like: str | None = None
    #: Where set, requires `address_id` to equal it.
    address_id: int | None = None
    #: Where set, requires `address_id` to be at least it.
    address_id_from: int | None = None
    #: Where set, requires `address_id` to be at most it.
    address_id_to: int | None = None
    #: Where set, requires `picture` to equal it.
    picture: bytes | None = None
    #: Where True, requires `picture` to be NULL; where False, requires it not to be.
    picture_is_null: bool | None = None
    #: Where set, requires `email` to equal it.
    email: str | None = None
    #: Where set, requires `email` to match it, as LIKE matches.
    email_like: str | None = None
    #: Where True, requires `email` to be NULL; where False, requires it not to be.
    email_is_null: bool | None = None
    #: Where set, requires `store_id` to equal it.
    store_id: int | None = None
    #: Where set, requires `store_id` to be at least it.
    store_id_from: int | None = None
    #: Where set, requires `store_id` to be at most it.
    store_id_to: int | None = None
    #: Where set, requires `active` to equal it.
    active: int | None = None
    #: Where set, requires `active` to be at least it.
    active_from: int | None = None
    #: Where set, requires `active` to be at most it.
    active_to: int | None = None
    #: Where set, requires `username` to equal it.
    username: str | None = None
    #: Where set, requires `username` to match it, as LIKE matches.
    username_like: str | None = None
    #: Where set, requires `password` to equal it.
    password: str | None = None
    #: Where set, requires `password` to match it, as LIKE matches.
    password_like: str | None = None
    #: Where True, requires `password` to be NULL; where False, requires it not to be.
    password_is_null: bool | None = None
    #: Where set, requires `last_update` to equal it.
    last_update: datetime.datetime | None = None
    #: Where set, requires `last_update` to be at least it.
    last_update_from: datetime.datetime | None = None
    #: Where set, requires `last_update` to be at most it.
    last_update_to: datetime.datetime | None = None

    def is_empty(self) -> bool:
        """Is no field set?

        A search over an empty filter builds no ``WHERE`` clause and matches
        every row of the table.
        """
        return (
            self.staff_id is None
            and self.staff_id_from is None
            and self.staff_id_to is None
            and self.first_name is None
            and self.first_name_like is None
            and self.last_name is None
            and self.last_name_like is None
            and self.address_id is None
            and self.address_id_from is None
            and self.address_id_to is None
            and self.picture is None
            and self.picture_is_null is None
            and self.email is None
            and self.email_like is None
            and self.email_is_null is None
            and self.store_id is None
            and self.store_id_from is None
            and self.store_id_to is None
            and self.active is None
            and self.active_from is None
            and self.active_to is None
            and self.username is None
            and self.username_like is None
            and self.password is None
            and self.password_like is None
            and self.password_is_null is None
            and self.last_update is None
            and self.last_update_from is None
            and self.last_update_to is None
        )
