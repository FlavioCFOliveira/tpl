# Code generated by tpl from sakila.address. DO NOT EDIT.

"""The PyMySQL implementation of the `sakila.address` data layer."""

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any, Final

from pymysql.connections import Connection
from pymysql.cursors import Cursor

from ._core import NotFound, translating
from .address import Address, AddressFilter, AddressKey, AddressOrder
from .address_repository import AddressRepository

__all__ = ["AddressPyMySQL"]

#: The table every statement of this module addresses, as an error names it.
TABLE: Final = "sakila.address"

#: Selects every column of the table, in catalogue order.
SELECT: Final = "SELECT `address_id`, `address`, `address2`, `district`, `city_id`, `postal_code`, `phone`, `last_update` FROM `address`"

#: Selects the one row a primary key names.
READ: Final = "SELECT `address_id`, `address`, `address2`, `district`, `city_id`, `postal_code`, `phone`, `last_update` FROM `address` WHERE `address_id` = %s"

#: Inserts one row. It names neither a generated column nor an auto-increment
#: key, because the server writes both.
INSERT: Final = "INSERT INTO `address` (`address`, `address2`, `district`, `city_id`, `postal_code`, `phone`, `last_update`) VALUES (%s, %s, %s, %s, %s, %s, %s)"

#: Writes every column that is neither generated nor part of the key.
UPDATE: Final = "UPDATE `address` SET `address` = %s, `address2` = %s, `district` = %s, `city_id` = %s, `postal_code` = %s, `phone` = %s, `last_update` = %s WHERE `address_id` = %s"

#: Removes the one row a primary key names.
DELETE: Final = "DELETE FROM `address` WHERE `address_id` = %s"


def _row(values: tuple[Any, ...]) -> Address:
    """Build a row from one result tuple.

    The positions are the order :data:`SELECT` names the columns in, which is
    catalogue order. The values are whatever PyMySQL decoded, and the types the
    dataclass declares are this example's record of what that is.
    """
    return Address(
        address_id=values[0],
        address=values[1],
        address2=values[2],
        district=values[3],
        city_id=values[4],
        postal_code=values[5],
        phone=values[6],
        last_update=values[7],
    )


class AddressPyMySQL:
    """The PyMySQL data layer of `sakila.address`.

    It names no base class. ``AddressRepository`` is a ``Protocol`` and is
    satisfied structurally, and the check at the foot of this module is where
    `mypy` proves that this class satisfies it.
    """

    def __init__(self, connection: Connection[Cursor]) -> None:
        """Build the data layer of `sakila.address` over `connection`.

        The connection must address the schema ``SCHEMA`` names, because the
        statements above address the table unqualified. ``connect`` is the
        connection that carries it.

        Nothing here commits. A connection opened by ``connect`` autocommits
        unless the caller said otherwise, so transaction control stays with the
        caller.
        """
        self._connection = connection

    def create(self, row: Address) -> None:
        """Insert `row`.

        The key is assigned by the server and is written back into `row`.

        Raises:
            DatabaseFailure: where the server refused the insert.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                INSERT,
                (
                    row.address,
                    row.address2,
                    row.district,
                    row.city_id,
                    row.postal_code,
                    row.phone,
                    row.last_update,
                ),
            )
            row.address_id = cursor.lastrowid

    def read(self, key: AddressKey) -> Address:
        """Return the row `key` names.

        Raises:
            NotFound: where the table carries no such row.
            DatabaseFailure: where the server refused the query.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute(
                READ,
                (
                    key.address_id,
                ),
            )
            found = cursor.fetchone()
        if found is None:
            raise NotFound(TABLE)
        return _row(found)

    def update(self, row: Address) -> int:
        """Write every writable column of `row` over the row its key names.

        Returns:
            The number of rows the server reported as changed.

        Raises:
            DatabaseFailure: where the server refused the update.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                UPDATE,
                (
                    row.address,
                    row.address2,
                    row.district,
                    row.city_id,
                    row.postal_code,
                    row.phone,
                    row.last_update,
                    row.address_id,
                ),
            )
        return affected

    def delete(self, key: AddressKey) -> int:
        """Remove the row `key` names.

        Returns:
            The number of rows the server reported as removed.

        Raises:
            DatabaseFailure: where the server refused the delete.
        """
        with translating(TABLE), self._connection.cursor() as cursor:
            affected = cursor.execute(
                DELETE,
                (
                    key.address_id,
                ),
            )
        return affected

    def search(
        self,
        criteria: AddressFilter,
        order: Sequence[AddressOrder] = (),
        limit: int | None = None,
        offset: int | None = None,
    ) -> list[Address]:
        """Return every row matching `criteria`, ordered by the terms of `order`.

        Raises:
            DatabaseFailure: where the server refused the query.
        """
        statement: list[str] = [SELECT]
        conditions: list[str] = []
        arguments: list[object] = []

        if criteria.address_id is not None:
            conditions.append("`address_id` = %s")
            arguments.append(criteria.address_id)
        if criteria.address_id_from is not None:
            conditions.append("`address_id` >= %s")
            arguments.append(criteria.address_id_from)
        if criteria.address_id_to is not None:
            conditions.append("`address_id` <= %s")
            arguments.append(criteria.address_id_to)
        if criteria.address is not None:
            conditions.append("`address` = %s")
            arguments.append(criteria.address)
        if criteria.address_like is not None:
            conditions.append("`address` LIKE %s")
            arguments.append(criteria.address_like)
        if criteria.address2 is not None:
            conditions.append("`address2` = %s")
            arguments.append(criteria.address2)
        if criteria.address2_like is not None:
            conditions.append("`address2` LIKE %s")
            arguments.append(criteria.address2_like)
        if criteria.address2_is_null is not None:
            conditions.append(
                "`address2` IS NULL"
                if criteria.address2_is_null
                else "`address2` IS NOT NULL"
            )
        if criteria.district is not None:
            conditions.append("`district` = %s")
            arguments.append(criteria.district)
        if criteria.district_like is not None:
            conditions.append("`district` LIKE %s")
            arguments.append(criteria.district_like)
        if criteria.city_id is not None:
            conditions.append("`city_id` = %s")
            arguments.append(criteria.city_id)
        if criteria.city_id_from is not None:
            conditions.append("`city_id` >= %s")
            arguments.append(criteria.city_id_from)
        if criteria.city_id_to is not None:
            conditions.append("`city_id` <= %s")
            arguments.append(criteria.city_id_to)
        if criteria.postal_code is not None:
            conditions.append("`postal_code` = %s")
            arguments.append(criteria.postal_code)
        if criteria.postal_code_like is not None:
            conditions.append("`postal_code` LIKE %s")
            arguments.append(criteria.postal_code_like)
        if criteria.postal_code_is_null is not None:
            conditions.append(
                "`postal_code` IS NULL"
                if criteria.postal_code_is_null
                else "`postal_code` IS NOT NULL"
            )
        if criteria.phone is not None:
            conditions.append("`phone` = %s")
            arguments.append(criteria.phone)
        if criteria.phone_like is not None:
            conditions.append("`phone` LIKE %s")
            arguments.append(criteria.phone_like)
        if criteria.last_update is not None:
            conditions.append("`last_update` = %s")
            arguments.append(criteria.last_update)
        if criteria.last_update_from is not None:
            conditions.append("`last_update` >= %s")
            arguments.append(criteria.last_update_from)
        if criteria.last_update_to is not None:
            conditions.append("`last_update` <= %s")
            arguments.append(criteria.last_update_to)

        if conditions:
            statement.append(" WHERE " + " AND ".join(conditions))
        if order:
            statement.append(
                " ORDER BY "
                + ", ".join(f"{term.column.sql} {term.direction.sql}" for term in order)
            )
        if limit is not None:
            statement.append(" LIMIT %s")
            arguments.append(limit)
            if offset is not None:
                statement.append(" OFFSET %s")
                arguments.append(offset)

        with translating(TABLE), self._connection.cursor() as cursor:
            cursor.execute("".join(statement), tuple(arguments))
            found = cursor.fetchall()
        return [_row(values) for values in found]


if TYPE_CHECKING:

    def _implements_the_protocol(
        implementation: AddressPyMySQL,
    ) -> AddressRepository:
        """Where `mypy` proves the implementation satisfies the contract.

        The return is the whole of the check: a method whose signature drifted
        from the protocol's fails the compile gate here, rather than a caller
        at run time. It costs nothing at run time because the block is never
        executed.
        """
        return implementation
